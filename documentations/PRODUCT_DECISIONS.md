# PRODUCT DECISIONS — Psihointegritet

Registar donetih odluka. **Samo doneto** — otvoreno stoji u `OPEN_DECISIONS.md`.

**Gde piše:** master plan §0 („If documents conflict, record the conflict here and ask the CTO") · §4 R0.1 · §13
**Pravilo:** odluka ulazi ovde tek kad ima **vlasnika i datum**. Bez toga je nacrt, ne odluka.
**Vlasnici:** Milan Dražić (CTO — tehničke, arhitektonske, proizvodne) · Anja Stamenković (vlasnica — sadržaj, zvanja, cene, brend) · pravnik (pravni tekstovi)

---

| #         | Datum          | Odluka                                                                                                                                                                                                                                                                                                                                   | Vlasnik                                 | Razlog                                                                                                                                                                                                                                                                   | Uticaj                                                                                                                                                                                                                                                                                                                                   |
| --------- | -------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| D-001     | 2026-07-17     | **Jezik javnog sadržaja: ekavica, srpski, latinica.** Ispravlja T9 koji je tvrdio „ijekavica — the owner's authored style"                                                                                                                                                                                                               | Milan (CTO)                             | Proposal v1.1 §3 — dokument koji je Anja dobila — piše „bračno savetovanje" i „psihoterapijsko savetovanje" (ekavica). Engines dokument isto (27 ekavskih : 4 ijekavska). Master plan je bio jedini ijekavski (13:2) i odudarao od dokumenta na koji se klijent obavezao | T1 → „Bračno savetovanje", T2 → „Psihoterapijsko savetovanje", T9 → ekavica. Sweep koda = R0.2.c (~15 fajlova; `homepage.ts` ima 18 ijekavskih linija). ⚠️ `homepage.ts` je Anjin autorski tekst — **obavestiti je pre objave**                                                                                                          |
| D-002     | 2026-07-17     | **Četiri obavezujuća dokumenta:** master plan v1.0, Proposal v1.1, IZMENE v1.0, ARCHITECTURAL_RULES_REVISED. Ostalo je kontekst ili arhiva                                                                                                                                                                                               | Milan (CTO)                             | 11 dokumenata se takmičilo za autoritet; tri konkurentne mape puta (M0–M6, R0–R7, Faze 1–9) radile su jedna protiv druge                                                                                                                                                 | 6 dokumenata arhivirano sa SUPERSEDED zaglavljem. `PRODUCT_CONTEXT.md` (bio autoritet #2) arhiviran — zamenjuju ga Proposal §3 + MP §1                                                                                                                                                                                                   |
| D-003     | 2026-07-17     | **`documentations/` vraćen u git.** `.md` se prati; `.docx`/`.pdf` ostaju van                                                                                                                                                                                                                                                            | Milan (CTO)                             | Commit `6452c70` je gitignorovao folder i obrisao 4.059 linija dokumentacije iz verzionisanja                                                                                                                                                                            | Dokumentacija je ponovo u PR review-u i deljiva                                                                                                                                                                                                                                                                                          |
| D-004     | 2026-07-17     | **v0.3 arhitektura ostaje aktivna**, podređena master planu                                                                                                                                                                                                                                                                              | Milan (CTO)                             | Širi model podataka (§6), granice modula (§4.2) i ADR lista (§16)                                                                                                                                                                                                        | Anotirana zaglavljem: guidance persistence ne ulazi u R1 (T13); R1.6 Booking ugovor iz D-035…D-037 / ADR-013 pobeđuje nad ranijim jednim appointment status modelom.                                                                                                                                                                     |
| D-005     | 2026-07-17     | **Engines dokument u repo** kao autoritet #6 (vodič, ne propis)                                                                                                                                                                                                                                                                          | Milan (CTO)                             | MP §0 ga navodi kao model granica i dijagnostičkog konteksta                                                                                                                                                                                                             | Kopiran; R1.6 Booking ugovor iz D-035…D-037/ADR-013 sada je referenca za §7.3 i event semantiku, dok Engines ostaje vodič za module i dijagnostiku.                                                                                                                                                                                      |
| **D-006** | **2026-07-17** | **S2 REŠEN — „Marjan Janković"** (ne „Marijan")                                                                                                                                                                                                                                                                                          | Milan (CTO), potvrda Anje               | Konačno pisanje imena                                                                                                                                                                                                                                                    | Slug: **`marjan-jankovic`**. Odblokira `/tim/[slug]` (R1.1). ⚠️ Tekst o lokacijama (Proposal §4 #2) i dalje otvoren — vidi O-02                                                                                                                                                                                                          |
| **D-007** | **2026-07-17** | **S10 spušten sa blokade launcha na parcijalno.** Footer: pravni linkovi (privatnost, uslovi, kolačići, pravila zakazivanja) + brzi linkovi (tim, radionice, rad sa kompanijama). **Logo: postojeći**                                                                                                                                    | Milan (CTO)                             | Struktura footera se zna, detalji stižu naknadno                                                                                                                                                                                                                         | Odblokira strukturu footera. ⚠️ Email/telefon/mreže i dalje nedostaju → `/kontakt`, Resend (`info@`, `termini@`) i JSON-LD **i dalje blokirani** — vidi O-06                                                                                                                                                                             |
| **D-008** | **2026-07-17** | **S5 pravni pravac (nacrt, NIJE pravna potvrda).** Platforma Psihointegritet nije odgovorna za pojedinačne terapeute; terapeuti preuzimaju odgovornost kao samostalni preduzetnici registrovani u APR. Stranice: privatnost, uslovi, kolačići, pravila zakazivanja. Tekstovi informativni — kako doći do terapeuta i koje programe imaju | Milan (CTO) — **pravnik nije potvrdio** | Definiše model odgovornosti                                                                                                                                                                                                                                              | ⚠️ **S5 NIJE zatvoren.** Stranice ostaju „u pripremi — pravna potvrda". Tri otvorene stavke: (1) APR registracija se mora **proveriti po terapeutu** — tvrdnja u pravnom tekstu mora biti tačna; (2) **krizni disclaimer (T11) nedostaje** u ovom pravcu; (3) da li je ovakva klauzula o odgovornosti izvršiva traži pravnika. Vidi O-03 |

| **D-009** | **2026-07-17** | **S13 REŠEN — autentične fotografije terapeuta** ugrađene; 3 stock portreta obrisana iz repoa | Milan (CTO), materijal od Anje | Prave fotografije stigle u dizajn handoff-u (`assets/{anja,marija,marjan}.jpeg`) | `public/images/therapists/`. Uklonjen rizik iz Proposal §6 („Nema tuđih (stock) portreta pod imenima terapeuta") |
| **D-010** | **2026-07-17** | **Dizajn handoff je autoritet #7 — najniži.** Layout se preuzima 1:1, **copy se ispravlja** po T1/T2/T7/T8/T9/T11 | Milan (CTO) | Handoff `data/therapists.js` je kršio: T1 („Partnersko savjetovanje" kod Anje, „Partnerska terapija" kod Marjana — dva imena za jednu uslugu), T2 („Psihološko savjetovanje" kod sve troje), T3 („pod supervizijom" kod sve troje), T9 (ijekavica), T8 („uživo u Nišu" na svakom profilu iako su Marija i Marjan u Leskovcu), MP §11 (poverljivost „Potpuna" — neistinita tvrdnja) | Sve ispravke u `content/therapists.ts`. Handoff premešten u `frontend/design-handoff/` i gitignorovan (14 MB) |
| **D-011** | **2026-07-17** | **Ruta je `/tim/[slug]` sa punim imenima** (`anja-stamenkovic`), ne `/tim/[id]` sa skraćenicama (`anja`) | Milan (CTO) — „ili kako smo definisali" | MP §5 R1.1 imenuje slugove eksplicitno; puna imena su bolja za SEO | `generateStaticParams` daje 3 statičke rute; kviz targetira po slugu |
| **D-012** | **2026-07-17** | **Intake & Matching Engine v1** zamenjuje stari kviz: 5 pitanja (za koga/uzrast/oblast/format/prioritet), tvrdi filteri (uzrast, grad), objašnjiv rezultat bez skora, grane B2B/tim/maloletnici. Hardcoded na frontendu, bez perzistencije | Milan (CTO) | Stari kviz je bio generički; spec traži tvrde filtere po uzrastu i objašnjive razloge. **Krizni oporavak se nikad ne dodeljuje automatski** (ni Marjanu iz biografije) — ide na tim-pregled | `matching.ts` + 13 testova; ulazi: „Zakaži termin" → chooser, hero/sekcija → kviz. Booking forma i slanje ostaju R1.3/R2 |
| **D-013** | **2026-07-17** | **Minimalna `/rad-sa-kompanijama` stranica** — showcase, bez forme | Milan (CTO) | B2B grana Matching Engine-a mora negde da vodi; hero kartica je bila 404 (D2) | Ponuda + 3 koraka + CTA na `/#kontakt`; forma sa R1.3. Rešava D2 i D3 |
| **D-014** | **2026-07-17** | **Marjanova najniža granica = 18** u v1 matrici (ne nudi se maloletnicima) | Milan (CTO) — privremeno | Spec traži potvrdu tima za adolescente kod Marjana; do tada konzervativno | Maloletnici (13–17) idu **samo Mariji** + napomena o roditelju/staratelju (T10). Menja se kad tim potvrdi (O-14) |
| **D-015** | **2026-07-17** | **Domen `psihointegritet.com` kupljen.** `qa.psihointegritet.com` (grana `features`) i `staging.psihointegritet.com` (grana `staging`) kao **odvojeni Railway backend servisi** — ne dele CORS allowlist. Resend domen **verifikovan** (SPF/DKIM/DMARC). Postojeći Railway `production` servis (dosad `psihointegritet.vercel.app`, sa `CORS_ORIGINS=["http://localhost:3007"]` koji je „radio" samo zato što frontend još nema nijedan browser→backend poziv) prelazi na custom domen | Milan (CTO) | R1.5.a/b/d (MP §5 R1.5); odvojeni servisi po MP §14 („Staging i production imaju odvojene baze, secrets i Clerk aplikacije") — isti princip proširen i na qa | **Konačne vrednosti** (JSON niz, `core/config.py` + `main.py:47-53`), zamena a ne dodavanje uz localhost: `production` → `["https://psihointegritet.com","https://www.psihointegritet.com"]` (+ `https://psihointegritet.vercel.app` **samo** ako taj URL još nije redirektovan na custom domen — R1.5.a „www redirect"); `qa` → `["https://qa.psihointegritet.com"]`; `staging` → `["https://staging.psihointegritet.com"]`. Frontend `NEXT_PUBLIC_APP_URL`/`NEXT_PUBLIC_API_URL` (Vercel env po environment-u) moraju se poklapati — utiču na `metadataBase` i `canonical` URL-ove dodate u R1.1 (`/tim`, `/tim/[slug]`, `/rad-sa-kompanijama`). R1.5.c (`info@`/`termini@` adrese) i dalje čeka S10 — domen verifikacija ≠ potvrđene adrese |
| **D-016** | **2026-07-18** | **VAPID ostaje van obima** (potvrđeno — QStash pokriva podsetnike, T14-stil „ne treba još"). **Email adrese kreirane**: po jedna za svakog terapeuta + `info@psihointegritet.com`; Resend šalje sistemske/verifikacione mejlove sa `noreply@`. `RESEND_API_KEY` postavljen u `frontend/.env.local`. Planiran zajednički `EmailService` + email wrapper za isti dizajn svih mejlova | Milan (CTO) | Notifikacije ostaju email-only (MP §6.1 `channel ENUM(email)`); adrese su preduslov za R1.3.i / S10 | Adrese **nisu** upisane ni u kod ni u `.env.example` — samo kreirane kod provajdera. `RESEND_API_KEY` **nema kopiju u backend env-u**; stvarno slanje (R1.3) mora ići iz FastAPI (rules §1.1), ne iz Next.js, pa će ključ trebati i tamo. `EmailService`/wrapper čeka odluku o lokaciji templejta (react-email FE vs Jinja BE — već otvoreno u MP §6.1, „decide by ADR") pre nego što se piše kod. Ništa od ovoga se ne implementira sada — samo zapisano za R1.3 |
| **D-017** | **2026-07-18** | **Ispravka D-001: Anjina ijekavica nije greška, ličan je govor.** Anja Stamenković je poreklom iz Prijedora i **lično govori i piše isključivo ijekavicom**. Marija Stamenković i Marjan Janković govore i pišu **isključivo ekavicom**. Sajt ostaje ekavica kao podrazumevani jezik (T9, D-001) — **osim** Anjinih ličnih citata i biografije, koji ostaju ijekavica | Milan (CTO) — potvrđena činjenica, ne stilska odluka | D-001 je pogrešno pretpostavio da je ijekavica u Anjinom tekstu artefakt prevoda telefona i vratio je sve na ekavicu. To je bila greška — dijalekat je namerni, lični izbor, ne tipfeler | `content/therapists.ts` — Anjin `quote`, `bio` (svih 5 pasusa) i `cardExcerpt` vraćeni na tačan izvorni ijekavski tekst iz dizajn handoff-a (verbatim, bez parafraziranja). Njeni `title`, `areas`, `formats`, `services` (nazivi usluga po T1/T2) ostaju nepromenjeni — to je terminologija, ne lični govor. Master plan T9 dopunjen ovim izuzetkom. **R0.2 sweep (D17 u TODO.md) i dalje važi za ostatak sajta** (homepage.ts, hero/footer/workshop/faq/resources komponente) — samo se Anjin lični sadržaj izuzima iz tog sweep-a jer je već ispravan |
| **D-018** | **2026-07-18** | **R0.2 sweep izvršen — D17 zatvoren.** Ceo sajt (osim Anjinog ličnog sadržaja) prebačen na ekavicu | Milan (CTO) | Nastavak D-017 — pravilo se sad primenilo na ostatak sajta, ne samo na Anjine profile | 8 fajlova izmenjeno: `content/homepage.ts` (uklj. zastareli komentar „Ijekavica is intentional") + `hero.tsx`, `site-footer.tsx`, `workshop.tsx`, `faq.tsx`, `resources.tsx`, `support-paths.tsx`, `first-session.tsx`, `app/layout.tsx` (meta opis). Usput ispravljen i T1/T2 u `midServices` („Partnersko/Psihološko savjetovanje" → „Bračno/Psihoterapijsko savetovanje") jer su iste linije — izbegnuto duplo editovanje. Grep preko celog `src/` potvrđuje 0 preostalih ijekavskih reči van `content/therapists.ts`. Ažurirano 5 e2e test asertacija (`home.spec.ts`) na nove stringove. Svi gate-ovi (typecheck/lint/format/test/build/23 e2e) zeleni |
| **D-019** | **2026-07-18** | **`PageHero` — jedinstven obrazac hero sekcije za sve javne stranice.** Zaobljen blok (`rounded-3xl md:rounded-[32px]`, isti radijus kao Hero slika na početnoj), pozadina `bg-surface` (belo) po default-u, bez odvojene sekcije koja se preklapa negativnom marginom — sadržaj (H1, paragraf, CTA) ide direktno u blok, sa `padding-top` koji čisti Header | Milan (CTO) | Header je `absolute` i pluta preko sadržaja na početnoj (Hero slika mu daje dovoljno prostora); na `/tim`, `/tim/[slug]`, `/rad-sa-kompanijama` sadržaj je ranije stajao na običnoj `bg-canvas` pozadini bez ijednog vizuelnog bloka — stranica je delovala odsečeno od Header-a, ne kao jedna celina | Novo: `components/shared/page-hero.tsx`. Izmereno u browseru (Playwright): Header visina **84px mobilni / 94px desktop**. `PageHero` koristi `pt-6` na spoljnoj sekciji + unutrašnji `pt-[96px] md:pt-[152px]` na bloku → sadržaj počinje na 120px/176px od vrha ekrana, isto kao stare `pt-[120px] md:pt-44` vrednosti koje su već radile, samo sad unutar zaobljenog bloka umesto na ravnoj pozadini. Primenjeno na `team-intro-section.tsx`, `therapist-hero-section.tsx`, `companies-page.tsx` (prva sekcija). `(public)/layout.tsx` već je ranije obmotavao `{children}` sa `SiteHeader`/`SiteFooter` — nije menjan. Vizuelno potvrđeno na sve tri stranice, desktop i mobilni. Gates + 23 e2e zeleni |
| **D-020** | **2026-07-18** | **`PageHero` tonovi po stranici, da se razbije monotonija.** `/tim` → `bg-surface` (belo, default) · `/tim/[slug]` → `bg-meadow/28` · `/rad-sa-kompanijama` → `bg-warm/20` + `text-coffee` | Milan (CTO) | Sve tri stranice su prvo dobile isti beli blok (D-019) — dovoljno za strukturni obrazac, ali monotono kad se gleda tim+profil+B2B redom | `page-hero.tsx` tone vrednosti usklađene na tačne opacitete (`meadow` /32→/28, `warm` puno→/20). `companies-page.tsx`: `Eyebrow tone="coffee"` i H1 `text-forest`→`text-coffee`, po istom obrascu koji već koristi `workshop.tsx` na `bg-warm` pozadini — ne izmišljena kombinacija, ponovljena postojeća. `therapist-hero-section.tsx` dobija `tone="meadow"` bez izmene teksta (postojeći `text-forest`/`text-coffee` ostaju, isto kao meadow kartica u `support-paths.tsx`). Vizuelno potvrđeno. Gates + 23 e2e zeleni |
| **D-021** | **2026-07-18** | **MVP Demo paket i redosled.** Za Anjin sastanak pravimo hardcoded, throwaway demo koji pokazuje **ceo MVP tok**: sajt → sadržaj → matching → zakazivanje → anketa → izbor terapeuta → panel „Preuzmi". Redosled: **Anketa → test → B2B konfigurator → demo booking-flow → (zatim R2 role/paneli)**. Sve iza feature flag-a `intake_matching_preview` (spec §3) — samo staging, mock adapteri, bez pravih upisa | Milan (CTO) | Anja i tim treba da odobre flow i **koje se podatke prosleđuje** pre nego što gradimo pravi backend (booking, role, paneli). Demo de-riskuje R1.3/R2. Novi spec: `PSIHOINTEGRITET_INTAKE_MATCHING_ENGINE_v0.1.md` | **Odstupanje od master plana §3** („ne scaffolduj R3–R7") — demo pomera **R4 (B2B) i R6 (anketa)** unapred. Dozvoljeno jer je throwaway frontend iza flag-a, **ne** scaffolding pravih modula: pravi B2B je R4, prava anketa/marketing je R6, pravi panel+role su R2 (M2.1+M2.4), pravi Booking Engine je M2.3. Demo panel je mock/in-memory/bez auth-a. Detalji u `TODO.md` §5A |
| **D-022** | **2026-07-18** | **Research Drawer (anketa) — zaseban modul, ne deo Matching-a** (spec §17). Isti drawer UI/UX, **drugi `surveyId`**, `?survey=online-experience` query param auto-otvara. Floating „?" se **preusmerava** sa matching-a na anketu (tooltip „Podelite mišljenje"); matching se otvara iz „Zakaži termin"/hero/„Vođeni izbor". Pitanja: online vs uživo, poverenje u ovakve platforme, da li su već koristili usluge, šta ih koči — anonimno, bez zdravstvenih podataka, opcioni tekst | Milan (CTO) | Anja želi stvarno prikupljanje mišljenja (da li je ideja dobra, da li je lakše online ili uživo) — odvojeno od zakazivanja | „Pošalji" šalje **pravi mejl na `info@psihointegritet.com`** (D-024). „Možda kasnije" zatvara i resetuje; korisnik može nazad/ispraviti. Kasnije prerasta u R6 (statistika, kampanje, `/istrazivanje`) |
| **D-023** | **2026-07-18** | **B2B konfigurator „Program podrške zaposlenima" — demo.** Otvara se iz Matching-a kad se izabere „Rad sa kompanijama". 7 hardcoded koraka (potrebe, veličina tima, model korišćenja, finansiranje, period, realizacija, kontakt) → hardcoded preporuka paketa + **okvirne demo cene** → „Pošalji upit" (mejl Anji + auto-odgovor kompaniji) | Milan (CTO) | Pokazuje vrednost B2B ponude pre backend-a; Anja dobija strukturisan upit bez zdravstvenih podataka zaposlenih | **Cene su radne** (polazna 5.000 RSD/termin) — Anja potvrđuje osnovnu cenu, poreski tretman, otkazivanje, cenu radionica (S-lista). Ne zvati „EAP" — „Program podrške zaposlenima". Pravi B2B modul (kompanijski kodovi, krediti, rezervisani kapacitet) je **R4** |
| **D-024** | **2026-07-18** | **Slanje mejla za demo: anketa i B2B šalju pravi mejl na `info@psihointegritet.com`** (adresa potvrđena, D-016). Mehanizam: **Next.js Route Handler → Resend** (bez baze, bez perzistencije) | Milan (CTO) | Anja traži stvarno prikupljanje podataka, ne samo vizuelni mock; matching/booking deo demoa ostaje mock (spec §3), ali anketa/B2B upit **jesu** izuzetak jer su prava svrha | Blago savija rules §1.1 (business logika u FastAPI) — opravdano jer je notifikacija bez perzistencije. **Migrira na backend** `notifications`/R1.3 kad se napravi. `RESEND_API_KEY` je već u `frontend/.env.local` (D-016) |
| **D-025** | **2026-07-19** | **Matching v2 + novi cenovnik + B2B rework po Anjinim odgovorima** (`documentations/odgovor-za-matching-anketa.pdf`). Upitnik: 5 pitanja + 2 uslovna (uzrast deteta kod „Roditelj i dete"; lokacija kod „Uživo"). Bodovanje: centralizovane težine u `matching.ts` (`matchingWeights`), čista `evaluateIntake()` — top ≥3 poena ispred → jedna primarna preporuka (+ „Pogledajte i drugu preporuku"), inače dve; **nikad „nema terapeuta"**; maks. 3 razloga običnim jezikom, bez skorova. Usluga se preporučuje odvojeno (Partner→Bračno; Roditelj i dete/Roditeljstvo/Adolescent→Roditeljsko; ostalo→Individualna). **Cene supersede T7**: Individualna 4.000/60min, Bračno 5.500/90min, Roditeljsko savetovanje 5.000/60min (novo u katalogu; „Psihoterapijsko savetovanje" uklonjeno kao zasebna cenovna stavka). Paketi: 5 seansi/3 mes./15.000 (⚠️ interna TODO — odstupa od 5×4.000) i 10/5 mes./38.000. 7 grupnih programa; samo „Tridesete — Vreme promene" ima cenu (3.500/susret, 25.000 program), ostali „Cena će biti objavljena naknadno." B2B: nova 4 pitanja (zaposleni/ciljevi/teme/format) → 6 modela, **sve „Cena po ponudi"** (stare demo cene iz D-023 povučene); >200 zaposlenih ili ≥3 cilja → „Program po meri" uz obavezan kontakt. Booking mejl sadrži strukturisan sažetak upitnika (odgovori, usluga, preporuka, razlozi) — **bez internih skorova** | Milan (CTO), sadržaj Anja | Anja odgovorila na matching anketu — prva stvarna specifikacija pitanja, težina i cenovnika od vlasnice sadržaja | `matching.ts` (v2, sve težine), `services.ts` (formatRsd + paketi + grupni programi), `therapists.ts`/`homepage.ts` (cene), `company.ts` (novi konfigurator), `guidance-drawer.tsx` (intro + uslovni koraci + novi rezultati), `booking-request` ruta (sažetak, 4.000). Otvoreno za Anju: „Bračno savetovanje" ekavica vs „savjetovanje" u njenom dokumentu; cena paketa 15.000; napomena „roditelji po ceni jednog" (Tridesete); burnout Marjan +2 (uslov nije merljiv u upitniku); uzrasne granice po terapeutu; `areas` čipovi na profilima nisu menjani |
| **D-026** | **2026-07-20** | **Superadmin Control Center v1 + role (Pre-R2 Control Center Preview; nije formalni R2).** Superadmin pristup ima **samo Milan** (`milan.drazic@dmdevelon.website`; privremeno i postojeći dev login `drazic.milan@gmail.com` dok se dmdevelon nalog ne kreira). Sva tri člana tima (Anja, Marija, Marjan) = **`org_admin` + `therapist`** (obe role). **Privremeni izvor rola: Clerk `publicMetadata`**, čitano server-side (`auth()` + `currentUser()`) — ⚠️ svesna, dokumentovana devijacija od ARCHITECTURAL_RULES §10.2/§10.3 (autorizacija pripada internom Postgres-u): metadata nosi **samo role, nikad domenske podatke**, a ceo kod zavisi od šava `getServerIdentity()` (`identity-server.ts`) — zamena za backend `GET /me` (M2.1) menja jednu liniju. **Svaka `/superadmin/*` stranica poziva `requireSuperadmin()` zasebno** (layout se ne re-izvršava na soft navigaciji; sakrivanje linkova nikad nije autorizacija); budući endpoint-i koriste `requireSuperadminApi()` (ne-superadmin → 404). `/radni-prostor` čuva `requireStaff()` — **D7 bug zatvoren** za obe rute. Panel: 4 prava taba (Pregled, Tenanti + profil, Feature Gates, Dijagnostika) + 3 „U pripremi" rute (billing/audit-log/settings); hardkodovan Psihointegritet tenant; health statičan; **Feature Gates demo toggle in-memory** (ConfirmModal + obavezan razlog → upis u „Poslednja aktivnost" feed; refresh resetuje — pravi Audit Log je R6) | Milan (CTO) | Faza treba da potvrdi rute, server-side autorizaciju i multi-tenant strukturu pre nego što se gradi poslovna logika (koja stiže poslednja). Dizajn 1:1 iz `design_handoff_paneli/` | Plan faze: `SUPERADMIN_CONTROL_CENTER_PLAN_v1_0.md` · TODO §5B (SA-1…SA-6). Novi auth sloj `lib/auth/{guards,identity-server}.ts` + `clerk/{public-metadata,server-identity}.ts`; `(superadmin)` ruta grupa sa posebnim layoutom; panel tokeni u `@theme`; `components/panel/*` primitive (koristiće ih i Control Center/Klijent panel); `scripts/set-clerk-roles.mjs` (`npm run roles:assign`). Otvoreno: O-17 (timski Clerk nalozi ne postoje), O-18 (dodela rola na produkcionoj instanci pri launchu) |
| **D-027** | **2026-07-20** | **Control Center (radni-prostor) v1 — role-based staff panel (Pre-R2 Control Center Preview; nije formalni R2).** Operativni panel za tim na `/radni-prostor`, isti pristup kao superadmin: **funkcionalan, pretežno read-only demo, mock podaci**, da tim vidi panel i da smernice. **Tabovi/kontrole po roli, server-side:** `org_admin` vidi poslovne tabove (Kompanije/Usluge/Istraživanja/Terapeuti/Podešavanja), `therapist` vidi Pregled/Termini/Klijenti/Moj profil, ko ima **obe role vidi uniju**; superadmin tretiran kao oba. **Bez Vlasnik/Terapeut prekidača** iz dizajna (to je bio demo) — nav se izvodi iz stvarnih rola (`visibleNav`), vlasnik filtrira „samo moje" preko izbora terapeuta u topbaru. Novi guardovi `requireOrgAdmin()`/`requireTherapist()` (+ `isWorkspaceAdmin/Therapist`) uz postojeći `requireStaff()`; svaka stranica poziva svoj guard (nav je samo prikaz). **Obim v1:** svi list/pregled ekrani + Moj profil (Javni profil / Matching preferencije / Dostupnost 4 sloja). **Odloženo:** duboki profili klijenta (Intake sažetak, Saglasnosti) i kompanije (Kapacitet), pravi termini (Booking engine M2.3). Mock podaci usklađeni sa našim katalogom (D-025 imena/cene), terapeuti iz `content/therapists.ts`. Privacy (MP §11): bez anamneze/kliničkih beleški; Istraživanja anonimna | Milan (CTO) | Tim treba da vidi radni interfejs i da konkretne smernice šta im olakšava poslovanje pre nego što se gradi Booking engine i pravi backend | Plan faze: `CONTROL_CENTER_PLAN_v1_0.md` · TODO §5C (CC-1…CC-6). Novo: `features/workspace/*` (context, nav, shell, 9 ekrana, data/types), `(staff)/radni-prostor/{layout,9 stranica}`, guardovi + testovi, `components/panel/progress-bar`. Verifikacija: 82 unit, 61 e2e (+10 workspace-auth), build/tsc/lint zeleni; vizuelno potvrđeno desktop+mobilni. Ručni smoke: superadmin nalog otvara `/radni-prostor` u punom pogledu |
| **D-028** | **2026-07-21** | **R1.4.i je `Content Governance & Discoverability Foundation`.** Sadrži samo ugovor sadržaja, statički provider, validatore/gate-ove, discoverability ugovore i CI Content Health. R1.5 ostaje Production Launch Gate; `Pre-R2 Booking Engine Decision Specification` je dokumentacioni gate (R1.6 samo interno), R2 ostaje Booking Engine, R3 je Content Engine / CMS, R5 je finansijski domen. | Milan (CTO) | Potrebno je razdvojiti pripremni sadržajni sloj od CMS aplikacije, preview panela i produkcionog Bookinga. | Master plan dobija R1.4.i bez promene R1.5. Nema CMS baze, FastAPI CMS CRUD-a, editora, naloga reviewera, audita, zakazane objave, booking ili payment koda u ovom slice-u. |
| **D-029** | **2026-07-21** | **Publication lifecycle ima pet stanja:** `draft → in_review → approved → published → archived`, uz dozvoljene povratke `in_review → draft`, `approved → draft` i `archived → draft`. Svaka izmena nakon odobrenja poništava odobrenje nove revizije. | Milan (CTO) | Objavljivanje, dostupnost, booking i indeksiranje nisu ista stvar. | R1.4.i uvodi statički status/approval ugovor i validatore; R3 kasnije dodaje stvarne revizije, audit i zakazanu objavu. |
| **D-030** | **2026-07-21** | **Request-first `BookingMode`:** `request | slot_request | disabled`; `live` je zabranjen. U oba aktivna režima korisnik kreira `AppointmentRequest`, a ne potvrđen termin. | Milan (CTO) | Izbor slota ne sme korisniku obećati rezervaciju ili automatsku potvrdu. | CTA/publish ugovori i Pre-R2 specifikacija koriste samo ova tri moda. R2 potvrđuje termin tek posle pregleda terapeuta ili alternativnog predloga. |
| **D-031** | **2026-07-21** | **Granica R2/R5:** R2 pokriva dostupnost, slotove, request-first tok, potvrđene termine, alternative, pomeranje/otkazivanje bez finansijskog obračuna, čekanje, osnovna obaveštenja, opcioni Google Calendar i operativni audit. | Milan (CTO) | Booking treba da bude nezavisan od neodobrenih finansijskih odluka. | Kupovina paketa, krediti/ledger, pretplate, kartice, B2B naplata, refundacije, fakture, kuponi i poravnanje su `BLOCKED` za R5: bez R2 tabela, migracija, endpointa, UI-ja ili provider integracija. `PackageOffer`/`CompanyPlan` su samo javni sadržaj. |
| **D-032** | **2026-07-21** | **Content Health u R1.4.i je statička CI provera.** Čisti read-only evaluatori daju `info | warning | error`; `error` ruši gate, a rezultat ide u terminal i `content-health-report.json`. | Milan (CTO) | Kvalitet sadržaja i discoverability moraju biti proverljivi pre CMS-a. | Nema Control Center taba, baze nalaza, istorije, repair akcija, notifikacija ni Diagnostic Engine dashboarda. R2 može kasnije prikazati poslednji nalaz; pun engine je kasniji scope. |
| **D-033** | **2026-07-21** | **Approval capabilities nisu auth role:** `clinical | legal | business`. Content navodi potrebne capability-je i statičke `ApprovalEvidence` zapise, bez identitetskog ili audit dokaza. | Milan (CTO) | Publish gate mora znati koje odobrenje nedostaje, bez preuranjenog CMS/auth sistema. | R3 mapira capability-je na stvarne naloge, dozvole, workflow i audit log. Clerk/FastAPI role ne dobijaju ovu semantiku u R1.4.i. |
| **D-034** | **2026-07-22** | **BDS-007 je razdvojen:** BDS-007A je APPROVED atomski, kratki internal contention hold samo za `slot_request`; BDS-007B ostaje OPEN za dostupnost slota nakon uspešnog zahteva. | Milan (CTO) | Tehnička zaštita od paralelnog slanja nije isto što i poslovna politika da li `under_review` blokira javni slot. | Hold nije Appointment, potvrda ni rezervacija; TTL ostaje konfiguraciona R2 vrednost, PostgreSQL je autoritet, retry je idempotentan. BDS-007B čeka SLA/reminder/escalation/reopen/Notify Me/prioritet odluke. |
| **D-035** | **2026-07-22** | **BookingMode se rešava na nivou konkretne ponude:** `Service + Therapist + Format + Location`. Override može samo da suzi režim `slot_request -> request -> disabled`; nepotpuna kombinacija je `disabled`, osim eksplicitno odobrenog team-review toka. | Milan (CTO) | Ista usluga može bezbedno imati različit flow po terapeutu, formatu ili lokaciji. Preference iz Intake-a ne smeju samostalno otvoriti slot ili proširiti capability. | BDS-001 je `APPROVED`; R2 resolver čuva najrestriktivniji mode. Početna matrica usluga ostaje poslovni predlog za Anju. |
| **D-036** | **2026-07-22** | **Appointment nastaje samo potvrdom terapeuta.** Izabrani slot ili željeni period može stvoriti `SlotHold` i `AppointmentRequest`, ali samo uspešna potvrda transakcijski stvara `Appointment`. | Milan (CTO) | Request-first flow ne sme korisniku obećati rezervaciju niti imati "skoro potvrđen" termin. | Canonicalni objekti R2 su odvojeni `SlotHold`, `AppointmentRequest` i `Appointment`; master plan i BDS-004/BDS-005 više ne koriste jedan appointment status za sve semantike. |
| **D-037** | **2026-07-22** | **Pomeranje termina je zaseban zahtev:** `AppointmentRequest.type = initial | reschedule`. Postojeći potvrđeni termin ostaje važeći dok novi zahtev ne bude atomski konvertovan. | Milan (CTO) | Neuspešna, odbijena ili istekla promena ne sme obrisati termin koji klijent već ima. | BDS-010A je `APPROVED`; pri prihvaćenoj promeni nastaje novi potvrđeni Appointment, a prethodni se zatvara sa audit reason code-om. Cancellation politika i finansijske posledice ostaju otvorene / R5 granica. |
| **D-038** | **2026-07-26** | **S1, S5 i S10 više ne blokiraju launch — postojeći tekst postaje fallback, CMS ga nadjačava.** Sav sadržaj koji sada stoji u `content/*` i komponentama ostaje u repou kao fallback sloj i **ne briše se**. Admin panel dobija CMS kroz koji Anja i tim sami uređuju svoj profil, zvanja, kontakt podatke, lokacije, pravne tekstove, uslove korišćenja, politiku privatnosti i ostale dokumente. Ako polje nije popunjeno u CMS-u, renderuje se fallback iz koda. Pregled i ispravka nesaglasja su obaveza tima, ne inženjeringa | Milan (CTO) | Čekanje na Anju i pravnika držalo je R1.5 zaustavljenim bez roka. Fallback + CMS override pretvara blokadu na ljudima u proizvodnu funkcionalnost, a tim dobija kontrolu nad sopstvenim sadržajem umesto da je traži kroz razvojni tim | STOP stavke S1/S5/S10 iz MP §13 prestaju da budu launch gate i postaju CMS zadaci vlasnika sadržaja. **Izuzetak: zvanja terapeuta ostaju generička i tačna** (`therapists.ts` već nema „pod supervizijom") — fallback nikad ne sme tvrditi nepotvrđenu kvalifikaciju. R1.4.i approval evidence za fallback sadržaj dobija privremeni CTO potpis da bi `published` bio moguć. Ne menja D-028: pun Content Engine ostaje R3 |
| **D-039** | **2026-07-26** | **Tekstovi saglasnosti idu kroz verzionisan registar dokumenata u admin panelu, ne kroz env promenljive i ne kroz naš provizorni tekst.** `INTAKE_DATA_PROCESSING_NOTICE_VERSION` i `INTAKE_REQUEST_ACKNOWLEDGEMENT_VERSION` prestaju da budu ručno podešene env vrednosti i postaju objavljena revizija dokumenta u bazi. Intake activation gate se otvara **onog trenutka kada Anja klikne Objavi**, ne kada neko ručno upiše env. Izričito odbijeno: da razvojni tim napiše provizoran pravni tekst, čak i samo za staging | Milan (CTO) | Za razliku od stranice o privatnosti, ovaj tekst se upisuje u `ConsentRecord.document_version` kao dokaz na šta je korisnik pristao. Provizoran tekst pod tuđim imenom je slab pravni dokaz i krši MP §0 tačku 8 („tekst ne pišemo mi"). Verzionisan registar rešava i buduće izmene: nova revizija → re-consent, bez migracije | Novi slice u `modules/privacy` (v0.3 §4.2 — **ne** u `content`, koji ostaje R3): `LegalDocument` + `LegalDocumentRevision`, publish lifecycle po D-029, admin editor u Control Center-u. `settings.intake_submission_ready` čita objavljenu reviziju umesto env stringa; env ostaje samo kao override za lokalni razvoj. Odstupanje od D-028 (nema CMS CRUD-a pre R3) → **ADR-014** |
| **D-040** | **2026-07-26** | **BDS-007B, BDS-008, BDS-009B i BDS-010B se usvajaju kao konfiguracija sa bezbednim default-ima, ne kao hardkodovane vrednosti.** Predložene vrednosti iz Pre-R2 spec-a (review target 12 radnih sati, istek zahteva 3 radna dana, pending-slot opcija A, Notify Me FIFO sa rokom od 4 radna sata, hold TTL) ulaze u bazu kao podrazumevana konfiguracija po organizaciji. Tim ih menja u panelu kada potvrdi sopstvenu praksu; kod nikad ne nosi broj | Milan (CTO) | Isti princip kao D-038: default sa mogućnošću izmene je bolji od blokade koja čeka sastanak. R2 dobija pun scope umesto da ostane bez tajmera, isteka i Notify Me | R2 dobija tabelu booking politike po organizaciji + panel ekran. Pre-R2 spec prelazi u v0.2: navedene odluke iz `PROPOSED`/`OPEN` u `APPROVED (default, configurable)`. **Ne menja** BDS-011 (consent/safety, i dalje Legal/Clinical) ni BDS-014 (finansije, i dalje R5). Javno obećanje roka se ne prikazuje dok operativni tim ne potvrdi poslovni kalendar |
| **D-041** | **2026-07-26** | **Četvrti `coffee` Booking Widget je B2B tok zakazivanja, ne samo tema.** Redosled je: **konfigurator prvo** (kompanija definiše vrstu i način tretmana), zatim coffee widget za samo zakazivanje. Vidljiv **isključivo ulogovanim zaposlenima ili kompaniji na njihovim nalozima** — nikad javno. Zaposleni sa pokrivenim seansama bira iz **rezervisanog kapaciteta kompanije**; ako kompanija nema rezervisane termine, pada na standardnu dostupnost. Obavezan **switch pojedinačno / grupno**: vebinar i predavanje su **samo grupno**, radionica dozvoljava oba. **Rezervisani kapacitet je kvota, ne novac** — bez cene, kredita, ledgera, faktura i refundacija. Tematsko mapiranje: `glass` → Individualna psihoterapija, `light` → Bračno savetovanje, `dark` → Roditeljsko savetovanje, `coffee` → Kompanije | Milan (CTO) | Zaposleni ne treba da prolazi kroz javni matching za nešto što je kompanija već ugovorila; treba mu samo izbor termina iz onoga na šta ima pravo. Kvota umesto ledgera drži R5 granicu netaknutom, a i dalje daje ceo opisani tok | Widget dobija **pun oblik odmah u R2** (coffee tema, switch, auth gate, pravila isključivanja), a iza njega stoji `CompanyAllocationAdapter` koji u R2 vraća „nema alokacije" → standardna dostupnost. **R4 samo puni adapter podacima** (BDS-013 ostaje R4) — UI se ne piše dvaput. Zahteva klasu naloga „zaposleni kompanije" u M2.1. Tema ≠ režim: po BDS-001 Bračno savetovanje ostaje `request`, pa `light` widget nema slot grid. Odstupanje od BDS-013 granice → **ADR-015** pre implementacije koraka 4 |
| **D-042** | **2026-07-26** | **STOP S1 je zatvoren: sva tri terapeuta su sertifikovani.** „Pod supervizijom" je zvanično zastarela formulacija i ne vraća se ni za koga, uključujući Marjana čija je predata biografija tu frazu još nosila. Javna zvanja glase: Anja — „Osnivačica Psihointegriteta · Socijalni radnik i **sertifikovana** geštalt psihoterapeutkinja", Marija — „Pedagog i **sertifikovana** geštalt psihoterapeutkinja", Marjan — „Psiholog i **sertifikovani** geštalt psihoterapeut" | Milan (CTO), potvrda Anja | Kontradikcija između Anjine poruke „svi smo sertifikovani" i zatečenog teksta stajala je od 2026-07-17 i držala objavu profila zatvorenom. CTO je potvrdio Anjinu verziju kao merodavnu | S1 iz 🔴 u ✅ u MP §13 i `TODO.md` §8; O-01 zatvoren u `OPEN_DECISIONS.md`. R0.2.d i R0.2.f prestaju da budu blokirani. `therapists.ts` je i dalje jedini izvor zvanja i sada je fallback sloj po D-038; CMS ga nadjačava kada tim unese svoje. Menja izuzetak iz D-038 — zvanja više nisu „generička jer nisu potvrđena", nego potvrđena. Sertifikat (izdavalac, godina) i dalje nije unet i ne prikazuje se dok ga terapeut ne dostavi |
| **D-043** | **2026-07-26** | **CMS Core + Content Governance je sledeći milestone i gradi se PRE R2 Booking Engine-a** (pre-R3 slice unutar MVP-a, ne formalni početak R3). Obim: uređivanje, verzionisanje, pregled i objava postojećih **6 tipova sadržaja** iz R1.4.i (`static_page`, `service`, `therapist`, `program`, `company_plan`, `package_offer`). Članci, biblioteka resursa, access grants, zakazana objava i AI asistencija **ostaju R3**. Redosled posle CMS-a: javni CMS renderer → Clerk nalozi → LD-5/LD-6/LD-7 → ADR-015 → R2 Booking Engine → Kompas | Milan (CTO) | D-038 je već napravio CMS launch-preduslovom (fallback + override); bez editora tim ne može uneti ni kontakt, ni pravne tekstove, ni ispravke profila. Booking bez stabilnog sadržaja zavisi od ručno unetih tekstova i promenljivih slugova | **Superseduje klauzulu „Ne menja D-028" iz D-038 u delu redosleda** — pun Content Engine i dalje ostaje R3, ali njegov Core (CRUD, revizije, review, publish za postojećih 6 tipova) ulazi sada. Master plan §3 dobija Pre-R3 anotaciju (kao Pre-R2); ⚠️ **napomena za Anju:** deo sadržaja Faze 3 iz Proposala ulazi ranije — evidentirati pri sledećem SoW razgovoru. Odstupanje od ADR-014 granice („`modules/content` ostaje netaknut") → **ADR-016**. Plan faze: `CMS_CORE_CONTENT_GOVERNANCE_PLAN_v0.1.md` |
| **D-044** | **2026-07-26** | **Governance rečnik je izveden sloj nad D-029 i D-032, ne novi kanon.** Statusi ostaju isključivo `draft → in_review → approved → published → archived`; `validation_failed` NIJE status nego rezultat provere vezan za reviziju, a `review_required` NIJE status nego nalaz koji reviziji dodaje obaveznu approval capability. Severity ostaje `info \| warning \| error`. Četiri klase rezultata su izvedene: **BLOCK** = nalaz `error` (objava nemoguća) · **REVIEW_REQUIRED** = nalaz sa `requiresApproval: clinical\|legal\|business` (objava tek uz to odobrenje) · **WARNING** = `warning` (prolazi uz pažnju) · **PASSED** = bez nalaza. Rule registry se proširuje **additivno**: svako pravilo dobija `version`, opciono `requiresApproval` i `remediation`; postojećih 13 prefiksa i 32 pravila iz `CONTENT_HEALTH_RULES` ostaju netaknuti. **Redosled provere je zaključan: sadržaj → tranzicija → odobrenja**, identičan u UI-ju i backendu; backend je jedini autoritet objave. Panel-error ključ je najmanje `organizationId + resourceType + resourceId + versionId + ruleId`; uspešna ponovna provera automatski uklanja zastarelu grešku | Milan (CTO) | Predloženi šesto-stanjski lifecycle i novi severity rečnik kosili su se sa zaključanim D-029/D-032, implementiranim na 3 mesta + CI gate. Dokumentacija je izvor istine; potrebe (automatska provera, stručni pregled, jasno objašnjenje) ostvaruju se izvedenim slojem bez lomljenja ugovora | Nalaz (`ContentHealthFinding`) dobija `ruleVersion` i opciono `requiresApproval` — `CONTENT_HEALTH_RULES` ide u v0.2 additivno. `ValidationResult` se čuva po reviziji kao snapshot provere (koja verzija pravila je važila pri odobrenju). Nijedan rok/rezultat se ne hardkoduje u status enum |
| **D-045** | **2026-07-26** | **Objavljena revizija se nikada ne briše.** Objavljen pravni dokument ili sadržaj može se samo arhivirati ili povući iz upotrebe; brisanje je dozvoljeno isključivo za radne verzije (`draft`). Odobrenja pripadaju tačno određenoj reviziji: svaka izmena posle odobrenja poništava Legal/Clinical/Business odobrenja za novu verziju (potvrda D-029 pravila) | Milan (CTO) | `ConsentRecord.document_version` i buduća istorija objava moraju uvek moći da se razreše na tekst koji je korisnik stvarno video; obrisana objavljena revizija bi uništila dokaz saglasnosti | Registar dokumenata (`screen-dokumenti.tsx` trenutno dozvoljava brisanje objavljenog uz upozorenje — **menja se u archive-only**) i `publication.py` dobijaju guard; isto pravilo važi za sav budući CMS sadržaj. Parity fixture testovi (TS ↔ Python) čuvaju identičnost pravila na obe strane |
| **D-046** | **2026-07-29** | **Autorski sloj CMS-a: `RichDoc` v1 kao kanonski format teksta, uvoz samo `.docx`, Tiptap kao editor.** Tekst se čuva kao strukturisan JSON (blokovi sa stabilnim `id`-jem, marks `bold`/`italic`/`underline`/`link`), **ne kao HTML ni Markdown**. Naslovi u telu idu od H2 (H1 je `title` slot). Tabele i slike **nisu** u v1 — tabela se pri uvozu izbacuje uz upozorenje, slike idu kroz galeriju sa obaveznim alt tekstom. Uvoz `.docx` radi **backend** (mammoth → jedan normalizator zajednički sa paste-om); `.doc` se odbija uz uputstvo „sačuvajte kao .docx", PDF nije strukturisan put. Glavne stranice dobijaju **registar šema slotova** (`SlotFieldSpec`) koji definiše koliko teksta, pasusa i slika ide u svaku sekciju. Editor je Tiptap sa šemom ograničenom na RichDoc čvorove, ali se u bazu upisuje RichDoc, ne Tiptap JSON | Milan (CTO) | Tim isporučuje pravne tekstove kao Word dokumente i mora da ih objavi sa pravom strukturom (naslovi, liste, bold/italic/underline). HTML kao format znači trajnu sanitizaciju i `dangerouslySetInnerHTML` na platformi koja objavljuje pravne tekstove i saglasnosti; Markdown nema underline. Strukturisan JSON čini XSS nemogućim po konstrukciji, daje `fieldPath` za nalaze i stabilnu adresu za kasniji AI predlog | Novi zadaci **CG-B6** (RichDoc ugovor + parity fixtures), **CG-B7** (uvoz `.docx` + `IMPORT-0xx` nalazi), **CG-C5** (Tiptap + adapter), **CG-C6** (SEO pregled); **CG-C1 proširen** registrom šema slotova — time se zatvara CG-B2 Nalaz 3 i `LIMIT-002` postaje sprovodiv. `LegalDocumentRevision.body` prelazi sa `Text` na RichDoc JSON. Nove zavisnosti: `mammoth` (backend), Tiptap (frontend). Upload je **prva autentifikovana tačka za prijem fajlova** — traži limit veličine, MIME proveru i rate limit. **AI sloj nije otvoren** — ADR-017 §12 fiksira samo šav (stabilan `blockId`, `fieldPath`, `dialect` polje zbog D-017 ijekavice); predlozi žive u zasebnoj tabeli i nikad ne upisuju u `slot_data`. Puno obrazloženje: **ADR-017** |
| **D-047** | **2026-07-29** | **Ne pravimo jedan CMS za sve — četiri tipa sadržaja sa različitim modelima uređivanja, i kernel pre engine-a.** (1) **Operativne stranice** (Home, O nama, Tim, profil terapeuta, Pronađi podršku, kompanije, usluge, kontakt) — `SlotFieldSpec`, stroge sekcije, `visible` prekidač, ograničeno premeštanje; **bez rich-text editora i bez slobodnog page buildera**. (2) **Stručni tekstovi i blog** — `.docx` → izveštaj o uvozu → RichDoc draft → preview → sitne korekcije → odobrenje → objava; **mali editor** (tekst, b/i/u, link, H2–H4, liste, quote, premeštanje/brisanje bloka, undo/redo) — bez fontova, veličina, boja, tabela, kolona, inline stilova, sirovog HTML-a i iframe-ova. (3) **Dokumenti i materijali** — PDF **nije** RichDoc; zaseban `ResourceAsset` entitet. (4) **Video** — zaseban asset tip, provider se bira kasnije. Uz to: **ne razvijati CMS, Layout Engine i AI Engine paralelno** — prvo stabilan publishing kernel i **jedan kompletan vertikalni tok**, pa tek onda AI i layout varijacije. **Prvi vertikalni tok je pravni dokument**, ne članak | Milan (CTO) | Jedan editor za sve tipove ili uništava dizajn operativnih stranica ili je prefrustrirajući za duge tekstove. Terapeuti pišu u Wordu/Google Docs-u i ne smeju da se vraćaju u izvorni dokument zbog svake slovne greške — to pravi nove verzije celog sadržaja i rizik da neko pregazi tuđe korekcije. Tri velika sistema paralelno znače da se greška u ugovoru otkriva na tri mesta | **Prvi tok je pravni dokument jer je jedini koji je već autorizovan i skoro gotov**: ADR-014 ga pokriva, `screen-dokumenti.tsx` postoji, modeli i migracija postoje, parity fixtures rade, a ADR-017 ga je već imenovao kao prvog potrošača RichDoc-a. Dokazuje ceo kernel (`.docx` → uvoz → RichDoc → odobrenja → objava → renderer) i **usput otvara Intake gate** koji danas drži slanje zatvorenim. Članak je odbijen kao prvi jer traži šest novih stvari pre prve objave: `ContentType.article` (danas namerno izostavljen uz ADR-016 komentar u `models.py:64`), `article` template, `SlotFieldSpec`, validaciona pravila, javnu rutu + renderer i proširenje `JsonLdKind` (danas nema `article`) — uz blokadu na O-17/O-18 zbog reviewer identiteta. Članak dobija **ADR-019**. Uz odluku ide i **Amandman 1 na ADR-017** (dialect po polju, uzak `ContentPatch`, AI nalazi kroz postojeći finding model) |
| **D-048** | **2026-07-29** | **Access model nosi samo nivoe koji se danas mogu evaluirati:** `public \| registered \| staff_only`, a odluka ide kroz port koji vraća `Grant \| Denial{reason_code}` — nikad poređenje enum-a u ruteru. Odbijen pun enum sa šest vrednosti (`SUBSCRIBER`/`PURCHASED`/`COMPANY_ENTITLEMENT`) u kome plaćeni nivoi tvrdo odbijaju. `priceId` **nije** polje na `ResourceAsset` | Milan (CTO) | Kolona koja sadrži `PURCHASED` **jeste** tabela i migracija — tačno ono što D-031 blokira do R5; pravilo ne kaže „blokirano osim ako vraća 403". Uz to `PURCHASED` odbija bez ijednog zapisa o kupovini na koji bi podrška mogla da pokaže, pa je odbijanje neproverljivo, a vrednost u šemi je stalan poziv da se doda dugme „Kupi" — što pali `PUB-003` (CI **error**) | R5 aditivno dodaje `subscriber` kao vrednost politike i `payment_required` kao reason kod — **bez migracije postojećih redova i bez izmene poziva**. `COMPANY_ENTITLEMENT` ostaje R4 (BDS-013) i traži ADR-015. `staff_only` se rešava kroz postojeći `resolve_staff_actor()` — **bez novih role semantika** (ADR-016 ih zabranjuje). `priceId` je FK ka tabeli koja ne sme da postoji; R5 ga dodaje. Puno obrazloženje: **ADR-018** |
| **D-049** | **2026-07-29** | **Google Drive je izvor za uvoz, nikad produkcijski storage.** Fajl stiže **ručnim download-om pa upload-om** u panel; bez Drive OAuth-a, bez Drive API integracije u v1. Na objavljivanju backend preuzima fajl, proverava MIME kroz magic bytes, veličinu i decompression ratio, računa checksum, smešta u **privatan** object storage i beleži verziju. Javne marketinške slike idu na Cloudinary; zaštićeni fajlovi u privatan bucket | Milan (CTO) | Dozvole Drive dokumenta se menjaju van platforme, fajl može biti pomeren ili obrisan, korisnik može videti ili proslediti originalni Drive URL, a entitlement, audit i statistika se ne mogu pouzdano voditi nad tuđim storage-om. Ručni upload dodatno izbegava dve konkretne opasnosti: `drive.readonly` scope daje čitanje **celog** Drive-a te osobe, a server-side fetch korisnički zadatog URL-a je SSRF | Nula koda i nula OAuth površine za v1. Ako Drive integracija ikad zatreba, ide isključivo kroz Picker sa `drive.file` scope-om i isključivo kroz Drive API — nikad sirov URL fetch. Preuzimanje ide kroz **jednokratan grant** (TTL 60–120 s, `jti` potrošen pri iskorišćenju, vezan za `resource_id` + `user_id`), ne kroz token u URL putanji — token u putanji se loguje na proxy-jima, ostaje u istoriji browsera i lomi range request. Puno obrazloženje: **ADR-018** |
| **D-050** | **2026-07-30** | **`SlotFieldSpec` iz ADR-017 §9 je bio nacrt, ne ugovor — ispravljen pre nego što je ijedan red `slot_data` upisan u bazu.** Sedam ispravki: (1) slot dobija stanje `editable \| computed \| unmodeled` iznad svojih polja — prazan `fields: {}` ne sme značiti i „izvedeno, odbij payload" i „nedokazano, ne izmišljaj" istovremeno; (2) `maxOptionalSections` se **uklanja** — brojao je pogrešnu stvar (ključeve slotova, ne stavke unutar kolekcije, kako CG-B2 Nalaz 3 već kaže); pravi `LIMIT-002` postaje `min`/`max` po `imageList`/`ctaList`/`repeater` polju; (3) `inherit \| override \| hidden` ulazi u payload ugovor sada, ne u CG-C3 — D-038 fallback traži tri razdvojiva stanja po slotu, ne prazno=fallback; (4) `number` se deli na `integer` (min/max/step/unit) i `money` (RSD, min/max) — cena i broj seansi nisu isto polje; (5) `repeater.item` **ne sme** sadržati `repeater`/`imageList`/`ctaList` — neograničeno ugnežđavanje je prvi korak ka page builder-u koji D-047 zabranjuje; (6) `cta` polje adresira postojeći `CtaAction` registry (`{action, label, targetId?}`), ne slobodan href; (7) `image` je `{assetId, alt, decorative?}` kao `AssetReference`, ne goli string — `ASSET-003` već traži alt. **CG-C1 se uz to deli na C1a (ugovor + parity fixture + pravi LIMIT-002, bez UI-ja) i C1b (draft editor bez lifecycle dugmadi — pošalji/odobri/objavi ostaju CG-C4)** | Milan (CTO) | Review CG-C1 implementacionog plana je proverio §9 protiv stvarnih stranica (`static-provider.ts`, `usluge/[slug]`, `tim/[slug]`, `cene`, `rad-sa-kompanijama`) i postojećih registry-ja (`cta.ts`, `AssetReference`) i našao da bi originalni nacrt zahtevao migraciju `slot_data` oblika posle prve iteracije — jeftinije je ispraviti ugovor pre koda nego posle | Zamenjuje ADR-017 §9 u celosti kroz **Amandman 2 na ADR-017**; §1–§8, §10–§12 se ne menjaju. `TemplateDefinition`/`TEMPLATE_REGISTRY` gube `maxOptionalSections` na obe strane (TS i Python). Pre CG-C1a: zatvoriti odloženu Fazu 0 verifikaciju (CG-B6 content publication.py testovi, CG-B7 `richdoc.v1.json` parity fixtures, CG-B8 docx/paste bezbednosni testovi) — nijedan nije bio deo zelenih 172 testa iz prethodnog verifikacionog prolaza. Puno obrazloženje: **ADR-017 Amandman 2** |
| **D-051** | **2026-07-30** | **Platform superadmin dobija pun staff kapacitet na backendu — `org_admin` + `therapist` u svakom tenantu, sa ili bez membership redova.** Nosilac je NOVA kolona `internal_users.is_superadmin` (migracija `20260730_0006`), a `resolve_staff_actor` vraća `StaffActor(roles=roles | {ORG_ADMIN, THERAPIST}, is_superadmin=True)` za takvog korisnika. Zastavica se pali isključivo eksplicitnom komandom (`provision_staff.py --superadmin` / `--revoke-superadmin`), tri-state je u `StaffProvisioningRequest` (`None` = ne diraj), i vidi se u `--list` sa ★. Važi u SVIM okruženjima, uključujući produkciju | Milan (CTO) | Frontend guard-ovi već puštaju superadmina i na `org_admin` i na `therapist` površine (`isWorkspaceAdmin`/`isWorkspaceTherapist`), a backend nije imao nikakav pojam superadmina — svaka takva stranica se renderovala pa pucala na prvom API pozivu (`Invalid bearer token` / `Staff membership is not provisioned`). Tačno taj rascep je 2026-07-30 učinio da CMS editor izgleda pokvareno. Traženo i radi testiranja CMS-a, Booking-a i budućeg Diagnostic Engine-a | **Kolona u PostgreSQL-u, ne Clerk claim** — rules §10.3 zabranjuje da dugoročna domenska autorizacija zavisi od izmenljive Clerk metadata; izmena `publicMetadata` u Clerk dashboard-u daje frontend prikaz, ali NIŠTA na backendu dok se komanda ne pokrene. To je i ono što je čini auditabilnom. **Audit ostaje istinit**: `StaffActor.user_id` je i dalje operaterov sopstveni red, pa `created_by_user_id` i `ContentPublicationEvent.actor_user_id` tačno pripisuju ko je delovao. **`is_superadmin` je zadržan kao zasebno polje na `StaffActor`** (ne stopljen u `roles`) da bi use case koji NE sme biti dostupan platformskom operateru mogao da razlikuje to dvoje. **Granica koja ostaje**: `claim_case` i dalje traži stvaran `TherapistMatchingProfile` — superadmin bez profila dobija jasno `No active therapist profile is provisioned for this account`, što je ispravno (preuzimanje pravog klijentskog slučaja kao platformski operater bi pokvarilo podatke o dodeli). `is_active=False` i dalje nadjačava zastavicu. 11 integracionih testova, uključujući negativne garancije (ne-superadmin bez membershipa i dalje odbijen) |
| **D-052** | **2026-07-30** | **Intake & Matching koristi pet stabilnih oblasti:** `anxiety_stress` → „Stres i anksioznost", `relationships` → „Odnosi i partnerske teme", `parenting` → „Roditeljstvo", `trauma_crisis` → „Trauma i krizna iskustva", `personal_growth` → „Lični rast i razvoj". **„Zavisnost" nije šesta široka oblast**, već uža capability oznaka `addiction_related_support`; Anja je potvrđena kao primarni terapeut za tu temu, dok ostali slučaj mogu da preuzmu kroz postojeći timski handoff. Javni čipovi i tekst profila ostaju prezentacioni sadržaj i nisu izvor matching pravila | Milan (CTO), činjenična potvrda Anje | Ranije su frontend, backend i DB koristili različite slobodne stringove; „zavisnost" je postojala samo u backendu/bazi, pa upitnik nije mogao da je izabere. Široke teme se u praksi preklapaju i konačna dodela može zavisiti od uzrasta, formata, lokacije, dostupnosti i ljudske procene | `matching.ts`, `matching.py` i `therapist_matching_profiles.areas` prelaze na iste ID-jeve; zajednički ugovor je `contracts/fixtures/taxonomy.v1.json`, a migracija `20260730_0010` re-keyuje postojeće profile. „Zavisnost" ulazi u upitnik i daje Anji prioritet bez uklanjanja drugih kandidata ili handoff mogućnosti. Pravilo dobija verziju `intake-matching-v3`. Ovo zatvara **O-24a (Intake drift)**; buduće CMS/Kompas ose ostaju zasebna **O-24b** odluka |
| **D-053** | **2026-07-31** | **Kompas dobija kanonski DB registar, a implementacija ide ovim redom: backend foundation → panel „Kompas" → kontrolisana CMS polja → recommendation/public Kompas.** Anja i ovlašćeni članovi tima kroz panel uređuju grupe tema, teme, publike i ciljeve sadržaja: javni naziv/opis, sinonime, hijerarhiju, redosled, ikonu/asset, javnu vidljivost, aktivnost u Kompasu, povezane teme, put korisnika, internu stručnu napomenu i lifecycle. Stabilni ID i semantika su nepromenljivi. Sistemske vrednosti (`journeyIntent`, format, access level, lifecycle i approval tipovi) administrator samo bira; može menjati odobrenu javnu labelu, ali ne i značenje. CMS odmah po završetku panela koristi reference iz registra, bez slobodnih taxonomy stringova | Milan Dražić (CTO); Anja Stamenković i ovlašćeni stručni tim uređuju stručne podatke i odobravaju njihovo značenje | Seed ili hardkodovan frontend bi ponovo napravio drift i terao izmenu koda za svaku stručnu korekciju. Jedan revisioned registar daje stabilne reference, stručni review, istoriju i isti izvor za CMS, Intake most i javni Kompas. Tehnička imena se ne prikazuju terapeutima; formular postavlja jasna pitanja na srpskom | **ADR-022 je prihvaćen.** O-24b je arhitektonski zatvoren; Anjina konačna kategorizacija je unos podataka, ne blokada backend modela. Postojećih pet D-052 Intake oblasti migrira se u isti registar kao sistemska osa, ali topic grupe/teme ostaju odvojene i povezuju se review-ovanim mostom bez therapist scoring-a. Ciljni access rečnik rezerviše `subscriber`/`purchased`, ali ih panel ne sme sačuvati niti objaviti pre R5 entitlement evaluator-a; D-048 ostaje na snazi. Article/Layout ADR-019/021 ostaju preduslov za article renderer, ne za početak registra i panela |

| **D-054** | **2026-07-31** | **Kanonske stranice jedne oblasti/teme odvojene su od dinamičkog Kompas prikaza.** U panelu je „Oblast” javni naziv za postojeći `topic_group`; D-052 `support_area` se prikazuje samo kao „Intake oblast podrške” u kontrolisanim Povezivanjima. Kanonske rute su `/kompas/oblast/[slug]` i `/kompas/tema/[slug]`, dok `/kompas` kombinuje trenutni izbor bez novih kombinacionih SEO stranica i čitljivih topic query parametara. Javni locale-aware slug je odvojen od internog stable ID-ja, ne menja se automatski sa labelom i čuva stari redirect pri eksplicitnoj korekciji. Opcioni revisioned `CompassGuide` može kasnije da nosi authored/approved sadržaj o čestim kombinacijama; generic engine samo grupiše i deduplikuje sadržaj i nikad ne izmišlja stručnu vezu tema | Milan Dražić (CTO) | Šira oblast, konkretna tema i trenutna kombinacija imaju različite SEO, stručne i privacy odgovornosti. Automatska stranica za svaku kombinaciju bi proizvela stotine duplikata, a engine-authored objašnjenje veze tema bi bilo neodobrena stručna tvrdnja | **ADR-022 Amandman 1.** K2 počinje malim backend route registry-jem, a tab „Grupe” postaje „Oblasti” bez nove taxonomy ose; K3B uvodi kanonske read-modele i opcioni `CompassGuide` tek nad stvarnim katalogom; K4 bira exact guide → najkonkretniji subset → generički prikaz; K6 dobija tri odvojene javne površine. `/kompas/putanja/[opaqueId]` ostaje kasniji privatni domen iza zasebne retention/access odluke |

| **D-055** | **2026-08-01** | **Komercijalni rečnik je zaključan, a `organization_id` ostaje granica izolacije podataka.** Četiri odvojena pojma, četiri odvojena modela: **`organizations`** = izolovani workspace/brend i **jedina RLS granica** — postoji danas · **`company_accounts`** = kupac platforme, nosilac pretplate, ugovora i affiliate/reseller statusa — dodaje se **iznad** postojećeg modela kao `organizations.account_id`, kasnije i additivno · **`legal_entities`** = APR pravno lice (tri paušalne radnje terapeuta) — zaseban model, **ne poistovećuje se** ni sa organizacijom ni sa brendom; jedno može biti platilac pretplate dok sva tri pružaju stručnu uslugu unutar iste organizacije · **`client_companies`** = B2B firma koja kupuje programe za zaposlene (R4) — **nikad se ne zove `organizations`**. Reč **„tenant" prestaje da bude identifikator** — ostaje samo kao pojam u prozi; `tenant_id` se ne uvodi ni na jednu tabelu i `organization_id` se ne preimenuje | Milan Dražić (CTO) | Reč „organization" je već nosila dva značenja: u bazi je koren vlasništva (1 red, `psihointegritet`, `organization_memberships` = osoblje), a u `CLEAN_..._PSIHOINTEGRITET.md` §8.10 i R4 B2B planu su to klijentske kompanije koje kupuju programe (`organization_contacts`, `organization_agreements`, `organization_benefit_tokens`). Da je to ostalo nerazrešeno, `organization_id` bi u istoj šemi značio i „čiji je ovo podatak" i „koja firma je klijent" — a to je tačno kolona na kojoj visi RLS polisa. Razdvajanje sada je preimenovanje pojma bez ijednog reda koda; posle R4 bilo bi migracija | **Redosled je obavezujući: prvo zaključati i osigurati postojeću `organization_id` granicu (ADR-023), pa tek onda dodavati `company_accounts` iznad.** Ne menja se bezbednosna granica dok se upravo štiti. Pri uvođenju account nivoa postojeći podaci ostaju vezani za isti `organization_id`, a RLS polise se **ne prepisuju** — samo se razrešavanje konteksta proširuje. Nezavisan affiliate klijent dobija sopstveni account i sopstvenu organizaciju; brend kojim Psihointegritet stvarno upravlja dobija dodatnu organizaciju unutar istog account-a. Platform-level uloga (D-051 `is_superadmin`) se **nadograđuje**, ne duplira novim `PlatformRole` modelom. Za Psihointegritet početno stanje: 1 account · 1 organizacija · 3 `legal_entities` · više terapeuta i lokacija |
| **D-056** | **2026-08-01** | **Javni Kompas v1 je odvojen, opcion i neklinički discovery tok.** `/kompas` koristi najviše pet inline pitanja nad objavljenim DB registrom; pitanja se mogu preskočiti ili prekinuti, a „Nisam siguran/na šta mi se događa” je korisnička akcija, ne taxonomy termin. Teme su nova multi-select kontrola sa najviše dve vrednosti. Kompas ne koristi Intake `GuidanceFlow`, ne rangira terapeute i u Intake prenosi samo neutralan, korisniku vidljiv i potvrdiv `CompassHandoffContextV1`. Anonimni aggregate i recommendation endpoint vraćaju isključivo objavljen sadržaj čiji je efektivni access eksplicitno `public`; `registered`, `staff_only` i `NULL` se ne podrazumevaju kao javni. `kompas` je rezervisan custom-document slug. Feedback je zaseban anonimni UX survey i ne ulazi u recommendation state. | Milan Dražić (CTO), kroz prihvaćen implementacioni handoff 2026-08-01 | Postojeći Intake `QuestionsScreen` bezuslovno prikazuje klinički `SafetyNotice`, postojeći izbori su single-select sa auto-advance, a custom dokument danas može da zaseni sistemsku rutu. D-048 definiše širi izvršivi access rečnik, ali javni anonimni Kompas mora imati užu bezbednosnu granicu. | **ADR-022 Amandman 2.** Javni route skup je `/kompas`, `/kompas/oblasti`, `/kompas/teme`, `/kompas/oblast/[slug]` i `/kompas/tema/[slug]`; DB alias ostaje runtime 308 izvor i ne ulazi u compile-time redirect registry. `/kompas` koristi kontrolisanu interaktivnu strukturu uz opcioni `static_page + static_information` CMS copy. Finalni vizuelni F3 čeka poseban Milanov design handoff; backend aggregate/Engine, kanonske stranice i admin „Brzi unos” ne čekaju taj mockup. |

| **D-057** | **2026-08-02** | **Research postaje pravi backend modul sa dve generičke tabele; anketa više nije email-only demo.** `research_surveys` drži jednu **verziju** jedne ankete (`stable_id` + `version`, verzija NIJE deo stabilnog ID-ja), `research_submissions` drži sve odgovore bez obzira na anketu. Dve ankete — `online-experience` i `compass-experience` — dele jedan konfigurabilan drawer, ali imaju odvojena pitanja, svrhu i rezultate. Panel ostaje jedna ruta `/radni-prostor/istrazivanja` sa tabovima; **odgovori se nikad ne mešaju u zajedničke procente**, ni između anketa ni između verzija iste ankete. `compass-experience` v1 **ne dozvoljava slobodan tekst** | Milan Dražić (CTO) | Postojeći `/api/survey` šalje odgovore mejlom i ništa ne čuva, pa nema ni broja odgovora ni raspodele. Zasebne tabele po anketi bi svaku novu anketu pretvorile u migraciju. Slobodan tekst je mesto gde bi korisnik najlakše ostavio zdravstveni ili lični podatak, a Kompas feedback-u proza ne treba | ⚠️ **Ovo pomera R6 posao unapred.** D-021 je anketu dozvolio izričito kao „throwaway frontend iza flag-a, **ne scaffolding pravih modula** — pravi moduli ostaju R2/R4/R6", a ADR-018 „does NOT open" lista kaže „no new backend module". Odluka svesno odstupa od oba i traži **ADR-024**. **Privatnost je svojstvo šeme, ne konvencija:** `research_submissions` nema `user_id`, email, IP, izabrane Kompas oblasti/teme ni recommendation rezultat — kolone koje bi omogućile povezivanje sa osobom ne postoje. Odgovori nose **stabilne `questionId`/`optionId`**, ne tekst, pa promena formulacije ne prepisuje istoriju. FK ka anketi je `RESTRICT`, ne `CASCADE`. Migracija `20260802_0015` je pisana **ručno** jer je autogenerate uz nove tabele emitovao i D19 drift, uključujući `DROP COLUMN intake_cases.age_group`. Nove tabele su `ORGANIZATION_SCOPED` po ADR-023 i ulaze u RLS rollout grupu 6. Puno obrazloženje: **ADR-024** |
| **D-058** | **2026-08-03** | **Kompas v1 dobija verzionisan Flow Registry i backend Result Composer, bez čuvanja korisničkih odgovora ili rezultata.** `modules/compass` poseduje samo `compass_flows`, atomske JSONB flow verzije, review odluke, determinističko mapiranje odgovora u `CompassSelection` i sastavljanje sekcionisanog read-modela. Taxonomy, discovery metadata, eligibility i CMS ostaju u `modules/content`; Intake matching ostaje u `modules/guidance`. | Milan Dražić (CTO), kroz Kompas v1 vertikalni handoff 2026-08-03 | Statička React pitanja ne daju administrativno verzionisanje, preview ni atomsku objavu. Tabele po pitanju/opciji bi omogućile parcijalno objavljivanje malog grafa. Čuvanje anonimnog selection-a nije potrebno za v1 i povećalo bi privacy površinu. | **ADR-025.** Flow `definition` je strogo validiran JSONB bez slobodnog DSL-a, težina, JavaScript izraza i ciklusa. Javni endpoint vraća samo published verziju; staff preview tačnu draft verziju. Result Composer vraća stabilne sekcije i jedini određuje deduplikaciju/prioritet. Research ankete ostaju potpuno odvojene i nikada ne utiču na preporuke. |
| **D-059** | **2026-08-03** | **Basic Kompas pripada R3 Content obimu, ali ima nezavisnu produkcionu aktivaciju preko feature flag-a.** Ne pravi se veštački nov proizvodni release; Kompas ostaje isključen u produkciji dok ne postoje minimalni urednički podaci, admin prihvatni testovi i zeleni javni E2E. | Milan Dražić (CTO), 2026-08-03 | Zaseban release bi udvostručio release proces zbog jedne vertikale. Uključivanje odmah po deploy-u bi javno izložilo prazan discovery sloj — bez Anjinih oblasti/tema i bez 2–3 eligible CMS sadržaja Kompas nema šta da preporuči. | Zatvara prvu otvorenu stavku iz `KOMPAS_TODO.md` §14 i release deo O-21. Aktivacija je operativna odluka nad flag-om, ne nov release plan. „Napredni Kompas” ostaje otvoren (O-21). **Implementirano kao `NEXT_PUBLIC_COMPASS_ENABLED`, default isključen:** bez njega `/kompas` i taxonomy rute vraćaju 404, nestaju header link i CTA na naslovnoj, a sitemap ne navodi nijednu Kompas rutu. Isključeno okruženje ne prerenderuje te rute, pa pad backend-a više ne može da obori njegov build. |
| **D-060** | **2026-08-03** | **D19 se rešava namernim uklanjanjem `intake_cases.age_group`, bez prelaska na PostgreSQL native enum.** Migracija `20260722_0002` je vrednosti već prenela u `subject_age_band`, a model i aplikacija koriste isključivo to polje. Pre `DROP COLUMN` migracija obavezno proverava nepoznate stare vrednosti i neslaganje sa `subject_age_band` i **prekida se radi ručnog pregleda** ako ih nađe. `native_enum=False` ostaje namerna strategija. Unique drift se prvo snima punim `alembic check` nad čistom bazom, pa se modeli i šema usklađuju oko eksplicitnih `UniqueConstraint` definicija. | Milan Dražić (CTO), 2026-08-03 | D19 od 2026-07-26 drži `alembic check` crvenim, pa on ne može biti CI gate, a blokira i RLS-1 preduslov. Slepi autogenerate bi emitovao `DROP COLUMN` bez provere podataka, na stagingu nad stvarnim Intake redovima. Prelazak na native enum bio bi tiha promena tipske strategije umesto uklanjanja drifta. | Zaseban commit i zasebna migracija, bez mešanja sa funkcionalnim paketom. Gate: upgrade/downgrade round-trip, test očuvanja podataka i **potpuno čist `alembic check`**. Tek tada `alembic check` ulazi u CI i otvara RLS-1. |
| **D-061** | **2026-08-03** | **Article vertikala je otvorena: `ContentType.article` sa segmentiranim telom, javnom rutom `/znanje/<slug>` i pravilom četiri oka.** Telo se cepa na sekcije (`hero`, `byline`, `body_intro`, `questions`, `practice`, `body_outro`, `sources`, `cta`) po verzionisanom receptu `article-v1`; **redosled renderovanja je fiksan, autor bira samo koje sekcije postoje i šta je u njima**. `questions` i `practice` mogu biti preporučeni samostalno u Kompas sekciji „Praktični alati” ako ih autor eksplicitno tako označi. Objava traži `clinical` + `business`; autor i poslednji sadržajni urednik ne mogu odobriti sopstvenu reviziju, a isti kolega sme da bude recenzent i objavljivač. Pravilo četiri oka važi **samo za članke** dok O-18 ne obezbedi drugog `org_admin`-a na produkciji. | Milan Dražić (CTO), 2026-08-03, povodom prvog stvarnog teksta od Anje i tima | Bez segmenata Kompas ne može da preporuči praktičan blok, što je bila eksplicitna stručna potreba. Autorski redosled sekcija bi značio da odobreni tekst i objavljena stranica nisu isto stanje, pa je odbačen. Zahtev za tri različite osobe po tekstu bi za tim od troje ljudi značio da jedan izostanak zaustavlja objavu. Postojeći `static_information` ne može da nosi telo članka: njegova dva prozna slota su `UnmodeledSlot`, pa svaki autorski unos daje `MODEL-003` grešku. | **ADR-019 i ADR-021.** Telo ostaje u postojećem `slot_data`, bez nove kolone i bez DDL-a. Automatska provera (`EDIT-0xx`) ne sudi o istinitosti nego traži stručno odobrenje kroz postojeći `requires_approval` mehanizam. Kod počinje **posle D19**. Otvara K3A.3–K3A.7. |
| **D-062** | **2026-08-04** | **Administratorski UX je task-based: korisnik opisuje šta želi, platforma formira tehnički ispravan zapis.** Standardni org_admin/terapeut ne unosi niti vidi stable ID, UUID, API pojmove, asset ID, icon key, taxonomy osu, selection target, option source, kanonsku rutu, lock version ni backend kod greške — sve to ostaje u modelu i prikazuje se samo u sklopivoj sekciji „Tehnički detalji". Svako polje objašnjava šta se unosi, gde se prikazuje, kako Kompas taj podatak koristi i ko ga vidi. Svaka greška kaže šta nije završeno, zašto je važno i koja je sledeća radnja. Pregled javnog prikaza prethodi slanju na stručni pregled i objavi. | Milan Dražić (CTO), 2026-08-04 | Panel je govorio jezikom registra: „Kratak javni opis" se tražio dvaput u istom wizardu (jednom kao obavezan, pa opet kao da nije odgovoren), stabilni ID je uređivao terapeut iako posle prvog čuvanja postaje nepromenljiv, ikona se unosila kao slobodan tekstualni ključ koji nijedan prikaz ne renderuje, a Kompas „Sadržaj" je bio samo link ka CMS-u. DOCX importer bi ubrzao unos u interfejs koji korisniku još ne objašnjava šta unosi. | Milestone **UX-0** sa tri PR-a: UX-0A registar, UX-0B sadržaj, UX-0C tok pitanja. Faza 3 (DOCX importer) se odlaže bez izmene tehničkog obima. **Ne menja** backend ugovore, lifecycle, approval pravila, taxonomy ose ni recommendation semantiku; dozvole ostaju na `org_admin`. Gde ugovor ne dozvoljava traženu kontrolu, UI prikazuje istinu: preskok pitanja je uvek uključen (`optional` je konstanta), a slobodan tekst stoji onemogućen uz „U pripremi" (K4.1 ga zabranjuje). |
| **D-063** | **2026-08-04** | **Kompas authoring je content-first.** Autor ne dolazi da napravi oblast ili temu nego da objavi tekst; oblast i tema su podaci koji tom tekstu trebaju da bi Kompas znao gde ga smesti, pa se biraju ili kreiraju **unutar** toka sadržaja. Kompas sadržaj dobija **zaseban administratorski workspace i sopstvene rute**, ali ponovo koristi zajedničko Content/RichDoc/lifecycle/review jezgro — ne pravi se drugi paralelni backend CMS. Registar oblasti i tema ostaje, ali kao **sekundaran** ekran za pregled, ispravku naziva, spajanje, arhiviranje, redosled i sinonime. Stranice sajta (Stranica, Usluga, Program, Paket, Kompanijski plan) ostaju u opštem CMS-u i **nisu** Kompas sadržaj. | Milan Dražić (CTO), 2026-08-04, posle pregleda `features` brancha | UX-0B je tehnički isporučen, ali je „Dodaj novi sadržaj" pravio prazan `article` zapis i odmah odbacivao autora na `/radni-prostor/sadrzaj?entryId=…`, gde ga dočeka katalog od šest sistemskih tipova bez taba „Članci" i editor renderovan ispod cele liste. Posle kreiranja oblasti i teme nije postojao nijedan put ka „sad napiši sadržaj", povezivati se moglo samo sa stranicama sajta, a stručni pregled se tražio za pojam iza kojeg nema nijednog teksta. | **UX-0B se vraća na 🟡 — tehnički isporučen, produktno zamenjen.** Milestone **§5H** sa četiri PR-a: §5H-1 odvojena površina, §5H-2 content-first klasifikacija, §5H-3 tekst editor + `.docx` **(2026-08-04: `.docx` proxy 404 popravljen, importovani tekst se odmah prikazuje u Tiptap editoru, editor ima `resize: vertical`)**, §5H-4 zajednički pregled i javna kapija. Nijedan PR ne uvodi migraciju. |
| **D-064** | **2026-08-04** | **`article` je MVP materijal; `CompassGuide` ostaje zaseban, kasniji entitet.** Članak je samostalan edukativni tekst o oblasti ili temi. `CompassGuide` (K3B.3–K3B.5) je urednički vodič nad **kombinacijom** izbora — ima `matchTopicGroupIds`/`matchTopicIds`, objašnjava odnos tema, koristi kontrolisane sekcije, Engine ga traži kao exact ili subset i nema sopstvenu javnu kombinacionu stranicu. Naziv `CompassGuide` se **ne koristi** za običan tekst. | Milan Dražić (CTO), 2026-08-04 | Odvajanje Kompas sadržaja od CMS stranica lako se pročita kao poziv da se sve piše kao `CompassGuide`. To bi spojilo dva različita proizvoda i naterao nas da drugi put projektujemo `article`, koji već postoji: `ContentType.article`, `article_detail`, osam sekcija recepta i `clinical` + `business` odobrenje su definisani u ADR-019/ADR-021. | `CompassGuide` kasnije sme da koristi iste RichDoc, lifecycle, audit i review primitive, ali **ne zamenjuje** `article`. Van obima §5H. |
| **D-065** | **2026-08-04** | **Provera „referencirani Kompas termin mora biti objavljen" seli se sa vremena upisa na kapiju.** Draft revizija sadržaja **sme** da referencira draft oblast/temu; zahtev da je sve objavljeno proverava se na `in_review → approved` i na objavi, kao **error** nalaz `KOMPAS-REF-001` sa imenom termina, ne kao sirov `ValueError` na upisu. Pravila osa, tenanta, tema↔oblast roditeljstva i „pristup mora biti javan" ostaju na upisu. Arhiviran termin se i dalje odbija odmah. | Milan Dražić (CTO), 2026-08-04, uz D-063 | `_require_taxonomy_term` je tražio objavljenu reviziju, pa članak nije mogao da referencira oblast napravljenu u istom toku — content-first tok bez ovoga ne postoji. Alternativa (čuvati izbor na klijentu) je odbačena: paket koji živi samo u browseru autora recenzent na drugom uređaju ne vidi, čime pada i pravilo četiri oka. | Provera je i danas bila **trenutna, ne trajna** — ni `check_publish` ni APPROVED grana je ne ponavljaju, pa termin arhiviran posle objave ostaje referenciran. Stvarna zaštita je na čitanju (`_eligible_candidates` gradi read-model iz objavljene taksonomije) i ne dira se. **Ide u istom commitu sa D25**, inače postoji prozor u kome se članak sa neobjavljenom oblašću može objaviti. `ContentRevisionTaxonomyTerm` čuva `term_id`, ne id revizije → **bez migracije**. |
| **D-066** | **2026-08-04** | **Pravilo četiri oka se implementira, ali aktivira feature flag-om.** Provera „autor i poslednji sadržajni urednik ne mogu odobriti sopstvenu reviziju" (ADR-019 §9) ulazi u kod u §5H-4 i pokrivena je testovima, ali se pali istim flag-om kao Kompas, da objava članaka ne bude zaključana dok O-18 ne obezbedi drugog `org_admin` naloga na produkciji. | Milan Dražić (CTO), 2026-08-04 | Produkcija danas ima jedan `org_admin` nalog. Provera aktivna od prvog dana značila bi da se prvi stvarni članak ne može objaviti — MVP bi stao na organizacionoj, ne tehničkoj prepreci. | Uz D26 ispravku (recenzent više ne postaje „poslednji urednik" samim odobravanjem) pravilo postaje primenjivo čim drugi nalog postoji. Do tada je kod tu, testiran i ugašen. |
| **D-067** | **2026-08-04** | **i18n osnova se sada samo zapisuje, ne implementira.** Ugovor je: jezik pripada organizaciji (`organization.ui_locale`, privremeno `PLATFORM_UI_LOCALE`), jedan tipizirani `getPlatformMessages(locale)` nad rečnicima `messages/<locale>/*.ts`, backend vraća `code` + `params` a frontend bira reči, i **bez lokalizovanih ruta** (`/sr/`, `/en/`) jer javni sajt nije višejezičan. | Milan Dražić (CTO), 2026-08-04, uz §5G/O-27 | Novo tržište je nov tenant sa svojim domenom i svojim sadržajem (§5G), pa sistemske poruke treba prikazati na jeziku organizacije. Ali uvođenje resolvera pre §5H-a odložilo bi tok zbog kojeg §5H i postoji. | §5H PR-ovi poštuju pravilo **„nov tekst ide u copy modul, ne inline u JSX"**, pa kasniji prelaz ostaje zamena mehanizma nad rečnicima koji već postoje. Odluka o `SYSTEM_CONTENT_LOCALE` po organizaciji ostaje **O-27**. |

---

## Kako se dopunjava

Novi red na dno. Ako odluka menja T-vrednost ili STOP stavku:

1. Ažurirati `CLAUDE_CODE_MASTER_PLAN_v1_0.md` (§1 T-tabela ili §13 STOP tabela)
2. Ažurirati `OPEN_DECISIONS.md` (izbaciti rešeno)
3. Ažurirati `TODO.md` §8 (blokade) i pogođene redove
4. Ako odstupa od `ARCHITECTUAL/ARCHITECTURAL_RULES_NEXTJS_REACT_QUERY_v1.1.md` ili v0.3 → **ADR u `adr/`** (MP §0, tačka 6)

/* @ds-bundle: {"namespace":"PsihointegritetUI","components":[{"name":"Accordion","sourcePath":"components/general/Accordion/Accordion.jsx"},{"name":"AnimatedCtaContent","sourcePath":"components/general/AnimatedCtaContent/AnimatedCtaContent.jsx"},{"name":"AnimatedCtaLink","sourcePath":"components/general/AnimatedCtaLink/AnimatedCtaLink.jsx"},{"name":"ArrowLink","sourcePath":"components/general/ArrowLink/ArrowLink.jsx"},{"name":"BackToSiteButton","sourcePath":"components/shared/BackToSiteButton/BackToSiteButton.jsx"},{"name":"BackToSiteMenuItem","sourcePath":"components/general/BackToSiteMenuItem/BackToSiteMenuItem.jsx"},{"name":"ButtonLink","sourcePath":"components/general/ButtonLink/ButtonLink.jsx"},{"name":"Chip","sourcePath":"components/general/Chip/Chip.jsx"},{"name":"ConfirmModal","sourcePath":"components/panel/ConfirmModal/ConfirmModal.jsx"},{"name":"EmptyDashedCard","sourcePath":"components/panel/EmptyDashedCard/EmptyDashedCard.jsx"},{"name":"Eyebrow","sourcePath":"components/general/Eyebrow/Eyebrow.jsx"},{"name":"JsonLd","sourcePath":"components/shared/JsonLd/JsonLd.jsx"},{"name":"LogoutAvatarMenu","sourcePath":"components/shared/LogoutAvatarMenu/LogoutAvatarMenu.jsx"},{"name":"MonogramAvatar","sourcePath":"components/general/MonogramAvatar/MonogramAvatar.jsx"},{"name":"PageHero","sourcePath":"components/shared/PageHero/PageHero.jsx"},{"name":"ProgressBar","sourcePath":"components/panel/ProgressBar/ProgressBar.jsx"},{"name":"ReasonCard","sourcePath":"components/shared/ReasonCard/ReasonCard.jsx"},{"name":"ResourceCard","sourcePath":"components/shared/ResourceCard/ResourceCard.jsx"},{"name":"Reveal","sourcePath":"components/motion/Reveal/Reveal.jsx"},{"name":"SectionHeading","sourcePath":"components/general/SectionHeading/SectionHeading.jsx"},{"name":"StatCard","sourcePath":"components/panel/StatCard/StatCard.jsx"},{"name":"StatusBadge","sourcePath":"components/panel/StatusBadge/StatusBadge.jsx"},{"name":"StickyBar","sourcePath":"components/motion/StickyBar/StickyBar.jsx"},{"name":"TabPills","sourcePath":"components/panel/TabPills/TabPills.jsx"},{"name":"TherapistCard","sourcePath":"components/shared/TherapistCard/TherapistCard.jsx"},{"name":"TherapistRow","sourcePath":"components/shared/TherapistRow/TherapistRow.jsx"},{"name":"Toggle","sourcePath":"components/panel/Toggle/Toggle.jsx"}],"sourceHashes":{"components/general/Accordion/Accordion.jsx":"5be4439d8110","components/general/Accordion/Accordion.d.ts":"6e6edba94907","components/general/Accordion/Accordion.prompt.md":"252ff57d05f0","components/general/AnimatedCtaContent/AnimatedCtaContent.jsx":"961aeea6cc9c","components/general/AnimatedCtaContent/AnimatedCtaContent.d.ts":"4aa3ac8176df","components/general/AnimatedCtaContent/AnimatedCtaContent.prompt.md":"4d019c1d53f1","components/general/AnimatedCtaLink/AnimatedCtaLink.jsx":"74106122e2a6","components/general/AnimatedCtaLink/AnimatedCtaLink.d.ts":"9d70d4f8d139","components/general/AnimatedCtaLink/AnimatedCtaLink.prompt.md":"95d116d3fcfd","components/general/ArrowLink/ArrowLink.jsx":"ef9caa03d72a","components/general/ArrowLink/ArrowLink.d.ts":"389970dee9a5","components/general/ArrowLink/ArrowLink.prompt.md":"0ded76ce6ebb","components/shared/BackToSiteButton/BackToSiteButton.jsx":"176f7509a37e","components/shared/BackToSiteButton/BackToSiteButton.d.ts":"cc3617fadaee","components/shared/BackToSiteButton/BackToSiteButton.prompt.md":"ce8069f5abcb","components/general/BackToSiteMenuItem/BackToSiteMenuItem.jsx":"73bae223c2e6","components/general/BackToSiteMenuItem/BackToSiteMenuItem.d.ts":"ecc9a5d286c0","components/general/BackToSiteMenuItem/BackToSiteMenuItem.prompt.md":"75b3b1a6ca5f","components/general/ButtonLink/ButtonLink.jsx":"6716fb5a1adc","components/general/ButtonLink/ButtonLink.d.ts":"e21adcd26b53","components/general/ButtonLink/ButtonLink.prompt.md":"c7ef3bf3be55","components/general/Chip/Chip.jsx":"fa5b2d6496a4","components/general/Chip/Chip.d.ts":"977e4047916c","components/general/Chip/Chip.prompt.md":"093245430b62","components/panel/ConfirmModal/ConfirmModal.jsx":"e33678f84fdf","components/panel/ConfirmModal/ConfirmModal.d.ts":"3607e05f3b00","components/panel/ConfirmModal/ConfirmModal.prompt.md":"e4ee7302cde6","components/panel/EmptyDashedCard/EmptyDashedCard.jsx":"91a2db497af3","components/panel/EmptyDashedCard/EmptyDashedCard.d.ts":"7d382cac9d1b","components/panel/EmptyDashedCard/EmptyDashedCard.prompt.md":"1be357350a9e","components/general/Eyebrow/Eyebrow.jsx":"8e3ba191f2d5","components/general/Eyebrow/Eyebrow.d.ts":"1b67bf03aaa2","components/general/Eyebrow/Eyebrow.prompt.md":"de1ef55a1dc8","components/shared/JsonLd/JsonLd.jsx":"b6c63a823cb9","components/shared/JsonLd/JsonLd.d.ts":"4907a58a5455","components/shared/JsonLd/JsonLd.prompt.md":"3bdd08117deb","components/shared/LogoutAvatarMenu/LogoutAvatarMenu.jsx":"886a17f4b076","components/shared/LogoutAvatarMenu/LogoutAvatarMenu.d.ts":"f61f1c9f1c2f","components/shared/LogoutAvatarMenu/LogoutAvatarMenu.prompt.md":"eb6ce9f8f664","components/general/MonogramAvatar/MonogramAvatar.jsx":"49cdf1cf41af","components/general/MonogramAvatar/MonogramAvatar.d.ts":"4d91b16ad518","components/general/MonogramAvatar/MonogramAvatar.prompt.md":"a8d8d3abc8aa","components/shared/PageHero/PageHero.jsx":"ed807172c749","components/shared/PageHero/PageHero.d.ts":"dbeba28b81b6","components/shared/PageHero/PageHero.prompt.md":"b7f3dcd260bf","components/panel/ProgressBar/ProgressBar.jsx":"a70d1885d9b6","components/panel/ProgressBar/ProgressBar.d.ts":"d1cde2f4a45e","components/panel/ProgressBar/ProgressBar.prompt.md":"fc7cad1410ec","components/shared/ReasonCard/ReasonCard.jsx":"9fc8e390f0e1","components/shared/ReasonCard/ReasonCard.d.ts":"26c1f89fa2de","components/shared/ReasonCard/ReasonCard.prompt.md":"2c7d6e95b338","components/shared/ResourceCard/ResourceCard.jsx":"b52a018d0610","components/shared/ResourceCard/ResourceCard.d.ts":"b1984adba59b","components/shared/ResourceCard/ResourceCard.prompt.md":"5bfdde06cd81","components/motion/Reveal/Reveal.jsx":"d227f412990a","components/motion/Reveal/Reveal.d.ts":"8496bd828a19","components/motion/Reveal/Reveal.prompt.md":"5c315082ac46","components/general/SectionHeading/SectionHeading.jsx":"6d94e6dff14b","components/general/SectionHeading/SectionHeading.d.ts":"88746b0ae28e","components/general/SectionHeading/SectionHeading.prompt.md":"5d4ac91e8bac","components/panel/StatCard/StatCard.jsx":"682a165fb204","components/panel/StatCard/StatCard.d.ts":"f1adbf050215","components/panel/StatCard/StatCard.prompt.md":"3f4ca2e6b129","components/panel/StatusBadge/StatusBadge.jsx":"5ec6785c145f","components/panel/StatusBadge/StatusBadge.d.ts":"29c0d16e1aa6","components/panel/StatusBadge/StatusBadge.prompt.md":"75a6ec6c38fd","components/motion/StickyBar/StickyBar.jsx":"be47005226d6","components/motion/StickyBar/StickyBar.d.ts":"6fe53d14c361","components/motion/StickyBar/StickyBar.prompt.md":"9ae64a1871ee","components/panel/TabPills/TabPills.jsx":"e6886d49f740","components/panel/TabPills/TabPills.d.ts":"9fc6f185ad17","components/panel/TabPills/TabPills.prompt.md":"df02158e44e2","components/shared/TherapistCard/TherapistCard.jsx":"05b23e6120e7","components/shared/TherapistCard/TherapistCard.d.ts":"e94e392bb6ae","components/shared/TherapistCard/TherapistCard.prompt.md":"3b0d2876c646","components/shared/TherapistRow/TherapistRow.jsx":"470f7b4390a4","components/shared/TherapistRow/TherapistRow.d.ts":"538e134b1fa9","components/shared/TherapistRow/TherapistRow.prompt.md":"eb0734e06eff","components/panel/Toggle/Toggle.jsx":"8a22c2919ae1","components/panel/Toggle/Toggle.d.ts":"3a79f0b99e03","components/panel/Toggle/Toggle.prompt.md":"2a3ba5e5de66"},"inlinedExternals":["@floating-ui/core","@floating-ui/dom","@floating-ui/react","@floating-ui/react-dom","@floating-ui/utils","@headlessui/react","@heroicons/react","@react-aria/focus","@react-aria/interactions","@tanstack/react-virtual","@tanstack/virtual-core","class-variance-authority","clsx","framer-motion","motion","motion-dom","motion-utils","psihointegritet-ds","react-aria","react-stately","tabbable","tailwind-merge","use-sync-external-store"],"builtBy":"cc-design-sync"} */
var PsihointegritetUI = (() => {
  var __create = Object.create;
  var __defProp = Object.defineProperty;
  var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
  var __getOwnPropNames = Object.getOwnPropertyNames;
  var __getProtoOf = Object.getPrototypeOf;
  var __hasOwnProp = Object.prototype.hasOwnProperty;
  var __require = /* @__PURE__ */ ((x9) => typeof require !== "undefined" ? require : typeof Proxy !== "undefined" ? new Proxy(x9, {
    get: (a21, b8) => (typeof require !== "undefined" ? require : a21)[b8]
  }) : x9)(function(x9) {
    if (typeof require !== "undefined") return require.apply(this, arguments);
    throw Error('Dynamic require of "' + x9 + '" is not supported');
  });
  var __esm = (fn, res, err) => function __init() {
    if (err) throw err[0];
    try {
      return fn && (res = (0, fn[__getOwnPropNames(fn)[0]])(fn = 0)), res;
    } catch (e8) {
      throw err = [e8], e8;
    }
  };
  var __commonJS = (cb, mod) => function __require2() {
    try {
      return mod || (0, cb[__getOwnPropNames(cb)[0]])((mod = { exports: {} }).exports, mod), mod.exports;
    } catch (e8) {
      throw mod = 0, e8;
    }
  };
  var __export = (target, all) => {
    for (var name in all)
      __defProp(target, name, { get: all[name], enumerable: true });
  };
  var __copyProps = (to, from, except, desc) => {
    if (from && typeof from === "object" || typeof from === "function") {
      for (let key of __getOwnPropNames(from))
        if (!__hasOwnProp.call(to, key) && key !== except)
          __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
    }
    return to;
  };
  var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
    // If the importer is in node compatibility mode or this is not an ESM
    // file that has been converted to a CommonJS file using a Babel-
    // compatible transform (i.e. "__esModule" has not been set), then set
    // "default" to the CommonJS "module.exports" for node compatibility.
    isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
    mod
  ));
  var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);

  // <define:import.meta.env>
  var init_define_import_meta_env = __esm({
    "<define:import.meta.env>"() {
    }
  });

  // shim:react-shim
  var require_react_shim = __commonJS({
    "shim:react-shim"(exports, module) {
      init_define_import_meta_env();
      var R5 = window.React;
      function np(p8, k10) {
        var o16 = {};
        for (var x9 in p8) if (x9 !== "children") o16[x9] = p8[x9];
        if (k10 !== void 0) o16.key = k10;
        return o16;
      }
      function jsx3(t12, p8, k10) {
        var c14 = p8 && p8.children;
        return c14 === void 0 ? R5.createElement(t12, np(p8, k10)) : R5.createElement(t12, np(p8, k10), c14);
      }
      function jsxs2(t12, p8, k10) {
        return R5.createElement.apply(R5, [t12, np(p8, k10)].concat(p8.children));
      }
      module.exports = R5;
      module.exports.jsx = jsx3;
      module.exports.jsxs = jsxs2;
      module.exports.jsxDEV = function(t12, p8, k10, s15) {
        return (s15 ? jsxs2 : jsx3)(t12, p8, k10);
      };
      module.exports.Fragment = R5.Fragment;
    }
  });

  // shim:react-dom-shim
  var require_react_dom_shim = __commonJS({
    "shim:react-dom-shim"(exports, module) {
      init_define_import_meta_env();
      var D8 = window.ReactDOM;
      var n13 = function() {
      };
      module.exports = Object.assign({ preload: n13, preinit: n13, preconnect: n13, prefetchDNS: n13, preloadModule: n13, preinitModule: n13 }, D8);
    }
  });

  // frontend/node_modules/use-sync-external-store/cjs/use-sync-external-store-with-selector.development.js
  var require_use_sync_external_store_with_selector_development = __commonJS({
    "frontend/node_modules/use-sync-external-store/cjs/use-sync-external-store-with-selector.development.js"(exports) {
      "use strict";
      init_define_import_meta_env();
      (function() {
        function is(x9, y6) {
          return x9 === y6 && (0 !== x9 || 1 / x9 === 1 / y6) || x9 !== x9 && y6 !== y6;
        }
        "undefined" !== typeof __REACT_DEVTOOLS_GLOBAL_HOOK__ && "function" === typeof __REACT_DEVTOOLS_GLOBAL_HOOK__.registerInternalModuleStart && __REACT_DEVTOOLS_GLOBAL_HOOK__.registerInternalModuleStart(Error());
        var React7 = require_react_shim(), objectIs = "function" === typeof Object.is ? Object.is : is, useSyncExternalStore = React7.useSyncExternalStore, useRef7 = React7.useRef, useEffect8 = React7.useEffect, useMemo7 = React7.useMemo, useDebugValue = React7.useDebugValue;
        exports.useSyncExternalStoreWithSelector = function(subscribe, getSnapshot, getServerSnapshot, selector, isEqual) {
          var instRef = useRef7(null);
          if (null === instRef.current) {
            var inst = { hasValue: false, value: null };
            instRef.current = inst;
          } else inst = instRef.current;
          instRef = useMemo7(
            function() {
              function memoizedSelector(nextSnapshot) {
                if (!hasMemo) {
                  hasMemo = true;
                  memoizedSnapshot = nextSnapshot;
                  nextSnapshot = selector(nextSnapshot);
                  if (void 0 !== isEqual && inst.hasValue) {
                    var currentSelection = inst.value;
                    if (isEqual(currentSelection, nextSnapshot))
                      return memoizedSelection = currentSelection;
                  }
                  return memoizedSelection = nextSnapshot;
                }
                currentSelection = memoizedSelection;
                if (objectIs(memoizedSnapshot, nextSnapshot))
                  return currentSelection;
                var nextSelection = selector(nextSnapshot);
                if (void 0 !== isEqual && isEqual(currentSelection, nextSelection))
                  return memoizedSnapshot = nextSnapshot, currentSelection;
                memoizedSnapshot = nextSnapshot;
                return memoizedSelection = nextSelection;
              }
              var hasMemo = false, memoizedSnapshot, memoizedSelection, maybeGetServerSnapshot = void 0 === getServerSnapshot ? null : getServerSnapshot;
              return [
                function() {
                  return memoizedSelector(getSnapshot());
                },
                null === maybeGetServerSnapshot ? void 0 : function() {
                  return memoizedSelector(maybeGetServerSnapshot());
                }
              ];
            },
            [getSnapshot, getServerSnapshot, selector, isEqual]
          );
          var value = useSyncExternalStore(subscribe, instRef[0], instRef[1]);
          useEffect8(
            function() {
              inst.hasValue = true;
              inst.value = value;
            },
            [value]
          );
          useDebugValue(value);
          return value;
        };
        "undefined" !== typeof __REACT_DEVTOOLS_GLOBAL_HOOK__ && "function" === typeof __REACT_DEVTOOLS_GLOBAL_HOOK__.registerInternalModuleStop && __REACT_DEVTOOLS_GLOBAL_HOOK__.registerInternalModuleStop(Error());
      })();
    }
  });

  // frontend/node_modules/use-sync-external-store/with-selector.js
  var require_with_selector = __commonJS({
    "frontend/node_modules/use-sync-external-store/with-selector.js"(exports, module) {
      "use strict";
      init_define_import_meta_env();
      if (false) {
        module.exports = null;
      } else {
        module.exports = require_use_sync_external_store_with_selector_development();
      }
    }
  });

  // ds-bundle/.pkg-entry.mjs
  var pkg_entry_exports = {};
  __export(pkg_entry_exports, {
    Accordion: () => Accordion,
    AnimatedCtaContent: () => AnimatedCtaContent,
    AnimatedCtaLink: () => AnimatedCtaLink,
    ArrowLink: () => ArrowLink,
    BackToSiteButton: () => BackToSiteButton,
    BackToSiteMenuItem: () => BackToSiteMenuItem,
    ButtonLink: () => ButtonLink,
    Chip: () => Chip,
    ConfirmModal: () => ConfirmModal,
    EmptyDashedCard: () => EmptyDashedCard,
    Eyebrow: () => Eyebrow,
    JsonLd: () => JsonLd,
    KV: () => KV,
    LogoutAvatarMenu: () => LogoutAvatarMenu,
    MonogramAvatar: () => MonogramAvatar,
    PageHero: () => PageHero,
    ProgressBar: () => ProgressBar,
    ReasonCard: () => ReasonCard,
    ResourceCard: () => ResourceCard,
    Reveal: () => Reveal,
    SectionHeading: () => SectionHeading,
    StatCard: () => StatCard,
    StatusBadge: () => StatusBadge,
    StickyBar: () => StickyBar,
    TabPills: () => TabPills,
    TherapistCard: () => TherapistCard,
    TherapistRow: () => TherapistRow,
    Toggle: () => Toggle,
    animatedCtaClassName: () => animatedCtaClassName,
    buttonLinkVariants: () => buttonLinkVariants,
    useClerk: () => useClerk,
    useUser: () => useUser
  });
  init_define_import_meta_env();

  // frontend/node_modules/psihointegritet-ds/src/_shims/clerk.tsx
  init_define_import_meta_env();
  function useClerk() {
    return { signOut: () => {
    } };
  }
  function useUser() {
    return { user: { hasImage: false, imageUrl: "" } };
  }

  // frontend/node_modules/psihointegritet-ds/src/_shims/next-image.tsx
  init_define_import_meta_env();
  var React2 = __toESM(require_react_shim());
  function Image({ src, alt, fill, priority, sizes, style, ...rest }) {
    const resolvedSrc = typeof src === "string" ? src : src?.src ?? "";
    const mergedStyle = fill ? { position: "absolute", inset: 0, width: "100%", height: "100%", objectFit: "cover", ...style } : style;
    return /* @__PURE__ */ React2.createElement("img", { src: resolvedSrc, alt, style: mergedStyle, ...rest });
  }

  // frontend/node_modules/psihointegritet-ds/src/_shims/next-link.tsx
  init_define_import_meta_env();
  var React3 = __toESM(require_react_shim());
  function Link({ href, children, ...rest }) {
    const resolvedHref = typeof href === "string" ? href : href?.pathname ?? "#";
    return /* @__PURE__ */ React3.createElement("a", { href: resolvedHref, ...rest }, children);
  }

  // frontend/node_modules/psihointegritet-ds/src/motion/reveal.tsx
  init_define_import_meta_env();

  // frontend/node_modules/motion/dist/es/react.mjs
  init_define_import_meta_env();

  // frontend/node_modules/framer-motion/dist/es/index.mjs
  init_define_import_meta_env();

  // frontend/node_modules/framer-motion/dist/es/context/LayoutGroupContext.mjs
  init_define_import_meta_env();
  var import_react = __toESM(require_react_shim(), 1);
  var LayoutGroupContext = (0, import_react.createContext)({});

  // frontend/node_modules/framer-motion/dist/es/utils/use-constant.mjs
  init_define_import_meta_env();
  var import_react2 = __toESM(require_react_shim(), 1);
  function useConstant(init) {
    const ref = (0, import_react2.useRef)(null);
    if (ref.current === null) {
      ref.current = init();
    }
    return ref.current;
  }

  // frontend/node_modules/framer-motion/dist/es/utils/use-isomorphic-effect.mjs
  init_define_import_meta_env();
  var import_react3 = __toESM(require_react_shim(), 1);

  // frontend/node_modules/framer-motion/dist/es/utils/is-browser.mjs
  init_define_import_meta_env();
  var isBrowser = typeof window !== "undefined";

  // frontend/node_modules/framer-motion/dist/es/utils/use-isomorphic-effect.mjs
  var useIsomorphicLayoutEffect = isBrowser ? import_react3.useLayoutEffect : import_react3.useEffect;

  // frontend/node_modules/framer-motion/dist/es/context/PresenceContext.mjs
  init_define_import_meta_env();
  var import_react4 = __toESM(require_react_shim(), 1);
  var PresenceContext = /* @__PURE__ */ (0, import_react4.createContext)(null);

  // frontend/node_modules/motion-dom/dist/es/index.mjs
  init_define_import_meta_env();

  // frontend/node_modules/motion-dom/dist/es/animation/AsyncMotionValueAnimation.mjs
  init_define_import_meta_env();

  // frontend/node_modules/motion-utils/dist/es/index.mjs
  init_define_import_meta_env();

  // frontend/node_modules/motion-utils/dist/es/array.mjs
  init_define_import_meta_env();
  function addUniqueItem(arr, item) {
    if (arr.indexOf(item) === -1)
      arr.push(item);
  }
  function removeItem(arr, item) {
    const index3 = arr.indexOf(item);
    if (index3 > -1)
      arr.splice(index3, 1);
  }

  // frontend/node_modules/motion-utils/dist/es/clamp.mjs
  init_define_import_meta_env();
  var clamp = (min2, max2, v6) => {
    if (v6 > max2)
      return max2;
    if (v6 < min2)
      return min2;
    return v6;
  };

  // frontend/node_modules/motion-utils/dist/es/errors.mjs
  init_define_import_meta_env();

  // frontend/node_modules/motion-utils/dist/es/format-error-message.mjs
  init_define_import_meta_env();
  function formatErrorMessage(message, errorCode) {
    return errorCode ? `${message}. For more information and steps for solving, visit https://motion.dev/troubleshooting/${errorCode}` : message;
  }

  // frontend/node_modules/motion-utils/dist/es/errors.mjs
  var warning = () => {
  };
  var invariant = () => {
  };
  if (typeof process !== "undefined" && true) {
    warning = (check, message, errorCode) => {
      if (!check && typeof console !== "undefined") {
        console.warn(formatErrorMessage(message, errorCode));
      }
    };
    invariant = (check, message, errorCode) => {
      if (!check) {
        throw new Error(formatErrorMessage(message, errorCode));
      }
    };
  }

  // frontend/node_modules/motion-utils/dist/es/global-config.mjs
  init_define_import_meta_env();
  var MotionGlobalConfig = {};

  // frontend/node_modules/motion-utils/dist/es/is-numerical-string.mjs
  init_define_import_meta_env();
  var isNumericalString = (v6) => /^-?(?:\d+(?:\.\d+)?|\.\d+)$/u.test(v6);

  // frontend/node_modules/motion-utils/dist/es/is-object.mjs
  init_define_import_meta_env();
  var isObject = (value) => typeof value === "object" && value !== null;

  // frontend/node_modules/motion-utils/dist/es/is-zero-value-string.mjs
  init_define_import_meta_env();
  var isZeroValueString = (v6) => /^0[^.\s]+$/u.test(v6);

  // frontend/node_modules/motion-utils/dist/es/memo.mjs
  init_define_import_meta_env();
  // @__NO_SIDE_EFFECTS__
  function memo(callback) {
    let result;
    return () => {
      if (result === void 0)
        result = callback();
      return result;
    };
  }

  // frontend/node_modules/motion-utils/dist/es/noop.mjs
  init_define_import_meta_env();
  var noop = /* @__NO_SIDE_EFFECTS__ */ (any) => any;

  // frontend/node_modules/motion-utils/dist/es/pipe.mjs
  init_define_import_meta_env();
  var pipe = (...transformers) => transformers.reduce((a21, b8) => (v6) => b8(a21(v6)));

  // frontend/node_modules/motion-utils/dist/es/progress.mjs
  init_define_import_meta_env();
  var progress = /* @__NO_SIDE_EFFECTS__ */ (from, to, value) => {
    const range = to - from;
    return range ? (value - from) / range : 1;
  };

  // frontend/node_modules/motion-utils/dist/es/subscription-manager.mjs
  init_define_import_meta_env();
  var SubscriptionManager = class {
    constructor() {
      this.subscriptions = [];
    }
    add(handler) {
      addUniqueItem(this.subscriptions, handler);
      return () => removeItem(this.subscriptions, handler);
    }
    notify(a21, b8, c14) {
      const numSubscriptions = this.subscriptions.length;
      if (!numSubscriptions)
        return;
      if (numSubscriptions === 1) {
        this.subscriptions[0](a21, b8, c14);
      } else {
        for (let i13 = 0; i13 < numSubscriptions; i13++) {
          const handler = this.subscriptions[i13];
          handler && handler(a21, b8, c14);
        }
      }
    }
    getSize() {
      return this.subscriptions.length;
    }
    clear() {
      this.subscriptions.length = 0;
    }
  };

  // frontend/node_modules/motion-utils/dist/es/time-conversion.mjs
  init_define_import_meta_env();
  var secondsToMilliseconds = /* @__NO_SIDE_EFFECTS__ */ (seconds) => seconds * 1e3;
  var millisecondsToSeconds = /* @__NO_SIDE_EFFECTS__ */ (milliseconds) => milliseconds / 1e3;

  // frontend/node_modules/motion-utils/dist/es/velocity-per-second.mjs
  init_define_import_meta_env();
  var velocityPerSecond = /* @__NO_SIDE_EFFECTS__ */ (velocity, frameDuration) => frameDuration ? velocity * (1e3 / frameDuration) : 0;

  // frontend/node_modules/motion-utils/dist/es/warn-once.mjs
  init_define_import_meta_env();
  var warned = /* @__PURE__ */ new Set();
  function warnOnce(condition, message, errorCode) {
    if (condition || warned.has(message))
      return;
    console.warn(formatErrorMessage(message, errorCode));
    warned.add(message);
  }

  // frontend/node_modules/motion-utils/dist/es/easing/anticipate.mjs
  init_define_import_meta_env();

  // frontend/node_modules/motion-utils/dist/es/easing/back.mjs
  init_define_import_meta_env();

  // frontend/node_modules/motion-utils/dist/es/easing/cubic-bezier.mjs
  init_define_import_meta_env();
  var calcBezier = (t12, a1, a22) => (((1 - 3 * a22 + 3 * a1) * t12 + (3 * a22 - 6 * a1)) * t12 + 3 * a1) * t12;
  var subdivisionPrecision = 1e-7;
  var subdivisionMaxIterations = 12;
  function binarySubdivide(x9, lowerBound, upperBound, mX1, mX2) {
    let currentX;
    let currentT;
    let i13 = 0;
    do {
      currentT = lowerBound + (upperBound - lowerBound) / 2;
      currentX = calcBezier(currentT, mX1, mX2) - x9;
      if (currentX > 0) {
        upperBound = currentT;
      } else {
        lowerBound = currentT;
      }
    } while (Math.abs(currentX) > subdivisionPrecision && ++i13 < subdivisionMaxIterations);
    return currentT;
  }
  // @__NO_SIDE_EFFECTS__
  function cubicBezier(mX1, mY1, mX2, mY2) {
    if (mX1 === mY1 && mX2 === mY2)
      return noop;
    const getTForX = (aX) => binarySubdivide(aX, 0, 1, mX1, mX2);
    return (t12) => t12 === 0 || t12 === 1 ? t12 : calcBezier(getTForX(t12), mY1, mY2);
  }

  // frontend/node_modules/motion-utils/dist/es/easing/modifiers/mirror.mjs
  init_define_import_meta_env();
  var mirrorEasing = /* @__NO_SIDE_EFFECTS__ */ (easing) => (p8) => p8 <= 0.5 ? easing(2 * p8) / 2 : (2 - easing(2 * (1 - p8))) / 2;

  // frontend/node_modules/motion-utils/dist/es/easing/modifiers/reverse.mjs
  init_define_import_meta_env();
  var reverseEasing = /* @__NO_SIDE_EFFECTS__ */ (easing) => (p8) => 1 - easing(1 - p8);

  // frontend/node_modules/motion-utils/dist/es/easing/back.mjs
  var backOut = /* @__PURE__ */ cubicBezier(0.33, 1.53, 0.69, 0.99);
  var backIn = /* @__PURE__ */ reverseEasing(backOut);
  var backInOut = /* @__PURE__ */ mirrorEasing(backIn);

  // frontend/node_modules/motion-utils/dist/es/easing/anticipate.mjs
  var anticipate = (p8) => p8 >= 1 ? 1 : (p8 *= 2) < 1 ? 0.5 * backIn(p8) : 0.5 * (2 - Math.pow(2, -10 * (p8 - 1)));

  // frontend/node_modules/motion-utils/dist/es/easing/circ.mjs
  init_define_import_meta_env();
  var circIn = (p8) => 1 - Math.sin(Math.acos(p8));
  var circOut = reverseEasing(circIn);
  var circInOut = mirrorEasing(circIn);

  // frontend/node_modules/motion-utils/dist/es/easing/ease.mjs
  init_define_import_meta_env();
  var easeIn = /* @__PURE__ */ cubicBezier(0.42, 0, 1, 1);
  var easeOut = /* @__PURE__ */ cubicBezier(0, 0, 0.58, 1);
  var easeInOut = /* @__PURE__ */ cubicBezier(0.42, 0, 0.58, 1);

  // frontend/node_modules/motion-utils/dist/es/easing/utils/is-easing-array.mjs
  init_define_import_meta_env();
  var isEasingArray = /* @__NO_SIDE_EFFECTS__ */ (ease2) => {
    return Array.isArray(ease2) && typeof ease2[0] !== "number";
  };

  // frontend/node_modules/motion-utils/dist/es/easing/utils/is-bezier-definition.mjs
  init_define_import_meta_env();
  var isBezierDefinition = /* @__NO_SIDE_EFFECTS__ */ (easing) => Array.isArray(easing) && typeof easing[0] === "number";

  // frontend/node_modules/motion-utils/dist/es/easing/utils/map.mjs
  init_define_import_meta_env();
  var easingLookup = {
    linear: noop,
    easeIn,
    easeInOut,
    easeOut,
    circIn,
    circInOut,
    circOut,
    backIn,
    backInOut,
    backOut,
    anticipate
  };
  var isValidEasing = (easing) => {
    return typeof easing === "string";
  };
  var easingDefinitionToFunction = (definition) => {
    if (isBezierDefinition(definition)) {
      invariant(definition.length === 4, `Cubic bezier arrays must contain four numerical values.`, "cubic-bezier-length");
      const [x1, y1, x22, y22] = definition;
      return cubicBezier(x1, y1, x22, y22);
    } else if (isValidEasing(definition)) {
      invariant(easingLookup[definition] !== void 0, `Invalid easing type '${definition}'`, "invalid-easing-type");
      return easingLookup[definition];
    }
    return definition;
  };

  // frontend/node_modules/motion-dom/dist/es/frameloop/sync-time.mjs
  init_define_import_meta_env();

  // frontend/node_modules/motion-dom/dist/es/frameloop/frame.mjs
  init_define_import_meta_env();

  // frontend/node_modules/motion-dom/dist/es/frameloop/batcher.mjs
  init_define_import_meta_env();

  // frontend/node_modules/motion-dom/dist/es/frameloop/order.mjs
  init_define_import_meta_env();
  var stepsOrder = [
    "setup",
    // Compute
    "read",
    // Read
    "resolveKeyframes",
    // Write/Read/Write/Read
    "preUpdate",
    // Compute
    "update",
    // Compute
    "preRender",
    // Compute
    "render",
    // Write
    "postRender"
    // Compute
  ];

  // frontend/node_modules/motion-dom/dist/es/frameloop/render-step.mjs
  init_define_import_meta_env();
  function createRenderStep(runNextFrame) {
    let thisFrame = /* @__PURE__ */ new Set();
    let nextFrame = /* @__PURE__ */ new Set();
    let isProcessing = false;
    let flushNextFrame = false;
    const toKeepAlive = /* @__PURE__ */ new WeakSet();
    let latestFrameData = {
      delta: 0,
      timestamp: 0,
      isProcessing: false
    };
    function triggerCallback(callback) {
      if (toKeepAlive.has(callback)) {
        step.schedule(callback);
        runNextFrame();
      }
      callback(latestFrameData);
    }
    const step = {
      /**
       * Schedule a process to run on the next frame.
       */
      schedule: (callback, keepAlive = false, immediate = false) => {
        const addToCurrentFrame = immediate && isProcessing;
        const queue = addToCurrentFrame ? thisFrame : nextFrame;
        if (keepAlive)
          toKeepAlive.add(callback);
        queue.add(callback);
        return callback;
      },
      /**
       * Cancel the provided callback from running on the next frame.
       */
      cancel: (callback) => {
        nextFrame.delete(callback);
        toKeepAlive.delete(callback);
      },
      /**
       * Execute all schedule callbacks.
       */
      process: (frameData2) => {
        latestFrameData = frameData2;
        if (isProcessing) {
          flushNextFrame = true;
          return;
        }
        isProcessing = true;
        const prevFrame = thisFrame;
        thisFrame = nextFrame;
        nextFrame = prevFrame;
        thisFrame.forEach(triggerCallback);
        thisFrame.clear();
        isProcessing = false;
        if (flushNextFrame) {
          flushNextFrame = false;
          step.process(frameData2);
        }
      }
    };
    return step;
  }

  // frontend/node_modules/motion-dom/dist/es/frameloop/batcher.mjs
  var maxElapsed = 40;
  function createRenderBatcher(scheduleNextBatch, allowKeepAlive) {
    let runNextFrame = false;
    let useDefaultElapsed = true;
    const state = {
      delta: 0,
      timestamp: 0,
      isProcessing: false
    };
    const flagRunNextFrame = () => runNextFrame = true;
    const steps = stepsOrder.reduce((acc, key) => {
      acc[key] = createRenderStep(flagRunNextFrame);
      return acc;
    }, {});
    const { setup, read, resolveKeyframes, preUpdate, update, preRender, render, postRender } = steps;
    const processBatch = () => {
      const useManualTiming = MotionGlobalConfig.useManualTiming;
      const timestamp = useManualTiming ? state.timestamp : performance.now();
      runNextFrame = false;
      if (!useManualTiming) {
        state.delta = useDefaultElapsed ? 1e3 / 60 : Math.max(Math.min(timestamp - state.timestamp, maxElapsed), 1);
      }
      state.timestamp = timestamp;
      state.isProcessing = true;
      setup.process(state);
      read.process(state);
      resolveKeyframes.process(state);
      preUpdate.process(state);
      update.process(state);
      preRender.process(state);
      render.process(state);
      postRender.process(state);
      state.isProcessing = false;
      if (runNextFrame && allowKeepAlive) {
        useDefaultElapsed = false;
        scheduleNextBatch(processBatch);
      }
    };
    const wake = () => {
      runNextFrame = true;
      useDefaultElapsed = true;
      if (!state.isProcessing) {
        scheduleNextBatch(processBatch);
      }
    };
    const schedule = stepsOrder.reduce((acc, key) => {
      const step = steps[key];
      acc[key] = (process2, keepAlive = false, immediate = false) => {
        if (!runNextFrame)
          wake();
        return step.schedule(process2, keepAlive, immediate);
      };
      return acc;
    }, {});
    const cancel = (process2) => {
      for (let i13 = 0; i13 < stepsOrder.length; i13++) {
        steps[stepsOrder[i13]].cancel(process2);
      }
    };
    return { schedule, cancel, state, steps };
  }

  // frontend/node_modules/motion-dom/dist/es/frameloop/frame.mjs
  var { schedule: frame, cancel: cancelFrame, state: frameData, steps: frameSteps } = /* @__PURE__ */ createRenderBatcher(typeof requestAnimationFrame !== "undefined" ? requestAnimationFrame : noop, true);

  // frontend/node_modules/motion-dom/dist/es/frameloop/sync-time.mjs
  var now;
  function clearTime() {
    now = void 0;
  }
  var time = {
    now: () => {
      if (now === void 0) {
        time.set(frameData.isProcessing || MotionGlobalConfig.useManualTiming ? frameData.timestamp : performance.now());
      }
      return now;
    },
    set: (newTime) => {
      now = newTime;
      queueMicrotask(clearTime);
    }
  };

  // frontend/node_modules/motion-dom/dist/es/animation/JSAnimation.mjs
  init_define_import_meta_env();

  // frontend/node_modules/motion-dom/dist/es/utils/mix/index.mjs
  init_define_import_meta_env();

  // frontend/node_modules/motion-dom/dist/es/utils/mix/complex.mjs
  init_define_import_meta_env();

  // frontend/node_modules/motion-dom/dist/es/animation/utils/is-css-variable.mjs
  init_define_import_meta_env();
  var checkStringStartsWith = (token) => (key) => typeof key === "string" && key.startsWith(token);
  var isCSSVariableName = /* @__PURE__ */ checkStringStartsWith("--");
  var startsAsVariableToken = /* @__PURE__ */ checkStringStartsWith("var(--");
  var isCSSVariableToken = (value) => {
    const startsWithToken = startsAsVariableToken(value);
    if (!startsWithToken)
      return false;
    return singleCssVariableRegex.test(value.split("/*")[0].trim());
  };
  var singleCssVariableRegex = /var\(--(?:[\w-]+\s*|[\w-]+\s*,(?:\s*[^)(\s]|\s*\((?:[^)(]|\([^)(]*\))*\))+\s*)\)$/iu;
  function containsCSSVariable(value) {
    if (typeof value !== "string")
      return false;
    return value.split("/*")[0].includes("var(--");
  }

  // frontend/node_modules/motion-dom/dist/es/value/types/color/index.mjs
  init_define_import_meta_env();

  // frontend/node_modules/motion-dom/dist/es/value/types/color/hex.mjs
  init_define_import_meta_env();

  // frontend/node_modules/motion-dom/dist/es/value/types/color/rgba.mjs
  init_define_import_meta_env();

  // frontend/node_modules/motion-dom/dist/es/value/types/numbers/index.mjs
  init_define_import_meta_env();
  var number = {
    test: (v6) => typeof v6 === "number",
    parse: parseFloat,
    transform: (v6) => v6
  };
  var alpha = {
    ...number,
    transform: (v6) => clamp(0, 1, v6)
  };
  var scale = {
    ...number,
    default: 1
  };

  // frontend/node_modules/motion-dom/dist/es/value/types/utils/sanitize.mjs
  init_define_import_meta_env();
  var sanitize = (v6) => Math.round(v6 * 1e5) / 1e5;

  // frontend/node_modules/motion-dom/dist/es/value/types/color/utils.mjs
  init_define_import_meta_env();

  // frontend/node_modules/motion-dom/dist/es/value/types/utils/float-regex.mjs
  init_define_import_meta_env();
  var floatRegex = /-?(?:\d+(?:\.\d+)?|\.\d+)/gu;

  // frontend/node_modules/motion-dom/dist/es/value/types/utils/is-nullish.mjs
  init_define_import_meta_env();
  function isNullish(v6) {
    return v6 == null;
  }

  // frontend/node_modules/motion-dom/dist/es/value/types/utils/single-color-regex.mjs
  init_define_import_meta_env();
  var singleColorRegex = /^(?:#[\da-f]{3,8}|(?:rgb|hsl)a?\((?:-?[\d.]+%?[,\s]+){2}-?[\d.]+%?\s*(?:[,/]\s*)?(?:\b\d+(?:\.\d+)?|\.\d+)?%?\))$/iu;

  // frontend/node_modules/motion-dom/dist/es/value/types/color/utils.mjs
  var isColorString = (type, testProp) => (v6) => {
    return Boolean(typeof v6 === "string" && singleColorRegex.test(v6) && v6.startsWith(type) || testProp && !isNullish(v6) && Object.prototype.hasOwnProperty.call(v6, testProp));
  };
  var splitColor = (aName, bName, cName) => (v6) => {
    if (typeof v6 !== "string")
      return v6;
    const [a21, b8, c14, alpha2] = v6.match(floatRegex);
    return {
      [aName]: parseFloat(a21),
      [bName]: parseFloat(b8),
      [cName]: parseFloat(c14),
      alpha: alpha2 !== void 0 ? parseFloat(alpha2) : 1
    };
  };

  // frontend/node_modules/motion-dom/dist/es/value/types/color/rgba.mjs
  var clampRgbUnit = (v6) => clamp(0, 255, v6);
  var rgbUnit = {
    ...number,
    transform: (v6) => Math.round(clampRgbUnit(v6))
  };
  var rgba = {
    test: /* @__PURE__ */ isColorString("rgb", "red"),
    parse: /* @__PURE__ */ splitColor("red", "green", "blue"),
    transform: ({ red, green, blue, alpha: alpha$1 = 1 }) => "rgba(" + rgbUnit.transform(red) + ", " + rgbUnit.transform(green) + ", " + rgbUnit.transform(blue) + ", " + sanitize(alpha.transform(alpha$1)) + ")"
  };

  // frontend/node_modules/motion-dom/dist/es/value/types/color/hex.mjs
  function parseHex(v6) {
    let r16 = "";
    let g3 = "";
    let b8 = "";
    let a21 = "";
    if (v6.length > 5) {
      r16 = v6.substring(1, 3);
      g3 = v6.substring(3, 5);
      b8 = v6.substring(5, 7);
      a21 = v6.substring(7, 9);
    } else {
      r16 = v6.substring(1, 2);
      g3 = v6.substring(2, 3);
      b8 = v6.substring(3, 4);
      a21 = v6.substring(4, 5);
      r16 += r16;
      g3 += g3;
      b8 += b8;
      a21 += a21;
    }
    return {
      red: parseInt(r16, 16),
      green: parseInt(g3, 16),
      blue: parseInt(b8, 16),
      alpha: a21 ? parseInt(a21, 16) / 255 : 1
    };
  }
  var hex = {
    test: /* @__PURE__ */ isColorString("#"),
    parse: parseHex,
    transform: rgba.transform
  };

  // frontend/node_modules/motion-dom/dist/es/value/types/color/hsla.mjs
  init_define_import_meta_env();

  // frontend/node_modules/motion-dom/dist/es/value/types/numbers/units.mjs
  init_define_import_meta_env();
  var createUnitType = /* @__NO_SIDE_EFFECTS__ */ (unit) => ({
    test: (v6) => typeof v6 === "string" && v6.endsWith(unit) && v6.split(" ").length === 1,
    parse: parseFloat,
    transform: (v6) => `${v6}${unit}`
  });
  var degrees = /* @__PURE__ */ createUnitType("deg");
  var percent = /* @__PURE__ */ createUnitType("%");
  var px = /* @__PURE__ */ createUnitType("px");
  var vh = /* @__PURE__ */ createUnitType("vh");
  var vw = /* @__PURE__ */ createUnitType("vw");
  var progressPercentage = /* @__PURE__ */ (() => ({
    ...percent,
    parse: (v6) => percent.parse(v6) / 100,
    transform: (v6) => percent.transform(v6 * 100)
  }))();

  // frontend/node_modules/motion-dom/dist/es/value/types/color/hsla.mjs
  var hsla = {
    test: /* @__PURE__ */ isColorString("hsl", "hue"),
    parse: /* @__PURE__ */ splitColor("hue", "saturation", "lightness"),
    transform: ({ hue, saturation, lightness, alpha: alpha$1 = 1 }) => {
      return "hsla(" + Math.round(hue) + ", " + percent.transform(sanitize(saturation)) + ", " + percent.transform(sanitize(lightness)) + ", " + sanitize(alpha.transform(alpha$1)) + ")";
    }
  };

  // frontend/node_modules/motion-dom/dist/es/value/types/color/index.mjs
  var color = {
    test: (v6) => rgba.test(v6) || hex.test(v6) || hsla.test(v6),
    parse: (v6) => {
      if (rgba.test(v6)) {
        return rgba.parse(v6);
      } else if (hsla.test(v6)) {
        return hsla.parse(v6);
      } else {
        return hex.parse(v6);
      }
    },
    transform: (v6) => {
      return typeof v6 === "string" ? v6 : v6.hasOwnProperty("red") ? rgba.transform(v6) : hsla.transform(v6);
    },
    getAnimatableNone: (v6) => {
      const parsed = color.parse(v6);
      parsed.alpha = 0;
      return color.transform(parsed);
    }
  };

  // frontend/node_modules/motion-dom/dist/es/value/types/complex/index.mjs
  init_define_import_meta_env();

  // frontend/node_modules/motion-dom/dist/es/value/types/utils/color-regex.mjs
  init_define_import_meta_env();
  var colorRegex = /(?:#[\da-f]{3,8}|(?:rgb|hsl)a?\((?:-?[\d.]+%?[,\s]+){2}-?[\d.]+%?\s*(?:[,/]\s*)?(?:\b\d+(?:\.\d+)?|\.\d+)?%?\))/giu;

  // frontend/node_modules/motion-dom/dist/es/value/types/complex/index.mjs
  function test(v6) {
    return isNaN(v6) && typeof v6 === "string" && (v6.match(floatRegex)?.length || 0) + (v6.match(colorRegex)?.length || 0) > 0;
  }
  var NUMBER_TOKEN = "number";
  var COLOR_TOKEN = "color";
  var VAR_TOKEN = "var";
  var VAR_FUNCTION_TOKEN = "var(";
  var SPLIT_TOKEN = "${}";
  var complexRegex = /var\s*\(\s*--(?:[\w-]+\s*|[\w-]+\s*,(?:\s*[^)(\s]|\s*\((?:[^)(]|\([^)(]*\))*\))+\s*)\)|#[\da-f]{3,8}|(?:rgb|hsl)a?\((?:-?[\d.]+%?[,\s]+){2}-?[\d.]+%?\s*(?:[,/]\s*)?(?:\b\d+(?:\.\d+)?|\.\d+)?%?\)|-?(?:\d+(?:\.\d+)?|\.\d+)/giu;
  function analyseComplexValue(value) {
    const originalValue = value.toString();
    const values = [];
    const indexes = {
      color: [],
      number: [],
      var: []
    };
    const types = [];
    let i13 = 0;
    const tokenised = originalValue.replace(complexRegex, (parsedValue) => {
      if (color.test(parsedValue)) {
        indexes.color.push(i13);
        types.push(COLOR_TOKEN);
        values.push(color.parse(parsedValue));
      } else if (parsedValue.startsWith(VAR_FUNCTION_TOKEN)) {
        indexes.var.push(i13);
        types.push(VAR_TOKEN);
        values.push(parsedValue);
      } else {
        indexes.number.push(i13);
        types.push(NUMBER_TOKEN);
        values.push(parseFloat(parsedValue));
      }
      ++i13;
      return SPLIT_TOKEN;
    });
    const split = tokenised.split(SPLIT_TOKEN);
    return { values, split, indexes, types };
  }
  function parseComplexValue(v6) {
    return analyseComplexValue(v6).values;
  }
  function buildTransformer({ split, types }) {
    const numSections = split.length;
    return (v6) => {
      let output = "";
      for (let i13 = 0; i13 < numSections; i13++) {
        output += split[i13];
        if (v6[i13] !== void 0) {
          const type = types[i13];
          if (type === NUMBER_TOKEN) {
            output += sanitize(v6[i13]);
          } else if (type === COLOR_TOKEN) {
            output += color.transform(v6[i13]);
          } else {
            output += v6[i13];
          }
        }
      }
      return output;
    };
  }
  function createTransformer(source) {
    return buildTransformer(analyseComplexValue(source));
  }
  var convertNumbersToZero = (v6) => typeof v6 === "number" ? 0 : color.test(v6) ? color.getAnimatableNone(v6) : v6;
  var convertToZero = (value, splitBefore) => {
    if (typeof value === "number") {
      return splitBefore?.trim().endsWith("/") ? value : 0;
    }
    return convertNumbersToZero(value);
  };
  function getAnimatableNone(v6) {
    const info = analyseComplexValue(v6);
    const transformer = buildTransformer(info);
    return transformer(info.values.map((value, i13) => convertToZero(value, info.split[i13])));
  }
  var complex = {
    test,
    parse: parseComplexValue,
    createTransformer,
    getAnimatableNone
  };

  // frontend/node_modules/motion-dom/dist/es/utils/mix/color.mjs
  init_define_import_meta_env();

  // frontend/node_modules/motion-dom/dist/es/value/types/color/hsla-to-rgba.mjs
  init_define_import_meta_env();
  function hueToRgb(p8, q3, t12) {
    if (t12 < 0)
      t12 += 1;
    if (t12 > 1)
      t12 -= 1;
    if (t12 < 1 / 6)
      return p8 + (q3 - p8) * 6 * t12;
    if (t12 < 1 / 2)
      return q3;
    if (t12 < 2 / 3)
      return p8 + (q3 - p8) * (2 / 3 - t12) * 6;
    return p8;
  }
  function hslaToRgba({ hue, saturation, lightness, alpha: alpha2 }) {
    hue /= 360;
    saturation /= 100;
    lightness /= 100;
    let red = 0;
    let green = 0;
    let blue = 0;
    if (!saturation) {
      red = green = blue = lightness;
    } else {
      const q3 = lightness < 0.5 ? lightness * (1 + saturation) : lightness + saturation - lightness * saturation;
      const p8 = 2 * lightness - q3;
      red = hueToRgb(p8, q3, hue + 1 / 3);
      green = hueToRgb(p8, q3, hue);
      blue = hueToRgb(p8, q3, hue - 1 / 3);
    }
    return {
      red: Math.round(red * 255),
      green: Math.round(green * 255),
      blue: Math.round(blue * 255),
      alpha: alpha2
    };
  }

  // frontend/node_modules/motion-dom/dist/es/utils/mix/immediate.mjs
  init_define_import_meta_env();
  function mixImmediate(a21, b8) {
    return (p8) => p8 > 0 ? b8 : a21;
  }

  // frontend/node_modules/motion-dom/dist/es/utils/mix/number.mjs
  init_define_import_meta_env();
  var mixNumber = (from, to, progress2) => {
    return from + (to - from) * progress2;
  };

  // frontend/node_modules/motion-dom/dist/es/utils/mix/color.mjs
  var mixLinearColor = (from, to, v6) => {
    const fromExpo = from * from;
    const expo = v6 * (to * to - fromExpo) + fromExpo;
    return expo < 0 ? 0 : Math.sqrt(expo);
  };
  var colorTypes = [hex, rgba, hsla];
  var getColorType = (v6) => colorTypes.find((type) => type.test(v6));
  function asRGBA(color2) {
    const type = getColorType(color2);
    warning(Boolean(type), `'${color2}' is not an animatable color. Use the equivalent color code instead.`, "color-not-animatable");
    if (!Boolean(type))
      return false;
    let model = type.parse(color2);
    if (type === hsla) {
      model = hslaToRgba(model);
    }
    return model;
  }
  var mixColor = (from, to) => {
    const fromRGBA = asRGBA(from);
    const toRGBA = asRGBA(to);
    if (!fromRGBA || !toRGBA) {
      return mixImmediate(from, to);
    }
    const blended = { ...fromRGBA };
    return (v6) => {
      blended.red = mixLinearColor(fromRGBA.red, toRGBA.red, v6);
      blended.green = mixLinearColor(fromRGBA.green, toRGBA.green, v6);
      blended.blue = mixLinearColor(fromRGBA.blue, toRGBA.blue, v6);
      blended.alpha = mixNumber(fromRGBA.alpha, toRGBA.alpha, v6);
      return rgba.transform(blended);
    };
  };

  // frontend/node_modules/motion-dom/dist/es/utils/mix/visibility.mjs
  init_define_import_meta_env();
  var invisibleValues = /* @__PURE__ */ new Set(["none", "hidden"]);
  function mixVisibility(origin, target) {
    if (invisibleValues.has(origin)) {
      return (p8) => p8 <= 0 ? origin : target;
    } else {
      return (p8) => p8 >= 1 ? target : origin;
    }
  }

  // frontend/node_modules/motion-dom/dist/es/utils/mix/complex.mjs
  function mixNumber2(a21, b8) {
    return (p8) => mixNumber(a21, b8, p8);
  }
  function getMixer(a21) {
    if (typeof a21 === "number") {
      return mixNumber2;
    } else if (typeof a21 === "string") {
      return isCSSVariableToken(a21) ? mixImmediate : color.test(a21) ? mixColor : mixComplex;
    } else if (Array.isArray(a21)) {
      return mixArray;
    } else if (typeof a21 === "object") {
      return color.test(a21) ? mixColor : mixObject;
    }
    return mixImmediate;
  }
  function mixArray(a21, b8) {
    const output = [...a21];
    const numValues = output.length;
    const blendValue = a21.map((v6, i13) => getMixer(v6)(v6, b8[i13]));
    return (p8) => {
      for (let i13 = 0; i13 < numValues; i13++) {
        output[i13] = blendValue[i13](p8);
      }
      return output;
    };
  }
  function mixObject(a21, b8) {
    const output = { ...a21, ...b8 };
    const blendValue = {};
    for (const key in output) {
      if (a21[key] !== void 0 && b8[key] !== void 0) {
        blendValue[key] = getMixer(a21[key])(a21[key], b8[key]);
      }
    }
    return (v6) => {
      for (const key in blendValue) {
        output[key] = blendValue[key](v6);
      }
      return output;
    };
  }
  function matchOrder(origin, target) {
    const orderedOrigin = [];
    const pointers = { color: 0, var: 0, number: 0 };
    for (let i13 = 0; i13 < target.values.length; i13++) {
      const type = target.types[i13];
      const originIndex = origin.indexes[type][pointers[type]];
      const originValue = origin.values[originIndex] ?? 0;
      orderedOrigin[i13] = originValue;
      pointers[type]++;
    }
    return orderedOrigin;
  }
  var mixComplex = (origin, target) => {
    const template = complex.createTransformer(target);
    const originStats = analyseComplexValue(origin);
    const targetStats = analyseComplexValue(target);
    const canInterpolate = originStats.indexes.var.length === targetStats.indexes.var.length && originStats.indexes.color.length === targetStats.indexes.color.length && originStats.indexes.number.length >= targetStats.indexes.number.length;
    if (canInterpolate) {
      if (invisibleValues.has(origin) && !targetStats.values.length || invisibleValues.has(target) && !originStats.values.length) {
        return mixVisibility(origin, target);
      }
      return pipe(mixArray(matchOrder(originStats, targetStats), targetStats.values), template);
    } else {
      warning(true, `Complex values '${origin}' and '${target}' too different to mix. Ensure all colors are of the same type, and that each contains the same quantity of number and color values. Falling back to instant transition.`, "complex-values-different");
      return mixImmediate(origin, target);
    }
  };

  // frontend/node_modules/motion-dom/dist/es/utils/mix/index.mjs
  function mix(from, to, p8) {
    if (typeof from === "number" && typeof to === "number" && typeof p8 === "number") {
      return mixNumber(from, to, p8);
    }
    const mixer = getMixer(from);
    return mixer(from, to);
  }

  // frontend/node_modules/motion-dom/dist/es/animation/drivers/frame.mjs
  init_define_import_meta_env();
  var frameloopDriver = (update) => {
    const passTimestamp = ({ timestamp }) => update(timestamp);
    return {
      start: (keepAlive = true) => frame.update(passTimestamp, keepAlive),
      stop: () => cancelFrame(passTimestamp),
      /**
       * If we're processing this frame we can use the
       * framelocked timestamp to keep things in sync.
       */
      now: () => frameData.isProcessing ? frameData.timestamp : time.now()
    };
  };

  // frontend/node_modules/motion-dom/dist/es/animation/generators/inertia.mjs
  init_define_import_meta_env();

  // frontend/node_modules/motion-dom/dist/es/animation/generators/spring.mjs
  init_define_import_meta_env();

  // frontend/node_modules/motion-dom/dist/es/animation/waapi/utils/linear.mjs
  init_define_import_meta_env();
  var generateLinearEasing = (easing, duration, resolution = 10) => {
    let points = "";
    const numPoints = Math.max(Math.round(duration / resolution), 2);
    for (let i13 = 0; i13 < numPoints; i13++) {
      points += Math.round(easing(i13 / (numPoints - 1)) * 1e4) / 1e4 + ", ";
    }
    return `linear(${points.substring(0, points.length - 2)})`;
  };

  // frontend/node_modules/motion-dom/dist/es/animation/generators/utils/calc-duration.mjs
  init_define_import_meta_env();
  var maxGeneratorDuration = 2e4;
  function calcGeneratorDuration(generator) {
    let duration = 0;
    const timeStep = 50;
    let state = generator.next(duration);
    while (!state.done && duration < maxGeneratorDuration) {
      duration += timeStep;
      state = generator.next(duration);
    }
    return duration >= maxGeneratorDuration ? Infinity : duration;
  }

  // frontend/node_modules/motion-dom/dist/es/animation/generators/utils/create-generator-easing.mjs
  init_define_import_meta_env();
  function createGeneratorEasing(options, scale2 = 100, createGenerator) {
    const generator = createGenerator({ ...options, keyframes: [0, scale2] });
    const duration = Math.min(calcGeneratorDuration(generator), maxGeneratorDuration);
    return {
      type: "keyframes",
      ease: (progress2) => {
        return generator.next(duration * progress2).value / scale2;
      },
      duration: millisecondsToSeconds(duration)
    };
  }

  // frontend/node_modules/motion-dom/dist/es/animation/generators/spring.mjs
  var springDefaults = {
    // Default spring physics
    stiffness: 100,
    damping: 10,
    mass: 1,
    velocity: 0,
    // Default duration/bounce-based options
    duration: 800,
    // in ms
    bounce: 0.3,
    visualDuration: 0.3,
    // in seconds
    // Rest thresholds
    restSpeed: {
      granular: 0.01,
      default: 2
    },
    restDelta: {
      granular: 5e-3,
      default: 0.5
    },
    // Limits
    minDuration: 0.01,
    // in seconds
    maxDuration: 10,
    // in seconds
    minDamping: 0.05,
    maxDamping: 1
  };
  function calcAngularFreq(undampedFreq, dampingRatio) {
    return undampedFreq * Math.sqrt(1 - dampingRatio * dampingRatio);
  }
  var rootIterations = 12;
  function approximateRoot(envelope, derivative, initialGuess) {
    let result = initialGuess;
    for (let i13 = 1; i13 < rootIterations; i13++) {
      result = result - envelope(result) / derivative(result);
    }
    return result;
  }
  var safeMin = 1e-3;
  function findSpring({ duration = springDefaults.duration, bounce = springDefaults.bounce, velocity = springDefaults.velocity, mass = springDefaults.mass }) {
    let envelope;
    let derivative;
    warning(duration <= secondsToMilliseconds(springDefaults.maxDuration), "Spring duration must be 10 seconds or less", "spring-duration-limit");
    let dampingRatio = 1 - bounce;
    dampingRatio = clamp(springDefaults.minDamping, springDefaults.maxDamping, dampingRatio);
    duration = clamp(springDefaults.minDuration, springDefaults.maxDuration, millisecondsToSeconds(duration));
    if (dampingRatio < 1) {
      envelope = (undampedFreq2) => {
        const exponentialDecay = undampedFreq2 * dampingRatio;
        const delta = exponentialDecay * duration;
        const a21 = exponentialDecay - velocity;
        const b8 = calcAngularFreq(undampedFreq2, dampingRatio);
        const c14 = Math.exp(-delta);
        return safeMin - a21 / b8 * c14;
      };
      derivative = (undampedFreq2) => {
        const exponentialDecay = undampedFreq2 * dampingRatio;
        const delta = exponentialDecay * duration;
        const d9 = delta * velocity + velocity;
        const e8 = Math.pow(dampingRatio, 2) * Math.pow(undampedFreq2, 2) * duration;
        const f13 = Math.exp(-delta);
        const g3 = calcAngularFreq(Math.pow(undampedFreq2, 2), dampingRatio);
        const factor = -envelope(undampedFreq2) + safeMin > 0 ? -1 : 1;
        return factor * ((d9 - e8) * f13) / g3;
      };
    } else {
      envelope = (undampedFreq2) => {
        const a21 = Math.exp(-undampedFreq2 * duration);
        const b8 = (undampedFreq2 - velocity) * duration + 1;
        return -safeMin + a21 * b8;
      };
      derivative = (undampedFreq2) => {
        const a21 = Math.exp(-undampedFreq2 * duration);
        const b8 = (velocity - undampedFreq2) * (duration * duration);
        return a21 * b8;
      };
    }
    const initialGuess = 5 / duration;
    const undampedFreq = approximateRoot(envelope, derivative, initialGuess);
    duration = secondsToMilliseconds(duration);
    if (isNaN(undampedFreq)) {
      return {
        stiffness: springDefaults.stiffness,
        damping: springDefaults.damping,
        duration
      };
    } else {
      const stiffness = Math.pow(undampedFreq, 2) * mass;
      return {
        stiffness,
        damping: dampingRatio * 2 * Math.sqrt(mass * stiffness),
        duration
      };
    }
  }
  var durationKeys = ["duration", "bounce"];
  var physicsKeys = ["stiffness", "damping", "mass"];
  function isSpringType(options, keys) {
    return keys.some((key) => options[key] !== void 0);
  }
  function getSpringOptions(options) {
    let springOptions = {
      velocity: springDefaults.velocity,
      stiffness: springDefaults.stiffness,
      damping: springDefaults.damping,
      mass: springDefaults.mass,
      isResolvedFromDuration: false,
      ...options
    };
    if (!isSpringType(options, physicsKeys) && isSpringType(options, durationKeys)) {
      springOptions.velocity = 0;
      if (options.visualDuration) {
        const visualDuration = options.visualDuration;
        const root = 2 * Math.PI / (visualDuration * 1.2);
        const stiffness = root * root;
        const damping = 2 * clamp(0.05, 1, 1 - (options.bounce || 0)) * Math.sqrt(stiffness);
        springOptions = {
          ...springOptions,
          mass: springDefaults.mass,
          stiffness,
          damping
        };
      } else {
        const derived = findSpring({ ...options, velocity: 0 });
        springOptions = {
          ...springOptions,
          ...derived,
          mass: springDefaults.mass
        };
        springOptions.isResolvedFromDuration = true;
      }
    }
    return springOptions;
  }
  function spring(optionsOrVisualDuration = springDefaults.visualDuration, bounce = springDefaults.bounce) {
    const options = typeof optionsOrVisualDuration !== "object" ? {
      visualDuration: optionsOrVisualDuration,
      keyframes: [0, 1],
      bounce
    } : optionsOrVisualDuration;
    let { restSpeed, restDelta } = options;
    const origin = options.keyframes[0];
    const target = options.keyframes[options.keyframes.length - 1];
    const state = { done: false, value: origin };
    const { stiffness, damping, mass, duration, velocity, isResolvedFromDuration } = getSpringOptions({
      ...options,
      velocity: -millisecondsToSeconds(options.velocity || 0)
    });
    const initialVelocity = velocity || 0;
    const dampingRatio = damping / (2 * Math.sqrt(stiffness * mass));
    const initialDelta = target - origin;
    const undampedAngularFreq = millisecondsToSeconds(Math.sqrt(stiffness / mass));
    const isGranularScale = Math.abs(initialDelta) < 5;
    restSpeed || (restSpeed = isGranularScale ? springDefaults.restSpeed.granular : springDefaults.restSpeed.default);
    restDelta || (restDelta = isGranularScale ? springDefaults.restDelta.granular : springDefaults.restDelta.default);
    let resolveSpring;
    let resolveVelocity;
    let angularFreq;
    let A5;
    let sinCoeff;
    let cosCoeff;
    if (dampingRatio < 1) {
      angularFreq = calcAngularFreq(undampedAngularFreq, dampingRatio);
      A5 = (initialVelocity + dampingRatio * undampedAngularFreq * initialDelta) / angularFreq;
      resolveSpring = (t12) => {
        const envelope = Math.exp(-dampingRatio * undampedAngularFreq * t12);
        return target - envelope * (A5 * Math.sin(angularFreq * t12) + initialDelta * Math.cos(angularFreq * t12));
      };
      sinCoeff = dampingRatio * undampedAngularFreq * A5 + initialDelta * angularFreq;
      cosCoeff = dampingRatio * undampedAngularFreq * initialDelta - A5 * angularFreq;
      resolveVelocity = (t12) => {
        const envelope = Math.exp(-dampingRatio * undampedAngularFreq * t12);
        return envelope * (sinCoeff * Math.sin(angularFreq * t12) + cosCoeff * Math.cos(angularFreq * t12));
      };
    } else if (dampingRatio === 1) {
      resolveSpring = (t12) => target - Math.exp(-undampedAngularFreq * t12) * (initialDelta + (initialVelocity + undampedAngularFreq * initialDelta) * t12);
      const C8 = initialVelocity + undampedAngularFreq * initialDelta;
      resolveVelocity = (t12) => Math.exp(-undampedAngularFreq * t12) * (undampedAngularFreq * C8 * t12 - initialVelocity);
    } else {
      const dampedAngularFreq = undampedAngularFreq * Math.sqrt(dampingRatio * dampingRatio - 1);
      resolveSpring = (t12) => {
        const envelope = Math.exp(-dampingRatio * undampedAngularFreq * t12);
        const freqForT = Math.min(dampedAngularFreq * t12, 300);
        return target - envelope * ((initialVelocity + dampingRatio * undampedAngularFreq * initialDelta) * Math.sinh(freqForT) + dampedAngularFreq * initialDelta * Math.cosh(freqForT)) / dampedAngularFreq;
      };
      const P5 = (initialVelocity + dampingRatio * undampedAngularFreq * initialDelta) / dampedAngularFreq;
      const sinhCoeff = dampingRatio * undampedAngularFreq * P5 - initialDelta * dampedAngularFreq;
      const coshCoeff = dampingRatio * undampedAngularFreq * initialDelta - P5 * dampedAngularFreq;
      resolveVelocity = (t12) => {
        const envelope = Math.exp(-dampingRatio * undampedAngularFreq * t12);
        const freqForT = Math.min(dampedAngularFreq * t12, 300);
        return envelope * (sinhCoeff * Math.sinh(freqForT) + coshCoeff * Math.cosh(freqForT));
      };
    }
    const generator = {
      calculatedDuration: isResolvedFromDuration ? duration || null : null,
      velocity: (t12) => secondsToMilliseconds(resolveVelocity(t12)),
      next: (t12) => {
        if (!isResolvedFromDuration && dampingRatio < 1) {
          const envelope = Math.exp(-dampingRatio * undampedAngularFreq * t12);
          const sin = Math.sin(angularFreq * t12);
          const cos = Math.cos(angularFreq * t12);
          const current2 = target - envelope * (A5 * sin + initialDelta * cos);
          const currentVelocity = secondsToMilliseconds(envelope * (sinCoeff * sin + cosCoeff * cos));
          state.done = Math.abs(currentVelocity) <= restSpeed && Math.abs(target - current2) <= restDelta;
          state.value = state.done ? target : current2;
          return state;
        }
        const current = resolveSpring(t12);
        if (!isResolvedFromDuration) {
          const currentVelocity = secondsToMilliseconds(resolveVelocity(t12));
          state.done = Math.abs(currentVelocity) <= restSpeed && Math.abs(target - current) <= restDelta;
        } else {
          state.done = t12 >= duration;
        }
        state.value = state.done ? target : current;
        return state;
      },
      toString: () => {
        const calculatedDuration = Math.min(calcGeneratorDuration(generator), maxGeneratorDuration);
        const easing = generateLinearEasing((progress2) => generator.next(calculatedDuration * progress2).value, calculatedDuration, 30);
        return calculatedDuration + "ms " + easing;
      },
      toTransition: () => {
      }
    };
    return generator;
  }
  spring.applyToOptions = (options) => {
    const generatorOptions = createGeneratorEasing(options, 100, spring);
    options.ease = generatorOptions.ease;
    options.duration = secondsToMilliseconds(generatorOptions.duration);
    options.type = "keyframes";
    return options;
  };

  // frontend/node_modules/motion-dom/dist/es/animation/generators/utils/velocity.mjs
  init_define_import_meta_env();
  var velocitySampleDuration = 5;
  function getGeneratorVelocity(resolveValue, t12, current) {
    const prevT = Math.max(t12 - velocitySampleDuration, 0);
    return velocityPerSecond(current - resolveValue(prevT), t12 - prevT);
  }

  // frontend/node_modules/motion-dom/dist/es/animation/generators/inertia.mjs
  function inertia({ keyframes: keyframes2, velocity = 0, power = 0.8, timeConstant = 325, bounceDamping = 10, bounceStiffness = 500, modifyTarget, min: min2, max: max2, restDelta = 0.5, restSpeed }) {
    const origin = keyframes2[0];
    const state = {
      done: false,
      value: origin
    };
    const isOutOfBounds = (v6) => min2 !== void 0 && v6 < min2 || max2 !== void 0 && v6 > max2;
    const nearestBoundary = (v6) => {
      if (min2 === void 0)
        return max2;
      if (max2 === void 0)
        return min2;
      return Math.abs(min2 - v6) < Math.abs(max2 - v6) ? min2 : max2;
    };
    let amplitude = power * velocity;
    const ideal = origin + amplitude;
    const target = modifyTarget === void 0 ? ideal : modifyTarget(ideal);
    if (target !== ideal)
      amplitude = target - origin;
    const calcDelta = (t12) => -amplitude * Math.exp(-t12 / timeConstant);
    const calcLatest = (t12) => target + calcDelta(t12);
    const applyFriction = (t12) => {
      const delta = calcDelta(t12);
      const latest = calcLatest(t12);
      state.done = Math.abs(delta) <= restDelta;
      state.value = state.done ? target : latest;
    };
    let timeReachedBoundary;
    let spring$1;
    const checkCatchBoundary = (t12) => {
      if (!isOutOfBounds(state.value))
        return;
      timeReachedBoundary = t12;
      spring$1 = spring({
        keyframes: [state.value, nearestBoundary(state.value)],
        velocity: getGeneratorVelocity(calcLatest, t12, state.value),
        // TODO: This should be passing * 1000
        damping: bounceDamping,
        stiffness: bounceStiffness,
        restDelta,
        restSpeed
      });
    };
    checkCatchBoundary(0);
    return {
      calculatedDuration: null,
      next: (t12) => {
        let hasUpdatedFrame = false;
        if (!spring$1 && timeReachedBoundary === void 0) {
          hasUpdatedFrame = true;
          applyFriction(t12);
          checkCatchBoundary(t12);
        }
        if (timeReachedBoundary !== void 0 && t12 >= timeReachedBoundary) {
          return spring$1.next(t12 - timeReachedBoundary);
        } else {
          !hasUpdatedFrame && applyFriction(t12);
          return state;
        }
      }
    };
  }

  // frontend/node_modules/motion-dom/dist/es/animation/generators/keyframes.mjs
  init_define_import_meta_env();

  // frontend/node_modules/motion-dom/dist/es/utils/interpolate.mjs
  init_define_import_meta_env();
  function createMixers(output, ease2, customMixer) {
    const mixers = [];
    const mixerFactory = customMixer || MotionGlobalConfig.mix || mix;
    const numMixers = output.length - 1;
    for (let i13 = 0; i13 < numMixers; i13++) {
      let mixer = mixerFactory(output[i13], output[i13 + 1]);
      if (ease2) {
        const easingFunction = Array.isArray(ease2) ? ease2[i13] || noop : ease2;
        mixer = pipe(easingFunction, mixer);
      }
      mixers.push(mixer);
    }
    return mixers;
  }
  function interpolate(input, output, { clamp: isClamp = true, ease: ease2, mixer } = {}) {
    const inputLength = input.length;
    invariant(inputLength === output.length, "Both input and output ranges must be the same length", "range-length");
    if (inputLength === 1)
      return () => output[0];
    if (inputLength === 2 && output[0] === output[1])
      return () => output[1];
    const isZeroDeltaRange = input[0] === input[1];
    if (input[0] > input[inputLength - 1]) {
      input = [...input].reverse();
      output = [...output].reverse();
    }
    const mixers = createMixers(output, ease2, mixer);
    const numMixers = mixers.length;
    const interpolator = (v6) => {
      if (isZeroDeltaRange && v6 < input[0])
        return output[0];
      let i13 = 0;
      if (numMixers > 1) {
        for (; i13 < input.length - 2; i13++) {
          if (v6 < input[i13 + 1])
            break;
        }
      }
      const progressInRange = progress(input[i13], input[i13 + 1], v6);
      return mixers[i13](progressInRange);
    };
    return isClamp ? (v6) => interpolator(clamp(input[0], input[inputLength - 1], v6)) : interpolator;
  }

  // frontend/node_modules/motion-dom/dist/es/animation/keyframes/offsets/default.mjs
  init_define_import_meta_env();

  // frontend/node_modules/motion-dom/dist/es/animation/keyframes/offsets/fill.mjs
  init_define_import_meta_env();
  function fillOffset(offset4, remaining) {
    const min2 = offset4[offset4.length - 1];
    for (let i13 = 1; i13 <= remaining; i13++) {
      const offsetProgress = progress(0, remaining, i13);
      offset4.push(mixNumber(min2, 1, offsetProgress));
    }
  }

  // frontend/node_modules/motion-dom/dist/es/animation/keyframes/offsets/default.mjs
  function defaultOffset(arr) {
    const offset4 = [0];
    fillOffset(offset4, arr.length - 1);
    return offset4;
  }

  // frontend/node_modules/motion-dom/dist/es/animation/keyframes/offsets/time.mjs
  init_define_import_meta_env();
  function convertOffsetToTimes(offset4, duration) {
    return offset4.map((o16) => o16 * duration);
  }

  // frontend/node_modules/motion-dom/dist/es/animation/generators/keyframes.mjs
  function defaultEasing(values, easing) {
    return values.map(() => easing || easeInOut).splice(0, values.length - 1);
  }
  function keyframes({ duration = 300, keyframes: keyframeValues, times, ease: ease2 = "easeInOut" }) {
    const easingFunctions = isEasingArray(ease2) ? ease2.map(easingDefinitionToFunction) : easingDefinitionToFunction(ease2);
    const state = {
      done: false,
      value: keyframeValues[0]
    };
    const absoluteTimes = convertOffsetToTimes(
      // Only use the provided offsets if they're the correct length
      // TODO Maybe we should warn here if there's a length mismatch
      times && times.length === keyframeValues.length ? times : defaultOffset(keyframeValues),
      duration
    );
    const mapTimeToKeyframe = interpolate(absoluteTimes, keyframeValues, {
      ease: Array.isArray(easingFunctions) ? easingFunctions : defaultEasing(keyframeValues, easingFunctions)
    });
    return {
      calculatedDuration: duration,
      next: (t12) => {
        state.value = mapTimeToKeyframe(t12);
        state.done = t12 >= duration;
        return state;
      }
    };
  }

  // frontend/node_modules/motion-dom/dist/es/animation/keyframes/get-final.mjs
  init_define_import_meta_env();
  var isNotNull = (value) => value !== null;
  function getFinalKeyframe(keyframes2, { repeat, repeatType = "loop" }, finalKeyframe, speed = 1) {
    const resolvedKeyframes = keyframes2.filter(isNotNull);
    const useFirstKeyframe = speed < 0 || repeat && repeatType !== "loop" && repeat % 2 === 1;
    const index3 = useFirstKeyframe ? 0 : resolvedKeyframes.length - 1;
    return !index3 || finalKeyframe === void 0 ? resolvedKeyframes[index3] : finalKeyframe;
  }

  // frontend/node_modules/motion-dom/dist/es/animation/utils/replace-transition-type.mjs
  init_define_import_meta_env();
  var transitionTypeMap = {
    decay: inertia,
    inertia,
    tween: keyframes,
    keyframes,
    spring
  };
  function replaceTransitionType(transition) {
    if (typeof transition.type === "string") {
      transition.type = transitionTypeMap[transition.type];
    }
  }

  // frontend/node_modules/motion-dom/dist/es/animation/utils/WithPromise.mjs
  init_define_import_meta_env();
  var WithPromise = class {
    constructor() {
      this.updateFinished();
    }
    get finished() {
      return this._finished;
    }
    updateFinished() {
      this._finished = new Promise((resolve) => {
        this.resolve = resolve;
      });
    }
    notifyFinished() {
      this.resolve();
    }
    /**
     * Allows the animation to be awaited.
     *
     * @deprecated Use `finished` instead.
     */
    then(onResolve, onReject) {
      return this.finished.then(onResolve, onReject);
    }
  };

  // frontend/node_modules/motion-dom/dist/es/animation/JSAnimation.mjs
  var percentToProgress = (percent2) => percent2 / 100;
  var JSAnimation = class extends WithPromise {
    constructor(options) {
      super();
      this.state = "idle";
      this.startTime = null;
      this.isStopped = false;
      this.currentTime = 0;
      this.holdTime = null;
      this.playbackSpeed = 1;
      this.delayState = {
        done: false,
        value: void 0
      };
      this.stop = () => {
        const { motionValue: motionValue2 } = this.options;
        if (motionValue2 && motionValue2.updatedAt !== time.now()) {
          this.tick(time.now());
        }
        this.isStopped = true;
        if (this.state === "idle")
          return;
        this.teardown();
        this.options.onStop?.();
      };
      this.options = options;
      this.initAnimation();
      this.play();
      if (options.autoplay === false)
        this.pause();
    }
    initAnimation() {
      const { options } = this;
      replaceTransitionType(options);
      const { type = keyframes, repeat = 0, repeatDelay = 0, repeatType, velocity = 0 } = options;
      let { keyframes: keyframes$1 } = options;
      const generatorFactory = type || keyframes;
      if (generatorFactory !== keyframes) {
        invariant(keyframes$1.length <= 2, `Only two keyframes currently supported with spring and inertia animations. Trying to animate ${keyframes$1}`, "spring-two-frames");
      }
      if (generatorFactory !== keyframes && typeof keyframes$1[0] !== "number") {
        this.mixKeyframes = pipe(percentToProgress, mix(keyframes$1[0], keyframes$1[1]));
        keyframes$1 = [0, 100];
      }
      const generator = generatorFactory({ ...options, keyframes: keyframes$1 });
      if (repeatType === "mirror") {
        this.mirroredGenerator = generatorFactory({
          ...options,
          keyframes: [...keyframes$1].reverse(),
          velocity: -velocity
        });
      }
      if (generator.calculatedDuration === null) {
        generator.calculatedDuration = calcGeneratorDuration(generator);
      }
      const { calculatedDuration } = generator;
      this.calculatedDuration = calculatedDuration;
      this.resolvedDuration = calculatedDuration + repeatDelay;
      this.totalDuration = this.resolvedDuration * (repeat + 1) - repeatDelay;
      this.generator = generator;
    }
    updateTime(timestamp) {
      const animationTime = Math.round(timestamp - this.startTime) * this.playbackSpeed;
      if (this.holdTime !== null) {
        this.currentTime = this.holdTime;
      } else {
        this.currentTime = animationTime;
      }
    }
    tick(timestamp, sample = false) {
      const { generator, totalDuration, mixKeyframes, mirroredGenerator, resolvedDuration, calculatedDuration } = this;
      if (this.startTime === null)
        return generator.next(0);
      const { delay: delay2 = 0, keyframes: keyframes2, repeat, repeatType, repeatDelay, type, onUpdate, finalKeyframe } = this.options;
      if (this.speed > 0) {
        this.startTime = Math.min(this.startTime, timestamp);
      } else if (this.speed < 0) {
        this.startTime = Math.min(timestamp - totalDuration / this.speed, this.startTime);
      }
      if (sample) {
        this.currentTime = timestamp;
      } else {
        this.updateTime(timestamp);
      }
      const timeWithoutDelay = this.currentTime - delay2 * (this.playbackSpeed >= 0 ? 1 : -1);
      const isInDelayPhase = this.playbackSpeed >= 0 ? timeWithoutDelay < 0 : timeWithoutDelay > totalDuration;
      this.currentTime = Math.max(timeWithoutDelay, 0);
      if (this.state === "finished" && this.holdTime === null) {
        this.currentTime = totalDuration;
      }
      let elapsed = this.currentTime;
      let frameGenerator = generator;
      if (repeat) {
        const progress2 = Math.min(this.currentTime, totalDuration) / resolvedDuration;
        let currentIteration = Math.floor(progress2);
        let iterationProgress = progress2 % 1;
        if (!iterationProgress && progress2 >= 1) {
          iterationProgress = 1;
        }
        iterationProgress === 1 && currentIteration--;
        currentIteration = Math.min(currentIteration, repeat + 1);
        const isOddIteration = Boolean(currentIteration % 2);
        if (isOddIteration) {
          if (repeatType === "reverse") {
            iterationProgress = 1 - iterationProgress;
            if (repeatDelay) {
              iterationProgress -= repeatDelay / resolvedDuration;
            }
          } else if (repeatType === "mirror") {
            frameGenerator = mirroredGenerator;
          }
        }
        elapsed = clamp(0, 1, iterationProgress) * resolvedDuration;
      }
      let state;
      if (isInDelayPhase) {
        this.delayState.value = keyframes2[0];
        state = this.delayState;
      } else {
        state = frameGenerator.next(elapsed);
      }
      if (mixKeyframes && !isInDelayPhase) {
        state.value = mixKeyframes(state.value);
      }
      let { done } = state;
      if (!isInDelayPhase && calculatedDuration !== null) {
        done = this.playbackSpeed >= 0 ? this.currentTime >= totalDuration : this.currentTime <= 0;
      }
      const isAnimationFinished = this.holdTime === null && (this.state === "finished" || this.state === "running" && done);
      if (isAnimationFinished && type !== inertia) {
        state.value = getFinalKeyframe(keyframes2, this.options, finalKeyframe, this.speed);
      }
      if (onUpdate) {
        onUpdate(state.value);
      }
      if (isAnimationFinished) {
        this.finish();
      }
      return state;
    }
    /**
     * Allows the returned animation to be awaited or promise-chained. Currently
     * resolves when the animation finishes at all but in a future update could/should
     * reject if its cancels.
     */
    then(resolve, reject) {
      return this.finished.then(resolve, reject);
    }
    get duration() {
      return millisecondsToSeconds(this.calculatedDuration);
    }
    get iterationDuration() {
      const { delay: delay2 = 0 } = this.options || {};
      return this.duration + millisecondsToSeconds(delay2);
    }
    get time() {
      return millisecondsToSeconds(this.currentTime);
    }
    set time(newTime) {
      newTime = secondsToMilliseconds(newTime);
      this.currentTime = newTime;
      if (this.startTime === null || this.holdTime !== null || this.playbackSpeed === 0) {
        this.holdTime = newTime;
      } else if (this.driver) {
        this.startTime = this.driver.now() - newTime / this.playbackSpeed;
      }
      if (this.driver) {
        this.driver.start(false);
      } else {
        this.startTime = 0;
        this.state = "paused";
        this.holdTime = newTime;
        this.tick(newTime);
      }
    }
    /**
     * Returns the generator's velocity at the current time in units/second.
     * Uses the analytical derivative when available (springs), avoiding
     * the MotionValue's frame-dependent velocity estimation.
     */
    getGeneratorVelocity() {
      const t12 = this.currentTime;
      if (t12 <= 0)
        return this.options.velocity || 0;
      if (this.generator.velocity) {
        return this.generator.velocity(t12);
      }
      const current = this.generator.next(t12).value;
      return getGeneratorVelocity((s15) => this.generator.next(s15).value, t12, current);
    }
    get speed() {
      return this.playbackSpeed;
    }
    set speed(newSpeed) {
      const hasChanged = this.playbackSpeed !== newSpeed;
      if (hasChanged && this.driver) {
        this.updateTime(time.now());
      }
      this.playbackSpeed = newSpeed;
      if (hasChanged && this.driver) {
        this.time = millisecondsToSeconds(this.currentTime);
      }
    }
    play() {
      if (this.isStopped)
        return;
      const { driver = frameloopDriver, startTime } = this.options;
      if (!this.driver) {
        this.driver = driver((timestamp) => this.tick(timestamp));
      }
      this.options.onPlay?.();
      const now2 = this.driver.now();
      if (this.state === "finished") {
        this.updateFinished();
        this.startTime = now2;
      } else if (this.holdTime !== null) {
        this.startTime = now2 - this.holdTime;
      } else if (!this.startTime) {
        this.startTime = startTime ?? now2;
      }
      if (this.state === "finished" && this.speed < 0) {
        this.startTime += this.calculatedDuration;
      }
      this.holdTime = null;
      this.state = "running";
      this.driver.start();
    }
    pause() {
      this.state = "paused";
      this.updateTime(time.now());
      this.holdTime = this.currentTime;
    }
    complete() {
      if (this.state !== "running") {
        this.play();
      }
      this.state = "finished";
      this.holdTime = null;
    }
    finish() {
      this.notifyFinished();
      this.teardown();
      this.state = "finished";
      this.options.onComplete?.();
    }
    cancel() {
      this.holdTime = null;
      this.startTime = 0;
      this.tick(0);
      this.teardown();
      this.options.onCancel?.();
    }
    teardown() {
      this.state = "idle";
      this.stopDriver();
      this.startTime = this.holdTime = null;
    }
    stopDriver() {
      if (!this.driver)
        return;
      this.driver.stop();
      this.driver = void 0;
    }
    sample(sampleTime) {
      this.startTime = 0;
      return this.tick(sampleTime, true);
    }
    attachTimeline(timeline) {
      if (this.options.allowFlatten) {
        this.options.type = "keyframes";
        this.options.ease = "linear";
        this.initAnimation();
      }
      this.driver?.stop();
      return timeline.observe(this);
    }
  };

  // frontend/node_modules/motion-dom/dist/es/animation/keyframes/KeyframesResolver.mjs
  init_define_import_meta_env();

  // frontend/node_modules/motion-dom/dist/es/animation/keyframes/utils/fill-wildcards.mjs
  init_define_import_meta_env();
  function fillWildcards(keyframes2) {
    for (let i13 = 1; i13 < keyframes2.length; i13++) {
      keyframes2[i13] ?? (keyframes2[i13] = keyframes2[i13 - 1]);
    }
  }

  // frontend/node_modules/motion-dom/dist/es/animation/keyframes/utils/unit-conversion.mjs
  init_define_import_meta_env();

  // frontend/node_modules/motion-dom/dist/es/render/dom/parse-transform.mjs
  init_define_import_meta_env();
  var radToDeg = (rad) => rad * 180 / Math.PI;
  var rotate = (v6) => {
    const angle = radToDeg(Math.atan2(v6[1], v6[0]));
    return rebaseAngle(angle);
  };
  var matrix2dParsers = {
    x: 4,
    y: 5,
    translateX: 4,
    translateY: 5,
    scaleX: 0,
    scaleY: 3,
    scale: (v6) => (Math.abs(v6[0]) + Math.abs(v6[3])) / 2,
    rotate,
    rotateZ: rotate,
    skewX: (v6) => radToDeg(Math.atan(v6[1])),
    skewY: (v6) => radToDeg(Math.atan(v6[2])),
    skew: (v6) => (Math.abs(v6[1]) + Math.abs(v6[2])) / 2
  };
  var rebaseAngle = (angle) => {
    angle = angle % 360;
    if (angle < 0)
      angle += 360;
    return angle;
  };
  var rotateZ = rotate;
  var scaleX = (v6) => Math.sqrt(v6[0] * v6[0] + v6[1] * v6[1]);
  var scaleY = (v6) => Math.sqrt(v6[4] * v6[4] + v6[5] * v6[5]);
  var matrix3dParsers = {
    x: 12,
    y: 13,
    z: 14,
    translateX: 12,
    translateY: 13,
    translateZ: 14,
    scaleX,
    scaleY,
    scale: (v6) => (scaleX(v6) + scaleY(v6)) / 2,
    rotateX: (v6) => rebaseAngle(radToDeg(Math.atan2(v6[6], v6[5]))),
    rotateY: (v6) => rebaseAngle(radToDeg(Math.atan2(-v6[2], v6[0]))),
    rotateZ,
    rotate: rotateZ,
    skewX: (v6) => radToDeg(Math.atan(v6[4])),
    skewY: (v6) => radToDeg(Math.atan(v6[1])),
    skew: (v6) => (Math.abs(v6[1]) + Math.abs(v6[4])) / 2
  };
  function defaultTransformValue(name) {
    return name.includes("scale") ? 1 : 0;
  }
  function parseValueFromTransform(transform, name) {
    if (!transform || transform === "none") {
      return defaultTransformValue(name);
    }
    const matrix3dMatch = transform.match(/^matrix3d\(([-\d.e\s,]+)\)$/u);
    let parsers;
    let match;
    if (matrix3dMatch) {
      parsers = matrix3dParsers;
      match = matrix3dMatch;
    } else {
      const matrix2dMatch = transform.match(/^matrix\(([-\d.e\s,]+)\)$/u);
      parsers = matrix2dParsers;
      match = matrix2dMatch;
    }
    if (!match) {
      return defaultTransformValue(name);
    }
    const valueParser = parsers[name];
    const values = match[1].split(",").map(convertTransformToNumber);
    return typeof valueParser === "function" ? valueParser(values) : values[valueParser];
  }
  var readTransformValue = (instance, name) => {
    const { transform = "none" } = getComputedStyle(instance);
    return parseValueFromTransform(transform, name);
  };
  function convertTransformToNumber(value) {
    return parseFloat(value.trim());
  }

  // frontend/node_modules/motion-dom/dist/es/render/utils/keys-transform.mjs
  init_define_import_meta_env();
  var transformPropOrder = [
    "transformPerspective",
    "x",
    "y",
    "z",
    "translateX",
    "translateY",
    "translateZ",
    "scale",
    "scaleX",
    "scaleY",
    "rotate",
    "rotateX",
    "rotateY",
    "rotateZ",
    "skew",
    "skewX",
    "skewY"
  ];
  var transformProps = /* @__PURE__ */ (() => /* @__PURE__ */ new Set([...transformPropOrder, "pathRotation"]))();

  // frontend/node_modules/motion-dom/dist/es/animation/keyframes/utils/unit-conversion.mjs
  var isNumOrPxType = (v6) => v6 === number || v6 === px;
  var transformKeys = /* @__PURE__ */ new Set(["x", "y", "z"]);
  var nonTranslationalTransformKeys = transformPropOrder.filter((key) => !transformKeys.has(key));
  function removeNonTranslationalTransform(visualElement) {
    const removedTransforms = [];
    nonTranslationalTransformKeys.forEach((key) => {
      const value = visualElement.getValue(key);
      if (value !== void 0) {
        removedTransforms.push([key, value.get()]);
        value.set(key.startsWith("scale") ? 1 : 0);
      }
    });
    return removedTransforms;
  }
  var positionalValues = {
    // Dimensions
    width: ({ x: x9 }, { paddingLeft = "0", paddingRight = "0", boxSizing }) => {
      const width = x9.max - x9.min;
      return boxSizing === "border-box" ? width : width - parseFloat(paddingLeft) - parseFloat(paddingRight);
    },
    height: ({ y: y6 }, { paddingTop = "0", paddingBottom = "0", boxSizing }) => {
      const height = y6.max - y6.min;
      return boxSizing === "border-box" ? height : height - parseFloat(paddingTop) - parseFloat(paddingBottom);
    },
    top: (_bbox, { top }) => parseFloat(top),
    left: (_bbox, { left }) => parseFloat(left),
    bottom: ({ y: y6 }, { top }) => parseFloat(top) + (y6.max - y6.min),
    right: ({ x: x9 }, { left }) => parseFloat(left) + (x9.max - x9.min),
    // Transform
    x: (_bbox, { transform }) => parseValueFromTransform(transform, "x"),
    y: (_bbox, { transform }) => parseValueFromTransform(transform, "y")
  };
  positionalValues.translateX = positionalValues.x;
  positionalValues.translateY = positionalValues.y;

  // frontend/node_modules/motion-dom/dist/es/animation/keyframes/KeyframesResolver.mjs
  var toResolve = /* @__PURE__ */ new Set();
  var isScheduled = false;
  var anyNeedsMeasurement = false;
  var isForced = false;
  function measureAllKeyframes() {
    if (anyNeedsMeasurement) {
      const resolversToMeasure = Array.from(toResolve).filter((resolver) => resolver.needsMeasurement);
      const elementsToMeasure = new Set(resolversToMeasure.map((resolver) => resolver.element));
      const transformsToRestore = /* @__PURE__ */ new Map();
      elementsToMeasure.forEach((element) => {
        const removedTransforms = removeNonTranslationalTransform(element);
        if (!removedTransforms.length)
          return;
        transformsToRestore.set(element, removedTransforms);
        element.render();
      });
      resolversToMeasure.forEach((resolver) => resolver.measureInitialState());
      elementsToMeasure.forEach((element) => {
        element.render();
        const restore = transformsToRestore.get(element);
        if (restore) {
          restore.forEach(([key, value]) => {
            element.getValue(key)?.set(value);
          });
        }
      });
      resolversToMeasure.forEach((resolver) => resolver.measureEndState());
      resolversToMeasure.forEach((resolver) => {
        if (resolver.suspendedScrollY !== void 0) {
          window.scrollTo(0, resolver.suspendedScrollY);
        }
      });
    }
    anyNeedsMeasurement = false;
    isScheduled = false;
    toResolve.forEach((resolver) => resolver.complete(isForced));
    toResolve.clear();
  }
  function readAllKeyframes() {
    toResolve.forEach((resolver) => {
      resolver.readKeyframes();
      if (resolver.needsMeasurement) {
        anyNeedsMeasurement = true;
      }
    });
  }
  function flushKeyframeResolvers() {
    isForced = true;
    readAllKeyframes();
    measureAllKeyframes();
    isForced = false;
  }
  var KeyframeResolver = class {
    constructor(unresolvedKeyframes, onComplete, name, motionValue2, element, isAsync = false) {
      this.state = "pending";
      this.isAsync = false;
      this.needsMeasurement = false;
      this.unresolvedKeyframes = [...unresolvedKeyframes];
      this.onComplete = onComplete;
      this.name = name;
      this.motionValue = motionValue2;
      this.element = element;
      this.isAsync = isAsync;
    }
    scheduleResolve() {
      this.state = "scheduled";
      if (this.isAsync) {
        toResolve.add(this);
        if (!isScheduled) {
          isScheduled = true;
          frame.read(readAllKeyframes);
          frame.resolveKeyframes(measureAllKeyframes);
        }
      } else {
        this.readKeyframes();
        this.complete();
      }
    }
    readKeyframes() {
      const { unresolvedKeyframes, name, element, motionValue: motionValue2 } = this;
      if (unresolvedKeyframes[0] === null) {
        const currentValue = motionValue2?.get();
        const finalKeyframe = unresolvedKeyframes[unresolvedKeyframes.length - 1];
        if (currentValue !== void 0) {
          unresolvedKeyframes[0] = currentValue;
        } else if (element && name) {
          const valueAsRead = element.readValue(name, finalKeyframe);
          if (valueAsRead !== void 0 && valueAsRead !== null) {
            unresolvedKeyframes[0] = valueAsRead;
          }
        }
        if (unresolvedKeyframes[0] === void 0) {
          unresolvedKeyframes[0] = finalKeyframe;
        }
        if (motionValue2 && currentValue === void 0) {
          motionValue2.set(unresolvedKeyframes[0]);
        }
      }
      fillWildcards(unresolvedKeyframes);
    }
    setFinalKeyframe() {
    }
    measureInitialState() {
    }
    renderEndStyles() {
    }
    measureEndState() {
    }
    complete(isForcedComplete = false) {
      this.state = "complete";
      this.onComplete(this.unresolvedKeyframes, this.finalKeyframe, isForcedComplete);
      toResolve.delete(this);
    }
    cancel() {
      if (this.state === "scheduled") {
        toResolve.delete(this);
        this.state = "pending";
      }
    }
    resume() {
      if (this.state === "pending")
        this.scheduleResolve();
    }
  };

  // frontend/node_modules/motion-dom/dist/es/animation/NativeAnimationExtended.mjs
  init_define_import_meta_env();

  // frontend/node_modules/motion-dom/dist/es/render/dom/style-set.mjs
  init_define_import_meta_env();

  // frontend/node_modules/motion-dom/dist/es/render/dom/is-css-var.mjs
  init_define_import_meta_env();
  var isCSSVar = (name) => name.startsWith("--");

  // frontend/node_modules/motion-dom/dist/es/render/dom/style-set.mjs
  function setStyle(element, name, value) {
    isCSSVar(name) ? element.style.setProperty(name, value) : element.style[name] = value;
  }

  // frontend/node_modules/motion-dom/dist/es/animation/NativeAnimation.mjs
  init_define_import_meta_env();

  // frontend/node_modules/motion-dom/dist/es/utils/supports/scroll-timeline.mjs
  init_define_import_meta_env();

  // frontend/node_modules/motion-dom/dist/es/utils/supports/memo.mjs
  init_define_import_meta_env();

  // frontend/node_modules/motion-dom/dist/es/utils/supports/flags.mjs
  init_define_import_meta_env();
  var supportsFlags = {};

  // frontend/node_modules/motion-dom/dist/es/utils/supports/memo.mjs
  function memoSupports(callback, supportsFlag) {
    const memoized = memo(callback);
    return () => supportsFlags[supportsFlag] ?? memoized();
  }

  // frontend/node_modules/motion-dom/dist/es/utils/supports/scroll-timeline.mjs
  var supportsScrollTimeline = /* @__PURE__ */ memoSupports(() => window.ScrollTimeline !== void 0, "scrollTimeline");

  // frontend/node_modules/motion-dom/dist/es/animation/waapi/start-waapi-animation.mjs
  init_define_import_meta_env();

  // frontend/node_modules/motion-dom/dist/es/animation/waapi/easing/map-easing.mjs
  init_define_import_meta_env();

  // frontend/node_modules/motion-dom/dist/es/utils/supports/linear-easing.mjs
  init_define_import_meta_env();
  var supportsLinearEasing = /* @__PURE__ */ memoSupports(() => {
    try {
      document.createElement("div").animate({ opacity: 0 }, { easing: "linear(0, 1)" });
    } catch (e8) {
      return false;
    }
    return true;
  }, "linearEasing");

  // frontend/node_modules/motion-dom/dist/es/animation/waapi/easing/cubic-bezier.mjs
  init_define_import_meta_env();
  var cubicBezierAsString = ([a21, b8, c14, d9]) => `cubic-bezier(${a21}, ${b8}, ${c14}, ${d9})`;

  // frontend/node_modules/motion-dom/dist/es/animation/waapi/easing/supported.mjs
  init_define_import_meta_env();
  var supportedWaapiEasing = {
    linear: "linear",
    ease: "ease",
    easeIn: "ease-in",
    easeOut: "ease-out",
    easeInOut: "ease-in-out",
    circIn: /* @__PURE__ */ cubicBezierAsString([0, 0.65, 0.55, 1]),
    circOut: /* @__PURE__ */ cubicBezierAsString([0.55, 0, 1, 0.45]),
    backIn: /* @__PURE__ */ cubicBezierAsString([0.31, 0.01, 0.66, -0.59]),
    backOut: /* @__PURE__ */ cubicBezierAsString([0.33, 1.53, 0.69, 0.99])
  };

  // frontend/node_modules/motion-dom/dist/es/animation/waapi/easing/map-easing.mjs
  function mapEasingToNativeEasing(easing, duration) {
    if (!easing) {
      return void 0;
    } else if (typeof easing === "function") {
      return supportsLinearEasing() ? generateLinearEasing(easing, duration) : "ease-out";
    } else if (isBezierDefinition(easing)) {
      return cubicBezierAsString(easing);
    } else if (Array.isArray(easing)) {
      return easing.map((segmentEasing) => mapEasingToNativeEasing(segmentEasing, duration) || supportedWaapiEasing.easeOut);
    } else {
      return supportedWaapiEasing[easing];
    }
  }

  // frontend/node_modules/motion-dom/dist/es/animation/waapi/start-waapi-animation.mjs
  function startWaapiAnimation(element, valueName, keyframes2, { delay: delay2 = 0, duration = 300, repeat = 0, repeatType = "loop", ease: ease2 = "easeOut", times } = {}, pseudoElement = void 0) {
    const keyframeOptions = {
      [valueName]: keyframes2
    };
    if (times)
      keyframeOptions.offset = times;
    const easing = mapEasingToNativeEasing(ease2, duration);
    if (Array.isArray(easing))
      keyframeOptions.easing = easing;
    const options = {
      delay: delay2,
      duration,
      easing: !Array.isArray(easing) ? easing : "linear",
      fill: "both",
      iterations: repeat + 1,
      direction: repeatType === "reverse" ? "alternate" : "normal"
    };
    if (pseudoElement)
      options.pseudoElement = pseudoElement;
    return element.animate(keyframeOptions, options);
  }

  // frontend/node_modules/motion-dom/dist/es/animation/waapi/utils/apply-generator.mjs
  init_define_import_meta_env();

  // frontend/node_modules/motion-dom/dist/es/animation/generators/utils/is-generator.mjs
  init_define_import_meta_env();
  function isGenerator(type) {
    return typeof type === "function" && "applyToOptions" in type;
  }

  // frontend/node_modules/motion-dom/dist/es/animation/waapi/utils/apply-generator.mjs
  function applyGeneratorOptions({ type, ...options }) {
    if (isGenerator(type) && supportsLinearEasing()) {
      return type.applyToOptions(options);
    } else {
      options.duration ?? (options.duration = 300);
      options.ease ?? (options.ease = "easeOut");
    }
    return options;
  }

  // frontend/node_modules/motion-dom/dist/es/animation/NativeAnimation.mjs
  var NativeAnimation = class extends WithPromise {
    constructor(options) {
      super();
      this.finishedTime = null;
      this.isStopped = false;
      this.manualStartTime = null;
      if (!options)
        return;
      const { element, name, keyframes: keyframes2, pseudoElement, allowFlatten = false, finalKeyframe, onComplete } = options;
      this.isPseudoElement = Boolean(pseudoElement);
      this.allowFlatten = allowFlatten;
      this.options = options;
      invariant(typeof options.type !== "string", `Mini animate() doesn't support "type" as a string.`, "mini-spring");
      const transition = applyGeneratorOptions(options);
      this.animation = startWaapiAnimation(element, name, keyframes2, transition, pseudoElement);
      if (transition.autoplay === false) {
        this.animation.pause();
      }
      this.animation.onfinish = () => {
        this.finishedTime = this.time;
        if (!pseudoElement) {
          const keyframe = getFinalKeyframe(keyframes2, this.options, finalKeyframe, this.speed);
          if (this.updateMotionValue) {
            this.updateMotionValue(keyframe);
          }
          setStyle(element, name, keyframe);
          this.animation.cancel();
        }
        onComplete?.();
        this.notifyFinished();
      };
    }
    play() {
      if (this.isStopped)
        return;
      this.manualStartTime = null;
      this.animation.play();
      if (this.state === "finished") {
        this.updateFinished();
      }
    }
    pause() {
      this.animation.pause();
    }
    complete() {
      this.animation.finish?.();
    }
    cancel() {
      try {
        this.animation.cancel();
      } catch (e8) {
      }
    }
    stop() {
      if (this.isStopped)
        return;
      this.isStopped = true;
      const { state } = this;
      if (state === "idle" || state === "finished") {
        return;
      }
      if (this.updateMotionValue) {
        this.updateMotionValue();
      } else {
        this.commitStyles();
      }
      if (!this.isPseudoElement)
        this.cancel();
    }
    /**
     * WAAPI doesn't natively have any interruption capabilities.
     *
     * In this method, we commit styles back to the DOM before cancelling
     * the animation.
     *
     * This is designed to be overridden by NativeAnimationExtended, which
     * will create a renderless JS animation and sample it twice to calculate
     * its current value, "previous" value, and therefore allow
     * Motion to also correctly calculate velocity for any subsequent animation
     * while deferring the commit until the next animation frame.
     */
    commitStyles() {
      const element = this.options?.element;
      if (!this.isPseudoElement && element?.isConnected) {
        this.animation.commitStyles?.();
      }
    }
    get duration() {
      const duration = this.animation.effect?.getComputedTiming?.().duration || 0;
      return millisecondsToSeconds(Number(duration));
    }
    get iterationDuration() {
      const { delay: delay2 = 0 } = this.options || {};
      return this.duration + millisecondsToSeconds(delay2);
    }
    get time() {
      return millisecondsToSeconds(Number(this.animation.currentTime) || 0);
    }
    set time(newTime) {
      const wasFinished = this.finishedTime !== null;
      this.manualStartTime = null;
      this.finishedTime = null;
      this.animation.currentTime = secondsToMilliseconds(newTime);
      if (wasFinished) {
        this.animation.pause();
      }
    }
    /**
     * The playback speed of the animation.
     * 1 = normal speed, 2 = double speed, 0.5 = half speed.
     */
    get speed() {
      return this.animation.playbackRate;
    }
    set speed(newSpeed) {
      if (newSpeed < 0)
        this.finishedTime = null;
      this.animation.playbackRate = newSpeed;
    }
    get state() {
      return this.finishedTime !== null ? "finished" : this.animation.playState;
    }
    get startTime() {
      return this.manualStartTime ?? Number(this.animation.startTime);
    }
    set startTime(newStartTime) {
      this.manualStartTime = this.animation.startTime = newStartTime;
    }
    /**
     * Attaches a timeline to the animation, for instance the `ScrollTimeline`.
     */
    attachTimeline({ timeline, rangeStart, rangeEnd, observe }) {
      if (this.allowFlatten) {
        this.animation.effect?.updateTiming({ easing: "linear" });
      }
      this.animation.onfinish = null;
      if (timeline && supportsScrollTimeline()) {
        this.animation.timeline = timeline;
        if (rangeStart)
          this.animation.rangeStart = rangeStart;
        if (rangeEnd)
          this.animation.rangeEnd = rangeEnd;
        return noop;
      } else {
        return observe(this);
      }
    }
  };

  // frontend/node_modules/motion-dom/dist/es/animation/waapi/utils/unsupported-easing.mjs
  init_define_import_meta_env();
  var unsupportedEasingFunctions = {
    anticipate,
    backInOut,
    circInOut
  };
  function isUnsupportedEase(key) {
    return key in unsupportedEasingFunctions;
  }
  function replaceStringEasing(transition) {
    if (typeof transition.ease === "string" && isUnsupportedEase(transition.ease)) {
      transition.ease = unsupportedEasingFunctions[transition.ease];
    }
  }

  // frontend/node_modules/motion-dom/dist/es/animation/NativeAnimationExtended.mjs
  var sampleDelta = 10;
  var NativeAnimationExtended = class extends NativeAnimation {
    constructor(options) {
      replaceStringEasing(options);
      replaceTransitionType(options);
      super(options);
      if (options.startTime !== void 0 && options.autoplay !== false) {
        this.startTime = options.startTime;
      }
      this.options = options;
    }
    /**
     * WAAPI doesn't natively have any interruption capabilities.
     *
     * Rather than read committed styles back out of the DOM, we can
     * create a renderless JS animation and sample it twice to calculate
     * its current value, "previous" value, and therefore allow
     * Motion to calculate velocity for any subsequent animation.
     */
    updateMotionValue(value) {
      const { motionValue: motionValue2, onUpdate, onComplete, element, ...options } = this.options;
      if (!motionValue2)
        return;
      if (value !== void 0) {
        motionValue2.set(value);
        return;
      }
      const sampleAnimation = new JSAnimation({
        ...options,
        autoplay: false
      });
      const sampleTime = Math.max(sampleDelta, time.now() - this.startTime);
      const delta = clamp(0, sampleDelta, sampleTime - sampleDelta);
      const current = sampleAnimation.sample(sampleTime).value;
      const { name } = this.options;
      if (element && name)
        setStyle(element, name, current);
      motionValue2.setWithVelocity(sampleAnimation.sample(Math.max(0, sampleTime - delta)).value, current, delta);
      sampleAnimation.stop();
    }
  };

  // frontend/node_modules/motion-dom/dist/es/animation/utils/can-animate.mjs
  init_define_import_meta_env();

  // frontend/node_modules/motion-dom/dist/es/animation/utils/is-animatable.mjs
  init_define_import_meta_env();
  var isAnimatable = (value, name) => {
    if (name === "zIndex")
      return false;
    if (typeof value === "number" || Array.isArray(value))
      return true;
    if (typeof value === "string" && // It's animatable if we have a string
    (complex.test(value) || value === "0") && // And it contains numbers and/or colors
    !value.startsWith("url(")) {
      return true;
    }
    return false;
  };

  // frontend/node_modules/motion-dom/dist/es/animation/utils/can-animate.mjs
  function hasKeyframesChanged(keyframes2) {
    const current = keyframes2[0];
    if (keyframes2.length === 1)
      return true;
    for (let i13 = 0; i13 < keyframes2.length; i13++) {
      if (keyframes2[i13] !== current)
        return true;
    }
  }
  function canAnimate(keyframes2, name, type, velocity) {
    const originKeyframe = keyframes2[0];
    if (originKeyframe === null) {
      return false;
    }
    if (name === "display" || name === "visibility")
      return true;
    const targetKeyframe = keyframes2[keyframes2.length - 1];
    const isOriginAnimatable = isAnimatable(originKeyframe, name);
    const isTargetAnimatable = isAnimatable(targetKeyframe, name);
    warning(isOriginAnimatable === isTargetAnimatable, `You are trying to animate ${name} from "${originKeyframe}" to "${targetKeyframe}". "${isOriginAnimatable ? targetKeyframe : originKeyframe}" is not an animatable value.`, "value-not-animatable");
    if (!isOriginAnimatable || !isTargetAnimatable) {
      return false;
    }
    return hasKeyframesChanged(keyframes2) || (type === "spring" || isGenerator(type)) && velocity;
  }

  // frontend/node_modules/motion-dom/dist/es/animation/utils/make-animation-instant.mjs
  init_define_import_meta_env();
  function makeAnimationInstant(options) {
    options.duration = 0;
    options.type = "keyframes";
  }

  // frontend/node_modules/motion-dom/dist/es/animation/waapi/supports/waapi.mjs
  init_define_import_meta_env();

  // frontend/node_modules/motion-dom/dist/es/animation/waapi/utils/accelerated-values.mjs
  init_define_import_meta_env();
  var acceleratedValues = /* @__PURE__ */ new Set([
    "opacity",
    "clipPath",
    "filter",
    "transform"
    // TODO: Can be accelerated but currently disabled until https://issues.chromium.org/issues/41491098 is resolved
    // or until we implement support for linear() easing.
    // "background-color"
  ]);

  // frontend/node_modules/motion-dom/dist/es/animation/waapi/utils/is-browser-color.mjs
  init_define_import_meta_env();
  var browserColorFunctions = /^(?:oklch|oklab|lab|lch|color|color-mix|light-dark)\(/;
  function hasBrowserOnlyColors(keyframes2) {
    for (let i13 = 0; i13 < keyframes2.length; i13++) {
      if (typeof keyframes2[i13] === "string" && browserColorFunctions.test(keyframes2[i13])) {
        return true;
      }
    }
    return false;
  }

  // frontend/node_modules/motion-dom/dist/es/animation/waapi/supports/waapi.mjs
  var colorProperties = /* @__PURE__ */ new Set([
    "color",
    "backgroundColor",
    "outlineColor",
    "fill",
    "stroke",
    "borderColor",
    "borderTopColor",
    "borderRightColor",
    "borderBottomColor",
    "borderLeftColor"
  ]);
  var supportsWaapi = /* @__PURE__ */ memo(() => Object.hasOwnProperty.call(Element.prototype, "animate"));
  function supportsBrowserAnimation(options) {
    const { motionValue: motionValue2, name, repeatDelay, repeatType, damping, type, keyframes: keyframes2 } = options;
    const subject = motionValue2?.owner?.current;
    if (!(subject instanceof HTMLElement)) {
      return false;
    }
    const { onUpdate, transformTemplate } = motionValue2.owner.getProps();
    return supportsWaapi() && name && /**
     * Force WAAPI for color properties with browser-only color formats
     * (oklch, oklab, lab, lch, etc.) that the JS animation path can't parse.
     */
    (acceleratedValues.has(name) || colorProperties.has(name) && hasBrowserOnlyColors(keyframes2)) && (name !== "transform" || !transformTemplate) && /**
     * If we're outputting values to onUpdate then we can't use WAAPI as there's
     * no way to read the value from WAAPI every frame.
     */
    !onUpdate && !repeatDelay && repeatType !== "mirror" && damping !== 0 && type !== "inertia";
  }

  // frontend/node_modules/motion-dom/dist/es/animation/AsyncMotionValueAnimation.mjs
  var MAX_RESOLVE_DELAY = 40;
  var AsyncMotionValueAnimation = class extends WithPromise {
    constructor({ autoplay = true, delay: delay2 = 0, type = "keyframes", repeat = 0, repeatDelay = 0, repeatType = "loop", keyframes: keyframes2, name, motionValue: motionValue2, element, ...options }) {
      super();
      this.stop = () => {
        if (this._animation) {
          this._animation.stop();
          this.stopTimeline?.();
        }
        this.keyframeResolver?.cancel();
      };
      this.createdAt = time.now();
      const optionsWithDefaults = {
        autoplay,
        delay: delay2,
        type,
        repeat,
        repeatDelay,
        repeatType,
        name,
        motionValue: motionValue2,
        element,
        ...options
      };
      const KeyframeResolver$1 = element?.KeyframeResolver || KeyframeResolver;
      this.keyframeResolver = new KeyframeResolver$1(keyframes2, (resolvedKeyframes, finalKeyframe, forced) => this.onKeyframesResolved(resolvedKeyframes, finalKeyframe, optionsWithDefaults, !forced), name, motionValue2, element);
      this.keyframeResolver?.scheduleResolve();
    }
    onKeyframesResolved(keyframes2, finalKeyframe, options, sync) {
      this.keyframeResolver = void 0;
      const { name, type, velocity, delay: delay2, isHandoff, onUpdate } = options;
      this.resolvedAt = time.now();
      let canAnimateValue = true;
      if (!canAnimate(keyframes2, name, type, velocity)) {
        canAnimateValue = false;
        if (MotionGlobalConfig.instantAnimations || !delay2) {
          onUpdate?.(getFinalKeyframe(keyframes2, options, finalKeyframe));
        }
        keyframes2[0] = keyframes2[keyframes2.length - 1];
        makeAnimationInstant(options);
        options.repeat = 0;
      }
      const startTime = sync ? !this.resolvedAt ? this.createdAt : this.resolvedAt - this.createdAt > MAX_RESOLVE_DELAY ? this.resolvedAt : this.createdAt : void 0;
      const resolvedOptions = {
        startTime,
        finalKeyframe,
        ...options,
        keyframes: keyframes2
      };
      const useWaapi = canAnimateValue && !isHandoff && supportsBrowserAnimation(resolvedOptions);
      const element = resolvedOptions.motionValue?.owner?.current;
      let animation;
      if (useWaapi) {
        try {
          animation = new NativeAnimationExtended({
            ...resolvedOptions,
            element
          });
        } catch {
          animation = new JSAnimation(resolvedOptions);
        }
      } else {
        animation = new JSAnimation(resolvedOptions);
      }
      animation.finished.then(() => {
        this.notifyFinished();
      }).catch(noop);
      if (this.pendingTimeline) {
        this.stopTimeline = animation.attachTimeline(this.pendingTimeline);
        this.pendingTimeline = void 0;
      }
      this._animation = animation;
    }
    get finished() {
      if (!this._animation) {
        return this._finished;
      } else {
        return this.animation.finished;
      }
    }
    then(onResolve, _onReject) {
      return this.finished.finally(onResolve).then(() => {
      });
    }
    get animation() {
      if (!this._animation) {
        this.keyframeResolver?.resume();
        flushKeyframeResolvers();
      }
      return this._animation;
    }
    get duration() {
      return this.animation.duration;
    }
    get iterationDuration() {
      return this.animation.iterationDuration;
    }
    get time() {
      return this.animation.time;
    }
    set time(newTime) {
      this.animation.time = newTime;
    }
    get speed() {
      return this.animation.speed;
    }
    get state() {
      return this.animation.state;
    }
    set speed(newSpeed) {
      this.animation.speed = newSpeed;
    }
    get startTime() {
      return this.animation.startTime;
    }
    attachTimeline(timeline) {
      if (this._animation) {
        this.stopTimeline = this.animation.attachTimeline(timeline);
      } else {
        this.pendingTimeline = timeline;
      }
      return () => this.stop();
    }
    play() {
      this.animation.play();
    }
    pause() {
      this.animation.pause();
    }
    complete() {
      this.animation.complete();
    }
    cancel() {
      if (this._animation) {
        this.animation.cancel();
      }
      this.keyframeResolver?.cancel();
    }
  };

  // frontend/node_modules/motion-dom/dist/es/animation/utils/calc-child-stagger.mjs
  init_define_import_meta_env();
  function calcChildStagger(children, child, delayChildren, staggerChildren = 0, staggerDirection = 1) {
    const index3 = Array.from(children).sort((a21, b8) => a21.sortNodePosition(b8)).indexOf(child);
    const numChildren = children.size;
    const maxStaggerDuration = (numChildren - 1) * staggerChildren;
    const delayIsFunction = typeof delayChildren === "function";
    return delayIsFunction ? delayChildren(index3, numChildren) : staggerDirection === 1 ? index3 * staggerChildren : maxStaggerDuration - index3 * staggerChildren;
  }

  // frontend/node_modules/motion-dom/dist/es/value/index.mjs
  init_define_import_meta_env();
  var MAX_VELOCITY_DELTA = 30;
  var isFloat = (value) => {
    return !isNaN(parseFloat(value));
  };
  var collectMotionValues = {
    current: void 0
  };
  var MotionValue = class {
    /**
     * @param init - The initiating value
     * @param config - Optional configuration options
     *
     * -  `transformer`: A function to transform incoming values with.
     */
    constructor(init, options = {}) {
      this.canTrackVelocity = null;
      this.events = {};
      this.updateAndNotify = (v6) => {
        const currentTime = time.now();
        if (this.updatedAt !== currentTime) {
          this.setPrevFrameValue();
        }
        this.prev = this.current;
        this.setCurrent(v6);
        if (this.current !== this.prev) {
          this.events.change?.notify(this.current);
          if (this.dependents) {
            for (const dependent of this.dependents) {
              dependent.dirty();
            }
          }
        }
      };
      this.hasAnimated = false;
      this.setCurrent(init);
      this.owner = options.owner;
    }
    setCurrent(current) {
      this.current = current;
      this.updatedAt = time.now();
      if (this.canTrackVelocity === null && current !== void 0) {
        this.canTrackVelocity = isFloat(this.current);
      }
    }
    setPrevFrameValue(prevFrameValue = this.current) {
      this.prevFrameValue = prevFrameValue;
      this.prevUpdatedAt = this.updatedAt;
    }
    /**
     * Adds a function that will be notified when the `MotionValue` is updated.
     *
     * It returns a function that, when called, will cancel the subscription.
     *
     * When calling `onChange` inside a React component, it should be wrapped with the
     * `useEffect` hook. As it returns an unsubscribe function, this should be returned
     * from the `useEffect` function to ensure you don't add duplicate subscribers..
     *
     * ```jsx
     * export const MyComponent = () => {
     *   const x = useMotionValue(0)
     *   const y = useMotionValue(0)
     *   const opacity = useMotionValue(1)
     *
     *   useEffect(() => {
     *     function updateOpacity() {
     *       const maxXY = Math.max(x.get(), y.get())
     *       const newOpacity = transform(maxXY, [0, 100], [1, 0])
     *       opacity.set(newOpacity)
     *     }
     *
     *     const unsubscribeX = x.on("change", updateOpacity)
     *     const unsubscribeY = y.on("change", updateOpacity)
     *
     *     return () => {
     *       unsubscribeX()
     *       unsubscribeY()
     *     }
     *   }, [])
     *
     *   return <motion.div style={{ x }} />
     * }
     * ```
     *
     * @param subscriber - A function that receives the latest value.
     * @returns A function that, when called, will cancel this subscription.
     *
     * @deprecated
     */
    onChange(subscription) {
      if (true) {
        warnOnce(false, `value.onChange(callback) is deprecated. Switch to value.on("change", callback).`);
      }
      return this.on("change", subscription);
    }
    on(eventName, callback) {
      if (!this.events[eventName]) {
        this.events[eventName] = new SubscriptionManager();
      }
      const unsubscribe = this.events[eventName].add(callback);
      if (eventName === "change") {
        return () => {
          unsubscribe();
          frame.read(() => {
            if (!this.events.change.getSize()) {
              this.stop();
            }
          });
        };
      }
      return unsubscribe;
    }
    clearListeners() {
      for (const eventManagers in this.events) {
        this.events[eventManagers].clear();
      }
    }
    /**
     * Attaches a passive effect to the `MotionValue`.
     */
    attach(passiveEffect, stopPassiveEffect) {
      this.passiveEffect = passiveEffect;
      this.stopPassiveEffect = stopPassiveEffect;
    }
    /**
     * Sets the state of the `MotionValue`.
     *
     * @remarks
     *
     * ```jsx
     * const x = useMotionValue(0)
     * x.set(10)
     * ```
     *
     * @param latest - Latest value to set.
     * @param render - Whether to notify render subscribers. Defaults to `true`
     *
     * @public
     */
    set(v6) {
      if (!this.passiveEffect) {
        this.updateAndNotify(v6);
      } else {
        this.passiveEffect(v6, this.updateAndNotify);
      }
    }
    setWithVelocity(prev, current, delta) {
      this.set(current);
      this.prev = void 0;
      this.prevFrameValue = prev;
      this.prevUpdatedAt = this.updatedAt - delta;
    }
    /**
     * Set the state of the `MotionValue`, stopping any active animations,
     * effects, and resets velocity to `0`.
     */
    jump(v6, endAnimation = true) {
      this.updateAndNotify(v6);
      this.prev = v6;
      this.prevUpdatedAt = this.prevFrameValue = void 0;
      endAnimation && this.stop();
      if (this.stopPassiveEffect)
        this.stopPassiveEffect();
    }
    dirty() {
      this.events.change?.notify(this.current);
    }
    addDependent(dependent) {
      if (!this.dependents) {
        this.dependents = /* @__PURE__ */ new Set();
      }
      this.dependents.add(dependent);
    }
    removeDependent(dependent) {
      if (this.dependents) {
        this.dependents.delete(dependent);
      }
    }
    /**
     * Returns the latest state of `MotionValue`
     *
     * @returns - The latest state of `MotionValue`
     *
     * @public
     */
    get() {
      if (collectMotionValues.current) {
        collectMotionValues.current.push(this);
      }
      return this.current;
    }
    /**
     * @public
     */
    getPrevious() {
      return this.prev;
    }
    /**
     * Returns the latest velocity of `MotionValue`
     *
     * @returns - The latest velocity of `MotionValue`. Returns `0` if the state is non-numerical.
     *
     * @public
     */
    getVelocity() {
      const currentTime = time.now();
      if (!this.canTrackVelocity || this.prevFrameValue === void 0 || currentTime - this.updatedAt > MAX_VELOCITY_DELTA) {
        return 0;
      }
      const delta = Math.min(this.updatedAt - this.prevUpdatedAt, MAX_VELOCITY_DELTA);
      return velocityPerSecond(parseFloat(this.current) - parseFloat(this.prevFrameValue), delta);
    }
    /**
     * Registers a new animation to control this `MotionValue`. Only one
     * animation can drive a `MotionValue` at one time.
     *
     * ```jsx
     * value.start()
     * ```
     *
     * @param animation - A function that starts the provided animation
     */
    start(startAnimation) {
      this.stop();
      return new Promise((resolve) => {
        this.hasAnimated = true;
        this.animation = startAnimation(resolve);
        if (this.events.animationStart) {
          this.events.animationStart.notify();
        }
      }).then(() => {
        if (this.events.animationComplete) {
          this.events.animationComplete.notify();
        }
        this.clearAnimation();
      });
    }
    /**
     * Stop the currently active animation.
     *
     * @public
     */
    stop() {
      if (this.animation) {
        this.animation.stop();
        if (this.events.animationCancel) {
          this.events.animationCancel.notify();
        }
      }
      this.clearAnimation();
    }
    /**
     * Returns `true` if this value is currently animating.
     *
     * @public
     */
    isAnimating() {
      return !!this.animation;
    }
    clearAnimation() {
      delete this.animation;
    }
    /**
     * Destroy and clean up subscribers to this `MotionValue`.
     *
     * The `MotionValue` hooks like `useMotionValue` and `useTransform` automatically
     * handle the lifecycle of the returned `MotionValue`, so this method is only necessary if you've manually
     * created a `MotionValue` via the `motionValue` function.
     *
     * @public
     */
    destroy() {
      this.dependents?.clear();
      this.events.destroy?.notify();
      this.clearListeners();
      this.stop();
      if (this.stopPassiveEffect) {
        this.stopPassiveEffect();
      }
    }
  };
  function motionValue(init, options) {
    return new MotionValue(init, options);
  }

  // frontend/node_modules/motion-dom/dist/es/animation/interfaces/motion-value.mjs
  init_define_import_meta_env();

  // frontend/node_modules/motion-dom/dist/es/animation/utils/get-value-transition.mjs
  init_define_import_meta_env();

  // frontend/node_modules/motion-dom/dist/es/animation/utils/resolve-transition.mjs
  init_define_import_meta_env();
  function resolveTransition(transition, parentTransition) {
    if (transition?.inherit && parentTransition) {
      const { inherit: _5, ...rest } = transition;
      return { ...parentTransition, ...rest };
    }
    return transition;
  }

  // frontend/node_modules/motion-dom/dist/es/animation/utils/get-value-transition.mjs
  function getValueTransition(transition, key) {
    const valueTransition = transition?.[key] ?? transition?.["default"] ?? transition;
    if (valueTransition !== transition) {
      return resolveTransition(valueTransition, transition);
    }
    return valueTransition;
  }

  // frontend/node_modules/motion-dom/dist/es/animation/utils/default-transitions.mjs
  init_define_import_meta_env();
  var underDampedSpring = {
    type: "spring",
    stiffness: 500,
    damping: 25,
    restSpeed: 10
  };
  var criticallyDampedSpring = (target) => ({
    type: "spring",
    stiffness: 550,
    damping: target === 0 ? 2 * Math.sqrt(550) : 30,
    restSpeed: 10
  });
  var keyframesTransition = {
    type: "keyframes",
    duration: 0.8
  };
  var ease = {
    type: "keyframes",
    ease: [0.25, 0.1, 0.35, 1],
    duration: 0.3
  };
  var getDefaultTransition = (valueKey, { keyframes: keyframes2 }) => {
    if (keyframes2.length > 2) {
      return keyframesTransition;
    } else if (transformProps.has(valueKey)) {
      return valueKey.startsWith("scale") ? criticallyDampedSpring(keyframes2[1]) : underDampedSpring;
    }
    return ease;
  };

  // frontend/node_modules/motion-dom/dist/es/animation/utils/is-transition-defined.mjs
  init_define_import_meta_env();
  var orchestrationKeys = /* @__PURE__ */ new Set([
    "when",
    "delay",
    "delayChildren",
    "staggerChildren",
    "staggerDirection",
    "repeat",
    "repeatType",
    "repeatDelay",
    "from",
    "elapsed"
  ]);
  function isTransitionDefined(transition) {
    for (const key in transition) {
      if (!orchestrationKeys.has(key))
        return true;
    }
    return false;
  }

  // frontend/node_modules/motion-dom/dist/es/animation/interfaces/motion-value.mjs
  var animateMotionValue = (name, value, target, transition = {}, element, isHandoff) => (onComplete) => {
    const valueTransition = getValueTransition(transition, name) || {};
    const delay2 = valueTransition.delay || transition.delay || 0;
    let { elapsed = 0 } = transition;
    elapsed = elapsed - secondsToMilliseconds(delay2);
    const options = {
      keyframes: Array.isArray(target) ? target : [null, target],
      ease: "easeOut",
      velocity: value.getVelocity(),
      ...valueTransition,
      delay: -elapsed,
      onUpdate: (v6) => {
        value.set(v6);
        valueTransition.onUpdate && valueTransition.onUpdate(v6);
      },
      onComplete: () => {
        onComplete();
        valueTransition.onComplete && valueTransition.onComplete();
      },
      name,
      motionValue: value,
      element: isHandoff ? void 0 : element
    };
    if (!isTransitionDefined(valueTransition)) {
      Object.assign(options, getDefaultTransition(name, options));
    }
    options.duration && (options.duration = secondsToMilliseconds(options.duration));
    options.repeatDelay && (options.repeatDelay = secondsToMilliseconds(options.repeatDelay));
    if (options.from !== void 0) {
      options.keyframes[0] = options.from;
    }
    let shouldSkip = false;
    if (options.type === false || options.duration === 0 && !options.repeatDelay) {
      makeAnimationInstant(options);
      if (options.delay === 0) {
        shouldSkip = true;
      }
    }
    if (MotionGlobalConfig.instantAnimations || MotionGlobalConfig.skipAnimations || element?.shouldSkipAnimations || valueTransition.skipAnimations) {
      shouldSkip = true;
      makeAnimationInstant(options);
      options.delay = 0;
    }
    options.allowFlatten = !valueTransition.type && !valueTransition.ease;
    if (shouldSkip && !isHandoff && value.get() !== void 0) {
      const finalKeyframe = getFinalKeyframe(options.keyframes, valueTransition);
      if (finalKeyframe !== void 0) {
        frame.update(() => {
          options.onUpdate(finalKeyframe);
          options.onComplete();
        });
        return;
      }
    }
    return valueTransition.isSync ? new JSAnimation(options) : new AsyncMotionValueAnimation(options);
  };

  // frontend/node_modules/motion-dom/dist/es/animation/utils/css-variables-conversion.mjs
  init_define_import_meta_env();
  var splitCSSVariableRegex = (
    // eslint-disable-next-line redos-detector/no-unsafe-regex -- false positive, as it can match a lot of words
    /^var\(--(?:([\w-]+)|([\w-]+), ?([a-zA-Z\d ()%#.,-]+))\)/u
  );
  function parseCSSVariable(current) {
    const match = splitCSSVariableRegex.exec(current);
    if (!match)
      return [,];
    const [, token1, token2, fallback] = match;
    return [`--${token1 ?? token2}`, fallback];
  }
  var maxDepth = 4;
  function getVariableValue(current, element, depth = 1) {
    invariant(depth <= maxDepth, `Max CSS variable fallback depth detected in property "${current}". This may indicate a circular fallback dependency.`, "max-css-var-depth");
    const [token, fallback] = parseCSSVariable(current);
    if (!token)
      return;
    const resolved = window.getComputedStyle(element).getPropertyValue(token);
    if (resolved) {
      const trimmed = resolved.trim();
      return isNumericalString(trimmed) ? parseFloat(trimmed) : trimmed;
    }
    return isCSSVariableToken(fallback) ? getVariableValue(fallback, element, depth + 1) : fallback;
  }

  // frontend/node_modules/motion-dom/dist/es/animation/interfaces/visual-element.mjs
  init_define_import_meta_env();

  // frontend/node_modules/motion-dom/dist/es/render/utils/resolve-dynamic-variants.mjs
  init_define_import_meta_env();

  // frontend/node_modules/motion-dom/dist/es/render/utils/resolve-variants.mjs
  init_define_import_meta_env();
  function getValueState(visualElement) {
    const state = [{}, {}];
    visualElement?.values.forEach((value, key) => {
      state[0][key] = value.get();
      state[1][key] = value.getVelocity();
    });
    return state;
  }
  function resolveVariantFromProps(props, definition, custom, visualElement) {
    if (typeof definition === "function") {
      const [current, velocity] = getValueState(visualElement);
      definition = definition(custom !== void 0 ? custom : props.custom, current, velocity);
    }
    if (typeof definition === "string") {
      definition = props.variants && props.variants[definition];
    }
    if (typeof definition === "function") {
      const [current, velocity] = getValueState(visualElement);
      definition = definition(custom !== void 0 ? custom : props.custom, current, velocity);
    }
    return definition;
  }

  // frontend/node_modules/motion-dom/dist/es/render/utils/resolve-dynamic-variants.mjs
  function resolveVariant(visualElement, definition, custom) {
    const props = visualElement.getProps();
    return resolveVariantFromProps(props, definition, custom !== void 0 ? custom : props.custom, visualElement);
  }

  // frontend/node_modules/motion-dom/dist/es/animation/interfaces/visual-element-target.mjs
  init_define_import_meta_env();

  // frontend/node_modules/motion-dom/dist/es/render/utils/keys-position.mjs
  init_define_import_meta_env();
  var positionalKeys = /* @__PURE__ */ new Set([
    "width",
    "height",
    "top",
    "left",
    "right",
    "bottom",
    ...transformPropOrder
  ]);

  // frontend/node_modules/motion-dom/dist/es/render/utils/setters.mjs
  init_define_import_meta_env();

  // frontend/node_modules/motion-dom/dist/es/render/utils/is-keyframes-target.mjs
  init_define_import_meta_env();
  var isKeyframesTarget = (v6) => {
    return Array.isArray(v6);
  };

  // frontend/node_modules/motion-dom/dist/es/render/utils/setters.mjs
  function setMotionValue(visualElement, key, value) {
    if (visualElement.hasValue(key)) {
      visualElement.getValue(key).set(value);
    } else {
      visualElement.addValue(key, motionValue(value));
    }
  }
  function resolveFinalValueInKeyframes(v6) {
    return isKeyframesTarget(v6) ? v6[v6.length - 1] || 0 : v6;
  }
  function setTarget(visualElement, definition) {
    const resolved = resolveVariant(visualElement, definition);
    let { transitionEnd = {}, transition = {}, ...target } = resolved || {};
    target = { ...target, ...transitionEnd };
    for (const key in target) {
      const value = resolveFinalValueInKeyframes(target[key]);
      setMotionValue(visualElement, key, value);
    }
  }

  // frontend/node_modules/motion-dom/dist/es/value/will-change/add-will-change.mjs
  init_define_import_meta_env();

  // frontend/node_modules/motion-dom/dist/es/value/will-change/is.mjs
  init_define_import_meta_env();

  // frontend/node_modules/motion-dom/dist/es/value/utils/is-motion-value.mjs
  init_define_import_meta_env();
  var isMotionValue = (value) => Boolean(value && value.getVelocity);

  // frontend/node_modules/motion-dom/dist/es/value/will-change/is.mjs
  function isWillChangeMotionValue(value) {
    return Boolean(isMotionValue(value) && value.add);
  }

  // frontend/node_modules/motion-dom/dist/es/value/will-change/add-will-change.mjs
  function addValueToWillChange(visualElement, key) {
    const willChange = visualElement.getValue("willChange");
    if (isWillChangeMotionValue(willChange)) {
      return willChange.add(key);
    } else if (!willChange && MotionGlobalConfig.WillChange) {
      const newWillChange = new MotionGlobalConfig.WillChange("auto");
      visualElement.addValue("willChange", newWillChange);
      newWillChange.add(key);
    }
  }

  // frontend/node_modules/motion-dom/dist/es/animation/optimized-appear/get-appear-id.mjs
  init_define_import_meta_env();

  // frontend/node_modules/motion-dom/dist/es/animation/optimized-appear/data-id.mjs
  init_define_import_meta_env();

  // frontend/node_modules/motion-dom/dist/es/render/dom/utils/camel-to-dash.mjs
  init_define_import_meta_env();
  function camelToDash(str) {
    return str.replace(/([A-Z])/g, (match) => `-${match.toLowerCase()}`);
  }

  // frontend/node_modules/motion-dom/dist/es/animation/optimized-appear/data-id.mjs
  var optimizedAppearDataId = "framerAppearId";
  var optimizedAppearDataAttribute = "data-" + camelToDash(optimizedAppearDataId);

  // frontend/node_modules/motion-dom/dist/es/animation/optimized-appear/get-appear-id.mjs
  function getOptimisedAppearId(visualElement) {
    return visualElement.props[optimizedAppearDataAttribute];
  }

  // frontend/node_modules/motion-dom/dist/es/animation/interfaces/visual-element-target.mjs
  function shouldBlockAnimation({ protectedKeys, needsAnimating }, key) {
    const shouldBlock = protectedKeys.hasOwnProperty(key) && needsAnimating[key] !== true;
    needsAnimating[key] = false;
    return shouldBlock;
  }
  function animateTarget(visualElement, targetAndTransition, { delay: delay2 = 0, transitionOverride, type } = {}) {
    let { transition, transitionEnd, ...target } = targetAndTransition;
    const defaultTransition = visualElement.getDefaultTransition();
    transition = transition ? resolveTransition(transition, defaultTransition) : defaultTransition;
    const reduceMotion = transition?.reduceMotion;
    const skipAnimations = transition?.skipAnimations;
    if (transitionOverride)
      transition = transitionOverride;
    const animations2 = [];
    const animationTypeState = type && visualElement.animationState && visualElement.animationState.getState()[type];
    const path = transition?.path;
    if (path) {
      path.animateVisualElement(visualElement, target, transition, delay2, animations2);
    }
    for (const key in target) {
      const value = visualElement.getValue(key, visualElement.latestValues[key] ?? null);
      const valueTarget = target[key];
      if (valueTarget === void 0 || animationTypeState && shouldBlockAnimation(animationTypeState, key)) {
        continue;
      }
      const valueTransition = {
        delay: delay2,
        ...getValueTransition(transition || {}, key)
      };
      if (skipAnimations)
        valueTransition.skipAnimations = true;
      const currentValue = value.get();
      if (currentValue !== void 0 && !value.isAnimating() && !Array.isArray(valueTarget) && valueTarget === currentValue && !valueTransition.velocity) {
        frame.update(() => value.set(valueTarget));
        continue;
      }
      let isHandoff = false;
      if (window.MotionHandoffAnimation) {
        const appearId = getOptimisedAppearId(visualElement);
        if (appearId) {
          const startTime = window.MotionHandoffAnimation(appearId, key, frame);
          if (startTime !== null) {
            valueTransition.startTime = startTime;
            isHandoff = true;
          }
        }
      }
      addValueToWillChange(visualElement, key);
      const shouldReduceMotion = reduceMotion ?? visualElement.shouldReduceMotion;
      value.start(animateMotionValue(key, value, valueTarget, shouldReduceMotion && positionalKeys.has(key) ? { type: false } : valueTransition, visualElement, isHandoff));
      const animation = value.animation;
      if (animation) {
        animations2.push(animation);
      }
    }
    if (transitionEnd) {
      const applyTransitionEnd = () => frame.update(() => {
        transitionEnd && setTarget(visualElement, transitionEnd);
      });
      if (animations2.length) {
        Promise.all(animations2).then(applyTransitionEnd);
      } else {
        applyTransitionEnd();
      }
    }
    return animations2;
  }

  // frontend/node_modules/motion-dom/dist/es/animation/interfaces/visual-element-variant.mjs
  init_define_import_meta_env();
  function animateVariant(visualElement, variant, options = {}) {
    const resolved = resolveVariant(visualElement, variant, options.type === "exit" ? visualElement.presenceContext?.custom : void 0);
    let { transition = visualElement.getDefaultTransition() || {} } = resolved || {};
    if (options.transitionOverride) {
      transition = options.transitionOverride;
    }
    const getAnimation = resolved ? () => Promise.all(animateTarget(visualElement, resolved, options)) : () => Promise.resolve();
    const getChildAnimations = visualElement.variantChildren && visualElement.variantChildren.size ? (forwardDelay = 0) => {
      const { delayChildren = 0, staggerChildren, staggerDirection } = transition;
      return animateChildren(visualElement, variant, forwardDelay, delayChildren, staggerChildren, staggerDirection, options);
    } : () => Promise.resolve();
    const { when } = transition;
    if (when) {
      const [first, last] = when === "beforeChildren" ? [getAnimation, getChildAnimations] : [getChildAnimations, getAnimation];
      return first().then(() => last());
    } else {
      return Promise.all([getAnimation(), getChildAnimations(options.delay)]);
    }
  }
  function animateChildren(visualElement, variant, delay2 = 0, delayChildren = 0, staggerChildren = 0, staggerDirection = 1, options) {
    const animations2 = [];
    for (const child of visualElement.variantChildren) {
      child.notify("AnimationStart", variant);
      animations2.push(animateVariant(child, variant, {
        ...options,
        delay: delay2 + (typeof delayChildren === "function" ? 0 : delayChildren) + calcChildStagger(visualElement.variantChildren, child, delayChildren, staggerChildren, staggerDirection)
      }).then(() => child.notify("AnimationComplete", variant)));
    }
    return Promise.all(animations2);
  }

  // frontend/node_modules/motion-dom/dist/es/animation/interfaces/visual-element.mjs
  function animateVisualElement(visualElement, definition, options = {}) {
    visualElement.notify("AnimationStart", definition);
    let animation;
    if (Array.isArray(definition)) {
      const animations2 = definition.map((variant) => animateVariant(visualElement, variant, options));
      animation = Promise.all(animations2);
    } else if (typeof definition === "string") {
      animation = animateVariant(visualElement, definition, options);
    } else {
      const resolvedDefinition = typeof definition === "function" ? resolveVariant(visualElement, definition, options.custom) : definition;
      animation = Promise.all(animateTarget(visualElement, resolvedDefinition, options));
    }
    return animation.then(() => {
      visualElement.notify("AnimationComplete", definition);
    });
  }

  // frontend/node_modules/motion-dom/dist/es/animation/keyframes/DOMKeyframesResolver.mjs
  init_define_import_meta_env();

  // frontend/node_modules/motion-dom/dist/es/value/types/dimensions.mjs
  init_define_import_meta_env();

  // frontend/node_modules/motion-dom/dist/es/value/types/auto.mjs
  init_define_import_meta_env();
  var auto = {
    test: (v6) => v6 === "auto",
    parse: (v6) => v6
  };

  // frontend/node_modules/motion-dom/dist/es/value/types/test.mjs
  init_define_import_meta_env();
  var testValueType = (v6) => (type) => type.test(v6);

  // frontend/node_modules/motion-dom/dist/es/value/types/dimensions.mjs
  var dimensionValueTypes = [number, px, percent, degrees, vw, vh, auto];
  var findDimensionValueType = (v6) => dimensionValueTypes.find(testValueType(v6));

  // frontend/node_modules/motion-dom/dist/es/animation/keyframes/utils/is-none.mjs
  init_define_import_meta_env();
  function isNone(value) {
    if (typeof value === "number") {
      return value === 0;
    } else if (value !== null) {
      return value === "none" || value === "0" || isZeroValueString(value);
    } else {
      return true;
    }
  }

  // frontend/node_modules/motion-dom/dist/es/animation/keyframes/utils/make-none-animatable.mjs
  init_define_import_meta_env();

  // frontend/node_modules/motion-dom/dist/es/value/types/utils/animatable-none.mjs
  init_define_import_meta_env();

  // frontend/node_modules/motion-dom/dist/es/value/types/complex/filter.mjs
  init_define_import_meta_env();
  var maxDefaults = /* @__PURE__ */ new Set(["brightness", "contrast", "saturate", "opacity"]);
  function applyDefaultFilter(v6) {
    const [name, value] = v6.slice(0, -1).split("(");
    if (name === "drop-shadow")
      return v6;
    const [number2] = value.match(floatRegex) || [];
    if (!number2)
      return v6;
    const unit = value.replace(number2, "");
    let defaultValue = maxDefaults.has(name) ? 1 : 0;
    if (number2 !== value)
      defaultValue *= 100;
    return name + "(" + defaultValue + unit + ")";
  }
  var functionRegex = /\b([a-z-]*)\(.*?\)/gu;
  var filter = {
    ...complex,
    getAnimatableNone: (v6) => {
      const functions = v6.match(functionRegex);
      return functions ? functions.map(applyDefaultFilter).join(" ") : v6;
    }
  };

  // frontend/node_modules/motion-dom/dist/es/value/types/complex/mask.mjs
  init_define_import_meta_env();
  var mask = {
    ...complex,
    getAnimatableNone: (v6) => {
      const parsed = complex.parse(v6);
      const transformer = complex.createTransformer(v6);
      return transformer(parsed.map((v7) => typeof v7 === "number" ? 0 : typeof v7 === "object" ? { ...v7, alpha: 1 } : v7));
    }
  };

  // frontend/node_modules/motion-dom/dist/es/value/types/maps/defaults.mjs
  init_define_import_meta_env();

  // frontend/node_modules/motion-dom/dist/es/value/types/maps/number.mjs
  init_define_import_meta_env();

  // frontend/node_modules/motion-dom/dist/es/value/types/int.mjs
  init_define_import_meta_env();
  var int = {
    ...number,
    transform: Math.round
  };

  // frontend/node_modules/motion-dom/dist/es/value/types/maps/transform.mjs
  init_define_import_meta_env();
  var transformValueTypes = {
    rotate: degrees,
    /**
     * Internal channel for `transition.path` orientToPath. Composed onto
     * `rotate` at the transform-build sites so the user's `rotate` is
     * never read or overwritten. Not part of `transformPropOrder`.
     */
    pathRotation: degrees,
    rotateX: degrees,
    rotateY: degrees,
    rotateZ: degrees,
    scale,
    scaleX: scale,
    scaleY: scale,
    scaleZ: scale,
    skew: degrees,
    skewX: degrees,
    skewY: degrees,
    distance: px,
    translateX: px,
    translateY: px,
    translateZ: px,
    x: px,
    y: px,
    z: px,
    perspective: px,
    transformPerspective: px,
    opacity: alpha,
    originX: progressPercentage,
    originY: progressPercentage,
    originZ: px
  };

  // frontend/node_modules/motion-dom/dist/es/value/types/maps/number.mjs
  var numberValueTypes = {
    // Border props
    borderWidth: px,
    borderTopWidth: px,
    borderRightWidth: px,
    borderBottomWidth: px,
    borderLeftWidth: px,
    borderRadius: px,
    borderTopLeftRadius: px,
    borderTopRightRadius: px,
    borderBottomRightRadius: px,
    borderBottomLeftRadius: px,
    // Positioning props
    width: px,
    maxWidth: px,
    height: px,
    maxHeight: px,
    top: px,
    right: px,
    bottom: px,
    left: px,
    inset: px,
    insetBlock: px,
    insetBlockStart: px,
    insetBlockEnd: px,
    insetInline: px,
    insetInlineStart: px,
    insetInlineEnd: px,
    // Spacing props
    padding: px,
    paddingTop: px,
    paddingRight: px,
    paddingBottom: px,
    paddingLeft: px,
    paddingBlock: px,
    paddingBlockStart: px,
    paddingBlockEnd: px,
    paddingInline: px,
    paddingInlineStart: px,
    paddingInlineEnd: px,
    margin: px,
    marginTop: px,
    marginRight: px,
    marginBottom: px,
    marginLeft: px,
    marginBlock: px,
    marginBlockStart: px,
    marginBlockEnd: px,
    marginInline: px,
    marginInlineStart: px,
    marginInlineEnd: px,
    // Typography
    fontSize: px,
    // Misc
    backgroundPositionX: px,
    backgroundPositionY: px,
    ...transformValueTypes,
    zIndex: int,
    // SVG
    fillOpacity: alpha,
    strokeOpacity: alpha,
    numOctaves: int
  };

  // frontend/node_modules/motion-dom/dist/es/value/types/maps/defaults.mjs
  var defaultValueTypes = {
    ...numberValueTypes,
    // Color props
    color,
    backgroundColor: color,
    outlineColor: color,
    fill: color,
    stroke: color,
    // Border props
    borderColor: color,
    borderTopColor: color,
    borderRightColor: color,
    borderBottomColor: color,
    borderLeftColor: color,
    filter,
    WebkitFilter: filter,
    mask,
    WebkitMask: mask
  };
  var getDefaultValueType = (key) => defaultValueTypes[key];

  // frontend/node_modules/motion-dom/dist/es/value/types/utils/animatable-none.mjs
  var customTypes = /* @__PURE__ */ new Set([filter, mask]);
  function getAnimatableNone2(key, value) {
    let defaultValueType = getDefaultValueType(key);
    if (!customTypes.has(defaultValueType))
      defaultValueType = complex;
    return defaultValueType.getAnimatableNone ? defaultValueType.getAnimatableNone(value) : void 0;
  }

  // frontend/node_modules/motion-dom/dist/es/animation/keyframes/utils/make-none-animatable.mjs
  var invalidTemplates = /* @__PURE__ */ new Set(["auto", "none", "0"]);
  function makeNoneKeyframesAnimatable(unresolvedKeyframes, noneKeyframeIndexes, name) {
    let i13 = 0;
    let animatableTemplate = void 0;
    while (i13 < unresolvedKeyframes.length && !animatableTemplate) {
      const keyframe = unresolvedKeyframes[i13];
      if (typeof keyframe === "string" && !invalidTemplates.has(keyframe) && analyseComplexValue(keyframe).values.length) {
        animatableTemplate = unresolvedKeyframes[i13];
      }
      i13++;
    }
    if (animatableTemplate && name) {
      for (const noneIndex of noneKeyframeIndexes) {
        unresolvedKeyframes[noneIndex] = getAnimatableNone2(name, animatableTemplate);
      }
    }
  }

  // frontend/node_modules/motion-dom/dist/es/animation/keyframes/DOMKeyframesResolver.mjs
  var DOMKeyframesResolver = class extends KeyframeResolver {
    constructor(unresolvedKeyframes, onComplete, name, motionValue2, element) {
      super(unresolvedKeyframes, onComplete, name, motionValue2, element, true);
    }
    readKeyframes() {
      const { unresolvedKeyframes, element, name } = this;
      if (!element || !element.current)
        return;
      super.readKeyframes();
      for (let i13 = 0; i13 < unresolvedKeyframes.length; i13++) {
        let keyframe = unresolvedKeyframes[i13];
        if (typeof keyframe === "string") {
          keyframe = keyframe.trim();
          if (isCSSVariableToken(keyframe)) {
            const resolved = getVariableValue(keyframe, element.current);
            if (resolved !== void 0) {
              unresolvedKeyframes[i13] = resolved;
            }
            if (i13 === unresolvedKeyframes.length - 1) {
              this.finalKeyframe = keyframe;
            }
          }
        }
      }
      this.resolveNoneKeyframes();
      if (!positionalKeys.has(name) || unresolvedKeyframes.length !== 2) {
        return;
      }
      const [origin, target] = unresolvedKeyframes;
      const originType = findDimensionValueType(origin);
      const targetType = findDimensionValueType(target);
      const originHasVar = containsCSSVariable(origin);
      const targetHasVar = containsCSSVariable(target);
      if (originHasVar !== targetHasVar && positionalValues[name]) {
        this.needsMeasurement = true;
        return;
      }
      if (originType === targetType)
        return;
      if (isNumOrPxType(originType) && isNumOrPxType(targetType)) {
        for (let i13 = 0; i13 < unresolvedKeyframes.length; i13++) {
          const value = unresolvedKeyframes[i13];
          if (typeof value === "string") {
            unresolvedKeyframes[i13] = parseFloat(value);
          }
        }
      } else if (positionalValues[name]) {
        this.needsMeasurement = true;
      }
    }
    resolveNoneKeyframes() {
      const { unresolvedKeyframes, name } = this;
      const noneKeyframeIndexes = [];
      for (let i13 = 0; i13 < unresolvedKeyframes.length; i13++) {
        if (unresolvedKeyframes[i13] === null || isNone(unresolvedKeyframes[i13])) {
          noneKeyframeIndexes.push(i13);
        }
      }
      if (noneKeyframeIndexes.length) {
        makeNoneKeyframesAnimatable(unresolvedKeyframes, noneKeyframeIndexes, name);
      }
    }
    measureInitialState() {
      const { element, unresolvedKeyframes, name } = this;
      if (!element || !element.current)
        return;
      if (name === "height") {
        this.suspendedScrollY = window.pageYOffset;
      }
      this.measuredOrigin = positionalValues[name](element.measureViewportBox(), window.getComputedStyle(element.current));
      unresolvedKeyframes[0] = this.measuredOrigin;
      const measureKeyframe = unresolvedKeyframes[unresolvedKeyframes.length - 1];
      if (measureKeyframe !== void 0) {
        element.getValue(name, measureKeyframe).jump(measureKeyframe, false);
      }
    }
    measureEndState() {
      const { element, name, unresolvedKeyframes } = this;
      if (!element || !element.current)
        return;
      const value = element.getValue(name);
      value && value.jump(this.measuredOrigin, false);
      const finalKeyframeIndex = unresolvedKeyframes.length - 1;
      const finalKeyframe = unresolvedKeyframes[finalKeyframeIndex];
      unresolvedKeyframes[finalKeyframeIndex] = positionalValues[name](element.measureViewportBox(), window.getComputedStyle(element.current));
      if (finalKeyframe !== null && this.finalKeyframe === void 0) {
        this.finalKeyframe = finalKeyframe;
      }
      if (this.removedTransforms?.length) {
        this.removedTransforms.forEach(([unsetTransformName, unsetTransformValue]) => {
          element.getValue(unsetTransformName).set(unsetTransformValue);
        });
      }
      this.resolveNoneKeyframes();
    }
  };

  // frontend/node_modules/motion-dom/dist/es/utils/border-radius.mjs
  init_define_import_meta_env();
  var cornerRadiusProps = [
    "borderTopLeftRadius",
    "borderTopRightRadius",
    "borderBottomRightRadius",
    "borderBottomLeftRadius"
  ];

  // frontend/node_modules/motion-dom/dist/es/utils/resolve-elements.mjs
  init_define_import_meta_env();
  function resolveElements(elementOrSelector, scope, selectorCache) {
    if (elementOrSelector == null) {
      return [];
    }
    if (elementOrSelector instanceof EventTarget) {
      return [elementOrSelector];
    } else if (typeof elementOrSelector === "string") {
      let root = document;
      if (scope) {
        root = scope.current;
      }
      const elements = selectorCache?.[elementOrSelector] ?? root.querySelectorAll(elementOrSelector);
      return elements ? Array.from(elements) : [];
    }
    return Array.from(elementOrSelector).filter((element) => element != null);
  }

  // frontend/node_modules/motion-dom/dist/es/value/types/utils/get-as-type.mjs
  init_define_import_meta_env();
  var getValueAsType = (value, type) => {
    return type && typeof value === "number" ? type.transform(value) : value;
  };

  // frontend/node_modules/motion-dom/dist/es/utils/is-html-element.mjs
  init_define_import_meta_env();
  function isHTMLElement(element) {
    return isObject(element) && "offsetHeight" in element && !("ownerSVGElement" in element);
  }

  // frontend/node_modules/motion-dom/dist/es/frameloop/microtask.mjs
  init_define_import_meta_env();
  var { schedule: microtask, cancel: cancelMicrotask } = /* @__PURE__ */ createRenderBatcher(queueMicrotask, false);

  // frontend/node_modules/motion-dom/dist/es/gestures/drag/state/is-active.mjs
  init_define_import_meta_env();
  var isDragging = {
    x: false,
    y: false
  };
  function isDragActive() {
    return isDragging.x || isDragging.y;
  }

  // frontend/node_modules/motion-dom/dist/es/gestures/drag/state/set-active.mjs
  init_define_import_meta_env();
  function setDragLock(axis) {
    if (axis === "x" || axis === "y") {
      if (isDragging[axis]) {
        return null;
      } else {
        isDragging[axis] = true;
        return () => {
          isDragging[axis] = false;
        };
      }
    } else {
      if (isDragging.x || isDragging.y) {
        return null;
      } else {
        isDragging.x = isDragging.y = true;
        return () => {
          isDragging.x = isDragging.y = false;
        };
      }
    }
  }

  // frontend/node_modules/motion-dom/dist/es/gestures/hover.mjs
  init_define_import_meta_env();

  // frontend/node_modules/motion-dom/dist/es/gestures/utils/setup.mjs
  init_define_import_meta_env();
  function setupGesture(elementOrSelector, options) {
    const elements = resolveElements(elementOrSelector);
    const gestureAbortController = new AbortController();
    const eventOptions = {
      passive: true,
      ...options,
      signal: gestureAbortController.signal
    };
    const cancel = () => gestureAbortController.abort();
    return [elements, eventOptions, cancel];
  }

  // frontend/node_modules/motion-dom/dist/es/gestures/hover.mjs
  function isValidHover(event) {
    return !(event.pointerType === "touch" || isDragActive());
  }
  function hover(elementOrSelector, onHoverStart, options = {}) {
    const [elements, eventOptions, cancel] = setupGesture(elementOrSelector, options);
    elements.forEach((element) => {
      let isPressed = false;
      let deferredHoverEnd = false;
      let hoverEndCallback;
      const removePointerLeave = () => {
        element.removeEventListener("pointerleave", onPointerLeave);
      };
      const endHover = (event) => {
        if (hoverEndCallback) {
          hoverEndCallback(event);
          hoverEndCallback = void 0;
        }
        removePointerLeave();
      };
      const onPointerUp = (event) => {
        isPressed = false;
        window.removeEventListener("pointerup", onPointerUp);
        window.removeEventListener("pointercancel", onPointerUp);
        if (deferredHoverEnd) {
          deferredHoverEnd = false;
          endHover(event);
        }
      };
      const onPointerDown = () => {
        isPressed = true;
        window.addEventListener("pointerup", onPointerUp, eventOptions);
        window.addEventListener("pointercancel", onPointerUp, eventOptions);
      };
      const onPointerLeave = (leaveEvent) => {
        if (leaveEvent.pointerType === "touch")
          return;
        if (isPressed) {
          deferredHoverEnd = true;
          return;
        }
        endHover(leaveEvent);
      };
      const onPointerEnter = (enterEvent) => {
        if (!isValidHover(enterEvent))
          return;
        deferredHoverEnd = false;
        const onHoverEnd = onHoverStart(element, enterEvent);
        if (typeof onHoverEnd !== "function")
          return;
        hoverEndCallback = onHoverEnd;
        element.addEventListener("pointerleave", onPointerLeave, eventOptions);
      };
      element.addEventListener("pointerenter", onPointerEnter, eventOptions);
      element.addEventListener("pointerdown", onPointerDown, eventOptions);
    });
    return cancel;
  }

  // frontend/node_modules/motion-dom/dist/es/gestures/press/index.mjs
  init_define_import_meta_env();

  // frontend/node_modules/motion-dom/dist/es/gestures/utils/is-node-or-child.mjs
  init_define_import_meta_env();
  var isNodeOrChild = (parent, child) => {
    if (!child) {
      return false;
    } else if (parent === child) {
      return true;
    } else {
      return isNodeOrChild(parent, child.parentElement);
    }
  };

  // frontend/node_modules/motion-dom/dist/es/gestures/utils/is-primary-pointer.mjs
  init_define_import_meta_env();
  var isPrimaryPointer = (event) => {
    if (event.pointerType === "mouse") {
      return typeof event.button !== "number" || event.button <= 0;
    } else {
      return event.isPrimary !== false;
    }
  };

  // frontend/node_modules/motion-dom/dist/es/gestures/press/utils/is-keyboard-accessible.mjs
  init_define_import_meta_env();
  var keyboardAccessibleElements = /* @__PURE__ */ new Set([
    "BUTTON",
    "INPUT",
    "SELECT",
    "TEXTAREA",
    "A"
  ]);
  function isElementKeyboardAccessible(element) {
    return keyboardAccessibleElements.has(element.tagName) || element.isContentEditable === true;
  }
  var textInputElements = /* @__PURE__ */ new Set(["INPUT", "SELECT", "TEXTAREA"]);
  function isElementTextInput(element) {
    return textInputElements.has(element.tagName) || element.isContentEditable === true;
  }

  // frontend/node_modules/motion-dom/dist/es/gestures/press/utils/keyboard.mjs
  init_define_import_meta_env();

  // frontend/node_modules/motion-dom/dist/es/gestures/press/utils/state.mjs
  init_define_import_meta_env();
  var isPressing = /* @__PURE__ */ new WeakSet();

  // frontend/node_modules/motion-dom/dist/es/gestures/press/utils/keyboard.mjs
  function filterEvents(callback) {
    return (event) => {
      if (event.key !== "Enter")
        return;
      callback(event);
    };
  }
  function firePointerEvent(target, type) {
    target.dispatchEvent(new PointerEvent("pointer" + type, { isPrimary: true, bubbles: true }));
  }
  var enableKeyboardPress = (focusEvent, eventOptions) => {
    const element = focusEvent.currentTarget;
    if (!element)
      return;
    const handleKeydown = filterEvents(() => {
      if (isPressing.has(element))
        return;
      firePointerEvent(element, "down");
      const handleKeyup = filterEvents(() => {
        firePointerEvent(element, "up");
      });
      const handleBlur = () => firePointerEvent(element, "cancel");
      element.addEventListener("keyup", handleKeyup, eventOptions);
      element.addEventListener("blur", handleBlur, eventOptions);
    });
    element.addEventListener("keydown", handleKeydown, eventOptions);
    element.addEventListener("blur", () => element.removeEventListener("keydown", handleKeydown), eventOptions);
  };

  // frontend/node_modules/motion-dom/dist/es/gestures/press/index.mjs
  function isValidPressEvent(event) {
    return isPrimaryPointer(event) && !isDragActive();
  }
  var claimedPointerDownEvents = /* @__PURE__ */ new WeakSet();
  function press(targetOrSelector, onPressStart, options = {}) {
    const [targets, eventOptions, cancelEvents] = setupGesture(targetOrSelector, options);
    const startPress = (startEvent) => {
      const target = startEvent.currentTarget;
      if (!isValidPressEvent(startEvent))
        return;
      if (claimedPointerDownEvents.has(startEvent))
        return;
      isPressing.add(target);
      if (options.stopPropagation) {
        claimedPointerDownEvents.add(startEvent);
      }
      const onPressEnd = onPressStart(target, startEvent);
      const endEventOptions = { ...eventOptions, capture: true };
      const onPointerEnd = (endEvent, success) => {
        window.removeEventListener("pointerup", onPointerUp, endEventOptions);
        window.removeEventListener("pointercancel", onPointerCancel, endEventOptions);
        if (isPressing.has(target)) {
          isPressing.delete(target);
        }
        if (!isValidPressEvent(endEvent)) {
          return;
        }
        if (typeof onPressEnd === "function") {
          onPressEnd(endEvent, { success });
        }
      };
      const onPointerUp = (upEvent) => {
        onPointerEnd(upEvent, target === window || target === document || options.useGlobalTarget || isNodeOrChild(target, upEvent.target));
      };
      const onPointerCancel = (cancelEvent) => {
        onPointerEnd(cancelEvent, false);
      };
      window.addEventListener("pointerup", onPointerUp, endEventOptions);
      window.addEventListener("pointercancel", onPointerCancel, endEventOptions);
    };
    targets.forEach((target) => {
      const pointerDownTarget = options.useGlobalTarget ? window : target;
      pointerDownTarget.addEventListener("pointerdown", startPress, eventOptions);
      if (isHTMLElement(target)) {
        target.addEventListener("focus", (event) => enableKeyboardPress(event, eventOptions));
        if (!isElementKeyboardAccessible(target) && !target.hasAttribute("tabindex")) {
          target.tabIndex = 0;
        }
      }
    });
    return cancelEvents;
  }

  // frontend/node_modules/motion-dom/dist/es/resize/index.mjs
  init_define_import_meta_env();

  // frontend/node_modules/motion-dom/dist/es/resize/handle-element.mjs
  init_define_import_meta_env();

  // frontend/node_modules/motion-dom/dist/es/utils/is-svg-element.mjs
  init_define_import_meta_env();
  function isSVGElement(element) {
    return isObject(element) && "ownerSVGElement" in element;
  }

  // frontend/node_modules/motion-dom/dist/es/resize/handle-element.mjs
  var resizeHandlers = /* @__PURE__ */ new WeakMap();
  var observer;
  var getSize = (borderBoxAxis, svgAxis, htmlAxis) => (target, borderBoxSize) => {
    if (borderBoxSize && borderBoxSize[0]) {
      return borderBoxSize[0][borderBoxAxis + "Size"];
    } else if (isSVGElement(target) && "getBBox" in target) {
      return target.getBBox()[svgAxis];
    } else {
      return target[htmlAxis];
    }
  };
  var getWidth = /* @__PURE__ */ getSize("inline", "width", "offsetWidth");
  var getHeight = /* @__PURE__ */ getSize("block", "height", "offsetHeight");
  function notifyTarget({ target, borderBoxSize }) {
    resizeHandlers.get(target)?.forEach((handler) => {
      handler(target, {
        get width() {
          return getWidth(target, borderBoxSize);
        },
        get height() {
          return getHeight(target, borderBoxSize);
        }
      });
    });
  }
  function notifyAll(entries) {
    entries.forEach(notifyTarget);
  }
  function createResizeObserver() {
    if (typeof ResizeObserver === "undefined")
      return;
    observer = new ResizeObserver(notifyAll);
  }
  function resizeElement(target, handler) {
    if (!observer)
      createResizeObserver();
    const elements = resolveElements(target);
    elements.forEach((element) => {
      let elementHandlers = resizeHandlers.get(element);
      if (!elementHandlers) {
        elementHandlers = /* @__PURE__ */ new Set();
        resizeHandlers.set(element, elementHandlers);
      }
      elementHandlers.add(handler);
      observer?.observe(element);
    });
    return () => {
      elements.forEach((element) => {
        const elementHandlers = resizeHandlers.get(element);
        elementHandlers?.delete(handler);
        if (!elementHandlers?.size) {
          observer?.unobserve(element);
        }
      });
    };
  }

  // frontend/node_modules/motion-dom/dist/es/resize/handle-window.mjs
  init_define_import_meta_env();
  var windowCallbacks = /* @__PURE__ */ new Set();
  var windowResizeHandler;
  function createWindowResizeHandler() {
    windowResizeHandler = () => {
      const info = {
        get width() {
          return window.innerWidth;
        },
        get height() {
          return window.innerHeight;
        }
      };
      windowCallbacks.forEach((callback) => callback(info));
    };
    window.addEventListener("resize", windowResizeHandler);
  }
  function resizeWindow(callback) {
    windowCallbacks.add(callback);
    if (!windowResizeHandler)
      createWindowResizeHandler();
    return () => {
      windowCallbacks.delete(callback);
      if (!windowCallbacks.size && typeof windowResizeHandler === "function") {
        window.removeEventListener("resize", windowResizeHandler);
        windowResizeHandler = void 0;
      }
    };
  }

  // frontend/node_modules/motion-dom/dist/es/resize/index.mjs
  function resize(a21, b8) {
    return typeof a21 === "function" ? resizeWindow(a21) : resizeElement(a21, b8);
  }

  // frontend/node_modules/motion-dom/dist/es/stats/buffer.mjs
  init_define_import_meta_env();
  var statsBuffer = {
    value: null,
    addProjectionMetrics: null
  };

  // frontend/node_modules/motion-dom/dist/es/utils/is-svg-svg-element.mjs
  init_define_import_meta_env();
  function isSVGSVGElement(element) {
    return isSVGElement(element) && element.tagName === "svg";
  }

  // frontend/node_modules/motion-dom/dist/es/value/types/utils/find.mjs
  init_define_import_meta_env();
  var valueTypes = [...dimensionValueTypes, color, complex];
  var findValueType = (v6) => valueTypes.find(testValueType(v6));

  // frontend/node_modules/motion-dom/dist/es/render/dom/DOMVisualElement.mjs
  init_define_import_meta_env();

  // frontend/node_modules/motion-dom/dist/es/render/VisualElement.mjs
  init_define_import_meta_env();

  // frontend/node_modules/motion-dom/dist/es/projection/geometry/models.mjs
  init_define_import_meta_env();
  var createAxisDelta = () => ({
    translate: 0,
    scale: 1,
    origin: 0,
    originPoint: 0
  });
  var createDelta = () => ({
    x: createAxisDelta(),
    y: createAxisDelta()
  });
  var createAxis = () => ({ min: 0, max: 0 });
  var createBox = () => ({
    x: createAxis(),
    y: createAxis()
  });

  // frontend/node_modules/motion-dom/dist/es/render/store.mjs
  init_define_import_meta_env();
  var visualElementStore = /* @__PURE__ */ new WeakMap();

  // frontend/node_modules/motion-dom/dist/es/render/utils/is-controlling-variants.mjs
  init_define_import_meta_env();

  // frontend/node_modules/motion-dom/dist/es/render/utils/is-animation-controls.mjs
  init_define_import_meta_env();
  function isAnimationControls(v6) {
    return v6 !== null && typeof v6 === "object" && typeof v6.start === "function";
  }

  // frontend/node_modules/motion-dom/dist/es/render/utils/is-variant-label.mjs
  init_define_import_meta_env();
  function isVariantLabel(v6) {
    return typeof v6 === "string" || Array.isArray(v6);
  }

  // frontend/node_modules/motion-dom/dist/es/render/utils/variant-props.mjs
  init_define_import_meta_env();
  var variantPriorityOrder = [
    "animate",
    "whileInView",
    "whileFocus",
    "whileHover",
    "whileTap",
    "whileDrag",
    "exit"
  ];
  var variantProps = ["initial", ...variantPriorityOrder];

  // frontend/node_modules/motion-dom/dist/es/render/utils/is-controlling-variants.mjs
  function isControllingVariants(props) {
    return isAnimationControls(props.animate) || variantProps.some((name) => isVariantLabel(props[name]));
  }
  function isVariantNode(props) {
    return Boolean(isControllingVariants(props) || props.variants);
  }

  // frontend/node_modules/motion-dom/dist/es/render/utils/motion-values.mjs
  init_define_import_meta_env();
  function updateMotionValuesFromProps(element, next, prev) {
    for (const key in next) {
      const nextValue = next[key];
      const prevValue = prev[key];
      if (isMotionValue(nextValue)) {
        element.addValue(key, nextValue);
      } else if (isMotionValue(prevValue)) {
        element.addValue(key, motionValue(nextValue, { owner: element }));
      } else if (prevValue !== nextValue) {
        if (element.hasValue(key)) {
          const existingValue = element.getValue(key);
          if (existingValue.liveStyle === true) {
            existingValue.jump(nextValue);
          } else if (!existingValue.hasAnimated) {
            existingValue.set(nextValue);
          }
        } else {
          const latestValue = element.getStaticValue(key);
          element.addValue(key, motionValue(latestValue !== void 0 ? latestValue : nextValue, { owner: element }));
        }
      }
    }
    for (const key in prev) {
      if (next[key] === void 0)
        element.removeValue(key);
    }
    return next;
  }

  // frontend/node_modules/motion-dom/dist/es/render/utils/reduced-motion/index.mjs
  init_define_import_meta_env();

  // frontend/node_modules/motion-dom/dist/es/render/utils/reduced-motion/state.mjs
  init_define_import_meta_env();
  var prefersReducedMotion = { current: null };
  var hasReducedMotionListener = { current: false };

  // frontend/node_modules/motion-dom/dist/es/render/utils/reduced-motion/index.mjs
  var isBrowser2 = typeof window !== "undefined";
  function initPrefersReducedMotion() {
    hasReducedMotionListener.current = true;
    if (!isBrowser2)
      return;
    if (window.matchMedia) {
      const motionMediaQuery = window.matchMedia("(prefers-reduced-motion)");
      const setReducedMotionPreferences = () => prefersReducedMotion.current = motionMediaQuery.matches;
      motionMediaQuery.addEventListener("change", setReducedMotionPreferences);
      setReducedMotionPreferences();
    } else {
      prefersReducedMotion.current = false;
    }
  }

  // frontend/node_modules/motion-dom/dist/es/render/VisualElement.mjs
  var propEventHandlers = [
    "AnimationStart",
    "AnimationComplete",
    "Update",
    "BeforeLayoutMeasure",
    "LayoutMeasure",
    "LayoutAnimationStart",
    "LayoutAnimationComplete"
  ];
  var featureDefinitions = {};
  function setFeatureDefinitions(definitions) {
    featureDefinitions = definitions;
  }
  function getFeatureDefinitions() {
    return featureDefinitions;
  }
  var VisualElement = class {
    /**
     * This method takes React props and returns found MotionValues. For example, HTML
     * MotionValues will be found within the style prop, whereas for Three.js within attribute arrays.
     *
     * This isn't an abstract method as it needs calling in the constructor, but it is
     * intended to be one.
     */
    scrapeMotionValuesFromProps(_props, _prevProps, _visualElement) {
      return {};
    }
    constructor({ parent, props, presenceContext, reducedMotionConfig, skipAnimations, blockInitialAnimation, visualState }, options = {}) {
      this.current = null;
      this.children = /* @__PURE__ */ new Set();
      this.isVariantNode = false;
      this.isControllingVariants = false;
      this.shouldReduceMotion = null;
      this.shouldSkipAnimations = false;
      this.values = /* @__PURE__ */ new Map();
      this.KeyframeResolver = KeyframeResolver;
      this.features = {};
      this.valueSubscriptions = /* @__PURE__ */ new Map();
      this.prevMotionValues = {};
      this.hasBeenMounted = false;
      this.events = {};
      this.propEventSubscriptions = {};
      this.notifyUpdate = () => this.notify("Update", this.latestValues);
      this.render = () => {
        if (!this.current)
          return;
        this.triggerBuild();
        this.renderInstance(this.current, this.renderState, this.props.style, this.projection);
      };
      this.renderScheduledAt = 0;
      this.scheduleRender = () => {
        const now2 = time.now();
        if (this.renderScheduledAt < now2) {
          this.renderScheduledAt = now2;
          frame.render(this.render, false, true);
        }
      };
      const { latestValues, renderState } = visualState;
      this.latestValues = latestValues;
      this.baseTarget = { ...latestValues };
      this.initialValues = props.initial ? { ...latestValues } : {};
      this.renderState = renderState;
      this.parent = parent;
      this.props = props;
      this.presenceContext = presenceContext;
      this.depth = parent ? parent.depth + 1 : 0;
      this.reducedMotionConfig = reducedMotionConfig;
      this.skipAnimationsConfig = skipAnimations;
      this.options = options;
      this.blockInitialAnimation = Boolean(blockInitialAnimation);
      this.isControllingVariants = isControllingVariants(props);
      this.isVariantNode = isVariantNode(props);
      if (this.isVariantNode) {
        this.variantChildren = /* @__PURE__ */ new Set();
      }
      this.manuallyAnimateOnMount = Boolean(parent && parent.current);
      const { willChange, ...initialMotionValues } = this.scrapeMotionValuesFromProps(props, {}, this);
      for (const key in initialMotionValues) {
        const value = initialMotionValues[key];
        if (latestValues[key] !== void 0 && isMotionValue(value)) {
          value.set(latestValues[key]);
        }
      }
    }
    mount(instance) {
      if (this.hasBeenMounted) {
        for (const key in this.initialValues) {
          this.values.get(key)?.jump(this.initialValues[key]);
          this.latestValues[key] = this.initialValues[key];
        }
      }
      this.current = instance;
      visualElementStore.set(instance, this);
      if (this.projection && !this.projection.instance) {
        this.projection.mount(instance);
      }
      if (this.parent && this.isVariantNode && !this.isControllingVariants) {
        this.removeFromVariantTree = this.parent.addVariantChild(this);
      }
      this.values.forEach((value, key) => this.bindToMotionValue(key, value));
      if (this.reducedMotionConfig === "never") {
        this.shouldReduceMotion = false;
      } else if (this.reducedMotionConfig === "always") {
        this.shouldReduceMotion = true;
      } else {
        if (!hasReducedMotionListener.current) {
          initPrefersReducedMotion();
        }
        this.shouldReduceMotion = prefersReducedMotion.current;
      }
      if (true) {
        warnOnce(this.shouldReduceMotion !== true, "You have Reduced Motion enabled on your device. Animations may not appear as expected.", "reduced-motion-disabled");
      }
      this.shouldSkipAnimations = this.skipAnimationsConfig ?? false;
      this.parent?.addChild(this);
      this.update(this.props, this.presenceContext);
      this.hasBeenMounted = true;
    }
    unmount() {
      this.projection && this.projection.unmount();
      cancelFrame(this.notifyUpdate);
      cancelFrame(this.render);
      this.valueSubscriptions.forEach((remove) => remove());
      this.valueSubscriptions.clear();
      this.removeFromVariantTree && this.removeFromVariantTree();
      this.parent?.removeChild(this);
      for (const key in this.events) {
        this.events[key].clear();
      }
      for (const key in this.features) {
        const feature = this.features[key];
        if (feature) {
          feature.unmount();
          feature.isMounted = false;
        }
      }
      this.current = null;
    }
    addChild(child) {
      this.children.add(child);
      this.enteringChildren ?? (this.enteringChildren = /* @__PURE__ */ new Set());
      this.enteringChildren.add(child);
    }
    removeChild(child) {
      this.children.delete(child);
      this.enteringChildren && this.enteringChildren.delete(child);
    }
    bindToMotionValue(key, value) {
      if (this.valueSubscriptions.has(key)) {
        this.valueSubscriptions.get(key)();
      }
      if (value.accelerate && acceleratedValues.has(key) && this.current instanceof HTMLElement) {
        const { factory, keyframes: keyframes2, times, ease: ease2, duration } = value.accelerate;
        const animation = new NativeAnimation({
          element: this.current,
          name: key,
          keyframes: keyframes2,
          times,
          ease: ease2,
          duration: secondsToMilliseconds(duration)
        });
        const cleanup = factory(animation);
        this.valueSubscriptions.set(key, () => {
          cleanup();
          animation.cancel();
        });
        return;
      }
      const valueIsTransform = transformProps.has(key);
      if (valueIsTransform && this.onBindTransform) {
        this.onBindTransform();
      }
      const removeOnChange = value.on("change", (latestValue) => {
        this.latestValues[key] = latestValue;
        this.props.onUpdate && frame.preRender(this.notifyUpdate);
        if (valueIsTransform && this.projection) {
          this.projection.isTransformDirty = true;
        }
        this.scheduleRender();
      });
      let removeSyncCheck;
      if (typeof window !== "undefined" && window.MotionCheckAppearSync) {
        removeSyncCheck = window.MotionCheckAppearSync(this, key, value);
      }
      this.valueSubscriptions.set(key, () => {
        removeOnChange();
        if (removeSyncCheck)
          removeSyncCheck();
      });
    }
    sortNodePosition(other) {
      if (!this.current || !this.sortInstanceNodePosition || this.type !== other.type) {
        return 0;
      }
      return this.sortInstanceNodePosition(this.current, other.current);
    }
    updateFeatures() {
      let key = "animation";
      for (key in featureDefinitions) {
        const featureDefinition = featureDefinitions[key];
        if (!featureDefinition)
          continue;
        const { isEnabled, Feature: FeatureConstructor } = featureDefinition;
        if (!this.features[key] && FeatureConstructor && isEnabled(this.props)) {
          this.features[key] = new FeatureConstructor(this);
        }
        if (this.features[key]) {
          const feature = this.features[key];
          if (feature.isMounted) {
            feature.update();
          } else {
            feature.mount();
            feature.isMounted = true;
          }
        }
      }
    }
    triggerBuild() {
      this.build(this.renderState, this.latestValues, this.props);
    }
    /**
     * Measure the current viewport box with or without transforms.
     * Only measures axis-aligned boxes, rotate and skew must be manually
     * removed with a re-render to work.
     */
    measureViewportBox() {
      return this.current ? this.measureInstanceViewportBox(this.current, this.props) : createBox();
    }
    getStaticValue(key) {
      return this.latestValues[key];
    }
    setStaticValue(key, value) {
      this.latestValues[key] = value;
    }
    /**
     * Update the provided props. Ensure any newly-added motion values are
     * added to our map, old ones removed, and listeners updated.
     */
    update(props, presenceContext) {
      if (props.transformTemplate || this.props.transformTemplate) {
        this.scheduleRender();
      }
      this.prevProps = this.props;
      this.props = props;
      this.prevPresenceContext = this.presenceContext;
      this.presenceContext = presenceContext;
      for (let i13 = 0; i13 < propEventHandlers.length; i13++) {
        const key = propEventHandlers[i13];
        if (this.propEventSubscriptions[key]) {
          this.propEventSubscriptions[key]();
          delete this.propEventSubscriptions[key];
        }
        const listenerName = "on" + key;
        const listener = props[listenerName];
        if (listener) {
          this.propEventSubscriptions[key] = this.on(key, listener);
        }
      }
      this.prevMotionValues = updateMotionValuesFromProps(this, this.scrapeMotionValuesFromProps(props, this.prevProps || {}, this), this.prevMotionValues);
      if (this.handleChildMotionValue) {
        this.handleChildMotionValue();
      }
    }
    getProps() {
      return this.props;
    }
    /**
     * Returns the variant definition with a given name.
     */
    getVariant(name) {
      return this.props.variants ? this.props.variants[name] : void 0;
    }
    /**
     * Returns the defined default transition on this component.
     */
    getDefaultTransition() {
      return this.props.transition;
    }
    getTransformPagePoint() {
      return this.props.transformPagePoint;
    }
    getClosestVariantNode() {
      return this.isVariantNode ? this : this.parent ? this.parent.getClosestVariantNode() : void 0;
    }
    /**
     * Add a child visual element to our set of children.
     */
    addVariantChild(child) {
      const closestVariantNode = this.getClosestVariantNode();
      if (closestVariantNode) {
        closestVariantNode.variantChildren && closestVariantNode.variantChildren.add(child);
        return () => closestVariantNode.variantChildren.delete(child);
      }
    }
    /**
     * Add a motion value and bind it to this visual element.
     */
    addValue(key, value) {
      const existingValue = this.values.get(key);
      if (value !== existingValue) {
        if (existingValue)
          this.removeValue(key);
        this.bindToMotionValue(key, value);
        this.values.set(key, value);
        this.latestValues[key] = value.get();
      }
    }
    /**
     * Remove a motion value and unbind any active subscriptions.
     */
    removeValue(key) {
      this.values.delete(key);
      const unsubscribe = this.valueSubscriptions.get(key);
      if (unsubscribe) {
        unsubscribe();
        this.valueSubscriptions.delete(key);
      }
      delete this.latestValues[key];
      this.removeValueFromRenderState(key, this.renderState);
    }
    /**
     * Check whether we have a motion value for this key
     */
    hasValue(key) {
      return this.values.has(key);
    }
    getValue(key, defaultValue) {
      if (this.props.values && this.props.values[key]) {
        return this.props.values[key];
      }
      let value = this.values.get(key);
      if (value === void 0 && defaultValue !== void 0) {
        value = motionValue(defaultValue === null ? void 0 : defaultValue, { owner: this });
        this.addValue(key, value);
      }
      return value;
    }
    /**
     * If we're trying to animate to a previously unencountered value,
     * we need to check for it in our state and as a last resort read it
     * directly from the instance (which might have performance implications).
     */
    readValue(key, target) {
      let value = this.latestValues[key] !== void 0 || !this.current ? this.latestValues[key] : this.getBaseTargetFromProps(this.props, key) ?? this.readValueFromInstance(this.current, key, this.options);
      if (value !== void 0 && value !== null) {
        if (typeof value === "string" && (isNumericalString(value) || isZeroValueString(value))) {
          value = parseFloat(value);
        } else if (!findValueType(value) && complex.test(target)) {
          value = getAnimatableNone2(key, target);
        }
        this.setBaseTarget(key, isMotionValue(value) ? value.get() : value);
      }
      return isMotionValue(value) ? value.get() : value;
    }
    /**
     * Set the base target to later animate back to. This is currently
     * only hydrated on creation and when we first read a value.
     */
    setBaseTarget(key, value) {
      this.baseTarget[key] = value;
    }
    /**
     * Find the base target for a value thats been removed from all animation
     * props.
     */
    getBaseTarget(key) {
      const { initial } = this.props;
      let valueFromInitial;
      if (typeof initial === "string" || typeof initial === "object") {
        const variant = resolveVariantFromProps(this.props, initial, this.presenceContext?.custom);
        if (variant) {
          valueFromInitial = variant[key];
        }
      }
      if (initial && valueFromInitial !== void 0) {
        return valueFromInitial;
      }
      const target = this.getBaseTargetFromProps(this.props, key);
      if (target !== void 0 && !isMotionValue(target))
        return target;
      return this.initialValues[key] !== void 0 && valueFromInitial === void 0 ? void 0 : this.baseTarget[key];
    }
    on(eventName, callback) {
      if (!this.events[eventName]) {
        this.events[eventName] = new SubscriptionManager();
      }
      return this.events[eventName].add(callback);
    }
    notify(eventName, ...args) {
      if (this.events[eventName]) {
        this.events[eventName].notify(...args);
      }
    }
    scheduleRenderMicrotask() {
      microtask.render(this.render);
    }
  };

  // frontend/node_modules/motion-dom/dist/es/render/dom/DOMVisualElement.mjs
  var DOMVisualElement = class extends VisualElement {
    constructor() {
      super(...arguments);
      this.KeyframeResolver = DOMKeyframesResolver;
    }
    sortInstanceNodePosition(a21, b8) {
      return a21.compareDocumentPosition(b8) & 2 ? 1 : -1;
    }
    getBaseTargetFromProps(props, key) {
      const style = props.style;
      return style ? style[key] : void 0;
    }
    removeValueFromRenderState(key, { vars, style }) {
      delete vars[key];
      delete style[key];
    }
    handleChildMotionValue() {
      if (this.childSubscription) {
        this.childSubscription();
        delete this.childSubscription;
      }
      const { children } = this.props;
      if (isMotionValue(children)) {
        this.childSubscription = children.on("change", (latest) => {
          if (this.current) {
            this.current.textContent = `${latest}`;
          }
        });
      }
    }
  };

  // frontend/node_modules/motion-dom/dist/es/render/Feature.mjs
  init_define_import_meta_env();
  var Feature = class {
    constructor(node) {
      this.isMounted = false;
      this.node = node;
    }
    update() {
    }
  };

  // frontend/node_modules/motion-dom/dist/es/render/html/HTMLVisualElement.mjs
  init_define_import_meta_env();

  // frontend/node_modules/motion-dom/dist/es/projection/utils/measure.mjs
  init_define_import_meta_env();

  // frontend/node_modules/motion-dom/dist/es/projection/geometry/conversion.mjs
  init_define_import_meta_env();
  function convertBoundingBoxToBox({ top, left, right, bottom }) {
    return {
      x: { min: left, max: right },
      y: { min: top, max: bottom }
    };
  }
  function convertBoxToBoundingBox({ x: x9, y: y6 }) {
    return { top: y6.min, right: x9.max, bottom: y6.max, left: x9.min };
  }
  function transformBoxPoints(point, transformPoint2) {
    if (!transformPoint2)
      return point;
    const topLeft = transformPoint2({ x: point.left, y: point.top });
    const bottomRight = transformPoint2({ x: point.right, y: point.bottom });
    return {
      top: topLeft.y,
      left: topLeft.x,
      bottom: bottomRight.y,
      right: bottomRight.x
    };
  }

  // frontend/node_modules/motion-dom/dist/es/projection/geometry/delta-apply.mjs
  init_define_import_meta_env();

  // frontend/node_modules/motion-dom/dist/es/projection/utils/has-transform.mjs
  init_define_import_meta_env();
  function isIdentityScale(scale2) {
    return scale2 === void 0 || scale2 === 1;
  }
  function hasScale({ scale: scale2, scaleX: scaleX2, scaleY: scaleY2 }) {
    return !isIdentityScale(scale2) || !isIdentityScale(scaleX2) || !isIdentityScale(scaleY2);
  }
  function hasTransform(values) {
    return hasScale(values) || has2DTranslate(values) || values.z || values.rotate || values.rotateX || values.rotateY || values.skewX || values.skewY;
  }
  function has2DTranslate(values) {
    return is2DTranslate(values.x) || is2DTranslate(values.y);
  }
  function is2DTranslate(value) {
    return value && value !== "0%";
  }

  // frontend/node_modules/motion-dom/dist/es/projection/geometry/delta-apply.mjs
  function scalePoint(point, scale2, originPoint) {
    const distanceFromOrigin = point - originPoint;
    const scaled = scale2 * distanceFromOrigin;
    return originPoint + scaled;
  }
  function applyPointDelta(point, translate, scale2, originPoint, boxScale) {
    if (boxScale !== void 0) {
      point = scalePoint(point, boxScale, originPoint);
    }
    return scalePoint(point, scale2, originPoint) + translate;
  }
  function applyAxisDelta(axis, translate = 0, scale2 = 1, originPoint, boxScale) {
    axis.min = applyPointDelta(axis.min, translate, scale2, originPoint, boxScale);
    axis.max = applyPointDelta(axis.max, translate, scale2, originPoint, boxScale);
  }
  function applyBoxDelta(box, { x: x9, y: y6 }) {
    applyAxisDelta(box.x, x9.translate, x9.scale, x9.originPoint);
    applyAxisDelta(box.y, y6.translate, y6.scale, y6.originPoint);
  }
  var TREE_SCALE_SNAP_MIN = 0.999999999999;
  var TREE_SCALE_SNAP_MAX = 1.0000000000001;
  function applyTreeDeltas(box, treeScale, treePath, isSharedTransition = false) {
    const treeLength = treePath.length;
    if (!treeLength)
      return;
    treeScale.x = treeScale.y = 1;
    let node;
    let delta;
    for (let i13 = 0; i13 < treeLength; i13++) {
      node = treePath[i13];
      delta = node.projectionDelta;
      const { visualElement } = node.options;
      if (visualElement && visualElement.props.style && visualElement.props.style.display === "contents") {
        continue;
      }
      if (isSharedTransition && node.options.layoutScroll && node.scroll && node !== node.root) {
        translateAxis(box.x, -node.scroll.offset.x);
        translateAxis(box.y, -node.scroll.offset.y);
      }
      if (delta) {
        treeScale.x *= delta.x.scale;
        treeScale.y *= delta.y.scale;
        applyBoxDelta(box, delta);
      }
      if (isSharedTransition && hasTransform(node.latestValues)) {
        transformBox(box, node.latestValues, node.layout?.layoutBox);
      }
    }
    if (treeScale.x < TREE_SCALE_SNAP_MAX && treeScale.x > TREE_SCALE_SNAP_MIN) {
      treeScale.x = 1;
    }
    if (treeScale.y < TREE_SCALE_SNAP_MAX && treeScale.y > TREE_SCALE_SNAP_MIN) {
      treeScale.y = 1;
    }
  }
  function translateAxis(axis, distance2) {
    axis.min += distance2;
    axis.max += distance2;
  }
  function transformAxis(axis, axisTranslate, axisScale, boxScale, axisOrigin = 0.5) {
    const originPoint = mixNumber(axis.min, axis.max, axisOrigin);
    applyAxisDelta(axis, axisTranslate, axisScale, originPoint, boxScale);
  }
  function resolveAxisTranslate(value, axis) {
    if (typeof value === "string") {
      return parseFloat(value) / 100 * (axis.max - axis.min);
    }
    return value;
  }
  function transformBox(box, transform, sourceBox) {
    const resolveBox = sourceBox ?? box;
    transformAxis(box.x, resolveAxisTranslate(transform.x, resolveBox.x), transform.scaleX, transform.scale, transform.originX);
    transformAxis(box.y, resolveAxisTranslate(transform.y, resolveBox.y), transform.scaleY, transform.scale, transform.originY);
  }

  // frontend/node_modules/motion-dom/dist/es/projection/utils/measure.mjs
  function measureViewportBox(instance, transformPoint2) {
    return convertBoundingBoxToBox(transformBoxPoints(instance.getBoundingClientRect(), transformPoint2));
  }
  function measurePageBox(element, rootProjectionNode2, transformPagePoint) {
    const viewportBox = measureViewportBox(element, transformPagePoint);
    const { scroll } = rootProjectionNode2;
    if (scroll) {
      translateAxis(viewportBox.x, scroll.offset.x);
      translateAxis(viewportBox.y, scroll.offset.y);
    }
    return viewportBox;
  }

  // frontend/node_modules/motion-dom/dist/es/render/html/utils/build-styles.mjs
  init_define_import_meta_env();

  // frontend/node_modules/motion-dom/dist/es/render/html/utils/build-transform.mjs
  init_define_import_meta_env();
  var translateAlias = {
    x: "translateX",
    y: "translateY",
    z: "translateZ",
    transformPerspective: "perspective"
  };
  var numTransforms = transformPropOrder.length;
  function buildTransform(latestValues, transform, transformTemplate) {
    let transformString = "";
    let transformIsDefault = true;
    for (let i13 = 0; i13 < numTransforms; i13++) {
      const key = transformPropOrder[i13];
      const value = latestValues[key];
      if (value === void 0)
        continue;
      let valueIsDefault = true;
      if (typeof value === "number") {
        valueIsDefault = value === (key.startsWith("scale") ? 1 : 0);
      } else {
        const parsed = parseFloat(value);
        valueIsDefault = key.startsWith("scale") ? parsed === 1 : parsed === 0;
      }
      if (!valueIsDefault || transformTemplate) {
        const valueAsType = getValueAsType(value, numberValueTypes[key]);
        if (!valueIsDefault) {
          transformIsDefault = false;
          const transformName = translateAlias[key] || key;
          transformString += `${transformName}(${valueAsType}) `;
        }
        if (transformTemplate) {
          transform[key] = valueAsType;
        }
      }
    }
    const pathRotation = latestValues.pathRotation;
    if (pathRotation) {
      transformIsDefault = false;
      transformString += `rotate(${getValueAsType(pathRotation, numberValueTypes.pathRotation)}) `;
    }
    transformString = transformString.trim();
    if (transformTemplate) {
      transformString = transformTemplate(transform, transformIsDefault ? "" : transformString);
    } else if (transformIsDefault) {
      transformString = "none";
    }
    return transformString;
  }

  // frontend/node_modules/motion-dom/dist/es/render/html/utils/build-styles.mjs
  function buildHTMLStyles(state, latestValues, transformTemplate) {
    const { style, vars, transformOrigin } = state;
    let hasTransform2 = false;
    let hasTransformOrigin = false;
    for (const key in latestValues) {
      const value = latestValues[key];
      if (transformProps.has(key)) {
        hasTransform2 = true;
        continue;
      } else if (isCSSVariableName(key)) {
        vars[key] = value;
        continue;
      } else {
        const valueAsType = getValueAsType(value, numberValueTypes[key]);
        if (key.startsWith("origin")) {
          hasTransformOrigin = true;
          transformOrigin[key] = valueAsType;
        } else {
          style[key] = valueAsType;
        }
      }
    }
    if (!latestValues.transform) {
      if (hasTransform2 || transformTemplate) {
        style.transform = buildTransform(latestValues, state.transform, transformTemplate);
      } else if (style.transform) {
        style.transform = "none";
      }
    }
    if (hasTransformOrigin) {
      const { originX = "50%", originY = "50%", originZ = 0 } = transformOrigin;
      style.transformOrigin = `${originX} ${originY} ${originZ}`;
    }
  }

  // frontend/node_modules/motion-dom/dist/es/render/html/utils/render.mjs
  init_define_import_meta_env();
  function renderHTML(element, { style, vars }, styleProp, projection) {
    const elementStyle = element.style;
    let key;
    for (key in style) {
      elementStyle[key] = style[key];
    }
    projection?.applyProjectionStyles(elementStyle, styleProp);
    for (key in vars) {
      elementStyle.setProperty(key, vars[key]);
    }
  }

  // frontend/node_modules/motion-dom/dist/es/render/html/utils/scrape-motion-values.mjs
  init_define_import_meta_env();

  // frontend/node_modules/motion-dom/dist/es/render/utils/is-forced-motion-value.mjs
  init_define_import_meta_env();

  // frontend/node_modules/motion-dom/dist/es/projection/styles/scale-correction.mjs
  init_define_import_meta_env();

  // frontend/node_modules/motion-dom/dist/es/projection/styles/scale-border-radius.mjs
  init_define_import_meta_env();
  function pixelsToPercent(pixels, axis) {
    if (axis.max === axis.min)
      return 0;
    return pixels / (axis.max - axis.min) * 100;
  }
  var correctBorderRadius = {
    correct: (latest, node) => {
      if (!node.target)
        return latest;
      if (typeof latest === "string") {
        if (px.test(latest)) {
          latest = parseFloat(latest);
        } else {
          return latest;
        }
      }
      const x9 = pixelsToPercent(latest, node.target.x);
      const y6 = pixelsToPercent(latest, node.target.y);
      return `${x9}% ${y6}%`;
    }
  };

  // frontend/node_modules/motion-dom/dist/es/projection/styles/scale-box-shadow.mjs
  init_define_import_meta_env();
  var correctBoxShadow = {
    correct: (latest, { treeScale, projectionDelta }) => {
      const original = latest;
      const shadow = complex.parse(latest);
      if (shadow.length > 5)
        return original;
      const template = complex.createTransformer(latest);
      const offset4 = typeof shadow[0] !== "number" ? 1 : 0;
      const xScale = projectionDelta.x.scale * treeScale.x;
      const yScale = projectionDelta.y.scale * treeScale.y;
      shadow[0 + offset4] /= xScale;
      shadow[1 + offset4] /= yScale;
      const averageScale = mixNumber(xScale, yScale, 0.5);
      if (typeof shadow[2 + offset4] === "number")
        shadow[2 + offset4] /= averageScale;
      if (typeof shadow[3 + offset4] === "number")
        shadow[3 + offset4] /= averageScale;
      return template(shadow);
    }
  };

  // frontend/node_modules/motion-dom/dist/es/projection/styles/scale-correction.mjs
  var scaleCorrectors = {
    borderRadius: {
      ...correctBorderRadius,
      applyTo: [...cornerRadiusProps]
    },
    borderTopLeftRadius: correctBorderRadius,
    borderTopRightRadius: correctBorderRadius,
    borderBottomLeftRadius: correctBorderRadius,
    borderBottomRightRadius: correctBorderRadius,
    boxShadow: correctBoxShadow
  };

  // frontend/node_modules/motion-dom/dist/es/render/utils/is-forced-motion-value.mjs
  function isForcedMotionValue(key, { layout: layout2, layoutId }) {
    return transformProps.has(key) || key.startsWith("origin") || (layout2 || layoutId !== void 0) && (!!scaleCorrectors[key] || key === "opacity");
  }

  // frontend/node_modules/motion-dom/dist/es/render/html/utils/scrape-motion-values.mjs
  function scrapeMotionValuesFromProps(props, prevProps, visualElement) {
    const style = props.style;
    const prevStyle = prevProps?.style;
    const newValues = {};
    if (!style)
      return newValues;
    for (const key in style) {
      if (isMotionValue(style[key]) || prevStyle && isMotionValue(prevStyle[key]) || isForcedMotionValue(key, props) || visualElement?.getValue(key)?.liveStyle !== void 0) {
        newValues[key] = style[key];
      }
    }
    return newValues;
  }

  // frontend/node_modules/motion-dom/dist/es/render/html/HTMLVisualElement.mjs
  function getComputedStyle2(element) {
    return window.getComputedStyle(element);
  }
  var HTMLVisualElement = class extends DOMVisualElement {
    constructor() {
      super(...arguments);
      this.type = "html";
      this.renderInstance = renderHTML;
    }
    readValueFromInstance(instance, key) {
      if (transformProps.has(key)) {
        return this.projection?.isProjecting ? defaultTransformValue(key) : readTransformValue(instance, key);
      } else {
        const computedStyle = getComputedStyle2(instance);
        const value = (isCSSVariableName(key) ? computedStyle.getPropertyValue(key) : computedStyle[key]) || 0;
        return typeof value === "string" ? value.trim() : value;
      }
    }
    measureInstanceViewportBox(instance, { transformPagePoint }) {
      return measureViewportBox(instance, transformPagePoint);
    }
    build(renderState, latestValues, props) {
      buildHTMLStyles(renderState, latestValues, props.transformTemplate);
    }
    scrapeMotionValuesFromProps(props, prevProps, visualElement) {
      return scrapeMotionValuesFromProps(props, prevProps, visualElement);
    }
  };

  // frontend/node_modules/motion-dom/dist/es/render/svg/SVGVisualElement.mjs
  init_define_import_meta_env();

  // frontend/node_modules/motion-dom/dist/es/render/svg/utils/build-attrs.mjs
  init_define_import_meta_env();

  // frontend/node_modules/motion-dom/dist/es/render/svg/utils/path.mjs
  init_define_import_meta_env();
  var dashKeys = {
    offset: "stroke-dashoffset",
    array: "stroke-dasharray"
  };
  var camelKeys = {
    offset: "strokeDashoffset",
    array: "strokeDasharray"
  };
  function buildSVGPath(attrs, length, spacing = 1, offset4 = 0, useDashCase = true) {
    attrs.pathLength = 1;
    const keys = useDashCase ? dashKeys : camelKeys;
    attrs[keys.offset] = `${-offset4}`;
    attrs[keys.array] = `${length} ${spacing}`;
  }

  // frontend/node_modules/motion-dom/dist/es/render/svg/utils/build-attrs.mjs
  var cssMotionPathProperties = [
    "offsetDistance",
    "offsetPath",
    "offsetRotate",
    "offsetAnchor"
  ];
  function buildSVGAttrs(state, {
    attrX,
    attrY,
    attrScale,
    pathLength,
    pathSpacing = 1,
    pathOffset = 0,
    // This is object creation, which we try to avoid per-frame.
    ...latest
  }, isSVGTag2, transformTemplate, styleProp) {
    buildHTMLStyles(state, latest, transformTemplate);
    if (isSVGTag2) {
      if (state.style.viewBox) {
        state.attrs.viewBox = state.style.viewBox;
      }
      return;
    }
    state.attrs = state.style;
    state.style = {};
    const { attrs, style } = state;
    if (attrs.transform) {
      style.transform = attrs.transform;
      delete attrs.transform;
    }
    if (style.transform || attrs.transformOrigin) {
      style.transformOrigin = attrs.transformOrigin ?? "50% 50%";
      delete attrs.transformOrigin;
    }
    if (style.transform) {
      style.transformBox = styleProp?.transformBox ?? "fill-box";
      delete attrs.transformBox;
    }
    for (const key of cssMotionPathProperties) {
      if (attrs[key] !== void 0) {
        style[key] = attrs[key];
        delete attrs[key];
      }
    }
    if (attrX !== void 0)
      attrs.x = attrX;
    if (attrY !== void 0)
      attrs.y = attrY;
    if (attrScale !== void 0)
      attrs.scale = attrScale;
    if (pathLength !== void 0) {
      buildSVGPath(attrs, pathLength, pathSpacing, pathOffset, false);
    }
  }

  // frontend/node_modules/motion-dom/dist/es/render/svg/utils/camel-case-attrs.mjs
  init_define_import_meta_env();
  var camelCaseAttributes = /* @__PURE__ */ new Set([
    "baseFrequency",
    "diffuseConstant",
    "kernelMatrix",
    "kernelUnitLength",
    "keySplines",
    "keyTimes",
    "limitingConeAngle",
    "markerHeight",
    "markerWidth",
    "numOctaves",
    "targetX",
    "targetY",
    "surfaceScale",
    "specularConstant",
    "specularExponent",
    "stdDeviation",
    "tableValues",
    "viewBox",
    "gradientTransform",
    "pathLength",
    "startOffset",
    "textLength",
    "lengthAdjust"
  ]);

  // frontend/node_modules/motion-dom/dist/es/render/svg/utils/is-svg-tag.mjs
  init_define_import_meta_env();
  var isSVGTag = (tag) => typeof tag === "string" && tag.toLowerCase() === "svg";

  // frontend/node_modules/motion-dom/dist/es/render/svg/utils/render.mjs
  init_define_import_meta_env();
  function renderSVG(element, renderState, _styleProp, projection) {
    renderHTML(element, renderState, void 0, projection);
    for (const key in renderState.attrs) {
      element.setAttribute(!camelCaseAttributes.has(key) ? camelToDash(key) : key, renderState.attrs[key]);
    }
  }

  // frontend/node_modules/motion-dom/dist/es/render/svg/utils/scrape-motion-values.mjs
  init_define_import_meta_env();
  function scrapeMotionValuesFromProps2(props, prevProps, visualElement) {
    const newValues = scrapeMotionValuesFromProps(props, prevProps, visualElement);
    for (const key in props) {
      if (isMotionValue(props[key]) || isMotionValue(prevProps[key])) {
        const targetKey = transformPropOrder.indexOf(key) !== -1 ? "attr" + key.charAt(0).toUpperCase() + key.substring(1) : key;
        newValues[targetKey] = props[key];
      }
    }
    return newValues;
  }

  // frontend/node_modules/motion-dom/dist/es/render/svg/SVGVisualElement.mjs
  var SVGVisualElement = class extends DOMVisualElement {
    constructor() {
      super(...arguments);
      this.type = "svg";
      this.isSVGTag = false;
      this.measureInstanceViewportBox = createBox;
    }
    getBaseTargetFromProps(props, key) {
      return props[key];
    }
    readValueFromInstance(instance, key) {
      if (transformProps.has(key)) {
        const defaultType = getDefaultValueType(key);
        return defaultType ? defaultType.default || 0 : 0;
      }
      key = !camelCaseAttributes.has(key) ? camelToDash(key) : key;
      return instance.getAttribute(key);
    }
    scrapeMotionValuesFromProps(props, prevProps, visualElement) {
      return scrapeMotionValuesFromProps2(props, prevProps, visualElement);
    }
    build(renderState, latestValues, props) {
      buildSVGAttrs(renderState, latestValues, this.isSVGTag, props.transformTemplate, props.style);
    }
    renderInstance(instance, renderState, styleProp, projection) {
      renderSVG(instance, renderState, styleProp, projection);
    }
    mount(instance) {
      this.isSVGTag = isSVGTag(instance.tagName);
      super.mount(instance);
    }
  };

  // frontend/node_modules/motion-dom/dist/es/render/utils/animation-state.mjs
  init_define_import_meta_env();

  // frontend/node_modules/motion-dom/dist/es/render/utils/get-variant-context.mjs
  init_define_import_meta_env();
  var numVariantProps = variantProps.length;
  function getVariantContext(visualElement) {
    if (!visualElement)
      return void 0;
    if (!visualElement.isControllingVariants) {
      const context2 = visualElement.parent ? getVariantContext(visualElement.parent) || {} : {};
      if (visualElement.props.initial !== void 0) {
        context2.initial = visualElement.props.initial;
      }
      return context2;
    }
    const context = {};
    for (let i13 = 0; i13 < numVariantProps; i13++) {
      const name = variantProps[i13];
      const prop = visualElement.props[name];
      if (isVariantLabel(prop) || prop === false) {
        context[name] = prop;
      }
    }
    return context;
  }

  // frontend/node_modules/motion-dom/dist/es/render/utils/shallow-compare.mjs
  init_define_import_meta_env();
  function shallowCompare(next, prev) {
    if (!Array.isArray(prev))
      return false;
    const prevLength = prev.length;
    if (prevLength !== next.length)
      return false;
    for (let i13 = 0; i13 < prevLength; i13++) {
      if (prev[i13] !== next[i13])
        return false;
    }
    return true;
  }

  // frontend/node_modules/motion-dom/dist/es/render/utils/animation-state.mjs
  var reversePriorityOrder = [...variantPriorityOrder].reverse();
  var numAnimationTypes = variantPriorityOrder.length;
  function createAnimateFunction(visualElement) {
    return (animations2) => {
      return Promise.all(animations2.map(({ animation, options }) => animateVisualElement(visualElement, animation, options)));
    };
  }
  function createAnimationState(visualElement) {
    let animate = createAnimateFunction(visualElement);
    let state = createState();
    let isInitialRender = true;
    let wasReset = false;
    const buildResolvedTypeValues = (type) => (acc, definition) => {
      const resolved = resolveVariant(visualElement, definition, type === "exit" ? visualElement.presenceContext?.custom : void 0);
      if (resolved) {
        const { transition, transitionEnd, ...target } = resolved;
        acc = { ...acc, ...target, ...transitionEnd };
      }
      return acc;
    };
    function setAnimateFunction(makeAnimator) {
      animate = makeAnimator(visualElement);
    }
    function animateChanges(changedActiveType) {
      const { props } = visualElement;
      const context = getVariantContext(visualElement.parent) || {};
      const animations2 = [];
      const removedKeys = /* @__PURE__ */ new Set();
      let encounteredKeys = {};
      let removedVariantIndex = Infinity;
      for (let i13 = 0; i13 < numAnimationTypes; i13++) {
        const type = reversePriorityOrder[i13];
        const typeState = state[type];
        const prop = props[type] !== void 0 ? props[type] : context[type];
        const propIsVariant = isVariantLabel(prop);
        const activeDelta = type === changedActiveType ? typeState.isActive : null;
        if (activeDelta === false)
          removedVariantIndex = i13;
        let isInherited = prop === context[type] && prop !== props[type] && propIsVariant;
        if (isInherited && (isInitialRender || wasReset) && visualElement.manuallyAnimateOnMount) {
          isInherited = false;
        }
        typeState.protectedKeys = { ...encounteredKeys };
        if (
          // If it isn't active and hasn't *just* been set as inactive
          !typeState.isActive && activeDelta === null || // If we didn't and don't have any defined prop for this animation type
          !prop && !typeState.prevProp || // Or if the prop doesn't define an animation
          isAnimationControls(prop) || typeof prop === "boolean"
        ) {
          continue;
        }
        if (type === "exit" && typeState.isActive && activeDelta !== true) {
          if (typeState.prevResolvedValues) {
            encounteredKeys = {
              ...encounteredKeys,
              ...typeState.prevResolvedValues
            };
          }
          continue;
        }
        const variantDidChange = checkVariantsDidChange(typeState.prevProp, prop);
        let shouldAnimateType = variantDidChange || // If we're making this variant active, we want to always make it active
        type === changedActiveType && typeState.isActive && !isInherited && propIsVariant || // If we removed a higher-priority variant (i is in reverse order)
        i13 > removedVariantIndex && propIsVariant;
        let handledRemovedValues = false;
        const definitionList = Array.isArray(prop) ? prop : [prop];
        let resolvedValues = definitionList.reduce(buildResolvedTypeValues(type), {});
        if (activeDelta === false)
          resolvedValues = {};
        const { prevResolvedValues = {} } = typeState;
        const allKeys2 = {
          ...prevResolvedValues,
          ...resolvedValues
        };
        const markToAnimate = (key) => {
          shouldAnimateType = true;
          if (removedKeys.has(key)) {
            handledRemovedValues = true;
            removedKeys.delete(key);
          }
          typeState.needsAnimating[key] = true;
          const motionValue2 = visualElement.getValue(key);
          if (motionValue2)
            motionValue2.liveStyle = false;
        };
        for (const key in allKeys2) {
          const next = resolvedValues[key];
          const prev = prevResolvedValues[key];
          if (encounteredKeys.hasOwnProperty(key))
            continue;
          let valueHasChanged = false;
          if (isKeyframesTarget(next) && isKeyframesTarget(prev)) {
            valueHasChanged = !shallowCompare(next, prev) || variantDidChange;
          } else {
            valueHasChanged = next !== prev;
          }
          if (valueHasChanged) {
            if (next !== void 0 && next !== null) {
              markToAnimate(key);
            } else {
              removedKeys.add(key);
            }
          } else if (next !== void 0 && removedKeys.has(key)) {
            markToAnimate(key);
          } else {
            typeState.protectedKeys[key] = true;
          }
        }
        typeState.prevProp = prop;
        typeState.prevResolvedValues = resolvedValues;
        if (typeState.isActive) {
          encounteredKeys = { ...encounteredKeys, ...resolvedValues };
        }
        if ((isInitialRender || wasReset) && visualElement.blockInitialAnimation) {
          shouldAnimateType = false;
        }
        const willAnimateViaParent = isInherited && variantDidChange;
        const needsAnimating = !willAnimateViaParent || handledRemovedValues;
        if (shouldAnimateType && needsAnimating) {
          animations2.push(...definitionList.map((animation) => {
            const options = { type };
            if (typeof animation === "string" && (isInitialRender || wasReset) && !willAnimateViaParent && visualElement.manuallyAnimateOnMount && visualElement.parent) {
              const { parent } = visualElement;
              const parentVariant = resolveVariant(parent, animation);
              if (parent.enteringChildren && parentVariant) {
                const { delayChildren } = parentVariant.transition || {};
                options.delay = calcChildStagger(parent.enteringChildren, visualElement, delayChildren);
              }
            }
            return {
              animation,
              options
            };
          }));
        }
      }
      if (removedKeys.size) {
        const fallbackAnimation = {};
        if (typeof props.initial !== "boolean") {
          const initialTransition = resolveVariant(visualElement, Array.isArray(props.initial) ? props.initial[0] : props.initial);
          if (initialTransition && initialTransition.transition) {
            fallbackAnimation.transition = initialTransition.transition;
          }
        }
        removedKeys.forEach((key) => {
          const fallbackTarget = visualElement.getBaseTarget(key);
          const motionValue2 = visualElement.getValue(key);
          if (motionValue2)
            motionValue2.liveStyle = true;
          fallbackAnimation[key] = fallbackTarget ?? null;
        });
        animations2.push({ animation: fallbackAnimation });
      }
      let shouldAnimate = Boolean(animations2.length);
      if (isInitialRender && (props.initial === false || props.initial === props.animate) && !visualElement.manuallyAnimateOnMount) {
        shouldAnimate = false;
      }
      isInitialRender = false;
      wasReset = false;
      return shouldAnimate ? animate(animations2) : Promise.resolve();
    }
    function setActive(type, isActive) {
      if (state[type].isActive === isActive)
        return Promise.resolve();
      visualElement.variantChildren?.forEach((child) => child.animationState?.setActive(type, isActive));
      state[type].isActive = isActive;
      const animations2 = animateChanges(type);
      for (const key in state) {
        state[key].protectedKeys = {};
      }
      return animations2;
    }
    return {
      animateChanges,
      setActive,
      setAnimateFunction,
      getState: () => state,
      reset: () => {
        state = createState();
        wasReset = true;
      }
    };
  }
  function checkVariantsDidChange(prev, next) {
    if (typeof next === "string") {
      return next !== prev;
    } else if (Array.isArray(next)) {
      return !shallowCompare(next, prev);
    }
    return false;
  }
  function createTypeState(isActive = false) {
    return {
      isActive,
      protectedKeys: {},
      needsAnimating: {},
      prevResolvedValues: {}
    };
  }
  function createState() {
    return {
      animate: createTypeState(true),
      whileInView: createTypeState(),
      whileHover: createTypeState(),
      whileTap: createTypeState(),
      whileDrag: createTypeState(),
      whileFocus: createTypeState(),
      exit: createTypeState()
    };
  }

  // frontend/node_modules/motion-dom/dist/es/projection/geometry/copy.mjs
  init_define_import_meta_env();
  function copyAxisInto(axis, originAxis) {
    axis.min = originAxis.min;
    axis.max = originAxis.max;
  }
  function copyBoxInto(box, originBox) {
    copyAxisInto(box.x, originBox.x);
    copyAxisInto(box.y, originBox.y);
  }
  function copyAxisDeltaInto(delta, originDelta) {
    delta.translate = originDelta.translate;
    delta.scale = originDelta.scale;
    delta.originPoint = originDelta.originPoint;
    delta.origin = originDelta.origin;
  }

  // frontend/node_modules/motion-dom/dist/es/projection/geometry/delta-calc.mjs
  init_define_import_meta_env();
  var SCALE_PRECISION = 1e-4;
  var SCALE_MIN = 1 - SCALE_PRECISION;
  var SCALE_MAX = 1 + SCALE_PRECISION;
  var TRANSLATE_PRECISION = 0.01;
  var TRANSLATE_MIN = 0 - TRANSLATE_PRECISION;
  var TRANSLATE_MAX = 0 + TRANSLATE_PRECISION;
  function calcLength(axis) {
    return axis.max - axis.min;
  }
  function isNear(value, target, maxDistance) {
    return Math.abs(value - target) <= maxDistance;
  }
  function calcAxisDelta(delta, source, target, origin = 0.5) {
    delta.origin = origin;
    delta.originPoint = mixNumber(source.min, source.max, delta.origin);
    delta.scale = calcLength(target) / calcLength(source);
    delta.translate = mixNumber(target.min, target.max, delta.origin) - delta.originPoint;
    if (delta.scale >= SCALE_MIN && delta.scale <= SCALE_MAX || isNaN(delta.scale)) {
      delta.scale = 1;
    }
    if (delta.translate >= TRANSLATE_MIN && delta.translate <= TRANSLATE_MAX || isNaN(delta.translate)) {
      delta.translate = 0;
    }
  }
  function calcBoxDelta(delta, source, target, origin) {
    calcAxisDelta(delta.x, source.x, target.x, origin ? origin.originX : void 0);
    calcAxisDelta(delta.y, source.y, target.y, origin ? origin.originY : void 0);
  }
  function calcRelativeAxis(target, relative, parent, anchor = 0) {
    const anchorPoint = anchor ? mixNumber(parent.min, parent.max, anchor) : parent.min;
    target.min = anchorPoint + relative.min;
    target.max = target.min + calcLength(relative);
  }
  function calcRelativeBox(target, relative, parent, anchor) {
    calcRelativeAxis(target.x, relative.x, parent.x, anchor?.x);
    calcRelativeAxis(target.y, relative.y, parent.y, anchor?.y);
  }
  function calcRelativeAxisPosition(target, layout2, parent, anchor = 0) {
    const anchorPoint = anchor ? mixNumber(parent.min, parent.max, anchor) : parent.min;
    target.min = layout2.min - anchorPoint;
    target.max = target.min + calcLength(layout2);
  }
  function calcRelativePosition(target, layout2, parent, anchor) {
    calcRelativeAxisPosition(target.x, layout2.x, parent.x, anchor?.x);
    calcRelativeAxisPosition(target.y, layout2.y, parent.y, anchor?.y);
  }

  // frontend/node_modules/motion-dom/dist/es/projection/geometry/delta-remove.mjs
  init_define_import_meta_env();
  function removePointDelta(point, translate, scale2, originPoint, boxScale) {
    point -= translate;
    point = scalePoint(point, 1 / scale2, originPoint);
    if (boxScale !== void 0) {
      point = scalePoint(point, 1 / boxScale, originPoint);
    }
    return point;
  }
  function removeAxisDelta(axis, translate = 0, scale2 = 1, origin = 0.5, boxScale, originAxis = axis, sourceAxis = axis) {
    if (percent.test(translate)) {
      translate = parseFloat(translate);
      const relativeProgress = mixNumber(sourceAxis.min, sourceAxis.max, translate / 100);
      translate = relativeProgress - sourceAxis.min;
    }
    if (typeof translate !== "number")
      return;
    let originPoint = mixNumber(originAxis.min, originAxis.max, origin);
    if (axis === originAxis)
      originPoint -= translate;
    axis.min = removePointDelta(axis.min, translate, scale2, originPoint, boxScale);
    axis.max = removePointDelta(axis.max, translate, scale2, originPoint, boxScale);
  }
  function removeAxisTransforms(axis, transforms, [key, scaleKey, originKey], origin, sourceAxis) {
    removeAxisDelta(axis, transforms[key], transforms[scaleKey], transforms[originKey], transforms.scale, origin, sourceAxis);
  }
  var xKeys = ["x", "scaleX", "originX"];
  var yKeys = ["y", "scaleY", "originY"];
  function removeBoxTransforms(box, transforms, originBox, sourceBox) {
    removeAxisTransforms(box.x, transforms, xKeys, originBox ? originBox.x : void 0, sourceBox ? sourceBox.x : void 0);
    removeAxisTransforms(box.y, transforms, yKeys, originBox ? originBox.y : void 0, sourceBox ? sourceBox.y : void 0);
  }

  // frontend/node_modules/motion-dom/dist/es/projection/geometry/utils.mjs
  init_define_import_meta_env();
  function isAxisDeltaZero(delta) {
    return delta.translate === 0 && delta.scale === 1;
  }
  function isDeltaZero(delta) {
    return isAxisDeltaZero(delta.x) && isAxisDeltaZero(delta.y);
  }
  function axisEquals(a21, b8) {
    return a21.min === b8.min && a21.max === b8.max;
  }
  function boxEquals(a21, b8) {
    return axisEquals(a21.x, b8.x) && axisEquals(a21.y, b8.y);
  }
  function axisEqualsRounded(a21, b8) {
    return Math.round(a21.min) === Math.round(b8.min) && Math.round(a21.max) === Math.round(b8.max);
  }
  function boxEqualsRounded(a21, b8) {
    return axisEqualsRounded(a21.x, b8.x) && axisEqualsRounded(a21.y, b8.y);
  }
  function aspectRatio(box) {
    return calcLength(box.x) / calcLength(box.y);
  }
  function axisDeltaEquals(a21, b8) {
    return a21.translate === b8.translate && a21.scale === b8.scale && a21.originPoint === b8.originPoint;
  }

  // frontend/node_modules/motion-dom/dist/es/projection/utils/each-axis.mjs
  init_define_import_meta_env();
  function eachAxis(callback) {
    return [callback("x"), callback("y")];
  }

  // frontend/node_modules/motion-dom/dist/es/projection/styles/transform.mjs
  init_define_import_meta_env();
  function buildProjectionTransform(delta, treeScale, latestTransform) {
    let transform = "";
    const xTranslate = delta.x.translate / treeScale.x;
    const yTranslate = delta.y.translate / treeScale.y;
    const zTranslate = latestTransform?.z || 0;
    if (xTranslate || yTranslate || zTranslate) {
      transform = `translate3d(${xTranslate}px, ${yTranslate}px, ${zTranslate}px) `;
    }
    if (treeScale.x !== 1 || treeScale.y !== 1) {
      transform += `scale(${1 / treeScale.x}, ${1 / treeScale.y}) `;
    }
    if (latestTransform) {
      const { transformPerspective, rotate: rotate2, pathRotation, rotateX, rotateY, skewX, skewY } = latestTransform;
      if (transformPerspective)
        transform = `perspective(${transformPerspective}px) ${transform}`;
      if (rotate2)
        transform += `rotate(${rotate2}deg) `;
      if (pathRotation)
        transform += `rotate(${pathRotation}deg) `;
      if (rotateX)
        transform += `rotateX(${rotateX}deg) `;
      if (rotateY)
        transform += `rotateY(${rotateY}deg) `;
      if (skewX)
        transform += `skewX(${skewX}deg) `;
      if (skewY)
        transform += `skewY(${skewY}deg) `;
    }
    const elementScaleX = delta.x.scale * treeScale.x;
    const elementScaleY = delta.y.scale * treeScale.y;
    if (elementScaleX !== 1 || elementScaleY !== 1) {
      transform += `scale(${elementScaleX}, ${elementScaleY})`;
    }
    return transform || "none";
  }

  // frontend/node_modules/motion-dom/dist/es/projection/animation/mix-values.mjs
  init_define_import_meta_env();
  var numBorders = cornerRadiusProps.length;
  var asNumber = (value) => typeof value === "string" ? parseFloat(value) : value;
  var isPx = (value) => typeof value === "number" || px.test(value);
  function mixValues(target, follow, lead, progress2, shouldCrossfadeOpacity, isOnlyMember) {
    if (shouldCrossfadeOpacity) {
      target.opacity = mixNumber(0, lead.opacity ?? 1, easeCrossfadeIn(progress2));
      target.opacityExit = mixNumber(follow.opacity ?? 1, 0, easeCrossfadeOut(progress2));
    } else if (isOnlyMember) {
      target.opacity = mixNumber(follow.opacity ?? 1, lead.opacity ?? 1, progress2);
    }
    for (let i13 = 0; i13 < numBorders; i13++) {
      const borderLabel = cornerRadiusProps[i13];
      let followRadius = getRadius(follow, borderLabel);
      let leadRadius = getRadius(lead, borderLabel);
      if (followRadius === void 0 && leadRadius === void 0)
        continue;
      followRadius || (followRadius = 0);
      leadRadius || (leadRadius = 0);
      const canMix = followRadius === 0 || leadRadius === 0 || isPx(followRadius) === isPx(leadRadius);
      if (canMix) {
        target[borderLabel] = Math.max(mixNumber(asNumber(followRadius), asNumber(leadRadius), progress2), 0);
        if (percent.test(leadRadius) || percent.test(followRadius)) {
          target[borderLabel] += "%";
        }
      } else {
        target[borderLabel] = leadRadius;
      }
    }
    if (follow.rotate || lead.rotate) {
      target.rotate = mixNumber(follow.rotate || 0, lead.rotate || 0, progress2);
    }
  }
  function getRadius(values, radiusName) {
    return values[radiusName] !== void 0 ? values[radiusName] : values.borderRadius;
  }
  var easeCrossfadeIn = /* @__PURE__ */ compress(0, 0.5, circOut);
  var easeCrossfadeOut = /* @__PURE__ */ compress(0.5, 0.95, noop);
  function compress(min2, max2, easing) {
    return (p8) => {
      if (p8 < min2)
        return 0;
      if (p8 > max2)
        return 1;
      return easing(progress(min2, max2, p8));
    };
  }

  // frontend/node_modules/motion-dom/dist/es/animation/animate/single-value.mjs
  init_define_import_meta_env();
  function animateSingleValue(value, keyframes2, options) {
    const motionValue$1 = isMotionValue(value) ? value : motionValue(value);
    motionValue$1.start(animateMotionValue("", motionValue$1, keyframes2, options));
    return motionValue$1.animation;
  }

  // frontend/node_modules/motion-dom/dist/es/events/add-dom-event.mjs
  init_define_import_meta_env();
  function addDomEvent(target, eventName, handler, options = { passive: true }) {
    target.addEventListener(eventName, handler, options);
    return () => target.removeEventListener(eventName, handler, options);
  }

  // frontend/node_modules/motion-dom/dist/es/projection/utils/compare-by-depth.mjs
  init_define_import_meta_env();
  var compareByDepth = (a21, b8) => a21.depth - b8.depth;

  // frontend/node_modules/motion-dom/dist/es/projection/utils/flat-tree.mjs
  init_define_import_meta_env();
  var FlatTree = class {
    constructor() {
      this.children = [];
      this.isDirty = false;
    }
    add(child) {
      addUniqueItem(this.children, child);
      this.isDirty = true;
    }
    remove(child) {
      removeItem(this.children, child);
      this.isDirty = true;
    }
    forEach(callback) {
      this.isDirty && this.children.sort(compareByDepth);
      this.isDirty = false;
      this.children.forEach(callback);
    }
  };

  // frontend/node_modules/motion-dom/dist/es/utils/delay.mjs
  init_define_import_meta_env();
  function delay(callback, timeout) {
    const start = time.now();
    const checkElapsed = ({ timestamp }) => {
      const elapsed = timestamp - start;
      if (elapsed >= timeout) {
        cancelFrame(checkElapsed);
        callback(elapsed - timeout);
      }
    };
    frame.setup(checkElapsed, true);
    return () => cancelFrame(checkElapsed);
  }

  // frontend/node_modules/motion-dom/dist/es/value/utils/resolve-motion-value.mjs
  init_define_import_meta_env();
  function resolveMotionValue(value) {
    return isMotionValue(value) ? value.get() : value;
  }

  // frontend/node_modules/motion-dom/dist/es/projection/node/create-projection-node.mjs
  init_define_import_meta_env();

  // frontend/node_modules/motion-dom/dist/es/projection/shared/stack.mjs
  init_define_import_meta_env();
  var NodeStack = class {
    constructor() {
      this.members = [];
    }
    add(node) {
      addUniqueItem(this.members, node);
      for (let i13 = this.members.length - 1; i13 >= 0; i13--) {
        const member = this.members[i13];
        if (member === node || member === this.lead || member === this.prevLead)
          continue;
        const inst = member.instance;
        if ((!inst || inst.isConnected === false) && !member.snapshot) {
          removeItem(this.members, member);
          member.unmount();
        }
      }
      node.scheduleRender();
    }
    remove(node) {
      removeItem(this.members, node);
      if (node === this.prevLead)
        this.prevLead = void 0;
      if (node === this.lead) {
        const prevLead = this.members[this.members.length - 1];
        if (prevLead)
          this.promote(prevLead);
      }
    }
    relegate(node) {
      for (let i13 = this.members.indexOf(node) - 1; i13 >= 0; i13--) {
        const member = this.members[i13];
        if (member.isPresent !== false && member.instance?.isConnected !== false) {
          this.promote(member);
          return true;
        }
      }
      return false;
    }
    promote(node, preserveFollowOpacity) {
      const prevLead = this.lead;
      if (node === prevLead)
        return;
      this.prevLead = prevLead;
      this.lead = node;
      node.show();
      if (prevLead) {
        prevLead.updateSnapshot();
        node.scheduleRender();
        const { layoutDependency: prevDep } = prevLead.options;
        const { layoutDependency: nextDep } = node.options;
        if (prevDep === void 0 || prevDep !== nextDep) {
          node.resumeFrom = prevLead;
          if (preserveFollowOpacity)
            prevLead.preserveOpacity = true;
          if (prevLead.snapshot) {
            node.snapshot = prevLead.snapshot;
            node.snapshot.latestValues = prevLead.animationValues || prevLead.latestValues;
          }
          if (node.root?.isUpdating)
            node.isLayoutDirty = true;
        }
        if (node.options.crossfade === false)
          prevLead.hide();
      }
    }
    exitAnimationComplete() {
      this.members.forEach((member) => {
        member.options.onExitComplete?.();
        member.resumingFrom?.options.onExitComplete?.();
      });
    }
    scheduleRender() {
      this.members.forEach((member) => member.instance && member.scheduleRender(false));
    }
    removeLeadSnapshot() {
      if (this.lead?.snapshot)
        this.lead.snapshot = void 0;
    }
  };

  // frontend/node_modules/motion-dom/dist/es/projection/node/state.mjs
  init_define_import_meta_env();
  var globalProjectionState = {
    /**
     * Global flag as to whether the tree has animated since the last time
     * we resized the window
     */
    hasAnimatedSinceResize: true,
    /**
     * We set this to true once, on the first update. Any nodes added to the tree beyond that
     * update will be given a `data-projection-id` attribute.
     */
    hasEverUpdated: false
  };

  // frontend/node_modules/motion-dom/dist/es/projection/node/create-projection-node.mjs
  var metrics = {
    nodes: 0,
    calculatedTargetDeltas: 0,
    calculatedProjections: 0
  };
  var transformAxes = ["", "X", "Y", "Z"];
  var animationTarget = 1e3;
  var id = 0;
  function resetDistortingTransform(key, visualElement, values, sharedAnimationValues) {
    const { latestValues } = visualElement;
    if (latestValues[key]) {
      values[key] = latestValues[key];
      visualElement.setStaticValue(key, 0);
      if (sharedAnimationValues) {
        sharedAnimationValues[key] = 0;
      }
    }
  }
  function cancelTreeOptimisedTransformAnimations(projectionNode) {
    projectionNode.hasCheckedOptimisedAppear = true;
    if (projectionNode.root === projectionNode)
      return;
    const { visualElement } = projectionNode.options;
    if (!visualElement)
      return;
    const appearId = getOptimisedAppearId(visualElement);
    if (window.MotionHasOptimisedAnimation(appearId, "transform")) {
      const { layout: layout2, layoutId } = projectionNode.options;
      window.MotionCancelOptimisedAnimation(appearId, "transform", frame, !(layout2 || layoutId));
    }
    const { parent } = projectionNode;
    if (parent && !parent.hasCheckedOptimisedAppear) {
      cancelTreeOptimisedTransformAnimations(parent);
    }
  }
  function createProjectionNode({ attachResizeListener, defaultParent, measureScroll, checkIsScrollRoot, resetTransform }) {
    return class ProjectionNode {
      constructor(latestValues = {}, parent = defaultParent?.()) {
        this.id = id++;
        this.animationId = 0;
        this.animationCommitId = 0;
        this.children = /* @__PURE__ */ new Set();
        this.options = {};
        this.isTreeAnimating = false;
        this.isAnimationBlocked = false;
        this.isLayoutDirty = false;
        this.isProjectionDirty = false;
        this.isSharedProjectionDirty = false;
        this.isTransformDirty = false;
        this.updateManuallyBlocked = false;
        this.updateBlockedByResize = false;
        this.isUpdating = false;
        this.isSVG = false;
        this.needsReset = false;
        this.shouldResetTransform = false;
        this.hasCheckedOptimisedAppear = false;
        this.treeScale = { x: 1, y: 1 };
        this.eventHandlers = /* @__PURE__ */ new Map();
        this.hasTreeAnimated = false;
        this.layoutVersion = 0;
        this.updateScheduled = false;
        this.scheduleUpdate = () => this.update();
        this.projectionUpdateScheduled = false;
        this.checkUpdateFailed = () => {
          if (this.isUpdating) {
            this.isUpdating = false;
            this.clearAllSnapshots();
          }
        };
        this.updateProjection = () => {
          this.projectionUpdateScheduled = false;
          if (statsBuffer.value) {
            metrics.nodes = metrics.calculatedTargetDeltas = metrics.calculatedProjections = 0;
          }
          this.nodes.forEach(propagateDirtyNodes);
          this.nodes.forEach(resolveTargetDelta);
          this.nodes.forEach(calcProjection);
          this.nodes.forEach(cleanDirtyNodes);
          if (statsBuffer.addProjectionMetrics) {
            statsBuffer.addProjectionMetrics(metrics);
          }
        };
        this.resolvedRelativeTargetAt = 0;
        this.linkedParentVersion = 0;
        this.hasProjected = false;
        this.isVisible = true;
        this.animationProgress = 0;
        this.sharedNodes = /* @__PURE__ */ new Map();
        this.latestValues = latestValues;
        this.root = parent ? parent.root || parent : this;
        this.path = parent ? [...parent.path, parent] : [];
        this.parent = parent;
        this.depth = parent ? parent.depth + 1 : 0;
        for (let i13 = 0; i13 < this.path.length; i13++) {
          this.path[i13].shouldResetTransform = true;
        }
        if (this.root === this)
          this.nodes = new FlatTree();
      }
      addEventListener(name, handler) {
        if (!this.eventHandlers.has(name)) {
          this.eventHandlers.set(name, new SubscriptionManager());
        }
        return this.eventHandlers.get(name).add(handler);
      }
      notifyListeners(name, ...args) {
        const subscriptionManager = this.eventHandlers.get(name);
        subscriptionManager && subscriptionManager.notify(...args);
      }
      hasListeners(name) {
        return this.eventHandlers.has(name);
      }
      /**
       * Lifecycles
       */
      mount(instance) {
        if (this.instance)
          return;
        this.isSVG = isSVGElement(instance) && !isSVGSVGElement(instance);
        this.instance = instance;
        const { layoutId, layout: layout2, visualElement } = this.options;
        if (visualElement && !visualElement.current) {
          visualElement.mount(instance);
        }
        this.root.nodes.add(this);
        this.parent && this.parent.children.add(this);
        if (this.root.hasTreeAnimated && (layout2 || layoutId)) {
          this.isLayoutDirty = true;
        }
        if (attachResizeListener) {
          let cancelDelay;
          let innerWidth = 0;
          const resizeUnblockUpdate = () => this.root.updateBlockedByResize = false;
          frame.read(() => {
            innerWidth = window.innerWidth;
          });
          attachResizeListener(instance, () => {
            const newInnerWidth = window.innerWidth;
            if (newInnerWidth === innerWidth)
              return;
            innerWidth = newInnerWidth;
            this.root.updateBlockedByResize = true;
            cancelDelay && cancelDelay();
            cancelDelay = delay(resizeUnblockUpdate, 250);
            if (globalProjectionState.hasAnimatedSinceResize) {
              globalProjectionState.hasAnimatedSinceResize = false;
              this.nodes.forEach(finishAnimation);
            }
          });
        }
        if (layoutId) {
          this.root.registerSharedNode(layoutId, this);
        }
        if (this.options.animate !== false && visualElement && (layoutId || layout2)) {
          this.addEventListener("didUpdate", ({ delta, hasLayoutChanged, hasRelativeLayoutChanged, layout: newLayout }) => {
            if (this.isTreeAnimationBlocked()) {
              this.target = void 0;
              this.relativeTarget = void 0;
              return;
            }
            const layoutTransition = this.options.transition || visualElement.getDefaultTransition() || defaultLayoutTransition;
            const { onLayoutAnimationStart, onLayoutAnimationComplete } = visualElement.getProps();
            const hasTargetChanged = !this.targetLayout || !boxEqualsRounded(this.targetLayout, newLayout);
            const hasOnlyRelativeTargetChanged = !hasLayoutChanged && hasRelativeLayoutChanged;
            if (this.options.layoutRoot || this.resumeFrom || hasOnlyRelativeTargetChanged || hasLayoutChanged && (hasTargetChanged || !this.currentAnimation)) {
              if (this.resumeFrom) {
                this.resumingFrom = this.resumeFrom;
                this.resumingFrom.resumingFrom = void 0;
              }
              const animationOptions = {
                ...getValueTransition(layoutTransition, "layout"),
                onPlay: onLayoutAnimationStart,
                onComplete: onLayoutAnimationComplete
              };
              if (visualElement.shouldReduceMotion || this.options.layoutRoot) {
                animationOptions.delay = 0;
                animationOptions.type = false;
              }
              this.startAnimation(animationOptions);
              this.setAnimationOrigin(delta, hasOnlyRelativeTargetChanged, animationOptions.path);
            } else {
              if (!hasLayoutChanged) {
                finishAnimation(this);
              }
              if (this.isLead() && this.options.onExitComplete) {
                this.options.onExitComplete();
              }
            }
            this.targetLayout = newLayout;
          });
        }
      }
      unmount() {
        this.options.layoutId && this.willUpdate();
        this.root.nodes.remove(this);
        const stack = this.getStack();
        stack && stack.remove(this);
        this.parent && this.parent.children.delete(this);
        this.instance = void 0;
        this.eventHandlers.clear();
        cancelFrame(this.updateProjection);
      }
      // only on the root
      blockUpdate() {
        this.updateManuallyBlocked = true;
      }
      unblockUpdate() {
        this.updateManuallyBlocked = false;
      }
      isUpdateBlocked() {
        return this.updateManuallyBlocked || this.updateBlockedByResize;
      }
      isTreeAnimationBlocked() {
        return this.isAnimationBlocked || this.parent && this.parent.isTreeAnimationBlocked() || false;
      }
      // Note: currently only running on root node
      startUpdate() {
        if (this.isUpdateBlocked())
          return;
        this.isUpdating = true;
        this.nodes && this.nodes.forEach(resetSkewAndRotation);
        this.animationId++;
      }
      getTransformTemplate() {
        const { visualElement } = this.options;
        return visualElement && visualElement.getProps().transformTemplate;
      }
      willUpdate(shouldNotifyListeners = true) {
        this.root.hasTreeAnimated = true;
        if (this.root.isUpdateBlocked()) {
          this.options.onExitComplete && this.options.onExitComplete();
          return;
        }
        if (window.MotionCancelOptimisedAnimation && !this.hasCheckedOptimisedAppear) {
          cancelTreeOptimisedTransformAnimations(this);
        }
        !this.root.isUpdating && this.root.startUpdate();
        if (this.isLayoutDirty)
          return;
        this.isLayoutDirty = true;
        for (let i13 = 0; i13 < this.path.length; i13++) {
          const node = this.path[i13];
          node.shouldResetTransform = true;
          if (typeof node.latestValues.x === "string" || typeof node.latestValues.y === "string") {
            node.isLayoutDirty = true;
          }
          node.updateScroll("snapshot");
          if (node.options.layoutRoot) {
            node.willUpdate(false);
          }
        }
        const { layoutId, layout: layout2 } = this.options;
        if (layoutId === void 0 && !layout2)
          return;
        const transformTemplate = this.getTransformTemplate();
        this.prevTransformTemplateValue = transformTemplate ? transformTemplate(this.latestValues, "") : void 0;
        this.updateSnapshot();
        shouldNotifyListeners && this.notifyListeners("willUpdate");
      }
      update() {
        this.updateScheduled = false;
        const updateWasBlocked = this.isUpdateBlocked();
        if (updateWasBlocked) {
          const wasBlockedByResize = this.updateBlockedByResize;
          this.unblockUpdate();
          this.updateBlockedByResize = false;
          this.clearAllSnapshots();
          if (wasBlockedByResize) {
            this.nodes.forEach(forceLayoutMeasure);
          }
          this.nodes.forEach(clearMeasurements);
          return;
        }
        if (this.animationId <= this.animationCommitId) {
          this.nodes.forEach(clearIsLayoutDirty);
          return;
        }
        this.animationCommitId = this.animationId;
        if (!this.isUpdating) {
          this.nodes.forEach(clearIsLayoutDirty);
        } else {
          this.isUpdating = false;
          this.nodes.forEach(ensureDraggedNodesSnapshotted);
          this.nodes.forEach(resetTransformStyle);
          this.nodes.forEach(updateLayout);
          this.nodes.forEach(notifyLayoutUpdate);
        }
        this.clearAllSnapshots();
        const now2 = time.now();
        frameData.delta = clamp(0, 1e3 / 60, now2 - frameData.timestamp);
        frameData.timestamp = now2;
        frameData.isProcessing = true;
        frameSteps.update.process(frameData);
        frameSteps.preRender.process(frameData);
        frameSteps.render.process(frameData);
        frameData.isProcessing = false;
      }
      didUpdate() {
        if (!this.updateScheduled) {
          this.updateScheduled = true;
          microtask.read(this.scheduleUpdate);
        }
      }
      clearAllSnapshots() {
        this.nodes.forEach(clearSnapshot);
        this.sharedNodes.forEach(removeLeadSnapshots);
      }
      scheduleUpdateProjection() {
        if (!this.projectionUpdateScheduled) {
          this.projectionUpdateScheduled = true;
          frame.preRender(this.updateProjection, false, true);
        }
      }
      scheduleCheckAfterUnmount() {
        frame.postRender(() => {
          if (this.isLayoutDirty) {
            this.root.didUpdate();
          } else {
            this.root.checkUpdateFailed();
          }
        });
      }
      /**
       * Update measurements
       */
      updateSnapshot() {
        if (this.snapshot || !this.instance)
          return;
        this.snapshot = this.measure();
        if (this.snapshot && !calcLength(this.snapshot.measuredBox.x) && !calcLength(this.snapshot.measuredBox.y)) {
          this.snapshot = void 0;
        }
      }
      updateLayout() {
        if (!this.instance)
          return;
        this.updateScroll();
        if (!(this.options.alwaysMeasureLayout && this.isLead()) && !this.isLayoutDirty) {
          return;
        }
        if (this.resumeFrom && !this.resumeFrom.instance) {
          for (let i13 = 0; i13 < this.path.length; i13++) {
            const node = this.path[i13];
            node.updateScroll();
          }
        }
        const prevLayout = this.layout;
        this.layout = this.measure(false);
        this.layoutVersion++;
        if (!this.layoutCorrected)
          this.layoutCorrected = createBox();
        this.isLayoutDirty = false;
        this.projectionDelta = void 0;
        this.notifyListeners("measure", this.layout.layoutBox);
        const { visualElement } = this.options;
        visualElement && visualElement.notify("LayoutMeasure", this.layout.layoutBox, prevLayout ? prevLayout.layoutBox : void 0);
      }
      updateScroll(phase = "measure") {
        let needsMeasurement = Boolean(this.options.layoutScroll && this.instance);
        if (this.scroll && this.scroll.animationId === this.root.animationId && this.scroll.phase === phase) {
          needsMeasurement = false;
        }
        if (needsMeasurement && this.instance) {
          const isRoot = checkIsScrollRoot(this.instance);
          this.scroll = {
            animationId: this.root.animationId,
            phase,
            isRoot,
            offset: measureScroll(this.instance),
            wasRoot: this.scroll ? this.scroll.isRoot : isRoot
          };
        }
      }
      resetTransform() {
        if (!resetTransform)
          return;
        const isResetRequested = this.isLayoutDirty || this.shouldResetTransform || this.options.alwaysMeasureLayout;
        const hasProjection = this.projectionDelta && !isDeltaZero(this.projectionDelta);
        const transformTemplate = this.getTransformTemplate();
        const transformTemplateValue = transformTemplate ? transformTemplate(this.latestValues, "") : void 0;
        const transformTemplateHasChanged = transformTemplateValue !== this.prevTransformTemplateValue;
        if (isResetRequested && this.instance && (hasProjection || hasTransform(this.latestValues) || transformTemplateHasChanged)) {
          resetTransform(this.instance, transformTemplateValue);
          this.shouldResetTransform = false;
          this.scheduleRender();
        }
      }
      measure(removeTransform = true) {
        const pageBox = this.measurePageBox();
        let layoutBox = this.removeElementScroll(pageBox);
        if (removeTransform) {
          layoutBox = this.removeTransform(layoutBox);
        }
        roundBox(layoutBox);
        return {
          animationId: this.root.animationId,
          measuredBox: pageBox,
          layoutBox,
          latestValues: {},
          source: this.id
        };
      }
      measurePageBox() {
        const { visualElement } = this.options;
        if (!visualElement)
          return createBox();
        const box = visualElement.measureViewportBox();
        const wasInScrollRoot = this.scroll?.wasRoot || this.path.some(checkNodeWasScrollRoot);
        if (!wasInScrollRoot) {
          const { scroll } = this.root;
          if (scroll) {
            translateAxis(box.x, scroll.offset.x);
            translateAxis(box.y, scroll.offset.y);
          }
        }
        return box;
      }
      removeElementScroll(box) {
        const boxWithoutScroll = createBox();
        copyBoxInto(boxWithoutScroll, box);
        if (this.scroll?.wasRoot) {
          return boxWithoutScroll;
        }
        for (let i13 = 0; i13 < this.path.length; i13++) {
          const node = this.path[i13];
          const { scroll, options } = node;
          if (node !== this.root && scroll && options.layoutScroll) {
            if (scroll.wasRoot) {
              copyBoxInto(boxWithoutScroll, box);
            }
            translateAxis(boxWithoutScroll.x, scroll.offset.x);
            translateAxis(boxWithoutScroll.y, scroll.offset.y);
          }
        }
        return boxWithoutScroll;
      }
      applyTransform(box, transformOnly = false, output) {
        const withTransforms = output || createBox();
        copyBoxInto(withTransforms, box);
        for (let i13 = 0; i13 < this.path.length; i13++) {
          const node = this.path[i13];
          if (!transformOnly && node.options.layoutScroll && node.scroll && node !== node.root) {
            translateAxis(withTransforms.x, -node.scroll.offset.x);
            translateAxis(withTransforms.y, -node.scroll.offset.y);
          }
          if (!hasTransform(node.latestValues))
            continue;
          transformBox(withTransforms, node.latestValues, node.layout?.layoutBox);
        }
        if (hasTransform(this.latestValues)) {
          transformBox(withTransforms, this.latestValues, this.layout?.layoutBox);
        }
        return withTransforms;
      }
      removeTransform(box) {
        const boxWithoutTransform = createBox();
        copyBoxInto(boxWithoutTransform, box);
        for (let i13 = 0; i13 < this.path.length; i13++) {
          const node = this.path[i13];
          if (!hasTransform(node.latestValues))
            continue;
          let sourceBox;
          if (node.instance) {
            hasScale(node.latestValues) && node.updateSnapshot();
            sourceBox = createBox();
            copyBoxInto(sourceBox, node.measurePageBox());
          }
          removeBoxTransforms(boxWithoutTransform, node.latestValues, node.snapshot?.layoutBox, sourceBox);
        }
        if (hasTransform(this.latestValues)) {
          removeBoxTransforms(boxWithoutTransform, this.latestValues);
        }
        return boxWithoutTransform;
      }
      setTargetDelta(delta) {
        this.targetDelta = delta;
        this.root.scheduleUpdateProjection();
        this.isProjectionDirty = true;
      }
      setOptions(options) {
        this.options = {
          ...this.options,
          ...options,
          crossfade: options.crossfade !== void 0 ? options.crossfade : true
        };
      }
      clearMeasurements() {
        this.scroll = void 0;
        this.layout = void 0;
        this.snapshot = void 0;
        this.prevTransformTemplateValue = void 0;
        this.targetDelta = void 0;
        this.target = void 0;
        this.isLayoutDirty = false;
      }
      forceRelativeParentToResolveTarget() {
        if (!this.relativeParent)
          return;
        if (this.relativeParent.resolvedRelativeTargetAt !== frameData.timestamp) {
          this.relativeParent.resolveTargetDelta(true);
        }
      }
      resolveTargetDelta(forceRecalculation = false) {
        const lead = this.getLead();
        this.isProjectionDirty || (this.isProjectionDirty = lead.isProjectionDirty);
        this.isTransformDirty || (this.isTransformDirty = lead.isTransformDirty);
        this.isSharedProjectionDirty || (this.isSharedProjectionDirty = lead.isSharedProjectionDirty);
        const isShared = Boolean(this.resumingFrom) || this !== lead;
        const canSkip = !(forceRecalculation || isShared && this.isSharedProjectionDirty || this.isProjectionDirty || this.parent?.isProjectionDirty || this.attemptToResolveRelativeTarget || this.root.updateBlockedByResize);
        if (canSkip)
          return;
        const { layout: layout2, layoutId } = this.options;
        if (!this.layout || !(layout2 || layoutId))
          return;
        this.resolvedRelativeTargetAt = frameData.timestamp;
        const relativeParent = this.getClosestProjectingParent();
        if (relativeParent && this.linkedParentVersion !== relativeParent.layoutVersion && !relativeParent.options.layoutRoot) {
          this.removeRelativeTarget();
        }
        if (!this.targetDelta && !this.relativeTarget) {
          if (this.options.layoutAnchor !== false && relativeParent && relativeParent.layout) {
            this.createRelativeTarget(relativeParent, this.layout.layoutBox, relativeParent.layout.layoutBox);
          } else {
            this.removeRelativeTarget();
          }
        }
        if (!this.relativeTarget && !this.targetDelta)
          return;
        if (!this.target) {
          this.target = createBox();
          this.targetWithTransforms = createBox();
        }
        if (this.relativeTarget && this.relativeTargetOrigin && this.relativeParent && this.relativeParent.target) {
          this.forceRelativeParentToResolveTarget();
          calcRelativeBox(this.target, this.relativeTarget, this.relativeParent.target, this.options.layoutAnchor || void 0);
        } else if (this.targetDelta) {
          if (Boolean(this.resumingFrom)) {
            this.applyTransform(this.layout.layoutBox, false, this.target);
          } else {
            copyBoxInto(this.target, this.layout.layoutBox);
          }
          applyBoxDelta(this.target, this.targetDelta);
        } else {
          copyBoxInto(this.target, this.layout.layoutBox);
        }
        if (this.attemptToResolveRelativeTarget) {
          this.attemptToResolveRelativeTarget = false;
          if (this.options.layoutAnchor !== false && relativeParent && Boolean(relativeParent.resumingFrom) === Boolean(this.resumingFrom) && !relativeParent.options.layoutScroll && relativeParent.target && this.animationProgress !== 1) {
            this.createRelativeTarget(relativeParent, this.target, relativeParent.target);
          } else {
            this.relativeParent = this.relativeTarget = void 0;
          }
        }
        if (statsBuffer.value) {
          metrics.calculatedTargetDeltas++;
        }
      }
      getClosestProjectingParent() {
        if (!this.parent || hasScale(this.parent.latestValues) || has2DTranslate(this.parent.latestValues)) {
          return void 0;
        }
        if (this.parent.isProjecting()) {
          return this.parent;
        } else {
          return this.parent.getClosestProjectingParent();
        }
      }
      isProjecting() {
        return Boolean((this.relativeTarget || this.targetDelta || this.options.layoutRoot) && this.layout);
      }
      createRelativeTarget(relativeParent, layout2, parentLayout) {
        this.relativeParent = relativeParent;
        this.linkedParentVersion = relativeParent.layoutVersion;
        this.forceRelativeParentToResolveTarget();
        this.relativeTarget = createBox();
        this.relativeTargetOrigin = createBox();
        calcRelativePosition(this.relativeTargetOrigin, layout2, parentLayout, this.options.layoutAnchor || void 0);
        copyBoxInto(this.relativeTarget, this.relativeTargetOrigin);
      }
      removeRelativeTarget() {
        this.relativeParent = this.relativeTarget = void 0;
      }
      calcProjection() {
        const lead = this.getLead();
        const isShared = Boolean(this.resumingFrom) || this !== lead;
        let canSkip = true;
        if (this.isProjectionDirty || this.parent?.isProjectionDirty) {
          canSkip = false;
        }
        if (isShared && (this.isSharedProjectionDirty || this.isTransformDirty)) {
          canSkip = false;
        }
        if (this.resolvedRelativeTargetAt === frameData.timestamp) {
          canSkip = false;
        }
        if (canSkip)
          return;
        const { layout: layout2, layoutId } = this.options;
        this.isTreeAnimating = Boolean(this.parent && this.parent.isTreeAnimating || this.currentAnimation || this.pendingAnimation);
        if (!this.isTreeAnimating) {
          this.targetDelta = this.relativeTarget = void 0;
        }
        if (!this.layout || !(layout2 || layoutId))
          return;
        copyBoxInto(this.layoutCorrected, this.layout.layoutBox);
        const prevTreeScaleX = this.treeScale.x;
        const prevTreeScaleY = this.treeScale.y;
        applyTreeDeltas(this.layoutCorrected, this.treeScale, this.path, isShared);
        if (lead.layout && !lead.target && (this.treeScale.x !== 1 || this.treeScale.y !== 1)) {
          lead.target = lead.layout.layoutBox;
          lead.targetWithTransforms = createBox();
        }
        const { target } = lead;
        if (!target) {
          if (this.prevProjectionDelta) {
            this.createProjectionDeltas();
            this.scheduleRender();
          }
          return;
        }
        if (!this.projectionDelta || !this.prevProjectionDelta) {
          this.createProjectionDeltas();
        } else {
          copyAxisDeltaInto(this.prevProjectionDelta.x, this.projectionDelta.x);
          copyAxisDeltaInto(this.prevProjectionDelta.y, this.projectionDelta.y);
        }
        calcBoxDelta(this.projectionDelta, this.layoutCorrected, target, this.latestValues);
        if (this.treeScale.x !== prevTreeScaleX || this.treeScale.y !== prevTreeScaleY || !axisDeltaEquals(this.projectionDelta.x, this.prevProjectionDelta.x) || !axisDeltaEquals(this.projectionDelta.y, this.prevProjectionDelta.y)) {
          this.hasProjected = true;
          this.scheduleRender();
          this.notifyListeners("projectionUpdate", target);
        }
        if (statsBuffer.value) {
          metrics.calculatedProjections++;
        }
      }
      hide() {
        this.isVisible = false;
      }
      show() {
        this.isVisible = true;
      }
      scheduleRender(notifyAll2 = true) {
        this.options.visualElement?.scheduleRender();
        if (notifyAll2) {
          const stack = this.getStack();
          stack && stack.scheduleRender();
        }
        if (this.resumingFrom && !this.resumingFrom.instance) {
          this.resumingFrom = void 0;
        }
      }
      createProjectionDeltas() {
        this.prevProjectionDelta = createDelta();
        this.projectionDelta = createDelta();
        this.projectionDeltaWithTransform = createDelta();
      }
      setAnimationOrigin(delta, hasOnlyRelativeTargetChanged = false, pathFn) {
        const snapshot = this.snapshot;
        const snapshotLatestValues = snapshot ? snapshot.latestValues : {};
        const mixedValues = { ...this.latestValues };
        const targetDelta = createDelta();
        if (!this.relativeParent || !this.relativeParent.options.layoutRoot) {
          this.relativeTarget = this.relativeTargetOrigin = void 0;
        }
        this.attemptToResolveRelativeTarget = !hasOnlyRelativeTargetChanged;
        const relativeLayout = createBox();
        const snapshotSource = snapshot ? snapshot.source : void 0;
        const layoutSource = this.layout ? this.layout.source : void 0;
        const isSharedLayoutAnimation = snapshotSource !== layoutSource;
        const stack = this.getStack();
        const isOnlyMember = !stack || stack.members.length <= 1;
        const shouldCrossfadeOpacity = Boolean(isSharedLayoutAnimation && !isOnlyMember && this.options.crossfade === true && !this.path.some(hasOpacityCrossfade));
        this.animationProgress = 0;
        let prevRelativeTarget;
        const interpolate2 = pathFn?.interpolateProjection(delta);
        this.mixTargetDelta = (latest) => {
          const progress2 = latest / 1e3;
          const point = interpolate2?.(progress2);
          if (point) {
            targetDelta.x.translate = point.x;
            targetDelta.x.scale = mixNumber(delta.x.scale, 1, progress2);
            targetDelta.x.origin = delta.x.origin;
            targetDelta.x.originPoint = delta.x.originPoint;
            targetDelta.y.translate = point.y;
            targetDelta.y.scale = mixNumber(delta.y.scale, 1, progress2);
            targetDelta.y.origin = delta.y.origin;
            targetDelta.y.originPoint = delta.y.originPoint;
          } else {
            mixAxisDeltaLinear(targetDelta.x, delta.x, progress2);
            mixAxisDeltaLinear(targetDelta.y, delta.y, progress2);
          }
          this.setTargetDelta(targetDelta);
          if (this.relativeTarget && this.relativeTargetOrigin && this.layout && this.relativeParent && this.relativeParent.layout) {
            calcRelativePosition(relativeLayout, this.layout.layoutBox, this.relativeParent.layout.layoutBox, this.options.layoutAnchor || void 0);
            mixBox(this.relativeTarget, this.relativeTargetOrigin, relativeLayout, progress2);
            if (prevRelativeTarget && boxEquals(this.relativeTarget, prevRelativeTarget)) {
              this.isProjectionDirty = false;
            }
            if (!prevRelativeTarget)
              prevRelativeTarget = createBox();
            copyBoxInto(prevRelativeTarget, this.relativeTarget);
          }
          if (isSharedLayoutAnimation) {
            this.animationValues = mixedValues;
            mixValues(mixedValues, snapshotLatestValues, this.latestValues, progress2, shouldCrossfadeOpacity, isOnlyMember);
          }
          if (point && point.rotate !== void 0) {
            if (!this.animationValues)
              this.animationValues = mixedValues;
            this.animationValues.pathRotation = point.rotate;
          }
          this.root.scheduleUpdateProjection();
          this.scheduleRender();
          this.animationProgress = progress2;
        };
        this.mixTargetDelta(this.options.layoutRoot ? 1e3 : 0);
      }
      startAnimation(options) {
        this.notifyListeners("animationStart");
        this.currentAnimation?.stop();
        this.resumingFrom?.currentAnimation?.stop();
        if (this.pendingAnimation) {
          cancelFrame(this.pendingAnimation);
          this.pendingAnimation = void 0;
        }
        this.pendingAnimation = frame.update(() => {
          globalProjectionState.hasAnimatedSinceResize = true;
          this.motionValue || (this.motionValue = motionValue(0));
          this.motionValue.jump(0, false);
          this.currentAnimation = animateSingleValue(this.motionValue, [0, 1e3], {
            ...options,
            velocity: 0,
            isSync: true,
            onUpdate: (latest) => {
              this.mixTargetDelta(latest);
              options.onUpdate && options.onUpdate(latest);
            },
            onComplete: () => {
              options.onComplete && options.onComplete();
              this.completeAnimation();
            }
          });
          if (this.resumingFrom) {
            this.resumingFrom.currentAnimation = this.currentAnimation;
          }
          this.pendingAnimation = void 0;
        });
      }
      completeAnimation() {
        if (this.resumingFrom) {
          this.resumingFrom.currentAnimation = void 0;
          this.resumingFrom.preserveOpacity = void 0;
        }
        const stack = this.getStack();
        stack && stack.exitAnimationComplete();
        this.resumingFrom = this.currentAnimation = this.animationValues = void 0;
        this.notifyListeners("animationComplete");
      }
      finishAnimation() {
        if (this.currentAnimation) {
          this.mixTargetDelta && this.mixTargetDelta(animationTarget);
          this.currentAnimation.stop();
        }
        this.completeAnimation();
      }
      applyTransformsToTarget() {
        const lead = this.getLead();
        let { targetWithTransforms, target, layout: layout2, latestValues } = lead;
        if (!targetWithTransforms || !target || !layout2)
          return;
        if (this !== lead && this.layout && layout2 && shouldAnimatePositionOnly(this.options.animationType, this.layout.layoutBox, layout2.layoutBox)) {
          target = this.target || createBox();
          const xLength = calcLength(this.layout.layoutBox.x);
          target.x.min = lead.target.x.min;
          target.x.max = target.x.min + xLength;
          const yLength = calcLength(this.layout.layoutBox.y);
          target.y.min = lead.target.y.min;
          target.y.max = target.y.min + yLength;
        }
        copyBoxInto(targetWithTransforms, target);
        transformBox(targetWithTransforms, latestValues);
        calcBoxDelta(this.projectionDeltaWithTransform, this.layoutCorrected, targetWithTransforms, latestValues);
      }
      registerSharedNode(layoutId, node) {
        if (!this.sharedNodes.has(layoutId)) {
          this.sharedNodes.set(layoutId, new NodeStack());
        }
        const stack = this.sharedNodes.get(layoutId);
        stack.add(node);
        const config = node.options.initialPromotionConfig;
        node.promote({
          transition: config ? config.transition : void 0,
          preserveFollowOpacity: config && config.shouldPreserveFollowOpacity ? config.shouldPreserveFollowOpacity(node) : void 0
        });
      }
      isLead() {
        const stack = this.getStack();
        return stack ? stack.lead === this : true;
      }
      getLead() {
        const { layoutId } = this.options;
        return layoutId ? this.getStack()?.lead || this : this;
      }
      getPrevLead() {
        const { layoutId } = this.options;
        return layoutId ? this.getStack()?.prevLead : void 0;
      }
      getStack() {
        const { layoutId } = this.options;
        if (layoutId)
          return this.root.sharedNodes.get(layoutId);
      }
      promote({ needsReset, transition, preserveFollowOpacity } = {}) {
        const stack = this.getStack();
        if (stack)
          stack.promote(this, preserveFollowOpacity);
        if (needsReset) {
          this.projectionDelta = void 0;
          this.needsReset = true;
        }
        if (transition)
          this.setOptions({ transition });
      }
      relegate() {
        const stack = this.getStack();
        if (stack) {
          return stack.relegate(this);
        } else {
          return false;
        }
      }
      resetSkewAndRotation() {
        const { visualElement } = this.options;
        if (!visualElement)
          return;
        let hasDistortingTransform = false;
        const { latestValues } = visualElement;
        if (latestValues.z || latestValues.rotate || latestValues.rotateX || latestValues.rotateY || latestValues.rotateZ || latestValues.skewX || latestValues.skewY) {
          hasDistortingTransform = true;
        }
        if (!hasDistortingTransform)
          return;
        const resetValues = {};
        if (latestValues.z) {
          resetDistortingTransform("z", visualElement, resetValues, this.animationValues);
        }
        for (let i13 = 0; i13 < transformAxes.length; i13++) {
          resetDistortingTransform(`rotate${transformAxes[i13]}`, visualElement, resetValues, this.animationValues);
          resetDistortingTransform(`skew${transformAxes[i13]}`, visualElement, resetValues, this.animationValues);
        }
        visualElement.render();
        for (const key in resetValues) {
          visualElement.setStaticValue(key, resetValues[key]);
          if (this.animationValues) {
            this.animationValues[key] = resetValues[key];
          }
        }
        visualElement.scheduleRender();
      }
      applyProjectionStyles(targetStyle, styleProp) {
        if (!this.instance || this.isSVG)
          return;
        if (!this.isVisible) {
          targetStyle.visibility = "hidden";
          return;
        }
        const transformTemplate = this.getTransformTemplate();
        if (this.needsReset) {
          this.needsReset = false;
          targetStyle.visibility = "";
          targetStyle.opacity = "";
          targetStyle.pointerEvents = resolveMotionValue(styleProp?.pointerEvents) || "";
          targetStyle.transform = transformTemplate ? transformTemplate(this.latestValues, "") : "none";
          return;
        }
        const lead = this.getLead();
        if (!this.projectionDelta || !this.layout || !lead.target) {
          if (this.options.layoutId) {
            targetStyle.opacity = this.latestValues.opacity !== void 0 ? this.latestValues.opacity : 1;
            targetStyle.pointerEvents = resolveMotionValue(styleProp?.pointerEvents) || "";
          }
          if (this.hasProjected && !hasTransform(this.latestValues)) {
            targetStyle.transform = transformTemplate ? transformTemplate({}, "") : "none";
            this.hasProjected = false;
          }
          return;
        }
        targetStyle.visibility = "";
        const valuesToRender = lead.animationValues || lead.latestValues;
        this.applyTransformsToTarget();
        let transform = buildProjectionTransform(this.projectionDeltaWithTransform, this.treeScale, valuesToRender);
        if (transformTemplate) {
          transform = transformTemplate(valuesToRender, transform);
        }
        targetStyle.transform = transform;
        const { x: x9, y: y6 } = this.projectionDelta;
        targetStyle.transformOrigin = `${x9.origin * 100}% ${y6.origin * 100}% 0`;
        if (lead.animationValues) {
          targetStyle.opacity = lead === this ? valuesToRender.opacity ?? this.latestValues.opacity ?? 1 : this.preserveOpacity ? this.latestValues.opacity : valuesToRender.opacityExit;
        } else {
          targetStyle.opacity = lead === this ? valuesToRender.opacity !== void 0 ? valuesToRender.opacity : "" : valuesToRender.opacityExit !== void 0 ? valuesToRender.opacityExit : 0;
        }
        for (const key in scaleCorrectors) {
          if (valuesToRender[key] === void 0)
            continue;
          const { correct, applyTo, isCSSVariable } = scaleCorrectors[key];
          const corrected = transform === "none" ? valuesToRender[key] : correct(valuesToRender[key], lead);
          if (applyTo) {
            const num = applyTo.length;
            for (let i13 = 0; i13 < num; i13++) {
              targetStyle[applyTo[i13]] = corrected;
            }
          } else {
            if (isCSSVariable) {
              this.options.visualElement.renderState.vars[key] = corrected;
            } else {
              targetStyle[key] = corrected;
            }
          }
        }
        if (this.options.layoutId) {
          targetStyle.pointerEvents = lead === this ? resolveMotionValue(styleProp?.pointerEvents) || "" : "none";
        }
      }
      clearSnapshot() {
        this.resumeFrom = this.snapshot = void 0;
      }
      // Only run on root
      resetTree() {
        this.root.nodes.forEach((node) => node.currentAnimation?.stop());
        this.root.nodes.forEach(clearMeasurements);
        this.root.sharedNodes.clear();
      }
    };
  }
  function updateLayout(node) {
    node.updateLayout();
  }
  function notifyLayoutUpdate(node) {
    const snapshot = node.resumeFrom?.snapshot || node.snapshot;
    if (node.isLead() && node.layout && snapshot && node.hasListeners("didUpdate")) {
      const { layoutBox: layout2, measuredBox: measuredLayout } = node.layout;
      const { animationType } = node.options;
      const isShared = snapshot.source !== node.layout.source;
      if (animationType === "size") {
        eachAxis((axis) => {
          const axisSnapshot = isShared ? snapshot.measuredBox[axis] : snapshot.layoutBox[axis];
          const length = calcLength(axisSnapshot);
          axisSnapshot.min = layout2[axis].min;
          axisSnapshot.max = axisSnapshot.min + length;
        });
      } else if (animationType === "x" || animationType === "y") {
        const snapAxis = animationType === "x" ? "y" : "x";
        copyAxisInto(isShared ? snapshot.measuredBox[snapAxis] : snapshot.layoutBox[snapAxis], layout2[snapAxis]);
      } else if (shouldAnimatePositionOnly(animationType, snapshot.layoutBox, layout2)) {
        eachAxis((axis) => {
          const axisSnapshot = isShared ? snapshot.measuredBox[axis] : snapshot.layoutBox[axis];
          const length = calcLength(layout2[axis]);
          axisSnapshot.max = axisSnapshot.min + length;
          if (node.relativeTarget && !node.currentAnimation) {
            node.isProjectionDirty = true;
            node.relativeTarget[axis].max = node.relativeTarget[axis].min + length;
          }
        });
      }
      const layoutDelta = createDelta();
      calcBoxDelta(layoutDelta, layout2, snapshot.layoutBox);
      const visualDelta = createDelta();
      if (isShared) {
        calcBoxDelta(visualDelta, node.applyTransform(measuredLayout, true), snapshot.measuredBox);
      } else {
        calcBoxDelta(visualDelta, layout2, snapshot.layoutBox);
      }
      const hasLayoutChanged = !isDeltaZero(layoutDelta);
      let hasRelativeLayoutChanged = false;
      if (!node.resumeFrom) {
        const relativeParent = node.getClosestProjectingParent();
        if (relativeParent && !relativeParent.resumeFrom) {
          const { snapshot: parentSnapshot, layout: parentLayout } = relativeParent;
          if (parentSnapshot && parentLayout) {
            const anchor = node.options.layoutAnchor || void 0;
            const relativeSnapshot = createBox();
            calcRelativePosition(relativeSnapshot, snapshot.layoutBox, parentSnapshot.layoutBox, anchor);
            const relativeLayout = createBox();
            calcRelativePosition(relativeLayout, layout2, parentLayout.layoutBox, anchor);
            if (!boxEqualsRounded(relativeSnapshot, relativeLayout)) {
              hasRelativeLayoutChanged = true;
            }
            if (relativeParent.options.layoutRoot) {
              node.relativeTarget = relativeLayout;
              node.relativeTargetOrigin = relativeSnapshot;
              node.relativeParent = relativeParent;
            }
          }
        }
      }
      node.notifyListeners("didUpdate", {
        layout: layout2,
        snapshot,
        delta: visualDelta,
        layoutDelta,
        hasLayoutChanged,
        hasRelativeLayoutChanged
      });
    } else if (node.isLead()) {
      const { onExitComplete } = node.options;
      onExitComplete && onExitComplete();
    }
    node.options.transition = void 0;
  }
  function propagateDirtyNodes(node) {
    if (statsBuffer.value) {
      metrics.nodes++;
    }
    if (!node.parent)
      return;
    if (!node.isProjecting()) {
      node.isProjectionDirty = node.parent.isProjectionDirty;
    }
    node.isSharedProjectionDirty || (node.isSharedProjectionDirty = Boolean(node.isProjectionDirty || node.parent.isProjectionDirty || node.parent.isSharedProjectionDirty));
    node.isTransformDirty || (node.isTransformDirty = node.parent.isTransformDirty);
  }
  function cleanDirtyNodes(node) {
    node.isProjectionDirty = node.isSharedProjectionDirty = node.isTransformDirty = false;
  }
  function clearSnapshot(node) {
    node.clearSnapshot();
  }
  function clearMeasurements(node) {
    node.clearMeasurements();
  }
  function forceLayoutMeasure(node) {
    node.isLayoutDirty = true;
    node.updateLayout();
  }
  function clearIsLayoutDirty(node) {
    node.isLayoutDirty = false;
  }
  function ensureDraggedNodesSnapshotted(node) {
    if (node.isAnimationBlocked && node.layout && !node.isLayoutDirty) {
      node.snapshot = node.layout;
      node.isLayoutDirty = true;
    }
  }
  function resetTransformStyle(node) {
    const { visualElement } = node.options;
    if (visualElement && visualElement.getProps().onBeforeLayoutMeasure) {
      visualElement.notify("BeforeLayoutMeasure");
    }
    node.resetTransform();
  }
  function finishAnimation(node) {
    node.finishAnimation();
    node.targetDelta = node.relativeTarget = node.target = void 0;
    node.isProjectionDirty = true;
  }
  function resolveTargetDelta(node) {
    node.resolveTargetDelta();
  }
  function calcProjection(node) {
    node.calcProjection();
  }
  function resetSkewAndRotation(node) {
    node.resetSkewAndRotation();
  }
  function removeLeadSnapshots(stack) {
    stack.removeLeadSnapshot();
  }
  function mixAxisDeltaLinear(output, delta, p8) {
    output.translate = mixNumber(delta.translate, 0, p8);
    output.scale = mixNumber(delta.scale, 1, p8);
    output.origin = delta.origin;
    output.originPoint = delta.originPoint;
  }
  function mixAxis(output, from, to, p8) {
    output.min = mixNumber(from.min, to.min, p8);
    output.max = mixNumber(from.max, to.max, p8);
  }
  function mixBox(output, from, to, p8) {
    mixAxis(output.x, from.x, to.x, p8);
    mixAxis(output.y, from.y, to.y, p8);
  }
  function hasOpacityCrossfade(node) {
    return node.animationValues && node.animationValues.opacityExit !== void 0;
  }
  var defaultLayoutTransition = {
    duration: 0.45,
    ease: [0.4, 0, 0.1, 1]
  };
  var userAgentContains = (string) => typeof navigator !== "undefined" && navigator.userAgent && navigator.userAgent.toLowerCase().includes(string);
  var roundPoint = userAgentContains("applewebkit/") && !userAgentContains("chrome/") ? Math.round : noop;
  function roundAxis(axis) {
    axis.min = roundPoint(axis.min);
    axis.max = roundPoint(axis.max);
  }
  function roundBox(box) {
    roundAxis(box.x);
    roundAxis(box.y);
  }
  function shouldAnimatePositionOnly(animationType, snapshot, layout2) {
    return animationType === "position" || animationType === "preserve-aspect" && !isNear(aspectRatio(snapshot), aspectRatio(layout2), 0.2);
  }
  function checkNodeWasScrollRoot(node) {
    return node !== node.root && node.scroll?.wasRoot;
  }

  // frontend/node_modules/motion-dom/dist/es/projection/node/DocumentProjectionNode.mjs
  init_define_import_meta_env();
  var DocumentProjectionNode = createProjectionNode({
    attachResizeListener: (ref, notify) => addDomEvent(ref, "resize", notify),
    measureScroll: () => ({
      x: document.documentElement.scrollLeft || document.body?.scrollLeft || 0,
      y: document.documentElement.scrollTop || document.body?.scrollTop || 0
    }),
    checkIsScrollRoot: () => true
  });

  // frontend/node_modules/motion-dom/dist/es/projection/node/HTMLProjectionNode.mjs
  init_define_import_meta_env();
  var rootProjectionNode = {
    current: void 0
  };
  var HTMLProjectionNode = createProjectionNode({
    measureScroll: (instance) => ({
      x: instance.scrollLeft,
      y: instance.scrollTop
    }),
    defaultParent: () => {
      if (!rootProjectionNode.current) {
        const documentNode = new DocumentProjectionNode({});
        documentNode.mount(window);
        documentNode.setOptions({ layoutScroll: true });
        rootProjectionNode.current = documentNode;
      }
      return rootProjectionNode.current;
    },
    resetTransform: (instance, value) => {
      instance.style.transform = value !== void 0 ? value : "none";
    },
    checkIsScrollRoot: (instance) => Boolean(window.getComputedStyle(instance).position === "fixed")
  });

  // frontend/node_modules/framer-motion/dist/es/context/MotionConfigContext.mjs
  init_define_import_meta_env();
  var import_react5 = __toESM(require_react_shim(), 1);
  var MotionConfigContext = (0, import_react5.createContext)({
    transformPagePoint: (p8) => p8,
    isStatic: false,
    reducedMotion: "never"
  });

  // frontend/node_modules/framer-motion/dist/es/components/AnimatePresence/use-presence.mjs
  init_define_import_meta_env();
  var import_react6 = __toESM(require_react_shim(), 1);
  function usePresence(subscribe = true) {
    const context = (0, import_react6.useContext)(PresenceContext);
    if (context === null)
      return [true, null];
    const { isPresent, onExitComplete, register } = context;
    const id3 = (0, import_react6.useId)();
    (0, import_react6.useEffect)(() => {
      if (subscribe) {
        return register(id3);
      }
    }, [subscribe]);
    const safeToRemove = (0, import_react6.useCallback)(() => subscribe && onExitComplete && onExitComplete(id3), [id3, onExitComplete, subscribe]);
    return !isPresent && onExitComplete ? [false, safeToRemove] : [true];
  }

  // frontend/node_modules/framer-motion/dist/es/context/LazyContext.mjs
  init_define_import_meta_env();
  var import_react7 = __toESM(require_react_shim(), 1);
  var LazyContext = (0, import_react7.createContext)({ strict: false });

  // frontend/node_modules/framer-motion/dist/es/motion/features/load-features.mjs
  init_define_import_meta_env();

  // frontend/node_modules/framer-motion/dist/es/motion/features/definitions.mjs
  init_define_import_meta_env();
  var featureProps = {
    animation: [
      "animate",
      "variants",
      "whileHover",
      "whileTap",
      "exit",
      "whileInView",
      "whileFocus",
      "whileDrag"
    ],
    exit: ["exit"],
    drag: ["drag", "dragControls"],
    focus: ["whileFocus"],
    hover: ["whileHover", "onHoverStart", "onHoverEnd"],
    tap: ["whileTap", "onTap", "onTapStart", "onTapCancel"],
    pan: ["onPan", "onPanStart", "onPanSessionStart", "onPanEnd"],
    inView: ["whileInView", "onViewportEnter", "onViewportLeave"],
    layout: ["layout", "layoutId"]
  };
  var isInitialized = false;
  function initFeatureDefinitions() {
    if (isInitialized)
      return;
    const initialFeatureDefinitions = {};
    for (const key in featureProps) {
      initialFeatureDefinitions[key] = {
        isEnabled: (props) => featureProps[key].some((name) => !!props[name])
      };
    }
    setFeatureDefinitions(initialFeatureDefinitions);
    isInitialized = true;
  }
  function getInitializedFeatureDefinitions() {
    initFeatureDefinitions();
    return getFeatureDefinitions();
  }

  // frontend/node_modules/framer-motion/dist/es/motion/features/load-features.mjs
  function loadFeatures(features) {
    const featureDefinitions2 = getInitializedFeatureDefinitions();
    for (const key in features) {
      featureDefinitions2[key] = {
        ...featureDefinitions2[key],
        ...features[key]
      };
    }
    setFeatureDefinitions(featureDefinitions2);
  }

  // frontend/node_modules/framer-motion/dist/es/render/dom/utils/filter-props.mjs
  init_define_import_meta_env();

  // frontend/node_modules/framer-motion/dist/es/motion/utils/valid-prop.mjs
  init_define_import_meta_env();
  var validMotionProps = /* @__PURE__ */ new Set([
    "animate",
    "exit",
    "variants",
    "initial",
    "style",
    "values",
    "variants",
    "transition",
    "transformTemplate",
    "custom",
    "inherit",
    "onBeforeLayoutMeasure",
    "onAnimationStart",
    "onAnimationComplete",
    "onUpdate",
    "onDragStart",
    "onDrag",
    "onDragEnd",
    "onMeasureDragConstraints",
    "onDirectionLock",
    "onDragTransitionEnd",
    "_dragX",
    "_dragY",
    "onHoverStart",
    "onHoverEnd",
    "onViewportEnter",
    "onViewportLeave",
    "globalTapTarget",
    "propagate",
    "ignoreStrict",
    "viewport"
  ]);
  function isValidMotionProp(key) {
    return key.startsWith("while") || key.startsWith("drag") && key !== "draggable" || key.startsWith("layout") || key.startsWith("onTap") || key.startsWith("onPan") || key.startsWith("onLayout") || validMotionProps.has(key);
  }

  // frontend/node_modules/framer-motion/dist/es/render/dom/utils/filter-props.mjs
  var shouldForward = (key) => !isValidMotionProp(key);
  function loadExternalIsValidProp(isValidProp) {
    if (typeof isValidProp !== "function")
      return;
    shouldForward = (key) => key.startsWith("on") ? !isValidMotionProp(key) : isValidProp(key);
  }
  try {
    const emotionPkg = "@emotion/is-prop-valid";
    loadExternalIsValidProp(__require(emotionPkg).default);
  } catch {
  }
  function filterProps(props, isDom, forwardMotionProps) {
    const filteredProps = {};
    for (const key in props) {
      if (key === "values" && typeof props.values === "object")
        continue;
      if (isMotionValue(props[key]))
        continue;
      if (shouldForward(key) || forwardMotionProps === true && isValidMotionProp(key) || !isDom && !isValidMotionProp(key) || // If trying to use native HTML drag events, forward drag listeners
      props["draggable"] && key.startsWith("onDrag")) {
        filteredProps[key] = props[key];
      }
    }
    return filteredProps;
  }

  // frontend/node_modules/framer-motion/dist/es/render/components/create-proxy.mjs
  init_define_import_meta_env();

  // frontend/node_modules/framer-motion/dist/es/motion/index.mjs
  init_define_import_meta_env();
  var import_jsx_runtime = __toESM(require_react_shim(), 1);
  var import_react17 = __toESM(require_react_shim(), 1);

  // frontend/node_modules/framer-motion/dist/es/context/MotionContext/index.mjs
  init_define_import_meta_env();
  var import_react8 = __toESM(require_react_shim(), 1);
  var MotionContext = /* @__PURE__ */ (0, import_react8.createContext)({});

  // frontend/node_modules/framer-motion/dist/es/context/MotionContext/create.mjs
  init_define_import_meta_env();
  var import_react9 = __toESM(require_react_shim(), 1);

  // frontend/node_modules/framer-motion/dist/es/context/MotionContext/utils.mjs
  init_define_import_meta_env();
  function getCurrentTreeVariants(props, context) {
    if (isControllingVariants(props)) {
      const { initial, animate } = props;
      return {
        initial: initial === false || isVariantLabel(initial) ? initial : void 0,
        animate: isVariantLabel(animate) ? animate : void 0
      };
    }
    return props.inherit !== false ? context : {};
  }

  // frontend/node_modules/framer-motion/dist/es/context/MotionContext/create.mjs
  function useCreateMotionContext(props) {
    const { initial, animate } = getCurrentTreeVariants(props, (0, import_react9.useContext)(MotionContext));
    return (0, import_react9.useMemo)(() => ({ initial, animate }), [variantLabelsAsDependency(initial), variantLabelsAsDependency(animate)]);
  }
  function variantLabelsAsDependency(prop) {
    return Array.isArray(prop) ? prop.join(" ") : prop;
  }

  // frontend/node_modules/framer-motion/dist/es/render/dom/use-render.mjs
  init_define_import_meta_env();
  var import_react12 = __toESM(require_react_shim(), 1);

  // frontend/node_modules/framer-motion/dist/es/render/html/use-props.mjs
  init_define_import_meta_env();
  var import_react10 = __toESM(require_react_shim(), 1);

  // frontend/node_modules/framer-motion/dist/es/render/html/utils/create-render-state.mjs
  init_define_import_meta_env();
  var createHtmlRenderState = () => ({
    style: {},
    transform: {},
    transformOrigin: {},
    vars: {}
  });

  // frontend/node_modules/framer-motion/dist/es/render/html/use-props.mjs
  function copyRawValuesOnly(target, source, props) {
    for (const key in source) {
      if (!isMotionValue(source[key]) && !isForcedMotionValue(key, props)) {
        target[key] = source[key];
      }
    }
  }
  function useInitialMotionValues({ transformTemplate }, visualState) {
    return (0, import_react10.useMemo)(() => {
      const state = createHtmlRenderState();
      buildHTMLStyles(state, visualState, transformTemplate);
      return Object.assign({}, state.vars, state.style);
    }, [visualState]);
  }
  function useStyle(props, visualState) {
    const styleProp = props.style || {};
    const style = {};
    copyRawValuesOnly(style, styleProp, props);
    Object.assign(style, useInitialMotionValues(props, visualState));
    return style;
  }
  function useHTMLProps(props, visualState) {
    const htmlProps = {};
    const style = useStyle(props, visualState);
    if (props.drag && props.dragListener !== false) {
      htmlProps.draggable = false;
      style.userSelect = style.WebkitUserSelect = style.WebkitTouchCallout = "none";
      style.touchAction = props.drag === true ? "none" : `pan-${props.drag === "x" ? "y" : "x"}`;
    }
    if (props.tabIndex === void 0 && (props.onTap || props.onTapStart || props.whileTap)) {
      htmlProps.tabIndex = 0;
    }
    htmlProps.style = style;
    return htmlProps;
  }

  // frontend/node_modules/framer-motion/dist/es/render/svg/use-props.mjs
  init_define_import_meta_env();
  var import_react11 = __toESM(require_react_shim(), 1);

  // frontend/node_modules/framer-motion/dist/es/render/svg/utils/create-render-state.mjs
  init_define_import_meta_env();
  var createSvgRenderState = () => ({
    ...createHtmlRenderState(),
    attrs: {}
  });

  // frontend/node_modules/framer-motion/dist/es/render/svg/use-props.mjs
  function useSVGProps(props, visualState, _isStatic, Component2) {
    const visualProps = (0, import_react11.useMemo)(() => {
      const state = createSvgRenderState();
      buildSVGAttrs(state, visualState, isSVGTag(Component2), props.transformTemplate, props.style);
      return {
        ...state.attrs,
        style: { ...state.style }
      };
    }, [visualState]);
    if (props.style) {
      const rawStyles = {};
      copyRawValuesOnly(rawStyles, props.style, props);
      visualProps.style = { ...rawStyles, ...visualProps.style };
    }
    return visualProps;
  }

  // frontend/node_modules/framer-motion/dist/es/render/dom/utils/is-svg-component.mjs
  init_define_import_meta_env();

  // frontend/node_modules/framer-motion/dist/es/render/svg/lowercase-elements.mjs
  init_define_import_meta_env();
  var lowercaseSVGElements = [
    "animate",
    "circle",
    "defs",
    "desc",
    "ellipse",
    "g",
    "image",
    "line",
    "filter",
    "marker",
    "mask",
    "metadata",
    "path",
    "pattern",
    "polygon",
    "polyline",
    "rect",
    "stop",
    "switch",
    "symbol",
    "svg",
    "text",
    "tspan",
    "use",
    "view"
  ];

  // frontend/node_modules/framer-motion/dist/es/render/dom/utils/is-svg-component.mjs
  function isSVGComponent(Component2) {
    if (
      /**
       * If it's not a string, it's a custom React component. Currently we only support
       * HTML custom React components.
       */
      typeof Component2 !== "string" || /**
       * If it contains a dash, the element is a custom HTML webcomponent.
       */
      Component2.includes("-")
    ) {
      return false;
    } else if (
      /**
       * If it's in our list of lowercase SVG tags, it's an SVG component
       */
      lowercaseSVGElements.indexOf(Component2) > -1 || /**
       * If it contains a capital letter, it's an SVG component
       */
      /[A-Z]/u.test(Component2)
    ) {
      return true;
    }
    return false;
  }

  // frontend/node_modules/framer-motion/dist/es/render/dom/use-render.mjs
  function useRender(Component2, props, ref, { latestValues }, isStatic, forwardMotionProps = false, isSVG) {
    const useVisualProps = isSVG ?? isSVGComponent(Component2) ? useSVGProps : useHTMLProps;
    const visualProps = useVisualProps(props, latestValues, isStatic, Component2);
    const filteredProps = filterProps(props, typeof Component2 === "string", forwardMotionProps);
    const elementProps = Component2 !== import_react12.Fragment ? { ...filteredProps, ...visualProps, ref } : {};
    const { children } = props;
    const renderedChildren = (0, import_react12.useMemo)(() => isMotionValue(children) ? children.get() : children, [children]);
    return (0, import_react12.createElement)(Component2, {
      ...elementProps,
      children: renderedChildren
    });
  }

  // frontend/node_modules/framer-motion/dist/es/render/html/use-html-visual-state.mjs
  init_define_import_meta_env();

  // frontend/node_modules/framer-motion/dist/es/motion/utils/use-visual-state.mjs
  init_define_import_meta_env();
  var import_react13 = __toESM(require_react_shim(), 1);
  function makeState({ scrapeMotionValuesFromProps: scrapeMotionValuesFromProps3, createRenderState }, props, context, presenceContext) {
    const state = {
      latestValues: makeLatestValues(props, context, presenceContext, scrapeMotionValuesFromProps3),
      renderState: createRenderState()
    };
    return state;
  }
  function makeLatestValues(props, context, presenceContext, scrapeMotionValues) {
    const values = {};
    const motionValues = scrapeMotionValues(props, {});
    for (const key in motionValues) {
      values[key] = resolveMotionValue(motionValues[key]);
    }
    let { initial, animate } = props;
    const isControllingVariants$1 = isControllingVariants(props);
    const isVariantNode$1 = isVariantNode(props);
    if (context && isVariantNode$1 && !isControllingVariants$1 && props.inherit !== false) {
      if (initial === void 0)
        initial = context.initial;
      if (animate === void 0)
        animate = context.animate;
    }
    let isInitialAnimationBlocked = presenceContext ? presenceContext.initial === false : false;
    isInitialAnimationBlocked = isInitialAnimationBlocked || initial === false;
    const variantToSet = isInitialAnimationBlocked ? animate : initial;
    if (variantToSet && typeof variantToSet !== "boolean" && !isAnimationControls(variantToSet)) {
      const list = Array.isArray(variantToSet) ? variantToSet : [variantToSet];
      for (let i13 = 0; i13 < list.length; i13++) {
        const resolved = resolveVariantFromProps(props, list[i13]);
        if (resolved) {
          const { transitionEnd, transition, ...target } = resolved;
          for (const key in target) {
            let valueTarget = target[key];
            if (Array.isArray(valueTarget)) {
              const index3 = isInitialAnimationBlocked ? valueTarget.length - 1 : 0;
              valueTarget = valueTarget[index3];
            }
            if (valueTarget !== null) {
              values[key] = valueTarget;
            }
          }
          for (const key in transitionEnd) {
            values[key] = transitionEnd[key];
          }
        }
      }
    }
    return values;
  }
  var makeUseVisualState = (config) => (props, isStatic) => {
    const context = (0, import_react13.useContext)(MotionContext);
    const presenceContext = (0, import_react13.useContext)(PresenceContext);
    const make = () => makeState(config, props, context, presenceContext);
    return isStatic ? make() : useConstant(make);
  };

  // frontend/node_modules/framer-motion/dist/es/render/html/use-html-visual-state.mjs
  var useHTMLVisualState = /* @__PURE__ */ makeUseVisualState({
    scrapeMotionValuesFromProps,
    createRenderState: createHtmlRenderState
  });

  // frontend/node_modules/framer-motion/dist/es/render/svg/use-svg-visual-state.mjs
  init_define_import_meta_env();
  var useSVGVisualState = /* @__PURE__ */ makeUseVisualState({
    scrapeMotionValuesFromProps: scrapeMotionValuesFromProps2,
    createRenderState: createSvgRenderState
  });

  // frontend/node_modules/framer-motion/dist/es/motion/utils/symbol.mjs
  init_define_import_meta_env();
  var motionComponentSymbol = /* @__PURE__ */ Symbol.for("motionComponentSymbol");

  // frontend/node_modules/framer-motion/dist/es/motion/utils/use-motion-ref.mjs
  init_define_import_meta_env();
  var import_react14 = __toESM(require_react_shim(), 1);
  function useMotionRef(visualState, visualElement, externalRef) {
    const externalRefContainer = (0, import_react14.useRef)(externalRef);
    (0, import_react14.useInsertionEffect)(() => {
      externalRefContainer.current = externalRef;
    });
    const refCleanup = (0, import_react14.useRef)(null);
    return (0, import_react14.useCallback)((instance) => {
      if (instance) {
        visualState.onMount?.(instance);
      }
      if (visualElement) {
        instance ? visualElement.mount(instance) : visualElement.unmount();
      }
      const ref = externalRefContainer.current;
      if (typeof ref === "function") {
        if (instance) {
          const cleanup = ref(instance);
          if (typeof cleanup === "function") {
            refCleanup.current = cleanup;
          }
        } else if (refCleanup.current) {
          refCleanup.current();
          refCleanup.current = null;
        } else {
          ref(instance);
        }
      } else if (ref) {
        ref.current = instance;
      }
    }, [visualElement]);
  }

  // frontend/node_modules/framer-motion/dist/es/motion/utils/use-visual-element.mjs
  init_define_import_meta_env();
  var import_react16 = __toESM(require_react_shim(), 1);

  // frontend/node_modules/framer-motion/dist/es/context/SwitchLayoutGroupContext.mjs
  init_define_import_meta_env();
  var import_react15 = __toESM(require_react_shim(), 1);
  var SwitchLayoutGroupContext = (0, import_react15.createContext)({});

  // frontend/node_modules/framer-motion/dist/es/utils/is-ref-object.mjs
  init_define_import_meta_env();
  function isRefObject(ref) {
    return ref && typeof ref === "object" && Object.prototype.hasOwnProperty.call(ref, "current");
  }

  // frontend/node_modules/framer-motion/dist/es/motion/utils/use-visual-element.mjs
  function useVisualElement(Component2, visualState, props, createVisualElement, ProjectionNodeConstructor, isSVG) {
    const { visualElement: parent } = (0, import_react16.useContext)(MotionContext);
    const lazyContext = (0, import_react16.useContext)(LazyContext);
    const presenceContext = (0, import_react16.useContext)(PresenceContext);
    const motionConfig = (0, import_react16.useContext)(MotionConfigContext);
    const reducedMotionConfig = motionConfig.reducedMotion;
    const skipAnimations = motionConfig.skipAnimations;
    const visualElementRef = (0, import_react16.useRef)(null);
    const hasMountedOnce = (0, import_react16.useRef)(false);
    createVisualElement = createVisualElement || lazyContext.renderer;
    if (!visualElementRef.current && createVisualElement) {
      visualElementRef.current = createVisualElement(Component2, {
        visualState,
        parent,
        props,
        presenceContext,
        blockInitialAnimation: presenceContext ? presenceContext.initial === false : false,
        reducedMotionConfig,
        skipAnimations,
        isSVG
      });
      if (hasMountedOnce.current && visualElementRef.current) {
        visualElementRef.current.manuallyAnimateOnMount = true;
      }
    }
    const visualElement = visualElementRef.current;
    const initialLayoutGroupConfig = (0, import_react16.useContext)(SwitchLayoutGroupContext);
    if (visualElement && !visualElement.projection && ProjectionNodeConstructor && (visualElement.type === "html" || visualElement.type === "svg")) {
      createProjectionNode2(visualElementRef.current, props, ProjectionNodeConstructor, initialLayoutGroupConfig);
    }
    const isMounted = (0, import_react16.useRef)(false);
    (0, import_react16.useInsertionEffect)(() => {
      if (visualElement && isMounted.current) {
        visualElement.update(props, presenceContext);
      }
    });
    const optimisedAppearId = props[optimizedAppearDataAttribute];
    const wantsHandoff = (0, import_react16.useRef)(Boolean(optimisedAppearId) && typeof window !== "undefined" && !window.MotionHandoffIsComplete?.(optimisedAppearId) && window.MotionHasOptimisedAnimation?.(optimisedAppearId));
    useIsomorphicLayoutEffect(() => {
      hasMountedOnce.current = true;
      if (!visualElement)
        return;
      isMounted.current = true;
      window.MotionIsMounted = true;
      visualElement.updateFeatures();
      visualElement.scheduleRenderMicrotask();
      if (wantsHandoff.current && visualElement.animationState) {
        visualElement.animationState.animateChanges();
      }
    });
    (0, import_react16.useEffect)(() => {
      if (!visualElement)
        return;
      if (!wantsHandoff.current && visualElement.animationState) {
        visualElement.animationState.animateChanges();
      }
      if (wantsHandoff.current) {
        queueMicrotask(() => {
          window.MotionHandoffMarkAsComplete?.(optimisedAppearId);
        });
        wantsHandoff.current = false;
      }
      visualElement.enteringChildren = void 0;
    });
    return visualElement;
  }
  function createProjectionNode2(visualElement, props, ProjectionNodeConstructor, initialPromotionConfig) {
    const { layoutId, layout: layout2, drag: drag2, dragConstraints, layoutScroll, layoutRoot, layoutAnchor, layoutCrossfade } = props;
    visualElement.projection = new ProjectionNodeConstructor(visualElement.latestValues, props["data-framer-portal-id"] ? void 0 : getClosestProjectingNode(visualElement.parent));
    visualElement.projection.setOptions({
      layoutId,
      layout: layout2,
      alwaysMeasureLayout: Boolean(drag2) || dragConstraints && isRefObject(dragConstraints),
      visualElement,
      /**
       * TODO: Update options in an effect. This could be tricky as it'll be too late
       * to update by the time layout animations run.
       * We also need to fix this safeToRemove by linking it up to the one returned by usePresence,
       * ensuring it gets called if there's no potential layout animations.
       *
       */
      animationType: typeof layout2 === "string" ? layout2 : "both",
      initialPromotionConfig,
      crossfade: layoutCrossfade,
      layoutScroll,
      layoutRoot,
      layoutAnchor
    });
  }
  function getClosestProjectingNode(visualElement) {
    if (!visualElement)
      return void 0;
    return visualElement.options.allowProjection !== false ? visualElement.projection : getClosestProjectingNode(visualElement.parent);
  }

  // frontend/node_modules/framer-motion/dist/es/motion/index.mjs
  function createMotionComponent(Component2, { forwardMotionProps = false, type } = {}, preloadedFeatures, createVisualElement) {
    preloadedFeatures && loadFeatures(preloadedFeatures);
    const isSVG = type ? type === "svg" : isSVGComponent(Component2);
    const useVisualState = isSVG ? useSVGVisualState : useHTMLVisualState;
    function MotionDOMComponent(props, externalRef) {
      let MeasureLayout2;
      const configAndProps = {
        ...(0, import_react17.useContext)(MotionConfigContext),
        ...props,
        layoutId: useLayoutId(props)
      };
      const { isStatic } = configAndProps;
      const context = useCreateMotionContext(props);
      const visualState = useVisualState(props, isStatic);
      if (!isStatic && typeof window !== "undefined") {
        useStrictMode(configAndProps, preloadedFeatures);
        const layoutProjection = getProjectionFunctionality(configAndProps);
        MeasureLayout2 = layoutProjection.MeasureLayout;
        context.visualElement = useVisualElement(Component2, visualState, configAndProps, createVisualElement, layoutProjection.ProjectionNode, isSVG);
      }
      return (0, import_jsx_runtime.jsxs)(MotionContext.Provider, { value: context, children: [MeasureLayout2 && context.visualElement ? (0, import_jsx_runtime.jsx)(MeasureLayout2, { visualElement: context.visualElement, ...configAndProps }) : null, useRender(Component2, props, useMotionRef(visualState, context.visualElement, externalRef), visualState, isStatic, forwardMotionProps, isSVG)] });
    }
    MotionDOMComponent.displayName = `motion.${typeof Component2 === "string" ? Component2 : `create(${Component2.displayName ?? Component2.name ?? ""})`}`;
    const ForwardRefMotionComponent = (0, import_react17.forwardRef)(MotionDOMComponent);
    ForwardRefMotionComponent[motionComponentSymbol] = Component2;
    return ForwardRefMotionComponent;
  }
  function useLayoutId({ layoutId }) {
    const layoutGroupId = (0, import_react17.useContext)(LayoutGroupContext).id;
    return layoutGroupId && layoutId !== void 0 ? layoutGroupId + "-" + layoutId : layoutId;
  }
  function useStrictMode(configAndProps, preloadedFeatures) {
    const isStrict = (0, import_react17.useContext)(LazyContext).strict;
    if (preloadedFeatures && isStrict) {
      const strictMessage = "You have rendered a `motion` component within a `LazyMotion` component. This will break tree shaking. Import and render a `m` component instead.";
      configAndProps.ignoreStrict ? warning(false, strictMessage, "lazy-strict-mode") : invariant(false, strictMessage, "lazy-strict-mode");
    }
  }
  function getProjectionFunctionality(props) {
    const featureDefinitions2 = getInitializedFeatureDefinitions();
    const { drag: drag2, layout: layout2 } = featureDefinitions2;
    if (!drag2 && !layout2)
      return {};
    const combined = { ...drag2, ...layout2 };
    return {
      MeasureLayout: drag2?.isEnabled(props) || layout2?.isEnabled(props) ? combined.MeasureLayout : void 0,
      ProjectionNode: combined.ProjectionNode
    };
  }

  // frontend/node_modules/framer-motion/dist/es/render/components/create-proxy.mjs
  function createMotionProxy(preloadedFeatures, createVisualElement) {
    if (typeof Proxy === "undefined") {
      return createMotionComponent;
    }
    const componentCache = /* @__PURE__ */ new Map();
    const factory = (Component2, options) => {
      return createMotionComponent(Component2, options, preloadedFeatures, createVisualElement);
    };
    const deprecatedFactoryFunction = (Component2, options) => {
      if (true) {
        warnOnce(false, "motion() is deprecated. Use motion.create() instead.");
      }
      return factory(Component2, options);
    };
    return new Proxy(deprecatedFactoryFunction, {
      /**
       * Called when `motion` is referenced with a prop: `motion.div`, `motion.input` etc.
       * The prop name is passed through as `key` and we can use that to generate a `motion`
       * DOM component with that name.
       */
      get: (_target, key) => {
        if (key === "create")
          return factory;
        if (!componentCache.has(key)) {
          componentCache.set(key, createMotionComponent(key, void 0, preloadedFeatures, createVisualElement));
        }
        return componentCache.get(key);
      }
    });
  }

  // frontend/node_modules/framer-motion/dist/es/render/components/motion/proxy.mjs
  init_define_import_meta_env();

  // frontend/node_modules/framer-motion/dist/es/render/dom/create-visual-element.mjs
  init_define_import_meta_env();
  var import_react18 = __toESM(require_react_shim(), 1);
  var createDomVisualElement = (Component2, options) => {
    const isSVG = options.isSVG ?? isSVGComponent(Component2);
    return isSVG ? new SVGVisualElement(options) : new HTMLVisualElement(options, {
      allowProjection: Component2 !== import_react18.Fragment
    });
  };

  // frontend/node_modules/framer-motion/dist/es/render/components/motion/feature-bundle.mjs
  init_define_import_meta_env();

  // frontend/node_modules/framer-motion/dist/es/motion/features/animations.mjs
  init_define_import_meta_env();

  // frontend/node_modules/framer-motion/dist/es/motion/features/animation/index.mjs
  init_define_import_meta_env();
  var AnimationFeature = class extends Feature {
    /**
     * We dynamically generate the AnimationState manager as it contains a reference
     * to the underlying animation library. We only want to load that if we load this,
     * so people can optionally code split it out using the `m` component.
     */
    constructor(node) {
      super(node);
      node.animationState || (node.animationState = createAnimationState(node));
    }
    updateAnimationControlsSubscription() {
      const { animate } = this.node.getProps();
      if (isAnimationControls(animate)) {
        this.unmountControls = animate.subscribe(this.node);
      }
    }
    /**
     * Subscribe any provided AnimationControls to the component's VisualElement
     */
    mount() {
      this.updateAnimationControlsSubscription();
    }
    update() {
      const { animate } = this.node.getProps();
      const { animate: prevAnimate } = this.node.prevProps || {};
      if (animate !== prevAnimate) {
        this.updateAnimationControlsSubscription();
      }
    }
    unmount() {
      this.node.animationState.reset();
      this.unmountControls?.();
    }
  };

  // frontend/node_modules/framer-motion/dist/es/motion/features/animation/exit.mjs
  init_define_import_meta_env();
  var id2 = 0;
  var ExitAnimationFeature = class extends Feature {
    constructor() {
      super(...arguments);
      this.id = id2++;
      this.isExitComplete = false;
    }
    update() {
      if (!this.node.presenceContext)
        return;
      const { isPresent, onExitComplete } = this.node.presenceContext;
      const { isPresent: prevIsPresent } = this.node.prevPresenceContext || {};
      if (!this.node.animationState || isPresent === prevIsPresent) {
        return;
      }
      if (isPresent && prevIsPresent === false) {
        if (this.isExitComplete) {
          const { initial, custom } = this.node.getProps();
          if (typeof initial === "string" || typeof initial === "object" && initial !== null && !Array.isArray(initial)) {
            const resolved = resolveVariant(this.node, initial, custom);
            if (resolved) {
              const { transition, transitionEnd, ...target } = resolved;
              for (const key in target) {
                this.node.getValue(key)?.jump(target[key]);
              }
            }
          }
          this.node.animationState.reset();
          this.node.animationState.animateChanges();
        } else {
          this.node.animationState.setActive("exit", false);
        }
        this.isExitComplete = false;
        return;
      }
      const exitAnimation = this.node.animationState.setActive("exit", !isPresent);
      if (onExitComplete && !isPresent) {
        exitAnimation.then(() => {
          this.isExitComplete = true;
          onExitComplete(this.id);
        });
      }
    }
    mount() {
      const { register, onExitComplete } = this.node.presenceContext || {};
      if (onExitComplete) {
        onExitComplete(this.id);
      }
      if (register) {
        this.unmount = register(this.id);
      }
    }
    unmount() {
    }
  };

  // frontend/node_modules/framer-motion/dist/es/motion/features/animations.mjs
  var animations = {
    animation: {
      Feature: AnimationFeature
    },
    exit: {
      Feature: ExitAnimationFeature
    }
  };

  // frontend/node_modules/framer-motion/dist/es/motion/features/drag.mjs
  init_define_import_meta_env();

  // frontend/node_modules/framer-motion/dist/es/gestures/drag/index.mjs
  init_define_import_meta_env();

  // frontend/node_modules/framer-motion/dist/es/gestures/drag/VisualElementDragControls.mjs
  init_define_import_meta_env();

  // frontend/node_modules/framer-motion/dist/es/events/add-pointer-event.mjs
  init_define_import_meta_env();

  // frontend/node_modules/framer-motion/dist/es/events/event-info.mjs
  init_define_import_meta_env();
  function extractEventInfo(event) {
    return {
      point: {
        x: event.pageX,
        y: event.pageY
      }
    };
  }
  var addPointerInfo = (handler) => (event) => isPrimaryPointer(event) && handler(event, extractEventInfo(event));

  // frontend/node_modules/framer-motion/dist/es/events/add-pointer-event.mjs
  function addPointerEvent(target, eventName, handler, options) {
    return addDomEvent(target, eventName, addPointerInfo(handler), options);
  }

  // frontend/node_modules/framer-motion/dist/es/utils/get-context-window.mjs
  init_define_import_meta_env();
  var getContextWindow = ({ current }) => {
    return current ? current.ownerDocument.defaultView : null;
  };

  // frontend/node_modules/framer-motion/dist/es/gestures/pan/PanSession.mjs
  init_define_import_meta_env();

  // frontend/node_modules/framer-motion/dist/es/utils/distance.mjs
  init_define_import_meta_env();
  var distance = (a21, b8) => Math.abs(a21 - b8);
  function distance2D(a21, b8) {
    const xDelta = distance(a21.x, b8.x);
    const yDelta = distance(a21.y, b8.y);
    return Math.sqrt(xDelta ** 2 + yDelta ** 2);
  }

  // frontend/node_modules/framer-motion/dist/es/gestures/pan/PanSession.mjs
  var overflowStyles = /* @__PURE__ */ new Set(["auto", "scroll"]);
  var PanSession = class {
    constructor(event, handlers, { transformPagePoint, contextWindow = window, dragSnapToOrigin = false, distanceThreshold = 3, element } = {}) {
      this.startEvent = null;
      this.lastMoveEvent = null;
      this.lastMoveEventInfo = null;
      this.lastRawMoveEventInfo = null;
      this.handlers = {};
      this.contextWindow = window;
      this.scrollPositions = /* @__PURE__ */ new Map();
      this.removeScrollListeners = null;
      this.onElementScroll = (event2) => {
        this.handleScroll(event2.target);
      };
      this.onWindowScroll = () => {
        this.handleScroll(window);
      };
      this.updatePoint = () => {
        if (!(this.lastMoveEvent && this.lastMoveEventInfo))
          return;
        if (this.lastRawMoveEventInfo) {
          this.lastMoveEventInfo = transformPoint(this.lastRawMoveEventInfo, this.transformPagePoint);
        }
        const info2 = getPanInfo(this.lastMoveEventInfo, this.history);
        const isPanStarted = this.startEvent !== null;
        const isDistancePastThreshold = distance2D(info2.offset, { x: 0, y: 0 }) >= this.distanceThreshold;
        if (!isPanStarted && !isDistancePastThreshold)
          return;
        const { point: point2 } = info2;
        const { timestamp: timestamp2 } = frameData;
        this.history.push({ ...point2, timestamp: timestamp2 });
        const { onStart, onMove } = this.handlers;
        if (!isPanStarted) {
          onStart && onStart(this.lastMoveEvent, info2);
          this.startEvent = this.lastMoveEvent;
        }
        onMove && onMove(this.lastMoveEvent, info2);
      };
      this.handlePointerMove = (event2, info2) => {
        this.lastMoveEvent = event2;
        this.lastRawMoveEventInfo = info2;
        this.lastMoveEventInfo = transformPoint(info2, this.transformPagePoint);
        frame.update(this.updatePoint, true);
      };
      this.handlePointerUp = (event2, info2) => {
        this.end();
        const { onEnd, onSessionEnd, resumeAnimation } = this.handlers;
        if (this.dragSnapToOrigin || !this.startEvent) {
          resumeAnimation && resumeAnimation();
        }
        if (!(this.lastMoveEvent && this.lastMoveEventInfo))
          return;
        const panInfo = getPanInfo(event2.type === "pointercancel" ? this.lastMoveEventInfo : transformPoint(info2, this.transformPagePoint), this.history);
        if (this.startEvent && onEnd) {
          onEnd(event2, panInfo);
        }
        onSessionEnd && onSessionEnd(event2, panInfo);
      };
      if (!isPrimaryPointer(event))
        return;
      this.dragSnapToOrigin = dragSnapToOrigin;
      this.handlers = handlers;
      this.transformPagePoint = transformPagePoint;
      this.distanceThreshold = distanceThreshold;
      this.contextWindow = contextWindow || window;
      const info = extractEventInfo(event);
      const initialInfo = transformPoint(info, this.transformPagePoint);
      const { point } = initialInfo;
      const { timestamp } = frameData;
      this.history = [{ ...point, timestamp }];
      const { onSessionStart } = handlers;
      onSessionStart && onSessionStart(event, getPanInfo(initialInfo, this.history));
      const eventOptions = { passive: true, capture: true };
      this.removeListeners = pipe(addPointerEvent(this.contextWindow, "pointermove", this.handlePointerMove, eventOptions), addPointerEvent(this.contextWindow, "pointerup", this.handlePointerUp, eventOptions), addPointerEvent(this.contextWindow, "pointercancel", this.handlePointerUp, eventOptions));
      if (element) {
        this.startScrollTracking(element);
      }
    }
    /**
     * Start tracking scroll on ancestors and window.
     */
    startScrollTracking(element) {
      let current = element.parentElement;
      while (current) {
        const style = getComputedStyle(current);
        if (overflowStyles.has(style.overflowX) || overflowStyles.has(style.overflowY)) {
          this.scrollPositions.set(current, {
            x: current.scrollLeft,
            y: current.scrollTop
          });
        }
        current = current.parentElement;
      }
      this.scrollPositions.set(window, {
        x: window.scrollX,
        y: window.scrollY
      });
      window.addEventListener("scroll", this.onElementScroll, {
        capture: true
      });
      window.addEventListener("scroll", this.onWindowScroll);
      this.removeScrollListeners = () => {
        window.removeEventListener("scroll", this.onElementScroll, {
          capture: true
        });
        window.removeEventListener("scroll", this.onWindowScroll);
      };
    }
    /**
     * Handle scroll compensation during drag.
     *
     * For element scroll: adjusts history origin since pageX/pageY doesn't change.
     * For window scroll: adjusts lastMoveEventInfo since pageX/pageY would change.
     */
    handleScroll(target) {
      const initial = this.scrollPositions.get(target);
      if (!initial)
        return;
      const isWindow = target === window;
      const current = isWindow ? { x: window.scrollX, y: window.scrollY } : {
        x: target.scrollLeft,
        y: target.scrollTop
      };
      const delta = { x: current.x - initial.x, y: current.y - initial.y };
      if (delta.x === 0 && delta.y === 0)
        return;
      if (isWindow) {
        if (this.lastMoveEventInfo) {
          this.lastMoveEventInfo.point.x += delta.x;
          this.lastMoveEventInfo.point.y += delta.y;
        }
      } else {
        if (this.history.length > 0) {
          this.history[0].x -= delta.x;
          this.history[0].y -= delta.y;
        }
      }
      this.scrollPositions.set(target, current);
      frame.update(this.updatePoint, true);
    }
    updateHandlers(handlers) {
      this.handlers = handlers;
    }
    end() {
      this.removeListeners && this.removeListeners();
      this.removeScrollListeners && this.removeScrollListeners();
      this.scrollPositions.clear();
      cancelFrame(this.updatePoint);
    }
  };
  function transformPoint(info, transformPagePoint) {
    return transformPagePoint ? { point: transformPagePoint(info.point) } : info;
  }
  function subtractPoint(a21, b8) {
    return { x: a21.x - b8.x, y: a21.y - b8.y };
  }
  function getPanInfo({ point }, history) {
    return {
      point,
      delta: subtractPoint(point, lastDevicePoint(history)),
      offset: subtractPoint(point, startDevicePoint(history)),
      velocity: getVelocity(history, 0.1)
    };
  }
  function startDevicePoint(history) {
    return history[0];
  }
  function lastDevicePoint(history) {
    return history[history.length - 1];
  }
  function getVelocity(history, timeDelta) {
    if (history.length < 2) {
      return { x: 0, y: 0 };
    }
    let i13 = history.length - 1;
    let timestampedPoint = null;
    const lastPoint = lastDevicePoint(history);
    while (i13 >= 0) {
      timestampedPoint = history[i13];
      if (lastPoint.timestamp - timestampedPoint.timestamp > secondsToMilliseconds(timeDelta)) {
        break;
      }
      i13--;
    }
    if (!timestampedPoint) {
      return { x: 0, y: 0 };
    }
    if (timestampedPoint === history[0] && history.length > 2 && lastPoint.timestamp - timestampedPoint.timestamp > secondsToMilliseconds(timeDelta) * 2) {
      timestampedPoint = history[1];
    }
    const time2 = millisecondsToSeconds(lastPoint.timestamp - timestampedPoint.timestamp);
    if (time2 === 0) {
      return { x: 0, y: 0 };
    }
    const currentVelocity = {
      x: (lastPoint.x - timestampedPoint.x) / time2,
      y: (lastPoint.y - timestampedPoint.y) / time2
    };
    if (currentVelocity.x === Infinity) {
      currentVelocity.x = 0;
    }
    if (currentVelocity.y === Infinity) {
      currentVelocity.y = 0;
    }
    return currentVelocity;
  }

  // frontend/node_modules/framer-motion/dist/es/gestures/drag/utils/constraints.mjs
  init_define_import_meta_env();
  function applyConstraints(point, { min: min2, max: max2 }, elastic) {
    if (min2 !== void 0 && point < min2) {
      point = elastic ? mixNumber(min2, point, elastic.min) : Math.max(point, min2);
    } else if (max2 !== void 0 && point > max2) {
      point = elastic ? mixNumber(max2, point, elastic.max) : Math.min(point, max2);
    }
    return point;
  }
  function calcRelativeAxisConstraints(axis, min2, max2) {
    return {
      min: min2 !== void 0 ? axis.min + min2 : void 0,
      max: max2 !== void 0 ? axis.max + max2 - (axis.max - axis.min) : void 0
    };
  }
  function calcRelativeConstraints(layoutBox, { top, left, bottom, right }) {
    return {
      x: calcRelativeAxisConstraints(layoutBox.x, left, right),
      y: calcRelativeAxisConstraints(layoutBox.y, top, bottom)
    };
  }
  function calcViewportAxisConstraints(layoutAxis, constraintsAxis) {
    let min2 = constraintsAxis.min - layoutAxis.min;
    let max2 = constraintsAxis.max - layoutAxis.max;
    if (constraintsAxis.max - constraintsAxis.min < layoutAxis.max - layoutAxis.min) {
      [min2, max2] = [max2, min2];
    }
    return { min: min2, max: max2 };
  }
  function calcViewportConstraints(layoutBox, constraintsBox) {
    return {
      x: calcViewportAxisConstraints(layoutBox.x, constraintsBox.x),
      y: calcViewportAxisConstraints(layoutBox.y, constraintsBox.y)
    };
  }
  function calcOrigin(source, target) {
    let origin = 0.5;
    const sourceLength = calcLength(source);
    const targetLength = calcLength(target);
    if (targetLength > sourceLength) {
      origin = progress(target.min, target.max - sourceLength, source.min);
    } else if (sourceLength > targetLength) {
      origin = progress(source.min, source.max - targetLength, target.min);
    }
    return clamp(0, 1, origin);
  }
  function rebaseAxisConstraints(layout2, constraints) {
    const relativeConstraints = {};
    if (constraints.min !== void 0) {
      relativeConstraints.min = constraints.min - layout2.min;
    }
    if (constraints.max !== void 0) {
      relativeConstraints.max = constraints.max - layout2.min;
    }
    return relativeConstraints;
  }
  var defaultElastic = 0.35;
  function resolveDragElastic(dragElastic = defaultElastic) {
    if (dragElastic === false) {
      dragElastic = 0;
    } else if (dragElastic === true) {
      dragElastic = defaultElastic;
    }
    return {
      x: resolveAxisElastic(dragElastic, "left", "right"),
      y: resolveAxisElastic(dragElastic, "top", "bottom")
    };
  }
  function resolveAxisElastic(dragElastic, minLabel, maxLabel) {
    return {
      min: resolvePointElastic(dragElastic, minLabel),
      max: resolvePointElastic(dragElastic, maxLabel)
    };
  }
  function resolvePointElastic(dragElastic, label) {
    return typeof dragElastic === "number" ? dragElastic : dragElastic[label] || 0;
  }

  // frontend/node_modules/framer-motion/dist/es/gestures/drag/VisualElementDragControls.mjs
  var elementDragControls = /* @__PURE__ */ new WeakMap();
  var VisualElementDragControls = class {
    constructor(visualElement) {
      this.openDragLock = null;
      this.isDragging = false;
      this.currentDirection = null;
      this.originPoint = { x: 0, y: 0 };
      this.constraints = false;
      this.hasMutatedConstraints = false;
      this.elastic = createBox();
      this.latestPointerEvent = null;
      this.latestPanInfo = null;
      this.visualElement = visualElement;
    }
    start(originEvent, { snapToCursor = false, distanceThreshold } = {}) {
      const { presenceContext } = this.visualElement;
      if (presenceContext && presenceContext.isPresent === false)
        return;
      const onSessionStart = (event) => {
        if (snapToCursor) {
          this.snapToCursor(extractEventInfo(event).point);
        }
        this.stopAnimation();
      };
      const onStart = (event, info) => {
        const { drag: drag2, dragPropagation, onDragStart } = this.getProps();
        if (drag2 && !dragPropagation) {
          if (this.openDragLock)
            this.openDragLock();
          this.openDragLock = setDragLock(drag2);
          if (!this.openDragLock)
            return;
        }
        this.latestPointerEvent = event;
        this.latestPanInfo = info;
        this.isDragging = true;
        this.currentDirection = null;
        this.resolveConstraints();
        if (this.visualElement.projection) {
          this.visualElement.projection.isAnimationBlocked = true;
          this.visualElement.projection.target = void 0;
        }
        eachAxis((axis) => {
          let current = this.getAxisMotionValue(axis).get() || 0;
          if (percent.test(current)) {
            const { projection } = this.visualElement;
            if (projection && projection.layout) {
              const measuredAxis = projection.layout.layoutBox[axis];
              if (measuredAxis) {
                const length = calcLength(measuredAxis);
                current = length * (parseFloat(current) / 100);
              }
            }
          }
          this.originPoint[axis] = current;
        });
        if (onDragStart) {
          frame.update(() => onDragStart(event, info), false, true);
        }
        addValueToWillChange(this.visualElement, "transform");
        const { animationState } = this.visualElement;
        animationState && animationState.setActive("whileDrag", true);
      };
      const onMove = (event, info) => {
        this.latestPointerEvent = event;
        this.latestPanInfo = info;
        const { dragPropagation, dragDirectionLock, onDirectionLock, onDrag } = this.getProps();
        if (!dragPropagation && !this.openDragLock)
          return;
        const { offset: offset4 } = info;
        if (dragDirectionLock && this.currentDirection === null) {
          this.currentDirection = getCurrentDirection(offset4);
          if (this.currentDirection !== null) {
            onDirectionLock && onDirectionLock(this.currentDirection);
          }
          return;
        }
        this.updateAxis("x", info.point, offset4);
        this.updateAxis("y", info.point, offset4);
        this.visualElement.render();
        if (onDrag) {
          frame.update(() => onDrag(event, info), false, true);
        }
      };
      const onSessionEnd = (event, info) => {
        this.latestPointerEvent = event;
        this.latestPanInfo = info;
        this.stop(event, info);
        this.latestPointerEvent = null;
        this.latestPanInfo = null;
      };
      const resumeAnimation = () => {
        const { dragSnapToOrigin: snap } = this.getProps();
        if (snap || this.constraints) {
          this.startAnimation({ x: 0, y: 0 });
        }
      };
      const { dragSnapToOrigin } = this.getProps();
      this.panSession = new PanSession(originEvent, {
        onSessionStart,
        onStart,
        onMove,
        onSessionEnd,
        resumeAnimation
      }, {
        transformPagePoint: this.visualElement.getTransformPagePoint(),
        dragSnapToOrigin,
        distanceThreshold,
        contextWindow: getContextWindow(this.visualElement),
        element: this.visualElement.current
      });
    }
    /**
     * @internal
     */
    stop(event, panInfo) {
      const finalEvent = event || this.latestPointerEvent;
      const finalPanInfo = panInfo || this.latestPanInfo;
      const isDragging2 = this.isDragging;
      this.cancel();
      if (!isDragging2 || !finalPanInfo || !finalEvent)
        return;
      const { velocity } = finalPanInfo;
      this.startAnimation(velocity);
      const { onDragEnd } = this.getProps();
      if (onDragEnd) {
        frame.postRender(() => onDragEnd(finalEvent, finalPanInfo));
      }
    }
    /**
     * @internal
     */
    cancel() {
      this.isDragging = false;
      const { projection, animationState } = this.visualElement;
      if (projection) {
        projection.isAnimationBlocked = false;
      }
      this.endPanSession();
      const { dragPropagation } = this.getProps();
      if (!dragPropagation && this.openDragLock) {
        this.openDragLock();
        this.openDragLock = null;
      }
      animationState && animationState.setActive("whileDrag", false);
    }
    /**
     * Clean up the pan session without modifying other drag state.
     * This is used during unmount to ensure event listeners are removed
     * without affecting projection animations or drag locks.
     * @internal
     */
    endPanSession() {
      this.panSession && this.panSession.end();
      this.panSession = void 0;
    }
    updateAxis(axis, _point, offset4) {
      const { drag: drag2 } = this.getProps();
      if (!offset4 || !shouldDrag(axis, drag2, this.currentDirection))
        return;
      const axisValue = this.getAxisMotionValue(axis);
      let next = this.originPoint[axis] + offset4[axis];
      if (this.constraints && this.constraints[axis]) {
        next = applyConstraints(next, this.constraints[axis], this.elastic[axis]);
      }
      axisValue.set(next);
    }
    resolveConstraints() {
      const { dragConstraints, dragElastic } = this.getProps();
      const layout2 = this.visualElement.projection && !this.visualElement.projection.layout ? this.visualElement.projection.measure(false) : this.visualElement.projection?.layout;
      const prevConstraints = this.constraints;
      if (dragConstraints && isRefObject(dragConstraints)) {
        if (!this.constraints) {
          this.constraints = this.resolveRefConstraints();
        }
      } else {
        if (dragConstraints && layout2) {
          this.constraints = calcRelativeConstraints(layout2.layoutBox, dragConstraints);
        } else {
          this.constraints = false;
        }
      }
      this.elastic = resolveDragElastic(dragElastic);
      if (prevConstraints !== this.constraints && !isRefObject(dragConstraints) && layout2 && this.constraints && !this.hasMutatedConstraints) {
        eachAxis((axis) => {
          if (this.constraints !== false && this.getAxisMotionValue(axis)) {
            this.constraints[axis] = rebaseAxisConstraints(layout2.layoutBox[axis], this.constraints[axis]);
          }
        });
      }
    }
    resolveRefConstraints() {
      const { dragConstraints: constraints, onMeasureDragConstraints } = this.getProps();
      if (!constraints || !isRefObject(constraints))
        return false;
      const constraintsElement = constraints.current;
      invariant(constraintsElement !== null, "If `dragConstraints` is set as a React ref, that ref must be passed to another component's `ref` prop.", "drag-constraints-ref");
      const { projection } = this.visualElement;
      if (!projection || !projection.layout)
        return false;
      if (projection.root) {
        projection.root.scroll = void 0;
        projection.root.updateScroll();
      }
      const constraintsBox = measurePageBox(constraintsElement, projection.root, this.visualElement.getTransformPagePoint());
      let measuredConstraints = calcViewportConstraints(projection.layout.layoutBox, constraintsBox);
      if (onMeasureDragConstraints) {
        const userConstraints = onMeasureDragConstraints(convertBoxToBoundingBox(measuredConstraints));
        this.hasMutatedConstraints = !!userConstraints;
        if (userConstraints) {
          measuredConstraints = convertBoundingBoxToBox(userConstraints);
        }
      }
      return measuredConstraints;
    }
    startAnimation(velocity) {
      const { drag: drag2, dragMomentum, dragElastic, dragTransition, dragSnapToOrigin, onDragTransitionEnd } = this.getProps();
      const constraints = this.constraints || {};
      const momentumAnimations = eachAxis((axis) => {
        if (!shouldDrag(axis, drag2, this.currentDirection)) {
          return;
        }
        let transition = constraints && constraints[axis] || {};
        if (dragSnapToOrigin === true || dragSnapToOrigin === axis)
          transition = { min: 0, max: 0 };
        const bounceStiffness = dragElastic ? 200 : 1e6;
        const bounceDamping = dragElastic ? 40 : 1e7;
        const inertia2 = {
          type: "inertia",
          velocity: dragMomentum ? velocity[axis] : 0,
          bounceStiffness,
          bounceDamping,
          timeConstant: 750,
          restDelta: 1,
          restSpeed: 10,
          ...dragTransition,
          ...transition
        };
        return this.startAxisValueAnimation(axis, inertia2);
      });
      return Promise.all(momentumAnimations).then(onDragTransitionEnd);
    }
    startAxisValueAnimation(axis, transition) {
      const axisValue = this.getAxisMotionValue(axis);
      addValueToWillChange(this.visualElement, axis);
      return axisValue.start(animateMotionValue(axis, axisValue, 0, transition, this.visualElement, false));
    }
    stopAnimation() {
      eachAxis((axis) => this.getAxisMotionValue(axis).stop());
    }
    /**
     * Drag works differently depending on which props are provided.
     *
     * - If _dragX and _dragY are provided, we output the gesture delta directly to those motion values.
     * - Otherwise, we apply the delta to the x/y motion values.
     */
    getAxisMotionValue(axis) {
      const dragKey = `_drag${axis.toUpperCase()}`;
      const props = this.visualElement.getProps();
      const externalMotionValue = props[dragKey];
      return externalMotionValue ? externalMotionValue : this.visualElement.getValue(axis, this.visualElement.latestValues[axis] ?? 0);
    }
    snapToCursor(point) {
      eachAxis((axis) => {
        const { drag: drag2 } = this.getProps();
        if (!shouldDrag(axis, drag2, this.currentDirection))
          return;
        const { projection } = this.visualElement;
        const axisValue = this.getAxisMotionValue(axis);
        if (projection && projection.layout) {
          const { min: min2, max: max2 } = projection.layout.layoutBox[axis];
          const current = axisValue.get() || 0;
          axisValue.set(point[axis] - mixNumber(min2, max2, 0.5) + current);
        }
      });
    }
    /**
     * When the viewport resizes we want to check if the measured constraints
     * have changed and, if so, reposition the element within those new constraints
     * relative to where it was before the resize.
     */
    scalePositionWithinConstraints() {
      if (!this.visualElement.current)
        return;
      const { drag: drag2, dragConstraints } = this.getProps();
      const { projection } = this.visualElement;
      if (!isRefObject(dragConstraints) || !projection || !this.constraints)
        return;
      this.stopAnimation();
      const boxProgress = { x: 0, y: 0 };
      eachAxis((axis) => {
        const axisValue = this.getAxisMotionValue(axis);
        if (axisValue && this.constraints !== false) {
          const latest = axisValue.get();
          boxProgress[axis] = calcOrigin({ min: latest, max: latest }, this.constraints[axis]);
        }
      });
      const { transformTemplate } = this.visualElement.getProps();
      this.visualElement.current.style.transform = transformTemplate ? transformTemplate({}, "") : "none";
      projection.root && projection.root.updateScroll();
      projection.updateLayout();
      this.constraints = false;
      this.resolveConstraints();
      eachAxis((axis) => {
        if (!shouldDrag(axis, drag2, null))
          return;
        const axisValue = this.getAxisMotionValue(axis);
        const { min: min2, max: max2 } = this.constraints[axis];
        axisValue.set(mixNumber(min2, max2, boxProgress[axis]));
      });
      this.visualElement.render();
    }
    addListeners() {
      if (!this.visualElement.current)
        return;
      elementDragControls.set(this.visualElement, this);
      const element = this.visualElement.current;
      const stopPointerListener = addPointerEvent(element, "pointerdown", (event) => {
        const { drag: drag2, dragListener = true } = this.getProps();
        const target = event.target;
        const isClickingTextInputChild = target !== element && isElementTextInput(target);
        if (drag2 && dragListener && !isClickingTextInputChild) {
          this.start(event);
        }
      });
      let stopResizeObservers;
      const measureDragConstraints = () => {
        const { dragConstraints } = this.getProps();
        if (isRefObject(dragConstraints) && dragConstraints.current) {
          this.constraints = this.resolveRefConstraints();
          if (!stopResizeObservers) {
            stopResizeObservers = startResizeObservers(element, dragConstraints.current, () => this.scalePositionWithinConstraints());
          }
        }
      };
      const { projection } = this.visualElement;
      const stopMeasureLayoutListener = projection.addEventListener("measure", measureDragConstraints);
      if (projection && !projection.layout) {
        projection.root && projection.root.updateScroll();
        projection.updateLayout();
      }
      frame.read(measureDragConstraints);
      const stopResizeListener = addDomEvent(window, "resize", () => this.scalePositionWithinConstraints());
      const stopLayoutUpdateListener = projection.addEventListener("didUpdate", (({ delta, hasLayoutChanged }) => {
        if (this.isDragging && hasLayoutChanged) {
          eachAxis((axis) => {
            const motionValue2 = this.getAxisMotionValue(axis);
            if (!motionValue2)
              return;
            this.originPoint[axis] += delta[axis].translate;
            motionValue2.set(motionValue2.get() + delta[axis].translate);
          });
          this.visualElement.render();
        }
      }));
      return () => {
        stopResizeListener();
        stopPointerListener();
        stopMeasureLayoutListener();
        stopLayoutUpdateListener && stopLayoutUpdateListener();
        stopResizeObservers && stopResizeObservers();
      };
    }
    getProps() {
      const props = this.visualElement.getProps();
      const { drag: drag2 = false, dragDirectionLock = false, dragPropagation = false, dragConstraints = false, dragElastic = defaultElastic, dragMomentum = true } = props;
      return {
        ...props,
        drag: drag2,
        dragDirectionLock,
        dragPropagation,
        dragConstraints,
        dragElastic,
        dragMomentum
      };
    }
  };
  function skipFirstCall(callback) {
    let isFirst = true;
    return () => {
      if (isFirst) {
        isFirst = false;
        return;
      }
      callback();
    };
  }
  function startResizeObservers(element, constraintsElement, onResize) {
    const stopElement = resize(element, skipFirstCall(onResize));
    const stopContainer = resize(constraintsElement, skipFirstCall(onResize));
    return () => {
      stopElement();
      stopContainer();
    };
  }
  function shouldDrag(direction, drag2, currentDirection) {
    return (drag2 === true || drag2 === direction) && (currentDirection === null || currentDirection === direction);
  }
  function getCurrentDirection(offset4, lockThreshold = 10) {
    let direction = null;
    if (Math.abs(offset4.y) > lockThreshold) {
      direction = "y";
    } else if (Math.abs(offset4.x) > lockThreshold) {
      direction = "x";
    }
    return direction;
  }

  // frontend/node_modules/framer-motion/dist/es/gestures/drag/index.mjs
  var DragGesture = class extends Feature {
    constructor(node) {
      super(node);
      this.removeGroupControls = noop;
      this.removeListeners = noop;
      this.controls = new VisualElementDragControls(node);
    }
    mount() {
      const { dragControls } = this.node.getProps();
      if (dragControls) {
        this.removeGroupControls = dragControls.subscribe(this.controls);
      }
      this.removeListeners = this.controls.addListeners() || noop;
    }
    update() {
      const { dragControls } = this.node.getProps();
      const { dragControls: prevDragControls } = this.node.prevProps || {};
      if (dragControls !== prevDragControls) {
        this.removeGroupControls();
        if (dragControls) {
          this.removeGroupControls = dragControls.subscribe(this.controls);
        }
      }
    }
    unmount() {
      this.removeGroupControls();
      this.removeListeners();
      if (!this.controls.isDragging) {
        this.controls.endPanSession();
      }
    }
  };

  // frontend/node_modules/framer-motion/dist/es/gestures/pan/index.mjs
  init_define_import_meta_env();
  var asyncHandler = (handler) => (event, info) => {
    if (handler) {
      frame.update(() => handler(event, info), false, true);
    }
  };
  var PanGesture = class extends Feature {
    constructor() {
      super(...arguments);
      this.removePointerDownListener = noop;
    }
    onPointerDown(pointerDownEvent) {
      this.session = new PanSession(pointerDownEvent, this.createPanHandlers(), {
        transformPagePoint: this.node.getTransformPagePoint(),
        contextWindow: getContextWindow(this.node)
      });
    }
    createPanHandlers() {
      const { onPanSessionStart, onPanStart, onPan, onPanEnd } = this.node.getProps();
      return {
        onSessionStart: asyncHandler(onPanSessionStart),
        onStart: asyncHandler(onPanStart),
        onMove: asyncHandler(onPan),
        onEnd: (event, info) => {
          delete this.session;
          if (onPanEnd) {
            frame.postRender(() => onPanEnd(event, info));
          }
        }
      };
    }
    mount() {
      this.removePointerDownListener = addPointerEvent(this.node.current, "pointerdown", (event) => this.onPointerDown(event));
    }
    update() {
      this.session && this.session.updateHandlers(this.createPanHandlers());
    }
    unmount() {
      this.removePointerDownListener();
      this.session && this.session.end();
    }
  };

  // frontend/node_modules/framer-motion/dist/es/motion/features/layout/MeasureLayout.mjs
  init_define_import_meta_env();
  var import_jsx_runtime2 = __toESM(require_react_shim(), 1);
  var import_react19 = __toESM(require_react_shim(), 1);
  var hasTakenAnySnapshot = false;
  var MeasureLayoutWithContext = class extends import_react19.Component {
    /**
     * This only mounts projection nodes for components that
     * need measuring, we might want to do it for all components
     * in order to incorporate transforms
     */
    componentDidMount() {
      const { visualElement, layoutGroup, switchLayoutGroup, layoutId } = this.props;
      const { projection } = visualElement;
      if (projection) {
        if (layoutGroup.group)
          layoutGroup.group.add(projection);
        if (switchLayoutGroup && switchLayoutGroup.register && layoutId) {
          switchLayoutGroup.register(projection);
        }
        if (hasTakenAnySnapshot) {
          projection.root.didUpdate();
        }
        projection.addEventListener("animationComplete", () => {
          this.safeToRemove();
        });
        projection.setOptions({
          ...projection.options,
          layoutDependency: this.props.layoutDependency,
          onExitComplete: () => this.safeToRemove()
        });
      }
      globalProjectionState.hasEverUpdated = true;
    }
    getSnapshotBeforeUpdate(prevProps) {
      const { layoutDependency, visualElement, drag: drag2, isPresent } = this.props;
      const { projection } = visualElement;
      if (!projection)
        return null;
      projection.isPresent = isPresent;
      if (prevProps.layoutDependency !== layoutDependency) {
        projection.setOptions({
          ...projection.options,
          layoutDependency
        });
      }
      hasTakenAnySnapshot = true;
      if (drag2 || prevProps.layoutDependency !== layoutDependency || layoutDependency === void 0 || prevProps.isPresent !== isPresent) {
        projection.willUpdate();
      } else {
        this.safeToRemove();
      }
      if (prevProps.isPresent !== isPresent) {
        if (isPresent) {
          projection.promote();
        } else if (!projection.relegate()) {
          frame.postRender(() => {
            const stack = projection.getStack();
            if (!stack || !stack.members.length) {
              this.safeToRemove();
            }
          });
        }
      }
      return null;
    }
    componentDidUpdate() {
      const { visualElement, layoutAnchor } = this.props;
      const { projection } = visualElement;
      if (projection) {
        projection.options.layoutAnchor = layoutAnchor;
        projection.root.didUpdate();
        microtask.postRender(() => {
          if (!projection.currentAnimation && projection.isLead()) {
            this.safeToRemove();
          }
        });
      }
    }
    componentWillUnmount() {
      const { visualElement, layoutGroup, switchLayoutGroup: promoteContext } = this.props;
      const { projection } = visualElement;
      hasTakenAnySnapshot = true;
      if (projection) {
        projection.scheduleCheckAfterUnmount();
        if (layoutGroup && layoutGroup.group)
          layoutGroup.group.remove(projection);
        if (promoteContext && promoteContext.deregister)
          promoteContext.deregister(projection);
      }
    }
    safeToRemove() {
      const { safeToRemove } = this.props;
      safeToRemove && safeToRemove();
    }
    render() {
      return null;
    }
  };
  function MeasureLayout(props) {
    const [isPresent, safeToRemove] = usePresence();
    const layoutGroup = (0, import_react19.useContext)(LayoutGroupContext);
    return (0, import_jsx_runtime2.jsx)(MeasureLayoutWithContext, { ...props, layoutGroup, switchLayoutGroup: (0, import_react19.useContext)(SwitchLayoutGroupContext), isPresent, safeToRemove });
  }

  // frontend/node_modules/framer-motion/dist/es/motion/features/drag.mjs
  var drag = {
    pan: {
      Feature: PanGesture
    },
    drag: {
      Feature: DragGesture,
      ProjectionNode: HTMLProjectionNode,
      MeasureLayout
    }
  };

  // frontend/node_modules/framer-motion/dist/es/motion/features/gestures.mjs
  init_define_import_meta_env();

  // frontend/node_modules/framer-motion/dist/es/gestures/hover.mjs
  init_define_import_meta_env();
  function handleHoverEvent(node, event, lifecycle) {
    const { props } = node;
    if (node.animationState && props.whileHover) {
      node.animationState.setActive("whileHover", lifecycle === "Start");
    }
    const eventName = "onHover" + lifecycle;
    const callback = props[eventName];
    if (callback) {
      frame.postRender(() => callback(event, extractEventInfo(event)));
    }
  }
  var HoverGesture = class extends Feature {
    mount() {
      const { current } = this.node;
      if (!current)
        return;
      this.unmount = hover(current, (_element, startEvent) => {
        handleHoverEvent(this.node, startEvent, "Start");
        return (endEvent) => handleHoverEvent(this.node, endEvent, "End");
      });
    }
    unmount() {
    }
  };

  // frontend/node_modules/framer-motion/dist/es/gestures/focus.mjs
  init_define_import_meta_env();
  var FocusGesture = class extends Feature {
    constructor() {
      super(...arguments);
      this.isActive = false;
    }
    onFocus() {
      let isFocusVisible = false;
      try {
        isFocusVisible = this.node.current.matches(":focus-visible");
      } catch (e8) {
        isFocusVisible = true;
      }
      if (!isFocusVisible || !this.node.animationState)
        return;
      this.node.animationState.setActive("whileFocus", true);
      this.isActive = true;
    }
    onBlur() {
      if (!this.isActive || !this.node.animationState)
        return;
      this.node.animationState.setActive("whileFocus", false);
      this.isActive = false;
    }
    mount() {
      this.unmount = pipe(addDomEvent(this.node.current, "focus", () => this.onFocus()), addDomEvent(this.node.current, "blur", () => this.onBlur()));
    }
    unmount() {
    }
  };

  // frontend/node_modules/framer-motion/dist/es/gestures/press.mjs
  init_define_import_meta_env();
  function handlePressEvent(node, event, lifecycle) {
    const { props } = node;
    if (node.current instanceof HTMLButtonElement && node.current.disabled) {
      return;
    }
    if (node.animationState && props.whileTap) {
      node.animationState.setActive("whileTap", lifecycle === "Start");
    }
    const eventName = "onTap" + (lifecycle === "End" ? "" : lifecycle);
    const callback = props[eventName];
    if (callback) {
      frame.postRender(() => callback(event, extractEventInfo(event)));
    }
  }
  var PressGesture = class extends Feature {
    mount() {
      const { current } = this.node;
      if (!current)
        return;
      const { globalTapTarget, propagate } = this.node.props;
      this.unmount = press(current, (_element, startEvent) => {
        handlePressEvent(this.node, startEvent, "Start");
        return (endEvent, { success }) => handlePressEvent(this.node, endEvent, success ? "End" : "Cancel");
      }, {
        useGlobalTarget: globalTapTarget,
        stopPropagation: propagate?.tap === false
      });
    }
    unmount() {
    }
  };

  // frontend/node_modules/framer-motion/dist/es/motion/features/viewport/index.mjs
  init_define_import_meta_env();

  // frontend/node_modules/framer-motion/dist/es/motion/features/viewport/observers.mjs
  init_define_import_meta_env();
  var observerCallbacks = /* @__PURE__ */ new WeakMap();
  var observers = /* @__PURE__ */ new WeakMap();
  var fireObserverCallback = (entry) => {
    const callback = observerCallbacks.get(entry.target);
    callback && callback(entry);
  };
  var fireAllObserverCallbacks = (entries) => {
    entries.forEach(fireObserverCallback);
  };
  function initIntersectionObserver({ root, ...options }) {
    const lookupRoot = root || document;
    if (!observers.has(lookupRoot)) {
      observers.set(lookupRoot, {});
    }
    const rootObservers = observers.get(lookupRoot);
    const key = JSON.stringify(options);
    if (!rootObservers[key]) {
      rootObservers[key] = new IntersectionObserver(fireAllObserverCallbacks, { root, ...options });
    }
    return rootObservers[key];
  }
  function observeIntersection(element, options, callback) {
    const rootInteresectionObserver = initIntersectionObserver(options);
    observerCallbacks.set(element, callback);
    rootInteresectionObserver.observe(element);
    return () => {
      observerCallbacks.delete(element);
      rootInteresectionObserver.unobserve(element);
    };
  }

  // frontend/node_modules/framer-motion/dist/es/motion/features/viewport/index.mjs
  var thresholdNames = {
    some: 0,
    all: 1
  };
  var InViewFeature = class extends Feature {
    constructor() {
      super(...arguments);
      this.hasEnteredView = false;
      this.isInView = false;
    }
    startObserver() {
      this.stopObserver?.();
      const { viewport = {} } = this.node.getProps();
      const { root, margin: rootMargin, amount = "some", once } = viewport;
      const options = {
        root: root ? root.current : void 0,
        rootMargin,
        threshold: typeof amount === "number" ? amount : thresholdNames[amount]
      };
      const onIntersectionUpdate = (entry) => {
        const { isIntersecting } = entry;
        if (this.isInView === isIntersecting)
          return;
        this.isInView = isIntersecting;
        if (once && !isIntersecting && this.hasEnteredView) {
          return;
        } else if (isIntersecting) {
          this.hasEnteredView = true;
        }
        if (this.node.animationState) {
          this.node.animationState.setActive("whileInView", isIntersecting);
        }
        const { onViewportEnter, onViewportLeave } = this.node.getProps();
        const callback = isIntersecting ? onViewportEnter : onViewportLeave;
        callback && callback(entry);
      };
      this.stopObserver = observeIntersection(this.node.current, options, onIntersectionUpdate);
    }
    mount() {
      this.startObserver();
    }
    update() {
      if (typeof IntersectionObserver === "undefined")
        return;
      const { props, prevProps } = this.node;
      const hasOptionsChanged = ["amount", "margin", "root"].some(hasViewportOptionChanged(props, prevProps));
      if (hasOptionsChanged) {
        this.startObserver();
      }
    }
    unmount() {
      this.stopObserver?.();
      this.hasEnteredView = false;
      this.isInView = false;
    }
  };
  function hasViewportOptionChanged({ viewport = {} }, { viewport: prevViewport = {} } = {}) {
    return (name) => viewport[name] !== prevViewport[name];
  }

  // frontend/node_modules/framer-motion/dist/es/motion/features/gestures.mjs
  var gestureAnimations = {
    inView: {
      Feature: InViewFeature
    },
    tap: {
      Feature: PressGesture
    },
    focus: {
      Feature: FocusGesture
    },
    hover: {
      Feature: HoverGesture
    }
  };

  // frontend/node_modules/framer-motion/dist/es/motion/features/layout.mjs
  init_define_import_meta_env();
  var layout = {
    layout: {
      ProjectionNode: HTMLProjectionNode,
      MeasureLayout
    }
  };

  // frontend/node_modules/framer-motion/dist/es/render/components/motion/feature-bundle.mjs
  var featureBundle = {
    ...animations,
    ...gestureAnimations,
    ...drag,
    ...layout
  };

  // frontend/node_modules/framer-motion/dist/es/render/components/motion/proxy.mjs
  var motion = /* @__PURE__ */ createMotionProxy(featureBundle, createDomVisualElement);

  // frontend/node_modules/framer-motion/dist/es/utils/reduced-motion/use-reduced-motion.mjs
  init_define_import_meta_env();
  var import_react20 = __toESM(require_react_shim(), 1);
  function useReducedMotion() {
    !hasReducedMotionListener.current && initPrefersReducedMotion();
    const [shouldReduceMotion] = (0, import_react20.useState)(prefersReducedMotion.current);
    if (true) {
      warnOnce(shouldReduceMotion !== true, "You have Reduced Motion enabled on your device. Animations may not appear as expected.", "reduced-motion-disabled");
    }
    return shouldReduceMotion;
  }

  // frontend/node_modules/motion/dist/es/react.mjs
  var motion2 = motion;

  // frontend/node_modules/psihointegritet-ds/src/motion/reveal.tsx
  function Reveal({ children, className }) {
    const reduceMotion = useReducedMotion();
    if (reduceMotion) {
      return /* @__PURE__ */ React.createElement("div", { className }, children);
    }
    return /* @__PURE__ */ React.createElement(
      motion2.div,
      {
        className,
        initial: { opacity: 0, y: 28 },
        whileInView: { opacity: 1, y: 0 },
        viewport: { once: true, amount: 0.08, margin: "0px 0px -8% 0px" },
        transition: { duration: 0.7, ease: [0.22, 1, 0.36, 1] }
      },
      children
    );
  }

  // frontend/node_modules/psihointegritet-ds/src/motion/sticky-bar.tsx
  init_define_import_meta_env();
  var import_react22 = __toESM(require_react_shim());

  // frontend/src/helpers/cn.ts
  init_define_import_meta_env();

  // frontend/node_modules/clsx/dist/clsx.mjs
  init_define_import_meta_env();
  function r(e8) {
    var t12, f13, n13 = "";
    if ("string" == typeof e8 || "number" == typeof e8) n13 += e8;
    else if ("object" == typeof e8) if (Array.isArray(e8)) {
      var o16 = e8.length;
      for (t12 = 0; t12 < o16; t12++) e8[t12] && (f13 = r(e8[t12])) && (n13 && (n13 += " "), n13 += f13);
    } else for (f13 in e8) e8[f13] && (n13 && (n13 += " "), n13 += f13);
    return n13;
  }
  function clsx() {
    for (var e8, t12, f13 = 0, n13 = "", o16 = arguments.length; f13 < o16; f13++) (e8 = arguments[f13]) && (t12 = r(e8)) && (n13 && (n13 += " "), n13 += t12);
    return n13;
  }

  // frontend/node_modules/tailwind-merge/dist/bundle-mjs.mjs
  init_define_import_meta_env();
  var concatArrays = (array1, array2) => {
    const combinedArray = new Array(array1.length + array2.length);
    for (let i13 = 0; i13 < array1.length; i13++) {
      combinedArray[i13] = array1[i13];
    }
    for (let i13 = 0; i13 < array2.length; i13++) {
      combinedArray[array1.length + i13] = array2[i13];
    }
    return combinedArray;
  };
  var createClassValidatorObject = (classGroupId, validator) => ({
    classGroupId,
    validator
  });
  var createClassPartObject = (nextPart = /* @__PURE__ */ new Map(), validators = null, classGroupId) => ({
    nextPart,
    validators,
    classGroupId
  });
  var CLASS_PART_SEPARATOR = "-";
  var EMPTY_CONFLICTS = [];
  var ARBITRARY_PROPERTY_PREFIX = "arbitrary..";
  var createClassGroupUtils = (config) => {
    const classMap = createClassMap(config);
    const {
      conflictingClassGroups,
      conflictingClassGroupModifiers
    } = config;
    const getClassGroupId = (className) => {
      if (className.startsWith("[") && className.endsWith("]")) {
        return getGroupIdForArbitraryProperty(className);
      }
      const classParts = className.split(CLASS_PART_SEPARATOR);
      const startIndex = classParts[0] === "" && classParts.length > 1 ? 1 : 0;
      return getGroupRecursive(classParts, startIndex, classMap);
    };
    const getConflictingClassGroupIds = (classGroupId, hasPostfixModifier) => {
      if (hasPostfixModifier) {
        const modifierConflicts = conflictingClassGroupModifiers[classGroupId];
        const baseConflicts = conflictingClassGroups[classGroupId];
        if (modifierConflicts) {
          if (baseConflicts) {
            return concatArrays(baseConflicts, modifierConflicts);
          }
          return modifierConflicts;
        }
        return baseConflicts || EMPTY_CONFLICTS;
      }
      return conflictingClassGroups[classGroupId] || EMPTY_CONFLICTS;
    };
    return {
      getClassGroupId,
      getConflictingClassGroupIds
    };
  };
  var getGroupRecursive = (classParts, startIndex, classPartObject) => {
    const classPathsLength = classParts.length - startIndex;
    if (classPathsLength === 0) {
      return classPartObject.classGroupId;
    }
    const currentClassPart = classParts[startIndex];
    const nextClassPartObject = classPartObject.nextPart.get(currentClassPart);
    if (nextClassPartObject) {
      const result = getGroupRecursive(classParts, startIndex + 1, nextClassPartObject);
      if (result) return result;
    }
    const validators = classPartObject.validators;
    if (validators === null) {
      return void 0;
    }
    const classRest = startIndex === 0 ? classParts.join(CLASS_PART_SEPARATOR) : classParts.slice(startIndex).join(CLASS_PART_SEPARATOR);
    const validatorsLength = validators.length;
    for (let i13 = 0; i13 < validatorsLength; i13++) {
      const validatorObj = validators[i13];
      if (validatorObj.validator(classRest)) {
        return validatorObj.classGroupId;
      }
    }
    return void 0;
  };
  var getGroupIdForArbitraryProperty = (className) => className.slice(1, -1).indexOf(":") === -1 ? void 0 : (() => {
    const content = className.slice(1, -1);
    const colonIndex = content.indexOf(":");
    const property = content.slice(0, colonIndex);
    return property ? ARBITRARY_PROPERTY_PREFIX + property : void 0;
  })();
  var createClassMap = (config) => {
    const {
      theme,
      classGroups
    } = config;
    return processClassGroups(classGroups, theme);
  };
  var processClassGroups = (classGroups, theme) => {
    const classMap = createClassPartObject();
    for (const classGroupId in classGroups) {
      const group = classGroups[classGroupId];
      processClassesRecursively(group, classMap, classGroupId, theme);
    }
    return classMap;
  };
  var processClassesRecursively = (classGroup, classPartObject, classGroupId, theme) => {
    const len = classGroup.length;
    for (let i13 = 0; i13 < len; i13++) {
      const classDefinition = classGroup[i13];
      processClassDefinition(classDefinition, classPartObject, classGroupId, theme);
    }
  };
  var processClassDefinition = (classDefinition, classPartObject, classGroupId, theme) => {
    if (typeof classDefinition === "string") {
      processStringDefinition(classDefinition, classPartObject, classGroupId);
      return;
    }
    if (typeof classDefinition === "function") {
      processFunctionDefinition(classDefinition, classPartObject, classGroupId, theme);
      return;
    }
    processObjectDefinition(classDefinition, classPartObject, classGroupId, theme);
  };
  var processStringDefinition = (classDefinition, classPartObject, classGroupId) => {
    const classPartObjectToEdit = classDefinition === "" ? classPartObject : getPart(classPartObject, classDefinition);
    classPartObjectToEdit.classGroupId = classGroupId;
  };
  var processFunctionDefinition = (classDefinition, classPartObject, classGroupId, theme) => {
    if (isThemeGetter(classDefinition)) {
      processClassesRecursively(classDefinition(theme), classPartObject, classGroupId, theme);
      return;
    }
    if (classPartObject.validators === null) {
      classPartObject.validators = [];
    }
    classPartObject.validators.push(createClassValidatorObject(classGroupId, classDefinition));
  };
  var processObjectDefinition = (classDefinition, classPartObject, classGroupId, theme) => {
    const entries = Object.entries(classDefinition);
    const len = entries.length;
    for (let i13 = 0; i13 < len; i13++) {
      const [key, value] = entries[i13];
      processClassesRecursively(value, getPart(classPartObject, key), classGroupId, theme);
    }
  };
  var getPart = (classPartObject, path) => {
    let current = classPartObject;
    const parts = path.split(CLASS_PART_SEPARATOR);
    const len = parts.length;
    for (let i13 = 0; i13 < len; i13++) {
      const part = parts[i13];
      let next = current.nextPart.get(part);
      if (!next) {
        next = createClassPartObject();
        current.nextPart.set(part, next);
      }
      current = next;
    }
    return current;
  };
  var isThemeGetter = (func) => "isThemeGetter" in func && func.isThemeGetter === true;
  var createLruCache = (maxCacheSize) => {
    if (maxCacheSize < 1) {
      return {
        get: () => void 0,
        set: () => {
        }
      };
    }
    let cacheSize = 0;
    let cache = /* @__PURE__ */ Object.create(null);
    let previousCache = /* @__PURE__ */ Object.create(null);
    const update = (key, value) => {
      cache[key] = value;
      cacheSize++;
      if (cacheSize > maxCacheSize) {
        cacheSize = 0;
        previousCache = cache;
        cache = /* @__PURE__ */ Object.create(null);
      }
    };
    return {
      get(key) {
        let value = cache[key];
        if (value !== void 0) {
          return value;
        }
        if ((value = previousCache[key]) !== void 0) {
          update(key, value);
          return value;
        }
      },
      set(key, value) {
        if (key in cache) {
          cache[key] = value;
        } else {
          update(key, value);
        }
      }
    };
  };
  var IMPORTANT_MODIFIER = "!";
  var MODIFIER_SEPARATOR = ":";
  var EMPTY_MODIFIERS = [];
  var createResultObject = (modifiers, hasImportantModifier, baseClassName, maybePostfixModifierPosition, isExternal) => ({
    modifiers,
    hasImportantModifier,
    baseClassName,
    maybePostfixModifierPosition,
    isExternal
  });
  var createParseClassName = (config) => {
    const {
      prefix,
      experimentalParseClassName
    } = config;
    let parseClassName = (className) => {
      const modifiers = [];
      let bracketDepth = 0;
      let parenDepth = 0;
      let modifierStart = 0;
      let postfixModifierPosition;
      const len = className.length;
      for (let index3 = 0; index3 < len; index3++) {
        const currentCharacter = className[index3];
        if (bracketDepth === 0 && parenDepth === 0) {
          if (currentCharacter === MODIFIER_SEPARATOR) {
            modifiers.push(className.slice(modifierStart, index3));
            modifierStart = index3 + 1;
            continue;
          }
          if (currentCharacter === "/") {
            postfixModifierPosition = index3;
            continue;
          }
        }
        if (currentCharacter === "[") bracketDepth++;
        else if (currentCharacter === "]") bracketDepth--;
        else if (currentCharacter === "(") parenDepth++;
        else if (currentCharacter === ")") parenDepth--;
      }
      const baseClassNameWithImportantModifier = modifiers.length === 0 ? className : className.slice(modifierStart);
      let baseClassName = baseClassNameWithImportantModifier;
      let hasImportantModifier = false;
      if (baseClassNameWithImportantModifier.endsWith(IMPORTANT_MODIFIER)) {
        baseClassName = baseClassNameWithImportantModifier.slice(0, -1);
        hasImportantModifier = true;
      } else if (
        /**
         * In Tailwind CSS v3 the important modifier was at the start of the base class name. This is still supported for legacy reasons.
         * @see https://github.com/dcastil/tailwind-merge/issues/513#issuecomment-2614029864
         */
        baseClassNameWithImportantModifier.startsWith(IMPORTANT_MODIFIER)
      ) {
        baseClassName = baseClassNameWithImportantModifier.slice(1);
        hasImportantModifier = true;
      }
      const maybePostfixModifierPosition = postfixModifierPosition && postfixModifierPosition > modifierStart ? postfixModifierPosition - modifierStart : void 0;
      return createResultObject(modifiers, hasImportantModifier, baseClassName, maybePostfixModifierPosition);
    };
    if (prefix) {
      const fullPrefix = prefix + MODIFIER_SEPARATOR;
      const parseClassNameOriginal = parseClassName;
      parseClassName = (className) => className.startsWith(fullPrefix) ? parseClassNameOriginal(className.slice(fullPrefix.length)) : createResultObject(EMPTY_MODIFIERS, false, className, void 0, true);
    }
    if (experimentalParseClassName) {
      const parseClassNameOriginal = parseClassName;
      parseClassName = (className) => experimentalParseClassName({
        className,
        parseClassName: parseClassNameOriginal
      });
    }
    return parseClassName;
  };
  var createSortModifiers = (config) => {
    const modifierWeights = /* @__PURE__ */ new Map();
    config.orderSensitiveModifiers.forEach((mod, index3) => {
      modifierWeights.set(mod, 1e6 + index3);
    });
    return (modifiers) => {
      const result = [];
      let currentSegment = [];
      for (let i13 = 0; i13 < modifiers.length; i13++) {
        const modifier = modifiers[i13];
        const isArbitrary = modifier[0] === "[";
        const isOrderSensitive = modifierWeights.has(modifier);
        if (isArbitrary || isOrderSensitive) {
          if (currentSegment.length > 0) {
            currentSegment.sort();
            result.push(...currentSegment);
            currentSegment = [];
          }
          result.push(modifier);
        } else {
          currentSegment.push(modifier);
        }
      }
      if (currentSegment.length > 0) {
        currentSegment.sort();
        result.push(...currentSegment);
      }
      return result;
    };
  };
  var createConfigUtils = (config) => ({
    cache: createLruCache(config.cacheSize),
    parseClassName: createParseClassName(config),
    sortModifiers: createSortModifiers(config),
    postfixLookupClassGroupIds: createPostfixLookupClassGroupIds(config),
    ...createClassGroupUtils(config)
  });
  var createPostfixLookupClassGroupIds = (config) => {
    const lookup = /* @__PURE__ */ Object.create(null);
    const classGroupIds = config.postfixLookupClassGroups;
    if (classGroupIds) {
      for (let i13 = 0; i13 < classGroupIds.length; i13++) {
        lookup[classGroupIds[i13]] = true;
      }
    }
    return lookup;
  };
  var SPLIT_CLASSES_REGEX = /\s+/;
  var mergeClassList = (classList, configUtils) => {
    const {
      parseClassName,
      getClassGroupId,
      getConflictingClassGroupIds,
      sortModifiers,
      postfixLookupClassGroupIds
    } = configUtils;
    const classGroupsInConflict = [];
    const classNames = classList.trim().split(SPLIT_CLASSES_REGEX);
    let result = "";
    for (let index3 = classNames.length - 1; index3 >= 0; index3 -= 1) {
      const originalClassName = classNames[index3];
      const {
        isExternal,
        modifiers,
        hasImportantModifier,
        baseClassName,
        maybePostfixModifierPosition
      } = parseClassName(originalClassName);
      if (isExternal) {
        result = originalClassName + (result.length > 0 ? " " + result : result);
        continue;
      }
      let hasPostfixModifier = !!maybePostfixModifierPosition;
      let classGroupId;
      if (hasPostfixModifier) {
        const baseClassNameWithoutPostfix = baseClassName.substring(0, maybePostfixModifierPosition);
        classGroupId = getClassGroupId(baseClassNameWithoutPostfix);
        const classGroupIdWithPostfix = classGroupId && postfixLookupClassGroupIds[classGroupId] ? getClassGroupId(baseClassName) : void 0;
        if (classGroupIdWithPostfix && classGroupIdWithPostfix !== classGroupId) {
          classGroupId = classGroupIdWithPostfix;
          hasPostfixModifier = false;
        }
      } else {
        classGroupId = getClassGroupId(baseClassName);
      }
      if (!classGroupId) {
        if (!hasPostfixModifier) {
          result = originalClassName + (result.length > 0 ? " " + result : result);
          continue;
        }
        classGroupId = getClassGroupId(baseClassName);
        if (!classGroupId) {
          result = originalClassName + (result.length > 0 ? " " + result : result);
          continue;
        }
        hasPostfixModifier = false;
      }
      const variantModifier = modifiers.length === 0 ? "" : modifiers.length === 1 ? modifiers[0] : sortModifiers(modifiers).join(":");
      const modifierId = hasImportantModifier ? variantModifier + IMPORTANT_MODIFIER : variantModifier;
      const classId = modifierId + classGroupId;
      if (classGroupsInConflict.indexOf(classId) > -1) {
        continue;
      }
      classGroupsInConflict.push(classId);
      const conflictGroups = getConflictingClassGroupIds(classGroupId, hasPostfixModifier);
      for (let i13 = 0; i13 < conflictGroups.length; ++i13) {
        const group = conflictGroups[i13];
        classGroupsInConflict.push(modifierId + group);
      }
      result = originalClassName + (result.length > 0 ? " " + result : result);
    }
    return result;
  };
  var twJoin = (...classLists) => {
    let index3 = 0;
    let argument;
    let resolvedValue;
    let string = "";
    while (index3 < classLists.length) {
      if (argument = classLists[index3++]) {
        if (resolvedValue = toValue(argument)) {
          string && (string += " ");
          string += resolvedValue;
        }
      }
    }
    return string;
  };
  var toValue = (mix2) => {
    if (typeof mix2 === "string") {
      return mix2;
    }
    let resolvedValue;
    let string = "";
    for (let k10 = 0; k10 < mix2.length; k10++) {
      if (mix2[k10]) {
        if (resolvedValue = toValue(mix2[k10])) {
          string && (string += " ");
          string += resolvedValue;
        }
      }
    }
    return string;
  };
  var createTailwindMerge = (createConfigFirst, ...createConfigRest) => {
    let configUtils;
    let cacheGet;
    let cacheSet;
    let functionToCall;
    const initTailwindMerge = (classList) => {
      const config = createConfigRest.reduce((previousConfig, createConfigCurrent) => createConfigCurrent(previousConfig), createConfigFirst());
      configUtils = createConfigUtils(config);
      cacheGet = configUtils.cache.get;
      cacheSet = configUtils.cache.set;
      functionToCall = tailwindMerge;
      return tailwindMerge(classList);
    };
    const tailwindMerge = (classList) => {
      const cachedResult = cacheGet(classList);
      if (cachedResult) {
        return cachedResult;
      }
      const result = mergeClassList(classList, configUtils);
      cacheSet(classList, result);
      return result;
    };
    functionToCall = initTailwindMerge;
    return (...args) => functionToCall(twJoin(...args));
  };
  var fallbackThemeArr = [];
  var fromTheme = (key) => {
    const themeGetter = (theme) => theme[key] || fallbackThemeArr;
    themeGetter.isThemeGetter = true;
    return themeGetter;
  };
  var arbitraryValueRegex = /^\[(?:(\w[\w-]*):)?(.+)\]$/i;
  var arbitraryVariableRegex = /^\((?:(\w[\w-]*):)?(.+)\)$/i;
  var fractionRegex = /^\d+(?:\.\d+)?\/\d+(?:\.\d+)?$/;
  var tshirtUnitRegex = /^(\d+(\.\d+)?)?(xs|sm|md|lg|xl)$/;
  var lengthUnitRegex = /\d+(%|px|r?em|[sdl]?v([hwib]|min|max)|pt|pc|in|cm|mm|cap|ch|ex|r?lh|cq(w|h|i|b|min|max))|\b(calc|min|max|clamp)\(.+\)|^0$/;
  var colorFunctionRegex = /^(rgba?|hsla?|hwb|(ok)?(lab|lch)|color-mix)\(.+\)$/;
  var shadowRegex = /^(inset_)?-?((\d+)?\.?(\d+)[a-z]+|0)_-?((\d+)?\.?(\d+)[a-z]+|0)/;
  var imageRegex = /^(url|image|image-set|cross-fade|element|(repeating-)?(linear|radial|conic)-gradient)\(.+\)$/;
  var isFraction = (value) => fractionRegex.test(value);
  var isNumber = (value) => !!value && !Number.isNaN(Number(value));
  var isInteger = (value) => !!value && Number.isInteger(Number(value));
  var isPercent = (value) => value.endsWith("%") && isNumber(value.slice(0, -1));
  var isTshirtSize = (value) => tshirtUnitRegex.test(value);
  var isAny = () => true;
  var isLengthOnly = (value) => (
    // `colorFunctionRegex` check is necessary because color functions can have percentages in them which which would be incorrectly classified as lengths.
    // For example, `hsl(0 0% 0%)` would be classified as a length without this check.
    // I could also use lookbehind assertion in `lengthUnitRegex` but that isn't supported widely enough.
    lengthUnitRegex.test(value) && !colorFunctionRegex.test(value)
  );
  var isNever = () => false;
  var isShadow = (value) => shadowRegex.test(value);
  var isImage = (value) => imageRegex.test(value);
  var isAnyNonArbitrary = (value) => !isArbitraryValue(value) && !isArbitraryVariable(value);
  var isNamedContainerQuery = (value) => value.startsWith("@container") && (value[10] === "/" && value[11] !== void 0 || value[11] === "s" && value[16] !== void 0 && value.startsWith("-size/", 10) || value[11] === "n" && value[18] !== void 0 && value.startsWith("-normal/", 10));
  var isArbitrarySize = (value) => getIsArbitraryValue(value, isLabelSize, isNever);
  var isArbitraryValue = (value) => arbitraryValueRegex.test(value);
  var isArbitraryLength = (value) => getIsArbitraryValue(value, isLabelLength, isLengthOnly);
  var isArbitraryNumber = (value) => getIsArbitraryValue(value, isLabelNumber, isNumber);
  var isArbitraryWeight = (value) => getIsArbitraryValue(value, isLabelWeight, isAny);
  var isArbitraryFamilyName = (value) => getIsArbitraryValue(value, isLabelFamilyName, isNever);
  var isArbitraryPosition = (value) => getIsArbitraryValue(value, isLabelPosition, isNever);
  var isArbitraryImage = (value) => getIsArbitraryValue(value, isLabelImage, isImage);
  var isArbitraryShadow = (value) => getIsArbitraryValue(value, isLabelShadow, isShadow);
  var isArbitraryVariable = (value) => arbitraryVariableRegex.test(value);
  var isArbitraryVariableLength = (value) => getIsArbitraryVariable(value, isLabelLength);
  var isArbitraryVariableFamilyName = (value) => getIsArbitraryVariable(value, isLabelFamilyName);
  var isArbitraryVariablePosition = (value) => getIsArbitraryVariable(value, isLabelPosition);
  var isArbitraryVariableSize = (value) => getIsArbitraryVariable(value, isLabelSize);
  var isArbitraryVariableImage = (value) => getIsArbitraryVariable(value, isLabelImage);
  var isArbitraryVariableShadow = (value) => getIsArbitraryVariable(value, isLabelShadow, true);
  var isArbitraryVariableWeight = (value) => getIsArbitraryVariable(value, isLabelWeight, true);
  var getIsArbitraryValue = (value, testLabel, testValue) => {
    const result = arbitraryValueRegex.exec(value);
    if (result) {
      if (result[1]) {
        return testLabel(result[1]);
      }
      return testValue(result[2]);
    }
    return false;
  };
  var getIsArbitraryVariable = (value, testLabel, shouldMatchNoLabel = false) => {
    const result = arbitraryVariableRegex.exec(value);
    if (result) {
      if (result[1]) {
        return testLabel(result[1]);
      }
      return shouldMatchNoLabel;
    }
    return false;
  };
  var isLabelPosition = (label) => label === "position" || label === "percentage";
  var isLabelImage = (label) => label === "image" || label === "url";
  var isLabelSize = (label) => label === "length" || label === "size" || label === "bg-size";
  var isLabelLength = (label) => label === "length";
  var isLabelNumber = (label) => label === "number";
  var isLabelFamilyName = (label) => label === "family-name";
  var isLabelWeight = (label) => label === "number" || label === "weight";
  var isLabelShadow = (label) => label === "shadow";
  var getDefaultConfig = () => {
    const themeColor = fromTheme("color");
    const themeFont = fromTheme("font");
    const themeText = fromTheme("text");
    const themeFontWeight = fromTheme("font-weight");
    const themeTracking = fromTheme("tracking");
    const themeLeading = fromTheme("leading");
    const themeBreakpoint = fromTheme("breakpoint");
    const themeContainer = fromTheme("container");
    const themeSpacing = fromTheme("spacing");
    const themeRadius = fromTheme("radius");
    const themeShadow = fromTheme("shadow");
    const themeInsetShadow = fromTheme("inset-shadow");
    const themeTextShadow = fromTheme("text-shadow");
    const themeDropShadow = fromTheme("drop-shadow");
    const themeBlur = fromTheme("blur");
    const themePerspective = fromTheme("perspective");
    const themeAspect = fromTheme("aspect");
    const themeEase = fromTheme("ease");
    const themeAnimate = fromTheme("animate");
    const scaleBreak = () => ["auto", "avoid", "all", "avoid-page", "page", "left", "right", "column"];
    const scalePosition = () => [
      "center",
      "top",
      "bottom",
      "left",
      "right",
      "top-left",
      // Deprecated since Tailwind CSS v4.1.0, see https://github.com/tailwindlabs/tailwindcss/pull/17378
      "left-top",
      "top-right",
      // Deprecated since Tailwind CSS v4.1.0, see https://github.com/tailwindlabs/tailwindcss/pull/17378
      "right-top",
      "bottom-right",
      // Deprecated since Tailwind CSS v4.1.0, see https://github.com/tailwindlabs/tailwindcss/pull/17378
      "right-bottom",
      "bottom-left",
      // Deprecated since Tailwind CSS v4.1.0, see https://github.com/tailwindlabs/tailwindcss/pull/17378
      "left-bottom"
    ];
    const scalePositionWithArbitrary = () => [...scalePosition(), isArbitraryVariable, isArbitraryValue];
    const scaleOverflow = () => ["auto", "hidden", "clip", "visible", "scroll"];
    const scaleOverscroll = () => ["auto", "contain", "none"];
    const scaleUnambiguousSpacing = () => [isArbitraryVariable, isArbitraryValue, themeSpacing];
    const scaleInset = () => [isFraction, "full", "auto", ...scaleUnambiguousSpacing()];
    const scaleGridTemplateColsRows = () => [isInteger, "none", "subgrid", isArbitraryVariable, isArbitraryValue];
    const scaleGridColRowStartAndEnd = () => ["auto", {
      span: ["full", isInteger, isArbitraryVariable, isArbitraryValue]
    }, isInteger, isArbitraryVariable, isArbitraryValue];
    const scaleGridColRowStartOrEnd = () => [isInteger, "auto", isArbitraryVariable, isArbitraryValue];
    const scaleGridAutoColsRows = () => ["auto", "min", "max", "fr", isArbitraryVariable, isArbitraryValue];
    const scaleAlignPrimaryAxis = () => ["start", "end", "center", "between", "around", "evenly", "stretch", "baseline", "center-safe", "end-safe"];
    const scaleAlignSecondaryAxis = () => ["start", "end", "center", "stretch", "center-safe", "end-safe"];
    const scaleMargin = () => ["auto", ...scaleUnambiguousSpacing()];
    const scaleSizing = () => [isFraction, "auto", "full", "dvw", "dvh", "lvw", "lvh", "svw", "svh", "min", "max", "fit", ...scaleUnambiguousSpacing()];
    const scaleSizingInline = () => [isFraction, "screen", "full", "dvw", "lvw", "svw", "min", "max", "fit", ...scaleUnambiguousSpacing()];
    const scaleSizingBlock = () => [isFraction, "screen", "full", "lh", "dvh", "lvh", "svh", "min", "max", "fit", ...scaleUnambiguousSpacing()];
    const scaleColor = () => [themeColor, isArbitraryVariable, isArbitraryValue];
    const scaleBgPosition = () => [...scalePosition(), isArbitraryVariablePosition, isArbitraryPosition, {
      position: [isArbitraryVariable, isArbitraryValue]
    }];
    const scaleBgRepeat = () => ["no-repeat", {
      repeat: ["", "x", "y", "space", "round"]
    }];
    const scaleBgSize = () => ["auto", "cover", "contain", isArbitraryVariableSize, isArbitrarySize, {
      size: [isArbitraryVariable, isArbitraryValue]
    }];
    const scaleGradientStopPosition = () => [isPercent, isArbitraryVariableLength, isArbitraryLength];
    const scaleRadius = () => [
      // Deprecated since Tailwind CSS v4.0.0
      "",
      "none",
      "full",
      themeRadius,
      isArbitraryVariable,
      isArbitraryValue
    ];
    const scaleBorderWidth = () => ["", isNumber, isArbitraryVariableLength, isArbitraryLength];
    const scaleLineStyle = () => ["solid", "dashed", "dotted", "double"];
    const scaleBlendMode = () => ["normal", "multiply", "screen", "overlay", "darken", "lighten", "color-dodge", "color-burn", "hard-light", "soft-light", "difference", "exclusion", "hue", "saturation", "color", "luminosity"];
    const scaleMaskImagePosition = () => [isNumber, isPercent, isArbitraryVariablePosition, isArbitraryPosition];
    const scaleBlur = () => [
      // Deprecated since Tailwind CSS v4.0.0
      "",
      "none",
      themeBlur,
      isArbitraryVariable,
      isArbitraryValue
    ];
    const scaleRotate = () => ["none", isNumber, isArbitraryVariable, isArbitraryValue];
    const scaleScale = () => ["none", isNumber, isArbitraryVariable, isArbitraryValue];
    const scaleSkew = () => [isNumber, isArbitraryVariable, isArbitraryValue];
    const scaleTranslate = () => [isFraction, "full", ...scaleUnambiguousSpacing()];
    return {
      cacheSize: 500,
      theme: {
        animate: ["spin", "ping", "pulse", "bounce"],
        aspect: ["video"],
        blur: [isTshirtSize],
        breakpoint: [isTshirtSize],
        color: [isAny],
        container: [isTshirtSize],
        "drop-shadow": [isTshirtSize],
        ease: ["in", "out", "in-out"],
        font: [isAnyNonArbitrary],
        "font-weight": ["thin", "extralight", "light", "normal", "medium", "semibold", "bold", "extrabold", "black"],
        "inset-shadow": [isTshirtSize],
        leading: ["none", "tight", "snug", "normal", "relaxed", "loose"],
        perspective: ["dramatic", "near", "normal", "midrange", "distant", "none"],
        radius: [isTshirtSize],
        shadow: [isTshirtSize],
        spacing: ["px", isNumber],
        text: [isTshirtSize],
        "text-shadow": [isTshirtSize],
        tracking: ["tighter", "tight", "normal", "wide", "wider", "widest"]
      },
      classGroups: {
        // --------------
        // --- Layout ---
        // --------------
        /**
         * Aspect Ratio
         * @see https://tailwindcss.com/docs/aspect-ratio
         */
        aspect: [{
          aspect: ["auto", "square", isFraction, isArbitraryValue, isArbitraryVariable, themeAspect]
        }],
        /**
         * Container
         * @see https://tailwindcss.com/docs/container
         * @deprecated since Tailwind CSS v4.0.0
         */
        container: ["container"],
        /**
         * Container Type
         * @see https://tailwindcss.com/docs/responsive-design#container-queries
         */
        "container-type": [{
          "@container": ["", "normal", "size", isArbitraryVariable, isArbitraryValue]
        }],
        /**
         * Container Name
         * @see https://tailwindcss.com/docs/responsive-design#named-containers
         */
        "container-named": [isNamedContainerQuery],
        /**
         * Columns
         * @see https://tailwindcss.com/docs/columns
         */
        columns: [{
          columns: [isNumber, isArbitraryValue, isArbitraryVariable, themeContainer]
        }],
        /**
         * Break After
         * @see https://tailwindcss.com/docs/break-after
         */
        "break-after": [{
          "break-after": scaleBreak()
        }],
        /**
         * Break Before
         * @see https://tailwindcss.com/docs/break-before
         */
        "break-before": [{
          "break-before": scaleBreak()
        }],
        /**
         * Break Inside
         * @see https://tailwindcss.com/docs/break-inside
         */
        "break-inside": [{
          "break-inside": ["auto", "avoid", "avoid-page", "avoid-column"]
        }],
        /**
         * Box Decoration Break
         * @see https://tailwindcss.com/docs/box-decoration-break
         */
        "box-decoration": [{
          "box-decoration": ["slice", "clone"]
        }],
        /**
         * Box Sizing
         * @see https://tailwindcss.com/docs/box-sizing
         */
        box: [{
          box: ["border", "content"]
        }],
        /**
         * Display
         * @see https://tailwindcss.com/docs/display
         */
        display: ["block", "inline-block", "inline", "flex", "inline-flex", "table", "inline-table", "table-caption", "table-cell", "table-column", "table-column-group", "table-footer-group", "table-header-group", "table-row-group", "table-row", "flow-root", "grid", "inline-grid", "contents", "list-item", "hidden"],
        /**
         * Screen Reader Only
         * @see https://tailwindcss.com/docs/display#screen-reader-only
         */
        sr: ["sr-only", "not-sr-only"],
        /**
         * Floats
         * @see https://tailwindcss.com/docs/float
         */
        float: [{
          float: ["right", "left", "none", "start", "end"]
        }],
        /**
         * Clear
         * @see https://tailwindcss.com/docs/clear
         */
        clear: [{
          clear: ["left", "right", "both", "none", "start", "end"]
        }],
        /**
         * Isolation
         * @see https://tailwindcss.com/docs/isolation
         */
        isolation: ["isolate", "isolation-auto"],
        /**
         * Object Fit
         * @see https://tailwindcss.com/docs/object-fit
         */
        "object-fit": [{
          object: ["contain", "cover", "fill", "none", "scale-down"]
        }],
        /**
         * Object Position
         * @see https://tailwindcss.com/docs/object-position
         */
        "object-position": [{
          object: scalePositionWithArbitrary()
        }],
        /**
         * Overflow
         * @see https://tailwindcss.com/docs/overflow
         */
        overflow: [{
          overflow: scaleOverflow()
        }],
        /**
         * Overflow X
         * @see https://tailwindcss.com/docs/overflow
         */
        "overflow-x": [{
          "overflow-x": scaleOverflow()
        }],
        /**
         * Overflow Y
         * @see https://tailwindcss.com/docs/overflow
         */
        "overflow-y": [{
          "overflow-y": scaleOverflow()
        }],
        /**
         * Overscroll Behavior
         * @see https://tailwindcss.com/docs/overscroll-behavior
         */
        overscroll: [{
          overscroll: scaleOverscroll()
        }],
        /**
         * Overscroll Behavior X
         * @see https://tailwindcss.com/docs/overscroll-behavior
         */
        "overscroll-x": [{
          "overscroll-x": scaleOverscroll()
        }],
        /**
         * Overscroll Behavior Y
         * @see https://tailwindcss.com/docs/overscroll-behavior
         */
        "overscroll-y": [{
          "overscroll-y": scaleOverscroll()
        }],
        /**
         * Position
         * @see https://tailwindcss.com/docs/position
         */
        position: ["static", "fixed", "absolute", "relative", "sticky"],
        /**
         * Inset
         * @see https://tailwindcss.com/docs/top-right-bottom-left
         */
        inset: [{
          inset: scaleInset()
        }],
        /**
         * Inset Inline
         * @see https://tailwindcss.com/docs/top-right-bottom-left
         */
        "inset-x": [{
          "inset-x": scaleInset()
        }],
        /**
         * Inset Block
         * @see https://tailwindcss.com/docs/top-right-bottom-left
         */
        "inset-y": [{
          "inset-y": scaleInset()
        }],
        /**
         * Inset Inline Start
         * @see https://tailwindcss.com/docs/top-right-bottom-left
         * @todo class group will be renamed to `inset-s` in next major release
         */
        start: [{
          "inset-s": scaleInset(),
          /**
           * @deprecated since Tailwind CSS v4.2.0 in favor of `inset-s-*` utilities.
           * @see https://github.com/tailwindlabs/tailwindcss/pull/19613
           */
          start: scaleInset()
        }],
        /**
         * Inset Inline End
         * @see https://tailwindcss.com/docs/top-right-bottom-left
         * @todo class group will be renamed to `inset-e` in next major release
         */
        end: [{
          "inset-e": scaleInset(),
          /**
           * @deprecated since Tailwind CSS v4.2.0 in favor of `inset-e-*` utilities.
           * @see https://github.com/tailwindlabs/tailwindcss/pull/19613
           */
          end: scaleInset()
        }],
        /**
         * Inset Block Start
         * @see https://tailwindcss.com/docs/top-right-bottom-left
         */
        "inset-bs": [{
          "inset-bs": scaleInset()
        }],
        /**
         * Inset Block End
         * @see https://tailwindcss.com/docs/top-right-bottom-left
         */
        "inset-be": [{
          "inset-be": scaleInset()
        }],
        /**
         * Top
         * @see https://tailwindcss.com/docs/top-right-bottom-left
         */
        top: [{
          top: scaleInset()
        }],
        /**
         * Right
         * @see https://tailwindcss.com/docs/top-right-bottom-left
         */
        right: [{
          right: scaleInset()
        }],
        /**
         * Bottom
         * @see https://tailwindcss.com/docs/top-right-bottom-left
         */
        bottom: [{
          bottom: scaleInset()
        }],
        /**
         * Left
         * @see https://tailwindcss.com/docs/top-right-bottom-left
         */
        left: [{
          left: scaleInset()
        }],
        /**
         * Visibility
         * @see https://tailwindcss.com/docs/visibility
         */
        visibility: ["visible", "invisible", "collapse"],
        /**
         * Z-Index
         * @see https://tailwindcss.com/docs/z-index
         */
        z: [{
          z: [isInteger, "auto", isArbitraryVariable, isArbitraryValue]
        }],
        // ------------------------
        // --- Flexbox and Grid ---
        // ------------------------
        /**
         * Flex Basis
         * @see https://tailwindcss.com/docs/flex-basis
         */
        basis: [{
          basis: [isFraction, "full", "auto", themeContainer, ...scaleUnambiguousSpacing()]
        }],
        /**
         * Flex Direction
         * @see https://tailwindcss.com/docs/flex-direction
         */
        "flex-direction": [{
          flex: ["row", "row-reverse", "col", "col-reverse"]
        }],
        /**
         * Flex Wrap
         * @see https://tailwindcss.com/docs/flex-wrap
         */
        "flex-wrap": [{
          flex: ["nowrap", "wrap", "wrap-reverse"]
        }],
        /**
         * Flex
         * @see https://tailwindcss.com/docs/flex
         */
        flex: [{
          flex: [isNumber, isFraction, "auto", "initial", "none", isArbitraryValue]
        }],
        /**
         * Flex Grow
         * @see https://tailwindcss.com/docs/flex-grow
         */
        grow: [{
          grow: ["", isNumber, isArbitraryVariable, isArbitraryValue]
        }],
        /**
         * Flex Shrink
         * @see https://tailwindcss.com/docs/flex-shrink
         */
        shrink: [{
          shrink: ["", isNumber, isArbitraryVariable, isArbitraryValue]
        }],
        /**
         * Order
         * @see https://tailwindcss.com/docs/order
         */
        order: [{
          order: [isInteger, "first", "last", "none", isArbitraryVariable, isArbitraryValue]
        }],
        /**
         * Grid Template Columns
         * @see https://tailwindcss.com/docs/grid-template-columns
         */
        "grid-cols": [{
          "grid-cols": scaleGridTemplateColsRows()
        }],
        /**
         * Grid Column Start / End
         * @see https://tailwindcss.com/docs/grid-column
         */
        "col-start-end": [{
          col: scaleGridColRowStartAndEnd()
        }],
        /**
         * Grid Column Start
         * @see https://tailwindcss.com/docs/grid-column
         */
        "col-start": [{
          "col-start": scaleGridColRowStartOrEnd()
        }],
        /**
         * Grid Column End
         * @see https://tailwindcss.com/docs/grid-column
         */
        "col-end": [{
          "col-end": scaleGridColRowStartOrEnd()
        }],
        /**
         * Grid Template Rows
         * @see https://tailwindcss.com/docs/grid-template-rows
         */
        "grid-rows": [{
          "grid-rows": scaleGridTemplateColsRows()
        }],
        /**
         * Grid Row Start / End
         * @see https://tailwindcss.com/docs/grid-row
         */
        "row-start-end": [{
          row: scaleGridColRowStartAndEnd()
        }],
        /**
         * Grid Row Start
         * @see https://tailwindcss.com/docs/grid-row
         */
        "row-start": [{
          "row-start": scaleGridColRowStartOrEnd()
        }],
        /**
         * Grid Row End
         * @see https://tailwindcss.com/docs/grid-row
         */
        "row-end": [{
          "row-end": scaleGridColRowStartOrEnd()
        }],
        /**
         * Grid Auto Flow
         * @see https://tailwindcss.com/docs/grid-auto-flow
         */
        "grid-flow": [{
          "grid-flow": ["row", "col", "dense", "row-dense", "col-dense"]
        }],
        /**
         * Grid Auto Columns
         * @see https://tailwindcss.com/docs/grid-auto-columns
         */
        "auto-cols": [{
          "auto-cols": scaleGridAutoColsRows()
        }],
        /**
         * Grid Auto Rows
         * @see https://tailwindcss.com/docs/grid-auto-rows
         */
        "auto-rows": [{
          "auto-rows": scaleGridAutoColsRows()
        }],
        /**
         * Gap
         * @see https://tailwindcss.com/docs/gap
         */
        gap: [{
          gap: scaleUnambiguousSpacing()
        }],
        /**
         * Gap X
         * @see https://tailwindcss.com/docs/gap
         */
        "gap-x": [{
          "gap-x": scaleUnambiguousSpacing()
        }],
        /**
         * Gap Y
         * @see https://tailwindcss.com/docs/gap
         */
        "gap-y": [{
          "gap-y": scaleUnambiguousSpacing()
        }],
        /**
         * Justify Content
         * @see https://tailwindcss.com/docs/justify-content
         */
        "justify-content": [{
          justify: [...scaleAlignPrimaryAxis(), "normal"]
        }],
        /**
         * Justify Items
         * @see https://tailwindcss.com/docs/justify-items
         */
        "justify-items": [{
          "justify-items": [...scaleAlignSecondaryAxis(), "normal"]
        }],
        /**
         * Justify Self
         * @see https://tailwindcss.com/docs/justify-self
         */
        "justify-self": [{
          "justify-self": ["auto", ...scaleAlignSecondaryAxis()]
        }],
        /**
         * Align Content
         * @see https://tailwindcss.com/docs/align-content
         */
        "align-content": [{
          content: ["normal", ...scaleAlignPrimaryAxis()]
        }],
        /**
         * Align Items
         * @see https://tailwindcss.com/docs/align-items
         */
        "align-items": [{
          items: [...scaleAlignSecondaryAxis(), {
            baseline: ["", "last"]
          }]
        }],
        /**
         * Align Self
         * @see https://tailwindcss.com/docs/align-self
         */
        "align-self": [{
          self: ["auto", ...scaleAlignSecondaryAxis(), {
            baseline: ["", "last"]
          }]
        }],
        /**
         * Place Content
         * @see https://tailwindcss.com/docs/place-content
         */
        "place-content": [{
          "place-content": scaleAlignPrimaryAxis()
        }],
        /**
         * Place Items
         * @see https://tailwindcss.com/docs/place-items
         */
        "place-items": [{
          "place-items": [...scaleAlignSecondaryAxis(), "baseline"]
        }],
        /**
         * Place Self
         * @see https://tailwindcss.com/docs/place-self
         */
        "place-self": [{
          "place-self": ["auto", ...scaleAlignSecondaryAxis()]
        }],
        // Spacing
        /**
         * Padding
         * @see https://tailwindcss.com/docs/padding
         */
        p: [{
          p: scaleUnambiguousSpacing()
        }],
        /**
         * Padding Inline
         * @see https://tailwindcss.com/docs/padding
         */
        px: [{
          px: scaleUnambiguousSpacing()
        }],
        /**
         * Padding Block
         * @see https://tailwindcss.com/docs/padding
         */
        py: [{
          py: scaleUnambiguousSpacing()
        }],
        /**
         * Padding Inline Start
         * @see https://tailwindcss.com/docs/padding
         */
        ps: [{
          ps: scaleUnambiguousSpacing()
        }],
        /**
         * Padding Inline End
         * @see https://tailwindcss.com/docs/padding
         */
        pe: [{
          pe: scaleUnambiguousSpacing()
        }],
        /**
         * Padding Block Start
         * @see https://tailwindcss.com/docs/padding
         */
        pbs: [{
          pbs: scaleUnambiguousSpacing()
        }],
        /**
         * Padding Block End
         * @see https://tailwindcss.com/docs/padding
         */
        pbe: [{
          pbe: scaleUnambiguousSpacing()
        }],
        /**
         * Padding Top
         * @see https://tailwindcss.com/docs/padding
         */
        pt: [{
          pt: scaleUnambiguousSpacing()
        }],
        /**
         * Padding Right
         * @see https://tailwindcss.com/docs/padding
         */
        pr: [{
          pr: scaleUnambiguousSpacing()
        }],
        /**
         * Padding Bottom
         * @see https://tailwindcss.com/docs/padding
         */
        pb: [{
          pb: scaleUnambiguousSpacing()
        }],
        /**
         * Padding Left
         * @see https://tailwindcss.com/docs/padding
         */
        pl: [{
          pl: scaleUnambiguousSpacing()
        }],
        /**
         * Margin
         * @see https://tailwindcss.com/docs/margin
         */
        m: [{
          m: scaleMargin()
        }],
        /**
         * Margin Inline
         * @see https://tailwindcss.com/docs/margin
         */
        mx: [{
          mx: scaleMargin()
        }],
        /**
         * Margin Block
         * @see https://tailwindcss.com/docs/margin
         */
        my: [{
          my: scaleMargin()
        }],
        /**
         * Margin Inline Start
         * @see https://tailwindcss.com/docs/margin
         */
        ms: [{
          ms: scaleMargin()
        }],
        /**
         * Margin Inline End
         * @see https://tailwindcss.com/docs/margin
         */
        me: [{
          me: scaleMargin()
        }],
        /**
         * Margin Block Start
         * @see https://tailwindcss.com/docs/margin
         */
        mbs: [{
          mbs: scaleMargin()
        }],
        /**
         * Margin Block End
         * @see https://tailwindcss.com/docs/margin
         */
        mbe: [{
          mbe: scaleMargin()
        }],
        /**
         * Margin Top
         * @see https://tailwindcss.com/docs/margin
         */
        mt: [{
          mt: scaleMargin()
        }],
        /**
         * Margin Right
         * @see https://tailwindcss.com/docs/margin
         */
        mr: [{
          mr: scaleMargin()
        }],
        /**
         * Margin Bottom
         * @see https://tailwindcss.com/docs/margin
         */
        mb: [{
          mb: scaleMargin()
        }],
        /**
         * Margin Left
         * @see https://tailwindcss.com/docs/margin
         */
        ml: [{
          ml: scaleMargin()
        }],
        /**
         * Space Between X
         * @see https://tailwindcss.com/docs/margin#adding-space-between-children
         */
        "space-x": [{
          "space-x": scaleUnambiguousSpacing()
        }],
        /**
         * Space Between X Reverse
         * @see https://tailwindcss.com/docs/margin#adding-space-between-children
         */
        "space-x-reverse": ["space-x-reverse"],
        /**
         * Space Between Y
         * @see https://tailwindcss.com/docs/margin#adding-space-between-children
         */
        "space-y": [{
          "space-y": scaleUnambiguousSpacing()
        }],
        /**
         * Space Between Y Reverse
         * @see https://tailwindcss.com/docs/margin#adding-space-between-children
         */
        "space-y-reverse": ["space-y-reverse"],
        // --------------
        // --- Sizing ---
        // --------------
        /**
         * Size
         * @see https://tailwindcss.com/docs/width#setting-both-width-and-height
         */
        size: [{
          size: scaleSizing()
        }],
        /**
         * Inline Size
         * @see https://tailwindcss.com/docs/width
         */
        "inline-size": [{
          inline: ["auto", ...scaleSizingInline()]
        }],
        /**
         * Min-Inline Size
         * @see https://tailwindcss.com/docs/min-width
         */
        "min-inline-size": [{
          "min-inline": ["auto", ...scaleSizingInline()]
        }],
        /**
         * Max-Inline Size
         * @see https://tailwindcss.com/docs/max-width
         */
        "max-inline-size": [{
          "max-inline": ["none", ...scaleSizingInline()]
        }],
        /**
         * Block Size
         * @see https://tailwindcss.com/docs/height
         */
        "block-size": [{
          block: ["auto", ...scaleSizingBlock()]
        }],
        /**
         * Min-Block Size
         * @see https://tailwindcss.com/docs/min-height
         */
        "min-block-size": [{
          "min-block": ["auto", ...scaleSizingBlock()]
        }],
        /**
         * Max-Block Size
         * @see https://tailwindcss.com/docs/max-height
         */
        "max-block-size": [{
          "max-block": ["none", ...scaleSizingBlock()]
        }],
        /**
         * Width
         * @see https://tailwindcss.com/docs/width
         */
        w: [{
          w: [themeContainer, "screen", ...scaleSizing()]
        }],
        /**
         * Min-Width
         * @see https://tailwindcss.com/docs/min-width
         */
        "min-w": [{
          "min-w": [
            themeContainer,
            "screen",
            /** Deprecated. @see https://github.com/tailwindlabs/tailwindcss.com/issues/2027#issuecomment-2620152757 */
            "none",
            ...scaleSizing()
          ]
        }],
        /**
         * Max-Width
         * @see https://tailwindcss.com/docs/max-width
         */
        "max-w": [{
          "max-w": [
            themeContainer,
            "screen",
            "none",
            /** Deprecated since Tailwind CSS v4.0.0. @see https://github.com/tailwindlabs/tailwindcss.com/issues/2027#issuecomment-2620152757 */
            "prose",
            /** Deprecated since Tailwind CSS v4.0.0. @see https://github.com/tailwindlabs/tailwindcss.com/issues/2027#issuecomment-2620152757 */
            {
              screen: [themeBreakpoint]
            },
            ...scaleSizing()
          ]
        }],
        /**
         * Height
         * @see https://tailwindcss.com/docs/height
         */
        h: [{
          h: ["screen", "lh", ...scaleSizing()]
        }],
        /**
         * Min-Height
         * @see https://tailwindcss.com/docs/min-height
         */
        "min-h": [{
          "min-h": ["screen", "lh", "none", ...scaleSizing()]
        }],
        /**
         * Max-Height
         * @see https://tailwindcss.com/docs/max-height
         */
        "max-h": [{
          "max-h": ["screen", "lh", ...scaleSizing()]
        }],
        // ------------------
        // --- Typography ---
        // ------------------
        /**
         * Font Size
         * @see https://tailwindcss.com/docs/font-size
         */
        "font-size": [{
          text: ["base", themeText, isArbitraryVariableLength, isArbitraryLength]
        }],
        /**
         * Font Smoothing
         * @see https://tailwindcss.com/docs/font-smoothing
         */
        "font-smoothing": ["antialiased", "subpixel-antialiased"],
        /**
         * Font Style
         * @see https://tailwindcss.com/docs/font-style
         */
        "font-style": ["italic", "not-italic"],
        /**
         * Font Weight
         * @see https://tailwindcss.com/docs/font-weight
         */
        "font-weight": [{
          font: [themeFontWeight, isArbitraryVariableWeight, isArbitraryWeight]
        }],
        /**
         * Font Stretch
         * @see https://tailwindcss.com/docs/font-stretch
         */
        "font-stretch": [{
          "font-stretch": ["ultra-condensed", "extra-condensed", "condensed", "semi-condensed", "normal", "semi-expanded", "expanded", "extra-expanded", "ultra-expanded", isPercent, isArbitraryValue]
        }],
        /**
         * Font Family
         * @see https://tailwindcss.com/docs/font-family
         */
        "font-family": [{
          font: [isArbitraryVariableFamilyName, isArbitraryFamilyName, themeFont]
        }],
        /**
         * Font Feature Settings
         * @see https://tailwindcss.com/docs/font-feature-settings
         */
        "font-features": [{
          "font-features": [isArbitraryValue]
        }],
        /**
         * Font Variant Numeric
         * @see https://tailwindcss.com/docs/font-variant-numeric
         */
        "fvn-normal": ["normal-nums"],
        /**
         * Font Variant Numeric
         * @see https://tailwindcss.com/docs/font-variant-numeric
         */
        "fvn-ordinal": ["ordinal"],
        /**
         * Font Variant Numeric
         * @see https://tailwindcss.com/docs/font-variant-numeric
         */
        "fvn-slashed-zero": ["slashed-zero"],
        /**
         * Font Variant Numeric
         * @see https://tailwindcss.com/docs/font-variant-numeric
         */
        "fvn-figure": ["lining-nums", "oldstyle-nums"],
        /**
         * Font Variant Numeric
         * @see https://tailwindcss.com/docs/font-variant-numeric
         */
        "fvn-spacing": ["proportional-nums", "tabular-nums"],
        /**
         * Font Variant Numeric
         * @see https://tailwindcss.com/docs/font-variant-numeric
         */
        "fvn-fraction": ["diagonal-fractions", "stacked-fractions"],
        /**
         * Letter Spacing
         * @see https://tailwindcss.com/docs/letter-spacing
         */
        tracking: [{
          tracking: [themeTracking, isArbitraryVariable, isArbitraryValue]
        }],
        /**
         * Line Clamp
         * @see https://tailwindcss.com/docs/line-clamp
         */
        "line-clamp": [{
          "line-clamp": [isNumber, "none", isArbitraryVariable, isArbitraryNumber]
        }],
        /**
         * Line Height
         * @see https://tailwindcss.com/docs/line-height
         */
        leading: [{
          leading: [
            /** Deprecated since Tailwind CSS v4.0.0. @see https://github.com/tailwindlabs/tailwindcss.com/issues/2027#issuecomment-2620152757 */
            themeLeading,
            ...scaleUnambiguousSpacing()
          ]
        }],
        /**
         * List Style Image
         * @see https://tailwindcss.com/docs/list-style-image
         */
        "list-image": [{
          "list-image": ["none", isArbitraryVariable, isArbitraryValue]
        }],
        /**
         * List Style Position
         * @see https://tailwindcss.com/docs/list-style-position
         */
        "list-style-position": [{
          list: ["inside", "outside"]
        }],
        /**
         * List Style Type
         * @see https://tailwindcss.com/docs/list-style-type
         */
        "list-style-type": [{
          list: ["disc", "decimal", "none", isArbitraryVariable, isArbitraryValue]
        }],
        /**
         * Text Alignment
         * @see https://tailwindcss.com/docs/text-align
         */
        "text-alignment": [{
          text: ["left", "center", "right", "justify", "start", "end"]
        }],
        /**
         * Placeholder Color
         * @deprecated since Tailwind CSS v3.0.0
         * @see https://v3.tailwindcss.com/docs/placeholder-color
         */
        "placeholder-color": [{
          placeholder: scaleColor()
        }],
        /**
         * Text Color
         * @see https://tailwindcss.com/docs/text-color
         */
        "text-color": [{
          text: scaleColor()
        }],
        /**
         * Text Decoration
         * @see https://tailwindcss.com/docs/text-decoration
         */
        "text-decoration": ["underline", "overline", "line-through", "no-underline"],
        /**
         * Text Decoration Style
         * @see https://tailwindcss.com/docs/text-decoration-style
         */
        "text-decoration-style": [{
          decoration: [...scaleLineStyle(), "wavy"]
        }],
        /**
         * Text Decoration Thickness
         * @see https://tailwindcss.com/docs/text-decoration-thickness
         */
        "text-decoration-thickness": [{
          decoration: [isNumber, "from-font", "auto", isArbitraryVariable, isArbitraryLength]
        }],
        /**
         * Text Decoration Color
         * @see https://tailwindcss.com/docs/text-decoration-color
         */
        "text-decoration-color": [{
          decoration: scaleColor()
        }],
        /**
         * Text Underline Offset
         * @see https://tailwindcss.com/docs/text-underline-offset
         */
        "underline-offset": [{
          "underline-offset": [isNumber, "auto", isArbitraryVariable, isArbitraryValue]
        }],
        /**
         * Text Transform
         * @see https://tailwindcss.com/docs/text-transform
         */
        "text-transform": ["uppercase", "lowercase", "capitalize", "normal-case"],
        /**
         * Text Overflow
         * @see https://tailwindcss.com/docs/text-overflow
         */
        "text-overflow": ["truncate", "text-ellipsis", "text-clip"],
        /**
         * Text Wrap
         * @see https://tailwindcss.com/docs/text-wrap
         */
        "text-wrap": [{
          text: ["wrap", "nowrap", "balance", "pretty"]
        }],
        /**
         * Text Indent
         * @see https://tailwindcss.com/docs/text-indent
         */
        indent: [{
          indent: scaleUnambiguousSpacing()
        }],
        /**
         * Tab Size
         * @see https://tailwindcss.com/docs/tab-size
         */
        "tab-size": [{
          tab: [isInteger, isArbitraryVariable, isArbitraryValue]
        }],
        /**
         * Vertical Alignment
         * @see https://tailwindcss.com/docs/vertical-align
         */
        "vertical-align": [{
          align: ["baseline", "top", "middle", "bottom", "text-top", "text-bottom", "sub", "super", isArbitraryVariable, isArbitraryValue]
        }],
        /**
         * Whitespace
         * @see https://tailwindcss.com/docs/whitespace
         */
        whitespace: [{
          whitespace: ["normal", "nowrap", "pre", "pre-line", "pre-wrap", "break-spaces"]
        }],
        /**
         * Word Break
         * @see https://tailwindcss.com/docs/word-break
         */
        break: [{
          break: ["normal", "words", "all", "keep"]
        }],
        /**
         * Overflow Wrap
         * @see https://tailwindcss.com/docs/overflow-wrap
         */
        wrap: [{
          wrap: ["break-word", "anywhere", "normal"]
        }],
        /**
         * Hyphens
         * @see https://tailwindcss.com/docs/hyphens
         */
        hyphens: [{
          hyphens: ["none", "manual", "auto"]
        }],
        /**
         * Content
         * @see https://tailwindcss.com/docs/content
         */
        content: [{
          content: ["none", isArbitraryVariable, isArbitraryValue]
        }],
        // -------------------
        // --- Backgrounds ---
        // -------------------
        /**
         * Background Attachment
         * @see https://tailwindcss.com/docs/background-attachment
         */
        "bg-attachment": [{
          bg: ["fixed", "local", "scroll"]
        }],
        /**
         * Background Clip
         * @see https://tailwindcss.com/docs/background-clip
         */
        "bg-clip": [{
          "bg-clip": ["border", "padding", "content", "text"]
        }],
        /**
         * Background Origin
         * @see https://tailwindcss.com/docs/background-origin
         */
        "bg-origin": [{
          "bg-origin": ["border", "padding", "content"]
        }],
        /**
         * Background Position
         * @see https://tailwindcss.com/docs/background-position
         */
        "bg-position": [{
          bg: scaleBgPosition()
        }],
        /**
         * Background Repeat
         * @see https://tailwindcss.com/docs/background-repeat
         */
        "bg-repeat": [{
          bg: scaleBgRepeat()
        }],
        /**
         * Background Size
         * @see https://tailwindcss.com/docs/background-size
         */
        "bg-size": [{
          bg: scaleBgSize()
        }],
        /**
         * Background Image
         * @see https://tailwindcss.com/docs/background-image
         */
        "bg-image": [{
          bg: ["none", {
            linear: [{
              to: ["t", "tr", "r", "br", "b", "bl", "l", "tl"]
            }, isInteger, isArbitraryVariable, isArbitraryValue],
            radial: ["", isArbitraryVariable, isArbitraryValue],
            conic: [isInteger, isArbitraryVariable, isArbitraryValue]
          }, isArbitraryVariableImage, isArbitraryImage]
        }],
        /**
         * Background Color
         * @see https://tailwindcss.com/docs/background-color
         */
        "bg-color": [{
          bg: scaleColor()
        }],
        /**
         * Gradient Color Stops From Position
         * @see https://tailwindcss.com/docs/gradient-color-stops
         */
        "gradient-from-pos": [{
          from: scaleGradientStopPosition()
        }],
        /**
         * Gradient Color Stops Via Position
         * @see https://tailwindcss.com/docs/gradient-color-stops
         */
        "gradient-via-pos": [{
          via: scaleGradientStopPosition()
        }],
        /**
         * Gradient Color Stops To Position
         * @see https://tailwindcss.com/docs/gradient-color-stops
         */
        "gradient-to-pos": [{
          to: scaleGradientStopPosition()
        }],
        /**
         * Gradient Color Stops From
         * @see https://tailwindcss.com/docs/gradient-color-stops
         */
        "gradient-from": [{
          from: scaleColor()
        }],
        /**
         * Gradient Color Stops Via
         * @see https://tailwindcss.com/docs/gradient-color-stops
         */
        "gradient-via": [{
          via: scaleColor()
        }],
        /**
         * Gradient Color Stops To
         * @see https://tailwindcss.com/docs/gradient-color-stops
         */
        "gradient-to": [{
          to: scaleColor()
        }],
        // ---------------
        // --- Borders ---
        // ---------------
        /**
         * Border Radius
         * @see https://tailwindcss.com/docs/border-radius
         */
        rounded: [{
          rounded: scaleRadius()
        }],
        /**
         * Border Radius Start
         * @see https://tailwindcss.com/docs/border-radius
         */
        "rounded-s": [{
          "rounded-s": scaleRadius()
        }],
        /**
         * Border Radius End
         * @see https://tailwindcss.com/docs/border-radius
         */
        "rounded-e": [{
          "rounded-e": scaleRadius()
        }],
        /**
         * Border Radius Top
         * @see https://tailwindcss.com/docs/border-radius
         */
        "rounded-t": [{
          "rounded-t": scaleRadius()
        }],
        /**
         * Border Radius Right
         * @see https://tailwindcss.com/docs/border-radius
         */
        "rounded-r": [{
          "rounded-r": scaleRadius()
        }],
        /**
         * Border Radius Bottom
         * @see https://tailwindcss.com/docs/border-radius
         */
        "rounded-b": [{
          "rounded-b": scaleRadius()
        }],
        /**
         * Border Radius Left
         * @see https://tailwindcss.com/docs/border-radius
         */
        "rounded-l": [{
          "rounded-l": scaleRadius()
        }],
        /**
         * Border Radius Start Start
         * @see https://tailwindcss.com/docs/border-radius
         */
        "rounded-ss": [{
          "rounded-ss": scaleRadius()
        }],
        /**
         * Border Radius Start End
         * @see https://tailwindcss.com/docs/border-radius
         */
        "rounded-se": [{
          "rounded-se": scaleRadius()
        }],
        /**
         * Border Radius End End
         * @see https://tailwindcss.com/docs/border-radius
         */
        "rounded-ee": [{
          "rounded-ee": scaleRadius()
        }],
        /**
         * Border Radius End Start
         * @see https://tailwindcss.com/docs/border-radius
         */
        "rounded-es": [{
          "rounded-es": scaleRadius()
        }],
        /**
         * Border Radius Top Left
         * @see https://tailwindcss.com/docs/border-radius
         */
        "rounded-tl": [{
          "rounded-tl": scaleRadius()
        }],
        /**
         * Border Radius Top Right
         * @see https://tailwindcss.com/docs/border-radius
         */
        "rounded-tr": [{
          "rounded-tr": scaleRadius()
        }],
        /**
         * Border Radius Bottom Right
         * @see https://tailwindcss.com/docs/border-radius
         */
        "rounded-br": [{
          "rounded-br": scaleRadius()
        }],
        /**
         * Border Radius Bottom Left
         * @see https://tailwindcss.com/docs/border-radius
         */
        "rounded-bl": [{
          "rounded-bl": scaleRadius()
        }],
        /**
         * Border Width
         * @see https://tailwindcss.com/docs/border-width
         */
        "border-w": [{
          border: scaleBorderWidth()
        }],
        /**
         * Border Width Inline
         * @see https://tailwindcss.com/docs/border-width
         */
        "border-w-x": [{
          "border-x": scaleBorderWidth()
        }],
        /**
         * Border Width Block
         * @see https://tailwindcss.com/docs/border-width
         */
        "border-w-y": [{
          "border-y": scaleBorderWidth()
        }],
        /**
         * Border Width Inline Start
         * @see https://tailwindcss.com/docs/border-width
         */
        "border-w-s": [{
          "border-s": scaleBorderWidth()
        }],
        /**
         * Border Width Inline End
         * @see https://tailwindcss.com/docs/border-width
         */
        "border-w-e": [{
          "border-e": scaleBorderWidth()
        }],
        /**
         * Border Width Block Start
         * @see https://tailwindcss.com/docs/border-width
         */
        "border-w-bs": [{
          "border-bs": scaleBorderWidth()
        }],
        /**
         * Border Width Block End
         * @see https://tailwindcss.com/docs/border-width
         */
        "border-w-be": [{
          "border-be": scaleBorderWidth()
        }],
        /**
         * Border Width Top
         * @see https://tailwindcss.com/docs/border-width
         */
        "border-w-t": [{
          "border-t": scaleBorderWidth()
        }],
        /**
         * Border Width Right
         * @see https://tailwindcss.com/docs/border-width
         */
        "border-w-r": [{
          "border-r": scaleBorderWidth()
        }],
        /**
         * Border Width Bottom
         * @see https://tailwindcss.com/docs/border-width
         */
        "border-w-b": [{
          "border-b": scaleBorderWidth()
        }],
        /**
         * Border Width Left
         * @see https://tailwindcss.com/docs/border-width
         */
        "border-w-l": [{
          "border-l": scaleBorderWidth()
        }],
        /**
         * Divide Width X
         * @see https://tailwindcss.com/docs/border-width#between-children
         */
        "divide-x": [{
          "divide-x": scaleBorderWidth()
        }],
        /**
         * Divide Width X Reverse
         * @see https://tailwindcss.com/docs/border-width#between-children
         */
        "divide-x-reverse": ["divide-x-reverse"],
        /**
         * Divide Width Y
         * @see https://tailwindcss.com/docs/border-width#between-children
         */
        "divide-y": [{
          "divide-y": scaleBorderWidth()
        }],
        /**
         * Divide Width Y Reverse
         * @see https://tailwindcss.com/docs/border-width#between-children
         */
        "divide-y-reverse": ["divide-y-reverse"],
        /**
         * Border Style
         * @see https://tailwindcss.com/docs/border-style
         */
        "border-style": [{
          border: [...scaleLineStyle(), "hidden", "none"]
        }],
        /**
         * Divide Style
         * @see https://tailwindcss.com/docs/border-style#setting-the-divider-style
         */
        "divide-style": [{
          divide: [...scaleLineStyle(), "hidden", "none"]
        }],
        /**
         * Border Color
         * @see https://tailwindcss.com/docs/border-color
         */
        "border-color": [{
          border: scaleColor()
        }],
        /**
         * Border Color Inline
         * @see https://tailwindcss.com/docs/border-color
         */
        "border-color-x": [{
          "border-x": scaleColor()
        }],
        /**
         * Border Color Block
         * @see https://tailwindcss.com/docs/border-color
         */
        "border-color-y": [{
          "border-y": scaleColor()
        }],
        /**
         * Border Color Inline Start
         * @see https://tailwindcss.com/docs/border-color
         */
        "border-color-s": [{
          "border-s": scaleColor()
        }],
        /**
         * Border Color Inline End
         * @see https://tailwindcss.com/docs/border-color
         */
        "border-color-e": [{
          "border-e": scaleColor()
        }],
        /**
         * Border Color Block Start
         * @see https://tailwindcss.com/docs/border-color
         */
        "border-color-bs": [{
          "border-bs": scaleColor()
        }],
        /**
         * Border Color Block End
         * @see https://tailwindcss.com/docs/border-color
         */
        "border-color-be": [{
          "border-be": scaleColor()
        }],
        /**
         * Border Color Top
         * @see https://tailwindcss.com/docs/border-color
         */
        "border-color-t": [{
          "border-t": scaleColor()
        }],
        /**
         * Border Color Right
         * @see https://tailwindcss.com/docs/border-color
         */
        "border-color-r": [{
          "border-r": scaleColor()
        }],
        /**
         * Border Color Bottom
         * @see https://tailwindcss.com/docs/border-color
         */
        "border-color-b": [{
          "border-b": scaleColor()
        }],
        /**
         * Border Color Left
         * @see https://tailwindcss.com/docs/border-color
         */
        "border-color-l": [{
          "border-l": scaleColor()
        }],
        /**
         * Divide Color
         * @see https://tailwindcss.com/docs/divide-color
         */
        "divide-color": [{
          divide: scaleColor()
        }],
        /**
         * Outline Style
         * @see https://tailwindcss.com/docs/outline-style
         */
        "outline-style": [{
          outline: [...scaleLineStyle(), "none", "hidden"]
        }],
        /**
         * Outline Offset
         * @see https://tailwindcss.com/docs/outline-offset
         */
        "outline-offset": [{
          "outline-offset": [isNumber, isArbitraryVariable, isArbitraryValue]
        }],
        /**
         * Outline Width
         * @see https://tailwindcss.com/docs/outline-width
         */
        "outline-w": [{
          outline: ["", isNumber, isArbitraryVariableLength, isArbitraryLength]
        }],
        /**
         * Outline Color
         * @see https://tailwindcss.com/docs/outline-color
         */
        "outline-color": [{
          outline: scaleColor()
        }],
        // ---------------
        // --- Effects ---
        // ---------------
        /**
         * Box Shadow
         * @see https://tailwindcss.com/docs/box-shadow
         */
        shadow: [{
          shadow: [
            // Deprecated since Tailwind CSS v4.0.0
            "",
            "none",
            themeShadow,
            isArbitraryVariableShadow,
            isArbitraryShadow
          ]
        }],
        /**
         * Box Shadow Color
         * @see https://tailwindcss.com/docs/box-shadow#setting-the-shadow-color
         */
        "shadow-color": [{
          shadow: scaleColor()
        }],
        /**
         * Inset Box Shadow
         * @see https://tailwindcss.com/docs/box-shadow#adding-an-inset-shadow
         */
        "inset-shadow": [{
          "inset-shadow": ["none", themeInsetShadow, isArbitraryVariableShadow, isArbitraryShadow]
        }],
        /**
         * Inset Box Shadow Color
         * @see https://tailwindcss.com/docs/box-shadow#setting-the-inset-shadow-color
         */
        "inset-shadow-color": [{
          "inset-shadow": scaleColor()
        }],
        /**
         * Ring Width
         * @see https://tailwindcss.com/docs/box-shadow#adding-a-ring
         */
        "ring-w": [{
          ring: scaleBorderWidth()
        }],
        /**
         * Ring Width Inset
         * @see https://v3.tailwindcss.com/docs/ring-width#inset-rings
         * @deprecated since Tailwind CSS v4.0.0
         * @see https://github.com/tailwindlabs/tailwindcss/blob/v4.0.0/packages/tailwindcss/src/utilities.ts#L4158
         */
        "ring-w-inset": ["ring-inset"],
        /**
         * Ring Color
         * @see https://tailwindcss.com/docs/box-shadow#setting-the-ring-color
         */
        "ring-color": [{
          ring: scaleColor()
        }],
        /**
         * Ring Offset Width
         * @see https://v3.tailwindcss.com/docs/ring-offset-width
         * @deprecated since Tailwind CSS v4.0.0
         * @see https://github.com/tailwindlabs/tailwindcss/blob/v4.0.0/packages/tailwindcss/src/utilities.ts#L4158
         */
        "ring-offset-w": [{
          "ring-offset": [isNumber, isArbitraryLength]
        }],
        /**
         * Ring Offset Color
         * @see https://v3.tailwindcss.com/docs/ring-offset-color
         * @deprecated since Tailwind CSS v4.0.0
         * @see https://github.com/tailwindlabs/tailwindcss/blob/v4.0.0/packages/tailwindcss/src/utilities.ts#L4158
         */
        "ring-offset-color": [{
          "ring-offset": scaleColor()
        }],
        /**
         * Inset Ring Width
         * @see https://tailwindcss.com/docs/box-shadow#adding-an-inset-ring
         */
        "inset-ring-w": [{
          "inset-ring": scaleBorderWidth()
        }],
        /**
         * Inset Ring Color
         * @see https://tailwindcss.com/docs/box-shadow#setting-the-inset-ring-color
         */
        "inset-ring-color": [{
          "inset-ring": scaleColor()
        }],
        /**
         * Text Shadow
         * @see https://tailwindcss.com/docs/text-shadow
         */
        "text-shadow": [{
          "text-shadow": ["none", themeTextShadow, isArbitraryVariableShadow, isArbitraryShadow]
        }],
        /**
         * Text Shadow Color
         * @see https://tailwindcss.com/docs/text-shadow#setting-the-shadow-color
         */
        "text-shadow-color": [{
          "text-shadow": scaleColor()
        }],
        /**
         * Opacity
         * @see https://tailwindcss.com/docs/opacity
         */
        opacity: [{
          opacity: [isNumber, isArbitraryVariable, isArbitraryValue]
        }],
        /**
         * Mix Blend Mode
         * @see https://tailwindcss.com/docs/mix-blend-mode
         */
        "mix-blend": [{
          "mix-blend": [...scaleBlendMode(), "plus-darker", "plus-lighter"]
        }],
        /**
         * Background Blend Mode
         * @see https://tailwindcss.com/docs/background-blend-mode
         */
        "bg-blend": [{
          "bg-blend": scaleBlendMode()
        }],
        /**
         * Mask Clip
         * @see https://tailwindcss.com/docs/mask-clip
         */
        "mask-clip": [{
          "mask-clip": ["border", "padding", "content", "fill", "stroke", "view"]
        }, "mask-no-clip"],
        /**
         * Mask Composite
         * @see https://tailwindcss.com/docs/mask-composite
         */
        "mask-composite": [{
          mask: ["add", "subtract", "intersect", "exclude"]
        }],
        /**
         * Mask Image
         * @see https://tailwindcss.com/docs/mask-image
         */
        "mask-image-linear-pos": [{
          "mask-linear": [isNumber]
        }],
        "mask-image-linear-from-pos": [{
          "mask-linear-from": scaleMaskImagePosition()
        }],
        "mask-image-linear-to-pos": [{
          "mask-linear-to": scaleMaskImagePosition()
        }],
        "mask-image-linear-from-color": [{
          "mask-linear-from": scaleColor()
        }],
        "mask-image-linear-to-color": [{
          "mask-linear-to": scaleColor()
        }],
        "mask-image-t-from-pos": [{
          "mask-t-from": scaleMaskImagePosition()
        }],
        "mask-image-t-to-pos": [{
          "mask-t-to": scaleMaskImagePosition()
        }],
        "mask-image-t-from-color": [{
          "mask-t-from": scaleColor()
        }],
        "mask-image-t-to-color": [{
          "mask-t-to": scaleColor()
        }],
        "mask-image-r-from-pos": [{
          "mask-r-from": scaleMaskImagePosition()
        }],
        "mask-image-r-to-pos": [{
          "mask-r-to": scaleMaskImagePosition()
        }],
        "mask-image-r-from-color": [{
          "mask-r-from": scaleColor()
        }],
        "mask-image-r-to-color": [{
          "mask-r-to": scaleColor()
        }],
        "mask-image-b-from-pos": [{
          "mask-b-from": scaleMaskImagePosition()
        }],
        "mask-image-b-to-pos": [{
          "mask-b-to": scaleMaskImagePosition()
        }],
        "mask-image-b-from-color": [{
          "mask-b-from": scaleColor()
        }],
        "mask-image-b-to-color": [{
          "mask-b-to": scaleColor()
        }],
        "mask-image-l-from-pos": [{
          "mask-l-from": scaleMaskImagePosition()
        }],
        "mask-image-l-to-pos": [{
          "mask-l-to": scaleMaskImagePosition()
        }],
        "mask-image-l-from-color": [{
          "mask-l-from": scaleColor()
        }],
        "mask-image-l-to-color": [{
          "mask-l-to": scaleColor()
        }],
        "mask-image-x-from-pos": [{
          "mask-x-from": scaleMaskImagePosition()
        }],
        "mask-image-x-to-pos": [{
          "mask-x-to": scaleMaskImagePosition()
        }],
        "mask-image-x-from-color": [{
          "mask-x-from": scaleColor()
        }],
        "mask-image-x-to-color": [{
          "mask-x-to": scaleColor()
        }],
        "mask-image-y-from-pos": [{
          "mask-y-from": scaleMaskImagePosition()
        }],
        "mask-image-y-to-pos": [{
          "mask-y-to": scaleMaskImagePosition()
        }],
        "mask-image-y-from-color": [{
          "mask-y-from": scaleColor()
        }],
        "mask-image-y-to-color": [{
          "mask-y-to": scaleColor()
        }],
        "mask-image-radial": [{
          "mask-radial": [isArbitraryVariable, isArbitraryValue]
        }],
        "mask-image-radial-from-pos": [{
          "mask-radial-from": scaleMaskImagePosition()
        }],
        "mask-image-radial-to-pos": [{
          "mask-radial-to": scaleMaskImagePosition()
        }],
        "mask-image-radial-from-color": [{
          "mask-radial-from": scaleColor()
        }],
        "mask-image-radial-to-color": [{
          "mask-radial-to": scaleColor()
        }],
        "mask-image-radial-shape": [{
          "mask-radial": ["circle", "ellipse"]
        }],
        "mask-image-radial-size": [{
          "mask-radial": [{
            closest: ["side", "corner"],
            farthest: ["side", "corner"]
          }]
        }],
        "mask-image-radial-pos": [{
          "mask-radial-at": scalePosition()
        }],
        "mask-image-conic-pos": [{
          "mask-conic": [isNumber]
        }],
        "mask-image-conic-from-pos": [{
          "mask-conic-from": scaleMaskImagePosition()
        }],
        "mask-image-conic-to-pos": [{
          "mask-conic-to": scaleMaskImagePosition()
        }],
        "mask-image-conic-from-color": [{
          "mask-conic-from": scaleColor()
        }],
        "mask-image-conic-to-color": [{
          "mask-conic-to": scaleColor()
        }],
        /**
         * Mask Mode
         * @see https://tailwindcss.com/docs/mask-mode
         */
        "mask-mode": [{
          mask: ["alpha", "luminance", "match"]
        }],
        /**
         * Mask Origin
         * @see https://tailwindcss.com/docs/mask-origin
         */
        "mask-origin": [{
          "mask-origin": ["border", "padding", "content", "fill", "stroke", "view"]
        }],
        /**
         * Mask Position
         * @see https://tailwindcss.com/docs/mask-position
         */
        "mask-position": [{
          mask: scaleBgPosition()
        }],
        /**
         * Mask Repeat
         * @see https://tailwindcss.com/docs/mask-repeat
         */
        "mask-repeat": [{
          mask: scaleBgRepeat()
        }],
        /**
         * Mask Size
         * @see https://tailwindcss.com/docs/mask-size
         */
        "mask-size": [{
          mask: scaleBgSize()
        }],
        /**
         * Mask Type
         * @see https://tailwindcss.com/docs/mask-type
         */
        "mask-type": [{
          "mask-type": ["alpha", "luminance"]
        }],
        /**
         * Mask Image
         * @see https://tailwindcss.com/docs/mask-image
         */
        "mask-image": [{
          mask: ["none", isArbitraryVariable, isArbitraryValue]
        }],
        // ---------------
        // --- Filters ---
        // ---------------
        /**
         * Filter
         * @see https://tailwindcss.com/docs/filter
         */
        filter: [{
          filter: [
            // Deprecated since Tailwind CSS v3.0.0
            "",
            "none",
            isArbitraryVariable,
            isArbitraryValue
          ]
        }],
        /**
         * Blur
         * @see https://tailwindcss.com/docs/blur
         */
        blur: [{
          blur: scaleBlur()
        }],
        /**
         * Brightness
         * @see https://tailwindcss.com/docs/brightness
         */
        brightness: [{
          brightness: [isNumber, isArbitraryVariable, isArbitraryValue]
        }],
        /**
         * Contrast
         * @see https://tailwindcss.com/docs/contrast
         */
        contrast: [{
          contrast: [isNumber, isArbitraryVariable, isArbitraryValue]
        }],
        /**
         * Drop Shadow
         * @see https://tailwindcss.com/docs/drop-shadow
         */
        "drop-shadow": [{
          "drop-shadow": [
            // Deprecated since Tailwind CSS v4.0.0
            "",
            "none",
            themeDropShadow,
            isArbitraryVariableShadow,
            isArbitraryShadow
          ]
        }],
        /**
         * Drop Shadow Color
         * @see https://tailwindcss.com/docs/filter-drop-shadow#setting-the-shadow-color
         */
        "drop-shadow-color": [{
          "drop-shadow": scaleColor()
        }],
        /**
         * Grayscale
         * @see https://tailwindcss.com/docs/grayscale
         */
        grayscale: [{
          grayscale: ["", isNumber, isArbitraryVariable, isArbitraryValue]
        }],
        /**
         * Hue Rotate
         * @see https://tailwindcss.com/docs/hue-rotate
         */
        "hue-rotate": [{
          "hue-rotate": [isNumber, isArbitraryVariable, isArbitraryValue]
        }],
        /**
         * Invert
         * @see https://tailwindcss.com/docs/invert
         */
        invert: [{
          invert: ["", isNumber, isArbitraryVariable, isArbitraryValue]
        }],
        /**
         * Saturate
         * @see https://tailwindcss.com/docs/saturate
         */
        saturate: [{
          saturate: [isNumber, isArbitraryVariable, isArbitraryValue]
        }],
        /**
         * Sepia
         * @see https://tailwindcss.com/docs/sepia
         */
        sepia: [{
          sepia: ["", isNumber, isArbitraryVariable, isArbitraryValue]
        }],
        /**
         * Backdrop Filter
         * @see https://tailwindcss.com/docs/backdrop-filter
         */
        "backdrop-filter": [{
          "backdrop-filter": [
            // Deprecated since Tailwind CSS v3.0.0
            "",
            "none",
            isArbitraryVariable,
            isArbitraryValue
          ]
        }],
        /**
         * Backdrop Blur
         * @see https://tailwindcss.com/docs/backdrop-blur
         */
        "backdrop-blur": [{
          "backdrop-blur": scaleBlur()
        }],
        /**
         * Backdrop Brightness
         * @see https://tailwindcss.com/docs/backdrop-brightness
         */
        "backdrop-brightness": [{
          "backdrop-brightness": [isNumber, isArbitraryVariable, isArbitraryValue]
        }],
        /**
         * Backdrop Contrast
         * @see https://tailwindcss.com/docs/backdrop-contrast
         */
        "backdrop-contrast": [{
          "backdrop-contrast": [isNumber, isArbitraryVariable, isArbitraryValue]
        }],
        /**
         * Backdrop Grayscale
         * @see https://tailwindcss.com/docs/backdrop-grayscale
         */
        "backdrop-grayscale": [{
          "backdrop-grayscale": ["", isNumber, isArbitraryVariable, isArbitraryValue]
        }],
        /**
         * Backdrop Hue Rotate
         * @see https://tailwindcss.com/docs/backdrop-hue-rotate
         */
        "backdrop-hue-rotate": [{
          "backdrop-hue-rotate": [isNumber, isArbitraryVariable, isArbitraryValue]
        }],
        /**
         * Backdrop Invert
         * @see https://tailwindcss.com/docs/backdrop-invert
         */
        "backdrop-invert": [{
          "backdrop-invert": ["", isNumber, isArbitraryVariable, isArbitraryValue]
        }],
        /**
         * Backdrop Opacity
         * @see https://tailwindcss.com/docs/backdrop-opacity
         */
        "backdrop-opacity": [{
          "backdrop-opacity": [isNumber, isArbitraryVariable, isArbitraryValue]
        }],
        /**
         * Backdrop Saturate
         * @see https://tailwindcss.com/docs/backdrop-saturate
         */
        "backdrop-saturate": [{
          "backdrop-saturate": [isNumber, isArbitraryVariable, isArbitraryValue]
        }],
        /**
         * Backdrop Sepia
         * @see https://tailwindcss.com/docs/backdrop-sepia
         */
        "backdrop-sepia": [{
          "backdrop-sepia": ["", isNumber, isArbitraryVariable, isArbitraryValue]
        }],
        // --------------
        // --- Tables ---
        // --------------
        /**
         * Border Collapse
         * @see https://tailwindcss.com/docs/border-collapse
         */
        "border-collapse": [{
          border: ["collapse", "separate"]
        }],
        /**
         * Border Spacing
         * @see https://tailwindcss.com/docs/border-spacing
         */
        "border-spacing": [{
          "border-spacing": scaleUnambiguousSpacing()
        }],
        /**
         * Border Spacing X
         * @see https://tailwindcss.com/docs/border-spacing
         */
        "border-spacing-x": [{
          "border-spacing-x": scaleUnambiguousSpacing()
        }],
        /**
         * Border Spacing Y
         * @see https://tailwindcss.com/docs/border-spacing
         */
        "border-spacing-y": [{
          "border-spacing-y": scaleUnambiguousSpacing()
        }],
        /**
         * Table Layout
         * @see https://tailwindcss.com/docs/table-layout
         */
        "table-layout": [{
          table: ["auto", "fixed"]
        }],
        /**
         * Caption Side
         * @see https://tailwindcss.com/docs/caption-side
         */
        caption: [{
          caption: ["top", "bottom"]
        }],
        // ---------------------------------
        // --- Transitions and Animation ---
        // ---------------------------------
        /**
         * Transition Property
         * @see https://tailwindcss.com/docs/transition-property
         */
        transition: [{
          transition: ["", "all", "colors", "opacity", "shadow", "transform", "none", isArbitraryVariable, isArbitraryValue]
        }],
        /**
         * Transition Behavior
         * @see https://tailwindcss.com/docs/transition-behavior
         */
        "transition-behavior": [{
          transition: ["normal", "discrete"]
        }],
        /**
         * Transition Duration
         * @see https://tailwindcss.com/docs/transition-duration
         */
        duration: [{
          duration: [isNumber, "initial", isArbitraryVariable, isArbitraryValue]
        }],
        /**
         * Transition Timing Function
         * @see https://tailwindcss.com/docs/transition-timing-function
         */
        ease: [{
          ease: ["linear", "initial", themeEase, isArbitraryVariable, isArbitraryValue]
        }],
        /**
         * Transition Delay
         * @see https://tailwindcss.com/docs/transition-delay
         */
        delay: [{
          delay: [isNumber, isArbitraryVariable, isArbitraryValue]
        }],
        /**
         * Animation
         * @see https://tailwindcss.com/docs/animation
         */
        animate: [{
          animate: ["none", themeAnimate, isArbitraryVariable, isArbitraryValue]
        }],
        // ------------------
        // --- Transforms ---
        // ------------------
        /**
         * Backface Visibility
         * @see https://tailwindcss.com/docs/backface-visibility
         */
        backface: [{
          backface: ["hidden", "visible"]
        }],
        /**
         * Perspective
         * @see https://tailwindcss.com/docs/perspective
         */
        perspective: [{
          perspective: [themePerspective, isArbitraryVariable, isArbitraryValue]
        }],
        /**
         * Perspective Origin
         * @see https://tailwindcss.com/docs/perspective-origin
         */
        "perspective-origin": [{
          "perspective-origin": scalePositionWithArbitrary()
        }],
        /**
         * Rotate
         * @see https://tailwindcss.com/docs/rotate
         */
        rotate: [{
          rotate: scaleRotate()
        }],
        /**
         * Rotate X
         * @see https://tailwindcss.com/docs/rotate
         */
        "rotate-x": [{
          "rotate-x": scaleRotate()
        }],
        /**
         * Rotate Y
         * @see https://tailwindcss.com/docs/rotate
         */
        "rotate-y": [{
          "rotate-y": scaleRotate()
        }],
        /**
         * Rotate Z
         * @see https://tailwindcss.com/docs/rotate
         */
        "rotate-z": [{
          "rotate-z": scaleRotate()
        }],
        /**
         * Scale
         * @see https://tailwindcss.com/docs/scale
         */
        scale: [{
          scale: scaleScale()
        }],
        /**
         * Scale X
         * @see https://tailwindcss.com/docs/scale
         */
        "scale-x": [{
          "scale-x": scaleScale()
        }],
        /**
         * Scale Y
         * @see https://tailwindcss.com/docs/scale
         */
        "scale-y": [{
          "scale-y": scaleScale()
        }],
        /**
         * Scale Z
         * @see https://tailwindcss.com/docs/scale
         */
        "scale-z": [{
          "scale-z": scaleScale()
        }],
        /**
         * Scale 3D
         * @see https://tailwindcss.com/docs/scale
         */
        "scale-3d": ["scale-3d"],
        /**
         * Skew
         * @see https://tailwindcss.com/docs/skew
         */
        skew: [{
          skew: scaleSkew()
        }],
        /**
         * Skew X
         * @see https://tailwindcss.com/docs/skew
         */
        "skew-x": [{
          "skew-x": scaleSkew()
        }],
        /**
         * Skew Y
         * @see https://tailwindcss.com/docs/skew
         */
        "skew-y": [{
          "skew-y": scaleSkew()
        }],
        /**
         * Transform
         * @see https://tailwindcss.com/docs/transform
         */
        transform: [{
          transform: [isArbitraryVariable, isArbitraryValue, "", "none", "gpu", "cpu"]
        }],
        /**
         * Transform Origin
         * @see https://tailwindcss.com/docs/transform-origin
         */
        "transform-origin": [{
          origin: scalePositionWithArbitrary()
        }],
        /**
         * Transform Style
         * @see https://tailwindcss.com/docs/transform-style
         */
        "transform-style": [{
          transform: ["3d", "flat"]
        }],
        /**
         * Translate
         * @see https://tailwindcss.com/docs/translate
         */
        translate: [{
          translate: scaleTranslate()
        }],
        /**
         * Translate X
         * @see https://tailwindcss.com/docs/translate
         */
        "translate-x": [{
          "translate-x": scaleTranslate()
        }],
        /**
         * Translate Y
         * @see https://tailwindcss.com/docs/translate
         */
        "translate-y": [{
          "translate-y": scaleTranslate()
        }],
        /**
         * Translate Z
         * @see https://tailwindcss.com/docs/translate
         */
        "translate-z": [{
          "translate-z": scaleTranslate()
        }],
        /**
         * Translate None
         * @see https://tailwindcss.com/docs/translate
         */
        "translate-none": ["translate-none"],
        /**
         * Zoom
         * @see https://tailwindcss.com/docs/zoom
         */
        zoom: [{
          zoom: [isInteger, isArbitraryVariable, isArbitraryValue]
        }],
        // ---------------------
        // --- Interactivity ---
        // ---------------------
        /**
         * Accent Color
         * @see https://tailwindcss.com/docs/accent-color
         */
        accent: [{
          accent: scaleColor()
        }],
        /**
         * Appearance
         * @see https://tailwindcss.com/docs/appearance
         */
        appearance: [{
          appearance: ["none", "auto"]
        }],
        /**
         * Caret Color
         * @see https://tailwindcss.com/docs/just-in-time-mode#caret-color-utilities
         */
        "caret-color": [{
          caret: scaleColor()
        }],
        /**
         * Color Scheme
         * @see https://tailwindcss.com/docs/color-scheme
         */
        "color-scheme": [{
          scheme: ["normal", "dark", "light", "light-dark", "only-dark", "only-light"]
        }],
        /**
         * Cursor
         * @see https://tailwindcss.com/docs/cursor
         */
        cursor: [{
          cursor: ["auto", "default", "pointer", "wait", "text", "move", "help", "not-allowed", "none", "context-menu", "progress", "cell", "crosshair", "vertical-text", "alias", "copy", "no-drop", "grab", "grabbing", "all-scroll", "col-resize", "row-resize", "n-resize", "e-resize", "s-resize", "w-resize", "ne-resize", "nw-resize", "se-resize", "sw-resize", "ew-resize", "ns-resize", "nesw-resize", "nwse-resize", "zoom-in", "zoom-out", isArbitraryVariable, isArbitraryValue]
        }],
        /**
         * Field Sizing
         * @see https://tailwindcss.com/docs/field-sizing
         */
        "field-sizing": [{
          "field-sizing": ["fixed", "content"]
        }],
        /**
         * Pointer Events
         * @see https://tailwindcss.com/docs/pointer-events
         */
        "pointer-events": [{
          "pointer-events": ["auto", "none"]
        }],
        /**
         * Resize
         * @see https://tailwindcss.com/docs/resize
         */
        resize: [{
          resize: ["none", "", "y", "x"]
        }],
        /**
         * Scroll Behavior
         * @see https://tailwindcss.com/docs/scroll-behavior
         */
        "scroll-behavior": [{
          scroll: ["auto", "smooth"]
        }],
        /**
         * Scrollbar Thumb Color
         * @see https://tailwindcss.com/docs/scrollbar-color
         */
        "scrollbar-thumb-color": [{
          "scrollbar-thumb": scaleColor()
        }],
        /**
         * Scrollbar Track Color
         * @see https://tailwindcss.com/docs/scrollbar-color
         */
        "scrollbar-track-color": [{
          "scrollbar-track": scaleColor()
        }],
        /**
         * Scrollbar Gutter
         * @see https://tailwindcss.com/docs/scrollbar-gutter
         */
        "scrollbar-gutter": [{
          "scrollbar-gutter": ["auto", "stable", "both"]
        }],
        /**
         * Scrollbar Width
         * @see https://tailwindcss.com/docs/scrollbar-width
         */
        "scrollbar-w": [{
          scrollbar: ["auto", "thin", "none"]
        }],
        /**
         * Scroll Margin
         * @see https://tailwindcss.com/docs/scroll-margin
         */
        "scroll-m": [{
          "scroll-m": scaleUnambiguousSpacing()
        }],
        /**
         * Scroll Margin Inline
         * @see https://tailwindcss.com/docs/scroll-margin
         */
        "scroll-mx": [{
          "scroll-mx": scaleUnambiguousSpacing()
        }],
        /**
         * Scroll Margin Block
         * @see https://tailwindcss.com/docs/scroll-margin
         */
        "scroll-my": [{
          "scroll-my": scaleUnambiguousSpacing()
        }],
        /**
         * Scroll Margin Inline Start
         * @see https://tailwindcss.com/docs/scroll-margin
         */
        "scroll-ms": [{
          "scroll-ms": scaleUnambiguousSpacing()
        }],
        /**
         * Scroll Margin Inline End
         * @see https://tailwindcss.com/docs/scroll-margin
         */
        "scroll-me": [{
          "scroll-me": scaleUnambiguousSpacing()
        }],
        /**
         * Scroll Margin Block Start
         * @see https://tailwindcss.com/docs/scroll-margin
         */
        "scroll-mbs": [{
          "scroll-mbs": scaleUnambiguousSpacing()
        }],
        /**
         * Scroll Margin Block End
         * @see https://tailwindcss.com/docs/scroll-margin
         */
        "scroll-mbe": [{
          "scroll-mbe": scaleUnambiguousSpacing()
        }],
        /**
         * Scroll Margin Top
         * @see https://tailwindcss.com/docs/scroll-margin
         */
        "scroll-mt": [{
          "scroll-mt": scaleUnambiguousSpacing()
        }],
        /**
         * Scroll Margin Right
         * @see https://tailwindcss.com/docs/scroll-margin
         */
        "scroll-mr": [{
          "scroll-mr": scaleUnambiguousSpacing()
        }],
        /**
         * Scroll Margin Bottom
         * @see https://tailwindcss.com/docs/scroll-margin
         */
        "scroll-mb": [{
          "scroll-mb": scaleUnambiguousSpacing()
        }],
        /**
         * Scroll Margin Left
         * @see https://tailwindcss.com/docs/scroll-margin
         */
        "scroll-ml": [{
          "scroll-ml": scaleUnambiguousSpacing()
        }],
        /**
         * Scroll Padding
         * @see https://tailwindcss.com/docs/scroll-padding
         */
        "scroll-p": [{
          "scroll-p": scaleUnambiguousSpacing()
        }],
        /**
         * Scroll Padding Inline
         * @see https://tailwindcss.com/docs/scroll-padding
         */
        "scroll-px": [{
          "scroll-px": scaleUnambiguousSpacing()
        }],
        /**
         * Scroll Padding Block
         * @see https://tailwindcss.com/docs/scroll-padding
         */
        "scroll-py": [{
          "scroll-py": scaleUnambiguousSpacing()
        }],
        /**
         * Scroll Padding Inline Start
         * @see https://tailwindcss.com/docs/scroll-padding
         */
        "scroll-ps": [{
          "scroll-ps": scaleUnambiguousSpacing()
        }],
        /**
         * Scroll Padding Inline End
         * @see https://tailwindcss.com/docs/scroll-padding
         */
        "scroll-pe": [{
          "scroll-pe": scaleUnambiguousSpacing()
        }],
        /**
         * Scroll Padding Block Start
         * @see https://tailwindcss.com/docs/scroll-padding
         */
        "scroll-pbs": [{
          "scroll-pbs": scaleUnambiguousSpacing()
        }],
        /**
         * Scroll Padding Block End
         * @see https://tailwindcss.com/docs/scroll-padding
         */
        "scroll-pbe": [{
          "scroll-pbe": scaleUnambiguousSpacing()
        }],
        /**
         * Scroll Padding Top
         * @see https://tailwindcss.com/docs/scroll-padding
         */
        "scroll-pt": [{
          "scroll-pt": scaleUnambiguousSpacing()
        }],
        /**
         * Scroll Padding Right
         * @see https://tailwindcss.com/docs/scroll-padding
         */
        "scroll-pr": [{
          "scroll-pr": scaleUnambiguousSpacing()
        }],
        /**
         * Scroll Padding Bottom
         * @see https://tailwindcss.com/docs/scroll-padding
         */
        "scroll-pb": [{
          "scroll-pb": scaleUnambiguousSpacing()
        }],
        /**
         * Scroll Padding Left
         * @see https://tailwindcss.com/docs/scroll-padding
         */
        "scroll-pl": [{
          "scroll-pl": scaleUnambiguousSpacing()
        }],
        /**
         * Scroll Snap Align
         * @see https://tailwindcss.com/docs/scroll-snap-align
         */
        "snap-align": [{
          snap: ["start", "end", "center", "align-none"]
        }],
        /**
         * Scroll Snap Stop
         * @see https://tailwindcss.com/docs/scroll-snap-stop
         */
        "snap-stop": [{
          snap: ["normal", "always"]
        }],
        /**
         * Scroll Snap Type
         * @see https://tailwindcss.com/docs/scroll-snap-type
         */
        "snap-type": [{
          snap: ["none", "x", "y", "both"]
        }],
        /**
         * Scroll Snap Type Strictness
         * @see https://tailwindcss.com/docs/scroll-snap-type
         */
        "snap-strictness": [{
          snap: ["mandatory", "proximity"]
        }],
        /**
         * Touch Action
         * @see https://tailwindcss.com/docs/touch-action
         */
        touch: [{
          touch: ["auto", "none", "manipulation"]
        }],
        /**
         * Touch Action X
         * @see https://tailwindcss.com/docs/touch-action
         */
        "touch-x": [{
          "touch-pan": ["x", "left", "right"]
        }],
        /**
         * Touch Action Y
         * @see https://tailwindcss.com/docs/touch-action
         */
        "touch-y": [{
          "touch-pan": ["y", "up", "down"]
        }],
        /**
         * Touch Action Pinch Zoom
         * @see https://tailwindcss.com/docs/touch-action
         */
        "touch-pz": ["touch-pinch-zoom"],
        /**
         * User Select
         * @see https://tailwindcss.com/docs/user-select
         */
        select: [{
          select: ["none", "text", "all", "auto"]
        }],
        /**
         * Will Change
         * @see https://tailwindcss.com/docs/will-change
         */
        "will-change": [{
          "will-change": ["auto", "scroll", "contents", "transform", isArbitraryVariable, isArbitraryValue]
        }],
        // -----------
        // --- SVG ---
        // -----------
        /**
         * Fill
         * @see https://tailwindcss.com/docs/fill
         */
        fill: [{
          fill: ["none", ...scaleColor()]
        }],
        /**
         * Stroke Width
         * @see https://tailwindcss.com/docs/stroke-width
         */
        "stroke-w": [{
          stroke: [isNumber, isArbitraryVariableLength, isArbitraryLength, isArbitraryNumber]
        }],
        /**
         * Stroke
         * @see https://tailwindcss.com/docs/stroke
         */
        stroke: [{
          stroke: ["none", ...scaleColor()]
        }],
        // ---------------------
        // --- Accessibility ---
        // ---------------------
        /**
         * Forced Color Adjust
         * @see https://tailwindcss.com/docs/forced-color-adjust
         */
        "forced-color-adjust": [{
          "forced-color-adjust": ["auto", "none"]
        }]
      },
      conflictingClassGroups: {
        "container-named": ["container-type"],
        overflow: ["overflow-x", "overflow-y"],
        overscroll: ["overscroll-x", "overscroll-y"],
        inset: ["inset-x", "inset-y", "inset-bs", "inset-be", "start", "end", "top", "right", "bottom", "left"],
        "inset-x": ["right", "left"],
        "inset-y": ["top", "bottom"],
        flex: ["basis", "grow", "shrink"],
        gap: ["gap-x", "gap-y"],
        p: ["px", "py", "ps", "pe", "pbs", "pbe", "pt", "pr", "pb", "pl"],
        px: ["pr", "pl"],
        py: ["pt", "pb"],
        m: ["mx", "my", "ms", "me", "mbs", "mbe", "mt", "mr", "mb", "ml"],
        mx: ["mr", "ml"],
        my: ["mt", "mb"],
        size: ["w", "h"],
        "font-size": ["leading"],
        "fvn-normal": ["fvn-ordinal", "fvn-slashed-zero", "fvn-figure", "fvn-spacing", "fvn-fraction"],
        "fvn-ordinal": ["fvn-normal"],
        "fvn-slashed-zero": ["fvn-normal"],
        "fvn-figure": ["fvn-normal"],
        "fvn-spacing": ["fvn-normal"],
        "fvn-fraction": ["fvn-normal"],
        "line-clamp": ["display", "overflow"],
        rounded: ["rounded-s", "rounded-e", "rounded-t", "rounded-r", "rounded-b", "rounded-l", "rounded-ss", "rounded-se", "rounded-ee", "rounded-es", "rounded-tl", "rounded-tr", "rounded-br", "rounded-bl"],
        "rounded-s": ["rounded-ss", "rounded-es"],
        "rounded-e": ["rounded-se", "rounded-ee"],
        "rounded-t": ["rounded-tl", "rounded-tr"],
        "rounded-r": ["rounded-tr", "rounded-br"],
        "rounded-b": ["rounded-br", "rounded-bl"],
        "rounded-l": ["rounded-tl", "rounded-bl"],
        "border-spacing": ["border-spacing-x", "border-spacing-y"],
        "border-w": ["border-w-x", "border-w-y", "border-w-s", "border-w-e", "border-w-bs", "border-w-be", "border-w-t", "border-w-r", "border-w-b", "border-w-l"],
        "border-w-x": ["border-w-r", "border-w-l"],
        "border-w-y": ["border-w-t", "border-w-b"],
        "border-color": ["border-color-x", "border-color-y", "border-color-s", "border-color-e", "border-color-bs", "border-color-be", "border-color-t", "border-color-r", "border-color-b", "border-color-l"],
        "border-color-x": ["border-color-r", "border-color-l"],
        "border-color-y": ["border-color-t", "border-color-b"],
        translate: ["translate-x", "translate-y", "translate-none"],
        "translate-none": ["translate", "translate-x", "translate-y", "translate-z"],
        "scroll-m": ["scroll-mx", "scroll-my", "scroll-ms", "scroll-me", "scroll-mbs", "scroll-mbe", "scroll-mt", "scroll-mr", "scroll-mb", "scroll-ml"],
        "scroll-mx": ["scroll-mr", "scroll-ml"],
        "scroll-my": ["scroll-mt", "scroll-mb"],
        "scroll-p": ["scroll-px", "scroll-py", "scroll-ps", "scroll-pe", "scroll-pbs", "scroll-pbe", "scroll-pt", "scroll-pr", "scroll-pb", "scroll-pl"],
        "scroll-px": ["scroll-pr", "scroll-pl"],
        "scroll-py": ["scroll-pt", "scroll-pb"],
        touch: ["touch-x", "touch-y", "touch-pz"],
        "touch-x": ["touch"],
        "touch-y": ["touch"],
        "touch-pz": ["touch"]
      },
      conflictingClassGroupModifiers: {
        "font-size": ["leading"]
      },
      postfixLookupClassGroups: ["container-type"],
      orderSensitiveModifiers: ["*", "**", "after", "backdrop", "before", "details-content", "file", "first-letter", "first-line", "marker", "placeholder", "selection"]
    };
  };
  var twMerge = /* @__PURE__ */ createTailwindMerge(getDefaultConfig);

  // frontend/src/helpers/cn.ts
  function cn(...inputs) {
    return twMerge(clsx(inputs));
  }

  // frontend/node_modules/psihointegritet-ds/src/motion/sticky-bar.tsx
  function StickyBar({ children }) {
    const [visible, setVisible] = (0, import_react22.useState)(false);
    (0, import_react22.useEffect)(() => {
      const onScroll = () => {
        setVisible(window.scrollY > 50);
      };
      onScroll();
      window.addEventListener("scroll", onScroll, { passive: true });
      return () => {
        window.removeEventListener("scroll", onScroll);
      };
    }, []);
    return /* @__PURE__ */ React.createElement(
      "div",
      {
        className: cn(
          "shadow-pill ease-soft fixed top-5 left-1/2 z-[65] flex -translate-x-1/2 items-center gap-[clamp(14px,1.3vw,24px)] rounded-full border border-white/35 bg-gray-400/32 py-2 pr-2 pl-[26px] whitespace-nowrap backdrop-blur-[14px] transition-transform duration-500",
          visible ? "translate-y-0" : "-translate-y-[220%]"
        )
      },
      children
    );
  }

  // frontend/node_modules/psihointegritet-ds/src/panel/confirm-modal.tsx
  init_define_import_meta_env();

  // frontend/node_modules/@react-aria/focus/dist/import.mjs
  init_define_import_meta_env();

  // frontend/node_modules/react-aria/dist/private/utils/shadowdom/DOMFunctions.mjs
  init_define_import_meta_env();

  // frontend/node_modules/react-aria/dist/private/utils/domHelpers.mjs
  init_define_import_meta_env();
  var $d447af545b77c9f1$export$b204af158042fbac = (el) => {
    return el?.ownerDocument ?? document;
  };
  var $d447af545b77c9f1$export$f21a1ffae260145a = (el) => {
    if (el && "window" in el && el.window === el) return el;
    const doc = $d447af545b77c9f1$export$b204af158042fbac(el);
    return doc.defaultView || window;
  };
  function $d447af545b77c9f1$var$isNode(value) {
    return value !== null && typeof value === "object" && "nodeType" in value && typeof value.nodeType === "number";
  }
  function $d447af545b77c9f1$export$af51f0f06c0f328a(node) {
    return $d447af545b77c9f1$var$isNode(node) && node.nodeType === Node.DOCUMENT_FRAGMENT_NODE && "host" in node;
  }

  // frontend/node_modules/react-stately/dist/exports/private/flags/flags.mjs
  init_define_import_meta_env();

  // frontend/node_modules/react-stately/dist/private/flags/flags.mjs
  init_define_import_meta_env();
  var $6a20a7989e6c817a$var$_shadowDOM = false;
  function $6a20a7989e6c817a$export$98658e8c59125e6a() {
    return $6a20a7989e6c817a$var$_shadowDOM;
  }

  // frontend/node_modules/react-aria/dist/private/utils/shadowdom/DOMFunctions.mjs
  function $23f2114a1b82827e$export$4282f70798064fe0(node, otherNode) {
    if (!(0, $6a20a7989e6c817a$export$98658e8c59125e6a)()) return otherNode && node ? node.contains(otherNode) : false;
    if (!node || !otherNode) return false;
    let currentNode = otherNode;
    while (currentNode !== null) {
      if (currentNode === node) return true;
      if (currentNode.tagName === "SLOT" && currentNode.assignedSlot)
        currentNode = currentNode.assignedSlot.parentNode;
      else if ((0, $d447af545b77c9f1$export$af51f0f06c0f328a)(currentNode))
        currentNode = currentNode.host;
      else currentNode = currentNode.parentNode;
    }
    return false;
  }
  var $23f2114a1b82827e$export$cd4e5573fbe2b576 = (doc = document) => {
    if (!(0, $6a20a7989e6c817a$export$98658e8c59125e6a)()) return doc.activeElement;
    let activeElement2 = doc.activeElement;
    while (activeElement2 && "shadowRoot" in activeElement2 && activeElement2.shadowRoot?.activeElement) activeElement2 = activeElement2.shadowRoot.activeElement;
    return activeElement2;
  };
  function $23f2114a1b82827e$export$e58f029f0fbfdb29(event) {
    if ((0, $6a20a7989e6c817a$export$98658e8c59125e6a)() && event.target instanceof Element && event.target.shadowRoot) {
      if ("composedPath" in event) return event.composedPath()[0] ?? null;
      else if ("composedPath" in event.nativeEvent) return event.nativeEvent.composedPath()[0] ?? null;
    }
    return event.target;
  }

  // frontend/node_modules/react-aria/dist/private/utils/focusWithoutScrolling.mjs
  init_define_import_meta_env();
  function $1969ac565cfec8d0$export$de79e2c695e052f3(element) {
    if ($1969ac565cfec8d0$var$supportsPreventScroll()) element.focus({
      preventScroll: true
    });
    else {
      let scrollableElements = $1969ac565cfec8d0$var$getScrollableElements(element);
      element.focus();
      $1969ac565cfec8d0$var$restoreScrollPosition(scrollableElements);
    }
  }
  var $1969ac565cfec8d0$var$supportsPreventScrollCached = null;
  function $1969ac565cfec8d0$var$supportsPreventScroll() {
    if ($1969ac565cfec8d0$var$supportsPreventScrollCached == null) {
      $1969ac565cfec8d0$var$supportsPreventScrollCached = false;
      try {
        let focusElem = document.createElement("div");
        focusElem.focus({
          get preventScroll() {
            $1969ac565cfec8d0$var$supportsPreventScrollCached = true;
            return true;
          }
        });
      } catch {
      }
    }
    return $1969ac565cfec8d0$var$supportsPreventScrollCached;
  }
  function $1969ac565cfec8d0$var$getScrollableElements(element) {
    let parent = element.parentNode;
    let scrollableElements = [];
    let rootScrollingElement = document.scrollingElement || document.documentElement;
    while (parent instanceof HTMLElement && parent !== rootScrollingElement) {
      if (parent.offsetHeight < parent.scrollHeight || parent.offsetWidth < parent.scrollWidth) scrollableElements.push({
        element: parent,
        scrollTop: parent.scrollTop,
        scrollLeft: parent.scrollLeft
      });
      parent = parent.parentNode;
    }
    if (rootScrollingElement instanceof HTMLElement) scrollableElements.push({
      element: rootScrollingElement,
      scrollTop: rootScrollingElement.scrollTop,
      scrollLeft: rootScrollingElement.scrollLeft
    });
    return scrollableElements;
  }
  function $1969ac565cfec8d0$var$restoreScrollPosition(scrollableElements) {
    for (let { element, scrollTop, scrollLeft } of scrollableElements) {
      element.scrollTop = scrollTop;
      element.scrollLeft = scrollLeft;
    }
  }

  // frontend/node_modules/react-aria/dist/private/interactions/useFocusVisible.mjs
  init_define_import_meta_env();

  // frontend/node_modules/react-aria/dist/private/interactions/utils.mjs
  init_define_import_meta_env();

  // frontend/node_modules/react-aria/dist/private/utils/useLayoutEffect.mjs
  init_define_import_meta_env();
  var import_react23 = __toESM(require_react_shim(), 1);
  var $c4867b2f328c2698$export$e5c5a5f917a5871c = typeof document !== "undefined" ? (0, import_react23.default).useLayoutEffect : () => {
  };

  // frontend/node_modules/react-aria/dist/private/interactions/utils.mjs
  var import_react24 = __toESM(require_react_shim(), 1);
  function $a92dc41f639950be$export$525bc4921d56d4a(nativeEvent) {
    let event = nativeEvent;
    event.nativeEvent = nativeEvent;
    event.isDefaultPrevented = () => event.defaultPrevented;
    event.isPropagationStopped = () => event.cancelBubble;
    event.persist = () => {
    };
    return event;
  }
  function $a92dc41f639950be$export$c2b7abe5d61ec696(event, target) {
    Object.defineProperty(event, "target", {
      value: target
    });
    Object.defineProperty(event, "currentTarget", {
      value: target
    });
  }
  function $a92dc41f639950be$export$715c682d09d639cc(onBlur) {
    let stateRef = (0, import_react24.useRef)({
      isFocused: false,
      observer: null
    });
    (0, $c4867b2f328c2698$export$e5c5a5f917a5871c)(() => {
      const state = stateRef.current;
      return () => {
        if (state.observer) {
          state.observer.disconnect();
          state.observer = null;
        }
      };
    }, []);
    return (0, import_react24.useCallback)((e8) => {
      let eventTarget = (0, $23f2114a1b82827e$export$e58f029f0fbfdb29)(e8);
      if (eventTarget instanceof HTMLButtonElement || eventTarget instanceof HTMLInputElement || eventTarget instanceof HTMLTextAreaElement || eventTarget instanceof HTMLSelectElement) {
        stateRef.current.isFocused = true;
        let target = eventTarget;
        let onBlurHandler = (e9) => {
          stateRef.current.isFocused = false;
          if (target.disabled) {
            let event = $a92dc41f639950be$export$525bc4921d56d4a(e9);
            onBlur?.(event);
          }
          if (stateRef.current.observer) {
            stateRef.current.observer.disconnect();
            stateRef.current.observer = null;
          }
        };
        target.addEventListener("focusout", onBlurHandler, {
          once: true
        });
        stateRef.current.observer = new MutationObserver(() => {
          if (stateRef.current.isFocused && target.disabled) {
            stateRef.current.observer?.disconnect();
            let relatedTargetEl = target === (0, $23f2114a1b82827e$export$cd4e5573fbe2b576)() ? null : (0, $23f2114a1b82827e$export$cd4e5573fbe2b576)();
            target.dispatchEvent(new FocusEvent("blur", {
              relatedTarget: relatedTargetEl
            }));
            target.dispatchEvent(new FocusEvent("focusout", {
              bubbles: true,
              relatedTarget: relatedTargetEl
            }));
          }
        });
        stateRef.current.observer.observe(target, {
          attributes: true,
          attributeFilter: [
            "disabled"
          ]
        });
      }
    }, [
      onBlur
    ]);
  }
  var $a92dc41f639950be$export$fda7da73ab5d4c48 = false;

  // frontend/node_modules/react-aria/dist/private/utils/platform.mjs
  init_define_import_meta_env();
  function $2add3ce32c6007eb$var$testUserAgent(re2) {
    if (typeof window === "undefined" || window.navigator == null) return false;
    let brands = window.navigator["userAgentData"]?.brands;
    return Array.isArray(brands) && brands.some((brand) => re2.test(brand.brand)) || re2.test(window.navigator.userAgent);
  }
  function $2add3ce32c6007eb$var$testPlatform(re2) {
    return typeof window !== "undefined" && window.navigator != null ? re2.test(window.navigator["userAgentData"]?.platform || window.navigator.platform) : false;
  }
  function $2add3ce32c6007eb$var$cached(fn) {
    if (false) return fn;
    let res = null;
    return () => {
      if (res == null) res = fn();
      return res;
    };
  }
  var $2add3ce32c6007eb$export$9ac100e40613ea10 = $2add3ce32c6007eb$var$cached(function() {
    return $2add3ce32c6007eb$var$testPlatform(/^Mac/i);
  });
  var $2add3ce32c6007eb$export$186c6964ca17d99 = $2add3ce32c6007eb$var$cached(function() {
    return $2add3ce32c6007eb$var$testPlatform(/^iPhone/i);
  });
  var $2add3ce32c6007eb$export$7bef049ce92e4224 = $2add3ce32c6007eb$var$cached(function() {
    return $2add3ce32c6007eb$var$testPlatform(/^iPad/i) || // iPadOS 13 lies and says it's a Mac, but we can distinguish by detecting touch support.
    $2add3ce32c6007eb$export$9ac100e40613ea10() && navigator.maxTouchPoints > 1;
  });
  var $2add3ce32c6007eb$export$fedb369cb70207f1 = $2add3ce32c6007eb$var$cached(function() {
    return $2add3ce32c6007eb$export$186c6964ca17d99() || $2add3ce32c6007eb$export$7bef049ce92e4224();
  });
  var $2add3ce32c6007eb$export$e1865c3bedcd822b = $2add3ce32c6007eb$var$cached(function() {
    return $2add3ce32c6007eb$export$9ac100e40613ea10() || $2add3ce32c6007eb$export$fedb369cb70207f1();
  });
  var $2add3ce32c6007eb$export$78551043582a6a98 = $2add3ce32c6007eb$var$cached(function() {
    return $2add3ce32c6007eb$var$testUserAgent(/AppleWebKit/i) && !$2add3ce32c6007eb$export$6446a186d09e379e();
  });
  var $2add3ce32c6007eb$export$6446a186d09e379e = $2add3ce32c6007eb$var$cached(function() {
    return $2add3ce32c6007eb$var$testUserAgent(/Chrome/i);
  });
  var $2add3ce32c6007eb$export$a11b0059900ceec8 = $2add3ce32c6007eb$var$cached(function() {
    return $2add3ce32c6007eb$var$testUserAgent(/Android/i);
  });
  var $2add3ce32c6007eb$export$b7d78993b74f766d = $2add3ce32c6007eb$var$cached(function() {
    return $2add3ce32c6007eb$var$testUserAgent(/Firefox/i);
  });

  // frontend/node_modules/react-aria/dist/private/utils/isVirtualEvent.mjs
  init_define_import_meta_env();
  function $b5c62b033c25b96d$export$60278871457622de(event) {
    if (event.pointerType === "" && event.isTrusted) return true;
    if ((0, $2add3ce32c6007eb$export$a11b0059900ceec8)() && event.pointerType) return event.type === "click" && event.buttons === 1;
    return event.detail === 0 && !event.pointerType;
  }

  // frontend/node_modules/react-aria/dist/private/utils/openLink.mjs
  init_define_import_meta_env();
  var import_react25 = __toESM(require_react_shim(), 1);
  function $caaf0dd3060ed57c$export$95185d699e05d4d7(target, modifiers, setOpening = true) {
    let { metaKey, ctrlKey, altKey, shiftKey } = modifiers;
    if ((0, $2add3ce32c6007eb$export$b7d78993b74f766d)() && window.event?.type?.startsWith("key") && target.target === "_blank") {
      if ((0, $2add3ce32c6007eb$export$9ac100e40613ea10)()) metaKey = true;
      else ctrlKey = true;
    }
    let event = (0, $2add3ce32c6007eb$export$78551043582a6a98)() && (0, $2add3ce32c6007eb$export$9ac100e40613ea10)() && !(0, $2add3ce32c6007eb$export$7bef049ce92e4224)() && true ? new KeyboardEvent("keydown", {
      keyIdentifier: "Enter",
      metaKey,
      ctrlKey,
      altKey,
      shiftKey
    }) : new MouseEvent("click", {
      metaKey,
      ctrlKey,
      altKey,
      shiftKey,
      detail: 1,
      bubbles: true,
      cancelable: true
    });
    $caaf0dd3060ed57c$export$95185d699e05d4d7.isOpening = setOpening;
    (0, $1969ac565cfec8d0$export$de79e2c695e052f3)(target);
    target.dispatchEvent(event);
    $caaf0dd3060ed57c$export$95185d699e05d4d7.isOpening = false;
  }
  $caaf0dd3060ed57c$export$95185d699e05d4d7.isOpening = false;

  // frontend/node_modules/react-aria/dist/private/interactions/useFocusVisible.mjs
  var import_react26 = __toESM(require_react_shim(), 1);
  var $8f5a2122b0992be3$var$currentModality = null;
  var $8f5a2122b0992be3$var$currentPointerType = "keyboard";
  var $8f5a2122b0992be3$export$901e90a13c50a14e = /* @__PURE__ */ new Set();
  var $8f5a2122b0992be3$export$d90243b58daecda7 = /* @__PURE__ */ new Map();
  var $8f5a2122b0992be3$var$hasEventBeforeFocus = false;
  var $8f5a2122b0992be3$var$hasBlurredWindowRecently = false;
  var $8f5a2122b0992be3$var$FOCUS_VISIBLE_INPUT_KEYS = {
    Tab: true,
    Escape: true
  };
  function $8f5a2122b0992be3$var$triggerChangeHandlers(modality, e8) {
    for (let handler of $8f5a2122b0992be3$export$901e90a13c50a14e) handler(modality, e8);
  }
  function $8f5a2122b0992be3$var$isValidKey(e8) {
    return !(e8.metaKey || !(0, $2add3ce32c6007eb$export$9ac100e40613ea10)() && e8.altKey || e8.ctrlKey || e8.key === "Control" || e8.key === "Shift" || e8.key === "Meta");
  }
  function $8f5a2122b0992be3$var$handleKeyboardEvent(e8) {
    $8f5a2122b0992be3$var$hasEventBeforeFocus = true;
    if (!(0, $caaf0dd3060ed57c$export$95185d699e05d4d7).isOpening && $8f5a2122b0992be3$var$isValidKey(e8)) {
      $8f5a2122b0992be3$var$currentModality = "keyboard";
      $8f5a2122b0992be3$var$currentPointerType = "keyboard";
      $8f5a2122b0992be3$var$triggerChangeHandlers("keyboard", e8);
    }
  }
  function $8f5a2122b0992be3$var$handlePointerEvent(e8) {
    $8f5a2122b0992be3$var$currentModality = "pointer";
    $8f5a2122b0992be3$var$currentPointerType = "pointerType" in e8 ? e8.pointerType : "mouse";
    if (e8.type === "mousedown" || e8.type === "pointerdown") {
      $8f5a2122b0992be3$var$hasEventBeforeFocus = true;
      $8f5a2122b0992be3$var$triggerChangeHandlers("pointer", e8);
    }
  }
  function $8f5a2122b0992be3$var$handleClickEvent(e8) {
    if (!(0, $caaf0dd3060ed57c$export$95185d699e05d4d7).isOpening && (0, $b5c62b033c25b96d$export$60278871457622de)(e8)) {
      $8f5a2122b0992be3$var$hasEventBeforeFocus = true;
      $8f5a2122b0992be3$var$currentModality = "virtual";
      $8f5a2122b0992be3$var$currentPointerType = "virtual";
    }
  }
  function $8f5a2122b0992be3$var$handleFocusEvent(e8) {
    let ownerWindow = (0, $d447af545b77c9f1$export$f21a1ffae260145a)((0, $23f2114a1b82827e$export$e58f029f0fbfdb29)(e8));
    let ownerDocument = (0, $d447af545b77c9f1$export$b204af158042fbac)((0, $23f2114a1b82827e$export$e58f029f0fbfdb29)(e8));
    if ((0, $23f2114a1b82827e$export$e58f029f0fbfdb29)(e8) === ownerWindow || (0, $23f2114a1b82827e$export$e58f029f0fbfdb29)(e8) === ownerDocument || (0, $a92dc41f639950be$export$fda7da73ab5d4c48) || !e8.isTrusted) return;
    if (!$8f5a2122b0992be3$var$hasEventBeforeFocus && !$8f5a2122b0992be3$var$hasBlurredWindowRecently) {
      $8f5a2122b0992be3$var$currentModality = "virtual";
      $8f5a2122b0992be3$var$currentPointerType = "virtual";
      $8f5a2122b0992be3$var$triggerChangeHandlers("virtual", e8);
    }
    $8f5a2122b0992be3$var$hasEventBeforeFocus = false;
    $8f5a2122b0992be3$var$hasBlurredWindowRecently = false;
  }
  function $8f5a2122b0992be3$var$handleWindowBlur() {
    if (0, $a92dc41f639950be$export$fda7da73ab5d4c48) return;
    $8f5a2122b0992be3$var$hasEventBeforeFocus = false;
    $8f5a2122b0992be3$var$hasBlurredWindowRecently = true;
  }
  function $8f5a2122b0992be3$var$setupGlobalFocusEvents(element) {
    if (typeof window === "undefined" || typeof document === "undefined") return;
    const windowObject = (0, $d447af545b77c9f1$export$f21a1ffae260145a)(element);
    const documentObject = (0, $d447af545b77c9f1$export$b204af158042fbac)(element);
    if ($8f5a2122b0992be3$export$d90243b58daecda7.get(windowObject)) return;
    let focus = windowObject.HTMLElement.prototype.focus;
    Reflect.defineProperty(windowObject.HTMLElement.prototype, "focus", {
      configurable: true,
      writable: true,
      value: function() {
        $8f5a2122b0992be3$var$hasEventBeforeFocus = true;
        focus.apply(this, arguments);
      }
    });
    documentObject.addEventListener("keydown", $8f5a2122b0992be3$var$handleKeyboardEvent, true);
    documentObject.addEventListener("keyup", $8f5a2122b0992be3$var$handleKeyboardEvent, true);
    documentObject.addEventListener("click", $8f5a2122b0992be3$var$handleClickEvent, true);
    windowObject.addEventListener("focus", $8f5a2122b0992be3$var$handleFocusEvent, true);
    windowObject.addEventListener("blur", $8f5a2122b0992be3$var$handleWindowBlur, false);
    if (typeof PointerEvent !== "undefined") {
      documentObject.addEventListener("pointerdown", $8f5a2122b0992be3$var$handlePointerEvent, true);
      documentObject.addEventListener("pointermove", $8f5a2122b0992be3$var$handlePointerEvent, true);
      documentObject.addEventListener("pointerup", $8f5a2122b0992be3$var$handlePointerEvent, true);
    } else if (false) {
      documentObject.addEventListener("mousedown", $8f5a2122b0992be3$var$handlePointerEvent, true);
      documentObject.addEventListener("mousemove", $8f5a2122b0992be3$var$handlePointerEvent, true);
      documentObject.addEventListener("mouseup", $8f5a2122b0992be3$var$handlePointerEvent, true);
    }
    windowObject.addEventListener("beforeunload", () => {
      $8f5a2122b0992be3$var$tearDownWindowFocusTracking(element);
    }, {
      once: true
    });
    $8f5a2122b0992be3$export$d90243b58daecda7.set(windowObject, {
      focus
    });
  }
  var $8f5a2122b0992be3$var$tearDownWindowFocusTracking = (element, loadListener) => {
    const windowObject = (0, $d447af545b77c9f1$export$f21a1ffae260145a)(element);
    const documentObject = (0, $d447af545b77c9f1$export$b204af158042fbac)(element);
    if (loadListener) documentObject.removeEventListener("DOMContentLoaded", loadListener);
    if (!$8f5a2122b0992be3$export$d90243b58daecda7.has(windowObject)) return;
    Reflect.defineProperty(windowObject.HTMLElement.prototype, "focus", {
      configurable: true,
      writable: true,
      value: $8f5a2122b0992be3$export$d90243b58daecda7.get(windowObject).focus
    });
    documentObject.removeEventListener("keydown", $8f5a2122b0992be3$var$handleKeyboardEvent, true);
    documentObject.removeEventListener("keyup", $8f5a2122b0992be3$var$handleKeyboardEvent, true);
    documentObject.removeEventListener("click", $8f5a2122b0992be3$var$handleClickEvent, true);
    windowObject.removeEventListener("focus", $8f5a2122b0992be3$var$handleFocusEvent, true);
    windowObject.removeEventListener("blur", $8f5a2122b0992be3$var$handleWindowBlur, false);
    if (typeof PointerEvent !== "undefined") {
      documentObject.removeEventListener("pointerdown", $8f5a2122b0992be3$var$handlePointerEvent, true);
      documentObject.removeEventListener("pointermove", $8f5a2122b0992be3$var$handlePointerEvent, true);
      documentObject.removeEventListener("pointerup", $8f5a2122b0992be3$var$handlePointerEvent, true);
    } else if (false) {
      documentObject.removeEventListener("mousedown", $8f5a2122b0992be3$var$handlePointerEvent, true);
      documentObject.removeEventListener("mousemove", $8f5a2122b0992be3$var$handlePointerEvent, true);
      documentObject.removeEventListener("mouseup", $8f5a2122b0992be3$var$handlePointerEvent, true);
    }
    $8f5a2122b0992be3$export$d90243b58daecda7.delete(windowObject);
  };
  function $8f5a2122b0992be3$export$2f1888112f558a7d(element) {
    const documentObject = (0, $d447af545b77c9f1$export$b204af158042fbac)(element);
    let loadListener;
    if (documentObject.readyState !== "loading") $8f5a2122b0992be3$var$setupGlobalFocusEvents(element);
    else {
      loadListener = () => {
        $8f5a2122b0992be3$var$setupGlobalFocusEvents(element);
      };
      documentObject.addEventListener("DOMContentLoaded", loadListener);
    }
    return () => $8f5a2122b0992be3$var$tearDownWindowFocusTracking(element, loadListener);
  }
  if (typeof document !== "undefined") $8f5a2122b0992be3$export$2f1888112f558a7d();
  function $8f5a2122b0992be3$export$b9b3dfddab17db27() {
    return $8f5a2122b0992be3$var$currentModality !== "pointer";
  }
  var $8f5a2122b0992be3$var$nonTextInputTypes = /* @__PURE__ */ new Set([
    "checkbox",
    "radio",
    "range",
    "color",
    "file",
    "image",
    "button",
    "submit",
    "reset"
  ]);
  function $8f5a2122b0992be3$var$isKeyboardFocusEvent(isTextInput, modality, e8) {
    let eventTarget = e8 ? (0, $23f2114a1b82827e$export$e58f029f0fbfdb29)(e8) : void 0;
    let document1 = (0, $d447af545b77c9f1$export$b204af158042fbac)(eventTarget);
    let ownerWindow = (0, $d447af545b77c9f1$export$f21a1ffae260145a)(eventTarget);
    const IHTMLInputElement = typeof ownerWindow !== "undefined" ? ownerWindow.HTMLInputElement : HTMLInputElement;
    const IHTMLTextAreaElement = typeof ownerWindow !== "undefined" ? ownerWindow.HTMLTextAreaElement : HTMLTextAreaElement;
    const IHTMLElement = typeof ownerWindow !== "undefined" ? ownerWindow.HTMLElement : HTMLElement;
    const IKeyboardEvent = typeof ownerWindow !== "undefined" ? ownerWindow.KeyboardEvent : KeyboardEvent;
    let activeElement2 = (0, $23f2114a1b82827e$export$cd4e5573fbe2b576)(document1);
    isTextInput = isTextInput || activeElement2 instanceof IHTMLInputElement && !$8f5a2122b0992be3$var$nonTextInputTypes.has(activeElement2.type) || activeElement2 instanceof IHTMLTextAreaElement || activeElement2 instanceof IHTMLElement && activeElement2.isContentEditable;
    return !(isTextInput && modality === "keyboard" && e8 instanceof IKeyboardEvent && !$8f5a2122b0992be3$var$FOCUS_VISIBLE_INPUT_KEYS[e8.key]);
  }
  function $8f5a2122b0992be3$export$ec71b4b83ac08ec3(fn, deps, opts) {
    $8f5a2122b0992be3$var$setupGlobalFocusEvents();
    (0, import_react26.useEffect)(() => {
      if (opts?.enabled === false) return;
      let handler = (modality, e8) => {
        if (!$8f5a2122b0992be3$var$isKeyboardFocusEvent(!!opts?.isTextInput, modality, e8)) return;
        fn($8f5a2122b0992be3$export$b9b3dfddab17db27());
      };
      $8f5a2122b0992be3$export$901e90a13c50a14e.add(handler);
      return () => {
        $8f5a2122b0992be3$export$901e90a13c50a14e.delete(handler);
      };
    }, deps);
  }

  // frontend/node_modules/react-aria/dist/private/focus/useFocusRing.mjs
  init_define_import_meta_env();

  // frontend/node_modules/react-aria/dist/private/interactions/useFocus.mjs
  init_define_import_meta_env();
  var import_react27 = __toESM(require_react_shim(), 1);
  function $1e74c67db218ce67$export$f8168d8dd8fd66e6(props) {
    let { isDisabled, onFocus: onFocusProp, onBlur: onBlurProp, onFocusChange } = props;
    const onBlur = (0, import_react27.useCallback)((e8) => {
      if ((0, $23f2114a1b82827e$export$e58f029f0fbfdb29)(e8) === e8.currentTarget) {
        if (onBlurProp) onBlurProp(e8);
        if (onFocusChange) onFocusChange(false);
        return true;
      }
    }, [
      onBlurProp,
      onFocusChange
    ]);
    const onSyntheticFocus = (0, $a92dc41f639950be$export$715c682d09d639cc)(onBlur);
    const onFocus = (0, import_react27.useCallback)((e8) => {
      let eventTarget = (0, $23f2114a1b82827e$export$e58f029f0fbfdb29)(e8);
      const ownerDocument = (0, $d447af545b77c9f1$export$b204af158042fbac)(eventTarget);
      const activeElement2 = ownerDocument ? (0, $23f2114a1b82827e$export$cd4e5573fbe2b576)(ownerDocument) : (0, $23f2114a1b82827e$export$cd4e5573fbe2b576)();
      if (eventTarget === e8.currentTarget && eventTarget === activeElement2) {
        if (onFocusProp) onFocusProp(e8);
        if (onFocusChange) onFocusChange(true);
        onSyntheticFocus(e8);
      }
    }, [
      onFocusChange,
      onFocusProp,
      onSyntheticFocus
    ]);
    return {
      focusProps: {
        onFocus: !isDisabled && (onFocusProp || onFocusChange || onBlurProp) ? onFocus : void 0,
        onBlur: !isDisabled && (onBlurProp || onFocusChange) ? onBlur : void 0
      }
    };
  }

  // frontend/node_modules/react-aria/dist/private/interactions/useFocusWithin.mjs
  init_define_import_meta_env();

  // frontend/node_modules/react-aria/dist/private/utils/useGlobalListeners.mjs
  init_define_import_meta_env();
  var import_react28 = __toESM(require_react_shim(), 1);
  function $48a7d519b337145d$export$4eaf04e54aa8eed6() {
    let globalListeners = (0, import_react28.useRef)(/* @__PURE__ */ new Map());
    let addGlobalListener = (0, import_react28.useCallback)((eventTarget, type, listener, options) => {
      let fn = options?.once ? (...args) => {
        globalListeners.current.delete(listener);
        listener(...args);
      } : listener;
      globalListeners.current.set(listener, {
        type,
        eventTarget,
        fn,
        options
      });
      eventTarget.addEventListener(type, fn, options);
    }, []);
    let removeGlobalListener = (0, import_react28.useCallback)((eventTarget, type, listener, options) => {
      let fn = globalListeners.current.get(listener)?.fn || listener;
      eventTarget.removeEventListener(type, fn, options);
      globalListeners.current.delete(listener);
    }, []);
    let removeAllGlobalListeners = (0, import_react28.useCallback)(() => {
      globalListeners.current.forEach((value, key) => {
        removeGlobalListener(value.eventTarget, value.type, key, value.options);
      });
    }, [
      removeGlobalListener
    ]);
    (0, import_react28.useEffect)(() => {
      return removeAllGlobalListeners;
    }, [
      removeAllGlobalListeners
    ]);
    return {
      addGlobalListener,
      removeGlobalListener,
      removeAllGlobalListeners
    };
  }

  // frontend/node_modules/react-aria/dist/private/interactions/useFocusWithin.mjs
  var import_react29 = __toESM(require_react_shim(), 1);
  function $2c9edc598a03d523$export$420e68273165f4ec(props) {
    let { isDisabled, onBlurWithin, onFocusWithin, onFocusWithinChange } = props;
    let state = (0, import_react29.useRef)({
      isFocusWithin: false
    });
    let { addGlobalListener, removeAllGlobalListeners } = (0, $48a7d519b337145d$export$4eaf04e54aa8eed6)();
    let onBlur = (0, import_react29.useCallback)((e8) => {
      if (!(0, $23f2114a1b82827e$export$4282f70798064fe0)(e8.currentTarget, (0, $23f2114a1b82827e$export$e58f029f0fbfdb29)(e8))) return;
      if (state.current.isFocusWithin && !(0, $23f2114a1b82827e$export$4282f70798064fe0)(e8.currentTarget, e8.relatedTarget)) {
        state.current.isFocusWithin = false;
        removeAllGlobalListeners();
        if (onBlurWithin) onBlurWithin(e8);
        if (onFocusWithinChange) onFocusWithinChange(false);
      }
    }, [
      onBlurWithin,
      onFocusWithinChange,
      state,
      removeAllGlobalListeners
    ]);
    let onSyntheticFocus = (0, $a92dc41f639950be$export$715c682d09d639cc)(onBlur);
    let onFocus = (0, import_react29.useCallback)((e8) => {
      if (!(0, $23f2114a1b82827e$export$4282f70798064fe0)(e8.currentTarget, (0, $23f2114a1b82827e$export$e58f029f0fbfdb29)(e8))) return;
      let eventTarget = (0, $23f2114a1b82827e$export$e58f029f0fbfdb29)(e8);
      const ownerDocument = (0, $d447af545b77c9f1$export$b204af158042fbac)(eventTarget);
      const activeElement2 = (0, $23f2114a1b82827e$export$cd4e5573fbe2b576)(ownerDocument);
      if (!state.current.isFocusWithin && activeElement2 === eventTarget) {
        if (onFocusWithin) onFocusWithin(e8);
        if (onFocusWithinChange) onFocusWithinChange(true);
        state.current.isFocusWithin = true;
        onSyntheticFocus(e8);
        let currentTarget = e8.currentTarget;
        addGlobalListener(ownerDocument, "focus", (e9) => {
          let eventTarget2 = (0, $23f2114a1b82827e$export$e58f029f0fbfdb29)(e9);
          if (state.current.isFocusWithin && !(0, $23f2114a1b82827e$export$4282f70798064fe0)(currentTarget, eventTarget2)) {
            let nativeEvent = new ownerDocument.defaultView.FocusEvent("blur", {
              relatedTarget: eventTarget2
            });
            (0, $a92dc41f639950be$export$c2b7abe5d61ec696)(nativeEvent, currentTarget);
            let event = (0, $a92dc41f639950be$export$525bc4921d56d4a)(nativeEvent);
            onBlur(event);
          }
        }, {
          capture: true
        });
      }
    }, [
      onFocusWithin,
      onFocusWithinChange,
      onSyntheticFocus,
      addGlobalListener,
      onBlur
    ]);
    if (isDisabled) return {
      focusWithinProps: {
        // These cannot be null, that would conflict in mergeProps
        onFocus: void 0,
        onBlur: void 0
      }
    };
    return {
      focusWithinProps: {
        onFocus,
        onBlur
      }
    };
  }

  // frontend/node_modules/react-aria/dist/private/focus/useFocusRing.mjs
  var import_react30 = __toESM(require_react_shim(), 1);
  function $0c4a58759813079a$export$4e328f61c538687f(props = {}) {
    let { autoFocus = false, isTextInput, within } = props;
    let state = (0, import_react30.useRef)({
      isFocused: false,
      isFocusVisible: autoFocus || (0, $8f5a2122b0992be3$export$b9b3dfddab17db27)()
    });
    let [isFocused, setFocused] = (0, import_react30.useState)(false);
    let [isFocusVisibleState, setFocusVisible] = (0, import_react30.useState)(() => state.current.isFocused && state.current.isFocusVisible);
    let updateState = (0, import_react30.useCallback)(() => setFocusVisible(state.current.isFocused && state.current.isFocusVisible), []);
    let onFocusChange = (0, import_react30.useCallback)((isFocused2) => {
      state.current.isFocused = isFocused2;
      state.current.isFocusVisible = (0, $8f5a2122b0992be3$export$b9b3dfddab17db27)();
      setFocused(isFocused2);
      updateState();
    }, [
      updateState
    ]);
    (0, $8f5a2122b0992be3$export$ec71b4b83ac08ec3)((isFocusVisible) => {
      state.current.isFocusVisible = isFocusVisible;
      updateState();
    }, [
      isTextInput,
      isFocused
    ], {
      enabled: isFocused,
      isTextInput
    });
    let { focusProps } = (0, $1e74c67db218ce67$export$f8168d8dd8fd66e6)({
      isDisabled: within,
      onFocusChange
    });
    let { focusWithinProps } = (0, $2c9edc598a03d523$export$420e68273165f4ec)({
      isDisabled: !within,
      onFocusWithinChange: onFocusChange
    });
    return {
      isFocused,
      isFocusVisible: isFocusVisibleState,
      focusProps: within ? focusWithinProps : focusProps
    };
  }

  // frontend/node_modules/react-aria/dist/exports/useFocusRing.mjs
  init_define_import_meta_env();

  // frontend/node_modules/@react-aria/interactions/dist/import.mjs
  init_define_import_meta_env();

  // frontend/node_modules/react-aria/dist/exports/useHover.mjs
  init_define_import_meta_env();

  // frontend/node_modules/react-aria/dist/private/interactions/useHover.mjs
  init_define_import_meta_env();
  var import_react31 = __toESM(require_react_shim(), 1);
  var $e969f22b6713ca4a$var$globalIgnoreEmulatedMouseEvents = false;
  var $e969f22b6713ca4a$var$hoverCount = 0;
  function $e969f22b6713ca4a$var$setGlobalIgnoreEmulatedMouseEvents() {
    $e969f22b6713ca4a$var$globalIgnoreEmulatedMouseEvents = true;
    setTimeout(() => {
      $e969f22b6713ca4a$var$globalIgnoreEmulatedMouseEvents = false;
    }, 500);
  }
  function $e969f22b6713ca4a$var$handleGlobalPointerEvent(e8) {
    if (e8.pointerType === "touch") $e969f22b6713ca4a$var$setGlobalIgnoreEmulatedMouseEvents();
  }
  function $e969f22b6713ca4a$var$setupGlobalTouchEvents() {
    let ownerDocument = (0, $d447af545b77c9f1$export$b204af158042fbac)(null);
    if (typeof ownerDocument === "undefined") return;
    if ($e969f22b6713ca4a$var$hoverCount === 0) {
      if (typeof PointerEvent !== "undefined") ownerDocument.addEventListener("pointerup", $e969f22b6713ca4a$var$handleGlobalPointerEvent);
      else if (false) ownerDocument.addEventListener("touchend", $e969f22b6713ca4a$var$setGlobalIgnoreEmulatedMouseEvents);
    }
    $e969f22b6713ca4a$var$hoverCount++;
    return () => {
      $e969f22b6713ca4a$var$hoverCount--;
      if ($e969f22b6713ca4a$var$hoverCount > 0) return;
      if (typeof PointerEvent !== "undefined") ownerDocument.removeEventListener("pointerup", $e969f22b6713ca4a$var$handleGlobalPointerEvent);
      else if (false) ownerDocument.removeEventListener("touchend", $e969f22b6713ca4a$var$setGlobalIgnoreEmulatedMouseEvents);
    };
  }
  function $e969f22b6713ca4a$export$ae780daf29e6d456(props) {
    let { onHoverStart, onHoverChange, onHoverEnd, isDisabled } = props;
    let [isHovered, setHovered] = (0, import_react31.useState)(false);
    let state = (0, import_react31.useRef)({
      isHovered: false,
      ignoreEmulatedMouseEvents: false,
      pointerType: "",
      target: null
    }).current;
    (0, import_react31.useEffect)($e969f22b6713ca4a$var$setupGlobalTouchEvents, []);
    let { addGlobalListener, removeAllGlobalListeners } = (0, $48a7d519b337145d$export$4eaf04e54aa8eed6)();
    let { hoverProps, triggerHoverEnd } = (0, import_react31.useMemo)(() => {
      let triggerHoverStart = (event, pointerType) => {
        state.pointerType = pointerType;
        if (isDisabled || pointerType === "touch" || state.isHovered || !(0, $23f2114a1b82827e$export$4282f70798064fe0)(event.currentTarget, (0, $23f2114a1b82827e$export$e58f029f0fbfdb29)(event))) return;
        state.isHovered = true;
        let target = event.currentTarget;
        state.target = target;
        addGlobalListener((0, $d447af545b77c9f1$export$b204af158042fbac)((0, $23f2114a1b82827e$export$e58f029f0fbfdb29)(event)), "pointerover", (e8) => {
          if (state.isHovered && state.target && !(0, $23f2114a1b82827e$export$4282f70798064fe0)(state.target, (0, $23f2114a1b82827e$export$e58f029f0fbfdb29)(e8))) triggerHoverEnd2(e8, e8.pointerType);
        }, {
          capture: true
        });
        if (onHoverStart) onHoverStart({
          type: "hoverstart",
          target,
          pointerType
        });
        if (onHoverChange) onHoverChange(true);
        setHovered(true);
      };
      let triggerHoverEnd2 = (event, pointerType) => {
        let target = state.target;
        state.pointerType = "";
        state.target = null;
        if (pointerType === "touch" || !state.isHovered || !target) return;
        state.isHovered = false;
        removeAllGlobalListeners();
        if (onHoverEnd) onHoverEnd({
          type: "hoverend",
          target,
          pointerType
        });
        if (onHoverChange) onHoverChange(false);
        setHovered(false);
      };
      let hoverProps2 = {};
      if (typeof PointerEvent !== "undefined") {
        hoverProps2.onPointerEnter = (e8) => {
          if ($e969f22b6713ca4a$var$globalIgnoreEmulatedMouseEvents && e8.pointerType === "mouse") return;
          triggerHoverStart(e8, e8.pointerType);
        };
        hoverProps2.onPointerLeave = (e8) => {
          if (!isDisabled && (0, $23f2114a1b82827e$export$4282f70798064fe0)(e8.currentTarget, (0, $23f2114a1b82827e$export$e58f029f0fbfdb29)(e8))) triggerHoverEnd2(e8, e8.pointerType);
        };
      } else if (false) {
        hoverProps2.onTouchStart = () => {
          state.ignoreEmulatedMouseEvents = true;
        };
        hoverProps2.onMouseEnter = (e8) => {
          if (!state.ignoreEmulatedMouseEvents && !$e969f22b6713ca4a$var$globalIgnoreEmulatedMouseEvents) triggerHoverStart(e8, "mouse");
          state.ignoreEmulatedMouseEvents = false;
        };
        hoverProps2.onMouseLeave = (e8) => {
          if (!isDisabled && (0, $23f2114a1b82827e$export$4282f70798064fe0)(e8.currentTarget, (0, $23f2114a1b82827e$export$e58f029f0fbfdb29)(e8))) triggerHoverEnd2(e8, "mouse");
        };
      }
      return {
        hoverProps: hoverProps2,
        triggerHoverEnd: triggerHoverEnd2
      };
    }, [
      onHoverStart,
      onHoverChange,
      onHoverEnd,
      isDisabled,
      state,
      addGlobalListener,
      removeAllGlobalListeners
    ]);
    (0, import_react31.useEffect)(() => {
      if (isDisabled) triggerHoverEnd({
        currentTarget: state.target
      }, state.pointerType);
    }, [
      isDisabled
    ]);
    return {
      hoverProps,
      isHovered
    };
  }

  // frontend/node_modules/@headlessui/react/dist/hooks/use-active-press.js
  init_define_import_meta_env();
  var import_react36 = __toESM(require_react_shim(), 1);

  // frontend/node_modules/@headlessui/react/dist/utils/owner.js
  init_define_import_meta_env();

  // frontend/node_modules/@headlessui/react/dist/utils/env.js
  init_define_import_meta_env();
  var i = Object.defineProperty;
  var d = (t12, e8, n13) => e8 in t12 ? i(t12, e8, { enumerable: true, configurable: true, writable: true, value: n13 }) : t12[e8] = n13;
  var r2 = (t12, e8, n13) => (d(t12, typeof e8 != "symbol" ? e8 + "" : e8, n13), n13);
  var o = class {
    constructor() {
      r2(this, "current", this.detect());
      r2(this, "handoffState", "pending");
      r2(this, "currentId", 0);
    }
    set(e8) {
      this.current !== e8 && (this.handoffState = "pending", this.currentId = 0, this.current = e8);
    }
    reset() {
      this.set(this.detect());
    }
    nextId() {
      return ++this.currentId;
    }
    get isServer() {
      return this.current === "server";
    }
    get isClient() {
      return this.current === "client";
    }
    detect() {
      return typeof window == "undefined" || typeof document == "undefined" ? "server" : "client";
    }
    handoff() {
      this.handoffState === "pending" && (this.handoffState = "complete");
    }
    get isHandoffComplete() {
      return this.handoffState === "complete";
    }
  };
  var s = new o();

  // frontend/node_modules/@headlessui/react/dist/utils/owner.js
  function l(n13) {
    var u16;
    return s.isServer ? null : n13 == null ? document : (u16 = n13 == null ? void 0 : n13.ownerDocument) != null ? u16 : document;
  }
  function r3(n13) {
    var u16, o16;
    return s.isServer ? null : n13 == null ? document : (o16 = (u16 = n13 == null ? void 0 : n13.getRootNode) == null ? void 0 : u16.call(n13)) != null ? o16 : document;
  }
  function e(n13) {
    var u16, o16;
    return (o16 = (u16 = r3(n13)) == null ? void 0 : u16.activeElement) != null ? o16 : null;
  }
  function d2(n13) {
    return e(n13) === n13;
  }

  // frontend/node_modules/@headlessui/react/dist/hooks/use-disposables.js
  init_define_import_meta_env();
  var import_react32 = __toESM(require_react_shim(), 1);

  // frontend/node_modules/@headlessui/react/dist/utils/disposables.js
  init_define_import_meta_env();

  // frontend/node_modules/@headlessui/react/dist/utils/micro-task.js
  init_define_import_meta_env();
  function t(e8) {
    typeof queueMicrotask == "function" ? queueMicrotask(e8) : Promise.resolve().then(e8).catch((o16) => setTimeout(() => {
      throw o16;
    }));
  }

  // frontend/node_modules/@headlessui/react/dist/utils/disposables.js
  function o2() {
    let s15 = [], r16 = { addEventListener(e8, t12, n13, i13) {
      return e8.addEventListener(t12, n13, i13), r16.add(() => e8.removeEventListener(t12, n13, i13));
    }, requestAnimationFrame(...e8) {
      let t12 = requestAnimationFrame(...e8);
      return r16.add(() => cancelAnimationFrame(t12));
    }, nextFrame(...e8) {
      return r16.requestAnimationFrame(() => r16.requestAnimationFrame(...e8));
    }, setTimeout(...e8) {
      let t12 = setTimeout(...e8);
      return r16.add(() => clearTimeout(t12));
    }, microTask(...e8) {
      let t12 = { current: true };
      return t(() => {
        t12.current && e8[0]();
      }), r16.add(() => {
        t12.current = false;
      });
    }, style(e8, t12, n13) {
      let i13 = e8.style.getPropertyValue(t12);
      return Object.assign(e8.style, { [t12]: n13 }), this.add(() => {
        Object.assign(e8.style, { [t12]: i13 });
      });
    }, group(e8) {
      let t12 = o2();
      return e8(t12), this.add(() => t12.dispose());
    }, add(e8) {
      return s15.includes(e8) || s15.push(e8), () => {
        let t12 = s15.indexOf(e8);
        if (t12 >= 0) for (let n13 of s15.splice(t12, 1)) n13();
      };
    }, dispose() {
      for (let e8 of s15.splice(0)) e8();
    } };
    return r16;
  }

  // frontend/node_modules/@headlessui/react/dist/hooks/use-disposables.js
  function p() {
    let [e8] = (0, import_react32.useState)(o2);
    return (0, import_react32.useEffect)(() => () => e8.dispose(), [e8]), e8;
  }

  // frontend/node_modules/@headlessui/react/dist/hooks/use-event.js
  init_define_import_meta_env();
  var import_react35 = __toESM(require_react_shim(), 1);

  // frontend/node_modules/@headlessui/react/dist/hooks/use-latest-value.js
  init_define_import_meta_env();
  var import_react34 = __toESM(require_react_shim(), 1);

  // frontend/node_modules/@headlessui/react/dist/hooks/use-iso-morphic-effect.js
  init_define_import_meta_env();
  var import_react33 = __toESM(require_react_shim(), 1);
  var n = (e8, t12) => {
    s.isServer ? (0, import_react33.useEffect)(e8, t12) : (0, import_react33.useLayoutEffect)(e8, t12);
  };

  // frontend/node_modules/@headlessui/react/dist/hooks/use-latest-value.js
  function s3(e8) {
    let r16 = (0, import_react34.useRef)(e8);
    return n(() => {
      r16.current = e8;
    }, [e8]), r16;
  }

  // frontend/node_modules/@headlessui/react/dist/hooks/use-event.js
  var o4 = function(t12) {
    let e8 = s3(t12);
    return import_react35.default.useCallback((...r16) => e8.current(...r16), [e8]);
  };

  // frontend/node_modules/@headlessui/react/dist/hooks/use-active-press.js
  function E(e8) {
    let t12 = e8.width / 2, n13 = e8.height / 2;
    return { top: e8.clientY - n13, right: e8.clientX + t12, bottom: e8.clientY + n13, left: e8.clientX - t12 };
  }
  function P(e8, t12) {
    return !(!e8 || !t12 || e8.right < t12.left || e8.left > t12.right || e8.bottom < t12.top || e8.top > t12.bottom);
  }
  function w({ disabled: e8 = false } = {}) {
    let t12 = (0, import_react36.useRef)(null), [n13, l13] = (0, import_react36.useState)(false), r16 = p(), o16 = o4(() => {
      t12.current = null, l13(false), r16.dispose();
    }), f13 = o4((s15) => {
      if (r16.dispose(), t12.current === null) {
        t12.current = s15.currentTarget, l13(true);
        {
          let i13 = l(s15.currentTarget);
          r16.addEventListener(i13, "pointerup", o16, false), r16.addEventListener(i13, "pointermove", (c14) => {
            if (t12.current) {
              let p8 = E(c14);
              l13(P(p8, t12.current.getBoundingClientRect()));
            }
          }, false), r16.addEventListener(i13, "pointercancel", o16, false);
        }
      }
    });
    return { pressed: n13, pressProps: e8 ? {} : { onPointerDown: f13, onPointerUp: o16, onClick: o16 } };
  }

  // frontend/node_modules/@headlessui/react/dist/hooks/use-slot.js
  init_define_import_meta_env();
  var import_react37 = __toESM(require_react_shim(), 1);
  function n2(e8) {
    return (0, import_react37.useMemo)(() => e8, Object.values(e8));
  }

  // frontend/node_modules/@headlessui/react/dist/internal/disabled.js
  init_define_import_meta_env();
  var import_react38 = __toESM(require_react_shim(), 1);
  var e2 = (0, import_react38.createContext)(void 0);
  function a3() {
    return (0, import_react38.useContext)(e2);
  }

  // frontend/node_modules/@headlessui/react/dist/utils/render.js
  init_define_import_meta_env();
  var import_react39 = __toESM(require_react_shim(), 1);

  // frontend/node_modules/@headlessui/react/dist/utils/class-names.js
  init_define_import_meta_env();
  function t4(...r16) {
    return Array.from(new Set(r16.flatMap((n13) => typeof n13 == "string" ? n13.split(" ") : []))).filter(Boolean).join(" ");
  }

  // frontend/node_modules/@headlessui/react/dist/utils/match.js
  init_define_import_meta_env();
  function u(r16, n13, ...a21) {
    if (r16 in n13) {
      let e8 = n13[r16];
      return typeof e8 == "function" ? e8(...a21) : e8;
    }
    let t12 = new Error(`Tried to handle "${r16}" but there is no handler defined. Only defined handlers are: ${Object.keys(n13).map((e8) => `"${e8}"`).join(", ")}.`);
    throw Error.captureStackTrace && Error.captureStackTrace(t12, u), t12;
  }

  // frontend/node_modules/@headlessui/react/dist/utils/render.js
  var A = ((a21) => (a21[a21.None = 0] = "None", a21[a21.RenderStrategy = 1] = "RenderStrategy", a21[a21.Static = 2] = "Static", a21))(A || {});
  var C = ((t12) => (t12[t12.Unmount = 0] = "Unmount", t12[t12.Hidden = 1] = "Hidden", t12))(C || {});
  function K() {
    let e8 = I();
    return (0, import_react39.useCallback)((r16) => U({ mergeRefs: e8, ...r16 }), [e8]);
  }
  function U({ ourProps: e8, theirProps: r16, slot: t12, defaultTag: a21, features: o16, visible: n13 = true, name: i13, mergeRefs: l13 }) {
    l13 = l13 != null ? l13 : H;
    let s15 = P2(r16, e8);
    if (n13) return F(s15, t12, a21, i13, l13);
    let y6 = o16 != null ? o16 : 0;
    if (y6 & 2) {
      let { static: f13 = false, ...u16 } = s15;
      if (f13) return F(u16, t12, a21, i13, l13);
    }
    if (y6 & 1) {
      let { unmount: f13 = true, ...u16 } = s15;
      return u(f13 ? 0 : 1, { [0]() {
        return null;
      }, [1]() {
        return F({ ...u16, hidden: true, style: { display: "none" } }, t12, a21, i13, l13);
      } });
    }
    return F(s15, t12, a21, i13, l13);
  }
  function F(e8, r16 = {}, t12, a21, o16) {
    let { as: n13 = t12, children: i13, refName: l13 = "ref", ...s15 } = h(e8, ["unmount", "static"]), y6 = e8.ref !== void 0 ? { [l13]: e8.ref } : {}, f13 = typeof i13 == "function" ? i13(r16) : i13;
    f13 = E2(f13), "className" in s15 && s15.className && typeof s15.className == "function" && (s15.className = s15.className(r16)), s15["aria-labelledby"] && s15["aria-labelledby"] === s15.id && (s15["aria-labelledby"] = void 0);
    let u16 = {};
    if (r16) {
      let d9 = false, p8 = [];
      for (let [c14, T9] of Object.entries(r16)) typeof T9 == "boolean" && (d9 = true), T9 === true && p8.push(c14.replace(/([A-Z])/g, (g3) => `-${g3.toLowerCase()}`));
      if (d9) {
        u16["data-headlessui-state"] = p8.join(" ");
        for (let c14 of p8) u16[`data-${c14}`] = "";
      }
    }
    if (b(n13) && (Object.keys(m3(s15)).length > 0 || Object.keys(m3(u16)).length > 0)) if (!(0, import_react39.isValidElement)(f13) || Array.isArray(f13) && f13.length > 1 || L(f13)) {
      if (Object.keys(m3(s15)).length > 0) throw new Error(['Passing props on "Fragment"!', "", `The current component <${a21} /> is rendering a "Fragment".`, "However we need to passthrough the following props:", Object.keys(m3(s15)).concat(Object.keys(m3(u16))).map((d9) => `  - ${d9}`).join(`
`), "", "You can apply a few solutions:", ['Add an `as="..."` prop, to ensure that we render an actual element instead of a "Fragment".', "Render a single element as the child so that we can forward the props onto that element."].map((d9) => `  - ${d9}`).join(`
`)].join(`
`));
    } else {
      let d9 = f13.props, p8 = d9 == null ? void 0 : d9.className, c14 = typeof p8 == "function" ? (...R5) => t4(p8(...R5), s15.className) : t4(p8, s15.className), T9 = c14 ? { className: c14 } : {}, g3 = P2(f13.props, m3(h(s15, ["ref"])));
      for (let R5 in u16) R5 in g3 && delete u16[R5];
      return (0, import_react39.cloneElement)(f13, Object.assign({}, g3, u16, y6, { ref: o16(D(f13), y6.ref) }, T9));
    }
    return (0, import_react39.createElement)(n13, Object.assign({}, h(s15, ["ref"]), !b(n13) && y6, !b(n13) && u16), f13);
  }
  function I() {
    let e8 = (0, import_react39.useRef)([]), r16 = (0, import_react39.useCallback)((t12) => {
      for (let a21 of e8.current) a21 != null && (typeof a21 == "function" ? a21(t12) : a21.current = t12);
    }, []);
    return (...t12) => {
      if (!t12.every((a21) => a21 == null)) return e8.current = t12, r16;
    };
  }
  function H(...e8) {
    return e8.every((r16) => r16 == null) ? void 0 : (r16) => {
      for (let t12 of e8) t12 != null && (typeof t12 == "function" ? t12(r16) : t12.current = r16);
    };
  }
  function P2(...e8) {
    var a21;
    if (e8.length === 0) return {};
    if (e8.length === 1) return e8[0];
    let r16 = {}, t12 = {};
    for (let o16 of e8) for (let n13 in o16) n13.startsWith("on") && typeof o16[n13] == "function" ? ((a21 = t12[n13]) != null || (t12[n13] = []), t12[n13].push(o16[n13])) : r16[n13] = o16[n13];
    if (r16.disabled || r16["aria-disabled"]) for (let o16 in t12) /^(on(?:Click|Pointer|Mouse|Key)(?:Down|Up|Press)?)$/.test(o16) && (t12[o16] = [(n13) => {
      var i13;
      return (i13 = n13 == null ? void 0 : n13.preventDefault) == null ? void 0 : i13.call(n13);
    }]);
    for (let o16 in t12) Object.assign(r16, { [o16](n13, ...i13) {
      let l13 = t12[o16];
      for (let s15 of l13) {
        if ((n13 instanceof Event || (n13 == null ? void 0 : n13.nativeEvent) instanceof Event) && n13.defaultPrevented) return;
        s15(n13, ...i13);
      }
    } });
    return r16;
  }
  function V(...e8) {
    var a21;
    if (e8.length === 0) return {};
    if (e8.length === 1) return e8[0];
    let r16 = {}, t12 = {};
    for (let o16 of e8) for (let n13 in o16) n13.startsWith("on") && typeof o16[n13] == "function" ? ((a21 = t12[n13]) != null || (t12[n13] = []), t12[n13].push(o16[n13])) : r16[n13] = o16[n13];
    for (let o16 in t12) Object.assign(r16, { [o16](...n13) {
      let i13 = t12[o16];
      for (let l13 of i13) l13 == null || l13(...n13);
    } });
    return r16;
  }
  function Y(e8) {
    var r16;
    return Object.assign((0, import_react39.forwardRef)(e8), { displayName: (r16 = e8.displayName) != null ? r16 : e8.name });
  }
  function m3(e8) {
    let r16 = Object.assign({}, e8);
    for (let t12 in r16) r16[t12] === void 0 && delete r16[t12];
    return r16;
  }
  function h(e8, r16 = []) {
    let t12 = Object.assign({}, e8);
    for (let a21 of r16) a21 in t12 && delete t12[a21];
    return t12;
  }
  function D(e8) {
    return import_react39.default.version.split(".")[0] >= "19" ? e8.props.ref : e8.ref;
  }
  function E2(e8) {
    if (e8 != null && e8.$$typeof === /* @__PURE__ */ Symbol.for("react.lazy")) {
      let r16 = e8._payload;
      if (r16 != null && r16.status === "fulfilled") return E2(r16.value);
    }
    return e8;
  }
  function b(e8) {
    return e8 === import_react39.Fragment || e8 === /* @__PURE__ */ Symbol.for("react.fragment");
  }
  function L(e8) {
    return b(e8.type);
  }

  // frontend/node_modules/@headlessui/react/dist/hooks/use-id.js
  init_define_import_meta_env();
  var import_react40 = __toESM(require_react_shim(), 1);

  // frontend/node_modules/@headlessui/react/dist/internal/hidden.js
  init_define_import_meta_env();
  var a4 = "span";
  var s4 = ((e8) => (e8[e8.None = 1] = "None", e8[e8.Focusable = 2] = "Focusable", e8[e8.Hidden = 4] = "Hidden", e8))(s4 || {});
  function l2(t12, r16) {
    var n13;
    let { features: d9 = 1, ...e8 } = t12, o16 = { ref: r16, "aria-hidden": (d9 & 2) === 2 ? true : (n13 = e8["aria-hidden"]) != null ? n13 : void 0, hidden: (d9 & 4) === 4 ? true : void 0, style: { position: "fixed", top: 1, left: 1, width: 1, height: 0, padding: 0, margin: -1, overflow: "hidden", clip: "rect(0, 0, 0, 0)", whiteSpace: "nowrap", borderWidth: "0", ...(d9 & 4) === 4 && (d9 & 2) !== 2 && { display: "none" } } };
    return K()({ ourProps: o16, theirProps: e8, slot: {}, defaultTag: a4, name: "Hidden" });
  }
  var f2 = Y(l2);

  // frontend/node_modules/@headlessui/react/dist/internal/id.js
  init_define_import_meta_env();
  var import_react41 = __toESM(require_react_shim(), 1);
  var e3 = (0, import_react41.createContext)(void 0);
  function u2() {
    return (0, import_react41.useContext)(e3);
  }

  // frontend/node_modules/@headlessui/react/dist/utils/bugs.js
  init_define_import_meta_env();

  // frontend/node_modules/@headlessui/react/dist/utils/dom.js
  init_define_import_meta_env();
  function o5(e8) {
    return typeof e8 != "object" || e8 === null ? false : "nodeType" in e8;
  }
  function t5(e8) {
    return o5(e8) && "tagName" in e8;
  }
  function n5(e8) {
    return t5(e8) && "accessKey" in e8;
  }
  function i4(e8) {
    return t5(e8) && "tabIndex" in e8;
  }
  function r6(e8) {
    return t5(e8) && "style" in e8;
  }
  function u3(e8) {
    return n5(e8) && e8.nodeName === "IFRAME";
  }
  function l3(e8) {
    return n5(e8) && e8.nodeName === "INPUT";
  }
  function m4(e8) {
    return n5(e8) && e8.nodeName === "LABEL";
  }
  function a5(e8) {
    return n5(e8) && e8.nodeName === "FIELDSET";
  }
  function E3(e8) {
    return n5(e8) && e8.nodeName === "LEGEND";
  }
  function L2(e8) {
    return t5(e8) ? e8.matches('a[href],audio[controls],button,details,embed,iframe,img[usemap],input:not([type="hidden"]),label,select,textarea,video[controls]') : false;
  }

  // frontend/node_modules/@headlessui/react/dist/utils/bugs.js
  function s5(l13) {
    let e8 = l13.parentElement, t12 = null;
    for (; e8 && !a5(e8); ) E3(e8) && (t12 = e8), e8 = e8.parentElement;
    let i13 = (e8 == null ? void 0 : e8.getAttribute("disabled")) === "";
    return i13 && r7(t12) ? false : i13;
  }
  function r7(l13) {
    if (!l13) return false;
    let e8 = l13.previousElementSibling;
    for (; e8 !== null; ) {
      if (E3(e8)) return false;
      e8 = e8.previousElementSibling;
    }
    return true;
  }

  // frontend/node_modules/@headlessui/react/dist/components/description/description.js
  init_define_import_meta_env();
  var import_react43 = __toESM(require_react_shim(), 1);

  // frontend/node_modules/@headlessui/react/dist/hooks/use-sync-refs.js
  init_define_import_meta_env();
  var import_react42 = __toESM(require_react_shim(), 1);
  var u4 = /* @__PURE__ */ Symbol();
  function T(t12, n13 = true) {
    return Object.assign(t12, { [u4]: n13 });
  }
  function y(...t12) {
    let n13 = (0, import_react42.useRef)(t12);
    (0, import_react42.useEffect)(() => {
      n13.current = t12;
    }, [t12]);
    let c14 = o4((e8) => {
      for (let o16 of n13.current) o16 != null && (typeof o16 == "function" ? o16(e8) : o16.current = e8);
    });
    return t12.every((e8) => e8 == null || (e8 == null ? void 0 : e8[u4])) ? void 0 : c14;
  }

  // frontend/node_modules/@headlessui/react/dist/components/description/description.js
  var a6 = (0, import_react43.createContext)(null);
  a6.displayName = "DescriptionContext";
  function f3() {
    let r16 = (0, import_react43.useContext)(a6);
    if (r16 === null) {
      let e8 = new Error("You used a <Description /> component, but it is not inside a relevant parent.");
      throw Error.captureStackTrace && Error.captureStackTrace(e8, f3), e8;
    }
    return r16;
  }
  function H2() {
    let [r16, e8] = (0, import_react43.useState)([]);
    return [r16.length > 0 ? r16.join(" ") : void 0, (0, import_react43.useMemo)(() => function(t12) {
      let i13 = o4((n13) => (e8((o16) => [...o16, n13]), () => e8((o16) => {
        let s15 = o16.slice(), p8 = s15.indexOf(n13);
        return p8 !== -1 && s15.splice(p8, 1), s15;
      }))), l13 = (0, import_react43.useMemo)(() => ({ register: i13, slot: t12.slot, name: t12.name, props: t12.props, value: t12.value }), [i13, t12.slot, t12.name, t12.props, t12.value]);
      return import_react43.default.createElement(a6.Provider, { value: l13 }, t12.children);
    }, [e8])];
  }
  var I2 = "p";
  function C2(r16, e8) {
    let c14 = (0, import_react40.useId)(), t12 = a3(), { id: i13 = `headlessui-description-${c14}`, ...l13 } = r16, n13 = f3(), o16 = y(e8);
    n(() => n13.register(i13), [i13, n13.register]);
    let s15 = n2({ ...n13.slot, disabled: t12 || false }), p8 = { ref: o16, ...n13.props, id: i13 };
    return K()({ ourProps: p8, theirProps: l13, slot: s15, defaultTag: I2, name: n13.name || "Description" });
  }
  var _ = Y(C2);
  var M2 = Object.assign(_, {});

  // frontend/node_modules/@headlessui/react/dist/components/keyboard.js
  init_define_import_meta_env();
  var o6 = ((r16) => (r16.Space = " ", r16.Enter = "Enter", r16.Escape = "Escape", r16.Backspace = "Backspace", r16.Delete = "Delete", r16.ArrowLeft = "ArrowLeft", r16.ArrowUp = "ArrowUp", r16.ArrowRight = "ArrowRight", r16.ArrowDown = "ArrowDown", r16.Home = "Home", r16.End = "End", r16.PageUp = "PageUp", r16.PageDown = "PageDown", r16.Tab = "Tab", r16))(o6 || {});

  // frontend/node_modules/@headlessui/react/dist/components/label/label.js
  init_define_import_meta_env();
  var import_react44 = __toESM(require_react_shim(), 1);
  var L3 = (0, import_react44.createContext)(null);
  L3.displayName = "LabelContext";
  function C3() {
    let n13 = (0, import_react44.useContext)(L3);
    if (n13 === null) {
      let l13 = new Error("You used a <Label /> component, but it is not inside a relevant parent.");
      throw Error.captureStackTrace && Error.captureStackTrace(l13, C3), l13;
    }
    return n13;
  }
  function N(n13) {
    var a21, e8, o16;
    let l13 = (e8 = (a21 = (0, import_react44.useContext)(L3)) == null ? void 0 : a21.value) != null ? e8 : void 0;
    return ((o16 = n13 == null ? void 0 : n13.length) != null ? o16 : 0) > 0 ? [l13, ...n13].filter(Boolean).join(" ") : l13;
  }
  function V2({ inherit: n13 = false } = {}) {
    let l13 = N(), [a21, e8] = (0, import_react44.useState)([]), o16 = n13 ? [l13, ...a21].filter(Boolean) : a21;
    return [o16.length > 0 ? o16.join(" ") : void 0, (0, import_react44.useMemo)(() => function(t12) {
      let p8 = o4((i13) => (e8((u16) => [...u16, i13]), () => e8((u16) => {
        let d9 = u16.slice(), f13 = d9.indexOf(i13);
        return f13 !== -1 && d9.splice(f13, 1), d9;
      }))), b8 = (0, import_react44.useMemo)(() => ({ register: p8, slot: t12.slot, name: t12.name, props: t12.props, value: t12.value }), [p8, t12.slot, t12.name, t12.props, t12.value]);
      return import_react44.default.createElement(L3.Provider, { value: b8 }, t12.children);
    }, [e8])];
  }
  var G = "label";
  function U2(n13, l13) {
    var y6;
    let a21 = (0, import_react40.useId)(), e8 = C3(), o16 = u2(), T9 = a3(), { id: t12 = `headlessui-label-${a21}`, htmlFor: p8 = o16 != null ? o16 : (y6 = e8.props) == null ? void 0 : y6.htmlFor, passive: b8 = false, ...i13 } = n13, u16 = y(l13);
    n(() => e8.register(t12), [t12, e8.register]);
    let d9 = o4((s15) => {
      let g3 = s15.currentTarget;
      if (!(s15.target !== s15.currentTarget && L2(s15.target)) && (m4(g3) && s15.preventDefault(), e8.props && "onClick" in e8.props && typeof e8.props.onClick == "function" && e8.props.onClick(s15), m4(g3))) {
        let r16 = document.getElementById(g3.htmlFor);
        if (r16) {
          let E9 = r16.getAttribute("disabled");
          if (E9 === "true" || E9 === "") return;
          let x9 = r16.getAttribute("aria-disabled");
          if (x9 === "true" || x9 === "") return;
          (l3(r16) && (r16.type === "file" || r16.type === "radio" || r16.type === "checkbox") || r16.role === "radio" || r16.role === "checkbox" || r16.role === "switch") && r16.click(), r16.focus({ preventScroll: true });
        }
      }
    }), f13 = n2({ ...e8.slot, disabled: T9 || false }), c14 = { ref: u16, ...e8.props, id: t12, htmlFor: p8, onClick: d9 };
    return b8 && ("onClick" in c14 && (delete c14.htmlFor, delete c14.onClick), "onClick" in i13 && delete i13.onClick), K()({ ourProps: c14, theirProps: i13, slot: f13, defaultTag: p8 ? G : "div", name: e8.name || "Label" });
  }
  var j2 = Y(U2);
  var Z = Object.assign(j2, {});

  // frontend/node_modules/@headlessui/react/dist/internal/close-provider.js
  init_define_import_meta_env();
  var import_react45 = __toESM(require_react_shim(), 1);
  var e4 = (0, import_react45.createContext)(() => {
  });
  function C4({ value: t12, children: o16 }) {
    return import_react45.default.createElement(e4.Provider, { value: t12 }, o16);
  }

  // frontend/node_modules/@headlessui/react/dist/hooks/use-element-size.js
  init_define_import_meta_env();
  var import_react46 = __toESM(require_react_shim(), 1);
  function h3(i13) {
    if (i13 === null) return { width: 0, height: 0 };
    let { width: t12, height: e8 } = i13.getBoundingClientRect();
    return { width: t12, height: e8 };
  }
  function w3(i13, t12, e8 = false) {
    let [r16, f13] = (0, import_react46.useState)(() => h3(t12));
    return n(() => {
      if (!t12 || !i13) return;
      let n13 = o2();
      return n13.requestAnimationFrame(function s15() {
        n13.requestAnimationFrame(s15), f13((u16) => {
          let o16 = h3(t12);
          return o16.width === u16.width && o16.height === u16.height ? u16 : o16;
        });
      }), () => {
        n13.dispose();
      };
    }, [t12, i13]), e8 ? { width: `${r16.width}px`, height: `${r16.height}px` } : r16;
  }

  // frontend/node_modules/@headlessui/react/dist/hooks/use-handle-toggle.js
  init_define_import_meta_env();
  var import_react47 = __toESM(require_react_shim(), 1);

  // frontend/node_modules/@headlessui/react/dist/components/mouse.js
  init_define_import_meta_env();
  var g = ((f13) => (f13[f13.Left = 0] = "Left", f13[f13.Right = 2] = "Right", f13))(g || {});

  // frontend/node_modules/@headlessui/react/dist/hooks/use-handle-toggle.js
  function s6(t12) {
    let r16 = (0, import_react47.useRef)(null), u16 = o4((e8) => {
      r16.current = e8.pointerType, !s5(e8.currentTarget) && e8.pointerType === "mouse" && e8.button === g.Left && (e8.preventDefault(), t12(e8));
    }), i13 = o4((e8) => {
      r16.current !== "mouse" && (s5(e8.currentTarget) || t12(e8));
    });
    return { onPointerDown: u16, onClick: i13 };
  }

  // frontend/node_modules/@headlessui/react/dist/hooks/use-inert-others.js
  init_define_import_meta_env();

  // frontend/node_modules/@headlessui/react/dist/hooks/use-is-top-layer.js
  init_define_import_meta_env();
  var import_react48 = __toESM(require_react_shim(), 1);

  // frontend/node_modules/@headlessui/react/dist/machines/stack-machine.js
  init_define_import_meta_env();

  // frontend/node_modules/@headlessui/react/dist/machine.js
  init_define_import_meta_env();

  // frontend/node_modules/@headlessui/react/dist/utils/default-map.js
  init_define_import_meta_env();
  var a7 = class extends Map {
    constructor(t12) {
      super();
      this.factory = t12;
    }
    get(t12) {
      let e8 = super.get(t12);
      return e8 === void 0 && (e8 = this.factory(t12), this.set(t12, e8)), e8;
    }
  };

  // frontend/node_modules/@headlessui/react/dist/machine.js
  var h4 = Object.defineProperty;
  var v3 = (t12, e8, r16) => e8 in t12 ? h4(t12, e8, { enumerable: true, configurable: true, writable: true, value: r16 }) : t12[e8] = r16;
  var S2 = (t12, e8, r16) => (v3(t12, typeof e8 != "symbol" ? e8 + "" : e8, r16), r16);
  var b2 = (t12, e8, r16) => {
    if (!e8.has(t12)) throw TypeError("Cannot " + r16);
  };
  var i7 = (t12, e8, r16) => (b2(t12, e8, "read from private field"), r16 ? r16.call(t12) : e8.get(t12));
  var c2 = (t12, e8, r16) => {
    if (e8.has(t12)) throw TypeError("Cannot add the same private member more than once");
    e8 instanceof WeakSet ? e8.add(t12) : e8.set(t12, r16);
  };
  var u6 = (t12, e8, r16, s15) => (b2(t12, e8, "write to private field"), s15 ? s15.call(t12, r16) : e8.set(t12, r16), r16);
  var n7;
  var a8;
  var o7;
  var T3 = class {
    constructor(e8) {
      c2(this, n7, {});
      c2(this, a8, new a7(() => /* @__PURE__ */ new Set()));
      c2(this, o7, /* @__PURE__ */ new Set());
      S2(this, "disposables", o2());
      u6(this, n7, e8), s.isServer && this.disposables.microTask(() => {
        this.dispose();
      });
    }
    dispose() {
      this.disposables.dispose();
    }
    get state() {
      return i7(this, n7);
    }
    subscribe(e8, r16) {
      if (s.isServer) return () => {
      };
      let s15 = { selector: e8, callback: r16, current: e8(i7(this, n7)) };
      return i7(this, o7).add(s15), this.disposables.add(() => {
        i7(this, o7).delete(s15);
      });
    }
    on(e8, r16) {
      return s.isServer ? () => {
      } : (i7(this, a8).get(e8).add(r16), this.disposables.add(() => {
        i7(this, a8).get(e8).delete(r16);
      }));
    }
    send(e8) {
      let r16 = this.reduce(i7(this, n7), e8);
      if (r16 !== i7(this, n7)) {
        u6(this, n7, r16);
        for (let s15 of i7(this, o7)) {
          let l13 = s15.selector(i7(this, n7));
          j3(s15.current, l13) || (s15.current = l13, s15.callback(l13));
        }
        for (let s15 of i7(this, a8).get(e8.type)) s15(i7(this, n7), e8);
      }
    }
  };
  n7 = /* @__PURE__ */ new WeakMap(), a8 = /* @__PURE__ */ new WeakMap(), o7 = /* @__PURE__ */ new WeakMap();
  function j3(t12, e8) {
    return Object.is(t12, e8) ? true : typeof t12 != "object" || t12 === null || typeof e8 != "object" || e8 === null ? false : Array.isArray(t12) && Array.isArray(e8) ? t12.length !== e8.length ? false : f4(t12[Symbol.iterator](), e8[Symbol.iterator]()) : t12 instanceof Map && e8 instanceof Map || t12 instanceof Set && e8 instanceof Set ? t12.size !== e8.size ? false : f4(t12.entries(), e8.entries()) : p2(t12) && p2(e8) ? f4(Object.entries(t12)[Symbol.iterator](), Object.entries(e8)[Symbol.iterator]()) : false;
  }
  function f4(t12, e8) {
    do {
      let r16 = t12.next(), s15 = e8.next();
      if (r16.done && s15.done) return true;
      if (r16.done || s15.done || !Object.is(r16.value, s15.value)) return false;
    } while (true);
  }
  function p2(t12) {
    if (Object.prototype.toString.call(t12) !== "[object Object]") return false;
    let e8 = Object.getPrototypeOf(t12);
    return e8 === null || Object.getPrototypeOf(e8) === null;
  }
  function k3(t12) {
    let [e8, r16] = t12(), s15 = o2();
    return (...l13) => {
      e8(...l13), s15.dispose(), s15.microTask(r16);
    };
  }

  // frontend/node_modules/@headlessui/react/dist/machines/stack-machine.js
  var a9 = Object.defineProperty;
  var r9 = (e8, c14, t12) => c14 in e8 ? a9(e8, c14, { enumerable: true, configurable: true, writable: true, value: t12 }) : e8[c14] = t12;
  var p3 = (e8, c14, t12) => (r9(e8, typeof c14 != "symbol" ? c14 + "" : c14, t12), t12);
  var k4 = ((t12) => (t12[t12.Push = 0] = "Push", t12[t12.Pop = 1] = "Pop", t12))(k4 || {});
  var y2 = { [0](e8, c14) {
    let t12 = c14.id, s15 = e8.stack, i13 = e8.stack.indexOf(t12);
    if (i13 !== -1) {
      let n13 = e8.stack.slice();
      return n13.splice(i13, 1), n13.push(t12), s15 = n13, { ...e8, stack: s15 };
    }
    return { ...e8, stack: [...e8.stack, t12] };
  }, [1](e8, c14) {
    let t12 = c14.id, s15 = e8.stack.indexOf(t12);
    if (s15 === -1) return e8;
    let i13 = e8.stack.slice();
    return i13.splice(s15, 1), { ...e8, stack: i13 };
  } };
  var o8 = class _o extends T3 {
    constructor() {
      super(...arguments);
      p3(this, "actions", { push: (t12) => this.send({ type: 0, id: t12 }), pop: (t12) => this.send({ type: 1, id: t12 }) });
      p3(this, "selectors", { isTop: (t12, s15) => t12.stack[t12.stack.length - 1] === s15, inStack: (t12, s15) => t12.stack.includes(s15) });
    }
    static new() {
      return new _o({ stack: [] });
    }
    reduce(t12, s15) {
      return u(s15.type, y2, t12, s15);
    }
  };
  var x2 = new a7(() => o8.new());

  // frontend/node_modules/@headlessui/react/dist/react-glue.js
  init_define_import_meta_env();
  var import_with_selector = __toESM(require_with_selector(), 1);
  function S3(e8, n13, r16 = j3) {
    return (0, import_with_selector.useSyncExternalStoreWithSelector)(o4((i13) => e8.subscribe(s7, i13)), o4(() => e8.state), o4(() => e8.state), o4(n13), r16);
  }
  function s7(e8) {
    return e8;
  }

  // frontend/node_modules/@headlessui/react/dist/hooks/use-is-top-layer.js
  function I3(o16, s15) {
    let t12 = (0, import_react48.useId)(), r16 = x2.get(s15), [i13, c14] = S3(r16, (0, import_react48.useCallback)((e8) => [r16.selectors.isTop(e8, t12), r16.selectors.inStack(e8, t12)], [r16, t12]));
    return n(() => {
      if (o16) return r16.actions.push(t12), () => r16.actions.pop(t12);
    }, [r16, o16, t12]), o16 ? c14 ? i13 : true : false;
  }

  // frontend/node_modules/@headlessui/react/dist/hooks/use-inert-others.js
  var f5 = /* @__PURE__ */ new Map();
  var u8 = /* @__PURE__ */ new Map();
  function h5(t12) {
    var e8;
    let r16 = (e8 = u8.get(t12)) != null ? e8 : 0;
    return u8.set(t12, r16 + 1), r16 !== 0 ? () => m6(t12) : (f5.set(t12, { "aria-hidden": t12.getAttribute("aria-hidden"), inert: t12.inert }), t12.setAttribute("aria-hidden", "true"), t12.inert = true, () => m6(t12));
  }
  function m6(t12) {
    var i13;
    let r16 = (i13 = u8.get(t12)) != null ? i13 : 1;
    if (r16 === 1 ? u8.delete(t12) : u8.set(t12, r16 - 1), r16 !== 1) return;
    let e8 = f5.get(t12);
    e8 && (e8["aria-hidden"] === null ? t12.removeAttribute("aria-hidden") : t12.setAttribute("aria-hidden", e8["aria-hidden"]), t12.inert = e8.inert, f5.delete(t12));
  }
  function y3(t12, { allowed: r16, disallowed: e8 } = {}) {
    let i13 = I3(t12, "inert-others");
    n(() => {
      var d9, c14;
      if (!i13) return;
      let a21 = o2();
      for (let n13 of (d9 = e8 == null ? void 0 : e8()) != null ? d9 : []) n13 && a21.add(h5(n13));
      let s15 = (c14 = r16 == null ? void 0 : r16()) != null ? c14 : [];
      for (let n13 of s15) {
        if (!n13) continue;
        let l13 = l(n13);
        if (!l13) continue;
        let o16 = n13.parentElement;
        for (; o16 && o16 !== l13.body; ) {
          for (let p8 of o16.children) s15.some((E9) => p8.contains(E9)) || a21.add(h5(p8));
          o16 = o16.parentElement;
        }
      }
      return a21.dispose;
    }, [i13, r16, e8]);
  }

  // frontend/node_modules/@headlessui/react/dist/hooks/use-on-disappear.js
  init_define_import_meta_env();
  var import_react49 = __toESM(require_react_shim(), 1);
  function p4(s15, n13, o16) {
    let i13 = s3((t12) => {
      let e8 = t12.getBoundingClientRect();
      e8.x === 0 && e8.y === 0 && e8.width === 0 && e8.height === 0 && o16();
    });
    (0, import_react49.useEffect)(() => {
      if (!s15) return;
      let t12 = n13 === null ? null : n5(n13) ? n13 : n13.current;
      if (!t12) return;
      let e8 = o2();
      if (typeof ResizeObserver != "undefined") {
        let r16 = new ResizeObserver(() => i13.current(t12));
        r16.observe(t12), e8.add(() => r16.disconnect());
      }
      if (typeof IntersectionObserver != "undefined") {
        let r16 = new IntersectionObserver(() => i13.current(t12));
        r16.observe(t12), e8.add(() => r16.disconnect());
      }
      return () => e8.dispose();
    }, [n13, i13, s15]);
  }

  // frontend/node_modules/@headlessui/react/dist/hooks/use-outside-click.js
  init_define_import_meta_env();
  var import_react52 = __toESM(require_react_shim(), 1);

  // frontend/node_modules/@headlessui/react/dist/utils/focus-management.js
  init_define_import_meta_env();
  var E4 = ["[contentEditable=true]", "[tabindex]", "a[href]", "area[href]", "button:not([disabled])", "iframe", "input:not([disabled])", "select:not([disabled])", "details>summary", "textarea:not([disabled])"].map((e8) => `${e8}:not([tabindex='-1'])`).join(",");
  var S4 = ["[data-autofocus]"].map((e8) => `${e8}:not([tabindex='-1'])`).join(",");
  var T4 = ((o16) => (o16[o16.First = 1] = "First", o16[o16.Previous = 2] = "Previous", o16[o16.Next = 4] = "Next", o16[o16.Last = 8] = "Last", o16[o16.WrapAround = 16] = "WrapAround", o16[o16.NoScroll = 32] = "NoScroll", o16[o16.AutoFocus = 64] = "AutoFocus", o16))(T4 || {});
  var A2 = ((n13) => (n13[n13.Error = 0] = "Error", n13[n13.Overflow = 1] = "Overflow", n13[n13.Success = 2] = "Success", n13[n13.Underflow = 3] = "Underflow", n13))(A2 || {});
  var O2 = ((t12) => (t12[t12.Previous = -1] = "Previous", t12[t12.Next = 1] = "Next", t12))(O2 || {});
  function x3(e8 = document.body) {
    return e8 == null ? [] : Array.from(e8.querySelectorAll(E4)).sort((r16, t12) => Math.sign((r16.tabIndex || Number.MAX_SAFE_INTEGER) - (t12.tabIndex || Number.MAX_SAFE_INTEGER)));
  }
  function h6(e8 = document.body) {
    return e8 == null ? [] : Array.from(e8.querySelectorAll(S4)).sort((r16, t12) => Math.sign((r16.tabIndex || Number.MAX_SAFE_INTEGER) - (t12.tabIndex || Number.MAX_SAFE_INTEGER)));
  }
  var I4 = ((t12) => (t12[t12.Strict = 0] = "Strict", t12[t12.Loose = 1] = "Loose", t12))(I4 || {});
  function H3(e8, r16 = 0) {
    var t12;
    return e8 === ((t12 = l(e8)) == null ? void 0 : t12.body) ? false : u(r16, { [0]() {
      return e8.matches(E4);
    }, [1]() {
      let l13 = e8;
      for (; l13 !== null; ) {
        if (l13.matches(E4)) return true;
        l13 = l13.parentElement;
      }
      return false;
    } });
  }
  function K2(e8) {
    o2().nextFrame(() => {
      let r16 = e(e8);
      r16 && i4(r16) && !H3(r16, 0) && w4(e8);
    });
  }
  var g2 = ((t12) => (t12[t12.Keyboard = 0] = "Keyboard", t12[t12.Mouse = 1] = "Mouse", t12))(g2 || {});
  typeof window != "undefined" && typeof document != "undefined" && (document.addEventListener("keydown", (e8) => {
    e8.metaKey || e8.altKey || e8.ctrlKey || (document.documentElement.dataset.headlessuiFocusVisible = "");
  }, true), document.addEventListener("click", (e8) => {
    e8.detail === 1 ? delete document.documentElement.dataset.headlessuiFocusVisible : e8.detail === 0 && (document.documentElement.dataset.headlessuiFocusVisible = "");
  }, true));
  function w4(e8) {
    e8 == null || e8.focus({ preventScroll: true });
  }
  var _2 = ["textarea", "input"].join(",");
  function P3(e8) {
    var r16, t12;
    return (t12 = (r16 = e8 == null ? void 0 : e8.matches) == null ? void 0 : r16.call(e8, _2)) != null ? t12 : false;
  }
  function G2(e8, r16 = (t12) => t12) {
    return e8.slice().sort((t12, l13) => {
      let n13 = r16(t12), a21 = r16(l13);
      if (n13 === null || a21 === null) return 0;
      let u16 = n13.compareDocumentPosition(a21);
      return u16 & Node.DOCUMENT_POSITION_FOLLOWING ? -1 : u16 & Node.DOCUMENT_POSITION_PRECEDING ? 1 : 0;
    });
  }
  function R2(e8, r16, t12 = e8 === null ? document.body : r3(e8)) {
    return v4(x3(t12), r16, { relativeTo: e8 });
  }
  function v4(e8, r16, { sorted: t12 = true, relativeTo: l13 = null, skipElements: n13 = [] } = {}) {
    let a21 = Array.isArray(e8) ? e8.length > 0 ? r3(e8[0]) : document : r3(e8), u16 = Array.isArray(e8) ? t12 ? G2(e8) : e8 : r16 & 64 ? h6(e8) : x3(e8);
    n13.length > 0 && u16.length > 1 && (u16 = u16.filter((i13) => !n13.some((d9) => d9 != null && "current" in d9 ? (d9 == null ? void 0 : d9.current) === i13 : d9 === i13))), l13 = l13 != null ? l13 : a21 == null ? void 0 : a21.activeElement;
    let o16 = (() => {
      if (r16 & 5) return 1;
      if (r16 & 10) return -1;
      throw new Error("Missing Focus.First, Focus.Previous, Focus.Next or Focus.Last");
    })(), M8 = (() => {
      if (r16 & 1) return 0;
      if (r16 & 2) return Math.max(0, u16.indexOf(l13)) - 1;
      if (r16 & 4) return Math.max(0, u16.indexOf(l13)) + 1;
      if (r16 & 8) return u16.length - 1;
      throw new Error("Missing Focus.First, Focus.Previous, Focus.Next or Focus.Last");
    })(), N3 = r16 & 32 ? { preventScroll: true } : {}, m9 = 0, c14 = u16.length, s15;
    do {
      if (m9 >= c14 || m9 + c14 <= 0) return 0;
      let i13 = M8 + m9;
      if (r16 & 16) i13 = (i13 + c14) % c14;
      else {
        if (i13 < 0) return 3;
        if (i13 >= c14) return 1;
      }
      s15 = u16[i13], s15 == null || s15.focus(N3), m9 += o16;
    } while (s15 !== e(s15));
    return r16 & 6 && P3(s15) && s15.select(), 2;
  }

  // frontend/node_modules/@headlessui/react/dist/utils/platform.js
  init_define_import_meta_env();
  function t6() {
    return /iPhone/gi.test(window.navigator.platform) || /Mac/gi.test(window.navigator.platform) && window.navigator.maxTouchPoints > 0;
  }
  function i8() {
    return /Android/gi.test(window.navigator.userAgent);
  }
  function n9() {
    return t6() || i8();
  }

  // frontend/node_modules/@headlessui/react/dist/hooks/use-document-event.js
  init_define_import_meta_env();
  var import_react50 = __toESM(require_react_shim(), 1);
  function i9(t12, e8, o16, n13) {
    let u16 = s3(o16);
    (0, import_react50.useEffect)(() => {
      if (!t12) return;
      function r16(m9) {
        u16.current(m9);
      }
      return document.addEventListener(e8, r16, n13), () => document.removeEventListener(e8, r16, n13);
    }, [t12, e8, n13]);
  }

  // frontend/node_modules/@headlessui/react/dist/hooks/use-window-event.js
  init_define_import_meta_env();
  var import_react51 = __toESM(require_react_shim(), 1);
  function s8(t12, e8, o16, n13) {
    let i13 = s3(o16);
    (0, import_react51.useEffect)(() => {
      if (!t12) return;
      function r16(d9) {
        i13.current(d9);
      }
      return window.addEventListener(e8, r16, n13), () => window.removeEventListener(e8, r16, n13);
    }, [t12, e8, n13]);
  }

  // frontend/node_modules/@headlessui/react/dist/hooks/use-outside-click.js
  var C5 = 30;
  function k5(o16, f13, h10) {
    let m9 = s3(h10), s15 = (0, import_react52.useCallback)(function(e8, c14) {
      if (e8.defaultPrevented) return;
      let r16 = c14(e8);
      if (r16 === null || !r16.getRootNode().contains(r16) || !r16.isConnected) return;
      let M8 = (function u16(n13) {
        return typeof n13 == "function" ? u16(n13()) : Array.isArray(n13) || n13 instanceof Set ? n13 : [n13];
      })(f13);
      for (let u16 of M8) if (u16 !== null && (u16.contains(r16) || e8.composed && e8.composedPath().includes(u16))) return;
      return !H3(r16, I4.Loose) && r16.tabIndex !== -1 && e8.preventDefault(), m9.current(e8, r16);
    }, [m9, f13]), i13 = (0, import_react52.useRef)(null);
    i9(o16, "pointerdown", (t12) => {
      var e8, c14;
      n9() || (i13.current = ((c14 = (e8 = t12.composedPath) == null ? void 0 : e8.call(t12)) == null ? void 0 : c14[0]) || t12.target);
    }, true), i9(o16, "pointerup", (t12) => {
      if (n9() || !i13.current) return;
      let e8 = i13.current;
      return i13.current = null, s15(t12, () => e8);
    }, true);
    let l13 = (0, import_react52.useRef)({ x: 0, y: 0 });
    i9(o16, "touchstart", (t12) => {
      l13.current.x = t12.touches[0].clientX, l13.current.y = t12.touches[0].clientY;
    }, true), i9(o16, "touchend", (t12) => {
      let e8 = { x: t12.changedTouches[0].clientX, y: t12.changedTouches[0].clientY };
      if (!(Math.abs(e8.x - l13.current.x) >= C5 || Math.abs(e8.y - l13.current.y) >= C5)) return s15(t12, () => i4(t12.target) ? t12.target : null);
    }, true), s8(o16, "blur", (t12) => s15(t12, () => u3(window.document.activeElement) ? window.document.activeElement : null), true);
  }

  // frontend/node_modules/@headlessui/react/dist/hooks/use-owner.js
  init_define_import_meta_env();
  var import_react53 = __toESM(require_react_shim(), 1);
  function u9(...e8) {
    return (0, import_react53.useMemo)(() => l(...e8), [...e8]);
  }

  // frontend/node_modules/@headlessui/react/dist/hooks/use-quick-release.js
  init_define_import_meta_env();
  var import_react54 = __toESM(require_react_shim(), 1);
  var H4 = ((e8) => (e8[e8.Ignore = 0] = "Ignore", e8[e8.Select = 1] = "Select", e8[e8.Close = 2] = "Close", e8))(H4 || {});
  var S5 = { Ignore: { kind: 0 }, Select: (r16) => ({ kind: 1, target: r16 }), Close: { kind: 2 } };
  var M3 = 200;
  var f6 = 5;
  function L4(r16, { trigger: n13, action: T9, close: e8, select: p8 }) {
    let l13 = (0, import_react54.useRef)(null), i13 = (0, import_react54.useRef)(null), u16 = (0, import_react54.useRef)(null);
    i9(r16 && n13 !== null, "pointerdown", (t12) => {
      o5(t12 == null ? void 0 : t12.target) && n13 != null && n13.contains(t12.target) && (i13.current = t12.x, u16.current = t12.y, l13.current = t12.timeStamp);
    }), i9(r16 && n13 !== null, "pointerup", (t12) => {
      var s15, m9;
      let c14 = l13.current;
      if (c14 === null || (l13.current = null, !i4(t12.target)) || Math.abs(t12.x - ((s15 = i13.current) != null ? s15 : t12.x)) < f6 && Math.abs(t12.y - ((m9 = u16.current) != null ? m9 : t12.y)) < f6) return;
      let a21 = T9(t12);
      switch (a21.kind) {
        case 0:
          return;
        case 1: {
          t12.timeStamp - c14 > M3 && (p8(a21.target), e8());
          break;
        }
        case 2: {
          e8();
          break;
        }
      }
    }, { capture: true });
  }

  // frontend/node_modules/@headlessui/react/dist/hooks/use-event-listener.js
  init_define_import_meta_env();
  var import_react55 = __toESM(require_react_shim(), 1);
  function E6(n13, e8, a21, t12) {
    let i13 = s3(a21);
    (0, import_react55.useEffect)(() => {
      n13 = n13 != null ? n13 : window;
      function r16(o16) {
        i13.current(o16);
      }
      return n13.addEventListener(e8, r16, t12), () => n13.removeEventListener(e8, r16, t12);
    }, [n13, e8, t12]);
  }

  // frontend/node_modules/@headlessui/react/dist/hooks/use-resolve-button-type.js
  init_define_import_meta_env();
  var import_react56 = __toESM(require_react_shim(), 1);
  function e5(t12, u16) {
    return (0, import_react56.useMemo)(() => {
      var n13;
      if (t12.type) return t12.type;
      let r16 = (n13 = t12.as) != null ? n13 : "button";
      if (typeof r16 == "string" && r16.toLowerCase() === "button" || (u16 == null ? void 0 : u16.tagName) === "BUTTON" && !u16.hasAttribute("type")) return "button";
    }, [t12.type, t12.as, u16]);
  }

  // frontend/node_modules/@headlessui/react/dist/hooks/use-scroll-lock.js
  init_define_import_meta_env();

  // frontend/node_modules/@headlessui/react/dist/hooks/document-overflow/use-document-overflow.js
  init_define_import_meta_env();

  // frontend/node_modules/@headlessui/react/dist/hooks/use-store.js
  init_define_import_meta_env();
  var import_react57 = __toESM(require_react_shim(), 1);
  function o11(t12) {
    return (0, import_react57.useSyncExternalStore)(t12.subscribe, t12.getSnapshot, t12.getSnapshot);
  }

  // frontend/node_modules/@headlessui/react/dist/hooks/document-overflow/overflow-store.js
  init_define_import_meta_env();

  // frontend/node_modules/@headlessui/react/dist/utils/store.js
  init_define_import_meta_env();
  function a13(o16, r16) {
    let t12 = o16(), n13 = /* @__PURE__ */ new Set();
    return { getSnapshot() {
      return t12;
    }, subscribe(e8) {
      return n13.add(e8), () => n13.delete(e8);
    }, dispatch(e8, ...s15) {
      let i13 = r16[e8].call(t12, ...s15);
      i13 && (t12 = i13, n13.forEach((c14) => c14()));
    } };
  }

  // frontend/node_modules/@headlessui/react/dist/hooks/document-overflow/adjust-scrollbar-padding.js
  init_define_import_meta_env();
  function d6() {
    let r16;
    return { before({ doc: e8 }) {
      var l13;
      let o16 = e8.documentElement, t12 = (l13 = e8.defaultView) != null ? l13 : window;
      r16 = Math.max(0, t12.innerWidth - o16.clientWidth);
    }, after({ doc: e8, d: o16 }) {
      let t12 = e8.documentElement, l13 = Math.max(0, t12.clientWidth - t12.offsetWidth), n13 = Math.max(0, r16 - l13);
      o16.style(t12, "paddingRight", `${n13}px`);
    } };
  }

  // frontend/node_modules/@headlessui/react/dist/hooks/document-overflow/handle-ios-locking.js
  init_define_import_meta_env();
  function w5() {
    return t6() ? { before({ doc: o16, d: r16, meta: m9 }) {
      function a21(s15) {
        for (let l13 of m9().containers) for (let c14 of l13()) if (c14.contains(s15)) return true;
        return false;
      }
      r16.microTask(() => {
        var c14;
        if (window.getComputedStyle(o16.documentElement).scrollBehavior !== "auto") {
          let t12 = o2();
          t12.style(o16.documentElement, "scrollBehavior", "auto"), r16.add(() => r16.microTask(() => t12.dispose()));
        }
        let s15 = (c14 = window.scrollY) != null ? c14 : window.pageYOffset, l13 = null;
        r16.addEventListener(o16, "click", (t12) => {
          if (i4(t12.target)) try {
            let e8 = t12.target.closest("a");
            if (!e8) return;
            let { hash: n13 } = new URL(e8.href), f13 = o16.querySelector(n13);
            i4(f13) && !a21(f13) && (l13 = f13);
          } catch {
          }
        }, true), r16.group((t12) => {
          r16.addEventListener(o16, "touchstart", (e8) => {
            if (t12.dispose(), i4(e8.target) && r6(e8.target)) if (a21(e8.target)) {
              let n13 = e8.target;
              for (; n13.parentElement && a21(n13.parentElement); ) n13 = n13.parentElement;
              t12.style(n13, "overscrollBehavior", "contain");
            } else t12.style(e8.target, "touchAction", "none");
          });
        }), r16.addEventListener(o16, "touchmove", (t12) => {
          if (i4(t12.target)) {
            if (l3(t12.target)) return;
            if (a21(t12.target)) {
              let e8 = t12.target;
              for (; e8.parentElement && e8.dataset.headlessuiPortal !== "" && !(e8.scrollHeight > e8.clientHeight || e8.scrollWidth > e8.clientWidth); ) e8 = e8.parentElement;
              e8.dataset.headlessuiPortal === "" && t12.preventDefault();
            } else t12.preventDefault();
          }
        }, { passive: false }), r16.add(() => {
          var e8;
          let t12 = (e8 = window.scrollY) != null ? e8 : window.pageYOffset;
          s15 !== t12 && window.scrollTo(0, s15), l13 && l13.isConnected && (l13.scrollIntoView({ block: "nearest" }), l13 = null);
        });
      });
    } } : {};
  }

  // frontend/node_modules/@headlessui/react/dist/hooks/document-overflow/prevent-scroll.js
  init_define_import_meta_env();
  function r10() {
    return { before({ doc: e8, d: o16 }) {
      o16.style(e8.documentElement, "overflow", "hidden");
    } };
  }

  // frontend/node_modules/@headlessui/react/dist/hooks/document-overflow/overflow-store.js
  function r11(e8) {
    let o16 = {};
    for (let t12 of e8) Object.assign(o16, t12(o16));
    return o16;
  }
  var c4 = a13(() => /* @__PURE__ */ new Map(), { PUSH(e8, o16) {
    var n13;
    let t12 = (n13 = this.get(e8)) != null ? n13 : { doc: e8, count: 0, d: o2(), meta: /* @__PURE__ */ new Set(), computedMeta: {} };
    return t12.count++, t12.meta.add(o16), t12.computedMeta = r11(t12.meta), this.set(e8, t12), this;
  }, POP(e8, o16) {
    let t12 = this.get(e8);
    return t12 && (t12.count--, t12.meta.delete(o16), t12.computedMeta = r11(t12.meta)), this;
  }, SCROLL_PREVENT(e8) {
    let o16 = { doc: e8.doc, d: e8.d, meta() {
      return e8.computedMeta;
    } }, t12 = [w5(), d6(), r10()];
    t12.forEach(({ before: n13 }) => n13 == null ? void 0 : n13(o16)), t12.forEach(({ after: n13 }) => n13 == null ? void 0 : n13(o16));
  }, SCROLL_ALLOW({ d: e8 }) {
    e8.dispose();
  }, TEARDOWN({ doc: e8 }) {
    this.delete(e8);
  } });
  c4.subscribe(() => {
    let e8 = c4.getSnapshot(), o16 = /* @__PURE__ */ new Map();
    for (let [t12] of e8) o16.set(t12, t12.documentElement.style.overflow);
    for (let t12 of e8.values()) {
      let n13 = o16.get(t12.doc) === "hidden", a21 = t12.count !== 0;
      (a21 && !n13 || !a21 && n13) && c4.dispatch(t12.count > 0 ? "SCROLL_PREVENT" : "SCROLL_ALLOW", t12), t12.count === 0 && c4.dispatch("TEARDOWN", t12);
    }
  });

  // frontend/node_modules/@headlessui/react/dist/hooks/document-overflow/use-document-overflow.js
  function a14(r16, e8, n13 = () => ({ containers: [] })) {
    let f13 = o11(c4), o16 = e8 ? f13.get(e8) : void 0, i13 = o16 ? o16.count > 0 : false;
    return n(() => {
      if (!(!e8 || !r16)) return c4.dispatch("PUSH", e8, n13), () => c4.dispatch("POP", e8, n13);
    }, [r16, e8]), i13;
  }

  // frontend/node_modules/@headlessui/react/dist/hooks/use-scroll-lock.js
  function f7(e8, c14, n13 = () => [document.body]) {
    let r16 = I3(e8, "scroll-lock");
    a14(r16, c14, (t12) => {
      var o16;
      return { containers: [...(o16 = t12.containers) != null ? o16 : [], n13] };
    });
  }

  // frontend/node_modules/@headlessui/react/dist/hooks/use-tracked-pointer.js
  init_define_import_meta_env();
  var import_react58 = __toESM(require_react_shim(), 1);
  function t7(e8) {
    return [e8.screenX, e8.screenY];
  }
  function u10() {
    let e8 = (0, import_react58.useRef)([-1, -1]);
    return { wasMoved(r16) {
      let n13 = t7(r16);
      return e8.current[0] === n13[0] && e8.current[1] === n13[1] ? false : (e8.current = n13, true);
    }, update(r16) {
      e8.current = t7(r16);
    } };
  }

  // frontend/node_modules/@headlessui/react/dist/hooks/use-transition.js
  init_define_import_meta_env();
  var import_react60 = __toESM(require_react_shim(), 1);

  // frontend/node_modules/@headlessui/react/dist/hooks/use-flags.js
  init_define_import_meta_env();
  var import_react59 = __toESM(require_react_shim(), 1);
  function c5(u16 = 0) {
    let [r16, a21] = (0, import_react59.useState)(u16), g3 = (0, import_react59.useCallback)((e8) => a21(e8), []), s15 = (0, import_react59.useCallback)((e8) => a21((l13) => l13 | e8), []), m9 = (0, import_react59.useCallback)((e8) => (r16 & e8) === e8, [r16]), n13 = (0, import_react59.useCallback)((e8) => a21((l13) => l13 & ~e8), []), F5 = (0, import_react59.useCallback)((e8) => a21((l13) => l13 ^ e8), []);
    return { flags: r16, setFlag: g3, addFlag: s15, hasFlag: m9, removeFlag: n13, toggleFlag: F5 };
  }

  // frontend/node_modules/@headlessui/react/dist/hooks/use-transition.js
  var T6;
  var S6;
  typeof process != "undefined" && typeof globalThis != "undefined" && typeof Element != "undefined" && ((T6 = process == null ? void 0 : process.env) == null ? void 0 : T6["NODE_ENV"]) === "test" && typeof ((S6 = Element == null ? void 0 : Element.prototype) == null ? void 0 : S6.getAnimations) == "undefined" && (Element.prototype.getAnimations = function() {
    return console.warn(["Headless UI has polyfilled `Element.prototype.getAnimations` for your tests.", "Please install a proper polyfill e.g. `jsdom-testing-mocks`, to silence these warnings.", "", "Example usage:", "```js", "import { mockAnimationsApi } from 'jsdom-testing-mocks'", "mockAnimationsApi()", "```"].join(`
`)), [];
  });
  var A3 = ((i13) => (i13[i13.None = 0] = "None", i13[i13.Closed = 1] = "Closed", i13[i13.Enter = 2] = "Enter", i13[i13.Leave = 4] = "Leave", i13))(A3 || {});
  function x4(e8) {
    let r16 = {};
    for (let t12 in e8) e8[t12] === true && (r16[`data-${t12}`] = "");
    return r16;
  }
  function N2(e8, r16, t12, n13) {
    let [i13, a21] = (0, import_react60.useState)(t12), { hasFlag: s15, addFlag: o16, removeFlag: l13 } = c5(e8 && i13 ? 3 : 0), u16 = (0, import_react60.useRef)(false), f13 = (0, import_react60.useRef)(false), E9 = p();
    return n(() => {
      var d9;
      if (e8) {
        if (t12 && a21(true), !r16) {
          t12 && o16(3);
          return;
        }
        return (d9 = n13 == null ? void 0 : n13.start) == null || d9.call(n13, t12), C6(r16, { inFlight: u16, prepare() {
          f13.current ? f13.current = false : f13.current = u16.current, u16.current = true, !f13.current && (t12 ? (o16(3), l13(4)) : (o16(4), l13(2)));
        }, run() {
          f13.current ? t12 ? (l13(3), o16(4)) : (l13(4), o16(3)) : t12 ? l13(1) : o16(1);
        }, done() {
          var p8;
          f13.current && D4(r16) || (u16.current = false, l13(7), t12 || a21(false), (p8 = n13 == null ? void 0 : n13.end) == null || p8.call(n13, t12));
        } });
      }
    }, [e8, t12, r16, E9]), e8 ? [i13, { closed: s15(1), enter: s15(2), leave: s15(4), transition: s15(2) || s15(4) }] : [t12, { closed: void 0, enter: void 0, leave: void 0, transition: void 0 }];
  }
  function C6(e8, { prepare: r16, run: t12, done: n13, inFlight: i13 }) {
    let a21 = o2();
    return j4(e8, { prepare: r16, inFlight: i13 }), a21.nextFrame(() => {
      t12(), a21.requestAnimationFrame(() => {
        a21.add(M4(e8, n13));
      });
    }), a21.dispose;
  }
  function M4(e8, r16) {
    var a21, s15;
    let t12 = o2();
    if (!e8) return t12.dispose;
    let n13 = false;
    t12.add(() => {
      n13 = true;
    });
    let i13 = (s15 = (a21 = e8.getAnimations) == null ? void 0 : a21.call(e8).filter((o16) => o16 instanceof CSSTransition)) != null ? s15 : [];
    return i13.length === 0 ? (r16(), t12.dispose) : (Promise.allSettled(i13.map((o16) => o16.finished)).then(() => {
      n13 || r16();
    }), t12.dispose);
  }
  function j4(e8, { inFlight: r16, prepare: t12 }) {
    if (r16 != null && r16.current) {
      t12();
      return;
    }
    let n13 = e8.style.transition;
    e8.style.transition = "none", t12(), e8.offsetHeight, e8.style.transition = n13;
  }
  function D4(e8) {
    var t12, n13;
    return ((n13 = (t12 = e8.getAnimations) == null ? void 0 : t12.call(e8)) != null ? n13 : []).some((i13) => i13 instanceof CSSTransition && i13.playState !== "finished");
  }

  // frontend/node_modules/@headlessui/react/dist/hooks/use-tree-walker.js
  init_define_import_meta_env();
  var import_react61 = __toESM(require_react_shim(), 1);
  function F2(c14, { container: e8, accept: t12, walk: r16 }) {
    let o16 = (0, import_react61.useRef)(t12), l13 = (0, import_react61.useRef)(r16);
    (0, import_react61.useEffect)(() => {
      o16.current = t12, l13.current = r16;
    }, [t12, r16]), n(() => {
      if (!e8 || !c14) return;
      let n13 = l(e8);
      if (!n13) return;
      let f13 = o16.current, p8 = l13.current, i13 = Object.assign((m9) => f13(m9), { acceptNode: f13 }), u16 = n13.createTreeWalker(e8, NodeFilter.SHOW_ELEMENT, i13, false);
      for (; u16.nextNode(); ) p8(u16.currentNode);
    }, [e8, c14, o16, l13]);
  }

  // frontend/node_modules/@headlessui/react/dist/hooks/use-watch.js
  init_define_import_meta_env();
  var import_react62 = __toESM(require_react_shim(), 1);
  function m7(u16, t12) {
    let e8 = (0, import_react62.useRef)([]), r16 = o4(u16);
    (0, import_react62.useEffect)(() => {
      let o16 = [...e8.current];
      for (let [a21, l13] of t12.entries()) if (e8.current[a21] !== l13) {
        let n13 = r16(t12, o16);
        return e8.current = t12, n13;
      }
    }, [r16, ...t12]);
  }

  // frontend/node_modules/@headlessui/react/dist/internal/floating.js
  init_define_import_meta_env();

  // frontend/node_modules/@floating-ui/react/dist/floating-ui.react.mjs
  init_define_import_meta_env();
  var React5 = __toESM(require_react_shim(), 1);
  var import_react64 = __toESM(require_react_shim(), 1);

  // frontend/node_modules/@floating-ui/react/dist/floating-ui.react.utils.mjs
  init_define_import_meta_env();

  // frontend/node_modules/@floating-ui/utils/dist/floating-ui.utils.dom.mjs
  init_define_import_meta_env();
  function hasWindow() {
    return typeof window !== "undefined";
  }
  function getNodeName(node) {
    if (isNode(node)) {
      return (node.nodeName || "").toLowerCase();
    }
    return "#document";
  }
  function getWindow(node) {
    var _node$ownerDocument;
    return (node == null || (_node$ownerDocument = node.ownerDocument) == null ? void 0 : _node$ownerDocument.defaultView) || window;
  }
  function getDocumentElement(node) {
    var _ref;
    return (_ref = (isNode(node) ? node.ownerDocument : node.document) || window.document) == null ? void 0 : _ref.documentElement;
  }
  function isNode(value) {
    if (!hasWindow()) {
      return false;
    }
    return value instanceof Node || value instanceof getWindow(value).Node;
  }
  function isElement(value) {
    if (!hasWindow()) {
      return false;
    }
    return value instanceof Element || value instanceof getWindow(value).Element;
  }
  function isHTMLElement2(value) {
    if (!hasWindow()) {
      return false;
    }
    return value instanceof HTMLElement || value instanceof getWindow(value).HTMLElement;
  }
  function isShadowRoot(value) {
    if (!hasWindow() || typeof ShadowRoot === "undefined") {
      return false;
    }
    return value instanceof ShadowRoot || value instanceof getWindow(value).ShadowRoot;
  }
  function isOverflowElement(element) {
    const {
      overflow,
      overflowX,
      overflowY,
      display
    } = getComputedStyle3(element);
    return /auto|scroll|overlay|hidden|clip/.test(overflow + overflowY + overflowX) && display !== "inline" && display !== "contents";
  }
  function isTableElement(element) {
    return /^(table|td|th)$/.test(getNodeName(element));
  }
  function isTopLayer(element) {
    try {
      if (element.matches(":popover-open")) {
        return true;
      }
    } catch (_e) {
    }
    try {
      return element.matches(":modal");
    } catch (_e) {
      return false;
    }
  }
  var willChangeRe = /transform|translate|scale|rotate|perspective|filter/;
  var containRe = /paint|layout|strict|content/;
  var isNotNone = (value) => !!value && value !== "none";
  var isWebKitValue;
  function isContainingBlock(elementOrCss) {
    const css = isElement(elementOrCss) ? getComputedStyle3(elementOrCss) : elementOrCss;
    return isNotNone(css.transform) || isNotNone(css.translate) || isNotNone(css.scale) || isNotNone(css.rotate) || isNotNone(css.perspective) || !isWebKit() && (isNotNone(css.backdropFilter) || isNotNone(css.filter)) || willChangeRe.test(css.willChange || "") || containRe.test(css.contain || "");
  }
  function getContainingBlock(element) {
    let currentNode = getParentNode(element);
    while (isHTMLElement2(currentNode) && !isLastTraversableNode(currentNode)) {
      if (isContainingBlock(currentNode)) {
        return currentNode;
      } else if (isTopLayer(currentNode)) {
        return null;
      }
      currentNode = getParentNode(currentNode);
    }
    return null;
  }
  function isWebKit() {
    if (isWebKitValue == null) {
      isWebKitValue = typeof CSS !== "undefined" && CSS.supports && CSS.supports("-webkit-backdrop-filter", "none");
    }
    return isWebKitValue;
  }
  function isLastTraversableNode(node) {
    return /^(html|body|#document)$/.test(getNodeName(node));
  }
  function getComputedStyle3(element) {
    return getWindow(element).getComputedStyle(element);
  }
  function getNodeScroll(element) {
    if (isElement(element)) {
      return {
        scrollLeft: element.scrollLeft,
        scrollTop: element.scrollTop
      };
    }
    return {
      scrollLeft: element.scrollX,
      scrollTop: element.scrollY
    };
  }
  function getParentNode(node) {
    if (getNodeName(node) === "html") {
      return node;
    }
    const result = (
      // Step into the shadow DOM of the parent of a slotted node.
      node.assignedSlot || // DOM Element detected.
      node.parentNode || // ShadowRoot detected.
      isShadowRoot(node) && node.host || // Fallback.
      getDocumentElement(node)
    );
    return isShadowRoot(result) ? result.host : result;
  }
  function getNearestOverflowAncestor(node) {
    const parentNode = getParentNode(node);
    if (isLastTraversableNode(parentNode)) {
      return (node.ownerDocument || node).body;
    }
    if (isHTMLElement2(parentNode) && isOverflowElement(parentNode)) {
      return parentNode;
    }
    return getNearestOverflowAncestor(parentNode);
  }
  function getOverflowAncestors(node, list, traverseIframes) {
    var _node$ownerDocument2;
    if (list === void 0) {
      list = [];
    }
    if (traverseIframes === void 0) {
      traverseIframes = true;
    }
    const scrollableAncestor = getNearestOverflowAncestor(node);
    const isBody = scrollableAncestor === ((_node$ownerDocument2 = node.ownerDocument) == null ? void 0 : _node$ownerDocument2.body);
    const win = getWindow(scrollableAncestor);
    if (isBody) {
      const frameElement = getFrameElement(win);
      return list.concat(win, win.visualViewport || [], isOverflowElement(scrollableAncestor) ? scrollableAncestor : [], frameElement && traverseIframes ? getOverflowAncestors(frameElement) : []);
    } else {
      return list.concat(scrollableAncestor, getOverflowAncestors(scrollableAncestor, [], traverseIframes));
    }
  }
  function getFrameElement(win) {
    return win.parent && Object.getPrototypeOf(win.parent) ? win.frameElement : null;
  }

  // frontend/node_modules/@floating-ui/react/dist/floating-ui.react.utils.mjs
  function getUserAgent() {
    const uaData = navigator.userAgentData;
    if (uaData && Array.isArray(uaData.brands)) {
      return uaData.brands.map((_ref) => {
        let {
          brand,
          version
        } = _ref;
        return brand + "/" + version;
      }).join(" ");
    }
    return navigator.userAgent;
  }

  // frontend/node_modules/@floating-ui/utils/dist/floating-ui.utils.mjs
  init_define_import_meta_env();
  var min = Math.min;
  var max = Math.max;
  var round = Math.round;
  var floor = Math.floor;
  var createCoords = (v6) => ({
    x: v6,
    y: v6
  });
  var oppositeSideMap = {
    left: "right",
    right: "left",
    bottom: "top",
    top: "bottom"
  };
  function clamp2(start, value, end) {
    return max(start, min(value, end));
  }
  function evaluate(value, param) {
    return typeof value === "function" ? value(param) : value;
  }
  function getSide(placement) {
    return placement.split("-")[0];
  }
  function getAlignment(placement) {
    return placement.split("-")[1];
  }
  function getOppositeAxis(axis) {
    return axis === "x" ? "y" : "x";
  }
  function getAxisLength(axis) {
    return axis === "y" ? "height" : "width";
  }
  function getSideAxis(placement) {
    const firstChar = placement[0];
    return firstChar === "t" || firstChar === "b" ? "y" : "x";
  }
  function getAlignmentAxis(placement) {
    return getOppositeAxis(getSideAxis(placement));
  }
  function getAlignmentSides(placement, rects, rtl) {
    if (rtl === void 0) {
      rtl = false;
    }
    const alignment = getAlignment(placement);
    const alignmentAxis = getAlignmentAxis(placement);
    const length = getAxisLength(alignmentAxis);
    let mainAlignmentSide = alignmentAxis === "x" ? alignment === (rtl ? "end" : "start") ? "right" : "left" : alignment === "start" ? "bottom" : "top";
    if (rects.reference[length] > rects.floating[length]) {
      mainAlignmentSide = getOppositePlacement(mainAlignmentSide);
    }
    return [mainAlignmentSide, getOppositePlacement(mainAlignmentSide)];
  }
  function getExpandedPlacements(placement) {
    const oppositePlacement = getOppositePlacement(placement);
    return [getOppositeAlignmentPlacement(placement), oppositePlacement, getOppositeAlignmentPlacement(oppositePlacement)];
  }
  function getOppositeAlignmentPlacement(placement) {
    return placement.includes("start") ? placement.replace("start", "end") : placement.replace("end", "start");
  }
  var lrPlacement = ["left", "right"];
  var rlPlacement = ["right", "left"];
  var tbPlacement = ["top", "bottom"];
  var btPlacement = ["bottom", "top"];
  function getSideList(side, isStart, rtl) {
    switch (side) {
      case "top":
      case "bottom":
        if (rtl) return isStart ? rlPlacement : lrPlacement;
        return isStart ? lrPlacement : rlPlacement;
      case "left":
      case "right":
        return isStart ? tbPlacement : btPlacement;
      default:
        return [];
    }
  }
  function getOppositeAxisPlacements(placement, flipAlignment, direction, rtl) {
    const alignment = getAlignment(placement);
    let list = getSideList(getSide(placement), direction === "start", rtl);
    if (alignment) {
      list = list.map((side) => side + "-" + alignment);
      if (flipAlignment) {
        list = list.concat(list.map(getOppositeAlignmentPlacement));
      }
    }
    return list;
  }
  function getOppositePlacement(placement) {
    const side = getSide(placement);
    return oppositeSideMap[side] + placement.slice(side.length);
  }
  function expandPaddingObject(padding) {
    var _padding$top, _padding$right, _padding$bottom, _padding$left;
    return {
      top: (_padding$top = padding.top) != null ? _padding$top : 0,
      right: (_padding$right = padding.right) != null ? _padding$right : 0,
      bottom: (_padding$bottom = padding.bottom) != null ? _padding$bottom : 0,
      left: (_padding$left = padding.left) != null ? _padding$left : 0
    };
  }
  function getPaddingObject(padding) {
    return typeof padding !== "number" ? expandPaddingObject(padding) : {
      top: padding,
      right: padding,
      bottom: padding,
      left: padding
    };
  }
  function rectToClientRect(rect) {
    const {
      x: x9,
      y: y6,
      width,
      height
    } = rect;
    return {
      width,
      height,
      top: y6,
      left: x9,
      right: x9 + width,
      bottom: y6 + height,
      x: x9,
      y: y6
    };
  }

  // frontend/node_modules/@floating-ui/react/dist/floating-ui.react.mjs
  var ReactDOM2 = __toESM(require_react_dom_shim(), 1);

  // frontend/node_modules/@floating-ui/react-dom/dist/floating-ui.react-dom.mjs
  init_define_import_meta_env();

  // frontend/node_modules/@floating-ui/dom/dist/floating-ui.dom.mjs
  init_define_import_meta_env();

  // frontend/node_modules/@floating-ui/core/dist/floating-ui.core.mjs
  init_define_import_meta_env();
  function computeCoordsFromPlacement(_ref, placement, rtl) {
    let {
      reference,
      floating
    } = _ref;
    const sideAxis = getSideAxis(placement);
    const alignmentAxis = getAlignmentAxis(placement);
    const alignLength = getAxisLength(alignmentAxis);
    const side = getSide(placement);
    const isVertical = sideAxis === "y";
    const commonX = reference.x + reference.width / 2 - floating.width / 2;
    const commonY = reference.y + reference.height / 2 - floating.height / 2;
    const commonAlign = reference[alignLength] / 2 - floating[alignLength] / 2;
    let coords;
    switch (side) {
      case "top":
        coords = {
          x: commonX,
          y: reference.y - floating.height
        };
        break;
      case "bottom":
        coords = {
          x: commonX,
          y: reference.y + reference.height
        };
        break;
      case "right":
        coords = {
          x: reference.x + reference.width,
          y: commonY
        };
        break;
      case "left":
        coords = {
          x: reference.x - floating.width,
          y: commonY
        };
        break;
      default:
        coords = {
          x: reference.x,
          y: reference.y
        };
    }
    const alignment = getAlignment(placement);
    if (alignment) {
      coords[alignmentAxis] += commonAlign * (alignment === "end" ? 1 : -1) * (rtl && isVertical ? -1 : 1);
    }
    return coords;
  }
  async function detectOverflow(state, options) {
    var _await$platform$isEle;
    if (options === void 0) {
      options = {};
    }
    const {
      x: x9,
      y: y6,
      platform: platform2,
      rects,
      elements,
      strategy
    } = state;
    const {
      boundary = "clippingAncestors",
      rootBoundary = "viewport",
      elementContext = "floating",
      altBoundary = false,
      padding = 0
    } = evaluate(options, state);
    const paddingObject = getPaddingObject(padding);
    const altContext = elementContext === "floating" ? "reference" : "floating";
    const element = elements[altBoundary ? altContext : elementContext];
    const clippingClientRect = rectToClientRect(await platform2.getClippingRect({
      element: ((_await$platform$isEle = await (platform2.isElement == null ? void 0 : platform2.isElement(element))) != null ? _await$platform$isEle : true) ? element : element.contextElement || await (platform2.getDocumentElement == null ? void 0 : platform2.getDocumentElement(elements.floating)),
      boundary,
      rootBoundary,
      strategy
    }));
    const rect = elementContext === "floating" ? {
      x: x9,
      y: y6,
      width: rects.floating.width,
      height: rects.floating.height
    } : rects.reference;
    const offsetParent = await (platform2.getOffsetParent == null ? void 0 : platform2.getOffsetParent(elements.floating));
    const offsetScale = await (platform2.isElement == null ? void 0 : platform2.isElement(offsetParent)) && await (platform2.getScale == null ? void 0 : platform2.getScale(offsetParent)) || {
      x: 1,
      y: 1
    };
    const elementClientRect = rectToClientRect(platform2.convertOffsetParentRelativeRectToViewportRelativeRect ? await platform2.convertOffsetParentRelativeRectToViewportRelativeRect({
      elements,
      rect,
      offsetParent,
      strategy
    }) : rect);
    return {
      top: (clippingClientRect.top - elementClientRect.top + paddingObject.top) / offsetScale.y,
      bottom: (elementClientRect.bottom - clippingClientRect.bottom + paddingObject.bottom) / offsetScale.y,
      left: (clippingClientRect.left - elementClientRect.left + paddingObject.left) / offsetScale.x,
      right: (elementClientRect.right - clippingClientRect.right + paddingObject.right) / offsetScale.x
    };
  }
  var MAX_RESET_COUNT = 50;
  var computePosition = async (reference, floating, config) => {
    const {
      placement = "bottom",
      strategy = "absolute",
      middleware = [],
      platform: platform2
    } = config;
    const platformWithDetectOverflow = platform2.detectOverflow ? platform2 : {
      ...platform2,
      detectOverflow
    };
    const rtl = await (platform2.isRTL == null ? void 0 : platform2.isRTL(floating));
    let rects = await platform2.getElementRects({
      reference,
      floating,
      strategy
    });
    let {
      x: x9,
      y: y6
    } = computeCoordsFromPlacement(rects, placement, rtl);
    let statefulPlacement = placement;
    let resetCount = 0;
    const middlewareData = {};
    for (let i13 = 0; i13 < middleware.length; i13++) {
      const currentMiddleware = middleware[i13];
      if (!currentMiddleware) {
        continue;
      }
      const {
        name,
        fn
      } = currentMiddleware;
      const {
        x: nextX,
        y: nextY,
        data,
        reset
      } = await fn({
        x: x9,
        y: y6,
        initialPlacement: placement,
        placement: statefulPlacement,
        strategy,
        middlewareData,
        rects,
        platform: platformWithDetectOverflow,
        elements: {
          reference,
          floating
        }
      });
      x9 = nextX != null ? nextX : x9;
      y6 = nextY != null ? nextY : y6;
      middlewareData[name] = {
        ...middlewareData[name],
        ...data
      };
      if (reset && resetCount < MAX_RESET_COUNT) {
        resetCount++;
        if (typeof reset === "object") {
          if (reset.placement) {
            statefulPlacement = reset.placement;
          }
          if (reset.rects) {
            rects = reset.rects === true ? await platform2.getElementRects({
              reference,
              floating,
              strategy
            }) : reset.rects;
          }
          ({
            x: x9,
            y: y6
          } = computeCoordsFromPlacement(rects, statefulPlacement, rtl));
        }
        i13 = -1;
      }
    }
    return {
      x: x9,
      y: y6,
      placement: statefulPlacement,
      strategy,
      middlewareData
    };
  };
  var flip = function(options) {
    if (options === void 0) {
      options = {};
    }
    return {
      name: "flip",
      options,
      async fn(state) {
        var _middlewareData$arrow, _middlewareData$flip;
        const {
          placement,
          middlewareData,
          rects,
          initialPlacement,
          platform: platform2,
          elements
        } = state;
        const {
          mainAxis: checkMainAxis = true,
          crossAxis: checkCrossAxis = true,
          fallbackPlacements: specifiedFallbackPlacements,
          fallbackStrategy = "bestFit",
          fallbackAxisSideDirection = "none",
          flipAlignment = true,
          ...detectOverflowOptions
        } = evaluate(options, state);
        if ((_middlewareData$arrow = middlewareData.arrow) != null && _middlewareData$arrow.alignmentOffset) {
          return {};
        }
        const side = getSide(placement);
        const initialSideAxis = getSideAxis(initialPlacement);
        const isBasePlacement = getSide(initialPlacement) === initialPlacement;
        const rtl = await (platform2.isRTL == null ? void 0 : platform2.isRTL(elements.floating));
        const fallbackPlacements = specifiedFallbackPlacements || (isBasePlacement || !flipAlignment ? [getOppositePlacement(initialPlacement)] : getExpandedPlacements(initialPlacement));
        const hasFallbackAxisSideDirection = fallbackAxisSideDirection !== "none";
        if (!specifiedFallbackPlacements && hasFallbackAxisSideDirection) {
          fallbackPlacements.push(...getOppositeAxisPlacements(initialPlacement, flipAlignment, fallbackAxisSideDirection, rtl));
        }
        const placements2 = [initialPlacement, ...fallbackPlacements];
        const overflow = await platform2.detectOverflow(state, detectOverflowOptions);
        const overflows = [];
        let overflowsData = ((_middlewareData$flip = middlewareData.flip) == null ? void 0 : _middlewareData$flip.overflows) || [];
        if (checkMainAxis) {
          overflows.push(overflow[side]);
        }
        if (checkCrossAxis) {
          const sides2 = getAlignmentSides(placement, rects, rtl);
          overflows.push(overflow[sides2[0]], overflow[sides2[1]]);
        }
        overflowsData = [...overflowsData, {
          placement,
          overflows
        }];
        if (!overflows.every((side2) => side2 <= 0)) {
          var _middlewareData$flip2, _overflowsData$filter;
          const nextIndex = (((_middlewareData$flip2 = middlewareData.flip) == null ? void 0 : _middlewareData$flip2.index) || 0) + 1;
          const nextPlacement = placements2[nextIndex];
          if (nextPlacement) {
            const ignoreCrossAxisOverflow = checkCrossAxis === "alignment" ? initialSideAxis !== getSideAxis(nextPlacement) : false;
            if (!ignoreCrossAxisOverflow || // We leave the current main axis only if every placement on that axis
            // overflows the main axis.
            overflowsData.every((d9) => getSideAxis(d9.placement) === initialSideAxis ? d9.overflows[0] > 0 : true)) {
              return {
                data: {
                  index: nextIndex,
                  overflows: overflowsData
                },
                reset: {
                  placement: nextPlacement
                }
              };
            }
          }
          let resetPlacement = (_overflowsData$filter = overflowsData.filter((d9) => d9.overflows[0] <= 0).sort((a21, b8) => a21.overflows[1] - b8.overflows[1])[0]) == null ? void 0 : _overflowsData$filter.placement;
          if (!resetPlacement) {
            switch (fallbackStrategy) {
              case "bestFit": {
                var _overflowsData$filter2;
                const placement2 = (_overflowsData$filter2 = overflowsData.filter((d9) => {
                  if (hasFallbackAxisSideDirection) {
                    const currentSideAxis = getSideAxis(d9.placement);
                    return currentSideAxis === initialSideAxis || // Create a bias to the `y` side axis due to horizontal
                    // reading directions favoring greater width.
                    currentSideAxis === "y";
                  }
                  return true;
                }).map((d9) => [d9.placement, d9.overflows.filter((overflow2) => overflow2 > 0).reduce((acc, overflow2) => acc + overflow2, 0)]).sort((a21, b8) => a21[1] - b8[1])[0]) == null ? void 0 : _overflowsData$filter2[0];
                if (placement2) {
                  resetPlacement = placement2;
                }
                break;
              }
              case "initialPlacement":
                resetPlacement = initialPlacement;
                break;
            }
          }
          if (placement !== resetPlacement) {
            return {
              reset: {
                placement: resetPlacement
              }
            };
          }
        }
        return {};
      }
    };
  };
  var originSides = /* @__PURE__ */ new Set(["left", "top"]);
  async function convertValueToCoords(state, options) {
    const {
      placement,
      platform: platform2,
      elements
    } = state;
    const rtl = await (platform2.isRTL == null ? void 0 : platform2.isRTL(elements.floating));
    const side = getSide(placement);
    const alignment = getAlignment(placement);
    const isVertical = getSideAxis(placement) === "y";
    const mainAxisMulti = originSides.has(side) ? -1 : 1;
    const crossAxisMulti = rtl && isVertical ? -1 : 1;
    const rawValue = evaluate(options, state);
    let {
      mainAxis,
      crossAxis,
      alignmentAxis
    } = typeof rawValue === "number" ? {
      mainAxis: rawValue,
      crossAxis: 0,
      alignmentAxis: null
    } : {
      mainAxis: rawValue.mainAxis || 0,
      crossAxis: rawValue.crossAxis || 0,
      alignmentAxis: rawValue.alignmentAxis
    };
    if (alignment && typeof alignmentAxis === "number") {
      crossAxis = alignment === "end" ? alignmentAxis * -1 : alignmentAxis;
    }
    return isVertical ? {
      x: crossAxis * crossAxisMulti,
      y: mainAxis * mainAxisMulti
    } : {
      x: mainAxis * mainAxisMulti,
      y: crossAxis * crossAxisMulti
    };
  }
  var offset = function(options) {
    if (options === void 0) {
      options = 0;
    }
    return {
      name: "offset",
      options,
      async fn(state) {
        var _middlewareData$offse, _middlewareData$arrow;
        const {
          x: x9,
          y: y6,
          placement,
          middlewareData
        } = state;
        const diffCoords = await convertValueToCoords(state, options);
        if (placement === ((_middlewareData$offse = middlewareData.offset) == null ? void 0 : _middlewareData$offse.placement) && (_middlewareData$arrow = middlewareData.arrow) != null && _middlewareData$arrow.alignmentOffset) {
          return {};
        }
        return {
          x: x9 + diffCoords.x,
          y: y6 + diffCoords.y,
          data: {
            ...diffCoords,
            placement
          }
        };
      }
    };
  };
  var shift = function(options) {
    if (options === void 0) {
      options = {};
    }
    return {
      name: "shift",
      options,
      async fn(state) {
        const {
          x: x9,
          y: y6,
          placement,
          platform: platform2
        } = state;
        const {
          mainAxis: checkMainAxis = true,
          crossAxis: checkCrossAxis = false,
          limiter = {
            fn: (_ref) => {
              let {
                x: x10,
                y: y7
              } = _ref;
              return {
                x: x10,
                y: y7
              };
            }
          },
          ...detectOverflowOptions
        } = evaluate(options, state);
        const coords = {
          x: x9,
          y: y6
        };
        const overflow = await platform2.detectOverflow(state, detectOverflowOptions);
        const crossAxis = getSideAxis(placement);
        const mainAxis = getOppositeAxis(crossAxis);
        let mainAxisCoord = coords[mainAxis];
        let crossAxisCoord = coords[crossAxis];
        const clampCoord = (axis, coord) => clamp2(coord + overflow[axis === "y" ? "top" : "left"], coord, coord - overflow[axis === "y" ? "bottom" : "right"]);
        if (checkMainAxis) {
          mainAxisCoord = clampCoord(mainAxis, mainAxisCoord);
        }
        if (checkCrossAxis) {
          crossAxisCoord = clampCoord(crossAxis, crossAxisCoord);
        }
        const limitedCoords = limiter.fn({
          ...state,
          [mainAxis]: mainAxisCoord,
          [crossAxis]: crossAxisCoord
        });
        return {
          ...limitedCoords,
          data: {
            x: limitedCoords.x - x9,
            y: limitedCoords.y - y6,
            enabled: {
              [mainAxis]: checkMainAxis,
              [crossAxis]: checkCrossAxis
            }
          }
        };
      }
    };
  };
  var size = function(options) {
    if (options === void 0) {
      options = {};
    }
    return {
      name: "size",
      options,
      async fn(state) {
        const {
          placement,
          rects,
          platform: platform2,
          elements
        } = state;
        const {
          apply = () => {
          },
          ...detectOverflowOptions
        } = evaluate(options, state);
        const overflow = await platform2.detectOverflow(state, detectOverflowOptions);
        const side = getSide(placement);
        const alignment = getAlignment(placement);
        const isYAxis = getSideAxis(placement) === "y";
        const {
          width,
          height
        } = rects.floating;
        let heightSide;
        let widthSide;
        if (side === "top" || side === "bottom") {
          heightSide = side;
          widthSide = alignment === (await (platform2.isRTL == null ? void 0 : platform2.isRTL(elements.floating)) ? "start" : "end") ? "left" : "right";
        } else {
          widthSide = side;
          heightSide = alignment === "end" ? "top" : "bottom";
        }
        const maximumClippingHeight = height - overflow.top - overflow.bottom;
        const maximumClippingWidth = width - overflow.left - overflow.right;
        const overflowAvailableHeight = min(height - overflow[heightSide], maximumClippingHeight);
        const overflowAvailableWidth = min(width - overflow[widthSide], maximumClippingWidth);
        const shiftData = state.middlewareData.shift;
        const noShift = !shiftData;
        let availableHeight = overflowAvailableHeight;
        let availableWidth = overflowAvailableWidth;
        if (shiftData != null && shiftData.enabled.x) {
          availableWidth = maximumClippingWidth;
        }
        if (shiftData != null && shiftData.enabled.y) {
          availableHeight = maximumClippingHeight;
        }
        if (noShift && !alignment) {
          if (isYAxis) {
            availableWidth = width - 2 * max(overflow.left, overflow.right);
          } else {
            availableHeight = height - 2 * max(overflow.top, overflow.bottom);
          }
        }
        await apply({
          ...state,
          availableWidth,
          availableHeight
        });
        const nextDimensions = await platform2.getDimensions(elements.floating);
        if (width !== nextDimensions.width || height !== nextDimensions.height) {
          return {
            reset: {
              rects: true
            }
          };
        }
        return {};
      }
    };
  };

  // frontend/node_modules/@floating-ui/dom/dist/floating-ui.dom.mjs
  function getCssDimensions(element) {
    const css = getComputedStyle3(element);
    let width = parseFloat(css.width) || 0;
    let height = parseFloat(css.height) || 0;
    const hasOffset = isHTMLElement2(element);
    const offsetWidth = hasOffset ? element.offsetWidth : width;
    const offsetHeight = hasOffset ? element.offsetHeight : height;
    const shouldFallback = round(width) !== offsetWidth || round(height) !== offsetHeight;
    if (shouldFallback) {
      width = offsetWidth;
      height = offsetHeight;
    }
    return {
      width,
      height,
      $: shouldFallback
    };
  }
  function unwrapElement(element) {
    return !isElement(element) ? element.contextElement : element;
  }
  function getScale(element) {
    const domElement = unwrapElement(element);
    if (!isHTMLElement2(domElement)) {
      return createCoords(1);
    }
    const rect = domElement.getBoundingClientRect();
    const {
      width,
      height,
      $: $5
    } = getCssDimensions(domElement);
    let x9 = ($5 ? round(rect.width) : rect.width) / width;
    let y6 = ($5 ? round(rect.height) : rect.height) / height;
    if (!x9 || !Number.isFinite(x9)) {
      x9 = 1;
    }
    if (!y6 || !Number.isFinite(y6)) {
      y6 = 1;
    }
    return {
      x: x9,
      y: y6
    };
  }
  var noOffsets = /* @__PURE__ */ createCoords(0);
  function getVisualOffsets(element) {
    const win = getWindow(element);
    if (!isWebKit() || !win.visualViewport) {
      return noOffsets;
    }
    return {
      x: win.visualViewport.offsetLeft,
      y: win.visualViewport.offsetTop
    };
  }
  function shouldAddVisualOffsets(element, isFixed, floatingOffsetParent) {
    if (isFixed === void 0) {
      isFixed = false;
    }
    return !!floatingOffsetParent && isFixed && floatingOffsetParent === getWindow(element);
  }
  function getBoundingClientRect(element, includeScale, isFixedStrategy, offsetParent) {
    if (includeScale === void 0) {
      includeScale = false;
    }
    if (isFixedStrategy === void 0) {
      isFixedStrategy = false;
    }
    const clientRect = element.getBoundingClientRect();
    const domElement = unwrapElement(element);
    let scale2 = createCoords(1);
    if (includeScale) {
      if (offsetParent) {
        if (isElement(offsetParent)) {
          scale2 = getScale(offsetParent);
        }
      } else {
        scale2 = getScale(element);
      }
    }
    const visualOffsets = shouldAddVisualOffsets(domElement, isFixedStrategy, offsetParent) ? getVisualOffsets(domElement) : createCoords(0);
    let x9 = (clientRect.left + visualOffsets.x) / scale2.x;
    let y6 = (clientRect.top + visualOffsets.y) / scale2.y;
    let width = clientRect.width / scale2.x;
    let height = clientRect.height / scale2.y;
    if (domElement && offsetParent) {
      const win = getWindow(domElement);
      const offsetWin = isElement(offsetParent) ? getWindow(offsetParent) : offsetParent;
      let currentWin = win;
      let currentIFrame = getFrameElement(currentWin);
      while (currentIFrame && offsetWin !== currentWin) {
        const iframeScale = getScale(currentIFrame);
        const iframeRect = currentIFrame.getBoundingClientRect();
        const css = getComputedStyle3(currentIFrame);
        const left = iframeRect.left + (currentIFrame.clientLeft + parseFloat(css.paddingLeft)) * iframeScale.x;
        const top = iframeRect.top + (currentIFrame.clientTop + parseFloat(css.paddingTop)) * iframeScale.y;
        x9 *= iframeScale.x;
        y6 *= iframeScale.y;
        width *= iframeScale.x;
        height *= iframeScale.y;
        x9 += left;
        y6 += top;
        currentWin = getWindow(currentIFrame);
        currentIFrame = getFrameElement(currentWin);
      }
    }
    return rectToClientRect({
      width,
      height,
      x: x9,
      y: y6
    });
  }
  function getWindowScrollBarX(element, rect) {
    const leftScroll = getNodeScroll(element).scrollLeft;
    if (!rect) {
      return getBoundingClientRect(getDocumentElement(element)).left + leftScroll;
    }
    return rect.left + leftScroll;
  }
  function getHTMLOffset(documentElement, scroll) {
    const htmlRect = documentElement.getBoundingClientRect();
    const x9 = htmlRect.left + scroll.scrollLeft - getWindowScrollBarX(documentElement, htmlRect);
    const y6 = htmlRect.top + scroll.scrollTop;
    return {
      x: x9,
      y: y6
    };
  }
  function convertOffsetParentRelativeRectToViewportRelativeRect(_ref) {
    let {
      elements,
      rect,
      offsetParent,
      strategy
    } = _ref;
    const isFixed = strategy === "fixed";
    const documentElement = getDocumentElement(offsetParent);
    const topLayer = elements ? isTopLayer(elements.floating) : false;
    if (offsetParent === documentElement || topLayer && isFixed) {
      return rect;
    }
    let scroll = {
      scrollLeft: 0,
      scrollTop: 0
    };
    let scale2 = createCoords(1);
    const offsets = createCoords(0);
    const isOffsetParentAnElement = isHTMLElement2(offsetParent);
    if (isOffsetParentAnElement || !isFixed) {
      if (getNodeName(offsetParent) !== "body" || isOverflowElement(documentElement)) {
        scroll = getNodeScroll(offsetParent);
      }
      if (isOffsetParentAnElement) {
        const offsetRect = getBoundingClientRect(offsetParent);
        scale2 = getScale(offsetParent);
        offsets.x = offsetRect.x + offsetParent.clientLeft;
        offsets.y = offsetRect.y + offsetParent.clientTop;
      }
    }
    const htmlOffset = documentElement && !isOffsetParentAnElement && !isFixed ? getHTMLOffset(documentElement, scroll) : createCoords(0);
    return {
      width: rect.width * scale2.x,
      height: rect.height * scale2.y,
      x: rect.x * scale2.x - scroll.scrollLeft * scale2.x + offsets.x + htmlOffset.x,
      y: rect.y * scale2.y - scroll.scrollTop * scale2.y + offsets.y + htmlOffset.y
    };
  }
  function getClientRects(element) {
    return element.getClientRects ? Array.from(element.getClientRects()) : [];
  }
  function getDocumentRect(html) {
    const scroll = getNodeScroll(html);
    const body = html.ownerDocument.body;
    const width = max(html.scrollWidth, html.clientWidth, body.scrollWidth, body.clientWidth);
    const height = max(html.scrollHeight, html.clientHeight, body.scrollHeight, body.clientHeight);
    let x9 = -scroll.scrollLeft + getWindowScrollBarX(html);
    const y6 = -scroll.scrollTop;
    if (getComputedStyle3(body).direction === "rtl") {
      x9 += max(html.clientWidth, body.clientWidth) - width;
    }
    return {
      width,
      height,
      x: x9,
      y: y6
    };
  }
  var SCROLLBAR_MAX = 25;
  function getViewportRect(element, strategy, rootBoundary) {
    if (rootBoundary === void 0) {
      rootBoundary = "viewport";
    }
    const isLayoutViewport = rootBoundary === "layoutViewport";
    const win = getWindow(element);
    const html = getDocumentElement(element);
    const visualViewport = win.visualViewport;
    let width = html.clientWidth;
    let height = html.clientHeight;
    let x9 = 0;
    let y6 = 0;
    if (visualViewport) {
      const layoutRelativeClientCoords = !isWebKit() || strategy === "fixed";
      if (isLayoutViewport) {
        if (!layoutRelativeClientCoords) {
          x9 = -visualViewport.offsetLeft;
          y6 = -visualViewport.offsetTop;
        }
      } else {
        width = visualViewport.width;
        height = visualViewport.height;
        if (layoutRelativeClientCoords) {
          x9 = visualViewport.offsetLeft;
          y6 = visualViewport.offsetTop;
        }
      }
    }
    const windowScrollbarX = getWindowScrollBarX(html);
    if (windowScrollbarX <= 0) {
      const doc = html.ownerDocument;
      const body = doc.body;
      const bodyStyles = getComputedStyle(body);
      const bodyMarginInline = doc.compatMode === "CSS1Compat" ? parseFloat(bodyStyles.marginLeft) + parseFloat(bodyStyles.marginRight) || 0 : 0;
      const reservedWidth = Math.abs(html.clientWidth - body.clientWidth - bodyMarginInline);
      const gutter = getComputedStyle(html).scrollbarGutter === "stable both-edges" ? reservedWidth / 2 : reservedWidth;
      if (gutter <= SCROLLBAR_MAX) {
        width -= gutter;
      }
    }
    return {
      width,
      height,
      x: x9,
      y: y6
    };
  }
  function getInnerBoundingClientRect(element, strategy) {
    const clientRect = getBoundingClientRect(element, true, strategy === "fixed");
    const top = clientRect.top + element.clientTop;
    const left = clientRect.left + element.clientLeft;
    const scale2 = getScale(element);
    const width = element.clientWidth * scale2.x;
    const height = element.clientHeight * scale2.y;
    const x9 = left * scale2.x;
    const y6 = top * scale2.y;
    return {
      width,
      height,
      x: x9,
      y: y6
    };
  }
  function getClientRectFromClippingAncestor(element, clippingAncestor, strategy) {
    let rect;
    if (clippingAncestor === "viewport" || clippingAncestor === "layoutViewport") {
      rect = getViewportRect(element, strategy, clippingAncestor);
    } else if (clippingAncestor === "document") {
      rect = getDocumentRect(getDocumentElement(element));
    } else if (isElement(clippingAncestor)) {
      rect = getInnerBoundingClientRect(clippingAncestor, strategy);
    } else {
      const visualOffsets = getVisualOffsets(element);
      rect = {
        x: clippingAncestor.x - visualOffsets.x,
        y: clippingAncestor.y - visualOffsets.y,
        width: clippingAncestor.width,
        height: clippingAncestor.height
      };
    }
    return rectToClientRect(rect);
  }
  function getClippingElementAncestors(element, cache) {
    const cachedResult = cache.get(element);
    if (cachedResult) {
      return cachedResult;
    }
    let result = getOverflowAncestors(element, [], false).filter((el) => isElement(el) && getNodeName(el) !== "body");
    let lastKeptComputedStyle = null;
    const elementIsFixed = getComputedStyle3(element).position === "fixed";
    let currentNode = elementIsFixed ? getParentNode(element) : element;
    while (isElement(currentNode) && !isLastTraversableNode(currentNode)) {
      const computedStyle = getComputedStyle3(currentNode);
      const currentNodeIsContaining = isContainingBlock(currentNode);
      const lastPosition = lastKeptComputedStyle ? lastKeptComputedStyle.position : elementIsFixed ? "fixed" : "";
      const shouldDropCurrentNode = !currentNodeIsContaining && (lastPosition === "fixed" || lastPosition === "absolute" && computedStyle.position === "static");
      if (shouldDropCurrentNode) {
        result = result.filter((ancestor) => ancestor !== currentNode);
      } else {
        lastKeptComputedStyle = computedStyle;
      }
      currentNode = getParentNode(currentNode);
    }
    cache.set(element, result);
    return result;
  }
  function getClippingRect(_ref) {
    let {
      element,
      boundary,
      rootBoundary,
      strategy
    } = _ref;
    const elementClippingAncestors = boundary === "clippingAncestors" ? isTopLayer(element) ? [] : getClippingElementAncestors(element, this._c) : [].concat(boundary);
    const clippingAncestors = [...elementClippingAncestors, rootBoundary];
    const firstRect = getClientRectFromClippingAncestor(element, clippingAncestors[0], strategy);
    let top = firstRect.top;
    let right = firstRect.right;
    let bottom = firstRect.bottom;
    let left = firstRect.left;
    for (let i13 = 1; i13 < clippingAncestors.length; i13++) {
      const rect = getClientRectFromClippingAncestor(element, clippingAncestors[i13], strategy);
      top = max(rect.top, top);
      right = min(rect.right, right);
      bottom = min(rect.bottom, bottom);
      left = max(rect.left, left);
    }
    return {
      width: right - left,
      height: bottom - top,
      x: left,
      y: top
    };
  }
  function getDimensions(element) {
    const {
      width,
      height
    } = getCssDimensions(element);
    return {
      width,
      height
    };
  }
  function getRectRelativeToOffsetParent(element, offsetParent, strategy) {
    const isOffsetParentAnElement = isHTMLElement2(offsetParent);
    const documentElement = getDocumentElement(offsetParent);
    const isFixed = strategy === "fixed";
    const rect = getBoundingClientRect(element, true, isFixed, offsetParent);
    let scroll = {
      scrollLeft: 0,
      scrollTop: 0
    };
    const offsets = createCoords(0);
    if (isOffsetParentAnElement || !isFixed) {
      if (getNodeName(offsetParent) !== "body" || isOverflowElement(documentElement)) {
        scroll = getNodeScroll(offsetParent);
      }
      if (isOffsetParentAnElement) {
        const offsetRect = getBoundingClientRect(offsetParent, true, isFixed, offsetParent);
        offsets.x = offsetRect.x + offsetParent.clientLeft;
        offsets.y = offsetRect.y + offsetParent.clientTop;
      }
    }
    if (!isOffsetParentAnElement && documentElement) {
      offsets.x = getWindowScrollBarX(documentElement);
    }
    const htmlOffset = documentElement && !isOffsetParentAnElement && !isFixed ? getHTMLOffset(documentElement, scroll) : createCoords(0);
    const x9 = rect.left + scroll.scrollLeft - offsets.x - htmlOffset.x;
    const y6 = rect.top + scroll.scrollTop - offsets.y - htmlOffset.y;
    return {
      x: x9,
      y: y6,
      width: rect.width,
      height: rect.height
    };
  }
  function isStaticPositioned(element) {
    return getComputedStyle3(element).position === "static";
  }
  function getTrueOffsetParent(element, polyfill) {
    if (!isHTMLElement2(element) || getComputedStyle3(element).position === "fixed") {
      return null;
    }
    if (polyfill) {
      return polyfill(element);
    }
    let rawOffsetParent = element.offsetParent;
    if (getDocumentElement(element) === rawOffsetParent) {
      rawOffsetParent = rawOffsetParent.ownerDocument.body;
    }
    return rawOffsetParent;
  }
  function getOffsetParent(element, polyfill) {
    const win = getWindow(element);
    if (isTopLayer(element)) {
      return win;
    }
    if (!isHTMLElement2(element)) {
      let svgOffsetParent = getParentNode(element);
      while (svgOffsetParent && !isLastTraversableNode(svgOffsetParent)) {
        if (isElement(svgOffsetParent) && !isStaticPositioned(svgOffsetParent)) {
          return svgOffsetParent;
        }
        svgOffsetParent = getParentNode(svgOffsetParent);
      }
      return win;
    }
    let offsetParent = getTrueOffsetParent(element, polyfill);
    while (offsetParent && isTableElement(offsetParent) && isStaticPositioned(offsetParent)) {
      offsetParent = getTrueOffsetParent(offsetParent, polyfill);
    }
    if (offsetParent && isLastTraversableNode(offsetParent) && isStaticPositioned(offsetParent) && !isContainingBlock(offsetParent)) {
      return win;
    }
    return offsetParent || getContainingBlock(element) || win;
  }
  var getElementRects = async function(data) {
    const getOffsetParentFn = this.getOffsetParent || getOffsetParent;
    const getDimensionsFn = this.getDimensions;
    const floatingDimensions = await getDimensionsFn(data.floating);
    return {
      reference: getRectRelativeToOffsetParent(data.reference, await getOffsetParentFn(data.floating), data.strategy),
      floating: {
        x: 0,
        y: 0,
        width: floatingDimensions.width,
        height: floatingDimensions.height
      }
    };
  };
  function isRTL(element) {
    return getComputedStyle3(element).direction === "rtl";
  }
  var platform = {
    convertOffsetParentRelativeRectToViewportRelativeRect,
    getDocumentElement,
    getClippingRect,
    getOffsetParent,
    getElementRects,
    getClientRects,
    getDimensions,
    getScale,
    isElement,
    isRTL
  };
  function rectsAreEqual(a21, b8) {
    return a21.x === b8.x && a21.y === b8.y && a21.width === b8.width && a21.height === b8.height;
  }
  function observeMove(element, onMove, ancestorResize) {
    let io = null;
    let timeoutId;
    const root = getDocumentElement(element);
    function cleanup() {
      var _io;
      clearTimeout(timeoutId);
      (_io = io) == null || _io.disconnect();
      io = null;
    }
    function refresh(skip, threshold) {
      if (skip === void 0) {
        skip = false;
      }
      if (threshold === void 0) {
        threshold = 1;
      }
      cleanup();
      const elementRectForRootMargin = element.getBoundingClientRect();
      const {
        left,
        top,
        width,
        height
      } = elementRectForRootMargin;
      if (!skip) {
        onMove();
      }
      if (!width || !height) {
        return;
      }
      const insetTop = floor(top);
      const insetRight = floor(root.clientWidth - (left + width));
      const insetBottom = floor(root.clientHeight - (top + height));
      const insetLeft = floor(left);
      const rootMargin = -insetTop + "px " + -insetRight + "px " + -insetBottom + "px " + -insetLeft + "px";
      const options = {
        rootMargin,
        threshold: max(0, min(1, threshold)) || 1
      };
      let isFirstUpdate = true;
      function handleObserve(entries) {
        const ratio = entries[0].intersectionRatio;
        if (!rectsAreEqual(elementRectForRootMargin, element.getBoundingClientRect())) {
          return refresh();
        }
        if (ratio !== threshold) {
          if (!isFirstUpdate) {
            return refresh();
          }
          if (!ratio) {
            timeoutId = setTimeout(() => {
              refresh(false, 1e-7);
            }, 1e3);
          } else {
            refresh(false, ratio);
          }
        }
        isFirstUpdate = false;
      }
      try {
        io = new IntersectionObserver(handleObserve, {
          ...options,
          // Handle <iframe>s
          root: root.ownerDocument
        });
      } catch (_e) {
        io = new IntersectionObserver(handleObserve, options);
      }
      io.observe(element);
    }
    const win = getWindow(element);
    const handleResize = () => refresh(ancestorResize);
    win.addEventListener("resize", handleResize);
    refresh(true);
    return () => {
      win.removeEventListener("resize", handleResize);
      cleanup();
    };
  }
  function autoUpdate(reference, floating, update, options) {
    if (options === void 0) {
      options = {};
    }
    const {
      ancestorScroll = true,
      ancestorResize = true,
      elementResize = typeof ResizeObserver === "function",
      layoutShift = typeof IntersectionObserver === "function",
      animationFrame = false
    } = options;
    const referenceEl = unwrapElement(reference);
    const ancestors = ancestorScroll || ancestorResize ? [...referenceEl ? getOverflowAncestors(referenceEl) : [], ...floating ? getOverflowAncestors(floating) : []] : [];
    ancestors.forEach((ancestor) => {
      ancestorScroll && ancestor.addEventListener("scroll", update);
      ancestorResize && ancestor.addEventListener("resize", update);
    });
    const cleanupIo = referenceEl && layoutShift ? observeMove(referenceEl, update, ancestorResize) : null;
    let reobserveFrame = -1;
    let resizeObserver = null;
    if (elementResize) {
      resizeObserver = new ResizeObserver((_ref) => {
        let [firstEntry] = _ref;
        if (firstEntry && firstEntry.target === referenceEl && resizeObserver && floating) {
          resizeObserver.unobserve(floating);
          cancelAnimationFrame(reobserveFrame);
          reobserveFrame = requestAnimationFrame(() => {
            var _resizeObserver;
            (_resizeObserver = resizeObserver) == null || _resizeObserver.observe(floating);
          });
        }
        update();
      });
      if (referenceEl && !animationFrame) {
        resizeObserver.observe(referenceEl);
      }
      if (floating) {
        resizeObserver.observe(floating);
      }
    }
    let frameId;
    let prevRefRect = animationFrame ? getBoundingClientRect(reference) : null;
    if (animationFrame) {
      frameLoop();
    }
    function frameLoop() {
      const nextRefRect = getBoundingClientRect(reference);
      if (prevRefRect && !rectsAreEqual(prevRefRect, nextRefRect)) {
        update();
      }
      prevRefRect = nextRefRect;
      frameId = requestAnimationFrame(frameLoop);
    }
    update();
    return () => {
      var _resizeObserver2;
      ancestors.forEach((ancestor) => {
        ancestorScroll && ancestor.removeEventListener("scroll", update);
        ancestorResize && ancestor.removeEventListener("resize", update);
      });
      cleanupIo == null || cleanupIo();
      (_resizeObserver2 = resizeObserver) == null || _resizeObserver2.disconnect();
      resizeObserver = null;
      if (animationFrame) {
        cancelAnimationFrame(frameId);
      }
    };
  }
  var detectOverflow2 = detectOverflow;
  var offset2 = offset;
  var shift2 = shift;
  var flip2 = flip;
  var size2 = size;
  var computePosition2 = (reference, floating, options) => {
    const cache = /* @__PURE__ */ new Map();
    const mergedOptions = options != null ? options : {};
    const platformWithCache = {
      ...platform,
      ...mergedOptions.platform,
      _c: cache
    };
    return computePosition(reference, floating, {
      ...mergedOptions,
      platform: platformWithCache
    });
  };

  // frontend/node_modules/@floating-ui/react-dom/dist/floating-ui.react-dom.mjs
  var React4 = __toESM(require_react_shim(), 1);
  var import_react63 = __toESM(require_react_shim(), 1);
  var ReactDOM = __toESM(require_react_dom_shim(), 1);
  var isClient = typeof document !== "undefined";
  var noop2 = function noop3() {
  };
  var index = isClient ? import_react63.useLayoutEffect : noop2;
  function deepEqual(a21, b8) {
    if (a21 === b8) {
      return true;
    }
    if (typeof a21 !== typeof b8) {
      return false;
    }
    if (typeof a21 === "function" && a21.toString() === b8.toString()) {
      return true;
    }
    let length;
    let i13;
    let keys;
    if (a21 && b8 && typeof a21 === "object") {
      if (Array.isArray(a21)) {
        length = a21.length;
        if (length !== b8.length) return false;
        for (i13 = length; i13-- !== 0; ) {
          if (!deepEqual(a21[i13], b8[i13])) {
            return false;
          }
        }
        return true;
      }
      keys = Object.keys(a21);
      length = keys.length;
      if (length !== Object.keys(b8).length) {
        return false;
      }
      for (i13 = length; i13-- !== 0; ) {
        if (!{}.hasOwnProperty.call(b8, keys[i13])) {
          return false;
        }
      }
      for (i13 = length; i13-- !== 0; ) {
        const key = keys[i13];
        if (key === "_owner" && a21.$$typeof) {
          continue;
        }
        if (!deepEqual(a21[key], b8[key])) {
          return false;
        }
      }
      return true;
    }
    return a21 !== a21 && b8 !== b8;
  }
  function getDPR(element) {
    if (typeof window === "undefined") {
      return 1;
    }
    const win = element.ownerDocument.defaultView || window;
    return win.devicePixelRatio || 1;
  }
  function roundByDPR(element, value) {
    const dpr = getDPR(element);
    return Math.round(value * dpr) / dpr;
  }
  function useLatestRef(value) {
    const ref = React4.useRef(value);
    index(() => {
      ref.current = value;
    });
    return ref;
  }
  function useFloating(options) {
    if (options === void 0) {
      options = {};
    }
    const {
      placement = "bottom",
      strategy = "absolute",
      middleware = [],
      platform: platform2,
      elements: {
        reference: externalReference,
        floating: externalFloating
      } = {},
      transform = true,
      whileElementsMounted,
      open
    } = options;
    const [data, setData] = React4.useState({
      x: 0,
      y: 0,
      strategy,
      placement,
      middlewareData: {},
      isPositioned: false
    });
    const [latestMiddleware, setLatestMiddleware] = React4.useState(middleware);
    if (!deepEqual(latestMiddleware, middleware)) {
      setLatestMiddleware(middleware);
    }
    const [_reference, _setReference] = React4.useState(null);
    const [_floating, _setFloating] = React4.useState(null);
    const setReference = React4.useCallback((node) => {
      if (node !== referenceRef.current) {
        referenceRef.current = node;
        _setReference(node);
      }
    }, []);
    const setFloating = React4.useCallback((node) => {
      if (node !== floatingRef.current) {
        floatingRef.current = node;
        _setFloating(node);
      }
    }, []);
    const referenceEl = externalReference || _reference;
    const floatingEl = externalFloating || _floating;
    const referenceRef = React4.useRef(null);
    const floatingRef = React4.useRef(null);
    const dataRef = React4.useRef(data);
    const hasWhileElementsMounted = whileElementsMounted != null;
    const whileElementsMountedRef = useLatestRef(whileElementsMounted);
    const platformRef = useLatestRef(platform2);
    const openRef = useLatestRef(open);
    const update = React4.useCallback(() => {
      if (!referenceRef.current || !floatingRef.current) {
        return;
      }
      const config = {
        placement,
        strategy,
        middleware: latestMiddleware
      };
      if (platformRef.current) {
        config.platform = platformRef.current;
      }
      computePosition2(referenceRef.current, floatingRef.current, config).then((data2) => {
        const fullData = {
          ...data2,
          // The floating element's position may be recomputed while it's closed
          // but still mounted (such as when transitioning out). To ensure
          // `isPositioned` will be `false` initially on the next open, avoid
          // setting it to `true` when `open === false` (must be specified).
          isPositioned: openRef.current !== false
        };
        if (isMountedRef.current && !deepEqual(dataRef.current, fullData)) {
          dataRef.current = fullData;
          ReactDOM.flushSync(() => {
            setData(fullData);
          });
        }
      });
    }, [latestMiddleware, placement, strategy, platformRef, openRef]);
    index(() => {
      if (open === false && dataRef.current.isPositioned) {
        dataRef.current.isPositioned = false;
        setData((data2) => ({
          ...data2,
          isPositioned: false
        }));
      }
    }, [open]);
    const isMountedRef = React4.useRef(false);
    index(() => {
      isMountedRef.current = true;
      return () => {
        isMountedRef.current = false;
      };
    }, []);
    index(() => {
      if (referenceEl) referenceRef.current = referenceEl;
      if (floatingEl) floatingRef.current = floatingEl;
      if (referenceEl && floatingEl) {
        if (whileElementsMountedRef.current) {
          return whileElementsMountedRef.current(referenceEl, floatingEl, update);
        }
        update();
      }
    }, [referenceEl, floatingEl, update, whileElementsMountedRef, hasWhileElementsMounted]);
    const refs = React4.useMemo(() => ({
      reference: referenceRef,
      floating: floatingRef,
      setReference,
      setFloating
    }), [setReference, setFloating]);
    const elements = React4.useMemo(() => ({
      reference: referenceEl,
      floating: floatingEl
    }), [referenceEl, floatingEl]);
    const floatingStyles = React4.useMemo(() => {
      const initialStyles = {
        position: strategy,
        left: 0,
        top: 0
      };
      if (!elements.floating) {
        return initialStyles;
      }
      const x9 = roundByDPR(elements.floating, data.x);
      const y6 = roundByDPR(elements.floating, data.y);
      if (transform) {
        return {
          ...initialStyles,
          transform: "translate(" + x9 + "px, " + y6 + "px)",
          ...getDPR(elements.floating) >= 1.5 && {
            willChange: "transform"
          }
        };
      }
      return {
        position: strategy,
        left: x9,
        top: y6
      };
    }, [strategy, transform, elements.floating, data.x, data.y]);
    return React4.useMemo(() => ({
      ...data,
      update,
      refs,
      elements,
      floatingStyles
    }), [data, update, refs, elements, floatingStyles]);
  }
  var offset3 = (options, deps) => {
    const result = offset2(options);
    return {
      name: result.name,
      fn: result.fn,
      options: [options, deps]
    };
  };
  var shift3 = (options, deps) => {
    const result = shift2(options);
    return {
      name: result.name,
      fn: result.fn,
      options: [options, deps]
    };
  };
  var flip3 = (options, deps) => {
    const result = flip2(options);
    return {
      name: result.name,
      fn: result.fn,
      options: [options, deps]
    };
  };
  var size3 = (options, deps) => {
    const result = size2(options);
    return {
      name: result.name,
      fn: result.fn,
      options: [options, deps]
    };
  };

  // frontend/node_modules/@floating-ui/react/dist/floating-ui.react.mjs
  var SafeReact = {
    ...React5
  };
  var useInsertionEffect3 = SafeReact.useInsertionEffect;
  var useSafeInsertionEffect = useInsertionEffect3 || ((fn) => fn());
  function useEffectEvent(callback) {
    const ref = React5.useRef(() => {
      if (true) {
        throw new Error("Cannot call an event handler while rendering.");
      }
    });
    useSafeInsertionEffect(() => {
      ref.current = callback;
    });
    return React5.useCallback(function() {
      for (var _len = arguments.length, args = new Array(_len), _key = 0; _key < _len; _key++) {
        args[_key] = arguments[_key];
      }
      return ref.current == null ? void 0 : ref.current(...args);
    }, []);
  }
  var ARROW_UP = "ArrowUp";
  var ARROW_DOWN = "ArrowDown";
  var ARROW_LEFT = "ArrowLeft";
  var ARROW_RIGHT = "ArrowRight";
  var index2 = typeof document !== "undefined" ? import_react64.useLayoutEffect : import_react64.useEffect;
  var horizontalKeys = [ARROW_LEFT, ARROW_RIGHT];
  var verticalKeys = [ARROW_UP, ARROW_DOWN];
  var allKeys = [...horizontalKeys, ...verticalKeys];
  var serverHandoffComplete = false;
  var count = 0;
  var genId = () => (
    // Ensure the id is unique with multiple independent versions of Floating UI
    // on <React 18
    "floating-ui-" + Math.random().toString(36).slice(2, 6) + count++
  );
  function useFloatingId() {
    const [id3, setId] = React5.useState(() => serverHandoffComplete ? genId() : void 0);
    index2(() => {
      if (id3 == null) {
        setId(genId());
      }
    }, []);
    React5.useEffect(() => {
      serverHandoffComplete = true;
    }, []);
    return id3;
  }
  var useReactId = SafeReact.useId;
  var useId2 = useReactId || useFloatingId;
  var devMessageSet;
  if (true) {
    devMessageSet = /* @__PURE__ */ new Set();
  }
  function warn() {
    var _devMessageSet;
    for (var _len = arguments.length, messages = new Array(_len), _key = 0; _key < _len; _key++) {
      messages[_key] = arguments[_key];
    }
    const message = "Floating UI: " + messages.join(" ");
    if (!((_devMessageSet = devMessageSet) != null && _devMessageSet.has(message))) {
      var _devMessageSet2;
      (_devMessageSet2 = devMessageSet) == null || _devMessageSet2.add(message);
      console.warn(message);
    }
  }
  function error() {
    var _devMessageSet3;
    for (var _len2 = arguments.length, messages = new Array(_len2), _key2 = 0; _key2 < _len2; _key2++) {
      messages[_key2] = arguments[_key2];
    }
    const message = "Floating UI: " + messages.join(" ");
    if (!((_devMessageSet3 = devMessageSet) != null && _devMessageSet3.has(message))) {
      var _devMessageSet4;
      (_devMessageSet4 = devMessageSet) == null || _devMessageSet4.add(message);
      console.error(message);
    }
  }
  function createPubSub() {
    const map = /* @__PURE__ */ new Map();
    return {
      emit(event, data) {
        var _map$get;
        (_map$get = map.get(event)) == null || _map$get.forEach((handler) => handler(data));
      },
      on(event, listener) {
        map.set(event, [...map.get(event) || [], listener]);
      },
      off(event, listener) {
        var _map$get2;
        map.set(event, ((_map$get2 = map.get(event)) == null ? void 0 : _map$get2.filter((l13) => l13 !== listener)) || []);
      }
    };
  }
  var FloatingNodeContext = /* @__PURE__ */ React5.createContext(null);
  var FloatingTreeContext = /* @__PURE__ */ React5.createContext(null);
  var useFloatingParentNodeId = () => {
    var _React$useContext;
    return ((_React$useContext = React5.useContext(FloatingNodeContext)) == null ? void 0 : _React$useContext.id) || null;
  };
  var useFloatingTree = () => React5.useContext(FloatingTreeContext);
  var FOCUSABLE_ATTRIBUTE = "data-floating-ui-focusable";
  function useFloatingRootContext(options) {
    const {
      open = false,
      onOpenChange: onOpenChangeProp,
      elements: elementsProp
    } = options;
    const floatingId = useId2();
    const dataRef = React5.useRef({});
    const [events] = React5.useState(() => createPubSub());
    const nested = useFloatingParentNodeId() != null;
    if (true) {
      const optionDomReference = elementsProp.reference;
      if (optionDomReference && !isElement(optionDomReference)) {
        error("Cannot pass a virtual element to the `elements.reference` option,", "as it must be a real DOM element. Use `refs.setPositionReference()`", "instead.");
      }
    }
    const [positionReference, setPositionReference] = React5.useState(elementsProp.reference);
    const onOpenChange = useEffectEvent((open2, event, reason) => {
      dataRef.current.openEvent = open2 ? event : void 0;
      events.emit("openchange", {
        open: open2,
        event,
        reason,
        nested
      });
      onOpenChangeProp == null || onOpenChangeProp(open2, event, reason);
    });
    const refs = React5.useMemo(() => ({
      setPositionReference
    }), []);
    const elements = React5.useMemo(() => ({
      reference: positionReference || elementsProp.reference || null,
      floating: elementsProp.floating || null,
      domReference: elementsProp.reference
    }), [positionReference, elementsProp.reference, elementsProp.floating]);
    return React5.useMemo(() => ({
      dataRef,
      open,
      onOpenChange,
      elements,
      events,
      floatingId,
      refs
    }), [open, onOpenChange, elements, events, floatingId, refs]);
  }
  function useFloating2(options) {
    if (options === void 0) {
      options = {};
    }
    const {
      nodeId
    } = options;
    const internalRootContext = useFloatingRootContext({
      ...options,
      elements: {
        reference: null,
        floating: null,
        ...options.elements
      }
    });
    const rootContext = options.rootContext || internalRootContext;
    const computedElements = rootContext.elements;
    const [_domReference, setDomReference] = React5.useState(null);
    const [positionReference, _setPositionReference] = React5.useState(null);
    const optionDomReference = computedElements == null ? void 0 : computedElements.domReference;
    const domReference = optionDomReference || _domReference;
    const domReferenceRef = React5.useRef(null);
    const tree = useFloatingTree();
    index2(() => {
      if (domReference) {
        domReferenceRef.current = domReference;
      }
    }, [domReference]);
    const position = useFloating({
      ...options,
      elements: {
        ...computedElements,
        ...positionReference && {
          reference: positionReference
        }
      }
    });
    const setPositionReference = React5.useCallback((node) => {
      const computedPositionReference = isElement(node) ? {
        getBoundingClientRect: () => node.getBoundingClientRect(),
        contextElement: node
      } : node;
      _setPositionReference(computedPositionReference);
      position.refs.setReference(computedPositionReference);
    }, [position.refs]);
    const setReference = React5.useCallback((node) => {
      if (isElement(node) || node === null) {
        domReferenceRef.current = node;
        setDomReference(node);
      }
      if (isElement(position.refs.reference.current) || position.refs.reference.current === null || // Don't allow setting virtual elements using the old technique back to
      // `null` to support `positionReference` + an unstable `reference`
      // callback ref.
      node !== null && !isElement(node)) {
        position.refs.setReference(node);
      }
    }, [position.refs]);
    const refs = React5.useMemo(() => ({
      ...position.refs,
      setReference,
      setPositionReference,
      domReference: domReferenceRef
    }), [position.refs, setReference, setPositionReference]);
    const elements = React5.useMemo(() => ({
      ...position.elements,
      domReference
    }), [position.elements, domReference]);
    const context = React5.useMemo(() => ({
      ...position,
      ...rootContext,
      refs,
      elements,
      nodeId
    }), [position, refs, elements, nodeId, rootContext]);
    index2(() => {
      rootContext.dataRef.current.floatingContext = context;
      const node = tree == null ? void 0 : tree.nodesRef.current.find((node2) => node2.id === nodeId);
      if (node) {
        node.context = context;
      }
    });
    return React5.useMemo(() => ({
      ...position,
      context,
      refs,
      elements
    }), [position, refs, elements, context]);
  }
  var ACTIVE_KEY = "active";
  var SELECTED_KEY = "selected";
  function mergeProps(userProps, propsList, elementKey) {
    const map = /* @__PURE__ */ new Map();
    const isItem = elementKey === "item";
    let domUserProps = userProps;
    if (isItem && userProps) {
      const {
        [ACTIVE_KEY]: _5,
        [SELECTED_KEY]: __,
        ...validProps
      } = userProps;
      domUserProps = validProps;
    }
    return {
      ...elementKey === "floating" && {
        tabIndex: -1,
        [FOCUSABLE_ATTRIBUTE]: ""
      },
      ...domUserProps,
      ...propsList.map((value) => {
        const propsOrGetProps = value ? value[elementKey] : null;
        if (typeof propsOrGetProps === "function") {
          return userProps ? propsOrGetProps(userProps) : null;
        }
        return propsOrGetProps;
      }).concat(userProps).reduce((acc, props) => {
        if (!props) {
          return acc;
        }
        Object.entries(props).forEach((_ref) => {
          let [key, value] = _ref;
          if (isItem && [ACTIVE_KEY, SELECTED_KEY].includes(key)) {
            return;
          }
          if (key.indexOf("on") === 0) {
            if (!map.has(key)) {
              map.set(key, []);
            }
            if (typeof value === "function") {
              var _map$get;
              (_map$get = map.get(key)) == null || _map$get.push(value);
              acc[key] = function() {
                var _map$get2;
                for (var _len = arguments.length, args = new Array(_len), _key = 0; _key < _len; _key++) {
                  args[_key] = arguments[_key];
                }
                return (_map$get2 = map.get(key)) == null ? void 0 : _map$get2.map((fn) => fn(...args)).find((val) => val !== void 0);
              };
            }
          } else {
            acc[key] = value;
          }
        });
        return acc;
      }, {})
    };
  }
  function useInteractions(propsList) {
    if (propsList === void 0) {
      propsList = [];
    }
    const referenceDeps = propsList.map((key) => key == null ? void 0 : key.reference);
    const floatingDeps = propsList.map((key) => key == null ? void 0 : key.floating);
    const itemDeps = propsList.map((key) => key == null ? void 0 : key.item);
    const getReferenceProps = React5.useCallback(
      (userProps) => mergeProps(userProps, propsList, "reference"),
      // eslint-disable-next-line react-hooks/exhaustive-deps
      referenceDeps
    );
    const getFloatingProps = React5.useCallback(
      (userProps) => mergeProps(userProps, propsList, "floating"),
      // eslint-disable-next-line react-hooks/exhaustive-deps
      floatingDeps
    );
    const getItemProps = React5.useCallback(
      (userProps) => mergeProps(userProps, propsList, "item"),
      // eslint-disable-next-line react-hooks/exhaustive-deps
      itemDeps
    );
    return React5.useMemo(() => ({
      getReferenceProps,
      getFloatingProps,
      getItemProps
    }), [getReferenceProps, getFloatingProps, getItemProps]);
  }
  function getArgsWithCustomFloatingHeight(state, height) {
    return {
      ...state,
      rects: {
        ...state.rects,
        floating: {
          ...state.rects.floating,
          height
        }
      }
    };
  }
  var inner = (props) => ({
    name: "inner",
    options: props,
    async fn(state) {
      const {
        listRef,
        overflowRef,
        onFallbackChange,
        offset: innerOffset = 0,
        index: index3 = 0,
        minItemsVisible = 4,
        referenceOverflowThreshold = 0,
        scrollRef,
        ...detectOverflowOptions
      } = evaluate(props, state);
      const {
        rects,
        elements: {
          floating
        }
      } = state;
      const item = listRef.current[index3];
      const scrollEl = (scrollRef == null ? void 0 : scrollRef.current) || floating;
      const clientTop = floating.clientTop || scrollEl.clientTop;
      const floatingIsBordered = floating.clientTop !== 0;
      const scrollElIsBordered = scrollEl.clientTop !== 0;
      const floatingIsScrollEl = floating === scrollEl;
      if (true) {
        if (!state.placement.startsWith("bottom")) {
          warn('`placement` side must be "bottom" when using the `inner`', "middleware.");
        }
      }
      if (!item) {
        return {};
      }
      const nextArgs = {
        ...state,
        ...await offset3(-item.offsetTop - floating.clientTop - rects.reference.height / 2 - item.offsetHeight / 2 - innerOffset).fn(state)
      };
      const overflow = await detectOverflow2(getArgsWithCustomFloatingHeight(nextArgs, scrollEl.scrollHeight + clientTop + floating.clientTop), detectOverflowOptions);
      const refOverflow = await detectOverflow2(nextArgs, {
        ...detectOverflowOptions,
        elementContext: "reference"
      });
      const diffY = max(0, overflow.top);
      const nextY = nextArgs.y + diffY;
      const isScrollable = scrollEl.scrollHeight > scrollEl.clientHeight;
      const rounder = isScrollable ? (v6) => v6 : round;
      const maxHeight = rounder(max(0, scrollEl.scrollHeight + (floatingIsBordered && floatingIsScrollEl || scrollElIsBordered ? clientTop * 2 : 0) - diffY - max(0, overflow.bottom)));
      scrollEl.style.maxHeight = maxHeight + "px";
      scrollEl.scrollTop = diffY;
      if (onFallbackChange) {
        const shouldFallback = scrollEl.offsetHeight < item.offsetHeight * min(minItemsVisible, listRef.current.length) - 1 || refOverflow.top >= -referenceOverflowThreshold || refOverflow.bottom >= -referenceOverflowThreshold;
        ReactDOM2.flushSync(() => onFallbackChange(shouldFallback));
      }
      if (overflowRef) {
        overflowRef.current = await detectOverflow2(getArgsWithCustomFloatingHeight({
          ...nextArgs,
          y: nextY
        }, scrollEl.offsetHeight + clientTop + floating.clientTop), detectOverflowOptions);
      }
      return {
        y: nextY
      };
    }
  });
  function useInnerOffset(context, props) {
    const {
      open,
      elements
    } = context;
    const {
      enabled = true,
      overflowRef,
      scrollRef,
      onChange: unstable_onChange
    } = props;
    const onChange = useEffectEvent(unstable_onChange);
    const controlledScrollingRef = React5.useRef(false);
    const prevScrollTopRef = React5.useRef(null);
    const initialOverflowRef = React5.useRef(null);
    React5.useEffect(() => {
      if (!enabled) return;
      function onWheel(e8) {
        if (e8.ctrlKey || !el || overflowRef.current == null) {
          return;
        }
        const dY = e8.deltaY;
        const isAtTop = overflowRef.current.top >= -0.5;
        const isAtBottom = overflowRef.current.bottom >= -0.5;
        const remainingScroll = el.scrollHeight - el.clientHeight;
        const sign = dY < 0 ? -1 : 1;
        const method = dY < 0 ? "max" : "min";
        if (el.scrollHeight <= el.clientHeight) {
          return;
        }
        if (!isAtTop && dY > 0 || !isAtBottom && dY < 0) {
          e8.preventDefault();
          ReactDOM2.flushSync(() => {
            onChange((d9) => d9 + Math[method](dY, remainingScroll * sign));
          });
        } else if (/firefox/i.test(getUserAgent())) {
          el.scrollTop += dY;
        }
      }
      const el = (scrollRef == null ? void 0 : scrollRef.current) || elements.floating;
      if (open && el) {
        el.addEventListener("wheel", onWheel);
        requestAnimationFrame(() => {
          prevScrollTopRef.current = el.scrollTop;
          if (overflowRef.current != null) {
            initialOverflowRef.current = {
              ...overflowRef.current
            };
          }
        });
        return () => {
          prevScrollTopRef.current = null;
          initialOverflowRef.current = null;
          el.removeEventListener("wheel", onWheel);
        };
      }
    }, [enabled, open, elements.floating, overflowRef, scrollRef, onChange]);
    const floating = React5.useMemo(() => ({
      onKeyDown() {
        controlledScrollingRef.current = true;
      },
      onWheel() {
        controlledScrollingRef.current = false;
      },
      onPointerMove() {
        controlledScrollingRef.current = false;
      },
      onScroll() {
        const el = (scrollRef == null ? void 0 : scrollRef.current) || elements.floating;
        if (!overflowRef.current || !el || !controlledScrollingRef.current) {
          return;
        }
        if (prevScrollTopRef.current !== null) {
          const scrollDiff = el.scrollTop - prevScrollTopRef.current;
          if (overflowRef.current.bottom < -0.5 && scrollDiff < -1 || overflowRef.current.top < -0.5 && scrollDiff > 1) {
            ReactDOM2.flushSync(() => onChange((d9) => d9 + scrollDiff));
          }
        }
        requestAnimationFrame(() => {
          prevScrollTopRef.current = el.scrollTop;
        });
      }
    }), [elements.floating, onChange, overflowRef, scrollRef]);
    return React5.useMemo(() => enabled ? {
      floating
    } : {}, [enabled, floating]);
  }

  // frontend/node_modules/@headlessui/react/dist/internal/floating.js
  var j5 = __toESM(require_react_shim(), 1);
  var import_react66 = __toESM(require_react_shim(), 1);
  var y4 = (0, import_react66.createContext)({ styles: void 0, setReference: () => {
  }, setFloating: () => {
  }, getReferenceProps: () => ({}), getFloatingProps: () => ({}), slot: {} });
  y4.displayName = "FloatingContext";
  var $ = (0, import_react66.createContext)(null);
  $.displayName = "PlacementContext";
  function ye(e8) {
    return (0, import_react66.useMemo)(() => e8 ? typeof e8 == "string" ? { to: e8 } : e8 : null, [e8]);
  }
  function Fe() {
    return (0, import_react66.useContext)(y4).setReference;
  }
  function be() {
    return (0, import_react66.useContext)(y4).getReferenceProps;
  }
  function Te() {
    let { getFloatingProps: e8, slot: t12 } = (0, import_react66.useContext)(y4);
    return (0, import_react66.useCallback)((...n13) => Object.assign({}, e8(...n13), { "data-anchor": t12.anchor }), [e8, t12]);
  }
  function Re(e8 = null) {
    e8 === false && (e8 = null), typeof e8 == "string" && (e8 = { to: e8 });
    let t12 = (0, import_react66.useContext)($), n13 = (0, import_react66.useMemo)(() => e8, [JSON.stringify(e8, (l13, o16) => {
      var u16;
      return (u16 = o16 == null ? void 0 : o16.outerHTML) != null ? u16 : o16;
    })]);
    n(() => {
      t12 == null || t12(n13 != null ? n13 : null);
    }, [t12, n13]);
    let r16 = (0, import_react66.useContext)(y4);
    return (0, import_react66.useMemo)(() => [r16.setFloating, e8 ? r16.styles : {}], [r16.setFloating, e8, r16.styles]);
  }
  var D5 = 4;
  function Ae({ children: e8, enabled: t12 = true }) {
    let [n13, r16] = (0, import_react66.useState)(null), [l13, o16] = (0, import_react66.useState)(0), u16 = (0, import_react66.useRef)(null), [f13, s15] = (0, import_react66.useState)(null);
    ce(f13);
    let i13 = t12 && n13 !== null && f13 !== null, { to: F5 = "bottom", gap: E9 = 0, offset: A5 = 0, padding: c14 = 0, inner: h10 } = ge(n13, f13), [a21, p8 = "center"] = F5.split(" ");
    n(() => {
      i13 && o16(0);
    }, [i13]);
    let { refs: b8, floatingStyles: S9, context: g3 } = useFloating2({ open: i13, placement: a21 === "selection" ? p8 === "center" ? "bottom" : `bottom-${p8}` : p8 === "center" ? `${a21}` : `${a21}-${p8}`, strategy: "absolute", transform: false, middleware: [offset3({ mainAxis: a21 === "selection" ? 0 : E9, crossAxis: A5 }), shift3({ padding: c14 }), a21 !== "selection" && flip3({ padding: c14 }), a21 === "selection" && h10 ? inner({ ...h10, padding: c14, overflowRef: u16, offset: l13, minItemsVisible: D5, referenceOverflowThreshold: c14, onFallbackChange(P5) {
      var L5, N3;
      if (!P5) return;
      let d9 = g3.elements.floating;
      if (!d9) return;
      let M8 = parseFloat(getComputedStyle(d9).scrollPaddingBottom) || 0, I7 = Math.min(D5, d9.childElementCount), W = 0, B2 = 0;
      for (let m9 of (N3 = (L5 = g3.elements.floating) == null ? void 0 : L5.childNodes) != null ? N3 : []) if (n5(m9)) {
        let x9 = m9.offsetTop, k10 = x9 + m9.clientHeight + M8, H6 = d9.scrollTop, U3 = H6 + d9.clientHeight;
        if (x9 >= H6 && k10 <= U3) I7--;
        else {
          B2 = Math.max(0, Math.min(k10, U3) - Math.max(x9, H6)), W = m9.clientHeight;
          break;
        }
      }
      I7 >= 1 && o16((m9) => {
        let x9 = W * I7 - B2 + M8;
        return m9 >= x9 ? m9 : x9;
      });
    } }) : null, size3({ padding: c14, apply({ availableWidth: P5, availableHeight: d9, elements: M8 }) {
      Object.assign(M8.floating.style, { overflow: "auto", maxWidth: `${P5}px`, maxHeight: `min(var(--anchor-max-height, 100vh), ${d9}px)` });
    } })].filter(Boolean), whileElementsMounted: autoUpdate }), [w9 = a21, V4 = p8] = g3.placement.split("-");
    a21 === "selection" && (w9 = "selection");
    let G5 = (0, import_react66.useMemo)(() => ({ anchor: [w9, V4].filter(Boolean).join(" ") }), [w9, V4]), K3 = useInnerOffset(g3, { overflowRef: u16, onChange: o16 }), { getReferenceProps: Q2, getFloatingProps: X3 } = useInteractions([K3]), Y2 = o4((P5) => {
      s15(P5), b8.setFloating(P5);
    });
    return j5.createElement($.Provider, { value: r16 }, j5.createElement(y4.Provider, { value: { setFloating: Y2, setReference: b8.setReference, styles: S9, getReferenceProps: Q2, getFloatingProps: X3, slot: G5 } }, e8));
  }
  function ce(e8) {
    n(() => {
      if (!e8) return;
      let t12 = new MutationObserver(() => {
        let n13 = window.getComputedStyle(e8).maxHeight, r16 = parseFloat(n13);
        if (isNaN(r16)) return;
        let l13 = parseInt(n13);
        isNaN(l13) || r16 !== l13 && (e8.style.maxHeight = `${Math.ceil(r16)}px`);
      });
      return t12.observe(e8, { attributes: true, attributeFilter: ["style"] }), () => {
        t12.disconnect();
      };
    }, [e8]);
  }
  function ge(e8, t12) {
    var o16, u16, f13;
    let n13 = O3((o16 = e8 == null ? void 0 : e8.gap) != null ? o16 : "var(--anchor-gap, 0)", t12), r16 = O3((u16 = e8 == null ? void 0 : e8.offset) != null ? u16 : "var(--anchor-offset, 0)", t12), l13 = O3((f13 = e8 == null ? void 0 : e8.padding) != null ? f13 : "var(--anchor-padding, 0)", t12);
    return { ...e8, gap: n13, offset: r16, padding: l13 };
  }
  function O3(e8, t12, n13 = void 0) {
    let r16 = p(), l13 = o4((s15, i13) => {
      if (s15 == null) return [n13, null];
      if (typeof s15 == "number") return [s15, null];
      if (typeof s15 == "string") {
        if (!i13) return [n13, null];
        let F5 = J(s15, i13);
        return [F5, (E9) => {
          let A5 = q(s15);
          {
            let c14 = A5.map((h10) => window.getComputedStyle(i13).getPropertyValue(h10));
            r16.requestAnimationFrame(function h10() {
              r16.nextFrame(h10);
              let a21 = false;
              for (let [b8, S9] of A5.entries()) {
                let g3 = window.getComputedStyle(i13).getPropertyValue(S9);
                if (c14[b8] !== g3) {
                  c14[b8] = g3, a21 = true;
                  break;
                }
              }
              if (!a21) return;
              let p8 = J(s15, i13);
              F5 !== p8 && (E9(p8), F5 = p8);
            });
          }
          return r16.dispose;
        }];
      }
      return [n13, null];
    }), o16 = (0, import_react66.useMemo)(() => l13(e8, t12)[0], [e8, t12]), [u16 = o16, f13] = (0, import_react66.useState)();
    return n(() => {
      let [s15, i13] = l13(e8, t12);
      if (f13(s15), !!i13) return i13(f13);
    }, [e8, t12]), u16;
  }
  function q(e8) {
    let t12 = /var\((.*)\)/.exec(e8);
    if (t12) {
      let n13 = t12[1].indexOf(",");
      if (n13 === -1) return [t12[1]];
      let r16 = t12[1].slice(0, n13).trim(), l13 = t12[1].slice(n13 + 1).trim();
      return l13 ? [r16, ...q(l13)] : [r16];
    }
    return [];
  }
  function J(e8, t12) {
    let n13 = document.createElement("div");
    t12.appendChild(n13), n13.style.setProperty("margin-top", "0px", "important"), n13.style.setProperty("margin-top", e8, "important");
    let r16 = parseFloat(window.getComputedStyle(n13).marginTop) || 0;
    return t12.removeChild(n13), r16;
  }

  // frontend/node_modules/@headlessui/react/dist/internal/open-closed.js
  init_define_import_meta_env();
  var import_react67 = __toESM(require_react_shim(), 1);
  var n10 = (0, import_react67.createContext)(null);
  n10.displayName = "OpenClosedContext";
  var i10 = ((e8) => (e8[e8.Open = 1] = "Open", e8[e8.Closed = 2] = "Closed", e8[e8.Closing = 4] = "Closing", e8[e8.Opening = 8] = "Opening", e8))(i10 || {});
  function u11() {
    return (0, import_react67.useContext)(n10);
  }
  function c7({ value: o16, children: t12 }) {
    return import_react67.default.createElement(n10.Provider, { value: o16 }, t12);
  }
  function s10({ children: o16 }) {
    return import_react67.default.createElement(n10.Provider, { value: null }, o16);
  }

  // frontend/node_modules/@headlessui/react/dist/utils/active-element-history.js
  init_define_import_meta_env();

  // frontend/node_modules/@headlessui/react/dist/utils/document-ready.js
  init_define_import_meta_env();
  function t9(n13) {
    function e8() {
      document.readyState !== "loading" && (n13(), document.removeEventListener("DOMContentLoaded", e8));
    }
    typeof window != "undefined" && typeof document != "undefined" && (document.addEventListener("DOMContentLoaded", e8), e8());
  }

  // frontend/node_modules/@headlessui/react/dist/utils/active-element-history.js
  var n11 = [];
  t9(() => {
    function e8(t12) {
      if (!i4(t12.target) || t12.target === document.body || n11[0] === t12.target) return;
      let r16 = t12.target;
      r16 = r16.closest(E4), n11.unshift(r16 != null ? r16 : t12.target), n11 = n11.filter((o16) => o16 != null && o16.isConnected), n11.splice(10);
    }
    window.addEventListener("click", e8, { capture: true }), window.addEventListener("mousedown", e8, { capture: true }), window.addEventListener("focus", e8, { capture: true }), document.body.addEventListener("click", e8, { capture: true }), document.body.addEventListener("mousedown", e8, { capture: true }), document.body.addEventListener("focus", e8, { capture: true });
  });

  // frontend/node_modules/@headlessui/react/dist/utils/calculate-active-index.js
  init_define_import_meta_env();
  function u12(l13) {
    throw new Error("Unexpected object: " + l13);
  }
  var c8 = ((i13) => (i13[i13.First = 0] = "First", i13[i13.Previous = 1] = "Previous", i13[i13.Next = 2] = "Next", i13[i13.Last = 3] = "Last", i13[i13.Specific = 4] = "Specific", i13[i13.Nothing = 5] = "Nothing", i13))(c8 || {});
  function f9(l13, n13) {
    let t12 = n13.resolveItems();
    if (t12.length <= 0) return null;
    let r16 = n13.resolveActiveIndex(), s15 = r16 != null ? r16 : -1;
    switch (l13.focus) {
      case 0: {
        for (let e8 = 0; e8 < t12.length; ++e8) if (!n13.resolveDisabled(t12[e8], e8, t12)) return e8;
        return r16;
      }
      case 1: {
        s15 === -1 && (s15 = t12.length);
        for (let e8 = s15 - 1; e8 >= 0; --e8) if (!n13.resolveDisabled(t12[e8], e8, t12)) return e8;
        return r16;
      }
      case 2: {
        for (let e8 = s15 + 1; e8 < t12.length; ++e8) if (!n13.resolveDisabled(t12[e8], e8, t12)) return e8;
        return r16;
      }
      case 3: {
        for (let e8 = t12.length - 1; e8 >= 0; --e8) if (!n13.resolveDisabled(t12[e8], e8, t12)) return e8;
        return r16;
      }
      case 4: {
        for (let e8 = 0; e8 < t12.length; ++e8) if (n13.resolveId(t12[e8], e8, t12) === l13.id) return e8;
        return r16;
      }
      case 5:
        return null;
      default:
        u12(l13);
    }
  }

  // frontend/node_modules/@headlessui/react/dist/components/portal/portal.js
  init_define_import_meta_env();
  var import_react70 = __toESM(require_react_shim(), 1);
  var import_react_dom3 = __toESM(require_react_dom_shim(), 1);

  // frontend/node_modules/@headlessui/react/dist/hooks/use-on-unmount.js
  init_define_import_meta_env();
  var import_react68 = __toESM(require_react_shim(), 1);
  function c9(t12) {
    let r16 = o4(t12), e8 = (0, import_react68.useRef)(false);
    (0, import_react68.useEffect)(() => (e8.current = false, () => {
      e8.current = true, t(() => {
        e8.current && r16();
      });
    }), [r16]);
  }

  // frontend/node_modules/@headlessui/react/dist/hooks/use-server-handoff-complete.js
  init_define_import_meta_env();
  var t10 = __toESM(require_react_shim(), 1);
  function s11() {
    let r16 = typeof document == "undefined";
    return "useSyncExternalStore" in t10 ? ((o16) => o16.useSyncExternalStore)(t10)(() => () => {
    }, () => false, () => !r16) : false;
  }
  function l9() {
    let r16 = s11(), [e8, n13] = t10.useState(s.isHandoffComplete);
    return e8 && s.isHandoffComplete === false && n13(false), t10.useEffect(() => {
      e8 !== true && n13(true);
    }, [e8]), t10.useEffect(() => s.handoff(), []), r16 ? false : e8;
  }

  // frontend/node_modules/@headlessui/react/dist/internal/portal-force-root.js
  init_define_import_meta_env();
  var import_react69 = __toESM(require_react_shim(), 1);
  var e7 = (0, import_react69.createContext)(false);
  function a15() {
    return (0, import_react69.useContext)(e7);
  }
  function l10(o16) {
    return import_react69.default.createElement(e7.Provider, { value: o16.force }, o16.children);
  }

  // frontend/node_modules/@headlessui/react/dist/components/portal/portal.js
  function j6(e8) {
    let o16 = a15(), l13 = (0, import_react70.useContext)(c11), [r16, p8] = (0, import_react70.useState)(() => {
      var s15;
      if (!o16 && l13 !== null) return (s15 = l13.current) != null ? s15 : null;
      if (s.isServer) return null;
      let t12 = e8 == null ? void 0 : e8.getElementById("headlessui-portal-root");
      if (t12) return t12;
      if (e8 === null) return null;
      let n13 = e8.createElement("div");
      return n13.setAttribute("id", "headlessui-portal-root"), e8.body.appendChild(n13);
    });
    return (0, import_react70.useEffect)(() => {
      r16 !== null && (e8 != null && e8.body.contains(r16) || e8 == null || e8.body.appendChild(r16));
    }, [r16, e8]), (0, import_react70.useEffect)(() => {
      o16 || l13 !== null && p8(l13.current);
    }, [l13, p8, o16]), r16;
  }
  var _4 = import_react70.Fragment;
  var I5 = Y(function(o16, l13) {
    let { ownerDocument: r16 = null, ...p8 } = o16, t12 = (0, import_react70.useRef)(null), n13 = y(T((a21) => {
      t12.current = a21;
    }), l13), s15 = u9(t12.current), C8 = r16 != null ? r16 : s15, u16 = j6(C8), y6 = (0, import_react70.useContext)(m8), g3 = p(), v6 = l9(), M8 = K();
    return c9(() => {
      var a21;
      u16 && u16.childNodes.length <= 0 && ((a21 = u16.parentElement) == null || a21.removeChild(u16));
    }), !u16 || !v6 ? null : (0, import_react_dom3.createPortal)(import_react70.default.createElement("div", { "data-headlessui-portal": "", ref: (a21) => {
      g3.dispose(), y6 && a21 && g3.add(y6.register(a21));
    } }, M8({ ourProps: { ref: n13 }, theirProps: p8, slot: {}, defaultTag: _4, name: "Portal" })), u16);
  });
  function D6(e8, o16) {
    let l13 = y(o16), { enabled: r16 = true, ownerDocument: p8, ...t12 } = e8, n13 = K();
    return r16 ? import_react70.default.createElement(I5, { ...t12, ownerDocument: p8, ref: l13 }) : n13({ ourProps: { ref: l13 }, theirProps: t12, slot: {}, defaultTag: _4, name: "Portal" });
  }
  var J2 = import_react70.Fragment;
  var c11 = (0, import_react70.createContext)(null);
  function X(e8, o16) {
    let { target: l13, ...r16 } = e8, t12 = { ref: y(o16) }, n13 = K();
    return import_react70.default.createElement(c11.Provider, { value: l13 }, n13({ ourProps: t12, theirProps: r16, defaultTag: J2, name: "Popover.Group" }));
  }
  var m8 = (0, import_react70.createContext)(null);
  function oe() {
    let e8 = (0, import_react70.useContext)(m8), o16 = (0, import_react70.useRef)([]), l13 = o4((t12) => (o16.current.push(t12), e8 && e8.register(t12), () => r16(t12))), r16 = o4((t12) => {
      let n13 = o16.current.indexOf(t12);
      n13 !== -1 && o16.current.splice(n13, 1), e8 && e8.unregister(t12);
    }), p8 = (0, import_react70.useMemo)(() => ({ register: l13, unregister: r16, portals: o16 }), [l13, r16, o16]);
    return [o16, (0, import_react70.useMemo)(() => function({ children: n13 }) {
      return import_react70.default.createElement(m8.Provider, { value: p8 }, n13);
    }, [p8])];
  }
  var k6 = Y(D6);
  var B = Y(X);
  var le = Object.assign(k6, { Group: B });

  // frontend/node_modules/@headlessui/react/dist/utils/element-movement.js
  init_define_import_meta_env();
  var c12 = { Idle: { kind: "Idle" }, Tracked: (e8) => ({ kind: "Tracked", position: e8 }), Moved: { kind: "Moved" } };
  function a16(e8) {
    let t12 = e8.getBoundingClientRect();
    return `${t12.x},${t12.y}`;
  }
  function p5(e8, t12, i13) {
    let n13 = o2();
    if (t12.kind === "Tracked") {
      let o16 = function() {
        d9 !== a16(e8) && (n13.dispose(), i13());
      };
      var f13 = o16;
      let { position: d9 } = t12, s15 = new ResizeObserver(o16);
      s15.observe(e8), n13.add(() => s15.disconnect()), n13.addEventListener(window, "scroll", o16, { passive: true }), n13.addEventListener(window, "resize", o16);
    }
    return () => n13.dispose();
  }

  // frontend/node_modules/@headlessui/react/dist/components/dialog/dialog.js
  init_define_import_meta_env();
  var import_react77 = __toESM(require_react_shim(), 1);

  // frontend/node_modules/@headlessui/react/dist/hooks/use-escape.js
  init_define_import_meta_env();
  function a17(o16, r16 = typeof document != "undefined" ? document.defaultView : null, t12) {
    let n13 = I3(o16, "escape");
    E6(r16, "keydown", (e8) => {
      n13 && (e8.defaultPrevented || e8.key === o6.Escape && t12(e8));
    });
  }

  // frontend/node_modules/@headlessui/react/dist/hooks/use-is-touch-device.js
  init_define_import_meta_env();
  var import_react71 = __toESM(require_react_shim(), 1);
  function f11() {
    var t12;
    let [e8] = (0, import_react71.useState)(() => typeof window != "undefined" && typeof window.matchMedia == "function" ? window.matchMedia("(pointer: coarse)") : null), [o16, c14] = (0, import_react71.useState)((t12 = e8 == null ? void 0 : e8.matches) != null ? t12 : false);
    return n(() => {
      if (!e8) return;
      function n13(r16) {
        c14(r16.matches);
      }
      return e8.addEventListener("change", n13), () => e8.removeEventListener("change", n13);
    }, [e8]), o16;
  }

  // frontend/node_modules/@headlessui/react/dist/hooks/use-root-containers.js
  init_define_import_meta_env();
  var import_react72 = __toESM(require_react_shim(), 1);
  function S7({ defaultContainers: l13 = [], portals: n13, mainTreeNode: o16 } = {}) {
    let c14 = o4(() => {
      var r16, u16;
      let i13 = l(o16), t12 = [];
      for (let e8 of l13) e8 !== null && (t5(e8) ? t12.push(e8) : "current" in e8 && t5(e8.current) && t12.push(e8.current));
      if (n13 != null && n13.current) for (let e8 of n13.current) t12.push(e8);
      for (let e8 of (r16 = i13 == null ? void 0 : i13.querySelectorAll("html > *, body > *")) != null ? r16 : []) e8 !== document.body && e8 !== document.head && t5(e8) && e8.id !== "headlessui-portal-root" && (o16 && (e8.contains(o16) || e8.contains((u16 = o16 == null ? void 0 : o16.getRootNode()) == null ? void 0 : u16.host)) || t12.some((E9) => e8.contains(E9)) || t12.push(e8));
      return t12;
    });
    return { resolveContainers: c14, contains: o4((i13) => c14().some((t12) => t12.contains(i13))) };
  }
  var d8 = (0, import_react72.createContext)(null);
  function j7({ children: l13, node: n13 }) {
    let [o16, c14] = (0, import_react72.useState)(null), i13 = x6(n13 != null ? n13 : o16);
    return import_react72.default.createElement(d8.Provider, { value: i13 }, l13, i13 === null && import_react72.default.createElement(f2, { features: s4.Hidden, ref: (t12) => {
      var r16, u16;
      if (t12) {
        for (let e8 of (u16 = (r16 = l(t12)) == null ? void 0 : r16.querySelectorAll("html > *, body > *")) != null ? u16 : []) if (e8 !== document.body && e8 !== document.head && t5(e8) && e8 != null && e8.contains(t12)) {
          c14(e8);
          break;
        }
      }
    } }));
  }
  function x6(l13 = null) {
    var n13;
    return (n13 = (0, import_react72.useContext)(d8)) != null ? n13 : l13;
  }

  // frontend/node_modules/@headlessui/react/dist/components/focus-trap/focus-trap.js
  init_define_import_meta_env();
  var import_react75 = __toESM(require_react_shim(), 1);

  // frontend/node_modules/@headlessui/react/dist/hooks/use-is-mounted.js
  init_define_import_meta_env();
  var import_react73 = __toESM(require_react_shim(), 1);
  function f12() {
    let e8 = (0, import_react73.useRef)(false);
    return n(() => (e8.current = true, () => {
      e8.current = false;
    }), []), e8;
  }

  // frontend/node_modules/@headlessui/react/dist/hooks/use-tab-direction.js
  init_define_import_meta_env();
  var import_react74 = __toESM(require_react_shim(), 1);
  var a18 = ((r16) => (r16[r16.Forwards = 0] = "Forwards", r16[r16.Backwards = 1] = "Backwards", r16))(a18 || {});
  function u14() {
    let e8 = (0, import_react74.useRef)(0);
    return s8(true, "keydown", (r16) => {
      r16.key === "Tab" && (e8.current = r16.shiftKey ? 1 : 0);
    }, true), e8;
  }

  // frontend/node_modules/@headlessui/react/dist/components/focus-trap/focus-trap.js
  function x7(o16) {
    if (!o16) return /* @__PURE__ */ new Set();
    if (typeof o16 == "function") return new Set(o16());
    let t12 = /* @__PURE__ */ new Set();
    for (let e8 of o16.current) t5(e8.current) && t12.add(e8.current);
    return t12;
  }
  var $2 = "div";
  var G4 = ((n13) => (n13[n13.None = 0] = "None", n13[n13.InitialFocus = 1] = "InitialFocus", n13[n13.TabLock = 2] = "TabLock", n13[n13.FocusLock = 4] = "FocusLock", n13[n13.RestoreFocus = 8] = "RestoreFocus", n13[n13.AutoFocus = 16] = "AutoFocus", n13))(G4 || {});
  function w6(o16, t12) {
    let e8 = (0, import_react75.useRef)(null), r16 = y(e8, t12), { initialFocus: u16, initialFocusFallback: a21, containers: n13, features: s15 = 15, ...f13 } = o16;
    l9() || (s15 = 0);
    let l13 = u9(e8.current);
    re(s15, { ownerDocument: l13 });
    let T9 = ne(s15, { ownerDocument: l13, container: e8, initialFocus: u16, initialFocusFallback: a21 });
    oe2(s15, { ownerDocument: l13, container: e8, containers: n13, previousActiveElement: T9 });
    let g3 = u14(), A5 = o4((c14) => {
      if (!n5(e8.current)) return;
      let E9 = e8.current;
      ((V4) => V4())(() => {
        u(g3.current, { [a18.Forwards]: () => {
          v4(E9, T4.First, { skipElements: [c14.relatedTarget, a21] });
        }, [a18.Backwards]: () => {
          v4(E9, T4.Last, { skipElements: [c14.relatedTarget, a21] });
        } });
      });
    }), v6 = I3(!!(s15 & 2), "focus-trap#tab-lock"), N3 = p(), b8 = (0, import_react75.useRef)(false), k10 = { ref: r16, onKeyDown(c14) {
      c14.key == "Tab" && (b8.current = true, N3.requestAnimationFrame(() => {
        b8.current = false;
      }));
    }, onBlur(c14) {
      if (!(s15 & 4)) return;
      let E9 = x7(n13);
      n5(e8.current) && E9.add(e8.current);
      let L5 = c14.relatedTarget;
      i4(L5) && L5.dataset.headlessuiFocusGuard !== "true" && (I6(E9, L5) || (b8.current ? v4(e8.current, u(g3.current, { [a18.Forwards]: () => T4.Next, [a18.Backwards]: () => T4.Previous }) | T4.WrapAround, { relativeTo: c14.target }) : i4(c14.target) && w4(c14.target)));
    } }, B2 = K();
    return import_react75.default.createElement(import_react75.default.Fragment, null, v6 && import_react75.default.createElement(f2, { as: "button", type: "button", "data-headlessui-focus-guard": true, onFocus: A5, features: s4.Focusable }), B2({ ourProps: k10, theirProps: f13, defaultTag: $2, name: "FocusTrap" }), v6 && import_react75.default.createElement(f2, { as: "button", type: "button", "data-headlessui-focus-guard": true, onFocus: A5, features: s4.Focusable }));
  }
  var ee = Y(w6);
  var ge2 = Object.assign(ee, { features: G4 });
  function te(o16 = true) {
    let t12 = (0, import_react75.useRef)(n11.slice());
    return m7(([e8], [r16]) => {
      r16 === true && e8 === false && t(() => {
        t12.current.splice(0);
      }), r16 === false && e8 === true && (t12.current = n11.slice());
    }, [o16, n11, t12]), o4(() => {
      var e8;
      return (e8 = t12.current.find((r16) => r16 != null && r16.isConnected)) != null ? e8 : null;
    });
  }
  function re(o16, { ownerDocument: t12 }) {
    let e8 = !!(o16 & 8), r16 = te(e8);
    m7(() => {
      e8 || d2(t12 == null ? void 0 : t12.body) && w4(r16());
    }, [e8]), c9(() => {
      e8 && w4(r16());
    });
  }
  function ne(o16, { ownerDocument: t12, container: e8, initialFocus: r16, initialFocusFallback: u16 }) {
    let a21 = (0, import_react75.useRef)(null), n13 = I3(!!(o16 & 1), "focus-trap#initial-focus"), s15 = f12();
    return m7(() => {
      if (o16 === 0) return;
      if (!n13) {
        u16 != null && u16.current && w4(u16.current);
        return;
      }
      let f13 = e8.current;
      f13 && t(() => {
        if (!s15.current) return;
        let l13 = t12 == null ? void 0 : t12.activeElement;
        if (r16 != null && r16.current) {
          if ((r16 == null ? void 0 : r16.current) === l13) {
            a21.current = l13;
            return;
          }
        } else if (f13.contains(l13)) {
          a21.current = l13;
          return;
        }
        if (r16 != null && r16.current) w4(r16.current);
        else {
          if (o16 & 16) {
            if (v4(f13, T4.First | T4.AutoFocus) !== A2.Error) return;
          } else if (v4(f13, T4.First) !== A2.Error) return;
          if (u16 != null && u16.current && (w4(u16.current), (t12 == null ? void 0 : t12.activeElement) === u16.current)) return;
          console.warn("There are no focusable elements inside the <FocusTrap />");
        }
        a21.current = t12 == null ? void 0 : t12.activeElement;
      });
    }, [u16, n13, o16]), a21;
  }
  function oe2(o16, { ownerDocument: t12, container: e8, containers: r16, previousActiveElement: u16 }) {
    let a21 = f12(), n13 = !!(o16 & 4);
    E6(t12 == null ? void 0 : t12.defaultView, "focus", (s15) => {
      if (!n13 || !a21.current) return;
      let f13 = x7(r16);
      n5(e8.current) && f13.add(e8.current);
      let l13 = u16.current;
      if (!l13) return;
      let T9 = s15.target;
      n5(T9) ? I6(f13, T9) ? (u16.current = T9, w4(T9)) : (s15.preventDefault(), s15.stopPropagation(), w4(l13)) : w4(u16.current);
    }, true);
  }
  function I6(o16, t12) {
    for (let e8 of o16) if (e8.contains(t12)) return true;
    return false;
  }

  // frontend/node_modules/@headlessui/react/dist/components/transition/transition.js
  init_define_import_meta_env();
  var import_react76 = __toESM(require_react_shim(), 1);
  function ue2(e8) {
    var t12;
    return !!(e8.enter || e8.enterFrom || e8.enterTo || e8.leave || e8.leaveFrom || e8.leaveTo) || !b((t12 = e8.as) != null ? t12 : de) || import_react76.default.Children.count(e8.children) === 1;
  }
  var V3 = (0, import_react76.createContext)(null);
  V3.displayName = "TransitionContext";
  var De = ((n13) => (n13.Visible = "visible", n13.Hidden = "hidden", n13))(De || {});
  function He() {
    let e8 = (0, import_react76.useContext)(V3);
    if (e8 === null) throw new Error("A <Transition.Child /> is used but it is missing a parent <Transition /> or <Transition.Root />.");
    return e8;
  }
  function Ae2() {
    let e8 = (0, import_react76.useContext)(w7);
    if (e8 === null) throw new Error("A <Transition.Child /> is used but it is missing a parent <Transition /> or <Transition.Root />.");
    return e8;
  }
  var w7 = (0, import_react76.createContext)(null);
  w7.displayName = "NestingContext";
  function M6(e8) {
    return "children" in e8 ? M6(e8.children) : e8.current.filter(({ el: t12 }) => t12.current !== null).filter(({ state: t12 }) => t12 === "visible").length > 0;
  }
  function Te2(e8, t12) {
    let n13 = s3(e8), l13 = (0, import_react76.useRef)([]), S9 = f12(), R5 = p(), d9 = o4((o16, i13 = C.Hidden) => {
      let a21 = l13.current.findIndex(({ el: s15 }) => s15 === o16);
      a21 !== -1 && (u(i13, { [C.Unmount]() {
        l13.current.splice(a21, 1);
      }, [C.Hidden]() {
        l13.current[a21].state = "hidden";
      } }), R5.microTask(() => {
        var s15;
        !M6(l13) && S9.current && ((s15 = n13.current) == null || s15.call(n13));
      }));
    }), y6 = o4((o16) => {
      let i13 = l13.current.find(({ el: a21 }) => a21 === o16);
      return i13 ? i13.state !== "visible" && (i13.state = "visible") : l13.current.push({ el: o16, state: "visible" }), () => d9(o16, C.Unmount);
    }), C8 = (0, import_react76.useRef)([]), p8 = (0, import_react76.useRef)(Promise.resolve()), h10 = (0, import_react76.useRef)({ enter: [], leave: [] }), g3 = o4((o16, i13, a21) => {
      C8.current.splice(0), t12 && (t12.chains.current[i13] = t12.chains.current[i13].filter(([s15]) => s15 !== o16)), t12 == null || t12.chains.current[i13].push([o16, new Promise((s15) => {
        C8.current.push(s15);
      })]), t12 == null || t12.chains.current[i13].push([o16, new Promise((s15) => {
        Promise.all(h10.current[i13].map(([r16, f13]) => f13)).then(() => s15());
      })]), i13 === "enter" ? p8.current = p8.current.then(() => t12 == null ? void 0 : t12.wait.current).then(() => a21(i13)) : a21(i13);
    }), v6 = o4((o16, i13, a21) => {
      Promise.all(h10.current[i13].splice(0).map(([s15, r16]) => r16)).then(() => {
        var s15;
        (s15 = C8.current.shift()) == null || s15();
      }).then(() => a21(i13));
    });
    return (0, import_react76.useMemo)(() => ({ children: l13, register: y6, unregister: d9, onStart: g3, onStop: v6, wait: p8, chains: h10 }), [y6, d9, l13, g3, v6, h10, p8]);
  }
  var de = import_react76.Fragment;
  var fe = A.RenderStrategy;
  function Fe2(e8, t12) {
    var ee3, te2;
    let { transition: n13 = true, beforeEnter: l13, afterEnter: S9, beforeLeave: R5, afterLeave: d9, enter: y6, enterFrom: C8, enterTo: p8, entered: h10, leave: g3, leaveFrom: v6, leaveTo: o16, ...i13 } = e8, [a21, s15] = (0, import_react76.useState)(null), r16 = (0, import_react76.useRef)(null), f13 = ue2(e8), U3 = y(...f13 ? [r16, t12, s15] : t12 === null ? [] : [t12]), H6 = (ee3 = i13.unmount) == null || ee3 ? C.Unmount : C.Hidden, { show: u16, appear: z2, initial: K3 } = He(), [m9, j9] = (0, import_react76.useState)(u16 ? "visible" : "hidden"), Q2 = Ae2(), { register: A5, unregister: F5 } = Q2;
    n(() => A5(r16), [A5, r16]), n(() => {
      if (H6 === C.Hidden && r16.current) {
        if (u16 && m9 !== "visible") {
          j9("visible");
          return;
        }
        return u(m9, { ["hidden"]: () => F5(r16), ["visible"]: () => A5(r16) });
      }
    }, [m9, r16, A5, F5, u16, H6]);
    let G5 = l9();
    n(() => {
      if (f13 && G5 && m9 === "visible" && r16.current === null) throw new Error("Did you forget to passthrough the `ref` to the actual DOM node?");
    }, [r16, m9, G5, f13]);
    let ce2 = K3 && !z2, Y2 = z2 && u16 && K3, B2 = (0, import_react76.useRef)(false), I7 = Te2(() => {
      B2.current || (j9("hidden"), F5(r16));
    }, Q2), Z2 = o4((W) => {
      B2.current = true;
      let L5 = W ? "enter" : "leave";
      I7.onStart(r16, L5, (_5) => {
        _5 === "enter" ? l13 == null || l13() : _5 === "leave" && (R5 == null || R5());
      });
    }), $5 = o4((W) => {
      let L5 = W ? "enter" : "leave";
      B2.current = false, I7.onStop(r16, L5, (_5) => {
        _5 === "enter" ? S9 == null || S9() : _5 === "leave" && (d9 == null || d9());
      }), L5 === "leave" && !M6(I7) && (j9("hidden"), F5(r16));
    });
    (0, import_react76.useEffect)(() => {
      f13 && n13 || (Z2(u16), $5(u16));
    }, [u16, f13, n13]);
    let pe2 = /* @__PURE__ */ (() => !(!n13 || !f13 || !G5 || ce2))(), [, T9] = N2(pe2, a21, u16, { start: Z2, end: $5 }), Ce = m3({ ref: U3, className: ((te2 = t4(i13.className, Y2 && y6, Y2 && C8, T9.enter && y6, T9.enter && T9.closed && C8, T9.enter && !T9.closed && p8, T9.leave && g3, T9.leave && !T9.closed && v6, T9.leave && T9.closed && o16, !T9.transition && u16 && h10)) == null ? void 0 : te2.trim()) || void 0, ...x4(T9) }), N3 = 0;
    m9 === "visible" && (N3 |= i10.Open), m9 === "hidden" && (N3 |= i10.Closed), u16 && m9 === "hidden" && (N3 |= i10.Opening), !u16 && m9 === "visible" && (N3 |= i10.Closing);
    let he = K();
    return import_react76.default.createElement(w7.Provider, { value: I7 }, import_react76.default.createElement(c7, { value: N3 }, he({ ourProps: Ce, theirProps: i13, defaultTag: de, features: fe, visible: m9 === "visible", name: "Transition.Child" })));
  }
  function Ie(e8, t12) {
    let { show: n13, appear: l13 = false, unmount: S9 = true, ...R5 } = e8, d9 = (0, import_react76.useRef)(null), y6 = ue2(e8), C8 = y(...y6 ? [d9, t12] : t12 === null ? [] : [t12]);
    l9();
    let p8 = u11();
    if (n13 === void 0 && p8 !== null && (n13 = (p8 & i10.Open) === i10.Open), n13 === void 0) throw new Error("A <Transition /> is used but it is missing a `show={true | false}` prop.");
    let [h10, g3] = (0, import_react76.useState)(n13 ? "visible" : "hidden"), v6 = Te2(() => {
      n13 || g3("hidden");
    }), [o16, i13] = (0, import_react76.useState)(true), a21 = (0, import_react76.useRef)([n13]);
    n(() => {
      o16 !== false && a21.current[a21.current.length - 1] !== n13 && (a21.current.push(n13), i13(false));
    }, [a21, n13]);
    let s15 = (0, import_react76.useMemo)(() => ({ show: n13, appear: l13, initial: o16 }), [n13, l13, o16]);
    n(() => {
      n13 ? g3("visible") : !M6(v6) && d9.current !== null && g3("hidden");
    }, [n13, v6]);
    let r16 = { unmount: S9 }, f13 = o4(() => {
      var u16;
      o16 && i13(false), (u16 = e8.beforeEnter) == null || u16.call(e8);
    }), U3 = o4(() => {
      var u16;
      o16 && i13(false), (u16 = e8.beforeLeave) == null || u16.call(e8);
    }), H6 = K();
    return import_react76.default.createElement(w7.Provider, { value: v6 }, import_react76.default.createElement(V3.Provider, { value: s15 }, H6({ ourProps: { ...r16, as: import_react76.Fragment, children: import_react76.default.createElement(me, { ref: C8, ...r16, ...R5, beforeEnter: f13, beforeLeave: U3 }) }, theirProps: {}, defaultTag: import_react76.Fragment, features: fe, visible: h10 === "visible", name: "Transition" })));
  }
  function Le(e8, t12) {
    let n13 = (0, import_react76.useContext)(V3) !== null, l13 = u11() !== null;
    return import_react76.default.createElement(import_react76.default.Fragment, null, !n13 && l13 ? import_react76.default.createElement(X2, { ref: t12, ...e8 }) : import_react76.default.createElement(me, { ref: t12, ...e8 }));
  }
  var X2 = Y(Ie);
  var me = Y(Fe2);
  var Oe = Y(Le);
  var Ke = Object.assign(X2, { Child: Oe, Root: X2 });

  // frontend/node_modules/@headlessui/react/dist/components/dialog/dialog.js
  var we = ((o16) => (o16[o16.Open = 0] = "Open", o16[o16.Closed = 1] = "Closed", o16))(we || {});
  var Be = ((t12) => (t12[t12.SetTitleId = 0] = "SetTitleId", t12))(Be || {});
  var Ue = { [0](e8, t12) {
    return e8.titleId === t12.id ? e8 : { ...e8, titleId: t12.id };
  } };
  var w8 = (0, import_react77.createContext)(null);
  w8.displayName = "DialogContext";
  function O5(e8) {
    let t12 = (0, import_react77.useContext)(w8);
    if (t12 === null) {
      let o16 = new Error(`<${e8} /> is missing a parent <Dialog /> component.`);
      throw Error.captureStackTrace && Error.captureStackTrace(o16, O5), o16;
    }
    return t12;
  }
  function He2(e8, t12) {
    return u(t12.type, Ue, e8, t12);
  }
  var z = Y(function(t12, o16) {
    let a21 = (0, import_react40.useId)(), { id: n13 = `headlessui-dialog-${a21}`, open: i13, onClose: p8, initialFocus: d9, role: s15 = "dialog", autoFocus: f13 = true, __demoMode: u16 = false, unmount: y6 = false, ...S9 } = t12, R5 = (0, import_react77.useRef)(false);
    s15 = (function() {
      return s15 === "dialog" || s15 === "alertdialog" ? s15 : (R5.current || (R5.current = true, console.warn(`Invalid role [${s15}] passed to <Dialog />. Only \`dialog\` and and \`alertdialog\` are supported. Using \`dialog\` instead.`)), "dialog");
    })();
    let g3 = u11();
    i13 === void 0 && g3 !== null && (i13 = (g3 & i10.Open) === i10.Open);
    let T9 = (0, import_react77.useRef)(null), I7 = y(T9, o16), F5 = u9(T9.current), c14 = i13 ? 0 : 1, [b8, Q2] = (0, import_react77.useReducer)(He2, { titleId: null, descriptionId: null, panelRef: (0, import_react77.createRef)() }), m9 = o4(() => p8(false)), B2 = o4((r16) => Q2({ type: 0, id: r16 })), D8 = l9() ? c14 === 0 : false, [Z2, ee3] = oe(), te2 = { get current() {
      var r16;
      return (r16 = b8.panelRef.current) != null ? r16 : T9.current;
    } }, v6 = x6(), { resolveContainers: M8 } = S7({ mainTreeNode: v6, portals: Z2, defaultContainers: [te2] }), U3 = g3 !== null ? (g3 & i10.Closing) === i10.Closing : false;
    y3(u16 || U3 ? false : D8, { allowed: o4(() => {
      var r16, W;
      return [(W = (r16 = T9.current) == null ? void 0 : r16.closest("[data-headlessui-portal]")) != null ? W : null];
    }), disallowed: o4(() => {
      var r16;
      return [(r16 = v6 == null ? void 0 : v6.closest("body > *:not(#headlessui-portal-root)")) != null ? r16 : null];
    }) });
    let P5 = x2.get(null);
    n(() => {
      if (D8) return P5.actions.push(n13), () => P5.actions.pop(n13);
    }, [P5, n13, D8]);
    let H6 = S3(P5, (0, import_react77.useCallback)((r16) => P5.selectors.isTop(r16, n13), [P5, n13]));
    k5(H6, M8, (r16) => {
      r16.preventDefault(), m9();
    }), a17(H6, F5 == null ? void 0 : F5.defaultView, (r16) => {
      r16.preventDefault(), r16.stopPropagation(), document.activeElement && "blur" in document.activeElement && typeof document.activeElement.blur == "function" && document.activeElement.blur(), m9();
    }), f7(u16 || U3 ? false : D8, F5, M8), p4(D8, T9, m9);
    let [oe3, ne3] = H2(), re2 = (0, import_react77.useMemo)(() => [{ dialogState: c14, close: m9, setTitleId: B2, unmount: y6 }, b8], [c14, m9, B2, y6, b8]), N3 = n2({ open: c14 === 0 }), le2 = { ref: I7, id: n13, role: s15, tabIndex: -1, "aria-modal": u16 ? void 0 : c14 === 0 ? true : void 0, "aria-labelledby": b8.titleId, "aria-describedby": oe3, unmount: y6 }, ae2 = !f11(), E9 = G4.None;
    D8 && !u16 && (E9 |= G4.RestoreFocus, E9 |= G4.TabLock, f13 && (E9 |= G4.AutoFocus), ae2 && (E9 |= G4.InitialFocus));
    let ie2 = K();
    return import_react77.default.createElement(s10, null, import_react77.default.createElement(l10, { force: true }, import_react77.default.createElement(le, null, import_react77.default.createElement(w8.Provider, { value: re2 }, import_react77.default.createElement(B, { target: T9 }, import_react77.default.createElement(l10, { force: false }, import_react77.default.createElement(ne3, { slot: N3 }, import_react77.default.createElement(ee3, null, import_react77.default.createElement(ge2, { initialFocus: d9, initialFocusFallback: T9, containers: M8, features: E9 }, import_react77.default.createElement(C4, { value: m9 }, ie2({ ourProps: le2, theirProps: S9, slot: N3, defaultTag: Ne, features: We, visible: c14 === 0, name: "Dialog" })))))))))));
  });
  var Ne = "div";
  var We = A.RenderStrategy | A.Static;
  function $e(e8, t12) {
    let { transition: o16 = false, open: a21, ...n13 } = e8, i13 = u11(), p8 = e8.hasOwnProperty("open") || i13 !== null, d9 = e8.hasOwnProperty("onClose");
    if (!p8 && !d9) throw new Error("You have to provide an `open` and an `onClose` prop to the `Dialog` component.");
    if (!p8) throw new Error("You provided an `onClose` prop to the `Dialog`, but forgot an `open` prop.");
    if (!d9) throw new Error("You provided an `open` prop to the `Dialog`, but forgot an `onClose` prop.");
    if (!i13 && typeof e8.open != "boolean") throw new Error(`You provided an \`open\` prop to the \`Dialog\`, but the value is not a boolean. Received: ${e8.open}`);
    if (typeof e8.onClose != "function") throw new Error(`You provided an \`onClose\` prop to the \`Dialog\`, but the value is not a function. Received: ${e8.onClose}`);
    return (a21 !== void 0 || o16) && !n13.static ? import_react77.default.createElement(j7, null, import_react77.default.createElement(Ke, { show: a21, transition: o16, unmount: n13.unmount }, import_react77.default.createElement(z, { ref: t12, ...n13 }))) : import_react77.default.createElement(j7, null, import_react77.default.createElement(z, { ref: t12, open: a21, ...n13 }));
  }
  var je = "div";
  function Ye(e8, t12) {
    let o16 = (0, import_react40.useId)(), { id: a21 = `headlessui-dialog-panel-${o16}`, transition: n13 = false, ...i13 } = e8, [{ dialogState: p8, unmount: d9 }, s15] = O5("Dialog.Panel"), f13 = y(t12, s15.panelRef), u16 = n2({ open: p8 === 0 }), y6 = o4((I7) => {
      I7.stopPropagation();
    }), S9 = { ref: f13, id: a21, onClick: y6 }, R5 = n13 ? Oe : import_react77.Fragment, g3 = n13 ? { unmount: d9 } : {}, T9 = K();
    return import_react77.default.createElement(R5, { ...g3 }, T9({ ourProps: S9, theirProps: i13, slot: u16, defaultTag: je, name: "Dialog.Panel" }));
  }
  var Je = "div";
  function Ke2(e8, t12) {
    let { transition: o16 = false, ...a21 } = e8, [{ dialogState: n13, unmount: i13 }] = O5("Dialog.Backdrop"), p8 = n2({ open: n13 === 0 }), d9 = { ref: t12, "aria-hidden": true }, s15 = o16 ? Oe : import_react77.Fragment, f13 = o16 ? { unmount: i13 } : {}, u16 = K();
    return import_react77.default.createElement(s15, { ...f13 }, u16({ ourProps: d9, theirProps: a21, slot: p8, defaultTag: Je, name: "Dialog.Backdrop" }));
  }
  var Xe = "h2";
  function Ve(e8, t12) {
    let o16 = (0, import_react40.useId)(), { id: a21 = `headlessui-dialog-title-${o16}`, ...n13 } = e8, [{ dialogState: i13, setTitleId: p8 }] = O5("Dialog.Title"), d9 = y(t12);
    (0, import_react77.useEffect)(() => (p8(a21), () => p8(null)), [a21, p8]);
    let s15 = n2({ open: i13 === 0 }), f13 = { ref: d9, id: a21 };
    return K()({ ourProps: f13, theirProps: n13, slot: s15, defaultTag: Xe, name: "Dialog.Title" });
  }
  var qe = Y($e);
  var ze = Y(Ye);
  var Lt = Y(Ke2);
  var Qe = Y(Ve);
  var ht = Object.assign(qe, { Panel: ze, Title: Qe, Description: M2 });

  // frontend/node_modules/@headlessui/react/dist/hooks/use-text-value.js
  init_define_import_meta_env();
  var import_react78 = __toESM(require_react_shim(), 1);

  // frontend/node_modules/@headlessui/react/dist/utils/get-text-value.js
  init_define_import_meta_env();
  var a19 = /([\u2700-\u27BF]|[\uE000-\uF8FF]|\uD83C[\uDC00-\uDFFF]|\uD83D[\uDC00-\uDFFF]|[\u2011-\u26FF]|\uD83E[\uDD10-\uDDFF])/g;
  function o14(e8) {
    var l13, n13;
    let i13 = (l13 = e8.innerText) != null ? l13 : "", t12 = e8.cloneNode(true);
    if (!n5(t12)) return i13;
    let u16 = false;
    for (let f13 of t12.querySelectorAll('[hidden],[aria-hidden],[role="img"]')) f13.remove(), u16 = true;
    let r16 = u16 ? (n13 = t12.innerText) != null ? n13 : "" : i13;
    return a19.test(r16) && (r16 = r16.replace(a19, "")), r16;
  }
  function F4(e8) {
    let i13 = e8.getAttribute("aria-label");
    if (typeof i13 == "string") return i13.trim();
    let t12 = e8.getAttribute("aria-labelledby");
    if (t12) {
      let u16 = t12.split(" ").map((r16) => {
        let l13 = document.getElementById(r16);
        if (l13) {
          let n13 = l13.getAttribute("aria-label");
          return typeof n13 == "string" ? n13.trim() : o14(l13).trim();
        }
        return null;
      }).filter(Boolean);
      if (u16.length > 0) return u16.join(", ");
    }
    return o14(e8).trim();
  }

  // frontend/node_modules/@headlessui/react/dist/hooks/use-text-value.js
  function s13(c14) {
    let t12 = (0, import_react78.useRef)(""), r16 = (0, import_react78.useRef)("");
    return o4(() => {
      let e8 = c14.current;
      if (!e8) return "";
      let u16 = e8.innerText;
      if (t12.current === u16) return r16.current;
      let n13 = F4(e8).trim().toLowerCase();
      return t12.current = u16, r16.current = n13, n13;
    });
  }

  // frontend/node_modules/@headlessui/react/dist/components/menu/menu.js
  init_define_import_meta_env();
  var import_react80 = __toESM(require_react_shim(), 1);
  var import_react_dom4 = __toESM(require_react_dom_shim(), 1);

  // frontend/node_modules/@headlessui/react/dist/components/menu/menu-machine.js
  init_define_import_meta_env();
  var y5 = Object.defineProperty;
  var M7 = (e8, i13, t12) => i13 in e8 ? y5(e8, i13, { enumerable: true, configurable: true, writable: true, value: t12 }) : e8[i13] = t12;
  var S8 = (e8, i13, t12) => (M7(e8, typeof i13 != "symbol" ? i13 + "" : i13, t12), t12);
  var P4 = ((t12) => (t12[t12.Open = 0] = "Open", t12[t12.Closed = 1] = "Closed", t12))(P4 || {});
  var D7 = ((t12) => (t12[t12.Pointer = 0] = "Pointer", t12[t12.Other = 1] = "Other", t12))(D7 || {});
  var C7 = ((o16) => (o16[o16.OpenMenu = 0] = "OpenMenu", o16[o16.CloseMenu = 1] = "CloseMenu", o16[o16.GoToItem = 2] = "GoToItem", o16[o16.Search = 3] = "Search", o16[o16.ClearSearch = 4] = "ClearSearch", o16[o16.RegisterItems = 5] = "RegisterItems", o16[o16.UnregisterItems = 6] = "UnregisterItems", o16[o16.SetButtonElement = 7] = "SetButtonElement", o16[o16.SetItemsElement = 8] = "SetItemsElement", o16[o16.SortItems = 9] = "SortItems", o16[o16.MarkButtonAsMoved = 10] = "MarkButtonAsMoved", o16))(C7 || {});
  function x8(e8, i13 = (t12) => t12) {
    let t12 = e8.activeItemIndex !== null ? e8.items[e8.activeItemIndex] : null, n13 = G2(i13(e8.items.slice()), (s15) => s15.dataRef.current.domRef.current), r16 = t12 ? n13.indexOf(t12) : null;
    return r16 === -1 && (r16 = null), { items: n13, activeItemIndex: r16 };
  }
  var k8 = { [1](e8) {
    if (e8.menuState === 1) return e8;
    let i13 = e8.buttonElement ? c12.Tracked(a16(e8.buttonElement)) : e8.buttonPositionState;
    return { ...e8, activeItemIndex: null, pendingFocus: { focus: c8.Nothing }, menuState: 1, buttonPositionState: i13 };
  }, [0](e8, i13) {
    return e8.menuState === 0 ? e8 : { ...e8, __demoMode: false, pendingFocus: i13.focus, menuState: 0, buttonPositionState: c12.Idle };
  }, [2]: (e8, i13) => {
    var s15, l13, a21, I7, f13;
    if (e8.menuState === 1) return e8;
    let t12 = { ...e8, searchQuery: "", activationTrigger: (s15 = i13.trigger) != null ? s15 : 1, __demoMode: false };
    if (i13.focus === c8.Nothing) return { ...t12, activeItemIndex: null };
    if (i13.focus === c8.Specific) return { ...t12, activeItemIndex: e8.items.findIndex((d9) => d9.id === i13.id) };
    if (i13.focus === c8.Previous) {
      let d9 = e8.activeItemIndex;
      if (d9 !== null) {
        let o16 = e8.items[d9].dataRef.current.domRef, c14 = f9(i13, { resolveItems: () => e8.items, resolveActiveIndex: () => e8.activeItemIndex, resolveId: (u16) => u16.id, resolveDisabled: (u16) => u16.dataRef.current.disabled });
        if (c14 !== null) {
          let u16 = e8.items[c14].dataRef.current.domRef;
          if (((l13 = o16.current) == null ? void 0 : l13.previousElementSibling) === u16.current || ((a21 = u16.current) == null ? void 0 : a21.previousElementSibling) === null) return { ...t12, activeItemIndex: c14 };
        }
      }
    } else if (i13.focus === c8.Next) {
      let d9 = e8.activeItemIndex;
      if (d9 !== null) {
        let o16 = e8.items[d9].dataRef.current.domRef, c14 = f9(i13, { resolveItems: () => e8.items, resolveActiveIndex: () => e8.activeItemIndex, resolveId: (u16) => u16.id, resolveDisabled: (u16) => u16.dataRef.current.disabled });
        if (c14 !== null) {
          let u16 = e8.items[c14].dataRef.current.domRef;
          if (((I7 = o16.current) == null ? void 0 : I7.nextElementSibling) === u16.current || ((f13 = u16.current) == null ? void 0 : f13.nextElementSibling) === null) return { ...t12, activeItemIndex: c14 };
        }
      }
    }
    let n13 = x8(e8), r16 = f9(i13, { resolveItems: () => n13.items, resolveActiveIndex: () => n13.activeItemIndex, resolveId: (d9) => d9.id, resolveDisabled: (d9) => d9.dataRef.current.disabled });
    return { ...t12, ...n13, activeItemIndex: r16 };
  }, [3]: (e8, i13) => {
    let n13 = e8.searchQuery !== "" ? 0 : 1, r16 = e8.searchQuery + i13.value.toLowerCase(), l13 = (e8.activeItemIndex !== null ? e8.items.slice(e8.activeItemIndex + n13).concat(e8.items.slice(0, e8.activeItemIndex + n13)) : e8.items).find((I7) => {
      var f13;
      return ((f13 = I7.dataRef.current.textValue) == null ? void 0 : f13.startsWith(r16)) && !I7.dataRef.current.disabled;
    }), a21 = l13 ? e8.items.indexOf(l13) : -1;
    return a21 === -1 || a21 === e8.activeItemIndex ? { ...e8, searchQuery: r16 } : { ...e8, searchQuery: r16, activeItemIndex: a21, activationTrigger: 1 };
  }, [4](e8) {
    return e8.searchQuery === "" ? e8 : { ...e8, searchQuery: "", searchActiveItemIndex: null };
  }, [5]: (e8, i13) => {
    let t12 = e8.items.concat(i13.items.map((r16) => r16)), n13 = e8.activeItemIndex;
    return e8.pendingFocus.focus !== c8.Nothing && (n13 = f9(e8.pendingFocus, { resolveItems: () => t12, resolveActiveIndex: () => e8.activeItemIndex, resolveId: (r16) => r16.id, resolveDisabled: (r16) => r16.dataRef.current.disabled })), { ...e8, items: t12, activeItemIndex: n13, pendingFocus: { focus: c8.Nothing }, pendingShouldSort: true };
  }, [6]: (e8, i13) => {
    let t12 = e8.items, n13 = [], r16 = new Set(i13.items);
    for (let [s15, l13] of t12.entries()) if (r16.has(l13.id) && (n13.push(s15), r16.delete(l13.id), r16.size === 0)) break;
    if (n13.length > 0) {
      t12 = t12.slice();
      for (let s15 of n13.reverse()) t12.splice(s15, 1);
    }
    return { ...e8, items: t12, activationTrigger: 1 };
  }, [7]: (e8, i13) => e8.buttonElement === i13.element ? e8 : { ...e8, buttonElement: i13.element }, [8]: (e8, i13) => e8.itemsElement === i13.element ? e8 : { ...e8, itemsElement: i13.element }, [9]: (e8) => e8.pendingShouldSort ? { ...e8, ...x8(e8), pendingShouldSort: false } : e8, [10](e8) {
    return e8.buttonPositionState.kind !== "Tracked" ? e8 : { ...e8, buttonPositionState: c12.Moved };
  } };
  var h8 = class _h extends T3 {
    constructor(t12) {
      super(t12);
      S8(this, "actions", { registerItem: k3(() => {
        let t13 = [], n13 = /* @__PURE__ */ new Set();
        return [(r16, s15) => {
          n13.has(s15) || (n13.add(s15), t13.push({ id: r16, dataRef: s15 }));
        }, () => (n13.clear(), this.send({ type: 5, items: t13.splice(0) }))];
      }), unregisterItem: k3(() => {
        let t13 = [];
        return [(n13) => t13.push(n13), () => this.send({ type: 6, items: t13.splice(0) })];
      }) });
      S8(this, "selectors", { activeDescendantId(t13) {
        var s15;
        let n13 = t13.activeItemIndex, r16 = t13.items;
        return n13 === null || (s15 = r16[n13]) == null ? void 0 : s15.id;
      }, isActive(t13, n13) {
        var l13;
        let r16 = t13.activeItemIndex, s15 = t13.items;
        return r16 !== null ? ((l13 = s15[r16]) == null ? void 0 : l13.id) === n13 : false;
      }, shouldScrollIntoView(t13, n13) {
        return t13.__demoMode || t13.menuState !== 0 || t13.activationTrigger === 0 ? false : this.isActive(t13, n13);
      }, didButtonMove(t13) {
        return t13.buttonPositionState.kind === "Moved";
      } });
      this.on(5, () => {
        this.disposables.requestAnimationFrame(() => {
          this.send({ type: 9 });
        });
      });
      {
        let n13 = this.state.id, r16 = x2.get(null);
        this.disposables.add(r16.on(k4.Push, (s15) => {
          !r16.selectors.isTop(s15, n13) && this.state.menuState === 0 && this.send({ type: 1 });
        })), this.on(0, () => r16.actions.push(n13)), this.on(1, () => r16.actions.pop(n13));
      }
      this.disposables.group((n13) => {
        this.on(1, (r16) => {
          r16.buttonElement && (n13.dispose(), n13.add(p5(r16.buttonElement, r16.buttonPositionState, () => {
            this.send({ type: 10 });
          })));
        });
      });
    }
    static new({ id: t12, __demoMode: n13 = false }) {
      return new _h({ id: t12, __demoMode: n13, menuState: n13 ? 0 : 1, buttonElement: null, itemsElement: null, items: [], searchQuery: "", activeItemIndex: null, activationTrigger: 1, pendingShouldSort: false, pendingFocus: { focus: c8.Nothing }, buttonPositionState: c12.Idle });
    }
    reduce(t12, n13) {
      return u(n13.type, k8, t12, n13);
    }
  };

  // frontend/node_modules/@headlessui/react/dist/components/menu/menu-machine-glue.js
  init_define_import_meta_env();
  var import_react79 = __toESM(require_react_shim(), 1);
  var a20 = (0, import_react79.createContext)(null);
  function p7(t12) {
    let n13 = (0, import_react79.useContext)(a20);
    if (n13 === null) {
      let e8 = new Error(`<${t12} /> is missing a parent <Menu /> component.`);
      throw Error.captureStackTrace && Error.captureStackTrace(e8, s14), e8;
    }
    return n13;
  }
  function s14({ id: t12, __demoMode: n13 = false }) {
    let e8 = (0, import_react79.useMemo)(() => h8.new({ id: t12, __demoMode: n13 }), []);
    return c9(() => e8.dispose()), e8;
  }

  // frontend/node_modules/@headlessui/react/dist/components/menu/menu.js
  var Ze = import_react80.Fragment;
  function et(m9, y6) {
    let l13 = (0, import_react40.useId)(), { __demoMode: a21 = false, ...p8 } = m9, s15 = s14({ id: l13, __demoMode: a21 }), [n13, M8, f13] = S3(s15, (d9) => [d9.menuState, d9.itemsElement, d9.buttonElement]), _5 = y(y6), o16 = x2.get(null), F5 = S3(o16, (0, import_react80.useCallback)((d9) => o16.selectors.isTop(d9, l13), [o16, l13]));
    k5(F5, [f13, M8], (d9, T9) => {
      var P5;
      s15.send({ type: C7.CloseMenu }), H3(T9, I4.Loose) || (d9.preventDefault(), (P5 = s15.state.buttonElement) == null || P5.focus());
    });
    let I7 = o4(() => {
      s15.send({ type: C7.CloseMenu });
    }), b8 = n2({ open: n13 === P4.Open, close: I7 }), i13 = { ref: _5 }, g3 = K();
    return import_react80.default.createElement(Ae, null, import_react80.default.createElement(a20.Provider, { value: s15 }, import_react80.default.createElement(c7, { value: u(n13, { [P4.Open]: i10.Open, [P4.Closed]: i10.Closed }) }, g3({ ourProps: i13, theirProps: p8, slot: b8, defaultTag: Ze, name: "Menu" }))));
  }
  var tt = "button";
  function ot(m9, y6) {
    let l13 = p7("Menu.Button"), a21 = (0, import_react40.useId)(), { id: p8 = `headlessui-menu-button-${a21}`, disabled: s15 = false, autoFocus: n13 = false, ...M8 } = m9, f13 = (0, import_react80.useRef)(null), _5 = be(), o16 = y(y6, f13, Fe(), o4((t12) => l13.send({ type: C7.SetButtonElement, element: t12 }))), F5 = o4((t12) => {
      switch (t12.key) {
        case o6.Space:
        case o6.Enter:
        case o6.ArrowDown:
          t12.preventDefault(), t12.stopPropagation(), l13.send({ type: C7.OpenMenu, focus: { focus: c8.First } });
          break;
        case o6.ArrowUp:
          t12.preventDefault(), t12.stopPropagation(), l13.send({ type: C7.OpenMenu, focus: { focus: c8.Last } });
          break;
      }
    }), I7 = o4((t12) => {
      switch (t12.key) {
        case o6.Space:
          t12.preventDefault();
          break;
      }
    }), [b8, i13, g3] = S3(l13, (t12) => [t12.menuState, t12.buttonElement, t12.itemsElement]), d9 = b8 === P4.Open;
    L4(d9, { trigger: i13, action: (0, import_react80.useCallback)((t12) => {
      if (i13 != null && i13.contains(t12.target)) return S5.Ignore;
      let S9 = t12.target.closest('[role="menuitem"]:not([data-disabled])');
      return n5(S9) ? S5.Select(S9) : g3 != null && g3.contains(t12.target) ? S5.Ignore : S5.Close;
    }, [i13, g3]), close: (0, import_react80.useCallback)(() => l13.send({ type: C7.CloseMenu }), []), select: (0, import_react80.useCallback)((t12) => t12.click(), []) });
    let T9 = s6((t12) => {
      var S9;
      s15 || (b8 === P4.Open ? ((0, import_react_dom4.flushSync)(() => l13.send({ type: C7.CloseMenu })), (S9 = f13.current) == null || S9.focus({ preventScroll: true })) : (t12.preventDefault(), l13.send({ type: C7.OpenMenu, focus: { focus: c8.Nothing }, trigger: D7.Pointer })));
    }), { isFocusVisible: P5, focusProps: L5 } = $0c4a58759813079a$export$4e328f61c538687f({ autoFocus: n13 }), { isHovered: O6, hoverProps: v6 } = $e969f22b6713ca4a$export$ae780daf29e6d456({ isDisabled: s15 }), { pressed: D8, pressProps: U3 } = w({ disabled: s15 }), H6 = n2({ open: b8 === P4.Open, active: D8 || b8 === P4.Open, disabled: s15, hover: O6, focus: P5, autofocus: n13 }), G5 = V(_5(), { ref: o16, id: p8, type: e5(m9, f13.current), "aria-haspopup": "menu", "aria-controls": g3 == null ? void 0 : g3.id, "aria-expanded": b8 === P4.Open, disabled: s15 || void 0, autoFocus: n13, onKeyDown: F5, onKeyUp: I7 }, T9, L5, v6, U3);
    return K()({ ourProps: G5, theirProps: M8, slot: H6, defaultTag: tt, name: "Menu.Button" });
  }
  var nt = "div";
  var rt = A.RenderStrategy | A.Static;
  function at(m9, y6) {
    let l13 = (0, import_react40.useId)(), { id: a21 = `headlessui-menu-items-${l13}`, anchor: p8, portal: s15 = false, modal: n13 = true, transition: M8 = false, ...f13 } = m9, _5 = ye(p8), o16 = p7("Menu.Items"), [F5, I7] = Re(_5), b8 = Te(), [i13, g3] = (0, import_react80.useState)(null), d9 = y(y6, _5 ? F5 : null, o4((e8) => o16.send({ type: C7.SetItemsElement, element: e8 })), g3), [T9, P5] = S3(o16, (e8) => [e8.menuState, e8.buttonElement]), L5 = u9(P5), O6 = u9(i13);
    _5 && (s15 = true);
    let v6 = u11(), [D8, U3] = N2(M8, i13, v6 !== null ? (v6 & i10.Open) === i10.Open : T9 === P4.Open);
    p4(D8, P5, () => {
      o16.send({ type: C7.CloseMenu });
    });
    let H6 = S3(o16, (e8) => e8.__demoMode), G5 = H6 ? false : n13 && T9 === P4.Open;
    f7(G5, O6);
    let w9 = H6 ? false : n13 && T9 === P4.Open;
    y3(w9, { allowed: (0, import_react80.useCallback)(() => [P5, i13], [P5, i13]) });
    let S9 = S3(o16, o16.selectors.didButtonMove) ? false : D8;
    (0, import_react80.useEffect)(() => {
      let e8 = i13;
      e8 && T9 === P4.Open && (d2(e8) || e8.focus({ preventScroll: true }));
    }, [T9, i13]), F2(T9 === P4.Open, { container: i13, accept(e8) {
      return e8.getAttribute("role") === "menuitem" ? NodeFilter.FILTER_REJECT : e8.hasAttribute("role") ? NodeFilter.FILTER_SKIP : NodeFilter.FILTER_ACCEPT;
    }, walk(e8) {
      e8.setAttribute("role", "none");
    } });
    let z2 = p(), le2 = o4((e8) => {
      var N3, Y2, Z2;
      switch (z2.dispose(), e8.key) {
        case o6.Space:
          if (o16.state.searchQuery !== "") return e8.preventDefault(), e8.stopPropagation(), o16.send({ type: C7.Search, value: e8.key });
        case o6.Enter:
          if (e8.preventDefault(), e8.stopPropagation(), o16.state.activeItemIndex !== null) {
            let { dataRef: de3 } = o16.state.items[o16.state.activeItemIndex];
            (Y2 = (N3 = de3.current) == null ? void 0 : N3.domRef.current) == null || Y2.click();
          }
          o16.send({ type: C7.CloseMenu }), K2(o16.state.buttonElement);
          break;
        case o6.ArrowDown:
          return e8.preventDefault(), e8.stopPropagation(), o16.send({ type: C7.GoToItem, focus: c8.Next });
        case o6.ArrowUp:
          return e8.preventDefault(), e8.stopPropagation(), o16.send({ type: C7.GoToItem, focus: c8.Previous });
        case o6.Home:
        case o6.PageUp:
          return e8.preventDefault(), e8.stopPropagation(), o16.send({ type: C7.GoToItem, focus: c8.First });
        case o6.End:
        case o6.PageDown:
          return e8.preventDefault(), e8.stopPropagation(), o16.send({ type: C7.GoToItem, focus: c8.Last });
        case o6.Escape:
          e8.preventDefault(), e8.stopPropagation(), (0, import_react_dom4.flushSync)(() => o16.send({ type: C7.CloseMenu })), (Z2 = o16.state.buttonElement) == null || Z2.focus({ preventScroll: true });
          break;
        case o6.Tab:
          e8.preventDefault(), e8.stopPropagation(), (0, import_react_dom4.flushSync)(() => o16.send({ type: C7.CloseMenu })), R2(o16.state.buttonElement, e8.shiftKey ? T4.Previous : T4.Next);
          break;
        default:
          e8.key.length === 1 && (o16.send({ type: C7.Search, value: e8.key }), z2.setTimeout(() => o16.send({ type: C7.ClearSearch }), 350));
          break;
      }
    }), pe2 = o4((e8) => {
      switch (e8.key) {
        case o6.Space:
          e8.preventDefault();
          break;
      }
    }), ie2 = n2({ open: T9 === P4.Open }), ue4 = V(_5 ? b8() : {}, { "aria-activedescendant": S3(o16, o16.selectors.activeDescendantId), "aria-labelledby": S3(o16, (e8) => {
      var N3;
      return (N3 = e8.buttonElement) == null ? void 0 : N3.id;
    }), id: a21, onKeyDown: le2, onKeyUp: pe2, role: "menu", tabIndex: T9 === P4.Open ? 0 : void 0, ref: d9, style: { ...f13.style, ...I7, "--button-width": w3(D8, P5, true).width }, ...x4(U3) }), me2 = K();
    return import_react80.default.createElement(le, { enabled: s15 ? m9.static || D8 : false, ownerDocument: L5 }, me2({ ourProps: ue4, theirProps: f13, slot: ie2, defaultTag: nt, features: rt, visible: S9, name: "Menu.Items" }));
  }
  var st = import_react80.Fragment;
  function lt(m9, y6) {
    let l13 = (0, import_react40.useId)(), { id: a21 = `headlessui-menu-item-${l13}`, disabled: p8 = false, ...s15 } = m9, n13 = p7("Menu.Item"), M8 = S3(n13, (t12) => n13.selectors.isActive(t12, a21)), f13 = (0, import_react80.useRef)(null), _5 = y(y6, f13), o16 = S3(n13, (t12) => n13.selectors.shouldScrollIntoView(t12, a21));
    n(() => {
      if (o16) return o2().requestAnimationFrame(() => {
        var t12, S9;
        (S9 = (t12 = f13.current) == null ? void 0 : t12.scrollIntoView) == null || S9.call(t12, { block: "nearest" });
      });
    }, [o16, f13]);
    let F5 = s13(f13), I7 = (0, import_react80.useRef)({ disabled: p8, domRef: f13, get textValue() {
      return F5();
    } });
    n(() => {
      I7.current.disabled = p8;
    }, [I7, p8]), n(() => (n13.actions.registerItem(a21, I7), () => n13.actions.unregisterItem(a21)), [I7, a21]);
    let b8 = o4(() => {
      n13.send({ type: C7.CloseMenu });
    }), i13 = o4((t12) => {
      if (p8) return t12.preventDefault();
      n13.send({ type: C7.CloseMenu }), K2(n13.state.buttonElement);
    }), g3 = o4(() => {
      if (p8) return n13.send({ type: C7.GoToItem, focus: c8.Nothing });
      n13.send({ type: C7.GoToItem, focus: c8.Specific, id: a21 });
    }), d9 = u10(), T9 = o4((t12) => d9.update(t12)), P5 = o4((t12) => {
      d9.wasMoved(t12) && (p8 || M8 || n13.send({ type: C7.GoToItem, focus: c8.Specific, id: a21, trigger: D7.Pointer }));
    }), L5 = o4((t12) => {
      d9.wasMoved(t12) && (p8 || M8 && n13.state.activationTrigger === D7.Pointer && n13.send({ type: C7.GoToItem, focus: c8.Nothing }));
    }), [O6, v6] = V2(), [D8, U3] = H2(), H6 = n2({ active: M8, focus: M8, disabled: p8, close: b8 }), G5 = { id: a21, ref: _5, role: "menuitem", tabIndex: p8 === true ? void 0 : -1, "aria-disabled": p8 === true ? true : void 0, "aria-labelledby": O6, "aria-describedby": D8, disabled: void 0, onClick: i13, onFocus: g3, onPointerEnter: T9, onMouseEnter: T9, onPointerMove: P5, onMouseMove: P5, onPointerLeave: L5, onMouseLeave: L5 }, w9 = K();
    return import_react80.default.createElement(v6, null, import_react80.default.createElement(U3, null, w9({ ourProps: G5, theirProps: s15, slot: H6, defaultTag: st, name: "Menu.Item" })));
  }
  var pt = "div";
  function it(m9, y6) {
    let [l13, a21] = V2(), p8 = m9, s15 = { ref: y6, "aria-labelledby": l13, role: "group" }, n13 = K();
    return import_react80.default.createElement(a21, null, n13({ ourProps: s15, theirProps: p8, slot: {}, defaultTag: pt, name: "Menu.Section" }));
  }
  var ut = "header";
  function mt(m9, y6) {
    let l13 = (0, import_react40.useId)(), { id: a21 = `headlessui-menu-heading-${l13}`, ...p8 } = m9, s15 = C3();
    n(() => s15.register(a21), [a21, s15.register]);
    let n13 = { id: a21, ref: y6, role: "presentation", ...s15.props };
    return K()({ ourProps: n13, theirProps: p8, slot: {}, defaultTag: ut, name: "Menu.Heading" });
  }
  var dt = "div";
  function Tt(m9, y6) {
    let l13 = m9, a21 = { ref: y6, role: "separator" };
    return K()({ ourProps: a21, theirProps: l13, slot: {}, defaultTag: dt, name: "Menu.Separator" });
  }
  var ct = Y(et);
  var ft = Y(ot);
  var yt = Y(at);
  var gt = Y(lt);
  var Pt = Y(it);
  var Et = Y(mt);
  var Mt = Y(Tt);
  var lo = Object.assign(ct, { Button: ft, Items: yt, Item: gt, Section: Pt, Heading: Et, Separator: Mt });

  // frontend/node_modules/psihointegritet-ds/src/panel/confirm-modal.tsx
  var import_react82 = __toESM(require_react_shim());
  function ConfirmModal({
    open,
    eyebrow,
    title,
    description,
    reasonLabel,
    reasonPlaceholder,
    note,
    confirmLabel,
    onConfirm,
    onClose
  }) {
    const [reason, setReason] = (0, import_react82.useState)("");
    const close = () => {
      setReason("");
      onClose();
    };
    return /* @__PURE__ */ React.createElement(ht, { open, onClose: close, className: "relative z-[90]" }, /* @__PURE__ */ React.createElement(Lt, { className: "bg-coffee/50 animate-fade-in fixed inset-0" }), /* @__PURE__ */ React.createElement("div", { className: "fixed inset-0 flex items-center justify-center p-4" }, /* @__PURE__ */ React.createElement(ze, { className: "rounded-modal shadow-panel-modal bg-panel-canvas w-[min(440px,100%)] px-7 py-8" }, /* @__PURE__ */ React.createElement("div", { className: "text-sage mb-2 text-[12px] font-semibold tracking-[0.16em] uppercase" }, eyebrow), /* @__PURE__ */ React.createElement(Qe, { className: "text-coffee font-serif text-[22px] leading-[1.2]" }, title), /* @__PURE__ */ React.createElement("p", { className: "text-ink-70 mt-2 text-sm leading-[1.5]" }, description), /* @__PURE__ */ React.createElement(
      "label",
      {
        htmlFor: "confirm-modal-reason",
        className: "text-ink-70 mt-5 mb-1.5 block text-[13px] font-semibold"
      },
      reasonLabel
    ), /* @__PURE__ */ React.createElement(
      "input",
      {
        id: "confirm-modal-reason",
        type: "text",
        value: reason,
        onChange: (event) => setReason(event.target.value),
        placeholder: reasonPlaceholder,
        className: "border-line-strong rounded-tile bg-surface text-coffee focus:border-sage w-full border px-3.5 py-2.5 text-sm outline-none"
      }
    ), /* @__PURE__ */ React.createElement("p", { className: "bg-badge-amber-bg text-badge-amber rounded-tile mt-4 px-4 py-3 text-[12.5px] leading-[1.5]" }, note), /* @__PURE__ */ React.createElement("div", { className: "mt-6 flex items-center justify-end gap-3" }, /* @__PURE__ */ React.createElement(
      "button",
      {
        type: "button",
        onClick: close,
        className: "border-line-strong text-ink-70 hover:border-coffee/40 cursor-pointer rounded-full border bg-transparent px-5 py-2.5 text-sm font-semibold transition-colors"
      },
      "Odustani"
    ), /* @__PURE__ */ React.createElement(
      "button",
      {
        type: "button",
        disabled: !reason.trim(),
        onClick: () => {
          onConfirm(reason.trim());
          setReason("");
        },
        className: "bg-forest text-panel-canvas hover:bg-forest-hover cursor-pointer rounded-full border-0 px-5 py-2.5 text-sm font-semibold transition-colors disabled:cursor-not-allowed disabled:opacity-50"
      },
      confirmLabel
    )))));
  }

  // frontend/node_modules/psihointegritet-ds/src/panel/empty-dashed-card.tsx
  init_define_import_meta_env();
  function EmptyDashedCard({
    title,
    children,
    soon,
    className
  }) {
    return /* @__PURE__ */ React.createElement(
      "div",
      {
        className: cn(
          "rounded-panel border-coffee/18 bg-surface/60 border-[1.5px] border-dashed px-6 py-7",
          className
        )
      },
      /* @__PURE__ */ React.createElement("div", { className: "flex items-center gap-3" }, /* @__PURE__ */ React.createElement("h3", { className: "text-coffee font-serif text-xl" }, title), soon ? /* @__PURE__ */ React.createElement("span", { className: "bg-badge-amber-bg text-badge-amber rounded-full px-2.5 py-1 text-[11px] font-semibold tracking-[0.08em] uppercase" }, "Uskoro") : null),
      children ? /* @__PURE__ */ React.createElement("div", { className: "text-ink-55 mt-2 text-sm leading-[1.55]" }, children) : null
    );
  }

  // frontend/node_modules/psihointegritet-ds/src/panel/kv.tsx
  init_define_import_meta_env();
  function KV({ label, children, serif, className }) {
    return /* @__PURE__ */ React.createElement("div", { className }, /* @__PURE__ */ React.createElement("div", { className: "text-ink-45 text-[11px] font-semibold tracking-[0.12em] uppercase" }, label), /* @__PURE__ */ React.createElement(
      "div",
      {
        className: cn(
          "text-coffee mt-1",
          serif ? "font-serif text-xl leading-[1.2]" : "text-sm leading-[1.45] font-semibold"
        )
      },
      children
    ));
  }

  // frontend/node_modules/psihointegritet-ds/src/panel/progress-bar.tsx
  init_define_import_meta_env();
  function ProgressBar({
    value,
    tone = "light",
    className
  }) {
    const pct = Math.max(0, Math.min(100, value));
    return /* @__PURE__ */ React.createElement(
      "span",
      {
        className: cn(
          "block h-2 overflow-hidden rounded-full",
          tone === "dark" ? "bg-canvas/14" : "bg-coffee/10",
          className
        )
      },
      /* @__PURE__ */ React.createElement(
        "span",
        {
          className: cn(
            "block h-full rounded-full",
            tone === "dark" ? "bg-meadow" : "bg-sage"
          ),
          style: { width: `${pct}%` }
        }
      )
    );
  }

  // frontend/node_modules/psihointegritet-ds/src/panel/stat-card.tsx
  init_define_import_meta_env();
  function StatCard({ value, label, dot, className }) {
    return /* @__PURE__ */ React.createElement(
      "div",
      {
        className: cn(
          "border-line rounded-stat bg-surface border px-4 py-[18px]",
          className
        )
      },
      /* @__PURE__ */ React.createElement("div", { className: "flex items-baseline gap-2" }, /* @__PURE__ */ React.createElement("span", { className: "text-coffee font-serif text-[30px] leading-none" }, value), dot ? /* @__PURE__ */ React.createElement(
        "span",
        {
          "aria-hidden": true,
          className: cn(
            "h-2 w-2 rounded-full",
            dot === "meadow" && "bg-meadow",
            dot === "warm" && "bg-warm",
            dot === "danger" && "bg-danger"
          )
        }
      ) : null),
      /* @__PURE__ */ React.createElement("div", { className: "text-ink-55 mt-2 text-[12.5px] leading-[1.35] font-medium" }, label)
    );
  }

  // frontend/node_modules/psihointegritet-ds/src/panel/status-badge.tsx
  init_define_import_meta_env();

  // frontend/node_modules/class-variance-authority/dist/index.mjs
  init_define_import_meta_env();
  var falsyToString = (value) => typeof value === "boolean" ? `${value}` : value === 0 ? "0" : value;
  var cx = clsx;
  var cva = (base, config) => (props) => {
    var _config_compoundVariants;
    if ((config === null || config === void 0 ? void 0 : config.variants) == null) return cx(base, props === null || props === void 0 ? void 0 : props.class, props === null || props === void 0 ? void 0 : props.className);
    const { variants, defaultVariants } = config;
    const getVariantClassNames = Object.keys(variants).map((variant) => {
      const variantProp = props === null || props === void 0 ? void 0 : props[variant];
      const defaultVariantProp = defaultVariants === null || defaultVariants === void 0 ? void 0 : defaultVariants[variant];
      if (variantProp === null) return null;
      const variantKey = falsyToString(variantProp) || falsyToString(defaultVariantProp);
      return variants[variant][variantKey];
    });
    const propsWithoutUndefined = props && Object.entries(props).reduce((acc, param) => {
      let [key, value] = param;
      if (value === void 0) {
        return acc;
      }
      acc[key] = value;
      return acc;
    }, {});
    const getCompoundVariantClassNames = config === null || config === void 0 ? void 0 : (_config_compoundVariants = config.compoundVariants) === null || _config_compoundVariants === void 0 ? void 0 : _config_compoundVariants.reduce((acc, param) => {
      let { class: cvClass, className: cvClassName, ...compoundVariantOptions } = param;
      return Object.entries(compoundVariantOptions).every((param2) => {
        let [key, value] = param2;
        return Array.isArray(value) ? value.includes({
          ...defaultVariants,
          ...propsWithoutUndefined
        }[key]) : {
          ...defaultVariants,
          ...propsWithoutUndefined
        }[key] === value;
      }) ? [
        ...acc,
        cvClass,
        cvClassName
      ] : acc;
    }, []);
    return cx(base, getVariantClassNames, getCompoundVariantClassNames, props === null || props === void 0 ? void 0 : props.class, props === null || props === void 0 ? void 0 : props.className);
  };

  // frontend/node_modules/psihointegritet-ds/src/panel/status-badge.tsx
  var statusBadgeVariants = cva(
    "inline-flex items-center gap-1.5 rounded-full px-2.5 py-1 text-xs font-semibold",
    {
      variants: {
        tone: {
          ok: "bg-badge-ok-bg text-badge-ok",
          wait: "bg-badge-wait-bg text-badge-wait",
          soft: "bg-badge-soft-bg text-badge-soft",
          amber: "bg-badge-amber-bg text-badge-amber",
          neutral: "bg-badge-neutral-bg text-badge-neutral",
          danger: "bg-badge-danger-bg text-badge-danger",
          dark: "bg-badge-dark-bg text-badge-dark"
        }
      },
      defaultVariants: {
        tone: "neutral"
      }
    }
  );
  function StatusBadge({ children, tone, className }) {
    return /* @__PURE__ */ React.createElement("span", { className: cn(statusBadgeVariants({ tone }), className) }, /* @__PURE__ */ React.createElement(
      "span",
      {
        "aria-hidden": true,
        className: "h-1.5 w-1.5 shrink-0 rounded-full bg-current"
      }
    ), children);
  }

  // frontend/node_modules/psihointegritet-ds/src/panel/tab-pills.tsx
  init_define_import_meta_env();
  function TabPills({
    tabs,
    activeId,
    onChange,
    className
  }) {
    return /* @__PURE__ */ React.createElement(
      "div",
      {
        role: "tablist",
        className: cn("flex flex-wrap items-center gap-2", className)
      },
      tabs.map(
        (tab) => tab.disabled ? /* @__PURE__ */ React.createElement(
          "span",
          {
            key: tab.id,
            className: "border-line-strong text-ink-45 rounded-full border border-dashed px-3.5 py-1.5 text-[13px] font-medium"
          },
          tab.label
        ) : /* @__PURE__ */ React.createElement(
          "button",
          {
            key: tab.id,
            type: "button",
            role: "tab",
            "aria-selected": tab.id === activeId,
            onClick: () => onChange(tab.id),
            className: cn(
              "cursor-pointer rounded-full border px-3.5 py-1.5 text-[13px] font-semibold transition-colors",
              tab.id === activeId ? "border-coffee bg-coffee text-panel-canvas" : "border-line-strong text-ink-70 hover:border-coffee/40 bg-transparent"
            )
          },
          tab.label
        )
      )
    );
  }

  // frontend/node_modules/psihointegritet-ds/src/panel/toggle.tsx
  init_define_import_meta_env();
  function Toggle({
    checked,
    onChange,
    label,
    disabled,
    className
  }) {
    return /* @__PURE__ */ React.createElement(
      "button",
      {
        type: "button",
        role: "switch",
        "aria-checked": checked,
        "aria-label": label,
        disabled,
        onClick: () => onChange(!checked),
        className: cn(
          "relative h-[23px] w-10 shrink-0 cursor-pointer rounded-full border-0 transition-colors duration-200",
          checked ? "bg-sage" : "bg-coffee/18",
          disabled && "cursor-not-allowed opacity-50",
          className
        )
      },
      /* @__PURE__ */ React.createElement(
        "span",
        {
          "aria-hidden": true,
          className: cn(
            "bg-surface absolute top-[3px] left-[3px] h-[17px] w-[17px] rounded-full shadow-sm transition-transform duration-200",
            checked && "translate-x-[17px]"
          )
        }
      )
    );
  }

  // frontend/node_modules/psihointegritet-ds/src/shared/back-to-site-button.tsx
  init_define_import_meta_env();
  function GlobeIcon() {
    return /* @__PURE__ */ React.createElement(
      "svg",
      {
        "aria-hidden": true,
        width: "17",
        height: "17",
        viewBox: "0 0 24 24",
        fill: "none",
        stroke: "currentColor",
        strokeWidth: "1.8",
        strokeLinecap: "round",
        strokeLinejoin: "round"
      },
      /* @__PURE__ */ React.createElement("circle", { cx: "12", cy: "12", r: "9" }),
      /* @__PURE__ */ React.createElement("path", { d: "M3 12h18" }),
      /* @__PURE__ */ React.createElement("path", { d: "M12 3a13 13 0 0 1 0 18 13 13 0 0 1 0-18" })
    );
  }
  function BackToSiteButton({ className }) {
    return /* @__PURE__ */ React.createElement(
      Link,
      {
        href: "/",
        target: "_blank",
        rel: "noopener noreferrer",
        title: "Glavni sajt",
        "aria-label": "Glavni sajt",
        className: cn(
          "inline-flex h-10 w-10 shrink-0 items-center justify-center gap-2 rounded-full border text-[13px] font-semibold no-underline transition-colors lg:w-auto lg:px-3.5",
          className
        )
      },
      /* @__PURE__ */ React.createElement(GlobeIcon, null),
      /* @__PURE__ */ React.createElement("span", { className: "hidden lg:inline" }, "Glavni sajt")
    );
  }
  function BackToSiteMenuItem() {
    return /* @__PURE__ */ React.createElement(gt, null, /* @__PURE__ */ React.createElement(
      Link,
      {
        href: "/",
        target: "_blank",
        rel: "noopener noreferrer",
        className: "text-coffee data-[focus]:bg-coffee/8 flex w-full items-center gap-2.5 rounded-xl px-3.5 py-2.5 text-[13.5px] font-semibold no-underline transition-colors"
      },
      /* @__PURE__ */ React.createElement(GlobeIcon, null),
      "Glavni sajt"
    ));
  }

  // frontend/node_modules/psihointegritet-ds/src/shared/json-ld.tsx
  init_define_import_meta_env();
  function JsonLd({ data }) {
    if (data.length === 0) return null;
    const json = JSON.stringify(data).replace(/</g, "\\u003c");
    return /* @__PURE__ */ React.createElement(
      "script",
      {
        type: "application/ld+json",
        dangerouslySetInnerHTML: { __html: json }
      }
    );
  }

  // frontend/node_modules/psihointegritet-ds/src/shared/logout-avatar-menu.tsx
  init_define_import_meta_env();

  // frontend/node_modules/@heroicons/react/24/outline/esm/index.js
  init_define_import_meta_env();

  // frontend/node_modules/@heroicons/react/24/outline/esm/PowerIcon.js
  init_define_import_meta_env();
  var React6 = __toESM(require_react_shim(), 1);
  function PowerIcon({
    title,
    titleId,
    ...props
  }, svgRef) {
    return /* @__PURE__ */ React6.createElement("svg", Object.assign({
      xmlns: "http://www.w3.org/2000/svg",
      fill: "none",
      viewBox: "0 0 24 24",
      strokeWidth: 1.5,
      stroke: "currentColor",
      "aria-hidden": "true",
      "data-slot": "icon",
      ref: svgRef,
      "aria-labelledby": titleId
    }, props), title ? /* @__PURE__ */ React6.createElement("title", {
      id: titleId
    }, title) : null, /* @__PURE__ */ React6.createElement("path", {
      strokeLinecap: "round",
      strokeLinejoin: "round",
      d: "M5.636 5.636a9 9 0 1 0 12.728 0M12 3v9"
    }));
  }
  var ForwardRef = /* @__PURE__ */ React6.forwardRef(PowerIcon);
  var PowerIcon_default = ForwardRef;

  // frontend/node_modules/psihointegritet-ds/src/shared/logout-avatar-menu.tsx
  function LogoutAvatarMenu({
    initials,
    label,
    triggerClassName,
    children
  }) {
    const { signOut } = useClerk();
    const { user } = useUser();
    const avatarUrl = user?.hasImage ? user.imageUrl : null;
    return /* @__PURE__ */ React.createElement(lo, { as: "div", className: "relative" }, /* @__PURE__ */ React.createElement(
      ft,
      {
        "aria-label": label,
        title: label,
        className: cn(
          triggerClassName,
          avatarUrl && "relative overflow-hidden p-0"
        )
      },
      avatarUrl ? /* @__PURE__ */ React.createElement(
        Image,
        {
          src: avatarUrl,
          alt: "",
          fill: true,
          sizes: "40px",
          className: "rounded-full object-cover"
        }
      ) : initials
    ), /* @__PURE__ */ React.createElement(
      yt,
      {
        transition: true,
        className: "ease-soft border-coffee/10 bg-surface absolute top-[calc(100%+10px)] right-0 z-[70] w-44 origin-top-right rounded-2xl border p-1.5 shadow-xl outline-none transition duration-200 data-[closed]:-translate-y-2 data-[closed]:scale-95 data-[closed]:opacity-0"
      },
      children ? /* @__PURE__ */ React.createElement(React.Fragment, null, children, /* @__PURE__ */ React.createElement("div", { className: "border-coffee/10 mx-2 my-1.5 border-t" })) : null,
      /* @__PURE__ */ React.createElement(gt, null, /* @__PURE__ */ React.createElement(
        "button",
        {
          type: "button",
          onClick: () => signOut({ redirectUrl: "/" }),
          className: "text-danger data-[focus]:bg-danger/10 flex w-full cursor-pointer items-center gap-2.5 rounded-xl px-3.5 py-2.5 text-[13.5px] font-bold transition-colors"
        },
        /* @__PURE__ */ React.createElement(PowerIcon_default, { className: "size-4 shrink-0" }),
        "Odjavi se"
      ))
    ));
  }

  // frontend/node_modules/psihointegritet-ds/src/shared/page-hero.tsx
  init_define_import_meta_env();
  var toneClasses = {
    /** Matches the homepage hero card — the safe default for inner pages. */
    surface: "bg-surface",
    forest: "bg-forest",
    /** Pair with text-coffee + <Eyebrow tone="coffee"> — matches workshop.tsx's bg-warm section. */
    warm: "bg-warm/20",
    meadow: "bg-meadow/28"
  };
  function PageHero({
    children,
    tone = "surface",
    id: id3,
    className
  }) {
    return /* @__PURE__ */ React.createElement("section", { id: id3, className: cn(id3 ? "scroll-mt-24" : void 0, "pt-6") }, /* @__PURE__ */ React.createElement("div", { className: "mx-auto max-w-[1536px] px-5 md:px-8" }, /* @__PURE__ */ React.createElement(
      "div",
      {
        className: cn(
          "rounded-3xl px-6 pt-[96px] pb-10 md:rounded-[32px] md:px-16 md:pt-[152px] md:pb-14",
          toneClasses[tone],
          className
        )
      },
      children
    )));
  }

  // frontend/node_modules/psihointegritet-ds/src/shared/reason-card.tsx
  init_define_import_meta_env();
  function ReasonCard({ reason }) {
    return /* @__PURE__ */ React.createElement(
      "a",
      {
        href: reason.href,
        className: "bg-surface border-coffee/6 hover:shadow-card-hover flex flex-col gap-10 rounded-[22px] border p-[30px] pb-7 no-underline transition-all duration-[250ms] hover:-translate-y-1"
      },
      /* @__PURE__ */ React.createElement("div", { className: "flex items-center justify-between" }, /* @__PURE__ */ React.createElement("span", { className: "text-sage font-serif text-[15px] italic" }, reason.number), /* @__PURE__ */ React.createElement(
        "span",
        {
          "aria-hidden": true,
          className: "border-coffee/14 text-forest flex h-[34px] w-[34px] items-center justify-center rounded-full border text-[15px]"
        },
        "\u2192"
      )),
      /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { className: "text-forest mb-2 font-serif text-[26px] leading-[1.15] font-normal" }, reason.title), /* @__PURE__ */ React.createElement("div", { className: "text-coffee/65 text-[14.5px] leading-[1.55]" }, reason.description))
    );
  }

  // frontend/node_modules/psihointegritet-ds/src/shared/resource-card.tsx
  init_define_import_meta_env();

  // frontend/node_modules/psihointegritet-ds/src/ui/arrow-link.tsx
  init_define_import_meta_env();
  var toneClasses2 = {
    /** Plain forest link with sage hover. */
    plain: "text-forest hover:text-sage",
    /** Underlined with a bottom border that recolors on hover. */
    underline: "border-b-[1.5px] border-coffee/30 pb-1 text-forest hover:border-sage hover:text-sage",
    /** Underlined in full forest (dark border), for emphasized links. */
    underlineStrong: "border-b-[1.5px] border-forest pb-1 text-forest hover:border-sage hover:text-sage"
  };
  function ArrowLink({
    href,
    children,
    tone = "plain",
    circled = false,
    className
  }) {
    const linkClassName = cn(
      "inline-flex items-center gap-2.5 text-[15px] font-semibold no-underline transition-colors duration-200",
      toneClasses2[tone],
      className
    );
    const content = /* @__PURE__ */ React.createElement(React.Fragment, null, /* @__PURE__ */ React.createElement("span", null, children), circled ? /* @__PURE__ */ React.createElement(
      "span",
      {
        "aria-hidden": true,
        className: "bg-meadow/40 inline-flex h-[30px] w-[30px] items-center justify-center rounded-full text-sm"
      },
      "\u2192"
    ) : /* @__PURE__ */ React.createElement("span", { "aria-hidden": true }, "\u2192"));
    if (href.startsWith("#")) {
      return /* @__PURE__ */ React.createElement("a", { href, className: linkClassName }, content);
    }
    return /* @__PURE__ */ React.createElement(Link, { href, className: linkClassName }, content);
  }

  // frontend/node_modules/psihointegritet-ds/src/ui/chip.tsx
  init_define_import_meta_env();
  var chipVariants = cva("inline-flex items-center rounded-full", {
    variants: {
      variant: {
        /** Small tag pill, e.g. service meta and therapist areas. */
        tag: "bg-meadow/22 px-[13px] py-1.5 text-[13px] font-medium text-coffee",
        /** Tag pill with a subtle sage border (therapist areas). */
        tagOutlined: "border border-sage/25 bg-meadow/22 px-[13px] py-1.5 text-[13px] font-medium text-coffee",
        /** Uppercase label, sage on meadow wash (therapist badge). */
        label: "bg-meadow/28 px-3.5 py-[7px] text-xs font-semibold tracking-[0.1em] text-sage uppercase",
        /** Uppercase label on warm wash (resource category). */
        labelWarm: "bg-warm/28 px-3.5 py-[7px] text-xs font-semibold tracking-[0.1em] text-coffee uppercase",
        /** Uppercase label, solid meadow (featured service). */
        labelSolid: "bg-meadow px-3.5 py-[7px] text-xs font-semibold tracking-[0.1em] text-forest uppercase"
      }
    },
    defaultVariants: {
      variant: "tag"
    }
  });
  function Chip({ children, variant, className }) {
    return /* @__PURE__ */ React.createElement("span", { className: cn(chipVariants({ variant }), className) }, children);
  }

  // frontend/node_modules/psihointegritet-ds/src/shared/resource-card.tsx
  function ResourceCard({ article }) {
    return /* @__PURE__ */ React.createElement("article", { className: "bg-surface border-coffee/6 hover:shadow-card-hover flex flex-col gap-5 rounded-3xl border px-8 pt-[34px] pb-[30px] transition-all duration-[250ms] hover:-translate-y-1" }, /* @__PURE__ */ React.createElement(Chip, { variant: "labelWarm", className: "self-start" }, article.category), /* @__PURE__ */ React.createElement("h3", { className: "text-forest font-serif text-[26px] leading-[1.2] font-normal text-pretty" }, article.title), /* @__PURE__ */ React.createElement("p", { className: "text-coffee/68 grow text-[14.5px] leading-[1.62]" }, article.description), /* @__PURE__ */ React.createElement(ArrowLink, { href: "#resursi" }, "Pro\u010Ditaj tekst"));
  }

  // frontend/node_modules/psihointegritet-ds/src/shared/therapist-card.tsx
  init_define_import_meta_env();

  // frontend/node_modules/psihointegritet-ds/src/ui/monogram-avatar.tsx
  init_define_import_meta_env();
  var sizeClasses = {
    md: "h-[72px] w-[72px] border-[3px] text-[22px]",
    sm: "h-[52px] w-[52px] border-2 text-base"
  };
  function MonogramAvatar({
    initials,
    name,
    imageSrc,
    size: size4 = "md",
    className
  }) {
    return /* @__PURE__ */ React.createElement(
      "span",
      {
        role: "img",
        "aria-label": name,
        className: cn(
          "border-meadow/60 bg-meadow/30 text-forest relative inline-flex shrink-0 items-center justify-center overflow-hidden rounded-full font-serif",
          sizeClasses[size4],
          className
        )
      },
      imageSrc ? /* @__PURE__ */ React.createElement(
        Image,
        {
          src: imageSrc,
          alt: name,
          fill: true,
          sizes: size4 === "md" ? "72px" : "52px",
          className: "object-cover"
        }
      ) : initials
    );
  }

  // frontend/node_modules/psihointegritet-ds/src/shared/therapist-card.tsx
  function TherapistCard({ therapist }) {
    return /* @__PURE__ */ React.createElement("article", { className: "bg-surface border-coffee/6 hover:shadow-card-hover-lg flex flex-col gap-[22px] rounded-3xl border px-8 pt-[34px] pb-[30px] transition-all duration-[250ms] hover:-translate-y-1" }, /* @__PURE__ */ React.createElement("div", { className: "flex items-start justify-between" }, /* @__PURE__ */ React.createElement(
      MonogramAvatar,
      {
        initials: therapist.initials,
        name: therapist.name,
        imageSrc: therapist.image
      }
    ), /* @__PURE__ */ React.createElement(Chip, { variant: "label" }, therapist.badge)), /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("h3", { className: "text-forest mb-1.5 font-serif text-[29px] leading-[1.1] font-normal" }, therapist.name), /* @__PURE__ */ React.createElement("div", { className: "text-coffee/60 text-[13.5px] font-semibold tracking-[0.04em]" }, therapist.title)), /* @__PURE__ */ React.createElement("p", { className: "text-coffee/82 font-serif text-[18.5px] leading-[1.45] italic" }, "\u201E", therapist.quote, "\u201C"), /* @__PURE__ */ React.createElement("div", { className: "border-coffee/8 border-t pt-[18px]" }, /* @__PURE__ */ React.createElement("div", { className: "text-sage mb-2 text-[11.5px] font-semibold tracking-[0.14em] uppercase" }, "Formati rada"), /* @__PURE__ */ React.createElement("div", { className: "text-coffee/75 text-[14.5px] leading-[1.6]" }, therapist.formats)), /* @__PURE__ */ React.createElement("div", null, /* @__PURE__ */ React.createElement("div", { className: "text-sage mb-2.5 text-[11.5px] font-semibold tracking-[0.14em] uppercase" }, "Glavne oblasti"), /* @__PURE__ */ React.createElement("div", { className: "flex flex-wrap gap-2" }, therapist.areas.map((area) => /* @__PURE__ */ React.createElement(Chip, { key: area, variant: "tagOutlined" }, area)))), /* @__PURE__ */ React.createElement("div", { className: "mt-auto pt-2" }, /* @__PURE__ */ React.createElement(ArrowLink, { href: `/tim/${therapist.slug}`, circled: true }, "Upoznaj terapeuta")));
  }

  // frontend/node_modules/psihointegritet-ds/src/shared/therapist-row.tsx
  init_define_import_meta_env();
  function TherapistRow({
    therapist,
    flipped,
    priority = false
  }) {
    return /* @__PURE__ */ React.createElement("div", { className: "border-coffee/10 grid grid-cols-1 items-center gap-8 border-t py-16 md:grid-cols-2 md:gap-[72px]" }, /* @__PURE__ */ React.createElement("div", { className: cn(flipped && "md:order-2") }, /* @__PURE__ */ React.createElement("div", { className: "bg-meadow/30 relative aspect-4/5 overflow-hidden rounded-[28px]" }, /* @__PURE__ */ React.createElement(
      Image,
      {
        src: therapist.image,
        alt: therapist.name,
        fill: true,
        priority,
        sizes: "(min-width: 768px) 50vw, 100vw",
        className: "object-cover"
      }
    ))), /* @__PURE__ */ React.createElement("div", { className: cn(flipped && "md:order-1") }, /* @__PURE__ */ React.createElement(Chip, { variant: "label", className: "mb-[22px]" }, therapist.badge), /* @__PURE__ */ React.createElement("h3", { className: "text-forest mb-2 font-serif text-[32px] leading-[1.1] font-normal md:text-[40px]" }, therapist.name), /* @__PURE__ */ React.createElement("div", { className: "text-coffee/60 mb-5 text-sm font-semibold" }, therapist.title), /* @__PURE__ */ React.createElement("p", { className: "text-coffee/82 mb-5 font-serif text-[19px] leading-[1.5] italic" }, "\u201E", therapist.quote, "\u201C"), /* @__PURE__ */ React.createElement("p", { className: "text-coffee/72 mb-6 max-w-[520px] text-[15.5px] leading-[1.65]" }, therapist.cardExcerpt), /* @__PURE__ */ React.createElement("div", { className: "mb-7 flex flex-wrap gap-2" }, therapist.areas.map((area) => /* @__PURE__ */ React.createElement(Chip, { key: area, variant: "tagOutlined" }, area))), /* @__PURE__ */ React.createElement(
      Link,
      {
        href: `/tim/${therapist.slug}`,
        className: "bg-forest text-canvas hover:bg-forest-hover inline-flex items-center gap-2.5 rounded-full px-[26px] py-3.5 text-[15px] font-semibold transition-colors"
      },
      "Upoznaj ",
      therapist.nameAccusative,
      " ",
      /* @__PURE__ */ React.createElement("span", { "aria-hidden": "true" }, "\u2192")
    )));
  }

  // frontend/node_modules/psihointegritet-ds/src/ui/accordion.tsx
  init_define_import_meta_env();
  var import_react85 = __toESM(require_react_shim());
  function Accordion({ items, defaultOpenId = null }) {
    const [openId, setOpenId] = (0, import_react85.useState)(defaultOpenId);
    return /* @__PURE__ */ React.createElement("div", null, items.map((item) => {
      const isOpen = item.id === openId;
      const panelId = `accordion-panel-${item.id}`;
      const buttonId = `accordion-button-${item.id}`;
      return /* @__PURE__ */ React.createElement("div", { key: item.id, className: "border-coffee/12 border-t" }, /* @__PURE__ */ React.createElement(
        "button",
        {
          type: "button",
          id: buttonId,
          "aria-expanded": isOpen,
          "aria-controls": panelId,
          onClick: () => setOpenId(isOpen ? null : item.id),
          className: "flex w-full cursor-pointer items-center justify-between gap-8 px-1 py-7 text-left"
        },
        /* @__PURE__ */ React.createElement("span", { className: "text-forest font-serif text-[19px] leading-[1.25] font-normal md:text-2xl" }, item.question),
        /* @__PURE__ */ React.createElement(
          "span",
          {
            "aria-hidden": true,
            className: `border-coffee/18 text-forest flex h-[38px] w-[38px] shrink-0 items-center justify-center rounded-full border font-sans text-[19px] transition-all duration-300 ${isOpen ? "bg-meadow/40 rotate-45" : "rotate-0 bg-transparent"}`
          },
          "+"
        )
      ), isOpen ? /* @__PURE__ */ React.createElement(
        "div",
        {
          id: panelId,
          role: "region",
          "aria-labelledby": buttonId,
          className: "animate-fade-in max-w-[560px] px-1 pb-[30px]"
        },
        /* @__PURE__ */ React.createElement("p", { className: "text-coffee/72 text-base leading-[1.7]" }, item.answer)
      ) : null);
    }), /* @__PURE__ */ React.createElement("div", { className: "border-coffee/12 border-t" }));
  }

  // frontend/node_modules/psihointegritet-ds/src/ui/animated-cta-link.tsx
  init_define_import_meta_env();

  // frontend/node_modules/psihointegritet-ds/src/ui/animated-cta.tsx
  init_define_import_meta_env();
  var sizeClasses2 = {
    md: {
      root: "gap-3 py-2 pr-6 pl-2 text-[14.5px]",
      circle: "h-9 w-9",
      icon: 16
    },
    sm: {
      root: "gap-2.5 py-1.5 pr-[18px] pl-1.5 text-[13.5px]",
      circle: "h-[30px] w-[30px]",
      icon: 14
    }
  };
  function animatedCtaClassName(size4, className) {
    return cn(
      "group bg-forest hover:bg-forest-lift text-canvas relative inline-flex items-center overflow-hidden rounded-full font-semibold whitespace-nowrap no-underline transition-colors duration-[250ms]",
      sizeClasses2[size4].root,
      className
    );
  }
  function AnimatedCtaContent({
    label,
    size: size4
  }) {
    const s15 = sizeClasses2[size4];
    return /* @__PURE__ */ React.createElement(React.Fragment, null, /* @__PURE__ */ React.createElement(
      "span",
      {
        "aria-hidden": true,
        className: "pointer-events-none absolute top-0 bottom-0 left-0 w-[55%] -translate-x-[150%] bg-[linear-gradient(100deg,transparent_15%,rgba(250,248,243,0.13)_50%,transparent_85%)] transition-transform duration-[600ms] group-hover:translate-x-[300%]"
      }
    ), /* @__PURE__ */ React.createElement(
      "span",
      {
        "aria-hidden": true,
        className: cn(
          "bg-forest-soft relative inline-flex -rotate-45 items-center justify-center rounded-full transition-transform duration-300 group-hover:rotate-0",
          s15.circle
        )
      },
      /* @__PURE__ */ React.createElement(
        "svg",
        {
          width: s15.icon,
          height: s15.icon,
          viewBox: "0 0 24 24",
          fill: "none",
          stroke: "var(--color-canvas)",
          strokeWidth: "2",
          strokeLinecap: "round",
          strokeLinejoin: "round"
        },
        /* @__PURE__ */ React.createElement("path", { d: "M5 12h14" }),
        /* @__PURE__ */ React.createElement("path", { d: "M13 6l6 6-6 6" })
      )
    ), /* @__PURE__ */ React.createElement("span", { className: "relative" }, label));
  }

  // frontend/node_modules/psihointegritet-ds/src/ui/animated-cta-link.tsx
  function AnimatedCtaLink({
    href,
    label,
    size: size4 = "md",
    className
  }) {
    const linkClassName = animatedCtaClassName(size4, className);
    const content = /* @__PURE__ */ React.createElement(AnimatedCtaContent, { label, size: size4 });
    if (href.startsWith("#")) {
      return /* @__PURE__ */ React.createElement("a", { href, className: linkClassName }, content);
    }
    return /* @__PURE__ */ React.createElement(Link, { href, className: linkClassName }, content);
  }

  // frontend/node_modules/psihointegritet-ds/src/ui/button-link.tsx
  init_define_import_meta_env();
  var buttonLinkVariants = cva(
    "inline-flex items-center justify-center rounded-full text-center font-semibold no-underline transition-colors duration-200",
    {
      variants: {
        variant: {
          primary: "bg-forest text-canvas hover:bg-forest-hover",
          meadow: "bg-meadow text-forest hover:bg-meadow-hover",
          warm: "bg-warm text-coffee hover:bg-warm-hover",
          coffee: "bg-coffee text-canvas hover:bg-coffee-hover",
          light: "bg-canvas text-forest hover:bg-meadow",
          outline: "border-[1.5px] border-coffee/25 text-coffee hover:border-sage hover:bg-meadow/15"
        },
        size: {
          md: "px-7 py-[15px] text-[15px]",
          lg: "px-8 py-4 text-[15.5px]"
        }
      },
      compoundVariants: [
        // Border replaces 1.5px of padding so outline height matches solid.
        { variant: "outline", size: "md", className: "py-[13.5px]" },
        { variant: "outline", size: "lg", className: "py-[14.5px]" }
      ],
      defaultVariants: {
        variant: "primary",
        size: "md"
      }
    }
  );
  function ButtonLink({
    href,
    children,
    variant,
    size: size4,
    className,
    ariaLabel
  }) {
    const linkClassName = cn(buttonLinkVariants({ variant, size: size4 }), className);
    if (href.startsWith("#")) {
      return /* @__PURE__ */ React.createElement("a", { href, "aria-label": ariaLabel, className: linkClassName }, children);
    }
    return /* @__PURE__ */ React.createElement(Link, { href, "aria-label": ariaLabel, className: linkClassName }, children);
  }

  // frontend/node_modules/psihointegritet-ds/src/ui/eyebrow.tsx
  init_define_import_meta_env();
  var toneClasses3 = {
    sage: "text-sage",
    meadow: "text-meadow",
    coffee: "text-coffee/60"
  };
  function Eyebrow({ children, tone = "sage", className }) {
    return /* @__PURE__ */ React.createElement(
      "div",
      {
        className: cn(
          "text-[12.5px] font-semibold tracking-[0.16em] uppercase",
          toneClasses3[tone],
          className
        )
      },
      children
    );
  }

  // frontend/node_modules/psihointegritet-ds/src/ui/section-heading.tsx
  init_define_import_meta_env();
  function SectionHeading({
    eyebrow,
    title,
    description,
    className
  }) {
    return /* @__PURE__ */ React.createElement("div", { className: cn("max-w-[640px]", className) }, /* @__PURE__ */ React.createElement(Eyebrow, { className: "mb-4" }, eyebrow), /* @__PURE__ */ React.createElement("h2", { className: "text-forest mb-[18px] font-serif text-[clamp(30px,8.5vw,38px)] leading-[1.06] font-normal tracking-[-0.015em] text-pretty md:text-[52px]" }, title), description ? /* @__PURE__ */ React.createElement("p", { className: "text-coffee/72 text-[16.5px] leading-[1.65]" }, description) : null);
  }
  return __toCommonJS(pkg_entry_exports);
})();
/*! Bundled license information:

use-sync-external-store/cjs/use-sync-external-store-with-selector.development.js:
  (**
   * @license React
   * use-sync-external-store-with-selector.development.js
   *
   * Copyright (c) Meta Platforms, Inc. and affiliates.
   *
   * This source code is licensed under the MIT license found in the
   * LICENSE file in the root directory of this source tree.
   *)
*/
window.PsihointegritetUI=PsihointegritetUI.__dsMainNs?Object.assign({},PsihointegritetUI,PsihointegritetUI.__dsMainNs,{__dsMainNs:undefined}):PsihointegritetUI;

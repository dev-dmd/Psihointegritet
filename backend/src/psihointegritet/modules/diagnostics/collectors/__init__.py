"""Diagnostic Engine — collectors package."""

from psihointegritet.modules.diagnostics.collectors import (  # noqa: F401
    booking,  # pyright: ignore[reportUnusedImport] — registry import target
)
